# CodeNTalk — правила работы над проектом

Этот файл читает каждая сессия и каждый агент. Он существует, чтобы работа не
начиналась с нуля и одни и те же ошибки не повторялись.

## Что это за проект

Платформа обучения языкам и программированию. Владелец — действующий
преподаватель английского из Казахстана. Общение с ним **только на русском**.

**Работа идёт в `main`, других веток нет.** По решению владельца вторая версия
перенесена туда перемоткой вперёд: платформа с регистрацией, подпиской и базой
данных теперь основная ветка. Прежняя ветка `v2` после переноса удалена — она
указывала туда же и только путала.

**Первая версия удалена целиком.** Её содержание было написано без опоры на
учебники, поэтому владелец решил его выбросить, а страницы снести: витрина
показывала ученику старую программу с юнитами и квестами, пока рядом строилось
новое. В дереве от неё не осталось ничего — ни `content/`, ни `app/course`, ни
`app/track`, ни движка квестов. Посмотреть можно только историей, начиная с
коммита `3f3a78e`. Со старых адресов стоят перенаправления в каталог.

Читай перед работой: [docs/TZ-platform-v2.md](docs/TZ-platform-v2.md) —
техническое задание, [docs/sources-learning-materials.md](docs/sources-learning-materials.md) —
источники учебных материалов, [docs/zadachi-vladeltsa.md](docs/zadachi-vladeltsa.md) —
что ждёт решения владельца, [docs/instrumenty.md](docs/instrumenty.md) — какие
инструменты смотрели, что взяли и почему отказались от остального.

## Язык текстов — самое частое замечание

Все тексты для людей пишутся на **литературном русском без калек с английского**.
Владелец на этом настаивает и проверяет.

Запрещено: «стрики», «дрип-контент», «онбординг», «гейт», «эндпоинт», «дедлайн»,
«фича», «юзер», «чекнуть», «задеплоить». Пиши так, как пишут на русских
обучающих платформах: «счётчик дней», «открытие материалов по расписанию»,
«первые шаги», «ограничение доступа», «метод интерфейса», «срок», «возможность»,
«ученик», «проверить», «выложить».

Это касается интерфейса, сообщений об ошибках, комментариев в коде, названий
коммитов и документов.

**Но запрет — на кальки, а не на терминологию предмета.** Решение владельца от
16 августа, после того как я понял правило слишком широко.

- **Калька** — английское слово вместо существующего русского: «юзер» вместо
  «ученик», «задеплоить» вместо «выложить». Запрещено.
- **Имя вещи** — название, которым эту вещь зовут везде и у которого русского
  обихода нет: **Present Simple, Past Simple, Present Continuous**; ступени
  **Beginner, Elementary, Pre-Intermediate, Intermediate**. Так учит весь мир,
  и переводить их («настоящее простое время») — значит выпустить ученика,
  который не поймёт ни учебника, ни других курсов.

Отсюда правило для уроков: **время называется один раз, при первой встрече,
английским именем и без объяснений, зачем оно нужно** («Это время называется
Present Simple.»). Рассказ о том, что название пригодится в учебниках, — это
разговор о нашей кухне, его быть не должно.

**«При первой встрече» значит внутри ступени, а не внутри курса вообще.** Между
ступенями нет ссылок на память: человек мог прийти сразу на Elementary. Имя,
названное на Beginner, для него не звучало.

**И называется оно ученику, а не мне.** Владелец нашёл 22 августа 2026, что вся
ступень Elementary — одиннадцать модулей про прошедшее и будущее — не назвала ни
одного времени, хотя шапки восьми модулей из двенадцати честно писали
«грамматика: Past Simple». Имя стояло в комментарии, и место выглядело закрытым:
я писал модуль, видел в собственной шапке нужное слово и считал правило
исполненным. Ученик в шапку не заходит.

Проверяющие поймать этого не могли, и это была дыра в их брифах. Редактор знал
про имена времён обратное — что их **не переводят**; текст, где имени нет вовсе,
переводом не является и проходил чисто. Методист сверяет содержание с
источниками, а требование наше: English Grammar Profile нигде не пишет «назовите
это ученику Past Simple». Теперь механическую половину ловит `check:content` —
сверяет строку `грамматика:` из шапки модуля с видимым текстом курса, — а
редактору дописан пункт: имя перевели — ошибка, имени нет вовсе — тоже ошибка.

**Имя даётся только там, где оно есть у источника.** Руководство A2 Key, с. 52,
перечень Tenses, называет `Past simple`, `Past continuous`, `Present simple`,
`Present continuous` — это имена. А `be going to` и `will` стоят там описанием
случая: «Future with going to», «Future with will and shall: offers, promises,
predictions, etc.».

Отсюда **модуль 8 ступени Elementary имени не получает**: у оборота `going to`
его нет ни у одного из трёх источников — ни у руководства A2 Key, ни у
руководства Young Learners (с. 55 и 77), ни у English Grammar Profile, где
подкатегория зовётся `future with be going to`. «Future Simple» дал бы ученику
не источник, а я.

**А с `will` вышло иначе, и это поучительно.** Я написал в шапке модуля 9, что
имени нет «по той же причине», — и ошибся: методист открыл English Grammar
Profile и показал подкатегорию `future simple (with will and shall)`, 16 строк.
Имя есть. Но тот же файл держит ещё 13 строк `MODALITY will`, а оба руководства
Cambridge дают `will` формой. Один предмет, два решения у источников — значит
вопрос к владельцу, и 22 августа 2026 он решил: **имя `Future Simple` ученику
даём**, оно самое ходовое в учебниках.

Урок здесь один и он не про `will`: **вывод «источник имени не даёт» проверяется
по каждому источнику отдельно**, а не переносится с соседнего случая по
похожести. Перенос — это то же самое, что «в источнике этого нет», сказанное не
открыв файла.

**Названия ступеней не переводятся и не поясняются переводом рядом.** Владелец
сказал это дважды: второй раз потому, что я всё-таки поставил «Elementary
(элементарная)». Просто `Beginner`, просто `Elementary`.

**Обращение к ученику — «ты».** Решение владельца. Из этого следует правило,
которое легко нарушить: обращение на «ты» делает прошедшее время родовым («ты
прошёл» или «ты прошла»), а пола ученика платформа не знает. Поэтому в текстах
для ученика **избегаем прошедшего времени во втором лице**: не «ты прошёл всё,
что взял», а «все взятые курсы пройдены». Настоящее время и повелительное
наклонение рода не требуют и годятся без оговорок.

## Ссылка на урок — всегда с номером модуля

Просьба владельца от 19 августа. Говоря об уроке, всегда называй **номер модуля
и номер урока внутри него**: «модуль 1, урок 4 — Спросить, как пишется». Одного
названия мало: уроков в курсе больше двухсот, названия похожи, и владелец не
обязан искать по всему курсу то, о чём я говорю.

Это касается и отчётов, и коммитов, и разборов проверяющих.

## Понятность объяснения — требование, а не пожелание

Владелец прошёл полтора модуля и сказал: **«Как будто ты объясняешь то, что
знаешь только ты… Ты отталкиваешься от структуры у тебя в голове, не учитывая
представление ученика»**. Он преподаватель английского и владеет им свободно —
и всё равно не понял. Значит дело не в сложности предмета.

Семь правил и разбор каждого — [docs/format-uroka.md](docs/format-uroka.md),
раздел «Понятность объяснения». Коротко:

1. **Ученику не рассказывают о нашей кухне.** Никаких «источник называет», «на
   этой ступени», «норма, которой держится курс». Обоснование живёт в поле
   `sources`. Ссылка на наш же урок — наоборот, хорошо.
2. **Сначала случай, потом правило.** Правило до примера — требование поверить
   на слово.
3. **Термин объясняется на месте**: гласный, местоимение, безударный, артикль.
   Либо строка с примером здесь же, либо слова нет. И термин **настоящий**, а не
   придуманный нами ярлык: модуль 11 звал прилагательное «признаком», и владелец
   первым же споткнулся — признак предмету присущ, а `hot` и `busy` говорят о
   состоянии. Курс вводит «местоимение», «предлог», «множественное число» — так
   же вводится и всё остальное.

   **И у главной вещи модуля имя одно.** Правило приписано 21 августа 2026:
   модуль 5 ступени Elementary звал одно и то же тремя именами — подпись таблицы
   «о том, каким было», объяснение под ней «о свойстве», урок письма «оценка».
   Ученик читает три слова и ищет три разные вещи. Список `YARLYKI` такого не
   ловит: там ловятся известные ярлыки, а не расхождение внутри модуля. Поэтому
   имя главной вещи **называется в шапке файла отдельной строкой** — тогда
   следующая правка сверяется с ней, а не с памятью.

   **И «понятное своё слово» термином не становится.** Решение владельца от
   2 сентября 2026. Модуль 24 Beginner звал половину сложного предложения то
   «полной мыслью», то «предложением», а работа звала целое «одним
   предложением»: одна строка получала два взаимоисключающих имени. Я свёл всё
   к «полной мысли» — и методист возразил, что это не термин русской
   грамматики, а выдуманный ярлык: у половины есть настоящее имя, **простое
   предложение**. Владелец выбрал настоящий термин.

   Порода та же, что «признак» вместо прилагательного в модуле 11 Elementary, и
   ловится тем же вопросом: **есть ли у этой вещи имя в русской грамматике?**
   Если есть — берём его и объясняем на месте («Каждое из них — простое
   предложение: у него один свой глагол»), а не придумываем своё, «попонятнее».

   **Термин можно укоротить, если полное имя занято другим.** Решение владельца
   от 22 августа 2026: модуль 16 зовёт `very`, `too`, `quite` **наречиями меры**,
   хотя полное русское имя класса — «наречия меры и степени». Слово «степень» в
   курсе занято сравнением (модули 14 и 15), и полное имя увело бы ученика туда.
   Укороченное имя — не ярлык: оно составлено из настоящего термина, а не
   выдумано взамен него.
4. **Задание не требует того, чего в уроке не было.**
5. **Не заканчивать мысль местоимением** — «так пишется только оно» прочли как
   «только it».
6. **Одно предложение — одна мысль**, до двадцати двух слов.
7. **Звук объясняется словом, а потом значком**, а не наоборот.

Первое, третье и шестое ловит `npm run check:content`. Остальное — редактор при
приёмке.

## Урок начинается сценой или русской фразой, а не определением

Правило приписано 3 сентября 2026. Владелец открыл модуль 2 ступени Elementary,
урок 4 «Мой вчерашний день» и сказал: **«мы же это сотню раз обсуждали. Что это
за объяснение машинное. Даже я, будучи опытным учителем, не понимаю вообще о чём
речь. рандомная блевотина из слов»**. На экране стояло:

> Одно дело — ещё не рассказ. Рассказ выходит, когда дел несколько и они идут по
> порядку.
> Для порядка есть три слова: first — сначала, then — потом, after that — после
> этого.

**Урок начинался с определения нашего же слова.** Ученику объясняли, что такое
«рассказ». Представить там нечего: ни человека, ни дня, ни случая.

**Три начала, и только третье — ошибка:**

| Начало | Пример |
|---|---|
| **сцена** — человек и время | «В субботу у Данияра шахматы, и он говорит об этом заранее» |
| **русская фраза** — то, что ученик говорит каждый день | «По-русски мы говорим „собираюсь“» |
| **обобщение** — подлежащее это наше слово о предмете | «Рассказ о случае строится в три хода» |

**Чем это поучительно — а поучительно тут не правило, а то, как оно уцелело.**
Метод был написан 30 августа (`docs/format-uroka.md`, правила 8–13), редактор
ему обучен в тот же день, ступень объявлена переделанной целиком, и всё-таки
владелец открыл наугад один урок и попал в непеределанный.

Причин две, и обе стоит помнить.

1. **Обе проверки метода этот текст пропускают.** «Убери английские слова —
   осталась ли русская фраза?» — осталась: «сначала, потом, после этого».
   «Прочитай, не зная языка» — предложение о делах по порядку не претендует на
   разбор строки. Проверка, отвечающая на свой вопрос, не отвечает на мой:
   то же самое, что записано ниже про чистый отчёт.
2. **Переделка шла по модулям, а внутри модуля я сам решал, каким урокам она
   нужна.** Коммит `17b152d` говорит прямо: «Уроки 2 и 3 оставлены как были: там
   русской параллели нет». Про урок 4 не сказано ничего — он молча получил одни
   переводы. А русская параллель там была самая полная, какая бывает: «сначала —
   потом — после этого» ложится на `first — then — after that` слово в слово.

**Отсюда правило шире одного урока: «русской параллели здесь нет» — это вывод,
а не факт.** Он делается за минуту и не проверяется ничем, а стоит целого урока,
оставшегося непеределанным. Прежде чем так решить, назови русскую фразу, которой
ученик сказал бы то же самое. Не нашёл фразы — ищи сцену.

Механическую половину считает `npm run perevody` четвёртым числом: уроки правил,
начатые обобщением. Было 12 на Elementary и 3 на Beginner, стало 0 и 0. Мерка
смотрит только первое предложение первого объяснения — середину урока ловит
редактор.

## Формы показываются таблицей, а не абзацем

Просьба владельца от 20 августа 2026. Он открыл модуль 1 ступени Elementary,
прочёл пять абзацев подряд про `was` и `were` и сказал: **«это что за понос из
слов. надо разделить каждое местоимение на одну линию, в стандартах так
объясняют. I am, внизу You are и тд. потом объяснения»**.

**Правило.** Парадигма — таблицей, по одному лицу в строке, сразу после
вступления. Объяснение идёт ПОСЛЕ таблицы и коротко.

| О прошлом | О настоящем |
|---|---|
| I was | I am |
| you were | you are |
| he was | he is |

Так переделаны в модуле 1 четыре места: формы (урок 1), отрицание (урок 3),
вопрос с коротким ответом (урок 4), устройство оборотов времени (урок 6).

**Чего при этом нельзя делать — на этом уже обожглись в тот же день.**
Сокращая абзацы, легко оставить вместо объяснения отсылку «кому какая — в
таблице выше». Таблица отвечает КАКАЯ форма, объяснение — ПОЧЕМУ она такая, и
второе выбрасывать нельзя. Проверка простая: если после таблицы убрать все
слова, потеряет ли урок что-нибудь? Если нет — объяснения там и не было.

**Ещё одно следствие, найденное редактором.** Таблица с местоимениями не
закрывает случай, когда вместо местоимения стоит имя: `my sister was`, `my
parents were`. Этот шаг говорится отдельной строкой, иначе ученик спотыкается
на первом же примере после таблицы.

## Переработка подачи — тоже через проверяющих, и ДО выкладки

20 августа 2026 я перестроил объяснения модуля 1 по замечанию владельца,
прогнал `check:content`, `kontrol` и `yazyk`, получил ноль ошибок и выложил на
боевой сайт. Владелец открыл и спросил: **«объяснение теперь ужасное, ты через
редактора не прогонял да?»** Не прогонял.

Редактор потом нашёл то, чего скрипты не видят в принципе: вместе с абзацами
ушло само объяснение, а текст стал отсылать к таблице; в уроке 4 исчезло
единственное трудное место (в вопросе `Was he …?`, а в ответе `Yes, he was` —
порядок обратный); в уроке 6 правило про `last` и `ago` осталось жить только в
красной врезке «так не говорят».

**Правило.** Проверяющих зовут не только на новый текст, но и на переработку
уже принятого. Порядок один и тот же: скрипты → редактор (и методист, если
менялось содержание или опоры) → правки по разбору → выкладка. Обратный порядок
недопустим: пока идёт проверка, на сайте висит непроверенное.

**Круг проверки один — решение владельца от 20 августа 2026.** Скрипты и по
одному проходу методиста и редактора. Дальше правки по разбору, и всё.

Повод: в тот день я прогнал четыре круга подряд по двум модулям. Каждый
следующий находил всё более мелкое (подпись над примером, число строк в
разборе), но круг стоил столько же, сколько первый, и работа встала на полдня.
Владелец сказал: **«впредь не надо 4 круга проверок проводить, одного круга
проверяющих и скриптов достаточно. когда все допишем, снова пройдем»**.

Отсюда порядок: **круг на модуль сейчас, общий проход по всей ступени — когда
она дописана.** Мелкое, найденное вторым и третьим кругом, к тому проходу
никуда не денется, а ступень будет расти, а не топтаться.

**Что из этого следует для работы.** Раз круг один, он должен быть полным:
проверяющему сразу говорят, что менялось, зачем и на что смотреть. Половина
находок второго и третьего круга появилась не в исходном тексте, а В МОИХ
ПРАВКАХ по первому разбору — заменил строку и не поправил подпись над ней,
сменил оборот и сломал обещание заголовка. Поэтому после правок по разбору
**перечитывай окружение каждого изменённого места сам**, а не жди, что это
поймает следующий круг: следующего круга не будет.

**Почему это не перестраховка.** Чистый отчёт скриптов не говорит о понятности
ничего. `check:content` меряет длину предложений и целостность, `yazyk` ловит
кальки и родовые формы, `kontrol` сверяет цитаты. Понятность, тон и связность
не проверяет ни один из них — это прямо записано ниже, в разделе о качестве
уроков.

## Ступени и доступ — решения владельца от 16 августа

Курс языка делится на ступени: Beginner ведёт к A1, Elementary — к A2,
Pre-Intermediate — к B1. Курс зовётся по той ступени, **к которой приводит**.

1. **Уроки всех ступеней открыты всем.** Замков нет нигде и никогда. Человек
   начинает там, где считает нужным: пришёл со своим A2 — идёт на Elementary,
   первую ступень проходить не обязан.
2. **Экзамен ступени и сертификат открываются, только когда ступень пройдена
   целиком** — все уроки и все проверочные работы модулей. Перепрыгнуть ступень
   сдачей одного экзамена НЕЛЬЗЯ.

   Осторожно, здесь была развилка. 16 августа владелец сначала предложил
   обратное — «сдал экзамен, и ступень зачтена», — мы разобрали последствия, и
   он от этого отказался в тот же день. Если встретишь в старых записях
   «экзамен зачитывает ступень» — это отменённое решение.
3. **Проверочные работы модулей закрыты до прохождения уроков модуля.** Они
   проверяют разобранное и без уроков не значат ничего.
4. **Между ступенями нет ссылок на память.** Следующая ступень не говорит «ты
   уже знаешь `to be`» и «как мы учили в модуле 5»: ученик мог прийти сразу на
   неё, не проходя прошлую. Нужное чужое знание вводится строкой с примером
   здесь же. **Внутри одной ступени ссылки на наши модули остаются и
   приветствуются** — там человек идёт по порядку.
5. **Экзамен ступени сдаёт только тот, кто прошёл её уроки** — значит он вправе
   опираться на курс. Это следствие пункта 2, и оно снимает заботу, которая была
   бы при вступительном испытании: там пришлось бы вычищать из вопросов все наши
   условности вроде `letter` только как «буквы». Здесь условности законны:
   человек их проходил.

## Проверочные работы частей

Курс делится на четыре части по шесть-семь модулей. У каждой части своя
проверочная работа: она приходит спустя шесть-семь модулей и спрашивает не
правило по отдельности, а случай целиком — заполни бланк, возрази, попроси,
объясни причину. Поэтому почти каждый вопрос требует **произвести** ответ:
узнавание среди трёх кнопок спустя месяц не показывает ничего.

Работа части открывается по тому же правилу, что и работа модуля, только уроков
больше: **пройдены все уроки всех модулей части**. Условие проверяется дважды —
на странице и в методе интерфейса.

Где что лежит: вопросы — `courses/english-starter/raboty-chastey.ts`, состав
частей — `courses/english-starter/index.ts`, страница —
`app/learn/[course]/rabota-chasti/[part]`, перенос в базу — `seed-courses.mts`,
вид работы в базе — `part_quiz` с полем `part_slug`.

**Чем это поучительно.** Работы были написаны 15 августа и почти неделю лежали
мёртвым грузом: перенос в базу о них не знал, страницы не было, ссылки не было —
и ни одна проверка об этом не сказала. Написанное содержание, до которого нет
дороги, выглядит в отчётах точно так же, как работающее. **Дописав содержание
нового вида, пройди весь путь до ученика: база → страница → ссылка → звук.**

**Работы частей ни на что не влияют — решение владельца от 20 августа.**
Экзамен ступени и сертификат по-прежнему открываются по прежнему условию: все
уроки и все двадцать пять работ МОДУЛЕЙ. Работы частей в это условие не входят
и добавляться в него не будут.

Основание: работа части труднее работы модуля нарочно — там почти каждый вопрос
требует произвести ответ, а не выбрать из трёх. Четыре такие работы с порогом
8 из 10 перед экзаменом — это лишний час тому, кто уже прошёл всё и всё сдал.
Их место — репетиция экзамена, а не ворота к нему.

**Работа части сверяется со всей частью, а не только с уроками.** Проверка
сравнивает её вопросы и с заданиями уроков, и с вопросами работ модулей —
как у экзамена. Написана 20 августа, после того как методист нашёл в новых
вопросах тринадцать близнецов из двадцати четырёх, и половина совпадала не с
уроком, а с работой модуля. Прогон по старым вопросам, написанным 15 августа,
нашёл ещё шесть — их не видел никто и никогда.

Три места, где этот путь оборвался, и все три молчали:

1. `seed-courses.mts` не знал вида `part_quiz`;
2. страницы и ссылки на неё не существовало;
3. `check:content` и `ozvuchka` не смотрели работы частей — стоило дописать в
   них вопрос со звуком, и ученик получил бы тишину при зелёном отчёте.

## Объём словаря и число модулей — решения владельца от 20 августа

Владелец посмотрел два первых модуля ступени Elementary и сказал: **«Нужно
больше слов в уроки вкладывать, и нам необязательно ограничиваться 25 модулями.
Я знаю, что мы скомпактировали уроки из источника, но всё же ученики должны
получить максимально приближенные к реальности знания.»**

Отсюда три правила.

**1. Число модулей потолком не ограничено.** Двадцать пять на ступени Beginner
— это то, что вышло, а не норма. Если материал ступени требует тридцати пяти
модулей, пишем тридцать пять. Мерило — знания ученика, а не круглое число.

**2. Слов в уроке должно быть больше, и они должны быть новыми.** Норма ступени:
**не меньше 30 словарных карточек на модуль, из них не меньше 15 таких, каких
на прошлой ступени не было.** Обе цифры проверяет `npm run kontrol`.

**3. Сжимать источник — не значит выбрасывать слова.** Скомпактировать можно
объяснение, но не словарь: ученик, прошедший ступень, должен говорить о жизни, а
не о десяти отобранных случаях.

**Уточнение владельца от 21 августа: норму не форсируем.** Столбец «Слов» в
программе — прикидка, а не обязательство, и добивать модуль до неё карточками
ради счёта не надо: **«не надо форсить, можно по ходу добавлять чуть больше
слов»**. Повод — модуль 8, где программа обещала пятьдесят слов, а вышло
тридцать девять при норме в тридцать.

Обязательна именно норма — тридцать карточек и пятнадцать новых, — а сверх неё
слова берутся тогда, когда им есть где работать. Проверку на это см. ниже, в
списке обязательных: карточка, не встречающаяся в модуле нигде, кроме себя, —
и есть след добитой ради счёта нормы.

**НОРМА СЧИТАЕТСЯ ПО ЧАСТИ, А НЕ ПО МОДУЛЮ — решение владельца от 29 августа
2026.** Два правила выше столкнулись друг с другом, и вот на чём.

Пока модули шли по бытовым темам, тридцать карточек набирались сами. К концу
ступени модули стали грамматическими — `should`, `could`, `if`, `который`,
пересказ, — и добирать в них норму стало нечем: девять модулей держали от 11 до
20 карточек. Дописать слова о еде в модуль про `if` значило сделать ровно те
мёртвые карточки, которые запрещает уточнение от 21 августа.

Владельцу были показаны цифры и три пути; он выбрал четвёртый вид урока:
**в конце каждой части ступени стоит урок «Слова части» — тридцать с лишним
слов одной темы с примерами и заданиями.** На Elementary их четыре: дом и день
(модуль 7), город и бланки (13), еда, спорт и природа (21), люди и жизнь (31).
Урок стоит последним в модуле, после письма: он не модуля, а части, и порядок
«правила → чтение → слушание → письмо» этим не ломается.

Отсюда правило для проверки: `kontrol` спрашивает норму **у урока слов части**,
а не у каждого модуля. Курс без таких уроков (ступень Beginner) проверяется
по-прежнему помодульно.

**И два источника слов вместо одного.** К четвёртой части словник A2 Key
оказался выбран: годных записей осталось четырнадцать, остальное — американские
варианты, которых курс не берёт. Второй источник законен и уже применялся в
модуле 17: **слово, которому Oxford ставит A1, но у которого нет карточки ни в
одном модуле ступени.** Между ступенями нет ссылок на память, значит человек,
пришедший сразу на Elementary, не получал ни `sister`, ни `key`, ни `door`.
Такие слова берутся карточкой, и это не добор нормы, а закрытие дыры.

**Чем это вызвано — счётом, а не впечатлением.** В двух первых модулях
Elementary 81 карточка, и **новых для ступени среди них три**: `alone`, `noisy`,
`invite`. Всё остальное Oxford 3000 помечает A1 — ученик видел это на прошлой
ступени. Ни одна проверка не возражала: превышения ступени нет, а что словарь
НИЖЕ ступени, не спрашивал никто.

**Откуда брать слова ступени A2.** Словник есть, искать не надо:
`materials/cambridge-vocab-a2-key.pdf` — 1637 слов, перечень требований экзамена
A2 Key. Из них 852 Oxford 3000 помечает как A1 (ученик их уже знает), а
**785 остаются запасом ступени** — этого хватает и на тридцать модулей, и
больше. Оба словника нужны вместе: Oxford отвечает «какая у слова ступень», но
бытовой половины экзамена не знает вовсе; словник A2 Key отвечает «входит ли
слово в требования», но по ступеням не размечен.

**Проверка новизны словаря** живёт в `npm run kontrol` и на первой ступени
молчит: там всё новое по определению. Подбирать слова помогает `npm run slovo`:
`npm run slovo -- castle zoo gallery` отвечает про каждое — какая ступень по
Oxford, есть ли в словнике A2 Key, как записано в `ipa-en-uk`, не дано ли уже и
в каком модуле. `npm run slovo -- --zapas 40` показывает слова ступени, до
которых курс ещё не дошёл.

Три ловушки, на которых эта проверка спотыкалась при написании. Все три
молчали — отчёт выглядел здоровым:

1. **`tekst()` схлопывает переносы строк.** Словник держится ровно на них, одна
   запись в строке, и первая редакция разобрала НОЛЬ слов вместо тысячи
   шестисот. Читать словник надо самим и построчно. Теперь у разбора есть
   сторож: меньше пятисот слов — ошибка.
2. **`resheno.ts` гасит замечание по вхождению строки.** Перечни английских
   слов внутри вопроса гасят его целиком: там записано слово `like`. Перечни
   ставим только в СВЕДЕНИЯ.
3. **Усечение основ врёт в обе стороны.** Проверке превышения ступени поблажка
   нужна (лучше промолчать, чем снять верное слово), а проверке новизны она
   объявляет новое старым: `est` превратило `forest` в `for`, `offer` в `off`.
   Для новизны сначала спрашиваем само слово, основы — только если его нет.

Осторожно, при её написании нашлась ловушка отчёта. `resheno.ts` гасит
замечание **по вхождению строки**, а разобранным там записано слово `like`.
Перечень старых слов внутри вопроса содержал `like` — и вопрос по модулю 2
замолчал целиком. **Перечни английских слов ставим только в СВЕДЕНИЯ**, текст
вопроса держим числовым.

## Принципы продукта — это ограничения, а не украшение

1. **Никаких счётчиков дней подряд, «жизней» и наказаний.** Ошибка ведёт к
   подсказке и новой попытке.
2. **Сначала понимание, потом применение.** Правило простым языком до практики.
3. **Материалы курса — данные, а не код.** Урок добавляется без программирования.
4. **Свой темп.** Ученик никому ничего не должен успеть.

Если новая возможность противоречит любому из четырёх — не делай молча, скажи
об этом прямо.

## Устройство второй версии

```
app/
  api/v1/             программный интерфейс — единственный вход к правилам
  login, register     вход и регистрация
lib/
  domain/             ЧИСТЫЕ правила: без базы, без React, покрыты тестами
  db/                 подключение к базе (generated/ собирается из схемы)
  auth/               пароли и проверка данных входа
content/              материалы первой версии, отсюда их переносят в базу
prisma/               схема и миграции
docs/                 техническое задание и разборы
```

**Правило слоёв.** `lib/domain/` не знает ни про базу, ни про React. Все правила
(занятые места, подсчёт баллов, выдача сертификатов) живут там и проверяются
тестами без запуска сервера. Это нужно, чтобы будущее мобильное приложение
переиспользовало их через программный интерфейс.

**Правило проверки прав.** Скрытая в интерфейсе кнопка защитой не является.
Каждый метод интерфейса заново проверяет подписку и занятое место.

**Правило ответов проверочных работ.** Правильные ответы никогда не уходят в
браузер до сдачи попытки.

## Ошибки, на которых уже обжигались

Не повторяй их — все они настоящие, из этого проекта.

**Windows и кириллица.** Нерусские строки нельзя передавать прямо в командной
строке: консоль их портит ещё до отправки. Записывай в файл и передавай файлом.
Однажды это выглядело как ошибка кодировки в приложении, хотя приложение было ни
при чём.

**Чтение файла «с заменой непонятых байтов» портит его при записи.** Правка
`.gitignore` 6 сентября 2026: файл прочитан как UTF-8 с `errors="replace"`, одна
старая строка была набрана в cp1251 — и превратилась в цепочку знаков замены,
необратимо. Восстанавливать пришлось из истории git.

Правило: **правя файл, не читай его с заменой.** Если чтение падает на кодировке,
это и есть находка: разбери байты, узнай кодировку (`cp1251` в этом проекте
встречается), приведи строку к UTF-8 нарочно и скажи об этом. Молчаливая замена
хуже ошибки — она проходит все проверки.

Это тот же капкан, что записан выше про кириллицу в командной строке, только с
другого конца: там строку портит консоль по дороге, здесь — собственное чтение.

**Остановка сервера.** `pkill` из Bash не убивает процесс на Windows — порт
остаётся занят, новый запуск падает, а проверка молча идёт против старой сборки.
Освобождать так:
`Get-NetTCPConnection -LocalPort 3010 -State Listen | Stop-Process -Id $_.OwningProcess -Force`

**Порт.** 3000 занят другим приложением, для проверок использовать **3010**.

**`npm run build` НЕЛЬЗЯ запускать поверх работающего `npm run dev`.** Оба пишут
в один каталог `.next`, и боевая сборка затирает рабочую: страница у владельца
остаётся старой, а клиентский код от неё отваливается — кнопки перестают
нажиматься. Выглядит это как «ты ничего не поменял», и однажды я так и решил.

Порядок: остановить сервер (`Get-NetTCPConnection` выше) → `npm run build` →
`rm -rf .next` → поднять `npx next dev -p 3010` заново. Ошибка сделана дважды за
один день, второй раз — через час после первого.

**Prisma и пул соединений.** Сайт ходит в базу через пул (`DATABASE_URL`),
миграции — напрямую (`DIRECT_URL`). Через пул миграции ломаются: он раздаёт
соединения по очереди и не переносит блокировки.

**Скрипты вне Next.js.** Обычный скрипт не подхватывает `.env.local` сам —
загружай настройки явно через `dotenv`.

**Размер клиентского кода.** В клиентских компонентах НИКОГДА не импортируй
`courses/index.ts` — он тянет содержание всех курсов целиком. Однажды такой
импорт раздул одну страницу с 5 кБ до 149 кБ. Нужны только заголовки — бери
`courseCards()`, он отдаёт лишь то, что нужно карточке.

**Хуки и серверный код.** Файл, который импортирует и серверный, и клиентский
код, не должен экспортировать React-хуки: Next.js проверяет это статически и
падает. Держи данные и хуки в разных файлах.

**Проверяй опытом, а не документацией.** Prisma 7 требует перевода проекта на
модульную сборку по документации — на деле заработала без этого. Пробуй.

**Оформление: элементу с обёрткой страницы нельзя задавать `padding`
сокращённой записью.** Только `padding-block`. Секции главной стояли на том же
элементе, что и `wrap-wide`, а запись `padding: 40px 0` обнуляла боковой отступ
страницы. На широком экране это не видно — там ширина и так ограничена, — а на
телефоне карточки прилипли к краям экрана. Нашёл владелец, глазами.

**Оформление: картинке во всю ширину нужна своя высота.** Иначе она диктует её
сама по своей пропорции. Флаг 3 к 2, растянутый на карточку шириной 575 точек,
вырастил обложку до четырёхсот вместо ста двадцати восьми.

**Оформление проверяется глазами, и это не заменить чтением кода.** За день
владелец нашёл на снимках три вещи, которых я чтением вёрстки не нашёл: прилипшие
карточки, обложку вчетверо выше нужного и японское солнце овалом. Просить снимок
дешевле, чем гадать.

## Английский — британская норма

Решение владельца. Держим её в трёх местах сразу, иначе получится язык, на
котором никто не говорит: **произношение** (транскрипция британская, `r` на
конце не произносится), **написание** (`colour`, `centre`, `organise`),
**слова** (`flat`, `lift`, `autumn`, `holiday`, `mobile`, `film`).

Экзамен Cambridge при этом принимает и американский вариант — британская норма
у нас ради единообразия, а не ради проходного балла.

Источники транскрипции лежат в `materials/`: `ipa-en-uk.txt` (свободная
лицензия, собран машиной — ударение стоит не по словарной норме),
`wikipron-en-uk.tsv` (из Викисловаря, писали люди, но есть областные варианты
вроде `fæŋk` вместо `θæŋk`), `cmudict.txt` (надёжен, но американский).
**Готовой правильной транскрипции ни один не даёт** — спорные слова решает
владелец как преподаватель.

**Условность записи, принятая владельцем для всего курса:** краткий гласный
пишем через `e`, а не `ɛ` — `/ef/`, `/el/`, `/em/`, `/en/`, `/es/`, `/eks/`,
`/zed/`, `/spel/`. Это обычная словарная британская запись, но в наших
источниках стоит `ɛ`, поэтому решение записано здесь: иначе следующий модуль
возьмёт другой знак и таблицы разойдутся.

**Второстепенное ударение показываем, но не объясняем.** Решение владельца от
6 сентября 2026, дословно: «можешь показывать, не надо объяснять». Значит
двухсловные формулы несут знак так, как его даёт словарь, — `/ˌgʊd ˈmɔːnɪŋ/`, —
а строки урока о самом знаке `ˌ` не пишется. Ученик видит его и читает по
главному ударению, которое курс объяснил в модуле 1.

Повод: методист модуля 2 нашёл, что курс сам с собой не сходится — `good
afternoon` в замороженном курсе с двумя знаками, а `goodbye` без второстепенного,
и знак `ˌ` стоит там ещё в пяти местах. Наши источники второстепенного ударения
не дают вовсе: `ipa-en-uk` пишет `/ɑːftənˈuːn/`, `wikipron-en-uk` ударений не
ставит совсем. Значит спорные случаи решает владелец как преподаватель, а курс
держит словарную запись.

**Согласный `r` пишем как `r`, а не `ɹ`.** В `ipa-en-uk.txt` везде стоит `ɹ`,
в словарях — `r`. Это условность того же ряда, действует во всех модулях.

**Согласный `g` пишем обычной буквой `g`, а не знаком `ɡ`.** Источник даёт `ɡ`,
словари — `g`. Основание то же, что у `r`. Впервые понадобилось в модуле 19,
слово `guitar` — `/gɪˈtɑː/`.

**Третья условность того же ряда: слоговой согласный пишем без `ə`** — `/ˈæpl/`,
`/ˈkæpɪtl/`. Источники дают `ˈæpəl` и `kˈæpɪtəl`, словари — без гласного.
**Владелец подтвердил 22 августа 2026**, после того как я показал ему
расхождение по Викисловарю: тот пишет `/ˈapəl/` и даёт `[ˈapl̩]` звучанием, но у
него своя система целиком — он и знак TRAP пишет как `a`, а не `æ`. Брать из
чужой системы одну условность нельзя, и курс держит словарную запись. Выписка —
[docs/istochniki-norma-yazyka.md](docs/istochniki-norma-yazyka.md), пункт 11.
Исключение — `student`: там `/ˈstjuːdənt/`, потому что гласный слышен и в
словарях.

**По тому же основанию — безударный гласный на конце пишем через `ə`:**
`/ˈtiːtʃə/`, `/ˈwɔːtə/`, `/ˈletə/`. В источниках стоят `ɐ` (ipa-dict) и `əː`
(Викисловарь), в словарях — `ə`. **Эту условность владелец пока не
подтверждал** — 22 августа он отвечал про слоговой согласный, а про конечный
гласный вопрос не задавался. Пока он не решил иначе, держим `ə` во всех модулях:
расхождение внутри курса хуже, чем неудачно выбранный знак.

Решённые владельцем спорные слова: `Kazakh` — `/kəˈzɑːk/`, буква `W` —
`/ˈdʌbljuː/`, буква `Z` — `zed`, а не `zee`.

**Короткий номер называют целиком — решение владельца от 2 сентября 2026.**
Комната 63 — это `sixty-three`, а не «six — three». По одной цифре диктуют
ДЛИННЫЙ номер: комнату из трёх цифр, рейс, счёт. Повод: работа модуля 9
объявляла `sixty-three` ошибкой, и ученик, ответивший верно, получал «пока не
так». Источники в `materials/` о чтении номеров молчат — методист проверил оба
руководства Cambridge, — поэтому решал владелец как преподаватель. Записано
врезкой в уроке 6 того же модуля, чтобы правило не прочли шире, чем оно есть.

## Звук — решение владельца от 16 августа

**Озвучивает синтез речи Azure, британские голоса.** Свой голос владелец
записывать не хочет; вопрос закрыт. Команда — `npm run ozvuchka`.

Звучат три вида: блоки материала (`kind: "audio"`), слова словарей по одному и
образцы к заданиям «произнеси вслух» — всего 589 записей на ступень Beginner.
Курс по коду не озвучивается: его словарь состоит из названий меток разметки.

**ЗВУК СТОИТ ТАМ, ГДЕ УЧЕНИК ЧИТАЕТ.** Решение владельца от 19 августа,
касается всех модулей и всех будущих работ.

Отдельная запись внизу урока новичку не говорит ничего: он видит набор слов и
не знает, к чему они относятся. Услышать надо ровно то, на что смотришь, — так
написанное и звучание связываются. Поэтому:

1. **Звучат примеры и таблицы**, а не отдельный блок под ними. Где пример
   зазвучал, блок `audio` снизу убирается — он стал лишним.
2. **Звук дробится по единицам.** Пара `G — J` одна запись, `B — V` другая.
   Сочетание `ea` одна, слова `tea, sea, eat` другая. Не одна запись на всё.
3. **Разговор звучит целиком, одной кнопкой и НА ДВА ГОЛОСА** (или три, если
   нужно). Он должен звучать разговором, а не одним длинным предложением.
   Это касается и вопросов на слух: `zvuk: "Are you a teacher? — No, I'm not."`
   — тоже разговор. До 20 августа такие вопросы читались одним голосом, потому
   что правило применили к примерам урока и забыли про работы. Границу реплик
   ставит тире с пробелами, а решает `razgovorLi()` из `lib/content/zvuk.ts` —
   там же, где считается имя файла, иначе страница и озвучка разойдутся и
   ученик получит тишину.

   **Границ на деле ДВЕ, и вторая молчала до 23 августа 2026.** Записи `audio`
   и вопросы работ пишут разговор одной строкой и разводят реплики тире.
   Примеры уроков (`razgovor: true`) устроены иначе: реплика на строку, тире
   между ними нет вовсе. А `repliki()` в `scripts/ozvuchka.mts` резал только по
   тире — значит **сорок примеров-разговоров ступени Elementary читались одним
   голосом**, и правило владельца от 19 августа для них не исполнялось.

   Найти это было нечем, и вот почему: **имя файла считается от текста и не
   зависит от числа голосов.** Запись на месте, кнопка работает, `check:content`
   доволен — а звучит она не тем, чем должна. Ни одна проверка такого не видит и
   увидеть не может: она сверяет наличие файла, а не то, что внутри. Теперь
   `repliki()` пробует перевод строки первым (в примере он и значит смену
   говорящего), а тире — вторым; сорок записей переделаны.

   **Урок шире озвучки: проверка «файл есть» не то же самое, что «звучит
   верно».** Где содержание файла зависит от кода, а имя — нет, там правка кода
   молча меняет смысл всех старых записей или, наоборот, не меняет ничего.
   Такие места надо слушать, а не считать.

   **И ЭТО ПОВТОРИЛОСЬ 28 АВГУСТА, В ДРУГОМ ОБЛИЧЬЕ.** Тогда починили примеры
   уроков (`razgovor: true`) и не посмотрели на записи `audio`. Число голосов у
   них решает поле `voice`, и его не было НИ У ОДНОГО из 29 блоков ступени
   Elementary, хотя на Beginner оно стоит везде: двадцать четыре разговора
   читались одним голосом. Нашёл методист чтением `scripts/ozvuchka.mts`, а не
   отчёт.

**И ТРЕТИЙ РАЗ, 5 сентября 2026, — на разделителе.** Владелец в тот день
попросил ставить в записях заданий запятые вместо дефисов: «дефис синтез читает
слитно». Просьбу исполнили, а образец, по которому буква отдаётся синтезу
звуком, принимал **только точку** — `A. H. J. K.`. Значит семь записей курса,
все ряды заданий (`C, A, T.`, `S, A, M.`, `A, U, R, O, R, A.`), шли синтезу
обычным текстом, без разметки. Ровно то, на что владелец жаловался тем же
утром: одиночная `A` без разметки читается безударным артиклем.

**Хуже всего, что замысел урока утверждал обратное** — «там работает ветка
`ryadBukv`». Утверждение о коде, сделанное чтением ПОХОЖЕГО места, а не
прогоном. Нашёл второй методист чтением; отчётом это не ловится совсем: имя
файла считается от текста, текст не менялся, файл на месте.

Отсюда правило: **утверждение о том, что делает код, проверяется прогоном, а не
чтением соседней ветки.** Одна строка в `node -e` отвечает быстрее, чем абзац
рассуждения, и отвечает верно.

   **Отсюда правило: починив один вид записи, пройди по всем видам.** Звук
   собирается из четырёх источников — блоки `audio`, примеры уроков, слова
   словарей и образцы к заданиям, — и правило владельца о двух голосах
   относится к первым двум. Проверка на это выглядит так:

   ```bash
   grep -c 'kind: "audio"' courses/<курс>/*.ts   # сколько записей
   grep -c 'voice:' courses/<курс>/*.ts          # у скольких два голоса
   ```
4. **Знаки препинания соблюдаются в озвучке.** Пауза на точке длиннее, чем на
   запятой. Раньше выходило наоборот, и владелец услышал это первым.
5. **Исключение — уроки слушания.** Там запись и есть предмет урока, она
   остаётся. Примеры внутри них могут получить кнопку вдобавок.

**Ступень Beginner переведена целиком: все 25 модулей.** Отдельных записей под
уроками осталось 51 — все в уроках слушания и три в первом модуле, где запись
о порядке букв, а не о букве.

**Как это записывается в уроке.** Обычный случай — `zvuchat: ["строка", …]`:
произносится ровно то, что напечатано. `zvuk: { "напечатано": "произнести" }`
нужен только при расхождении: в ячейке `A a`, а произнести надо название буквы
один раз. Собирает обе записи `zvuchashchee()` из `lib/content/zvuk.ts` — одна
точка на страницу, озвучку и проверки.

**Чего звук не получает — и это решение, а не недосмотр:**

- **Строка, показывающая, как говорить НЕЛЬЗЯ.** «I am work now», «two breads»,
  «a water» стоят в курсе нарочно, в столбце «так сказать нельзя». Услышанная
  неверная фраза запоминается наравне с верной, и кнопки у неё быть не должно.
- **Артикль и предлог сами по себе.** Одиночное `a` синтез прочтёт названием
  буквы, а не артиклем; `at`, `on`, `in` в речи безударны. Звучит пример со
  словом, а не служебное слово отдельно.
- **Дата цифрой** — `on 5 May`. Читается порядковым числительным («the fifth of
  May»), а порядковых на этой ступени нет: запись сказала бы ученику слово,
  которого в уроке не было.
- **Ячейки с кусками предложения.** У таблицы порядка слов (модуль 3, урок 6)
  в ячейках стоят `I`, `am`, `a student` по отдельности — читать вслух `am`
  не значит ничего. Предложения там звучат в примере под таблицей.
- **Русские подписи и транскрипция.** Их читают глазами.

Счётчик «без звука вовсе» ведёт `npm run check:content` — сведениями, не
ошибкой: решает методист.

**Имя файла выводится из самого текста** (`lib/content/zvuk.ts`), в уроке оно
не хранится. Отсюда два следствия. Первое: озвучка не трогает материалы, а
правка фразы сама означает другую запись. Второе, неприятное: поправил фразу и
не переозвучил — файла нет, и ученик получает тишину, а глазами это не видно.
Ловит `npm run check:content`.

**Чтение имён собственных — `lib/content/proiznoshenie.ts`.** Наши источники
транскрипции таких слов не знают и знать не могут, поэтому каждая строка там
решение владельца как преподавателя. `Almaty` и `Astana` читаются по-казахски,
а не так, как их произнёс бы англичанин: курс для казахстанцев.

**Правка любой строки там означает переозвучку всех записей с этим словом.**
Скрипт делает это сам — он помнит, из чего сделан каждый файл, — но только если
его запустить.

**Предлог при учреждении — `at`.** `at school`, `at a university`, `at a
college`, `at a company`. Помещение как место работы остаётся через `in`: `in a
shop`, `in an office`, `in a hospital`. Решение владельца как преподавателя,
принято после того, как курс набрал оба предлога вперемешку и одно задание
стало принимать только `at`, хотя весь модуль учил `in`.

**Подписи полей бланка.** `surname` — британская, `last name` — американская и
более распространённая. Преподаём `surname`, вторую даём на узнавание: бланк
выбирает не ученик. Это решение владельца как преподавателя — источники такого
вообще не знают.

**Короткая подпись `Name` значит ИМЯ, а не имя с фамилией** — решение владельца
от 6 сентября 2026. Образца бланка в `materials/` нет ни одного (методист искал
`form`, `registration`, `application` по трём руководствам), поэтому решал
владелец. Из этого следует, что бланк модуля 1 нового курса — `Name: Kim` —
верен и правки не требует, а модуль 5 разводит `First name` и `Surname` там, где
подписи полные.

**В поле `Nationality` казахстанец пишет `Kazakh`** — решение владельца того же
дня. Слово при этом остаётся и названием языка, как оно стоит в замороженном
курсе: одно слово на оба случая. Значит там, где ученик встретит его дважды,
разницу надо назвать словами, а не надеяться, что он поймёт по месту.

**Имена собственные ступенью не меряются.** Решение владельца 15 августа:
названия стран, городов, языков и людей берутся по смыслу урока, ступень им не
подтверждается и в шапках не оговаривается. Основание простое: частотные
словники составляются по нарицательным словам, и `Kazakhstan` в них не будет
никогда — не потому, что слово трудное, а потому что таких слов бесконечно
много и частота у каждого своя в каждой стране. Ждать от Oxford 3000 строки
`Kazakhstan A1` бессмысленно. Без этого правила к двадцать пятому модулю
накапливается два десятка одинаковых оговорок ни о чём.

**Чего наши источники не умеют вовсе.** Они отвечают «как читается слово» и «на
какой ступени слово стоит», но не «где как принято». Пометок разновидности
языка, оценок вежливости и данных о частотности нет ни в одном файле. Поэтому
любое утверждение вида «так говорят чаще», «это звучит вежливее», «в британском
принято» — либо решение владельца как преподавателя, либо выписка из открытого
источника, **либо его в тексте быть не должно**. За сессию проверяющие поймали
семь таких утверждений.

Третий путь появился 14 августа по решению владельца:
[docs/istochniki-vezhlivost.md](docs/istochniki-vezhlivost.md) — выписки об
обычаях из открытых источников, с точными цитатами и ссылками. Файл включён в
список источников `npm run kontrol`, так что его цитаты сверяются машиной
наравне с остальными. Правило внутри него важнее его содержания: **записывать
можно только то, что открыл своими глазами.** Пересказ поисковой выдачи цитатой
не считается и помечается отдельно.

**У него появился брат 22 августа 2026, по просьбе владельца:**
[docs/istochniki-norma-yazyka.md](docs/istochniki-norma-yazyka.md) — то же
самое, но не про обычаи, а про норму языка: что слово значит, как сильно звучит
и где как принято. Тоже включён в `kontrol`, и проверка испытана нарочно
испорченной цитатой.

Оттуда два решения, действующих во всех модулях:

- **Почта по-британски — `post`, а не `mail`.** Википедия, «Comparison of
  American and British English», таблица различий даёт пару прямо. Слово `mail`
  в словнике A2 Key есть, но словник отвечает за требования экзамена, а норму
  курса выбрал владелец.
- **Британское `second floor` — это третий этаж.** Википедия, «Storey»: «the
  floor at ground level is called the “ground floor”… the next floor up is
  assigned the number 1». Пока схема ученику не объяснена, **этажей в заданиях
  не спрашиваем вовсе**: перевод «first floor — первый этаж» будет ошибкой, а
  объяснение схемы — это отдельный урок.

И одно предупреждение о самих открытых источниках: **Cambridge Dictionary,
Oxford Learner's Dictionaries, British Council и Collins отдают 403** и с этой
машины не открываются. Открываются Викисловарь и Википедия. Поисковая выдача их
пересказывает — и этот пересказ в выписки попадать не должен.

## Задание не наказывает за верный ответ

Владелец прошёл один урок и нашёл три ошибки этой породы. Ни одну из них не
поймали ни скрипты, ни оба проверяющих — потому что источникам они не
противоречат и по-русски написаны верно. Видны только тому, кто проходит урок.

**1. У задания либо один верный ответ, либо принимаются все верные.** Решение
владельца: «несколько верных решений можно, если они правильные».

Задание на сопоставление давало три вопроса и три ответа: `Do you work here?`,
`Are you a teacher?`, `Do you know Dana?` — и `Yes, I do.`, `Yes, I am.`,
`No, I don't.`. Оба вопроса с `Do` одинаково допускают и да, и нет, выбор не
задан ничем — а принимался один. Ученик отвечает правильно и получает «пока не
так».

Проверять так: **прочитай условие и спроси, что ещё сюда подходит.** Если
подходит что-то ещё — либо доопредели условие, либо впиши в `accept`.

**2. Неверный вариант должен быть настоящей ошибкой, а не выдумкой.**
`Yes, I am work` не говорит никто и никогда: по-русски это «да, я работа». Такой
вариант не учит ничему — ученик отбрасывает его как бессмыслицу, не думая.
Настоящая ошибка здесь — `Yes, I work`: её ученики делают, и урок как раз о ней.

Отсюда следствие, на котором я обжёгся: **если проверяющий говорит, что верную
фразу нельзя объявлять ошибкой, её заменяют другой НАСТОЯЩЕЙ ошибкой**, а не
придуманной строкой.

**3. Условие, определяющее ответ, должно быть незаметно нельзя.** «Тебя
спросили: Do you work in an office? Ты работаешь. Ответь коротко» — слова «ты
работаешь» проглатываются, и даже преподаватель ответил «нет». Пиши прямо:
«Ответь коротко — да».

**4. Задание на сборку принимает ровно один порядок слов.** Где верных порядков
несколько (`I always work in the morning` и `In the morning I always work`),
либо оговаривай порядок в условии, либо не бери такое предложение.

**5. Поле `accept` перечитывается вместе с условием, и его не видит ни один
скрипт.** Приписано 22 августа 2026. В работе модуля 15 стояло:

```
prompt: "Ученик написал: It is the hotest place here. Запиши строку без ошибки."
answer: "It is the hottest place here."
accept: ["It is the hottest country here"]     ← country вместо place
```

Ученик, написавший верно, получал «пока не так»; написавший `country` — слово,
которого в условии нет вовсе, — получал «верно». `check:content` видит непустую
строку и ровно один верный ответ, `kontrol` — тоже. Нашёл методист чтением.

След от правки соседней строки: слово заменили в `answer` и в `prompt`, а в
`accept` не заменили. Порода та же, ради которой писался `npm run sosedi`, но
`sosedi` смотрит блоки, а не поля внутри одного задания. **Значит `accept`
проверяется глазами вместе с `answer` и условием — всегда.**

И вторая половина того же: **если у задания верных ответов несколько, они все
стоят в `accept`.** Модули о сравнении учат трём взаимозаменимым способам, и
проверяющий нашёл семь мест, где принимался один из них.

## Правка ломает соседа — и это не случайность, а устройство

Разбор ошибок 20 августа 2026, после того как владелец спросил, что сделать,
чтобы одна и та же ошибка не повторялась.

**Все дорогие промахи того дня одного вида.** Я правлю одно место, а связанное с
ним рядом молча устаревает:

| Что правил | Что осталось от прежнего |
|---|---|
| дописал строки в девять примеров | разборы под ними: «три строки» при пяти |
| заменил строку примера | подпись над ним: «Вчерашний день Данияра», а в строках она и он |
| сменил оборот, убирая повтор | заголовок: «Неделя», а в строке `a month ago` |
| добавил 47 слов | опоры и шапки, где перечислен прежний словарь |
| завёл карточку `desk` | перечень подпорок, где `desk` остался подпоркой |
| поправил фразу | запись звука, которой для новой фразы нет |

**Причина не в невнимательности.** Урок — это граф: пример и разбор под ним,
карточка и её звук, слово и перечень в шапке, ответ и разбор задания. Правится
он как текст, по одному месту. Значит связанный узел забудется НЕ иногда, а
всегда, и надеяться тут можно только на машину.

**Отсюда `npm run sosedi`.** Смотрит `git diff`, находит блоки, которых правка
коснулась, и говорит, какие поля этих блоков остались нетронутыми: «тронуто
text, а explain осталось прежним». Не ошибка и не запрет — список мест, куда
посмотреть. Пары, которые он знает: текст и разбор, текст и звук, перевод и
пример, ответ и подсказка с разбором.

**Порядок работы теперь такой:**

```
правка → npm run sosedi → посмотреть каждого названного соседа
       → скрипты → проверяющие (один круг) → правки → sosedi ещё раз → выкладка
```

Второй прогон `sosedi` после правок по разбору — не перестраховка: **половина
находок второго и третьего кругов 20 августа была следами моих же правок по
первому разбору.**

**НО ДЛЯ НОВОГО МОДУЛЯ `sosedi` СЛЕП, И ОТЧЁТ ОБ ЭТОМ НЕ ГОВОРИТ.** Он смотрит
`git diff`, а неотслеживаемый файл туда не попадает вовсе: пишешь новый модуль —
получаешь «правок, у которых сосед остался прежним, не нашлось», и это значит
только, что скрипт файла не видел.

Найдено 23 августа 2026 при написании модуля 19, и не скриптом, а редактором:
я поменял в задании `couldn't` на `didn't` по замечанию сверки, а разбор под
заданием остался цитировать `couldn't`. Ученик читал бы «почему» и не узнавал
собственной фразы.

Отсюда правило: **пока модуль не в истории, окружение каждой правки
перечитывается глазами.** Либо, если правок по разбору много, закоммить модуль
как есть и править уже поверх — тогда `sosedi` заработает как задумано.

## Шапка замысла называет три вещи, и называет их ДО первой строки редактора

Правило приписано 6 сентября 2026 методистом модуля 8, и он вывел его из цены
собственного промаха.

Правило «у главной вещи модуля имя одно» в проекте есть давно. Его замысел
объявил термином модуля одно **«множественное число»** — и промолчал про две
другие вещи, которые урок называет на каждом экране:

1. **артикль.** Модуль 7 уже дал ученику это имя прямо; модуль 8 звал ту же вещь
   «коротким словом `a`» девять раз;
2. **общее слово для того, о чём модуль говорит.** Модуль 7 зовёт это **вещью** и
   объясняет почему («„предмет" по-русски значит ещё и школьный предмет, а тема
   `Education` придёт модулем 13»); модуль 8 взял «предмет» не думая, двадцать с
   лишним раз.

**Цена оказалась несоразмерна правке.** Оба слова — видимый текст, значит менять
их вправе только редактор. До правки в уроке не было ни одной незаполненной
заглушки: редактор написал весь текст. После — **двадцать восемь заглушек**, то
есть урок переписывается целиком вместо замены слова.

**Ошибка была не в решении, а в том, когда оно принято.** Оба имени надо было
назвать до того, как редактор сел писать.

**Отсюда: шапка замысла называет три вещи, и все три до первой строки текста.**

| Что | Зачем |
|---|---|
| **имя главной вещи модуля** | правило уже было, и его мало |
| **имена заимствованных терминов и адрес, где они введены** | «артикль — имя дал модуль 7, мы им пользуемся» |
| **общее слово, которым модуль зовёт то, о чём говорит** | «вещь», «предмет», «человек», «запись» — это выбор, а не пустяк |

## Правило опровергает не свой пример, а СОСЕДНИЙ УРОК того же модуля

Порода найдена 6 сентября 2026 и в списках проекта её не было.

Случай 5 первого урока модуля 8 нового курса утверждал: «по-русски слово меняется
внутри, а по-английски прибавляется окончание». Цепочка правок вышла поучительной
тем, что **каждый шаг был верен по отдельности**:

1. методист дал правило и пару **«яйцо — яйца»**;
2. первый редактор заметил, что пример опровергает правило — там меняется ровно
   окончание, а не середина, — и заменил пару на **«человек — люди»**;
3. второй редактор показал, что это худшая пара из возможных: «человек — люди»
   по-английски это `man — men` и `person — people`, то есть **предмет урока 3
   того же модуля**.

**Негодным оказался не пример, а правило.** Собственный модуль отменял его
дважды: урок 2 даёт `city — cities` (меняется написание), урок 3 — `man — men`
(меняется всё слово). Правило было о ЯЗЫКЕ, а модуль тут же учит исключениям из
него.

**Чем это отличается от известных пород.** «Запрет ловится своим текстом» и «к
цитате читается её пример» ищут опровержение В ТОМ ЖЕ месте — в абзаце, в графе,
в примере под правилом. Здесь опровержение лежит **через два урока**, в файле,
которого в минуту написания ещё не существовало. Ни `grep` по модулю, ни чтение
случая целиком его не находят.

**Отсюда проверка: правило урока сверяется с ПЕРЕЧНЕМ УРОКОВ модуля.** Если
соседний урок назван «Когда написание меняется» или «Свои формы», то правило,
говорящее «больше в слове ничего не меняется», обязано говорить о СВОИХ словах, а
не о языке: «к слову прибавляется `-s`, и `cup` так и остаётся `cup`». А за
примерами идёт строка обещания с адресом.

## Замысел объявляет виды заданий своими, а сосед берёт их, пока замысел лежит

Приписано там же, по модулю 6.

Работа модуля писалась ПЕРВОЙ — правильно, по правилу «работу пишут до заданий
уроков» — и объявила в своём замысле: «„исправь чужую запись" и „соедини две
готовые строки". **Ни одного такого вида в уроках нет**».

Уроки 2–9, написанные позже, взяли ровно эти виды. **Шесть вопросов работы из
двенадцати стали близнецами заданий уроков**, причём один совпал условием
дословно и двумя строками из трёх.

**Утверждение было верным в день написания и стало ложным без единой правки** —
ровно как «в модуле 2 после имени стоит одно только `is`». Разница в том, что там
устаревало утверждение о ЧУЖОМ модуле, а здесь о своём же, но о будущей его
части.

**Отсюда: „ни одного такого вида в уроках нет" пишется с датой, а перед сборкой
проверяется заново.** И проверяется разбором обоих документов, а не памятью:
ручной свод у соседа в тот же день врал в четырёх строках из девяти.

## Настоящий термин называется всегда, но отдельного экрана не получает

Правило приписано 6 сентября 2026 методистом модуля 3, и у него три опоры, а не
одно соображение.

**Повод — расхождение между соседними модулями, которое увидел только тот, кто
читал их рядом.** Модуль 3 тратил целый случай на слово «предлог»: экран, где
нет ни одной новой английской строки, а сам термин ученику знаком по русскому
языку — правило на это и опиралось («русские „из" и „в" тоже предлоги»). Модуль
4 в такой же точке поступал наоборот: весь урок о множественном числе не называл
его никак, только «буква s» и «конец слова». Две противоположные повадки, обе от
вкуса редактора.

**Ни та, ни другая не годится, и вот почему.**

- Правило 3 понятности называет термины поимённо — «местоимение», «предлог»,
  «множественное число» — и требует объяснять их **на месте**. Молчать нельзя:
  ученик, не получивший имени, не найдёт вещь ни в учебнике, ни в другом курсе.
- Но «на месте» и значит **строкой внутри случая, где вещь работает**, а не
  экраном, посвящённым имени. Экран без нового английского ученик проходит как
  пустой.

**Третья опора — прогон, а не рассуждение.** `npm run tekst` по модулю 8 даёт
**пятнадцать** упоминаний «множественного числа» в ВИДИМОМ тексте, первое — «Слова
„яблоки" и „яйца" стоят во множественном числе». То есть курс уже так и делает
там, где делает хорошо.

**И проверять это надо именно прогоном по видимому тексту.** Имя, стоящее в шапке
файла, ученику не звучит — на этом курс обжёгся целой ступенью: одиннадцать
модулей Elementary не назвали ни одного времени, хотя шапки восьми честно писали
«грамматика: Past Simple».

## Совпадение ЗАГЛУШЕК — раннее предупреждение о близнецах текста

Правило приписано 6 сентября 2026 сборщиком модуля 4 — о собственной сборке.

Он собрал восемь уроков и работу, и две заглушки вышли дословно одинаковыми:
подсказка задания урока и подсказка вопроса работы. Причина простая — **предмет
у них один**, и, описывая его словами «что строка должна утверждать», он дважды
написал одно и то же.

**Отсюда вывод, который стоит держать:** заглушка — это сжатый пересказ будущего
текста. Совпали заглушки — **совпадут и тексты редактора**, потому что писать он
будет по ним. То есть близнец виден за круг до того, как появится.

Проверять дёшево и машинно: собрать все строки `ПИШЕТ РЕДАКТОР` модуля и
поискать среди них дословные совпадения. У `gap` и `short`, где `bliznetsy`
молчит по устройству, это единственный механический признак, какой вообще есть.

**Оговорка, без которой правило вредно:** заглушки нового модуля совпадают и
законно — «условие. Скажи, что…» повторяется у каждого `speak`. Смотреть надо на
совпадение ПРЕДМЕТА, а не рамки, ровно как у самих близнецов.

## Слово сетки прячет в себе другое слово курса, и перестановкой это не лечится

Правило приписано 6 сентября 2026, когда сетку со словами в поле букв ставили в
уроки впервые.

Методист модуля 5 взял в сетку подписи полей бланка — `NAME`, `FORM`, `COUNTRY`,
`ADDRESS`, `EMAIL` — и прогнал `lishnieSlova` из `lib/content/setka.ts`. Отчёт
назвал место: **`NAME` содержит внутри себя `AM`**, а `am` — карточка модуля 2.
Ученик отмечает `AM`, оно верное слово курса, и получает «пока не так».

**Перестановкой букв в поле это не лечится:** лишнее слово лежит внутри
искомого, а не рядом с ним. Слово меняется целиком — взято `PASSPORT`.

**Отсюда две вещи.**

1. **Поле сетки собирается машиной, а не глазами.** `bedySetki` проверяет, что
   каждое искомое слово в поле есть и ровно один раз; `lishnieSlova` — что в
   поле не сложилось ничего лишнего из словаря курса. Методист поля не строит:
   у него нет прогона.
2. **Короткие служебные слова курса — самая частая помеха.** `am`, `is`, `it`,
   `he`, `an`, `on`, `to` прячутся внутри длинных слов и в случайных буквах
   возникают сами. Методист модуля 4 назвал это же с другой стороны: числа до
   десяти (`one`, `two`, `six`, `ten`) в сетку не ставят — они складываются в
   поле сами.

## Заведённый вид задания, которым никто не пользуется, — та же немая дорога

Правило приписано 6 сентября 2026, счётом по всему новому курсу.

5 сентября владелец попросил три вида упражнений для новых слов: отгадать вещь
на картинке, собрать слово из рассыпанных букв, найти знакомые слова в поле
случайных букв. Все три были заведены в тот же день — тип, проверки, страница,
тесты, — и на этом работа выглядела сделанной.

Через сутки счёт по восьми модулям дал: **буквы вразброс — 2 задания, оба в
модуле 1; картинка в вариантах — 2; сетка со словами в поле букв — 0.**

**Ни одна проверка об этом не скажет, и сказать не может.** `check:content`
проверяет заведённые задания, а не отсутствующие; `bliznetsy` смотрит совпадения;
`programma` считает слова и пункты грамматики. Отчёт при нуле применений
выглядит ровно так же, как при сотне.

**Это обратная сторона давнего правила** «содержание, до которого нет дороги,
выглядит в отчётах как работающее»: здесь дорога есть, а содержания нет. И
опаснее она тем, что дорогу строит автор, а содержание пишет методист — то есть
никто не считает себя обязанным закрыть разрыв.

**Отсюда: завёл вид задания — посчитай, сколько его в курсе,** и назови число
тому, кто решает состав уроков. Одна команда:

```bash
grep -rc 'kind: "<вид>"' courses/<курс>/*.ts
```

Счёт делается не в день, когда вид заведён (он законно нулевой), а на ближайшем
круге по модулям, где этот вид уместен.

## Перестановка случаев двигает и то, что к ним приклеено

Правило приписано 6 сентября 2026, на правках строения модуля 7 нового курса.

Второй методист велел поменять местами случай об артикле и случай об ответе
одним словом. Между ними стояла таблица трёх длин ответа с полем
`naTomZheEkrane: true` — она едет вместе с ПРЕДЫДУЩИМ объяснением, а не стоит
сама по себе.

**Оставшись на месте, таблица приклеилась бы к чужому случаю** и показала бы
запись `A window.` за экран до того, как о такой записи сказано. Сборщик это
увидел и переставил её вместе со вторым случаем.

**Отсюда: переставляя блоки, спроси у каждого соседа, к чему он привязан.**
Привязка живёт полем (`naTomZheEkrane`), а не расстоянием в файле, и глазами в
списке блоков её не видно.

**И вторая половина того же случая: замысел ссылается на НОМЕР случая, а номера
после перестановки другие.** Замысел модуля 7 писал «при случае 5», и это была
нумерация до перестановки; три опоры заданий указывали на случаи по старому
счёту. Сборщик вёл их по содержанию и сказал об этом вслух — верно и то, и
другое. **Номер случая в замысле — не адрес, а слепок минуты, когда замысел
писали.**

## Имя задания зовёт его тем, что говорит условие

Приписано там же. У задания стоял `id: "z7-otvet-odnim-slovom"`, а условие после
правки просит назвать вещь — и ответ `A chair.` состоит из двух слов. Имя
осталось от прежнего замысла и говорило неправду тому, кто станет читать файл.

Смена `id` дешева, пока на него никто не ссылается, и дорога потом: он стоит в
опорах, в пометках итогов и в разборе. **Проверяется поиском по всему дереву, а
не по одному файлу.**

## Строка программы проверяется и по источнику, И ПО КУРСУ

Правило приписано 27 августа 2026, после самой дорогой находки модуля 26.

Перед написанием модуля я проверяю строку программы по источникам: есть ли это
на ступени, какие графы, какие примеры. Модуль 26 прошёл эту проверку лучше
всех предыдущих — у `Why don't we…?` нашлись три графы A2 и ни одной границы.

**А урок про `Why don't we` в курсе уже был.** Модуль 12 «Давай я», урок 4
«Почему бы не: Why don't we meet at the square?», та же графа источника. Модуль
26 написал по ней второй такой же урок: та же формула заголовка, тот же набор
заданий по порядку.

Хуже совпадения оказалось расхождение. Модуль 12 режет строку на `why` +
`don't we` + глагол, модуль 26 резал её на `why don't` + местоимение. **Ученик
одной ступени получал два несовместимых разбора одной строки** — и ни слова о
том, что это одна и та же строка.

**Причина проста и повторится.** Проверка строки задаёт вопрос ИСТОЧНИКУ: «есть
ли это на ступени». Вопрос «нет ли этого уже в курсе» задаётся не источнику, а
`courses/` — и я его не задавал вовсе.

**Отсюда правило: проверяя строку программы, ищи её предмет в самом курсе.**
Поиск по заголовкам и итогам уроков ступени занимает секунду:

```bash
grep -n "outcome:\|title:" courses/english-elementary/*.ts | grep -i "<оборот>"
```

**Механически это ловится только частью.** `check:content` следит за
неповторимостью ИМЁН уроков — а имена были разные: «Почему бы не: Why don't we
meet at the square?» и «Почему бы не: Why don't we go riding?». Совпадал
предмет, а не имя, и на такое проверки нет.

**И ПРОВЕРКА ПО КУРСУ ИДЁТ ПЕРВОЙ, А НЕ ВТОРОЙ.** Правило уточнено 29 августа
2026, после четвёртого случая подряд: модуль 26 написал второй урок про `Why
don't we`, модуль 28 — второе «Читаем приглашение» и второе «Согласиться и
отказаться», модуль 29 должен был писать про `when`, который целиком есть в
модуле 11, модуль 30 поставил уроки умений на доску объявлений, занятую
модулем 9. Четыре раза из шести. Курс большой, и новая тема почти наверняка
где-то задета — значит спрашивать надо сперва его, а уже потом источник.

## В English Grammar Profile ищи по всему файлу, а не по подкатегории

Правило приписано 29 августа 2026, после модуля 29.

Я объявил в шапке модуля и в программе «расхождение источников»: будто первое
условное (`if` + настоящее + `will`) стоит у English Grammar Profile на B1, а
берём мы его по руководству A2 Key. **Это было неверно.** `will` после `if`
стоит там на **A2 двумя графами**, и обе лежат ВНЕ подкатегории `conditional`:
`MODALITY will`, «FORM/USE: AFTER 'IF', FUTURE SITUATIONS» и `PRESENT present
simple`, «FORM/USE: REAL AND IMAGINED SITUATIONS AFTER 'IF'». Третья графа,
`CLAUSES subordinated`, подпирает и обратный порядок половин.

**Причина: я смотрел строки с `SubCategory = conditional` и остановился на
том, что нашёл.** Ровно эта ловушка уже записана выше — про `will` в модуле 9:
«тот же файл держит ещё 13 строк MODALITY will». Через двадцать модулей я
повторил её тем же способом.

**Источник раскладывает один оборот по разным разделам** — по слову, по
времени и по виду придаточного. Любая одна подкатегория даёт неполную картину,
и вывод «на этой ступени такого нет» из неё делать нельзя.

## Правка ради мёртвой карточки ломает счёт в разборе

Правило приписано 29 августа 2026, по двум случаям за одну сессию.

`check:content` сообщает сведениями: карточка не встречается в модуле нигде,
кроме себя самой. Починка простая — дописать строку в пример, чтобы слову было
где работать. И дважды подряд разбор под примером оставался считать
по-старому:

- модуль 29: дописал четвёртую строку ради `because of`, а разбор остался
  писать «в третьей и четвёртой стоит if» — пропустив вторую, где `if` тоже
  стоит;
- модуль 31: дописал строку ради `dot`, а разбор остался перечислять три
  хвостика при четырёх.

**`sosedi` этого не ловит:** он смотрит поля блока, а устаревает числительное в
соседнем поле того же блока. Значит: **дописал строку в пример — перечитай
разбор под ним.**

И второе следствие: если слову негде работать без выдуманной строки, верный
исход — снять карточку, а не дописывать строку. В модуле 31 так и вышло.

## О ненаписанном модуле не утверждаем ничего, даже мимоходом

Правило приписано 6 сентября 2026, на приёмке первого урока модуля 3 нового
курса.

Случай 5 говорил ученику: «В модуле 2 после имени стояло одно только `is`». А
шапка `privetstvie.ts` объявляет, что модуль 2 вводит притяжательные оборотом
`Dana is my friend` — то есть после имени там стоит не «одно только `is`».

**Чем это опаснее обычной ошибки.** Утверждение грамматически верно, источникам
не противоречит и в день написания даже похоже на правду. Оно становится ложным
**без единой правки** — оттого, что кто-то другой допишет свой файл. Ни один
скрипт такого не поймает: проверка ссылок смотрит, существует ли адрес, а не
верно ли сказанное о том, что по адресу лежит.

**Ссылка на наш модуль законна — но только на НАПИСАННЫЙ.** Связь между модулями
живёт в `egpPovtor` и в замысле, а не в тексте урока.

## Сошедшийся счёт не доказывает, что считали то самое

Приписано там же и уточняет давнюю проверку «разбор примера считает строки —
сходится ли счёт».

Разбор сцены говорил: «В каждом ответе после формы be стоит `from`». В сцене
четыре реплики, строк с `from` — четыре, **числа сошлись**. А признак был не тот:
в двух репликах после формы be стоит имя (`I am Dana`, `I am Kim`), и «ответов»
со словом `from` четыре, но это ДРУГАЯ четвёрка.

**Совпадение чисел и сделало промах невидимым.** Проверка `check:content` сверяет
числительное с числом строк примера — здесь она промолчала бы, потому что счёт
верен, а лжёт признак.

**Отсюда: у числительного в разборе спрашивают не только «сколько», но и «чего».**
Машине это недоступно; ловится чтением.

## `accept` может принимать неразобранное, а разбор — не может

Приписано там же. Задание принимало короткую запись `Dana's from Astana.`, и
разбор её называл — а случай о короткой записи стоит экраном ПОЗЖЕ.

Разведение простое и работает всюду: **`accept` служит ученику, и наказывать за
правоту нельзя — значит он вправе принимать то, чего урок ещё не разбирал.
Разбор УЧИТ, и неразобранного содержать не может.** Молчащий `accept` — законный
приём, а не недосмотр.

## Перевод карточки не должен говорить больше, чем говорит источник

Правило приписано 6 сентября 2026, на замысле модуля 7 нового курса.

Методист развёл пару `clock` и `watch` переводом: карточка `clock` получила
**«часы (настенные)»**. Толкований ни один файл `materials/` не содержит, поэтому
скобка была решением преподавателя — но написана она была так, будто это
словарная помета.

**Проверка в Викисловаре скобку сняла.** Первое значение `clock` — «A chronometer,
an instrument that measures time, particularly the time of day»; разницы с
наручными часами оно не даёт вовсе, а примечание говорит, что сегодня `clock`
это общее имя для приборов времени. То есть перевод СУЖАЛ то, что источник даёт
широким.

**Чем этот случай поучителен: «часы (настенные)» прошли бы любую нашу проверку.**
Ступень верна, слово в словнике, по-русски написано правильно, карточка работает
в примерах. `check:content`, `kontrol` и `programma` не видят такого и видеть не
могут — они меряют ступень и целостность, а не объём утверждения.

**Отсюда правило.** Скобка в переводе — это утверждение о языке, и подпирается
она так же, как всякое другое: графой источника или выпиской. Нет опоры — перевод
остаётся широким, а вещь показывает значок: **картинка отвечает «какая именно»
точнее скобки и ничего не сужает на письме.**

И обратная сторона, которую тот же методист назвал сам: **если двух английских
слов на одно русское модуль не тянет, второе слово просто не берётся** — но
причина записывается честно («модуль не тянет»), а не задним числом («мы их
отмежевали»).

**И правило, выведенное им же на следующем круге: карточка — не словарная
статья.** Если английское слово ШИРЕ русского (`desk` это и парта, и стойка;
`thing` это и штука, и дело), перевод остаётся широким или получает второе
русское слово (`picture` — «картина, картинка»). Граница называется не в
карточке, а там, где ученик на неё наткнётся. **Расширение перевода законно,
сужение требует опоры.**

## Голос человека в курсе один, и держится он порядком реплик

Правило приписано 6 сентября 2026 методистом модуля 2 — о собственном замысле, и
до того, как тот попал в код.

**Голос назначается ЧЕРЕДОВАНИЕМ РЕПЛИК, а не человеком.** Первую по умолчанию
читает женский, дальше голоса чередуются; порядок сдвигает поле `pervyyGolos`.
Значит один и тот же человек в разных сценах получает разные голоса — надо лишь
поставить его реплику на другое место.

Так и вышло. Имя `Kim` взято потому, что руководство прямо зовёт его годным и
мальчику, и девочке (Young Learners, с. 15), — и курс вывел из этого «имя не
навязывает пол». **В тексте не навязывает. В записи навязывает неизбежно:
нейтрального голоса не бывает.** В модулях 1 и 2 Ким читает мужской голос, а в
сцене, написанной для урока 4, ему доставалась женская очередь: ученик услышал
бы, что человек сменил пол между уроками.

**Решение: за каждым человеком закрепляется голос, и держится он порядком
реплик.** За `Kim` — мужской, тот, что уже звучит.

**У БЛОКА ЗАПИСИ ЭТОГО ПОЛЯ НЕ БЫЛО ВОВСЕ — до 6 сентября 2026.** `pervyyGolos`
читался только в ветке примера-разговора, а у `audio` число голосов решало поле
`voice` и больше ничего. Значит первую реплику записи всегда читал женский голос.

Нашёл сборщик модуля 5: администратор гостиницы мужской в трёх уроках, где
разговоры собраны примером, — а в записи четвёртого он стал бы женщиной, а
гостья мужчиной. Поле заведено, старые записи не тронуты.

**Проверяется это ТОЛЬКО СЛУХОМ.** Имя звукового файла считается от текста и от
числа голосов; порядок голосов в него не входит вовсе. Значит и
`npm run check:content`, и `npm run ozvuchka` промолчат при любом раскладе — они
видят, что файл есть, а не что в нём звучит. Порода известная: на ней проект
обжигался трижды (примеры-разговоры, блоки `audio`, ряд букв через запятую).

**Путь, который отвергнут не навсегда:** дать голос отдельным полем у каждой
реплики. Это сняло бы ручную подгонку, но требует правки типов и правки
`scripts/ozvuchka.mts`. Пока разговоров немного, порядок реплик дешевле.

**И тут же поправка к тому, что стояло здесь раньше.** Прежняя запись говорила,
что такой путь требует «включения раскладки голосов в отпечаток записи, иначе
старые файлы останутся звучать по-прежнему», то есть переозвучки всех разговоров
курса. **Это неверно, и проверено прогоном 6 сентября 2026.**

Отпечаток записи считается от ГОТОВОГО ТЕЛА SSML (`scripts/ozvuchka.mts`,
`main`), а разметка голосов в это тело уже входит. Значит блок, у которого нового
поля нет, даёт то же тело, тот же отпечаток и не переделывается вовсе. Поле
`pervyyGolos` завели у блока `audio` — переозвучено ноль записей.

**Урок шире одного поля: „это потребует переозвучки всего курса" — такое же
утверждение о коде, как всякое другое, и проверяется прогоном.** Сказанное
чтением, оно полгода отпугивало от дешёвой правки.

## «Не нашёл, искал там-то» относится и к соседним замыслам

Приписано 6 сентября 2026 методистом модуля 8 — о трёх собственных строках,
написанных не открыв файла.

Он написал «замысла модуля 7 не существует» — тот существовал и был старше его
собственного. Написал «карточку `teacher` просят себе оба соседа» — оба от неё
отказались. И написал «сборщику проверить, чья она» — проверялось двумя `grep`.

**Правило „никогда не писать «в источнике этого нет»" писалось про `materials/`,
и потому казалось, что о документах соседей можно говорить по памяти.** Нельзя:
замыслы пишутся параллельно, устаревают за часы, и утверждение о чужом документе
проверяется его открытием так же, как утверждение об источнике.

## Словарь курса меняется под руками, и замысел верен только на дату

Правило приписано 6 сентября 2026 методистом модуля 4, и он назвал его о том,
что увидел за одну ночь работы.

**Утром в файле модуля 2 не было ни одной карточки, к вечеру их стало четыре.**
Слово `Hi` в его записке стало законным не потому, что он что-то узнал, а
потому, что сосед дописал свой файл.

**Отсюда: замысел верен на дату, и не позже.** Перед сборкой каждого урока
словарь сверяется ЗАНОВО — по коду, а не по строке замысла, даже если замысел
написан вчера и проверен вторым методистом.

Это оборотная сторона правила «замысел соседа — такая же часть курса, как код»:
пока модули пишутся параллельно, обе стороны движутся, и снимок устаревает с
обеих сторон сразу.

**Механическая половина у этого есть**, и ею стоит пользоваться:

```bash
grep -n "term: \"<слово>\"" courses/<курс>/*.ts   # в каком файле карточка
grep -n "<файл>" courses/<курс>/index.ts            # какой это модуль по счёту
```

## Замысел соседа — такая же часть курса, как код

Правило приписано 6 сентября 2026 методистом модуля 6, и он назвал его о
собственной недоделке.

Правило «проверяя строку программы, ищи её предмет в самом курсе» он исполнил:
открыл замороженный курс, открыл новый, пересчитал карточки по коду. И всё равно
не знал, что слова `we`, `they` и `our` переданы ему соседями, — **потому что
передача записана не в коде, а в чужих замыслах**: `plan-modul-2.md`, раздел 1.3,
и `plan-modul-5.md`, строки 401–409.

Его объяснение точное: «Я читал соседние замыслы только на предмет грамматики, а
словарные передачи не смотрел вовсе».

**Отсюда: пока модуль не написан, его замысел И ЕСТЬ модуль.** Проверка по курсу
обязана читать не только `courses/`, но и `docs/beginner-2/plan-modul-*.md` —
и читать целиком, а не по своему разделу. Слово, отданное соседу строкой чужого
документа, для кода невидимо: в `grep` по `term:` его нет и не будет, пока модуль
не написан.

Та же порода дала в тот день ещё три места: замыслы модулей 3 и 5 держали
перечни слов снятого модуля; модуль 5 объявил своим пункт, который вводит модуль
2; модуль 4 и модуль 5 порознь считали, что закрывают один дескриптор.

**И следствие для сводящего:** соглашение, записанное у одного из двух, через
месяц становится ничьим. Всякую передачу между модулями записывают ОБА — тот,
кто отдал, и тот, кто взял.

## Находка о чужом модуле не доезжает до него сама

Правило приписано 30 августа 2026, общим проходом по ступени.

Модуль 26 при написании заметил, что модуль 25 вычитал у графы `must` запрет,
которого там нет, и **записал это у себя в шапке**: «в модуле 25 я вычитал у
графы `must` запрет, которого там не было, и модуль сам его нарушал девять
раз». До самого модуля 25 правка не дошла — там запрет так и стоял, а текст
чтения, таблица, образец письма и работа его нарушали двенадцать раз.

**Причина простая.** Находку записывают там, где её сделали, — в файле, над
которым работают. Чужой файл при этом не открывают: он не в правке.

**Отсюда: находка о чужом модуле правится в чужом модуле сразу или не правится
никогда.** Если сейчас нельзя (чужой файл читает проверяющий), запись идёт не в
шапку своего модуля, а в записку дня — оттуда её достанут.

Та же порода дала ещё три места за один проход: модуль 19 объявил `already`
снятым, и слово осталось живым в другом уроке того же модуля; модуль 17 нашёл
подстановку шкалы у себя и попросил разобрать модули 14 и 15 — за две недели
никто не разобрал; счёт карточек в шапке не пережил добавления слова в восьми
модулях из тридцати одного.

## Слово «всегда» в правиле — признак выдуманного запрета

Раздел «Графа — это разрешение» выше говорит то же самое, и всё равно за один
общий проход нашлось **три новых случая**, каждый с заданиями и вопросами
работ, требовавшими «исправить» безупречный английский:

| Модуль | Выдуманный запрет | Чем опровергнут |
|---|---|---|
| 7 | «после `while` **всегда** длительное действие» | примером той самой графы A2, которую модуль цитирует |
| 25 | «о третьем лице — **только** `has to`» | собственным текстом модуля, двенадцать раз |
| 22 | «у неправильных глаголов третья форма **тоже своя**» | тремя глаголами из десяти, которые даёт сам курс |

**Что из этого следует для работы.** Правило «графа — разрешение» знать
недостаточно: я его знал и трижды нарушил. Работает механический признак —
**остановись на словах «всегда», «только», «тоже», «не бывает», «нельзя» и
спроси, какая графа это ЗАПРЕЩАЕТ.** Если такой графы нет, а есть лишь графа,
разрешающая другое, запрета быть не должно.

И вторая половина: **запрет проверяется по собственному тексту модуля.**
Модуль 25 нарушал свой же запрет двенадцать раз — этого хватило бы, чтобы
увидеть ошибку без единого источника.

**И ТРЕТЬЯ ПОЛОВИНА, приписанная 6 сентября 2026: нарушение бывает не в своём
файле, а в соседнем модуле.** Модуль 7 нового курса собирался сказать «`that` —
о вещи подальше», и в его собственном тексте нарушений не нашлось бы ни одного.
А модуль 1 уже говорит `How do you spell that?` — о только что прозвучавшем
слове, то есть о близком, — и графа `USE: 'THAT', ALREADY MENTIONED` названа в
его опорах поимённо.

Ученик, прошедший модуль 1, получил бы противоречие внутри своего курса, а
проверка «grep по своему файлу» промолчала бы. **Значит запрет ищут в курсе
целиком, а не только в модуле, который его пишет.** Нашёл методист модуля 7 и
починил не смягчением слова, а восьмым случаем урока: ученику объясняют, почему
в модуле 1 стояло `that`, а не `this`.

## Близнец чинится сменой ВИДА, а не заменой слова

Правило приписано 2 сентября 2026, после разбора методистом средней зоны
близнецов Beginner. Из 33 мест настоящих оказалось тринадцать, и **все
тринадцать одной породы**: работа берёт урочный `gap` и меняет ТОЛЬКО имя или
предмет, оставляя ту же рамку, ту же строку и ту же ветвь правила.

**Дважды это уже чинили — и порода пережила обе правки.** В коде так и стояли
комментарии: «Взяты другой предмет и другое место», «Имя заменено, чтобы
проверялось правило, а не память». Менялось существительное, а действие
оставалось прежним, и ученик по-прежнему вписывал ответ по памяти.

**Отсюда: замена слова близнеца не снимает.** Снимает смена того, что ученик
должен СДЕЛАТЬ: исправить чужую запись, собрать строку, назвать причину, назвать
место. Ответ при этом может остаться прежним — меняется дорога к нему.

Осторожно с двумя проверками, которые тут же начинают ругаться: доля вопросов,
требующих произвести ответ, должна остаться больше половины (`gap` → `choice`
её роняет), и верные ответы в работе должны стоять на разных местах.

## Карточка — там, где слово работает, а не там, где названо

Мерка методиста от 2 сентября 2026, записана в `npm run kontrol`.

**Перевод, стоящий рядом со строкой, введением слова НЕ считается.** С 31 августа
перевод есть у каждой английской строки курса, и если считать его введением,
проверка перестанет находить что-либо. Карточка даёт то, чего перевод не даёт:
словарную форму в отрыве от строки, транскрипцию, отдельную запись звука и место
в словаре урока. Правило владельца от 16 августа требует, чтобы слово ступени
ученик ПОЛУЧИЛ, а не понял по месту.

**Исключение одно и узкое: имя нашего урока или модуля.** Строка «Полностью
разберём его в уроке „Без повтора: the pale one"» — это адрес обещания, которого
правила сами и требуют; английские слова стоят там внутри русской фразы как
название, и ученик не читает их как слова. Скрипт вырезает из текста подстроки,
дословно совпавшие с заголовком урока или модуля этого же курса.

**И третье, что показал тот же разбор:** проверка ловит не только долг словаря,
но и ФОРМУ ВПЕРЕДИ КУРСА. Модуль 13 употреблял `found`, а пара `find — found` в
десяток неправильных глаголов модуля 3 не попала вовсе — то есть ученик читал
форму глагола, которого не получал никак. Шапка модуля 21 об этом уже писала, и
до модуля 13 находка не доехала: находка о чужом модуле не доезжает сама.

## Английская строка в ПРАВИЛЕ случая даёт вторую кнопку звука

Правило приписано 6 сентября 2026 редактором модуля 2, и оно о коде, а не о
языке.

`sZvukom` в `components/lesson/Ekran.tsx` прогоняется **и по имени случая, и по
каждой строке правила**, а не только по примерам. Значит английская строка,
названная в `zvuchat`, ставит кнопку везде, где она напечатана: скажи в правиле
«…и получается `I am Alim`» — и на экране появится вторая кнопка рядом с той, что
у примера.

**Ученик видит две одинаковые кнопки на одном экране и не понимает, чем они
различаются.** Ни одна проверка этого не видит: ключ на месте, файл есть, текст
верен.

**Отсюда для редактора: правило говорит О строке, а не повторяет её.** Вместо «и
получается `I am Alim`» — «а имя называем после него»; вместо «передаёт строка
`My name is Alim`» — «строка, которая начинается словами `My name is`». Восемь
правил модуля 2 переписаны ровно поэтому.

**И заголовок случая тем же поражается втройне:** имя «Имя спрашиваем строкой
`Your name, please?`» дало бы три кнопки на экране.

## Имя рассказчика не стоит ни в одной строке его же сцены

Правило приписано 6 сентября 2026 методистом модуля 6.

Сцена рассказывалась от первого лица, говорящим был назначен Ким — и в его же
строках стояло `Kim is my best friend.` **Ким не может назвать лучшим другом
самого себя.** Нашёл сборщик, собирая урок.

**Проверка простая и механическая: найди имя говорящего среди слов строк
сцены.** Если оно там есть — либо говорит другой, либо в строках другие имена.

## Помета «редактору решить» — не починка

Приписано там же. Методист трижды пометил строки замысла словами «осторожно,
редактору решить» — и оставил их первыми в списке. **Редактор берёт то, что
напечатано:** помета живёт в замысле, а на экран уходит строка.

Отсюда: сомнительную строку либо чинят в замысле, либо снимают. Оговорка рядом
с ней ничего не меняет.

## Три вещи, которых не видит глаз, но видит тридцать строк разбора

Тот же методист написал в замысле, что оба найденных столкновения «ловятся
разбором в три строки», — **собрал разбор и нашёл три вещи, которых не видел
сам**:

1. **тело урока осталось прежним, когда разделы решений уже поправлены** —
   документ противоречил сам себе дважды, и редактор взял бы то, что стоит
   первым;
2. **`bus` не имеет карточки ни у кого**, хотя дважды объявлен «словом, которое
   ученик уже получил». А значит запасной выход, предложенный на случай отказа
   от двух других слов, стоял на таком же незакарточенном слове.

Устройство разбора, которое стоит повторять: **словарь берётся из кода, а не из
замысла**; окончание `-s` снимается только если слово без него известно; строкой
считается кусок с двумя и больше английскими словами. **Испытан порчей** —
подставленная строка нашлась; без условия про `-s` настоящая находка утонула бы
среди `parents`, `friends`, `grandparents`.

И честный предел: разбор не отличает перечёркнутую заготовку от строки урока.
Когда замысел пойдёт в код, остаток должен стать пустым.

## Свод собственного документа собирается разбором, а не глазами

Правило приписано 6 сентября 2026 методистом модуля 3, и он вывел его о своей же
сводной таблице.

Он написал восемь замыслов уроков и свёл их в таблицу «урок × вид задания»,
чтобы проверить, не идёт ли серия одним строем. **Второй методист нашёл в этой
таблице четыре ошибки, и все четыре считались по самому же документу:** строка
приписывала уроку вид задания, которого там нет, и отнимала тот, что есть;
столбец, полный во всех девяти уроках, стоял в перечне пустых мест; признак
«этот вид не в каждом уроке» был ложен.

**Заплатками это не чинится:** правя таблицу, автор снова сверяется с памятью о
том, что написал двумя экранами выше, — тем же способом, каким её и сломал.
Методист переписал документ целиком и собрал таблицу **скриптом**: разбор идёт
по заголовкам уроков, из таблиц заданий берёт вид и считает.

**Отсюда правило: если свод нельзя получить разбором документа — его нельзя и
проверить.** Всякая сводная таблица о собственном тексте (сколько случаев, какие
виды заданий, что где вводится) либо собирается машиной, либо не считается
проверкой.

Порода известная и записана выше дважды — «чистый отчёт не доказывает» и
«сошедшийся счёт не доказывает, что считали то самое». Здесь третий её вид:
**свод, составленный вручную, доказывает только усердие составителя.**

## У задания «о себе» границу задаёт экран, а не поле принимаемых ответов

Правило приписано 6 сентября 2026 на модуле 7.

Задание просило назвать вещь, которая есть рядом с учеником, и принимало шесть
слов словаря. **А урок называет вдвое больше вещей, чем даёт карточками:** ещё
шесть стоят в примерах случаев. Ученик с книгой на столе написал бы
`It's a book.` — верную строку по нашему же уроку — и получил бы «пока не так».

Методист чинил это сужением списка принимаемых ответов и не заметил второй
половины. Редактор починил иначе: **сузил подпись, которую ученик ВИДИТ**, — не
«вещь этого урока», а «вещь, названная в словах урока», где слов ровно шесть.

**Отсюда общее: список принимаемых ответов ученику невидим, и сузить им ничего
нельзя.** Границу открытого задания задаёт только то, что напечатано на экране.

## Проверке на близнецов нельзя верить, пока модуль не дописан

Приписано там же, и это третий случай породы «проверка отвечает на свой вопрос,
а не на мой».

`npm run bliznetsy` по модулю с незаполненными заглушками показывает совпадение
**заглушек**, то есть меряет однообразие рыбы, а не текста. Два «близнеца»
модуля 6 оказались ровно этим.

**Верить такому отчёту нельзя ни в какую сторону** — ни красному, ни зелёному:
настоящие близнецы он в том же модуле пропустит, потому что текста ещё нет.

## Пример словарной карточки — это «напечатано выше» при любом расстоянии

Уточнение приписано 6 сентября 2026 методистом модуля 6.

Мерка «строка-ответ задания не совпадает дословно ни с одним примером урока»
смягчалась расстоянием: урок идёт по шагам, назад ученик обычно не
возвращается, и от случая до задания пятнадцать экранов.

**Но в СЛОВАРЬ ученик заходит нарочно.** Карточка `brother` с примером `He is my
brother.` стояла за пять экранов до задания, чей ответ — та же строка. Такое
задание проходится открытием словаря и не проверяет ничего.

**Отсюда: пример карточки считается напечатанным выше всегда, сколько бы экранов
ни лежало между ними.** Расстояние смягчает повтор из объяснения; повтор из
словаря оно не смягчает.

И побочная выгода починки, которую тот же методист назвал сам: пять новых строк
задания — это пять новых сочетаний из тех же восьми слов. **Ступенька
«произвести» тем и отличается от «узнать», что ученик СОБИРАЕТ невиданное из
разобранного**, а прежние задания просили вспомнить строку.

## В задании на сличение все строки стоят в одинаковом отношении к материалу

Правило приписано 6 сентября 2026 методистом модуля 3 — о собственной правке,
сделанной кругом раньше.

Задание просило отметить строки с ошибкой. Оба верных ответа повторяли строки,
напечатанные в уроке (`I am from Spain.` — случай 2, `She is from Madrid.` —
случай 4), а оба неверных не повторяли ничего. **Отмечать надо было ровно те
строки, которые ученик где-то видел** — и различие бралось не из заглавных букв,
которым учит урок, а из памяти об экранах.

**Хуже всего, что это создала предыдущая правка.** Кругом раньше отвлекающий
`Kim is from Almaty.` был заменён, потому что дословно повторял пример урока и
пропускался по узнаванию. Довод верен — а результат оказался хуже исходного:
эхо перестало быть шумом и стало сигналом.

**Отсюда: в задании на СЛИЧЕНИЕ либо все строки повторяют напечатанное, либо ни
одна.** Смешанное отношение отличает ответы от не-ответов признаком, не имеющим
отношения к языку.

Починка проверяется тем же вопросом: если все четыре строки эхом повторяют
урок и две из них испорчены, узнавание перестаёт быть обходом — чтобы им
воспользоваться, надо вспомнить написание и сличить, **а это и есть задание**.

**Правило узкое нарочно.** К заданию на смысл оно не относится: там повтор из
материала законен и даже нужен.

## У отвлекающего ДВЕ проверки, и вторая — по языку вообще

Правило приписано 6 сентября 2026, на замысле модуля 8 нового курса.

Правило ниже («поищи отвлекающий в собственном модуле») методист исполнил: все
четыре его неверных варианта модуль не подтверждал. И всё равно **два из четырёх
оказались правильным английским**: `two milks` и `a bread` в кафе говорят —
это порции, и Викисловарь помечает такие слова `countable and uncountable` с
формой множественного.

**Первая проверка на это не отвечает и ответить не может.** Она спрашивает: «не
утверждает ли мой модуль того же самого?» Второй вопрос другой: **«а говорят ли
так вообще?»** — и своих знаний тут не хватает, нужна выписка из источника.

Порода дорогая: отвлекающий, оказавшийся правдой, наказывает ученика за верный
ответ, и найти это можно только зная язык лучше, чем знает автор.

**И третье следствие того же разбора.** Беда была не в отвлекающих, а в РАМКЕ
урока: он говорил «у этих слов нет `-s`», и ученик опроверг бы это не в жизни, а
**в модуле 22 своего же курса, первым же меню**. Отсюда: правило урока
проверяется не только по своему модулю, но и по тому, что курс скажет ПОЗЖЕ.

## Отвлекающий проверяется по тексту своего модуля

Правило приписано 2 сентября 2026, после того как я поставил в работу модуля 25
Elementary отвлекающий **«Бегать в бассейне запрещено»** — и он оказался
**правдой**: правила того же модуля говорят `You mustn't run indoors`, а соседний
вопрос той же работы требует ответить `run` на «чего делать нельзя». Работа
наказывала за верный ответ, а мой комментарий в коде хвалил этот отвлекающий как
«настоящий». Он и был настоящим — настолько, что стал верным.

**Причина та же, что у выдуманных запретов, только с другого конца.** Я сочиняю
неверный вариант, держа в голове предмет вопроса, а не текст модуля. Проверка
одна и стоит секунду: **прочитай отвлекающий как утверждение и поищи его в
собственном модуле.** Если модуль его подтверждает — это не отвлекающий.

Та же порода дала в тот день ещё три случая, все в новых строках: заметка
модуля 15 говорит, что самый маленький в зоопарке паук, а моя строка называла
кроликов; подпись, объявленная в работе модуля 30 «полной», не содержала
описания со словом `who`, которого требует список того же урока; варианты
модуля 27 говорили о знании при строке про пение.

**Общее правило:** новая строка работы сверяется не только со ступенью и
источником, но и **с материалом того модуля, куда она встаёт**.

## Слово проверяется по порядку модулей, а не по списку слов

Правило приписано 2 сентября 2026, после двух промахов за один день.

Я взял в работу модуля 4 Beginner строку `He is in the city.` — а артикль `the`
вводится в модуле 10, и до этой правки в модуле 4 его не было **ни разу в
видимом тексте**. И взял `such` в работу модуля 22 Elementary — карточка стоит в
модуле 24.

**При этом я проверял слова.** `npm run slovo` отвечает «с прошлой ступени» и
«уже дано» — но не отвечает, **в каком модуле** карточка и раньше ли он того
места, куда я пишу. `grep 'term:'` даёт файл, а не номер модуля: порядок живёт в
`courses/<курс>/index.ts`.

**Отсюда: у каждого слова новой строки спрашивается не «есть ли карточка», а
„есть ли карточка РАНЬШЕ этого модуля"** — и порядок берётся из `index.ts`.

```bash
grep -n "term: \"<слово>\"" courses/<курс>/*.ts   # в каком файле карточка
grep -n "<файл>" courses/<курс>/index.ts           # какой это модуль по счёту
```

## Чистый отчёт не доказывает, что порода снята

Правило приписано 2 сентября 2026. Я отчитался владельцу, что порода «работа
цитирует урочный текст» на Elementary **снята целиком**: `npm run bliznetsy`
показывал ноль. Методист прогнал ту же сверку своей меркой — семь слов подряд
вместо пяти — и нашёл **семнадцать мест**, из них одно в шестнадцать слов.

Это то же самое, что записано выше про мёртвые проверки, только с другой
стороны: **там чистый отчёт не доказывал, что содержание в порядке; здесь он не
доказывает, что порода вычищена.** Скрипт отвечает на свой вопрос со своим
порогом, а не на мой.

**Отсюда: утверждение „порода снята" не выводится из отчёта.** Либо считаешь по
курсу сам и говоришь, каким признаком считал, либо пишешь «отчёт чист», а это
не одно и то же.

## Строка, сочинённая ради карточки, — такая же строка урока

Правило приписано 30 августа 2026, после круга по четырём урокам слов.

Урок слов пишется от списка слов: берётся запись, и под неё сочиняется
предложение. Такая строка ощущается служебной — подставкой, чтобы слову было
где работать, — и потому её не проверяют на ступень. А ученик читает её
наравне со строками урока правил.

Четыре находки методиста, все из таких строк: `fell` (форму `fall — fell` курс
не даёт), `had to` за восемнадцать модулей до модуля 25, `another one` за
тринадцать модулей до модуля 20, `lying` — четвёртый случай написания `-ing`
там, где модуль 6 сказал «правил всего три».

**Хуже прочих `fell`, и вот чем.** Ту же форму прошлый разбор из модуля 7 уже
убирал, и об этом написано в шапке того же файла. Я читал шапку, писал урок в
том же файле — и вернул форму обратно. **Запись «это мы уже убирали» не
защищает: она стоит в шапке, а рука пишет в теле.**

Отсюда и второе, про словарь: **употребление карточки проверяется на ступень
вместе с самим словом.** Слово `since` стоит в словнике A2 Key — и потому
законно; но его единственный пример требовал Present Perfect, то есть B1 и
модуль 22 нашей же ступени. Карточка законна, строка — нет.

## Однородные уроки подряд повторяют один промах столько раз, сколько их

Правило приписано 30 августа 2026, тем же кругом.

Четыре урока слов писались один за другим по одному образцу. Промахи вышли не
разные, а **одни и те же, повторённые четырежды**: вопросы работ во всех
четырёх модулях оказались близнецами заданий тех же уроков; порядок видов
заданий одинаков во всех четырёх; рамки заданий совпали попарно; заголовки
всех четырёх звали ученику «часть» — то, чего он в платформе не видит.

Причина не в спешке. **Образец, взятый один раз, дальше воспроизводится сам, и
вместе с удачным в нём воспроизводится неудачное.** Проверяющий, позванный
после четвёртого урока, находит одну ошибку в четырёх экземплярах, и править
надо четыре места вместо одного.

**Отсюда: пишешь несколько однородных уроков подряд — зови проверяющих после
ПЕРВОГО.** Это не лишний круг: круг на серию всё равно один, просто он стоит
не в конце, а после образца.

## Проверяющие ходят по очереди, если один из них правит файл

Правило приписано 27 августа 2026, по второму случаю подряд.

У редактора есть право на правку, и он им пользуется разумно: опечатку дешевле
починить сразу. Но методисту при этом обещано, что файл не меняется, пока он
читает, — и дважды выходило, что менялся. В первый раз методист на это
пожаловался: разбор пришлось вести по рабочему дереву, а не по коммиту.

**Отсюда: либо редактор зовётся после методиста, либо ему прямо сказано только
докладывать.** Одновременно их можно пускать лишь тогда, когда оба читают, а не
пишут.

**Второй путь испытан 30 августа 2026 и работает.** Оба были позваны разом,
редактору прямо запрещено править файлы. Разборы вышли непересекающимися —
методист о ступени и источниках, редактор о понятности, — а одну находку
(задание требовало слова `skates`, которого курс не даёт) они получили
независимо друг от друга и с разных сторон. Круг при этом идёт вдвое быстрее.

**И ТО ЖЕ САМОЕ ОТНОСИТСЯ КО МНЕ, а не только к двум проверяющим. Приписано
4 сентября 2026.** Пока редактор писал объяснения урока об алфавите, я в том же
файле нарочно испортил два блока — испытывал новую проверку. Оба его блока в ту
минуту переписывались, моя строка `zvuk` встала перед его строкой `zvuk`, и в
объекте осталась вторая: **проверка промолчала, и я чуть не записал её мёртвой**.

Отсюда два правила:

1. **Файл, в котором работает редактор, автор не открывает на запись** — даже
   на одну строку и даже ради испытания.
2. **Новую проверку испытывают порчей на СВОЁМ файле, а не на живом уроке.**
   Правило «испытывать нарочно испорченным содержанием» никуда не делось, но
   портить надо то, что никто другой сейчас не правит.

## Как править содержание — решение владельца

**Содержание правится по одному месту с чтением окружения.** Скрипт с заменой
строк — только для механического по всему курсу, вроде предлога `at`.

Основание — счёт ошибок за одну сессию. Скриптовые правки дали: верную
английскую фразу, заменённую на бессмыслицу; три разбора, оставшихся от прежних
заданий; два сломанных файла; два одинаковых варианта в одном задании, доехавших
до боевого сайта. Все — оттого, что заменялась строка, а не читалось место.

Отсюда ещё четыре правила того же разбора:

1. **Никогда не писать «в источнике этого нет».** Писать «не нашёл, проверить» и
   называть, где искал. Утверждение об отсутствии ставит только методист,
   открывший файл. За сессию такое утверждение не выдержало проверки четырежды.

   **И к цитате всегда читается её пример.** Правило приписано 21 августа 2026,
   после самой дорогой ошибки того дня. Модуль 7 ступени Elementary ввёл правило
   «после `when` стоит только законченное действие» и трижды потребовал
   «исправить» безупречный английский. Опровергла его **та самая графа, на
   которой модуль стоял**: CLAUSES subordinated, A2, «FORM/USE: TIME» — пример
   источника «I feel comfortable **when I am wearing** these clothes». Цитата
   была выписана верно, в опоры вставлена верно, а вывод сделан обратный её же
   примеру. Описание в источнике общее, а **пример показывает, что засчитано**:
   выписал цитату — прочитай её пример и проверь, не противоречит ли он тому,
   что ты собрался запретить.

   **И не только запретить: цифра в графе тоже проверяется её примером.**
   Приписано 22 августа 2026, после третьего срабатывания этого правила. Модуль
   14 написал «длинным считают прилагательное от трёх слогов» — так стоит в графе
   A2 «FORM: WITH 'MORE'»: «usually three or more syllables». А единственный
   пример той же графы — `more careful`, и `careful` это ДВА слога. Значит три —
   частотное наблюдение с оговоркой «usually», а я превратил оговорку в порог,
   потеряв само слово «usually».

   Путаница вышла не в теории: тот же урок называл `polite` двусложным и сам же
   учил писать `more polite`, а соседний модуль одиннадцать раз писал
   `the quietest`, что по его правилу должно было быть `the most quiet`. Слова
   «usually», «often», «a limited range of» в графе — не украшение: они говорят,
   что правила там нет, а есть наблюдение.

   **И главное правило этого ряда, выведенное 21 августа после ТРЕТЬЕГО такого
   случая: графа источника — это РАЗРЕШЕНИЕ, а не запрет.** Каждая начинается
   словом «Can»: «Can use the present simple with 'I hope'…». Из неё следует
   «так можно», и ничего больше. Вывести «а иначе нельзя» из неё невозможно, и
   всякий раз, когда я это делал, курс начинал объявлять ошибкой безупречный
   английский.

   Три случая, все на ступени Elementary и все за два дня:

   - **модуль 7:** «после `when` стоит только законченное действие» — опроверг
     пример той же графы;
   - **модуль 10:** «есть время в строке — значит договорённость» — опровергли
     примеры графы `be going to`, где время стоит на всех ступенях, и наш же
     модуль 8;
   - **модуль 11:** «после `hope` не ставят `will`» — опровергли три работы
     учеников из того же файла, две из них A1, все сданы.

   Признак опасного места простой: **как только в тексте урока появляется
   «только», «не бывает», «нельзя»** — надо остановиться и спросить, какая графа
   это ЗАПРЕЩАЕТ. Если такой графы нет, а есть лишь графа, разрешающая другое, —
   запрета быть не должно. Учить надо своему случаю, а не отрицанию чужого.
2. **После любой правки — перечитать задание целиком** через
   `npm run zadaniya`. Не строку, а задание: условие, все варианты, разбор.
3. **Докладывать по `git diff`, а не по памяти.** Список правок, названный по
   памяти, однажды разошёлся с настоящим, и проверяющий разбирал не то.
4. **Любая правка видимого текста — новое содержание.** На сайт без
   проверяющего не идёт, независимо от размера.

## Виды уроков: правила, чтение, слушание, письмо, говорение

Модуль состоит из уроков правил, а в конце стоят уроки умений. Порядок в
модуле: правила → чтение → слушание → письмо.

**Чтение — в каждом модуле.** Один урок, один-два текста, задания к ним. Вид
текста задаётся полем `genre`: `notice`, `message`, `email`, `article`.
Проверка требует от `notice` шести слов, от прочих — пятнадцати.

**Слушание — в каждом модуле.** Там запись и есть предмет урока, поэтому
блоки `audio` в нём остаются (см. раздел о звуке).

**Письмо — только там, где источник называет жанр.** Не в каждом модуле:
шесть уроков на ступень Beginner. Основание — описания Совета Европы:

- **Эссе на A1 не существует.** CEFR Companion 2020, с. 68, шкала Reports and
  essays: графы A1 и Pre-A1 говорят `No descriptors available`, первые
  описания начинаются с A2. Если кто-то предложит «эссе для начинающих»,
  ответ здесь.
- Что источник даёт: с. 66 — «simple isolated phrases and sentences»; с. 67,
  Creative writing — описание комнаты и вещи; с. 83, Correspondence — открытка
  и короткое сообщение.

**Вопрос на слух отвечается одним словом, числом, датой или временем.**
Руководство Cambridge A2 Key, с. 29, раздел THE TASK: «The candidates should
only write ONE word, or a number, or a date, or a time for their answer.»
Остальные части слушания там — выбор из трёх или сопоставление. Описания Совета
Европы, с. 48, шкала Overall oral comprehension, A1: «Can **recognise** concrete
information… provided it is delivered slowly and clearly» — узнать сведение, а
не воспроизвести фразу.

Курс держал это правило сам собой во всех 25 модулях: ответы вопросов на слух —
`Petrov`, `16`, `book`, `evening`, `station`, `often`, `tea`, `driver`. 20
августа я написал четыре вопроса, где надо записать услышанное предложение
целиком, — они проверяли не слух, а набор текста и написание. Нашёл методист.

**Правила для урока письма, выведенные из приёмки:**

1. **Итог называется по проверяемой половине.** Письмо машина не оценивает, а
   каждый итог обязан спрашиваться работой. Поэтому итог говорит «находить»,
   «проверять», «различать», а не «писать».
2. **Образец обязан проходить собственный список самопроверки.** Пункт «в
   каждом предложении есть I'm» и образец, начинающийся с «Hello!», — ошибка.
3. **Порог `minWords` берётся по самому короткому ВЕРНОМУ ответу**, а не по
   образцу. Иначе ученик пишет ровно то, что просили, и читает упрёк.
4. **Жанр берётся из урока чтения того же модуля.** Тогда новых слов не
   нужно: ученик прочитал такой текст двумя уроками раньше. Где это не так —
   оговорить в `sources` (у открытки в модуле 25 текста для чтения нет).
5. **Урок письма не пересказывает устный урок модуля.** Проверить дословно:
   сравнить образец урока письма с образцом восьмого урока и с текстом урока
   чтения. Совпадение в трёх строках из четырёх — это один урок, а не два.

## Проверки, обязательные для учебных материалов

Делаются **скриптом, а не агентом**: тысяча заданий проверяется за секунду,
бесплатно и без ошибок.

- в задании на сборку фразы слова должны складываться ровно в ответ;
- в группе вариантов ровно один правильный;
- у заданий на вставку и сборку ответ не пустой;
- ссылки на правила ведут в существующие якоря;
- положение правильного ответа в банке вопросов распределено равномерно —
  однажды во всех двадцати вопросах теста верный ответ стоял первым, и тест
  проходился нажатием первой кнопки;
- подсказка и разбор проверочной работы не повторяют урок слово в слово;
- **имя урока уникально во всём курсе**, а не только внутри модуля: адрес
  урока модуля не знает, и второй урок с тем же именем недостижим;
- **опора в `sources` называет урок, который в модуле есть**: урок
  переименовали, а опору не поправили — и следующему проверяющему сверять не с
  чем;
- **верный ответ задания не напечатан во врезке выше него** — сведениями, не
  ошибкой: решает методист;
- **разбор примера считает строки — сходится ли счёт**: если в подписи или
  первом предложении разбора стоит числительное при слове «строк», «предложен»
  (а при слове-итоге «подряд», «во всех» — ещё и при «вопрос», «дело»,
  «глагол»), оно сравнивается с числом строк примера. Вопросом, не ошибкой:
  числительное может считать не строки. Написана после того, как правка примеров
  оставила девять разборов лгущими, и ни один скрипт этого не увидел;
- **все ли строки примера звучат** — сведениями: у примера законно бывают
  немые строки (русская подпись, дата цифрой, заголовок объявления), и скрипт
  их не считает. Ловит обратный случай к давней проверке: та ищет звук без
  строки, эта — строку без звука. Нашлось: образец письма с четырьмя строками
  и двумя кнопками, лежавший так с написания;
- **подписи блоков не повторяются внутри модуля** (кроме словарей, у которых
  «Слова урока» — условность). Ученик читает заголовки подряд и от двух
  одинаковых решит, что вернулся назад;
- **слово ступени введено карточкой, и не позже, чем впервые понадобилось** —
  `npm run kontrol`, по всему курсу сразу. Правило владельца от 16 августа не
  оставляет выбора: между ступенями нет ссылок на память, значит слово, которого
  прошлая ступень не даёт, ученик обязан получить здесь. Проверка появилась
  22 августа, когда методист нашёл, что модуль 17 стоит на парах `good — well` и
  `bad — badly`, а карточки нет ни у одного из восьми опорных слов; модуль 18 —
  то же с `water` и `bread`. Ни одна прежняя проверка этого не видела: `kontrol`
  смотрит, что ВВЕДЕНО в модуле, `chain` — на порядок внутри курса, а вопрос
  «получил ли ученик это слово хоть раз» лежит между ними. Разбор и что сделано
  — [docs/dolg-slovarya-elementary.md](docs/dolg-slovarya-elementary.md);
- **ссылка на наш модуль называет его тем именем, которое видит ученик** —
  `npm run check:content`. У модуля два имени: короткое служебное
  (`osobye-formy`) и видимое (`Неправильные глаголы`), и в тексте урока легко
  назвать первое. Модуль 22 отправлял ученика «в модуль „Особые формы“», а
  такой заголовок в курсе есть только у таблицы внутри модуля 15, и та про
  сравнение прилагательных. Нашли оба проверяющих независимо.

  **Первая редакция этой проверки была мертва, и это стоит помнить.** Я написал
  образец `(?:модул|урок)\w*` — а `\w` в JavaScript значит `[A-Za-z0-9_]` и
  кириллицы не знает вовсе. Проверка не находила НИЧЕГО, даже той самой ссылки,
  ради которой писалась. Та же ловушка, из-за которой когда-то не находилось ни
  одной кальки. Испытывать новую проверку нарочно испорченным содержанием надо
  именно поэтому: чистый отчёт бывает и у мёртвой;
- **разговор в примере разнесён по строкам, а не слипся в одну** —
  `npm run check:content`. Решение владельца от 19 августа: разговор звучит
  целиком и на два голоса, реплики делятся по строкам. Без поля `razgovor: true`
  и переводов строки страница показывает разговор одной строкой, а озвучка
  читает его одним голосом — владелец назвал это скороговоркой. Нашёл редактор в
  модуле 18, а прогон по курсу дал **тридцать три** таких примера в семи
  модулях; на ступени Beginner — ноль. Тонкость: тире внутри строки бывает и
  законным (`G — J` — пара букв), поэтому ловится только пример, который целиком
  одна строка;
- **словарная карточка встречается в модуле где-то ещё, кроме себя самой** —
  сведениями, не ошибкой: решает методист. Слово с одним придуманным примером
  ученик больше не встретит никогда, а норма ступени в тридцать карточек делает
  соблазн добрать их пустыми большим. Найдено руками дважды — восемь карточек в
  модуле 6 ступени Elementary и четыре в модуле 8, — и потому ушло в скрипт.
  Первый прогон по всему курсу нашёл ещё 86 мест; они ждут общего прохода.
  Поблажка в окончаниях нарочная: `degree` работает строкой `twenty degrees`;
  промолчать лучше, чем снять верное. **Поблажка чинилась четырежды**: выпадение
  конечного `-e` (`leave` — `leaving`), переход `-y` в `-ier` (`lazy` — `lazier`),
  удвоение согласной (`slim` — `slimmer`) и окончания `-er`/`-est` (`clever` —
  `cleverer`). На модулях о сравнении незнание последних трёх дало пятнадцать
  ложных срабатываний подряд: карточки работали ровно той формой, которой модуль
  и учит. **Проверка на однокоренные слова обязана знать словоизменение того
  предмета, который проверяет** — иначе она кричит на правильное;
- **у задания «исправь и запиши» стоит `exact: true`**, если вся ошибка сводится
  к регистру. Сверка по умолчанию регистр снимает, и дословно переписанная из
  условия ошибка засчитывается верной. Об этом стоит комментарий в самом
  `lib/content/check.ts` с пометкой «проверено опытом: засчитывало» — и всё
  равно 20 августа я написал такое задание снова. Первый же прогон новой
  проверки нашёл ещё одно, в экзамене ступени.

**Про последнюю отдельно, потому что она ловит самую частую ошибку 19
августа.** Во всех пяти уроках письма первое задание отвечалось не разбором
языка, а взглядом на два абзаца вверх: врезка называла ошибку и тут же давала
верную запись, а задание спрашивало ровно её. Методист нашёл это дважды подряд
— второй раз потому, что моя правка ПЕРЕНЕСЛА совпадение с неверного варианта
на верный.

Проверка смотрит только назад по уроку и только врезки: текст, пример и таблица
— предмет задания, повтор из них законен. Задания с `about` и `hottext`
пропускаются по той же причине. Первая редакция без этих оговорок дала 168
срабатываний почти сплошь на правильном — а скрипт, который кричит на верное,
хуже, чем никакого.

**Про последнюю проверку отдельно, потому что ошибка оказалась моей самой
устойчивой.** Я писал работу ПОСЛЕ заданий уроков и по их образцу: тот же вид
задания, то же условие, та же подсказка, менялось одно слово. В модуле 24 так
совпало девятнадцать подсказок из двадцати четырёх. Ученик тогда отвечает по
узнаванию вида задания, а не разбирая языковой факт заново, и работа перестаёт
что-либо показывать.

Когда проверку написали, нашлось **пятьдесят восемь таких мест в десяти
модулях**, из них один уже стоял на боевом сайте. Ни один проверяющий этого сам
не находил: методист смотрел на виды заданий, а совпадали слова.

Отсюда правило работы: **проверочную работу пишут до заданий уроков или
отдельно от них**, с другого угла — исправить чужую запись, назвать причину
ошибки, отличить по смыслу, собрать другой разговор.

**И ЭТА ПРОВЕРКА ЛОВИЛА НЕ ТО. Найдено 31 августа 2026.** Она сверяет
**подсказку и разбор**, потому что писалась против списанного разбора. А
совпадают чаще всего **условие и варианты** — методист нашёл это руками:
39 близнецов в трёх модулях ступени Beginner, из них четырнадцать дословных,
при чистом отчёте `check:content`.

Его черновик стал `npm run bliznetsy`. Первый прогон: **375 мест на Beginner и
99 на Elementary.** Разница объяснима — на Elementary породу уже вычищали
кругом.

**Сырое число заведомо завышено, и разбирать список надо сверху.** «Что
вписывают в поле Address?» против «В какое поле вписывают фамилию?» — вопрос
обратный, и это не близнец. Поэтому `--hudshie N` даёт худшие по силе признака:
сперва дословно совпавшие варианты, потом доля общих слов. Признак «совпал
вариант» сильнее любой доли — у худших совпадают все кнопки разом, включая
верную.

Это сведения, а не ошибка: совпадение бывает законным («Сопоставь букву и её
название» сказать иначе нельзя). **Решает методист, решённое кладётся в
`courses/resheno.ts`.** Заглушить свою же находку автор не вправе — список
нарочно не принимает `kto: "автор"`, и сборка на этом останавливается.

**КРУГ ПО НИМ ПРОЙДЕН 1 сентября 2026, и он дал две вещи важнее счёта.**

**Первое: работа списывает не только у ЗАДАНИЙ урока, но и у САМОГО МАТЕРИАЛА.**
Она вставляет в условие дословную строку из текста для чтения того же модуля и
спрашивает по ней — урок чтения переигрывается второй раз вместо нового умения.
Методист насчитал 32 таких места на Elementary. **И пряталась порода ровно тем,
что цитата ДОБАВЛЯЕТ СЛОВ:** доля общих слов делилась на больший из двух
наборов, значит росла с длиной условия и падала ниже порога. Модуль 24 давал
43% при дословно том же вопросе, ответе и списке `accept`.

**Второе, и это правило шире одного скрипта: ПРОВЕРКА НА СОВПАДЕНИЕ ОБЯЗАНА
ЗНАТЬ, ЧТО У ПРОВЕРЯЕМОГО ПРЕДМЕТА СОВПАДАТЬ ОБЯЗАНО.** Рамку вопроса на слух
задаёт источник (A2 Key, с. 29: одно слово, число, дата или время), рамку
`gap` и `short` — вид задания («Допиши недостающее слово»). Сказать их иначе
нельзя, и без поблажки проверка кричала на правильное. Опасен там не вид
задания, а ОТВЕТ.

Четыре починки по разбору методиста: партнёр выбирается по силе признака, а не
по доле слов; доля делится на меньший набор; тексты вариантов чистятся тем же
разбором, что и условие (настоящий близнец прятался за одной точкой в конце
строки); добавлена сверка с материалом. **После починок счёт вырос — проверка
нашла то, что пропускала.**

## Близнец на вставке машиной не ловится, и починить это нельзя

Правило приписано 6 сентября 2026, на приёмке первого урока модуля 2 нового
курса.

Я поставил в работу модуля заглушку — вопрос вида `gap`: `before: "Good "`,
`after: ", Bill."`, ответ `afternoon`. В уроке при этом стоит задание `gap`:
`before: "Good "`, `after: ", Kim."`, ответ `morning`. Та же рамка, та же ветвь
правила, изменены только имя и время дня — ровно то, о чём правило от 2 сентября
говорит «замена слова близнеца не снимает».

**`npm run bliznetsy` показал ноль.** И показал верно — по своей мерке. У
задания на вставку рамку задаёт САМ ВИД ЗАДАНИЯ: `before` и `after` короткие,
общих слов почти нет, а совпадать в них обязано. Поблажка на это стоит нарочно и
писалась после того, как проверка кричала на правильное. Убрать её — значит
вернуть крик.

**Отсюда: у `gap` и `short` близнеца ищут глазами, а не отчётом.** Признак у
человека простой и машине недоступный: ученик делает ОДНО И ТО ЖЕ ДЕЙСТВИЕ —
подставляет слово в готовую рамку, — и второй раз оно ничего не показывает.
Нашёл редактор, читая урок и работу подряд.

Это тот же урок, что записан выше про чистый отчёт, только с новой стороны:
**проверка отвечает на свой вопрос со своим порогом, и её ноль не значит, что
породы нет.**

## Проверка, написанная под модули, нового вида содержания не видит

Правило приписано 3 сентября 2026, когда методист впервые разобрал работы
частей — восемьдесят вопросов, простоявших сутки на боевом сайте.

**`npm run kontrol` и `npm run bliznetsy` обходили `course.modules` и в
`course.parts[].quiz` не заходили вовсе.** То есть долг словаря, форма впереди
курса и близнецы для этого файла не искались никогда. Отчёт при этом был чист:
ноль ошибок, ноль вопросов.

Хуже: у `kontrol` дыра была шире, чем у одних работ частей. Проверка долга
словаря обходила только `les.blocks` — значит **ни один банк вопросов** не
входил в счёт: ни работа модуля, ни работа части, ни экзамен. Методист нашёл
руками то, что она обязана была найти: `repair` требовался в части 1, а карточка
стоит в модуле 9; `windy` — в части 2, хотя модуль 13 снял это слово нарочно;
форма `paid` не даётся курсом нигде.

**Обе проверки научены видеть банки вопросов**, каждый на своём месте в порядке:
работа модуля сразу за его уроками, работа части после последнего модуля части,
экзамен в конце. `bliznetsy` при этом знает, что работа части могла списать у
ЛЮБОГО модуля своей части.

**И ЗДЕСЬ ЖЕ — ПРИМЕР ТОГО, ПОЧЕМУ ПРОВЕРКУ ИСПЫТЫВАЮТ ПОРЧЕЙ.** Первая
редакция расширения брала у вопроса только ОТВЕТ. Отчёт вышел чистым, и я чуть
не записал это победой. Нарочно возвращённая строка `I didn't repair the
printer` проверку не разбудила: английское слово вопроса живёт в трёх местах —
в ответе, в условии (там стоят строки для чтения) и в поле `zvuk`, том, что
ученик слышит. После починки проверка нашла порчу и назвала место.

Отсюда правило шире одного скрипта: **дописав новый вид содержания, пройди по
проверкам и спроси у каждой, видит ли она его.** Молчание проверки о новом виде
неотличимо от её одобрения.

## Безопасность — двадцать правил владельца

Список принесён владельцем 16 августа и лежит в
[docs/bezopasnost-principy.md](docs/bezopasnost-principy.md) с пометкой
состояния у каждого правила. Смотреть туда перед любой работой, которая
касается входа, чужих данных или обращений извне.

Два вывода оттуда действуют всегда:

1. **Репозиторий открыт всем.** Всё, что в него попадает, считается
   опубликованным завтра в газете. Однажды это уже подвело: пароль от учебных
   аккаунтов лежал открытой строкой в скрипте, а сами аккаунты жили на рабочем
   сайте.
2. **Пометка состояния ставится только после того, как открыл файл.** Где не
   проверял — пишем «проверить», а не «сделано» и не «нет». Правило то же, что
   и для источников учебных материалов.

## Как проверять качество уроков

Автор не принимает свою работу. Порядок приёмки — три ступени, каждая ловит
своё, и пропускать их нельзя.

**1. Скрипт** — `npm run check:content`. Механическая целостность: пустые
ответы, повторы имён, покрытие итогов, распределение верных ответов. Тысяча
заданий за секунду, бесплатно и без пропусков.

**1б. Сверка с источниками** — `npm run kontrol -- english-starter`. Ищет
каждую цитату из `sources` в самих материалах, сверяет словарь со словником,
строит таблицу «задание → блок», ловит задание, которое требует слова раньше
материала. Написан после разговора о том, что машинная проверка не заменяет
проверяющего: она снимает с него разобранное, чтобы внимание уходило на новое.

**Устроен он на трёх правилах, и без них был бы вреден.** Скрипт, который
кричит на правильное, хуже, чем никакого: его перестают читать, а потом
начинают «исправлять» верное.

1. Три уровня вместо одного: **ошибка** (неверно наверняка), **вопрос**
   (решает методист), **сведения** (без оценки).
2. **«Не нашёл» — это не «нет».** Неудачный поиск никогда не становится
   ошибкой: в этом проекте дважды вывод «такой строки в источнике нет»
   оказывался неверным.
3. **Разобранное молчит.** Решённые случаи лежат в `courses/resheno.ts` вместе
   с причиной. Запись без причины скрипт не принимает.

Чего он не умеет: отличить верную цитату из нужной графы от верной цитаты из
соседней. Это остаётся работой методиста навсегда.

**1а. Цепочка по всему курсу** — `npm run chain -- english-starter`. Не правила,
а картина: что употребляется часто и не объясняется нигде, что задание требует
раньше, чем дал словарь, какие обещания «разберём позже» остались без адреса.
Сборку не останавливает, разбирает её методист. Написан после того, как слово
`the` нашлось в курсе 418 раз без единого объяснения: проверка по одному модулю
такую дыру не видит в принципе.

**1в. Механические проверки русского языка** — `npm run yazyk`. Кальки, родовое
прошедшее время при обращении на «ты», повелительное наклонение на «вы»,
склонение при числах, знаки транскрипции не той условности. Редактору остаётся
понятность, тон и связность. Ему же готовится текст урока без кода:
`npm run --silent tekst -- english-starter <краткое имя модуля> > m.md`. Имя, а
не номер: номер скрипт молча не понимает и отдаёт пустой файл.

**Правило, выведенное из обеих новых проверок: новую проверку испытывают
нарочно испорченным содержанием.** Чистый отчёт сам по себе не доказывает
ничего. Так нашлись три мёртвых проверки: сверка со словником, где каждое слово
подбирало ступень A1 у соседней записи; поиск калек, где `\b` в JavaScript не
знает кириллицы и «дедлайн» не находился никогда; и то же с обращением.

**2. Два проверяющих агента** (описаны в `.claude/agents/`):

- **`metodist`** — состав, ступень, правильность объяснений, качество заданий.
  Обязан **открывать файлы в `materials/` и приводить цитаты**. Проверка по
  собственным знаниям запрещена: агент — та же модель, что и автор, и общий с
  ним пробел он не заметит.
- **`redaktor`** — русский язык: кальки, единое обращение к ученику,
  понятность, тон, склонение при числах.

Их работа не пересекается: методист не оценивает язык, редактор не оценивает
методику. Оба ищут ошибки, а не соглашаются.

**3. Владелец.** Финальная приёмка всегда за ним. Он преподаватель и видит то,
чего не видит модель: объяснение формально верное, но ученику непонятное.

## Откуда берётся содержание

Источники в `materials/` (описания Совета Европы, руководства Cambridge,
English Grammar Profile, словники) определяют **что** преподавать и **на какой
ступени**. Готовых уроков в открытом доступе нет, а текст учебников охраняется
авторским правом.

Поэтому: состав, порядок и уровень — из источников и сверяются по ним.
Формулировки объяснений и задания пишем своими словами: сведения о языке не
охраняются, тексты учебников охраняются.

**У «как объяснить это по-русски» источник СВОЙ, и он отдельный** —
[docs/istochniki-russkogo-obyasneniya.md](docs/istochniki-russkogo-obyasneniya.md),
заведён 30 августа 2026 по просьбе владельца. До него у курса были источники
состава и не было источника объяснения: `English Grammar Profile` отвечает «что
и на какой ступени» и больше ничего, поэтому объяснения писались пересказом
описаний уровня — тот самый «набор слов», на который владелец жалуется.

Оттуда берётся УСТРОЙСТВО (порядок частей, вид таблицы, перевод рядом со
строкой) и не берётся ни одного утверждения о языке: точность у образца хуже
нашей, он полон выдуманных запретов, и каждый разобран в файле с опровержением
из `materials/`. Разделение работ такое:

| Вопрос | Кто отвечает |
|---|---|
| Что преподавать и на какой ступени | `materials/` — EGP, Cambridge, CEFR |
| Какое слово на какой ступени | Oxford 3000/5000, словник A2 Key |
| Верно ли утверждение об английском | `materials/`, `istochniki-norma-yazyka.md` |
| **Как объяснить это по-русски** | **`istochniki-russkogo-obyasneniya.md`** | У каждого модуля во внутреннем поле
`sources` остаётся ссылка, по которой проверяющий может сверить.

Задания опираются на источники так же, как объяснения: формат — на образцы из
руководств Cambridge, лексика — на словник ступени.

## Команды

## Инструменты, поставленные 14 августа

**Два разбора источников вместо одного.** PDF в `materials/` разбираются дважды:
`npm run pdf` (pdf-parse, кэш в `materials/.text/`) и `npm run md`
(markitdown, кэш в `materials/.md/`). Разборщики портят по-разному: первый
теряет строение таблиц, второй местами рассыпает буквы. `npm run kontrol` ищет
цитату в обоих, и **это прямо бьёт по самой опасной ошибке проекта** —
утверждению «в источнике этого нет», которое семь раз оказывалось неверным.

Испытано нарочно: цитата, потерянная первым разбором, находится вторым, а если
убрать второй кэш — сверка её теряет.

**Важное следствие, которое надо помнить.** Два разбора одной таблицы иногда
**расходятся в том, к какой ступени относится строка**. На с. 79 описаний
Совета Европы пометка `A1` у них стоит в разных местах. Значит машинный разбор
таблицы вообще не доказывает ступень: спорные места решает методист, открыв
страницу. Раньше это молчало, теперь хотя бы видно.

**Навык `task-observer`** (`.claude/skills/task-observer/`) — наблюдает за
работой и предлагает, что дописать в правила и скрипты. Это то же правило
храповика, только замеченное машиной. **Вызывай его в начале сессии, где идёт
содержательная работа с инструментами.**

## Команды

```bash
npm run dev                 разработка
npm run build               сборка
npm test                    тесты правил
npm run check:content       проверки учебного содержания
npm run chain -- english-starter   цепочка уроков по всему курсу
npm run kontrol -- english-starter сверка модуля с источниками
npm run yazyk -- english-starter   механические проверки русского языка
npm run --silent tekst -- english-starter privetstvie > m.md  текст урока для чтения (краткое имя модуля, не номер)
npm run slovo -- castle zoo  справка о слове: ступень, словник, чтение, дано ли
npm run slovo -- --zapas 40  слова ступени, ещё не взятые курсом
npm run bliznetsy -- english-starter --hudshie 20   вопросы работ, повторяющие уроки
npm run programma -- english-beginner-2   темы словника и раздача пунктов грамматики
npm run perevody -- english-starter   сколько строк примеров без перевода
npm run perevody -- english-starter --chego   и каких именно
npm run sosedi              что правка задела, а сосед остался прежним
npm run sosedi -- HEAD~3    то же против другого коммита
npm run md                  второй разбор PDF (markitdown), нужен один раз
npm run pdf -- materials/файл.pdf --find "что искать"     поиск по источнику
npm run znachki             забрать значки Twemoji, названные уроками, в public/twemoji
npm run pamyat              снимок памяти агента в docs/pamyat (перед переездом)
npm run db:up               локальная база в Docker
npm run db:migrate          накатить миграции
npx next start -p 3010      проверка собранного сайта
```

## Выкладка: пуш ничего не выкладывает, и это проверено

Вопрос, висевший в записке от 20 августа («выкладка по пушу отключена нарочно
или просто не настроена?»), закрыт запросом к программному интерфейсу Vercel:
**у проекта поле `link` пустое — связи с репозиторием нет вообще**. GitHub и
Vercel не связаны никак, значит пуш не выкладывает и не выложит. Отправка в
GitHub нужна для того, чтобы работа доехала до другой машины, и только.

Выкладка одна:

```bash
npx vercel --prod --token <токен>
```

**Токен нужен от учётной записи** (vercel.com/account/tokens, Account Settings),
а не от проекта. Токен проекта `vcp_…` через программный интерфейс работает, а
CLI отвергает его: он сначала грузит пользователя и получает «User not found».
`VERCEL_OIDC_TOKEN` из `.env.local` не годится тоже — это короткоживущий ключ
для доступа приложения к внешним службам.

**Где он лежит и почему его дважды «не находили».** Токен учётной записи стоит в
`.env.local` строкой `VERCEL=`, но у неё **есть ведущий пробел, а значение взято
в кавычки**. Отсюда две потери времени 21 августа: поиск `grep "^VERCEL="` дал
ноль, и я доложил владельцу, что токена нет; а когда нашёлся — `vercel` отверг
его с «Must not contain: "\""». Доставать так:

```bash
VT=$(sed -n 's/^[[:space:]]*VERCEL=//p' .env.local | tr -d '\r' | head -1 | sed 's/^"//; s/"$//')
npx vercel --prod --token "$VT"
```

**И проверка после выкладки — запросом, а не на глаз.** Урок за замком отвечает
**307** (переадресация на вход), невыложенный — **404**. Этим и различают, что
доехало:

```bash
curl -s -o /dev/null -w "%{http_code}" https://codentalk.kz/learn/<курс>/<урок>
```

**Выложить часть модулей** можно только через список в `courses/<курс>/index.ts`:
временно убрать лишние из `modules`, выложить, вернуть строку. Другого способа
нет — выкладывается рабочее дерево целиком, а не коммит.

**И то, что легко забыть:** банк вопросов работ живёт в базе, а не в файлах.
Правки работ доезжают до ученика только через `npm run db:seed:courses`, сколько
раз сайт ни выкладывай. Уроки едут с кодом, работы — переносом.

## Перевод у каждой английской строки — и три оговорки к правилу

Правило 9 (`docs/format-uroka.md`): английская строка примера не стоит без
русского перевода рядом. К 31 августа 2026 обе ступени переведены целиком —
573 строки на Beginner и 776 на Elementary, — и мерку ведёт `npm run perevody`.

**Три оговорки вывел редактор на первых трёх модулях Beginner, и каждая иначе
повторилась бы двадцать два раза.** Потому круг и зовут после ПЕРВОГО модуля
серии, а не после последнего.

1. **Диктовка по буквам не переводится русскими буквами.** «Н-У-Р-Л-А-Н-О-В-А»
   отменяет урок, который учит, что `H` — это не «Н». Переводится смысл строки,
   буквы остаются латинскими: `W-A-T-E-R. Water.` → «W-A-T-E-R. Вода.»
2. **Строка со своим тире перевода не получает.** Перевод выводится на страницу
   через тире и с ним сольётся: `a doctor — an artist` дало бы четыре куска
   подряд. Такие пары переводит словарь урока или столбец таблицы.
3. **Перевод сверяется с разбором под ним и таблицей над ним.** Больше половины
   находок обоих кругов — этой породы: разбор велел отвечать целым предложением,
   а перевод давал «Вон там»; разбор просил заметить `they're` против `it's`, а
   местоимений в переводе не было ни в одном.

**Следствие из третьей, и его ловит машина:** одна английская строка с двумя
переводами в одном модуле заставляет ученика искать разницу, которой нет.
`Can I help you?` жил в модуле 23 тремя жизнями, причём третья стояла в
собственном разборе того же модуля. Считает `npm run perevody`, сведениями.

## КУРС ПЕРЕДЕЛЫВАЕТСЯ ЦЕЛИКОМ — решение владельца от 3 сентября 2026

**«Мне не нравится вообще ничего. Я бы не хотел учиться на этом сайте, если я
самостоятельно делаю. Потому что скучно и объяснения непонятны вообще. Нам нужно
всё переделывать, и строение модулей и визуальное составляющее.»**

Оба упрёка проверены счётом, а не приняты на слово, и оба подтвердились.

**Однообразие** ([docs/izuchenie-chuzhih-platform.md](docs/izuchenie-chuzhih-platform.md)):
29 модулей Elementary из 31 идут одним порядком «правила ×5 → чтение → слушание →
письмо», на Beginner 18 из 25; два вида заданий дают половину курса, четыре —
восемьдесят процентов; картинок две на всю ступень.

**Короткость объяснений** ([docs/istochniki-russkogo-obyasneniya.md](docs/istochniki-russkogo-obyasneniya.md)):
одна чужая статья про Past Simple даёт 42 раздела правила и 91 пример с
переводом, а наш модуль о том же ЦЕЛИКОМ, восемь уроков, — 51 строку объяснений и
5 примеров. **Их объяснение не длиннее нашего, оно ДРОБНЕЕ.**

### Новый формат урока — образец принят владельцем

Замысел — [docs/beginner-2/zamysel.md](docs/beginner-2/zamysel.md), образец —
`courses/english-beginner-2/`, вид — `npm run obrazec-urokov`, открывать
`docs/mockups/beginner-2-urok.html`.

1. **Объяснение дробится на НАЗВАННЫЕ СЛУЧАИ.** У случая имя, правило одной
   строкой и ДВА целых примера с переводом. Мерка на урок правил: не меньше
   восьми случаев и шестнадцати примеров. Счёт на одном предмете: случаев было 3,
   стало 23; строк с переводом было 24, стало 94.
2. **Урок начинается связным куском**, где форма живёт (приём Мёрфи), и только
   потом разбор.
3. **Задания идут лестницей** «узнать → произвести → применить к себе», а не
   вперемешку.
4. **Один экран — одна мысль.** Урок проходится по шагам, а не читается длинной
   страницей: 67 экранов на три урока.

Владелец: **«так интереснее да, как дуолинго»**. Сходство с Duolingo в одном и
нарочно — шаг за шагом. Счётчиков дней подряд, «жизней» и наказаний за ошибку не
берём: это против первого принципа продукта.

### Звук стоит у транскрипции, а не отдельным экраном — 4 сентября 2026

Владелец прошёл урок об алфавите и сказал: **«когда объясняешь звучание букв,
надо учитывать, что ученик не знает, как читать транскрипцию, поэтому звучание
букв тоже надо рядом добавить… рядом с самой транскрипцией или же буквой должна
быть кнопка звука, чтобы прослушать»**. И там же: **«отдельный экран убери»**.

Из этого две вещи в коде, обе общие для всего курса.

1. **У блоков `explain` и `note` появилось поле `zvuk`** — такое же, как у
   примера и таблицы: ключ это кусок строки как напечатан (`/biː/`), значение —
   что произнести (`B`). На странице рядом с куском встаёт кнопка. Значение
   задаёт имя файла, поэтому одно и то же название буквы в объяснении и в
   таблице — **одна запись, а не две**, и переозвучка не нужна.
2. **У блока `table` появилось поле `naTomZheEkrane`** — таблица едет вместе с
   предыдущим объяснением, а не отдельным шагом. Полем, а не догадкой по числу
   строк: граница «тут строк мало» выбиралась бы на глаз.

Ловушка, на которой это уже обожглись: **ключ, которого нет в тексте, — это
кнопка, которая никогда не появится.** Файл озвучится, проверка звука
промолчит, а на странице пусто. Ловит `check:content`, отдельной проверкой на
`explain` и на `note`.

### Объяснение не опирается на заголовок — 4 сентября 2026

Владелец: **«объясняй так: „У шести букв название начинается со звука /e/".
Потому что ученик не свяжет заголовок с объяснением»**. Стояло: «У шести букв
название с ЭТОГО звука и начинается».

Правило общее: **указательные слова («этот», «эти», «так», «они») не отсылают
наружу абзаца.** Заголовок ученик уже прочёл и держать его в памяти не обязан;
звук, буква и отношение называются в объяснении заново.

Оттуда же — слово **«заканчиваются», а не «кончаются»**.

### СТАРЫЙ КУРС ЗАМОРОЖЕН — решение владельца от 5 сентября 2026

**«Старый курс навсегда оставляем, я его не принимаю, ошибок много, но можем
пока оттуда брать нужное.»**

Речь о `english-starter` (25 модулей) и `english-elementary` (31 модуль) —
пятьдесят шесть модулей, выложенных на боевой сайт. Они **не чинятся и не
переделываются**: доживают, пока новый курс их не заменит, и служат складом
готовых кусков — уроков, текстов, записей, заданий, карточек.

Из этого три следствия для работы:

1. **Находки в старых курсах не превращаются в правки.** Разбор их не запрещён —
   счёт по ним показывает породы, — но чинить там нечего: всё уйдёт под нож.
2. **Все решения о структуре относятся к новому курсу** `english-beginner-2`.
   Там написано два урока из десяти в одном модуле, то есть мы почти ничем не
   связаны.
3. **Проверки по старым курсам гонять по-прежнему надо:** они лежат в общем
   отчёте, и красный отчёт мешает увидеть новое.

### УРОК ПИШУТ МЕТОДИСТ И РЕДАКТОР. АВТОР — ТОЛЬКО КОД

Решение владельца от 5 сентября 2026, сказанное дважды и второй раз резко:
**«НИЧЕГО НЕ ПИШИ. Это самое главное для тебя в новой схеме. Пусть весь урок
пишут редактор и методист»**.

Порядок работы:

| Кто | Что делает |
|---|---|
| **методист** | придумывает состав и порядок: сцена, случаи, задания, слова, итоги. Опирается на `materials/` и приводит цитаты со страницами |
| **второй методист** | разбирает замысел, которого не писал. Свою работу автор не принимает — с переносом замысла это правило перешло и на методиста |
| **владелец** | читает замысел до того, как в урок вложен день работы |
| **редактор** | пишет весь видимый текст: объяснения, врезки, подписи, условия и разборы заданий |
| **автор (я)** | перевожу в код: типы, экраны, звук, проверки, выкладка |

**Ни одной строки видимого текста от автора.** Ни объяснения, ни условия
задания, ни подписи, ни «поправлю одно слово». Повод: 4–5 сентября я вносил
разбор владельца сам, потому что текст был его, — и между его фразами встали
мои: «Букву прочитали как /es/», «Из букв P, K и T одна заканчивается не так».
Владелец узнал их сразу: «почему ты пишешь объяснения? где редактор».

**Отсюда следствие для работы с проверяющими.** Нужна строка — её называет
методист или редактор, а не я «по дороге». Механическая замена слова по всему
курсу — тоже текст: сначала спроси редактора.

**И ещё одно: замысел живёт документом, а не в моей голове.**
`docs/beginner-2/plan-<модуль>-urok-N.md` и разбор рядом. Владелец читает план,
а не готовый урок, и «не то» стоит одну правку вместо дня.

### ПРОГРАММА НОВОЙ СТУПЕНИ ПРИНЯТА — 5 сентября 2026

Владелец ответил на пять вопросов методиста: **«1.да. 2. нужны. 3. да 4. пусть
решает методист. 5. давай… я бы предпочел делать все правильно»**. Программа —
[docs/beginner-2/programma-stupeni.md](docs/beginner-2/programma-stupeni.md).

1. **Модуль есть ДЕЛО с дескриптором A1 или Pre-A1, а внутри дела грамматика.**
   Нет дескриптора — модуль не заводится. Тридцать модулей, пять частей.
2. **Части нужны:** работа модуля спрашивает правило, экзамен приходит через
   полгода, часть — единственное место, где спрашивается дело целиком.
3. **Экзамен — четыре раздела по четверти:** чтение, слушание, письмо (бланк и
   записка), словарь и строй речи; говорение отдельным разделом без оценки, и
   сертификат прямо называет, что меряется. До этого экзамен мерил одно умение
   из четырёх.
4. **Модуль «Погода» снят методистом:** погода стоит на A2 у всех четырёх
   источников (CEFR с. 52 и 57, A2 Key с. 51, English Grammar Profile — пустое
   `it` о погоде тоже A2). На A1 законны только СЛОВА, а слово разрешает
   карточку, а не модуль. Тема `Weather` ушла модулю 28, времена года — модулю
   18. На освободившееся место встал модуль **«В сети: запись о себе и
   отклик»** — CEFR с. 86, два дескриптора A1 и два Pre-A1.
5. **Норма словаря считается ПО ТЕМЕ, а не по числу карточек.**

**И отдельно — исключения для модуля 1 НЕТ.** Прежняя редакция программы
записывала алфавит как «не дело, а письменность». Методист открыл источники: у
алфавита свой дескриптор есть — CEFR с. 136, графа A1, «Can spell their address,
nationality and other personal details»; Young Learners с. 9. Правило «нет
дескриптора — модуль не заводится» остаётся без единого исключения. Заодно
выяснилось, что прежняя опора исключения (руководство, с. 59) была цитатой не из
той графы.

**Что из этого стало кодом:** у модуля появились поля `temy`, `egp` и
`egpPovtor`, и появилась команда **`npm run programma -- english-beginner-2`** —
две проверки, которых у курса не было никогда:

- **норма словаря по теме.** Спрашивает не «тридцать ли карточек», а сколько
  слов A1 из объявленной темы ученик получил карточкой. Тема — дословное имя из
  Приложения 2 словника A2 Key (с. 25–32, двадцать пять списков). Читается
  ВТОРОЙ разбор (`materials/.md/`): списки набраны четырьмя колонками, и
  `pdf-parse` склеивает их в строку — наивное чтение даёт 296 записей вместо
  1159. **У руководства есть вторая опись тем, на с. 53** — с другими именами и
  без слов; для нормы она не годится, и прежняя программа брала имена
  вперемешку из обеих;
- **раздача пунктов грамматики.** Модуль объявляет, какие пункты English Grammar
  Profile он вводит (`egp`) и к каким возвращается (`egpPovtor`), а счёт
  сверяет, что все пункты ступени розданы и ни один не роздан дважды. Ключ —
  поле `id`, а не заголовок: `Guideword` не уникален, 129 повторов. Файл не
  построчный (6091 строка на 1222 записи), читается настоящим разбором CSV.

**Зачем это было нужно — счётом.** Пока такой росписи не существовало,
грамматика решала, где стоять словам: одежда 5 карточек из 16 слов ступени,
природа 5 из 22, `platform` вводился в модуле о неправильных глаголах. И модули
12 и 26 Elementary оба взялись за `Why don't we…?`, дав ученику два
несовместимых разбора одной строки.

Проверка испытана нарочной порчей — выдуманной темой, несуществующим пунктом,
пунктом чужой ступени и одним пунктом у двух модулей: все четыре случая названы.
Порча ставилась в модуль, над которым никто в ту минуту не работал.

### РАБОТУ РЕДАКТОРА ПРИНИМАЕТ ВТОРОЙ РЕДАКТОР — 5 сентября 2026

Просьба владельца: **«Пусть еще 1 один редактор проверяет работу редактора»**.
Правило «автор не принимает свою работу» дошло до последнего звена: методиста
принимает второй методист, редактора — второй редактор.

**Второй редактор ТОЛЬКО ДОКЛАДЫВАЕТ.** Правки вносит тот, кто писал. Причина
не в вежливости: два редактора, пишущие в один файл, дают текст с двумя
голосами, а находки теряются между ними.

**Круг на модуль вышел такой:** методист пишет замысел → второй методист
разбирает → владелец читает → сборщик собирает скелет с заглушками → редактор
пишет текст → второй редактор принимает → правки вносит первый редактор →
спорное решает методист → сборщик вносит состав и звук.

**Что это дало на модуле 1, счётом.** Второй редактор нашёл за три круга около
сорока мест, из них четыре такого рода, что не видит ни один скрипт и не видел
автор текста:

- **столбец таблицы печатал одно и то же дважды:** в сравнении стояли
  кириллические В, С, Н, Р, Х, а на экране они неотличимы от латинских, и
  строка читалась как опечатка;
- **правило опровергалось таблицей своего же урока** («имя Kim ищи в середине
  списка», а Kim там пятое из шести);
- **урок объяснял обратное тому, что ученик слышал в записи;**
- **урок не давал правила вовсе** — восемь экранов перечисляли разряды, а
  признак жил только в подсказке задания.

**И одно правило, выведенное из его находок:** подсказка — это дорога, а не
ответ. «Названная буква похожа на русскую В» при шести буквах урока, из которых
такая одна, — это ответ, и задание со звуком проходится, не включая звук. На
трёх кругах приёмки это была самая частая находка: девять подсказок из
двадцати восьми.

### Расходятся Cambridge и English Grammar Profile — берём по Cambridge

Источники разошлись, и разошлись прямо: **English Grammar Profile кладёт эти
обороты на A2, а руководство Cambridge — на самую младшую ступень Pre A1
Starters** (Young Learners, с. 29: «It's a banana. **This is an apple.**»,
«**This is my car.**», «**Who is that man?** Where is Alex?»).

Владелец решил: **берём по Cambridge.** Оборот `This is Dana.`, вопросы
`Who is that man?`, `Where are you from?`, `What is this?` законны на ступени и
разбираются, а не идут готовой формулой.

Решение общее на весь курс и принято один раз — ровно потому, что курс на этом
уже обжигался: ступень Elementary сняла `Who's that?` в модуле 11 как A2 и взяла
его в модуле 16 как Pre A1, и один оборот получил в одном курсе две ступени.

**Расхождение при этом объявляется, а не прячется:** модуль, берущий такой
оборот, пишет в шапке одной строкой, что источники расходятся и по какому взято.
Без этой строки следующий проверяющий снимет оборот как превышение ступени —
и будет прав по своему источнику.

**РЕШЕНИЕ РАСПРОСТРАНЕНО НА ВЕСЬ КЛАСС — 6 сентября 2026, тем же днём.** Случай
повторился четыре раза за сутки: `This is …` и вопросительные слова (модули 2,
3, 7), повелительное наклонение `Turn left.` (модуль 23), `How many …?`
(модуль 4), притяжательный апостроф `the boy's head` вместе с `mine` и `yours`
(модуль 5). Владелец решил не разбирать каждый: **где руководство Cambridge
даёт оборот на Pre A1 Starters, а English Grammar Profile кладёт его на A2,
курс берёт по Cambridge.**

**Но общее правило НЕ отменяет обязанности назвать графу.** Каждый такой случай
по-прежнему открывает источник и приводит цитату со страницей: правило говорит,
чем кончится спор, а не позволяет его не проверять. Перенос вывода «у соседа
было так же» остаётся запрещённым — им уже дважды выводили расхождение, которого
нет, и один раз запрет, которого нет.

**И расхождение по-прежнему объявляется в шапке модуля.** Строка нужна не нам, а
следующему проверяющему: без неё он снимет оборот как превышение ступени и будет
прав по своему источнику.

**И осторожно с половинами.** Спорна не вся семья оборотов: `this` ПРИ
СУЩЕСТВИТЕЛЬНОМ (`this book`) стоит у English Grammar Profile на A1 тремя
графами и решения не требовал вовсе. Спорно `this` ВМЕСТО существительного.
Больше того, источник расходится сам с собой: строка `How are you?` стоит
примером и у графы A1 (`CLAUSES / interrogatives`), и у графы A2
(`QUESTIONS / wh-`). Поэтому решение не разрешает брать что угодно по
похожести: **графа называется каждый раз заново**, а перенос вывода с соседнего
случая здесь запрещён так же, как везде.

**И ПОВЕЛИТЕЛЬНОЕ НАКЛОНЕНИЕ — ТУДА ЖЕ, решение того же дня.** `Turn left.`,
`Eat your lunch.` берём на ступени. Расхождение ровно такого же вида: список
Pre A1 Starters (Young Learners, с. 29) держит графу «Verbs (Positive, negative,
question, imperative and short-answer forms)» с примером `Eat your lunch!`, а
English Grammar Profile повелительного на A1 не даёт. Нашёл методист модуля 5 и,
что важнее, **не стал переносить решение по похожести**, а спросил — хотя ему
самому оборот был не нужен. Без этого решения модуль 23 «Как пройти» не
обошёлся бы вовсе.

### Гласные буквы: пять основных и Y особо — решение владельца от 5 сентября 2026

Курс писал «гласных пять, остальные двадцать одна согласные, и Y среди них», и
сам себе противоречил через два экрана: «Буква Y согласная, но даёт то согласный
звук, то гласный».

Ни один источник в `materials/` гласных не перечисляет вовсе: слово `vowel`
встречается в руководстве Young Learners дважды и ни разу списком, в описаниях
Совета Европы и обоих словниках — ноль. Обе доступные русские справки считают
гласных шесть, включая Y.

Владелец решил как преподаватель: **пять основных гласных букв — A, E, I, O, U,
а Y условная: считается гласной, когда даёт гласный звук** (`sky`, `myth`).

Отсюда: «Буква Y согласная» из курса уходит; задание с ответом «согласных 21» и
вопрос банка «Запиши все пять гласных букв» пересматриваются.

### Картинки — только из открытых библиотек

**«Сделанные тобой картинки плохие»** — своё рисование прекращено. Четыре
библиотеки, лицензии проверены по их же страницам:
[docs/beginner-2/kartinki-otkuda.md](docs/beginner-2/kartinki-otkuda.md).
Основа — **Twemoji** (CC-BY 4.0, довольно строки в подвале): 3600 значков в одном
стиле, чего своё рисование не даст никогда. Дальше Openclipart (CC0), Open Peeps
(CC0), unDraw (**пачкой качать нельзя** — только по одной руками).

Своё рисование остаётся только для схем, которых нет ни в одной библиотеке.

### ОБЪЯСНЕНИЯ ПИШЕТ РЕДАКТОР — решение владельца от 4 сентября 2026

**«пусть редактор сам пишет объяснения»**. До этого дня он только докладывал, а
переписывал автор — теми же словами, потому что писал их тот же, кто и в первый
раз. Теперь блоки `explain` и врезки — работа редактора целиком, и он правит их
прямо в файлах курса.

**Повод.** Владелец открыл урок «Он, она, оно — is» и прочёл: «После he и she
стоит is. **He — о мужчине, she — о женщине.**» И следом: «Слово it **ставят о
предмете, а не о человеке**». Ответ: «что это за объяснение… снова ужасное
иишное описание. например по-человечески: После it тоже **ставим** is. It
означает оно… Неужели так сложно нормально написать».

**Три признака машинного объяснения — разобраны из этого случая:**

1. **Назван разряд там, где ученик ждёт русского слова.** «he — о мужчине»
   вместо «he — это он». Ученик УЖЕ знает «он», «она», «оно»: дай соответствие,
   и объяснять больше нечего. Правило: **есть русское соответствие — оно
   называется ПЕРВЫМ**, всё остальное после него.
2. **Безличная форма вместо учительской.** «Ставят», «употребляется»,
   «считается» — язык справочника, где действующего лица нет. Учитель говорит
   «ставим», «берём», «говорим». Владелец в своём образце написал именно
   «ставим», и это не мелочь: безличность превращает указание себе в описание
   чужого явления.
3. **Определение через отрицание.** «…о предмете, **а не о человеке**» —
   ученик держит в голове отрицание вместо правила. Сперва говорится, что есть;
   отрицание — отдельной строкой и только там, где ученик правда ошибается.

**Проверка, ловящая все три разом: прочитай вслух — так учитель говорит
ученику?**

**Одно место, где за образцом владельца идти нельзя дословно.** Он написал «it
**всегда** используется для предметов». Устройство фразы берём, слово «всегда» —
нет: `it` говорят и о погоде, и о времени, и о животном, а тот же урок через два
экрана учит `It is cold`. Абсолютные слова ставит методист по источнику.

**Переписываем только НОВЫЕ уроки** (`courses/english-beginner-2/`) — решение
владельца там же: старые ступени будут выброшены, когда новый курс допишется, и
править 796 объяснений в них — потерянный труд.

### Формат живёт в приложении, а не в макете — 3 сентября 2026

Владелец: «так давай сделай полную версию с редизайном и запусти localhost».
Пошаговый вид переехал из `docs/mockups/` в само приложение и открывается по
обычному адресу урока.

**Вид объявляет курс, а не угадывает страница.** У курса поле
`format: "shagi"`, у прежних курсов его нет — они показываются страницей, как
раньше. Оба вида живут рядом, пока идёт переделка.

Где что: ход по экранам — `components/lesson/Shagi.tsx`, вид экрана —
`Ekran.tsx`, что считать экраном — `UrokShagami.tsx`, оформление —
`shagi.module.css` (цвета все до одного из `app/globals.css`, своего набора нет).

**Задания не переписаны:** на экране стоит прежний `TaskCard`. Учёт работы тоже
общий — `LessonFlow` стоит снаружи обоих видов, и ответы не теряются при
переходе с одного на другой.

**Значок картинки стоит в содержании** (поле `znak` у блока и у слова словаря),
а не в коде страницы: в макете связь лежала таблицей в скрипте, и переименование
блока молча убивало картинку. Файлы забирает `npm run znachki` в
`public/twemoji` — на боевом сайте картинки лежат у нас, а не тянутся с чужого
адреса. Twemoji это CC-BY 4.0, упоминание стоит в подвале.

**Что дало подключение курса к `courses/index.ts`** (сделано первым же делом, по
правилу, нажитому на экзамене Elementary): шесть ошибок при прежнем чистом
отчёте. Из них одна оказалась **ошибкой самой проверки** — `checkDubli` считала
поломкой два одинаковых отмечаемых слова, а у `hottext` повтор законен: ученик
отмечает МЕСТА, а не строки, и «отметь все формы be» иначе не написать. И
`yazyk` дал 61 ошибку на пустом месте, пока `scripts/vidimoe.mts` не научили
полю `znak`. Разбор целиком — [docs/beginner-2/zamysel.md](docs/beginner-2/zamysel.md).

**Лестница заданий ушла из подписей к экранам.** Стоит «задание 3 из 10», а не
«ступенька 2: произвести»: лестница — наша кухня, и слово «ступень» в курсе
занято ступенями A1 и A2. Поймала проверка, в трёх условиях заданий сразу.

**Задание дорогу не запирает.** На неотвеченном задании кнопка называется
«Пропустить» и выглядит тише, на отвеченном — «Дальше». Подсказывать — да,
запирать — нет.

### Карта курса вместо списка — 3 сентября 2026

Владелец: «мне не нравится бесконечный список модулей и потом списков… может
как в пиндоше по шагам на карте».

Изучены разборы тропы Duolingo. Взято главное: тропа отвечает на вопрос «что
делать дальше», ради которого её и вводили в 2022 году. Обойдена главная жалоба
на неё — «листать вечность, чтобы вернуться к теме»: у нас **развёрнут только
текущий модуль**, остальные свёрнуты в строку со значком, названием и полосой
успеха. До любого модуля два нажатия.

Устройство: часть — раздел (у каждой свой цвет), модуль — участок, урок — узел
на пунктирной нити, работа модуля и работа части — узлы в конце своего участка.
Наверху свод: сколько уроков и работ пройдено на ступени целиком.

**Замков на уроках нет** — узел впереди обычная ссылка. Закрыты только работы.

**У модуля есть значок** (`znak` в `Module`) — 57 значков проставлено на трёх
курсах. Он нужен не для красоты: свёрнутые модули стоят строками, и одинаковые
кружки с цифрами глазом не различаются. Номер ушёл в подпись под названием.

**ВИД УРОКА — сделано 4 сентября 2026.** Правила, чтение, слушание, письмо,
говорение: `lib/content/vid-uroka.ts`. Значок стоит у уроков умений, у правил
его нет нарочно — иначе он у каждого второго узла и превращается в шум.

Устроено выводом, а не полем у пятисот уроков: текст для чтения бывает только в
уроке чтения, запись — только в уроке слушания. Но **вывод ошибается
предсказуемо**, и потому у урока есть поле `vid`, которое главнее вывода:
проставлено ровно там, где вывод врал, — девять уроков на четыре курса (запись в
уроке правил про алфавит, сочинение в уроках правил нового формата).

**Чтобы это не разъехалось молча, `check:content` сверяет ДВА независимых
признака** — вывод по составу и заголовок («Читаем…», «Слушаем…», «Пишем…»).
Дописал в урок правил запись — вид съедет, и проверка спросит. Испытана порчей:
переименовал урок правил в «Читаем…» — назвала место.

**Оформление проверяется глазами.** Раскладку тропы владелец поправил с первого
взгляда: подписи левых узлов уезжали к чужому номеру, потому что сетка
расставляет элементы без явно названной строки только вперёд. Чтением кода это
не ловится — разметка была верной.

### ТЁМНЫЙ ЛИСТ ПО УМОЛЧАНИЮ — решение от 3 сентября 2026

**«Тёмная тема лучше, можно оставить по дефолту, и светлая тема на выбор.»**

**Это ОТМЕНЯЕТ прежнее решение**, записанное в `app/globals.css`: «по умолчанию
сайт светлый… тёмный лист — это решение, которое человек принимает сам». Если
встретишь ту запись — она отменена.

**Сделано и на сайте** 3 сентября 2026, вместе с переносом формата в
приложение: `app/layout.tsx` ставит `data-theme="dark"`, комментарий в
`app/globals.css` поправлен. Светлый остаётся переключателем в шапке, выбор
запоминается и применяется до первой отрисовки. На боевой сайт это доедет
только с ближайшей выкладкой, а её без слова владельца не делаем.

### Русские объяснения скачаны — 673 страницы

`materials/russkie-obyasneniya/` (вне репозитория), команда
`npm run skachat-obyasneniya`: puzzle-english 307, lingust (Мёрфи по-русски) 145,
native-english 141, grammarway 80.

**Оттуда берётся УСТРОЙСТВО объяснения и ничего больше.** Тексты чужие, а
точность у них бывает хуже нашей: `englex` зовёт `to be` «глаголом-связкой», а
это слово у нас занято под `is` в модуле 21 Elementary. Всякое «нельзя», «только»,
«всегда» проверяется по `materials/`.

### Чего в образце нет и почему

Картинок к остальным модулям, нового вида задания `bank` (восстановить текст из
словаря-подсказки — требует правки `lib/content/types.ts`), второй дороги по
умениям, уроков умений в новом модуле. Владелец: «делай только пока уроки, не
заморачивайся на остальные».

**Новый курс ПОДКЛЮЧЁН к `courses/index.ts`** — 3 сентября 2026, первым же
делом, и это сразу дало шесть ошибок при прежнем чистом отчёте (разбор выше). В
каталоге его при этом не видно: недописанные курсы прячет `courseCards` по
списку `NEDOPISANY` — из перечня их выбрасывать нельзя, иначе их перестанут
проверять скрипты.

## Где мы остановились

**СТУПЕНЬ BEGINNER ПЕРЕВЕДЕНА ЦЕЛИКОМ — 31 августа 2026.** 573 строки из 573,
два методиста, два редактора, около пятидесяти находок. Журнал — тот же
[docs/peredelka-obyasneniy.md](docs/peredelka-obyasneniy.md), раздел о Beginner.

**Важное для следующей сессии: объяснения Beginner переделывать НЕ пришлось.**
Ступень уже написана по принятому методу — начинает с русской фразы, называет
расхождение с русским, ссылается на соседние модули (93 ссылки против 36 у
переделанной Elementary). Работа свелась к переводам.

**СТУПЕНЬ ELEMENTARY ПЕРЕДЕЛАНА ЦЕЛИКОМ И ПРОШЛА ПОЛНЫЙ КРУГ — 31 августа 2026.**
Тридцать один модуль, два методиста, два редактора, около семидесяти находок,
все внесены. Журнал — [docs/peredelka-obyasneniy.md](docs/peredelka-obyasneniy.md),
он же точка входа для новой сессии.

**Повод переделки — слова владельца:** «объяснения слабые, переведены с
английского и написаны тобой, читаются механически и трудно понять». Диагноз
оказался точнее: **у курса были источники состава и не было источника
объяснения.** English Grammar Profile отвечает «что и на какой ступени», и
больше ничего; объяснения писались пересказом описаний уровня.

**Метод — шесть правил, [docs/format-uroka.md](docs/format-uroka.md), раздел
«Объяснение пишется по-русски».** Коротко: называется русская фраза, а не
грамматическая категория; английская строка не стоит без перевода; строка
описывается по смыслу, а не слева направо; таблица делает работу, которой не
сделать словами; расхождение с русским — это и есть предмет объяснения; связь с
соседними модулями — часть объяснения.

**Карта ступени — [docs/karta-stupeni-elementary.md](docs/karta-stupeni-elementary.md).**
Восемь связок вместо четырёх частей по счёту и пять узлов, где обязан стоять
свод. Счёт, из-за которого карта понадобилась: ссылок на другие модули во всех
31 модуле было **три**, а упоминаний `Past Simple` в модулях 22–24 о Present
Perfect — **ноль**.

**Итог в числах:** строк примеров с переводом рядом было 0 из 785, стало 780;
ссылок на другие модули — 19 вместо 3; сводов в узлах — 5 из 5, не было ни
одного.

### Семь выдуманных запретов за один день, и как их ловить

Каждый опровергался **собственным текстом модуля быстрее, чем источником**.
Модуль 14 нарушал свой запрет 42 раза в том же файле. Модуль 15 запрещал то,
чему учит его же урок 5. Модуль 23 писал «место одно», а его задание спрашивало
«на обычном месте».

**Отсюда правило: проверяя запрет, СНАЧАЛА ищи его нарушения в самом модуле.**
Одна команда `grep` отвечает быстрее любого источника. Полная таблица семи — в
журнале переделки.

И вторая половина: **своды тянут к ярлыкам сильнее любого другого блока.** Свод
модуля 11 вернул «намерение», «обещание», «договорённость» — ровно то, против
чего затевалась переделка, и в самом заметном месте. Проверять своды на правило
8 надо отдельно и первыми.

### Редактор на opus — решение подтвердилось счётом

Владелец вернул его на opus 30 августа. За весь прежний общий проход редакторы
на sonnet дали 7 находок; здесь один редактор на opus дал больше двадцати в
двенадцати видах, и среди них — та самая про ярлыки в сводах, которой не увидел
никто.

### Три ошибки, которые сделал я и поймал не я

1. **Сплошная замена термина сломала чужое решение.** Замена «тот, кто
   действует» задела модуль 30, где эта формула стоит нарочно. `sosedi` такого
   не видит: он смотрит соседние поля, а ломалось решение из шапки.
2. **Запись в `resheno.ts` гасила слишком широко** — 16 случаев вместо десяти,
   включая те, где род навязывался ученику. Список едва не стал свалкой, о
   которой предупреждает его же шапка.
3. **Ярлык, снятый в модуле 12, вернулся из модуля 28.** Та самая порода:
   находка о чужом модуле не доезжает до него сама.

### Что дальше

**ОБЕ СТУПЕНИ ВЫЛОЖЕНЫ 1 сентября 2026** по слову владельца, банк вопросов
перенесён в базу тем же днём. Проверено запросами: первый и последний модуль
каждой ступени отвечают 307, работы модулей и частей тоже.

0. **РАЗБОР МЕТОДИСТА ВНЕСЁН, И КРУГ ПО ПРАВКАМ ПРОЙДЕН — 2 сентября 2026.**
   Восемь находок «по дороге», вторая и третья очередь близнецов, круг обоих
   проверяющих по самим правкам. Журнал —
   [docs/peredelka-obyasneniy.md](docs/peredelka-obyasneniy.md).

   **Порода «работа цитирует урочный текст» вычищена 2 сентября:** Elementary
   12 → 3, Beginner 3 → 1. Сначала починена сама проверка — она искала вхождение
   строки ЦЕЛИКОМ и потому давала ноль там, где методист своей меркой (семь слов
   подряд) находил семнадцать мест.

   **Долг словаря разобран тем же днём:** из восьми мест настоящими были три,
   пять были дырами в `kontrol`. Проверка научена двум вещам: слово вводит и
   строка таблицы с русским переводом рядом (так курс даёт `written`), а
   латиница после дефиса — не слово, а окончание, названное по-русски (`-self`).

   **ОСТАЛОСЬ ОТТУДА — и всё это решает МЕТОДИСТ, не автор:**
   - **средняя зона Beginner** — около сорока близнецов с долей 50–70% и
     совпавшим ответом. Методист разобрал их списком, а не поштучно;
   - **четыре законных цитирования**: `we're taking one textbook for two` стоит
     в словнике модуля и есть предмет вопроса; `I haven't washed the sink yet`
     нужна списку самопроверки; `You don't have to bring a` — оборот, которому
     учит модуль; `How do you spell your name?` сказать иначе нельзя. Их место в
     `resheno.ts`, а автор туда писать не вправе;
   - **три поздние карточки** — `found`, `number`, `pale`: слово объяснено на
     месте переводом рядом или помянуто названием будущего урока, а карточка
     приходит позже. Считать ли перевод рядом введением слова — решение о том,
     что вообще меряет проверка.

   **Оба вопроса владельцу закрыты 2 сентября 2026:** половина сложного
   предложения зовётся «простым предложением» (модуль 24 переведён на настоящий
   термин, 32 места), а короткий номер называют целиком — `sixty-three`.

1. **ЭКЗАМЕН И РАБОТЫ ЧАСТЕЙ ELEMENTARY НАПИСАНЫ И ВКЛЮЧЕНЫ — 2 сентября 2026.**
   Долг, который вскрыла выкладка: адрес `/learn/english-elementary/ekzamen`
   отвечал 404, `parts` в `index.ts` объявлены не были, и ученик упирался в
   пустоту после тридцать первого модуля.

   Сделано: четыре работы частей (`raboty-chastey.ts`, 80 вопросов, границы из
   программы — модули 1–7, 8–13, 14–21, 22–31), экзамен на 76 вопросов включён
   в курс, 25 записей на слух озвучены, сборка проходит.

   **ВКЛЮЧЕНИЕ СРАЗУ ОПРАВДАЛО СЕБЯ, и это стоит помнить.** Проверка, впервые
   добравшись до экзамена, нашла в нём то, чего не видел никто: верный ответ
   стоял на втором месте в одиннадцати вопросах выбора из двенадцати, девять из
   них подряд. Экзамен проходился нажатием одной кнопки. Пока курс не объявлял
   `exam`, каждая проверка экзамена молчала — они все обёрнуты в
   `if (course.exam)`. **Содержание, до которого нет дороги, выглядит в отчёте
   как работающее.**

   **ВЫЛОЖЕНО 2 сентября 2026 по слову владельца, банк вопросов перенесён в
   базу тем же днём.** Проверено запросами: экзамен и все четыре работы частей
   отвечают 307 (страница за замком), а до выкладки экзамен отвечал 404.

   **ОСТАЛОСЬ ОДНО, И ЭТО ДОЛГ:** методист работы частей НЕ СМОТРЕЛ. Он прямо
   написал, что файл правился под ним и целиком он его не разбирал. Редактор их
   прочёл, тридцать находок внесены, скрипты чисты — но методической проверки на
   восьмидесяти новых вопросах не было. Позвать при первой возможности.

2. **139 близнецов на Beginner и 31 на Elementary** — `npm run bliznetsy`.
   Худшие переписаны в два приёма. Остальные молча править нельзя: часть
   совпадений законна, решает методист.
3. **Владельцу стоит пройти модуль-другой глазами.** Из всей переделки самое
   важное нашёл он сам; проверяющие понятность ловят хуже всего. Теперь это
   можно делать прямо на боевом сайте.
4. **Долги, названные проверяющими 3 сентября 2026.** Ни один не создан правкой
   того дня — все старше её, и потому не чинились наспех:

   **Все шесть разобраны 3 сентября 2026** — четыре правкой, один решением, один
   оказался не долгом:

   - **`first`, `then`, `after that` остаются «словами порядка».** Решено, а не
     отложено. Настоящего имени у этого класса взять неоткуда: English Grammar
     Profile зовёт их `adverbs and adverb phrases`, и «наречиями» их назвать
     нельзя — `after that` не наречие; русские учебные материалы зовут их
     «словами-связками», а «связка» на ступени занята (так курс зовёт `is` в
     модуле 21). «Слова порядка» вещь не искажают: эти слова и вправду
     показывают порядок. Разница с «признаком» и «полной мыслью» в этом: те
     подменяли существующий термин, здесь термина нет. Владелец вправе решить
     иначе — цена перемены 28 мест, включая экзамен;
   - **«простое предложение» доехало до Elementary.** Модуль 4 переведён на
     настоящий термин целиком (19 мест) и вводит его строкой с примером — курс
     не ссылается на прошлую ступень. Там, где половины НЕ простые предложения
     (модули 7, 11, 29, 30, 31 — придаточные с `while`, `when`, `if`, `who`,
     `that`), стоит «часть»: это тоже настоящий термин, и он верен для
     придаточной. Слово «половина» из этих модулей убрано целиком, а в экзамене
     и работах частей — там, где оно называло часть предложения; «половина
     записи» и «половина дел» остались, там это обычное слово;
   - **модуль 22 переведён на «оборот времени»** — имя, которое курс уже дал в
     модулях 1–3. Слово «день» ушло: `in May` и `last summer` днями не были;
   - **пол ученика снят с девяти заданий**, восемь из них в экзамене. Правка
     доедет до ученика только переносом в базу (`npm run db:seed:courses`);
   - **`the` на Elementary — НЕ ДОЛГ, и это стоит записать.** Объяснение стоит в
     модуле 1 с прямым комментарием «артикль вводится здесь, а не когда-нибудь»;
     `npm run chain` считает словарные карточки, а не объяснения, и служебные
     слова карточек не получают. Настоящая дыра была уже, и её закрыли: чтения
     `the` объяснение не давало вовсе — /ðə/ и /ði/ живут только в карточке, а
     карточки на ступени не было. Теперь есть, как на Beginner;
   - **четыре урока слов части разведены.** Модуль 7 говорит «глаголом с
     приставленным словом» (`wash up` — по отдельным словам смысл не виден),
     модули 13 и 21 — «названия составлены из двух слов» (`ice cream` — первое
     уточняет второе). Раньше обе подсказки стояли под одной формулой и
     противоречили друг другу.

Записка прежнего дня —
[docs/zapiska-obshchiy-prohod.md](docs/zapiska-obshchiy-prohod.md): общий проход
по ступени, 73 находки, восемь проверяющих.

## Переезд на другой компьютер

Порядок и то, чего переносить нельзя, —
[docs/perenos-na-drugoy-kompyuter.md](docs/perenos-na-drugoy-kompyuter.md).

Два правила оттуда действуют всегда:

1. **`.env.local` в репозиторий не кладём никогда.** Он открыт всем. Настройки
   забираются из Vercel командой `vercel env pull`, а ключ синтеза речи — из
   личного кабинета Azure.
2. **Память агента живёт вне репозитория**, и потому её снимок лежит в
   `docs/pamyat/`. Команда — `npm run pamyat` (снять) и `npm run pamyat --
   --vernut` (вернуть). Снимай перед переездом: иначе новая машина получит
   вчерашнее знание.

## Работа с репозиторием

Коммиты и их описания — на русском, по существу: что изменилось и **почему**.
Не выкладывай сайт без явной просьбы владельца.
