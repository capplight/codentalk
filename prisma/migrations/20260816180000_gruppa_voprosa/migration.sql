-- Группа вопроса для равномерной выборки.
--
-- У экзамена ступени и работ частей это модуль: без такого признака выборка
-- шла по теме, а тема на экзамене у каждого вопроса своя — круг вырождался в
-- случайную горсть, и примерно четыре модуля из двадцати пяти не проверялись
-- в попытке вовсе.
--
-- Поле необязательное: у работы модуля группировать по модулю нечего, там
-- по-прежнему годится тема.

ALTER TABLE "test_questions" ADD COLUMN "group_key" TEXT;
