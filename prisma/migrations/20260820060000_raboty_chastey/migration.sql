-- Проверочные работы частей курса.
--
-- Сами работы были написаны 15 августа и всё это время лежали в материалах
-- мёртвым грузом: перенос в базу их не знал, страницы у них не было, и ученик
-- не мог их открыть никак. Здесь база узнаёт о них два обстоятельства.
--
-- Первое: работа части — отдельный вид. Не работа модуля (модуля у неё нет,
-- она спрашивает сразу о шести-семи) и не экзамен ступени.
--
-- Второе: чтобы найти работу и проверить условие её открытия, базе нужно имя
-- части. Своей записи у части нет и не заводится — части живут в материалах,
-- а в базе от них требуется только имя.

ALTER TYPE "TestKind" ADD VALUE 'part_quiz' AFTER 'module_quiz';

ALTER TABLE "tests" ADD COLUMN "part_slug" TEXT;
