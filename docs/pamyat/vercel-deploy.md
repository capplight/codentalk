---
name: vercel-deploy
description: "Как выкладывается CodeNTalk: какая учётная запись Vercel, какие переменные окружения нужны, что было сломано"
metadata:
  node_type: memory
  type: project
  originSessionId: 6e097c62-64e7-44f0-9277-b74520eb44f7
  modified: 2026-08-10T18:40:51.037Z
---

Сайт живёт на **https://codentalk.vercel.app**. Владелец разрешил выкладывать
и переписывать прежнюю версию (10 августа 2026).

## Учётная запись

Vercel: `nauryzbekali-6287`, команда одна — `nauryzbeks-projects-75b67797`
(«Nauryzbek's projects»). **Корпоративной в этой учётной записи нет**, перепутать
не с чем. Проект `codentalk`, там же лежат другие проекты владельца.

Не путать с GitHub, где две учётные записи и корпоративную трогать нельзя —
[[github-two-accounts]].

## Что было сломано и починено

В Vercel **не было ни одной переменной окружения**. Выкладка тринадцатидневной
давности стояла без адреса базы и без ключа входа: любая страница с базой
падала, войти было нельзя.

Заведены четыре, все из локального `.env.local`:
`DATABASE_URL`, `DIRECT_URL`, `AUTH_SECRET`, а `AUTH_URL` —
`https://codentalk.vercel.app`.

**Если появится новая переменная в `.env.local` — её надо добавить и в Vercel
вручную**, иначе сборка пройдёт, а страница упадёт.

## Команда

```
npx vercel deploy --prod --yes
```

Раньше блокировалась правами; сейчас проходит. Перед выкладкой:
`npm run check:content`, `npm run db:seed:courses`, `npm run build`.

## ПУШ В РЕПОЗИТОРИЙ ВЫКЛАДКУ НЕ ЗАПУСКАЕТ

Выяснено 14 августа 2026. Я отправил шестьдесят коммитов в `main` и ждал, что
Vercel соберёт сам, — он не собрал. `npx vercel ls` показал, что последние
выкладки двухдневной давности и все сделаны рукой.

**Коммит не равен выкладке.** Выкладывать только командой, и после неё
проверять боевой сайт: обещать владельцу выложенное, не убедившись, нельзя.

## Что важно помнить

- Домен `codentalk.kz` РАБОТАЕТ (проверено 14 августа: главная и страница
  модулей отвечают). Прежняя запись о неотвечающем домене устарела.
- Сайт **закрыт от поисковых систем**: `robots.txt` запрещает обход целиком.
  Так задумано до команды владельца о запуске.
- Локальная проверка — порт **3010**; запущенный из моей оболочки сервер
  умирает вскоре после конца ответа, надёжнее владельцу запускать самому.

## Выкладка по пушу НЕ настроена — и это выяснено, а не предположено

20 августа 2026 записка владельца оставила вопрос: выкладка по пушу отключена
нарочно или не настроена? Ответ получен запросом к API Vercel: **у проекта поле
`link` пустое — связи с репозиторием нет вообще**. GitHub и Vercel не связаны
никак, значит пуш не выкладывает и не выложит.

Выкладка только командой:

```
npx vercel --prod --token <токен>
```

## Какой токен годится

**Токен проекта (`vcp_…`) для CLI НЕ годится.** Через API он работает (запрос к
`/v9/projects/…` отдаёт 200), а CLI сначала грузит пользователя и получает 404
«User not found». Нужен **токен учётной записи** — vercel.com/account/tokens,
Account Settings, а не Project Settings. С ним `vercel whoami` отвечает
`nauryzbekali-6287`, и `--prod` проходит.

`VERCEL_OIDC_TOKEN` из `.env.local` — тоже не то: это короткоживущий JWT для
доступа приложения к внешним службам, CLI отвергает его как невалидный.

## Домены

`codentalk.kz`, `www.codentalk.kz`, `codentalk.vercel.app` — все три ведут на
production.
