# Модуль 9, урок 1 «Большой и маленький: The box is big». Подробный замысел

6 сентября 2026. Пишет методист. **Весь видимый русский текст пишет редактор:**
вступление, объяснения случаев, врезки, подписи, условия и разборы заданий.
Здесь стоят состав, порядок, английские строки, опоры и запреты — и ничего из
того, что ученик прочтёт по-русски.

Замысел модуля — [plan-modul-9.md](plan-modul-9.md).

**Итог урока:** говорить, какая вещь: `The box is big`.

Проверка итога подстановкой под общий хвост «Чему научишься в модуле»: «говорить,
какая вещь» — правда, урок этому учит, и работа модуля спрашивает это тремя
вопросами (1, 2, 3). Слов о цвете в итоге нет нарочно: цвет — урок 2. Слов о
порядке слов (`a big box`) тоже нет: это урок 4.

---

## 1. Чем этот урок НЕ является

**Это не урок о форме `be`.** Форма введена модулем 2 и работает во всех восьми
модулях до этого. Урок 1 берёт её как известную и добавляет ровно одно: **после
`is` может стоять не только название вещи и не только место, но и слово о том,
КАКАЯ она.**

**Это не урок о порядке слов.** Строй `a big box` — урок 4 того же модуля, и
правило урока 1 обязано быть написано так, чтобы урок 4 его не опровергал.
Правило проекта: правило урока сверяется с ПЕРЕЧНЕМ УРОКОВ модуля.

> **Редактору, прямым запретом.** Правило случая 1 не говорит «прилагательное
> стоит после `is`» и не говорит «между названием вещи и прилагательным ставим
> `is`». Модуль опровергает обе эти формулы сам, и в разных местах:
> **урок 4** показывает `a big box`, где прилагательное стоит ПЕРЕД названием;
> **урок 5** показывает `The apples are red.`, где стоит не `is`, а `are`.
> Правило говорит о СВОЁМ случае: **между названием вещи и прилагательным встаёт
> форма глагола `be`; при одной вещи это `is`.** Оба вторых строя приходят с
> адресом — место в уроке 4 «Красная машина: a red car», форма `are` в уроке 5
> «Красные яблоки: red apples».

**Термин модуля называется здесь, до первого задания:** слово, которое говорит,
какая вещь, зовётся **прилагательным**. Термин настоящий, курс так же вводит
«местоимение», «предлог», «артикль», «множественное число». Другого имени у этой
вещи в модуле нет ни разу — ни «признак», ни «описание», ни «качество». **И это
относится к итогам уроков тоже:** прежняя редакция замысла модуля запрещала слово
«признак» в разделе 0.1 и сама звала им главную вещь в четырёх итогах. Итог —
строка, которую сверяют точным совпадением в трёх местах разом (`outcomes`
модуля, `outcome` урока, `outcome` каждого вопроса работы), и правится она потом
поиском по всему курсу.

**Порядок в объяснении задаёт правило владельца от 4 сентября:** есть русское
соответствие — оно называется ПЕРВЫМ. Сначала «большая», «новая», «длинная», и
только потом имя класса.

---

## 2. Вступление: текст, а не обобщение

**Модуль 9 — модуль-текст** (замысел модуля, раздел 5.5), и первый экран курса в
нём — написанная записка, а не разговор и не показ вещей.

**Сцена.** Ким уехал и оставил Дане записку о двух телефонах, которые лежат на
столе рядом и на вид одинаковые: один его, другой её.

Блок — **`example` с подписью, а НЕ `kind: "text"`.** Причина в коде:
`vidPoSostavu()` объявляет уроком чтения любой урок, где есть блок `text`
(`lib/content/vid-uroka.ts`). Сборщику проверить прогоном.

| Английская строка | Перевод рядом |
|---|---|
| Dana, my phone is new. | Дана, мой телефон новый. |
| Your phone is old. | Твой телефон старый. |
| Kim | Ким |

**ЗАПИСКА ПЕРЕПИСАНА, И ПРЕЖНЯЯ НЕ РАБОТАЛА ДВАЖДЫ.** Стояло:

    Dana, this phone is new.
    This phone is old.

1. **Она не делала того, ради чего взята.** Замысел писал: «два одинаковых
   телефона, и без слов `new` и `old` их не различить». Со словами тоже не
   различить: обе строки начинались одинаково — `this phone`. Ким ушёл,
   показывать некому, и Дана, читая записку, не могла узнать, какой телефон
   «этот», а какой «этот».
2. **`this` там употреблено против правила, которому курс уже научил.** Модуль 7,
   видимый текст (прогон `npm run tekst -- english-beginner-2 chto-eto-takoe`):
   «Спрашивают о вещи словом `this`, потому что на неё **показывают**»; «О ближней
   вещи говорят `this`, а о дальней `that`»; подсказка задания: «Вещь у Даны **в
   руках**, то есть совсем рядом». В записке никто не показывает и никого рядом
   нет — ученик получил бы противоречие внутри своего курса.

**Новая записка чинит обе беды одной правкой:** телефоны различаются словами `my`
и `your`, показывать ничего не надо, и `this` не нужно вовсе.

**Все слова уже даны:** `my` — модуль 1 (`alfavit.ts:1672`), `your` — модуль 1
(`alfavit.ts:565`), `phone` — модуль 4 (`chisla-i-telefon.ts:3055`), `old` —
модуль 4 (`chisla-i-telefon.ts:2632`), `is` — модуль 2. Новое здесь ровно одно
слово, `new`, и оно карточка этого урока.

**Почему текст, а не сцена-разговор.** Дело модуля — отличить одну вещь от
другой словом. Записка это делает наглядно: два одинаковых телефона, и без слов
`new` и `old` не понять, который твой. Разговором это было бы то же самое, что у
модуля 8, а часть 2 требует третьего вида начала.

**Русская параллель называется вторым экраном, а не первым.** По-русски «мой
телефон новый» — трёх слов довольно, глагола нет вовсе. По-английски между
названием вещи и словом о ней стоит `is`. Это расхождение и есть предмет урока.

**Строка `My phone is new.` в заданиях урока не повторяется.** Прежняя редакция
задания 5 просила записать ровно её; после правки записки это был бы ответ,
напечатанный на первом экране. Задание 5 теперь о ручке (раздел 6).

**Чего во вступлении быть не должно:** перечисления того, что будет разобрано
(«разберём размер, порядок слов, множественное…»). Владелец сказал об этом
прямо, и так открывался последний урок каждого модуля старого курса.

**Звук.** Записка звучит одним голосом — её читает Дана про себя, это не
разговор. `razgovor` не ставится, `pervyyGolos` не нужен. Три строки — три
записи, по правилу дробления звука.

**Имя рассказчика в строках сцены не стоит.** `Kim` в записке — подпись, а не
реплика Кима: читает записку Дана. Проверено правилом методиста модуля 6.

---

## 3. Семь названных случаев и четырнадцать примеров

Мерка нового формата — восемь случаев и шестнадцать примеров — **ориентир, а не
требование**: владелец ответил 6 сентября «пофиг. некритично, необязательно
строго это соблюдать, пусть это больше будет ориентиром» (ответ 18) и «пиши по
факту, не надо искусственно удлинять или укорачивать» (ответ 1).

**Случаев стало семь, а не восемь, и это правка, а не недобор.** Прежний случай 5
(«вместо названия вещи говорят `it`», с разбором «по-русски на месте `it` три
слова — он, она, оно») слово в слово повторял случай урока 2, и оба объявляли,
что закрывают один и тот же долг модуля 7. Ученик получил бы один экран дважды.
Случай оставлен уроку 2 — там долг модуля 7 назван его собственной строкой
`It is black.` (`plan-modul-7-urok-1.md`, строки 181–182) и там `it` нужно для
цепочки «назвал вещь — говорю о её цвете». Здесь `it` работает только в коротком
ответе `Yes, it is`, а его ученик получил в модуле 7.

Русские имена случаев и текст правил пишет редактор — ниже стоит только то, ЧТО
в случае разбирается и какие английские строки в нём работают.

### Случай 1 — какая вещь: форма `be` между названием и словом о ней

Что разбирается: чтобы сказать, какая вещь, между её названием и словом о ней
встаёт форма глагола `be`; при одной вещи это `is`. **Здесь же называется
термин** — строкой внутри случая, не отдельным экраном.

| Строка | Перевод |
|---|---|
| The box is big. | Коробка большая. |
| The car is new. | Машина новая. |

**Опора:** English Grammar Profile, `1741163706530x753542801715210100`,
`ADJECTIVES / position`, FORM: PREDICATIVE, WITH BE, **A1** — «Can use a limited
range of adjectives predicatively, after be», пример источника «I like my home
because my bedroom is big».

**`the` тут не разбирается** — артикль ввёл модуль 3, урок «Тот самый: the».
Коробка та самая, о которой речь, и это ровно тот случай, которому модуль 3
учил.

**Строка обещания с адресом стоит здесь же:** при нескольких вещах форма другая,
и это урок 5 «Красные яблоки: red apples». Без неё правило урока 1 читалось бы
как утверждение о языке, а урок 5 его опроверг бы.

### Случай 2 — по-русски глагола нет, по-английски он нужен

Что разбирается: **расхождение с русским, главное во всём уроке.** По-русски
«Коробка большая» — двух слов довольно. По-английски между ними стоит `is`.

| Строка | Перевод |
|---|---|
| The table is long. | Стол длинный. |
| The pen is short. | Ручка короткая. |

**Чего в правиле быть не должно:** слова «всегда». Графы, которая бы запрещала
обойтись без `be`, я не нашёл ни одной, а `How big?` — правильный английский.
Учим своему случаю, а не отрицанию чужого.

**Что взамен даёт силу правилу:** столбец «так сказать нельзя» с двумя строками
без `is`. Они **не звучат** — правило проекта.

### Случай 3 — пары: большой и маленький, новый и старый

Что разбирается: слова ходят парами, и пару легче запомнить целиком. Таблица
ставится сразу после правила, полем `naTomZheEkrane: true`.

| Строка | Перевод |
|---|---|
| The pen is new. | Ручка новая. |
| The table is small. | Стол маленький. |

**Примеры этого случая правлены:** прежде здесь стояли `The box is big.` и
`The pen is short.` — обе строки уже напечатаны в случаях 1 и 2, и экран
повторял увиденное.

**Таблица трёх пар** (объяснение идёт ПОСЛЕ неё и коротко):

| Одно | Другое |
|---|---|
| big — большой | small — маленький |
| long — длинный | short — короткий |
| new — новый | old — старый |

**Что объяснение говорит после таблицы, чего таблица не говорит:** пара — это
про одну и ту же вещь с двух сторон, и в строке стоит одно слово из пары, а не
оба. Проверка правила проекта: убери после таблицы все слова — урок потеряет
именно это.

**Звучат клетки по отдельности:** `big` одна запись, `small` другая. Пара в
одной записи — ошибка. **Строки таблицы со своим тире перевода через тире не
получают** — перевод выводится на страницу через тире и слипся бы; здесь его даёт
сама клетка.

**Осторожно, `old` — карточка модуля 4**, и там оно значит возраст человека
(`I am ten years old`). Второе употребление разбирает случай 6; в таблице оно
стоит уже как известное слово.

### Случай 4 — сказать, какая вещь НЕ

Что разбирается: возврат к модулю 3 (`I am not from Astana`) и к модулю 7
(`It isn't a pen.`). Здесь то же самое, но после `not` стоит прилагательное.

| Строка | Перевод |
|---|---|
| The car is not new. | Машина не новая. |
| The box isn't small. | Коробка не маленькая. |

**ЭТОТ СЛУЧАЙ ЗАЯВЛЯЕТ СВОЙ ПУНКТ ИСТОЧНИКА, и прежде он был бесхозным.**
English Grammar Profile, `1741163716067x824562184770361200`, `NEGATION /
negation`, FORM: MAIN VERB 'BE', **A1** — «Can form negative statements of main
verb 'be', with contracted and uncontracted forms». Примеры графы: «My college is
not very big», «I know you are not well», «I'm not a good swimmer», «This place
isn’t big» — три из четырёх с прилагательным после `be`, два буквально о размере.
Обе записи, `is not` и `isn't`, названы в самой формулировке графы, и случай даёт
обе. Разбор, почему пункт берётся именно здесь, — замысел модуля, раздел 4.3.

**Обе записи — `is not` и `isn't` — принимаются везде, где ученик пишет сам.**
Решение курса, приписанное по разбору модуля 7: шесть заданий серии принимали
одну запись из двух, и ученик, написавший верно, получал «пока не так».
**Ограничение, если оно понадобится, стоит в условии, а не в подсказке:**
подсказка — дорога, а не ответ, и открывает её не каждый.

### Случай 5 — спросить, какая вещь

Что разбирается: возврат к модулю 3 (`Are you from Spain?`) и к модулю 7
(`Is this a key?`). Порядок обратный: в вопросе `is` идёт вперёд.

| Строка | Перевод |
|---|---|
| Is the box big? — Yes, it is. | Коробка большая? — Да. |
| Is the car new? — No, it is not. | Машина новая? — Нет. |

**Это единственное трудное место урока, и пропускать его нельзя:** в вопросе
`Is the box…`, а в ответе `it is` — порядок обратный. Редактор модуля 1
ступени Elementary нашёл, что при сокращении объяснений именно это место
исчезает первым.

**Слово `it` здесь не разбирается, а употребляется.** Ученик получил его в
модуле 7 (`It is a table.`), и короткий ответ `Yes, it is` — тоже итог модуля 7.
Русское соответствие «он / она / оно» разбирает урок 2, и только он.

**ЛОВУШКА ПОРОДЫ 14, И ОНА ЗДЕСЬ ГЛАВНАЯ.** У вопроса `Is the box big?` два
верных ответа, и какой верен, решает не язык, а картинка. **Каждое задание этого
урока с коротким ответом обязано либо показывать вещь значком, либо называть её
словом в условии**, и условие говорит прямо: «Ответь: да», а не «ты знаешь, что
коробка большая».

**Опора:** `1741163708336x928067678664856700`, `CLAUSES / interrogatives`, FORM:
AFFIRMATIVE INTERROGATIVE, WITH BE, A1; и `1741163713867x150062658595893500`,
`PRONOUNS`, FORM: (SUBJECT) QUESTIONS WITH BE, A1 — «Can use the pronouns you and
**it** after be in question forms». Оба введены модулем 3, здесь `egpPovtor`.

### Случай 6 — `old` не только о возрасте

Что разбирается: слово, которое ученик получил в модуле 4 внутри оборота
`I am ten years old`, работает и о вещи. Русский перевод там и тут разный:
«мне десять лет» и «старый».

| Строка | Перевод |
|---|---|
| I am ten years old. | Мне десять лет. |
| My phone is old. | Мой телефон старый. |

**Ссылка на модуль 4 называет его тем именем, которое видит ученик** — «Числа,
возраст, телефон». Служебного имени `chisla-i-telefon` в тексте быть не должно:
`check:content` следит за этим, и модуль 22 прошлой ступени на этом попался.

**Это законный случай, а не добор:** правило проекта требует называть границу
там, где ученик на неё наткнётся, и `old` — ровно такое место.

### Случай 7 — своя вещь: `My box is new`

Что разбирается: возврат к модулю 5 (притяжательные). Вместо `the` перед
названием вещи может стоять `my` или `your`, а строй остаётся тот же.

| Строка | Перевод |
|---|---|
| My box is new. | Моя коробка новая. |
| Your pen is short. | Твоя ручка короткая. |

**Почему этот случай стоит последним:** нового строя он не даёт, он показывает,
что строй урока работает не только с `the`. Урок письма модуля стоит именно на
нём — там ученик пишет о СВОЕЙ вещи.

**Чего здесь нет:** `his` и `her`. Они карточки модуля 5, но о вещи чужого
человека урок не говорит, и лишняя пара увела бы к людям — это модуль 10.

---

## 4. Чего в уроке нет и почему

| Что просится | Почему не здесь | Куда |
|---|---|---|
| `it` вместо названия вещи, «он / она / оно» | долг модуля 7 закрывается ОДИН раз, и там, где `it` нужно для цепочки «назвал вещь — говорю о её цвете» | урок 2 |
| цвет (`The box is red`) | девять слов сразу — это отдельный урок | урок 2 |
| `a big box` | другой строй, отдельные пункты EGP | урок 4 |
| `big boxes`, `The boxes are big` | множественное с прилагательным — отдельный пункт | урок 5 |
| форма `are` при нескольких вещах | правило урока 1 обещает её с адресом, но не разбирает | урок 5 |
| `very big` | своя пятёрка пунктов | урок 6 |
| `a big red box` | **два прилагательных подряд — A2** (замысел модуля, раздел 3.2) | нет на ступени |
| `big and new` | пункт отдан модулю 10 | модуль 10 |
| `tall`, `young`, `nice` | это о людях | модуль 10 |
| `It is cold` | пустое `it` — A2, снято со ступени | нет на ступени |
| `Is it big or small?` | `or` между прилагательными на ступени не разобран | нет в модуле |

---

## 5. Карточки урока — шесть

| Слово | Перевод | Ступень | Чтение | Значок | Где работает |
|---|---|---|---|---|---|
| `big` | большой | Oxford **A1**, словник A2 Key — да | `/bɪg/` | нет | случаи 1, 3, 5; задания 1, 2, 3 |
| `small` | маленький | Oxford **A1**, словник — да | `/smɔːl/` | нет | случаи 3, 4; задания 1, 3 |
| `long` | длинный | Oxford **A1**, словник — да | `/lɒŋ/` | нет | случаи 2, 3; задания 3, 6 |
| `short` | короткий | Oxford **A1**, словник — да | `/ʃɔːt/` | нет | случаи 2, 3, 7; задание 3 |
| `new` | новый | Oxford **A1**, словник — да | `/njuː/` | нет | сцена, случаи 1, 3, 4, 5, 7; задания 3, 5 |
| `car` | машина | Oxford **A1**, словник — да | `/kɑː/` | `1f697` | случаи 1, 4, 5; задания 1, 4 |

**Столбец «где работает» сверен с таблицей заданий строка в строку** — сборщик
собирает словарь урока по этой таблице, и расхождение уехало бы в код. Первая
редакция замысла модуля 8 на этом попалась.

**Ни одно слово ступень не превышает:** шесть из шести стоят у Oxford 3000 на A1
и в словнике A2 Key (`npm run slovo`, каждое отдельно). Объявлять в шапке
нечего.

**Примеры карточек не совпадают ни с одним ответом задания:** `big` —
`The box is big.`, `small` — `The box isn't small.`, `long` — `The table is
long.`, `short` — `Your pen is short.`, `new` — `The car is new.`, `car` —
`My car is new.` Мерка правила проекта: в словарь ученик заходит нарочно, значит
пример карточки считается напечатанным выше при любом расстоянии.

### 5.1. Значков у пяти карточек нет — и это спор с решением владельца, а не недосмотр

`big`, `small`, `long`, `short`, `new` — не вещи, а прилагательные, и любой
значок показал бы вещь вместо прилагательного (слон вместо «большой», мышь
вместо «маленький»). Правило методиста модуля 8: «неверная картинка хуже её
отсутствия». Прилагательное показывается в ЗАДАНИИ — двумя картинками одной вещи
разного размера, а не значком у карточки.

**НО РЕШЕНИЕ ВЛАДЕЛЬЦА ОТ 6 СЕНТЯБРЯ ГОВОРИТ ИНАЧЕ, и назвать его надо здесь, а
не умолчать** (`otvety-vladeltsa-6-sentyabrya.md`, снимок 2): «на многое из них
можно сделать картинку. писатель допустим перо и т.д. **Желательно не оставляй
без картинок ни одну карточку**». Там же разбор: «Служебные слова (`what`,
`this`, `it`) картинки не имеют по существу; **вещи, занятия и всё, что
рисуется, — обязаны**».

Прилагательное вещью и занятием не является, но и служебным словом его владелец
не называл. **Это вопрос к нему, и он стоит в замысле модуля, раздел 10, строка
6.** Готовые ходы, если он решит, что значок нужен: у `new` есть знак `1f195`;
для пар можно взять две картинки одной вещи разного размера прямо в карточке,
если сборщик такое умеет — но это правка кода, а не содержания.

**Чтение взято со склада** замороженного курса (`kakoy-on.ts`), где условности
курса уже применены: `r` как `r`, `g` как `g`, краткий гласный через `e`.
**Сборщику сверить** — записей звука ни у одного из шести слов в
`public/zvuk/slovo/` может не быть, их сделает `npm run ozvuchka`.

**Карточка `old` в уроке НЕ заводится** — она стоит в модуле 4. Слово работает в
случаях 3, 6 и в заданиях 2, 3, 4, но словарь урока его не содержит. Проверка
долга словаря (`kontrol`) промолчит: карточка есть и стоит раньше.

**Слова урока, у которых карточки нет и не будет:** `the`, `a`, `is`, `not`,
`isn't`, `it`, `my`, `your`, `yes`, `no` — служебные, все введены модулями 1–7.
`box`, `pen`, `table`, `phone` — карточки модулей 1, 4 и 7.

---

## 6. Задания: семь, лестницей

Порядок ступенек — узнать, произвести, применить к себе. Подписей «ступенька 2»
на экране нет: лестница — наша кухня, и слово «ступень» в курсе занято ступенями
A1 и A2.

**ТРИ ЗАДАНИЯ ПЕРЕПИСАНЫ ПОСЛЕ РАЗБОРА, И ВОТ ПОЧЕМУ.** Прежняя редакция брала
себе два из четырёх углов, которые сам же замысел модуля объявил работе и
запретил урокам (раздел 8 замысла модуля), а третье задание проходилось не
думая.

| Было | Чем это плохо | Стало |
|---|---|---|
| 5: «Ким написал: `My phone new.` Запиши строку без ошибки» | угол «исправить чужую запись» отдан работе (вопросы 2 и 10); вдобавок после правки записки ответ стоял бы на первом экране урока | 5: `short` — записать СВОЮ строку по условию, не исправляя чужую |
| 4: `order`, собрать **вопрос** `Is the car old?` | угол «собрать вопрос из кусков» был отдан работе (вопрос 7) | угол переразделён: `order` отдан УРОКАМ, вопрос 7 работы переведён на `short` (раздел 8.1 замысла модуля) |
| 1: `choice` по картинке большой машины | близнец вопроса 1 работы: две строки из трёх совпадали дословно, а верный ответ работы был отвлекающим урока | вопрос 1 работы переведён с `choice` на `gap`; задание урока осталось |
| 6: `gap`, ответ `It` | стояло на снятом случае 5 | снято вместе со случаем |
| 2: `hottext`, нажимаемы только два слова, и оба верные | ученик нажимает обе кнопки, ничего не разбирая | нажимаемы все знаменательные слова, верных два |

| № | Вид | Что делает ученик | На каком блоке стоит |
|---|---|---|---|
| 1 | `choice` | под картинкой большой машины выбирает подпись: `The car is big.` · `The car big.` · `The car is small.` | случаи 1, 2, 3 |
| 2 | `hottext` | в строке `The box is old. My car is big.` отмечает слова, которые говорят, КАКАЯ вещь | случай 1, термин |
| 3 | `match` | соединяет три слова с их парой: `big` — `small`, `long` — `short`, `new` — `old` | случай 3, таблица |
| 4 | `order` | собирает `Is the car old?` из `old` · `the` · `Is` · `car` | случаи 5, 6 |
| 5 | `short` | «Дана говорит о своей ручке: она не новая. Запиши её строку целиком, начав со слова My» | случаи 4, 7 |
| 6 | `speak` | называет вслух, какая вещь у него рядом; образец записью — `My table is long.` | случай 7 |
| 7 | `vozvrat` | задание модуля 7 «Что это такое», урок «Это не ручка: It isn't a pen» — `r3-dopisat-isnt` | модуль 7 |

**Заданий семь, а не восемь, потому что случаев семь, а не восемь.** Мерка —
ориентир (ответы владельца 1 и 18 от 6 сентября), и добирать восьмое ради счёта
значило бы делать ровно то, что он запретил.

**Блоков без задания в уроке нет, и заданий без блока тоже.** Случай 1 работает в
заданиях 1 и 2, случай 2 — в 1 и 5, случай 3 — в 3, случай 4 — в 5, случай 5 —
в 4, случай 6 — в 3 и 4, случай 7 — в 5 и 6.

### 6.1. Проверки, прогнанные по каждому заданию

**Мерка 1 — строка-ответ не совпадает дословно ни с примером, ни со сценой.**
Прогнал по всем четырнадцати примерам, трём строкам записки и таблице пар.
Ответы: `The car is big.` (в уроке не печатается: там `The box is big.` и
`The car is new.`), отмеченные `old` и `big`, три пары, `Is the car old?`
(в уроке `Is the box big?` и `Is the car new?`), `My pen is not new.`
(в уроке `Your pen is short.` и `My box is new.`), произнесённая строка о своей
вещи.

**Мерка 2 — пример словарной карточки считается напечатанным выше при любом
расстоянии.** Шесть примеров карточек перечислены в разделе 5; ни один не равен
ни одному ответу задания.

**Мерка 3 — верный вариант не единственный из напечатанных.** В задании 1 верный
вариант `The car is big.` в уроке не печатался, а отвлекающий `The car is small.`
повторяет строй случаев 1 и 3. Узнавание по знакомому виду строки ведёт к
неверному ответу, а не к верному.

**Мерка 4 — все строки задания на сличение стоят в одинаковом отношении к
уроку.** Задание 1 сличением не является (там картинка решает), поэтому правило
к нему не применяется. Задание 5 — свободная строка, там вариантов нет.

**Мерка 5 — второго верного ответа нет.**

- задание 1: картинка показывает большую машину, и `The car is small.` неверно
  по картинке, а `The car big.` — по языку. Верный один;
- задание 2: **нажимаемы все знаменательные слова** — `The`, `box`, `is`, `old`,
  `My`, `car`, `is`, `big`, — а верных два: `old` и `big`. Прежняя редакция
  делала нажимаемыми только два слова, и оба были верными: на экране стояли две
  кнопки, и ученик нажимал обе, ничего не разбирая. Курс так не делает —
  `chto-eto-takoe.ts`, строки 1164–1172: нажимаемы четыре слова, верных два.
  Второго верного набора не возникает: `is`, `The`, `My` и названия вещей о том,
  КАКАЯ вещь, не говорят;
- задание 3: пары взяты из таблицы, третьего варианта нет;
- задание 4: **порядок один.** Из кусков `old` · `the` · `Is` · `car`
  собирается ровно одна верная строка. `Is` набрано с заглавной, значит стоять
  оно может только первым, а `the car` иначе не переставить;
- задание 5: два верных ответа, и **оба обязаны стоять в `accept`** —
  `My pen is not new.` и `My pen isn't new.` Решение курса от разбора модуля 7:
  обе записи принимаются везде, где ученик пишет сам. Условие называет вещь
  («о своей ручке») и первое слово («начав со слова My»), иначе верными стали бы
  и `The pen is not new.`, и `It is not new.`;
- задание 6: `speak`, машина не оценивает. **Условие называет, из каких слов
  выбирать** — правило от 6 сентября: границу открытого задания задаёт то, что
  напечатано на экране, а не поле `accept`. Слова урока: `big`, `small`, `long`,
  `short`, `new`;
- задание 7: чужое задание, проверено в модуле 7.

**Мерка 6 — задание не проходится, не читая урока.** Задание 1 без урока
проходится с вероятностью один к трём: картинка отсекает `small`, а между
`is big` и `big` без урока не выбрать. Задание 5 без урока не проходится вовсе.

**Мерка 7 — задание проверяет ИТОГ, а не соседнее умение.** Итог урока —
«говорить, какая вещь». Задания 4, 5, 6 требуют произвести строку с
прилагательным; задания 1, 2, 3 — узнать. Доля производящих — три из семи, и
лестница «узнать → произвести → применить к себе» на них и стоит: 1–3 узнать,
4–5 произвести, 6–7 применить к себе.

**Мерка 8 — ни одно задание не повторяет ДЕЙСТВИЯ вопроса работы на тот же
итог.** Работа спрашивает итог 1 тремя вопросами: `gap` с пропущенным `is`,
исправление чужой записи, называние причины ошибки. Ни одного из этих трёх
действий в уроке нет. Разделение углов целиком — раздел 8.1 замысла модуля.
**После сборки проверить прогоном `npm run bliznetsy`:** по ненаписанному модулю
он бессмыслен, а здесь всё сделано чтением.

**Мерка 9 — верный ответ не напечатан во врезке выше.** Врезок в уроке две:
«так сказать нельзя» (случай 2) и «две записи `is not`» (случай 4). Ни одна не
печатает ответа ни одного задания. **Осторожно с врезкой случая 4:** ответ
задания 5 — отрицание, и если редактор напишет во врезке готовую строку с
`is not`, совпадение появится. **Сборщику проверить заново после того, как
редактор напишет текст:** правка врезки переносит совпадение, и на этом курс уже
обжигался дважды.

### 6.2. Отвлекающие задания 1: обе проверки

**`The car big.`** — настоящая ошибка, и главная в уроке: русскоязычный
пропускает `is`, потому что по-русски глагола там нет. Ученика, который её
делает, представить легко — он сам её и сделает на следующем экране.

**`The car is small.`** — ошибка по картинке, а не по языку. Строка правильная, и
это законный вид отвлекающего: она неверна по УСЛОВИЮ. Тот же приём применил
модуль 8 (`a carrot` при трёх морковках).

**Чего среди отвлекающих нет:** `The car is bigs`, `The car are big`. Первую не
пишет никто: окончания `-s` у прилагательного ученик ещё не видел и приписать его
неоткуда. Вторая — ошибка модуля 8 (число), а не этого урока.

---

## 7. Что урок обещает и где обещание закрывается

Долги называются с адресом — правило проекта, и адресом должно быть имя урока
или модуля, а не «позже».

| Долг | Адрес |
|---|---|
| форма `are`, когда вещей несколько | урок 5 модуля, «Красные яблоки: red apples» |
| `it` вместо названия вещи: «он», «она», «оно» | урок 2 модуля, «Какого цвета: The car is red» |
| цвет вещи | урок 2 модуля, «Какого цвета: The car is red» |
| `a big box` — прилагательное перед названием | урок 4 модуля, «Красная машина: a red car» |
| несколько вещей сразу | урок 5 модуля, «Красные яблоки: red apples» |
| `very` | урок 6 модуля, «Очень: very big» |

**Первый долг — новый, и он обязателен.** Без него правило случая 1 читается как
утверждение о языке, а урок 5 показывает `The apples are red.` и опровергает
его. Разбор — раздел 1 и замысел модуля, раздел 5.2.

**Ссылок на модуль 10 и дальше в тексте урока нет** — правило от 6 сентября: о
ненаписанном модуле не утверждаем ничего, даже мимоходом.

---

## 8. Звук урока

| Что | Как |
|---|---|
| три строки записки | три записи, один голос |
| четырнадцать строк примеров | по одной записи на строку |
| клетки таблицы пар | по одной записи на слово: `big`, `small`, `long`, `short`, `new`, `old` |
| шесть слов словаря | по одной записи |
| образец задания 6 | одна запись |
| строки «так сказать нельзя» | **звука нет** |
| русские подписи и переводы | **звука нет** |

**Кнопка стоит у строки, а не отдельным блоком снизу** — решение владельца от
19 августа.

**И ловушка, которую надо помнить редактору** (правило от 6 сентября): `sZvukom`
прогоняется и по имени случая, и по каждой строке правила. Значит английская
строка, названная в правиле, ставит на экране вторую кнопку рядом с той, что у
примера. **Правило говорит О строке, а не повторяет её:** не «и получается
`The box is big`», а «между названием вещи и словом о ней встаёт форма глагола
`be`; при одной вещи это `is`».
**И заголовок случая английской строки не содержит** — иначе кнопок станет три.
Рабочие заголовки случаев в этом файле — мои, для сборщика; **редактор пишет
русские имена случаев заново и без английских строк**, включая случай 7, который
я назвал здесь `My box is new` только ради узнаваемости.

---

## 9. Что проверено и чего проверить не удалось

**Проверено:**

- ступень шести карточек — `npm run slovo`, каждое слово отдельно;
- что `old`, `black`, `phone`, `box`, `table`, `pen`, `my`, `your`, `it` уже даны
  карточками РАНЬШЕ модуля 9 — `grep` по `term:` и порядок из `index.ts`
  (`alfavit.ts` — `my`, `your`, `pen`, `box`, `black`; `chisla-i-telefon.ts` —
  `old`, `phone`; `chto-eto-takoe.ts` — `table`, `it`);
- опора случая 1 — English Grammar Profile, открыт по `id` разбором CSV,
  уровень A1, пример графы выписан целиком;
- опора случая 4 — `NEGATION, FORM: MAIN VERB 'BE'`, строка 3315 файла
  `materials/English Grammar Profile Online.csv`, уровень A1, все четыре примера
  графы выписаны; поиск по `courses/` и `docs/beginner-2/` показал, что пункт не
  заявлен никем, кроме двух отказов;
- опоры случаев 4 и 5 из модуля 3 — проверено по коду, что модуль 3 их и
  объявил;
- долг модуля 7 про `it` — открыт в `plan-modul-7-urok-1.md`, строки 181–182, и
  строка `It is black.` названа там дословно; она стоит в уроке 2, а не здесь;
- правило модуля 7 про `this` — прогон `npm run tekst -- english-beginner-2
  chto-eto-takoe`: «Спрашивают о вещи словом `this`, потому что на неё
  показывают». Ради него переписана записка вступления;
- устройство `hottext` в курсе — `chto-eto-takoe.ts`, строки 1164–1172: четыре
  нажимаемых слова, два верных;
- запрет на два прилагательных подряд — по всем четырнадцати примерам урока: ни
  одной такой строки нет;
- имя рассказчика в строках сцены — нет.

**Чего проверить не удалось:**

1. **Не сломает ли блок записки вывод вида урока.** Утверждение о коде сделано
   чтением `lib/content/vid-uroka.ts`, а правило проекта требует прогона.
   Сборщику проверить.
2. **Есть ли записи звука у шести слов урока.** `adresSlova()` я не прогонял.
3. **Длина урока.** Семь случаев, таблица, две врезки, словарь и семь заданий
   дают около восемнадцати экранов. Владелец ответил 6 сентября: «пиши по факту,
   не надо искусственно удлинять или укорачивать» (ответ 1), — значит вопроса
   здесь больше нет.
4. **Близнецы.** Развод углов урока и работы (мерка 8) сделан чтением. После
   сборки обязателен прогон `npm run bliznetsy`, особо по паре «задания урока 1
   против вопросов 1–3 работы» и по заданию 7: оно прямой возврат к
   `r3-dopisat-isnt`, и это законно (вид `vozvrat`), но проверка может назвать
   его совпадением — тогда в `resheno.ts` с причиной.
5. **Значки у пяти прилагательных.** Решение владельца от 6 сентября требует
   картинку у каждой карточки, довод против — «неверная картинка хуже её
   отсутствия». Спор к владельцу, разбор — раздел 5.1.
