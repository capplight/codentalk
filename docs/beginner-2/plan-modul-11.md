# Модуль 11 «Одежда». Замысел

6 сентября 2026. Пишет методист. **Весь видимый русский текст — вступления,
объяснения случаев, врезки, подписи, условия, подсказки и разборы заданий —
пишет РЕДАКТОР.** Здесь стоят состав, порядок, английские строки, опоры,
запреты и решения; ничего из того, что ученик прочтёт по-русски, здесь нет.

Подробный замысел первого урока — [plan-modul-11-urok-1.md](plan-modul-11-urok-1.md).

Строка принятой программы ([programma-stupeni.md](programma-stupeni.md),
раздел 8, часть 2 «Вещи и место»):

| № | Модуль | Итог | Грамматика | Тема | Опора |
|---|---|---|---|---|---|
| 11 | Одежда | сказать, что надето, и спросить о цвете | `have got`, прилагательное | Clothes and Accessories | CEFR с. 79 |

**Одна половина этой строки исполнена быть не может, и я говорю об этом сразу.**
Разбор — раздел 2.4: «что надето» по-английски говорят `She is wearing a hat`
(Present Continuous, модуль 17) или `I wear jeans` (Present Simple, модуль 13), а
ни одного из двух времён у ученика к модулю 11 ещё нет. Модуль говорит о том же
деле двумя строями, которые ступень даёт: `I have got a red jacket.` и
`My shoes are white.` **Итог модуля переписан по тому, чему модуль вправду
учит**, а слово `wear` передано модулю 17 с записью в обе стороны.

---

## 0. Шапка: три вещи, названные ДО первой строки редактора

Правило приписано 6 сентября 2026 методистом модуля 8: два имени назвали после
того, как редактор написал весь текст, и в уроке стало двадцать восемь заглушек
вместо замены слова.

### 0.1. Имя главной вещи модуля — **оборот `have got`**

Главное новое в модуле — не слова об одежде, а способ сказать «у меня есть».
Курс зовёт такую вещь **оборотом**, и другого имени у неё в модуле нет ни разу:
ни «конструкция», ни «выражение», ни «глагол have».

**Почему «оборот», а не термин русской грамматики.** Правило проекта требует
настоящего имени там, где оно у вещи есть: «есть ли у этой вещи имя в русской
грамматике?» У `have got` его нет — это устройство английского языка, а не
русского, и подобрать ему русский разряд нельзя. Слово «оборот» при этом не
выдумано нами: курс уже зовёт так `there is` (`plan-modul-12.md`, раздел 6.2) и
звал «оборотом времени» на ступени Elementary. Это обычное русское слово об
устойчивом сочетании, а не ярлык взамен существующего термина.

**Где имя называется:** урок 1, случай 1, строкой внутри случая — не отдельным
экраном. Правило от 6 сентября: «настоящий термин называется всегда, но
отдельного экрана не получает».

**Редактору: слова «глагол» рядом с `have got` в модуле нет.** Урок не говорит
«глагол have» и «вспомогательный глагол»: устройство оборота ступень разобрать
не может, а имя, которого урок не объясняет, ученику ничего не даёт.

### 0.2. Заимствованные термины и адреса, где они введены

| Термин | Кто дал | Как модуль 11 им пользуется |
|---|---|---|
| **артикль** | модуль 3 «Откуда ты и где живёшь», уроки «Один из таких: a и an» и «Тот самый: the» | берём как известное: `a hat`, `an umbrella`. Заново не объясняем |
| **множественное число** | модуль 8 «Один и много», урок 1 «Яблоко и яблоки: a уходит» | берём как известное. Урок 4 добавляет одно: у четырёх слов есть только эта форма |
| **окончание** | модуль 4 «Числа, возраст, телефон», урок 2 «Два предмета: буква -s» | `shoes`, `hats`. Слова «хвост» в модуле нет ни разу — находка редактора модуля 8 |
| **прилагательное** | модуль 9 «Цвет и размер», урок 1 | `a red hat`, `My bag is black`. Имя вводит модуль 9, модуль 11 второй раз его НЕ вводит |
| **местоимение** | модуль 2 «Здравствуйте, меня зовут» | таблица форм оборота; `They are white.` |
| **цвет** | не термин, а обычное слово; карточка `colour` — модуль 9, урок 3 | вопрос `What colour are your shoes?` |

### 0.3. Общее слово, которым модуль зовёт то, о чём говорит, — **вещь**

Как у модулей 7 и 9 и по доводу модуля 7, записанному дословно: «„предмет"
по-русски значит ещё и школьный предмет, а тема `Education` придёт модулем 13».
Модуль 8 взял «предмет», расхождение в части 2 уже есть; я держу слово модуля 7,
потому что стою на его карточке `thing`, переведённой «вещь».

**Слово «одежда» при этом — не общее слово модуля, а название темы.** Оно годится
там, где речь вправду об одежде (`clothes` — карточка урока 4), и не годится там,
где строка о сумке, зонте или очках: сумка одеждой не является. Общее слово на
такие строки — **вещь**.

**Строка сводящему:** расхождение «вещь» (модули 7, 9, 11) против «предмет»
(модуль 8) сводится по всей части 2 разом, а не помодульно. Я выбираю «вещь» и
называю это здесь, чтобы следующий не выбирал заново.

### 0.4. Строка расхождения источников — в шапку файла модуля

Пишется не нам, а следующему проверяющему: без неё он снимет половину модуля как
превышение ступени и будет прав по своему источнику. Разбор каждой строки —
раздел 3.

> **1. Оборот `have got` взят по руководству Cambridge.** Young Learners,
> **с. 29**, Grammar and Structures List, ступень **Pre A1 Starters**, раздел
> `Verbs` («Positive, negative, question, imperative and short-answer forms,
> including contractions»), строка **«Have (got) for possession»**, примеры
> «Have you got a pen?» и «She hasn't got a dog.»; там же строка `Conjunctions`
> — «I've got a pen and a pencil.» **English Grammar Profile этого оборота не
> держит ни на одной ступени:** разобраны все 1222 записи настоящим разбором CSV
> и пройдены поиском `have`, `got`, `possession` — есть только
> `MODALITY / have (got) to` (обязанность, A2 и выше) и вспомогательный `have`
> для перфекта (A2). **Это не расхождение источников, а молчание одного из
> двух:** единственный источник, который оборот вообще размечает по ступеням,
> ставит его на самую младшую.
>
> **2. Слова, у которых есть только форма множественного числа** — `trousers`,
> `jeans`, `glasses`, `clothes` — взяты по руководству Cambridge: Young
> Learners, **с. 79**, тематический список `Clothes`, столбец **PRE A1
> STARTERS** держит `clothes`, `glasses`, `jeans`, `trousers` поимённо. English
> Grammar Profile ставит правило о таких словах на **A2**:
> `1741163711822x913552415415866000`, `NOUNS / plural`, FORM: NOUNS ONLY USED IN
> THE PLURAL, — и его примеры это ровно наши слова: «The clothes are very
> cheap», «The jeans were expensive», «I left my glasses in your house».
> Решение владельца от 6 сентября 2026: где Cambridge даёт на Pre A1 Starters, а
> English Grammar Profile кладёт на A2, курс берёт по Cambridge.
>
> **3. Вопрос `What colour are your shoes?`** стоит на том же расхождении, что и
> `What colour is it?` в модуле 9: English Grammar Profile,
> `1741163715031x898125206259626100`, `QUESTIONS / wh-`, FORM: MAIN VERB 'BE',
> **A2**; руководство Cambridge, Young Learners, **с. 28**, Pre A1 Starters
> Speaking Part 3, печатает «What colour is it? (green)». Согласование при
> нескольких вещах ученик получил в модуле 9, урок 5 (`Are the apples red?`).
>
> **4. Два слова взяты не по Oxford, а по Cambridge, и одно наоборот.** `sock` —
> Oxford 3000 ставит **A2**, а Young Learners, с. 79, столбец Pre A1 Starters,
> держит его поимённо. `umbrella` — наоборот: Oxford 3000 ставит **A1**, а Young
> Learners, с. 79, кладёт его в столбец A2 Flyers. Оба взяты по младшей помете —
> тем же ходом, каким владелец 6 сентября решил `airport` («Oxford A1 против
> Cambridge A2 Flyers — бери»).

---

## 1. Сперва курс, потом источник

Правило проекта: проверка по курсу идёт ПЕРВОЙ. Она нажита на четырёх случаях
подряд, когда модуль писал второй урок об уже написанном.

### 1.1. Что нашлось в коде нового курса

Прогоны:

```
grep -n "outcome:\|title:" courses/english-beginner-2/*.ts | grep -iE "одежд|есть|цвет"
grep -o 'term: "[^"]*"' courses/english-beginner-2/*.ts
grep -rn "have got\|has got" courses/english-beginner-2/*.ts
```

**Находка 1. Ни одного слова одежды в курсе нет.** Все 179 карточек восьми
написанных модулей выписаны и просмотрены поимённо: ни `hat`, ни `bag`, ни
`shirt`, ни `shoes`. Слово `bag` стоит карточкой в ЗАМОРОЖЕННОМ курсе
(`english-starter/chto-eto-takoe.ts`), а в новом его нет.

**Находка 2. `have got` в видимом тексте курса не встречается ни разу.** Пять
совпадений `grep` лежат в комментариях и в полях `sources` (модули 4, 5, 6, 8),
и все говорят одно: оборот роздан модулю 11.

**Находка 3. Три слова, похожих на мои, уже заняты, и все три придётся
разводить:**

| Слово в коде | Где | Чем грозит модулю 11 |
|---|---|---|
| `glass` — «стакан», форма `glasses` в таблице | модуль 8, урок 2 | `glasses` — «очки» будет ТОЙ ЖЕ строкой. Разбор — раздел 6.4 |
| `short` — «короткий» | модуль 9, урок 1 (замысел) | `shorts` — «шорты» прочтётся как форма множественного от него. Слово не беру, раздел 6.5 |
| `clock` — «часы» | модуль 7, урок 1 | `watch` пришлось бы переводить тем же русским словом. Слово не беру, раздел 6.5 |

**Находка 4. Прилагательное после `be` и перед названием вещи в коде ещё не
стоит** — его вводит модуль 9, а он не написан. Значит все мои строки вида
`My bag is black` и `a red hat` опираются на замысел соседа, а не на код, и
сборщик обязан сверить это заново перед сборкой (раздел 12).

### 1.2. Что нашлось в замыслах соседей

Пока модуль не написан, его замысел И ЕСТЬ модуль. Прочитаны целиком
`plan-modul-2.md` … `plan-modul-10.md`, `plan-modul-12.md`,
`razbor-plana-modul-9.md` и `otvety-vladeltsa-6-sentyabrya.md`. Модулю 11
адресованы **восемь** строк.

| Откуда | Что сказано | Мой ответ |
|---|---|---|
| `razbor-plana-modul-9.md`, строки 612–690 | **граница модулей 9 и 11**, решённая вторым методистом по слову владельца | принимаю целиком и переношу к себе, раздел 1.3 |
| `plan-modul-9.md`, строки 168–189 | «модуль 11 не вводит ни одной карточки цвета и не повторяет разбора `What colour is it?`» | принято, разделы 4.2 и 6 |
| `plan-modul-10.md`, раздел 11.2 | три строки мне: слова о человеке и части тела — модуля 10; **`have got` целиком мой** | принято, ни одного слова о внешности не беру |
| `plan-modul-10.md`, раздел 1.5 | «одежду модуль 10 не называет: ни `dress`, ни `hat`, ни `shirt`» | принято; сборщику проверить `grep` по обоим |
| `plan-modul-7.md`, строки 239 и 1101 | `I have got a book.` — «`have got` роздан модулю 11» | принято |
| `plan-modul-6.md`, строка 1226 | притяжательный апостроф (`It is my sister's.`) предложен модулю 11 **или** модулю 12 | **не беру**, разбор и адрес — раздел 11, строка 2 |
| `plan-modul-6-urok-1.md`, строка 435 | «`have got` — оборот стоит в модуле 11» | принято |
| `plan-modul-8.md`, раздел 10 | `some` и `a lot of` — модуля 21 | принято, в модуле 11 их нет |

**Осторожно: замыслы модулей 9, 10 и 12 писались в те же сутки, что и мой.**
Файлы `plan-modul-10.md` и `plan-modul-12.md` легли на диск 6 сентября в 22:45 и
22:46, `plan-modul-9.md` правился в 19:22. Правило проекта «замысел верен только
на дату» здесь не отвлечённое: **сборщик обязан перечитать разделы 1.2 и 6
заново перед сборкой**, а не взять их отсюда.

### 1.3. Граница с модулем 9 — принята, не пересматривается

Вопрос задавал владелец, он же передал его методисту («слушай методиста»,
`otvety-vladeltsa-6-sentyabrya.md`, ответ 19); решение принял второй методист
(`razbor-plana-modul-9.md`, раздел «Ответ на первый вопрос владельца»). Границу
называет сам дескриптор: CEFR с. 79, A1 — «Can name the colour of **clothes or
other familiar objects**».

| | Модуль 9 «Цвет и размер» | Модуль 11 «Одежда» |
|---|---|---|
| половина дескриптора | **other familiar objects** — вещи | **clothes** — одежда |
| карточки цвета | вводит все девять; тема `Colours` объявлена им | **не вводит ни одной** |
| `colour`, `favourite` | его карточки | берёт данными |
| устройство прилагательного: после `be`, перед названием, при нескольких вещах, `very` | вводит целиком, шесть уроков правил | берёт данным, ставит в `egpPovtor` |
| вопрос о цвете **об одной вещи** — `What colour is it?` | **вводит и разбирает** | берёт как известный, устройства не разбирает |
| вопрос о цвете **о нескольких вещах** — `What colour are your shoes? — They are white.` | не берёт вовсе | **его собственная половина** |
| грамматика сверх этого | — | `have got`, одежда |
| вещи | машина, коробка, стол, часы, дверь, телефон, еда | одежда, и только она |

**Три запрета, без которых граница не держится** (переношу дословно):

1. модуль 9 не берёт ни одного слова одежды;
2. **модуль 11 не заводит ни одной карточки цвета** и не повторяет разбора
   `What colour is it?`;
3. `What colour is …?` разбирается один раз, в модуле 9; `What colour are …?` —
   один раз, в модуле 11.

**Слово `dark` — модуля 12**, я его не беру (там же).

### 1.4. Что берём со склада замороженного курса

`courses/english-starter/moi-veshchi.ts` — модуль «Мои вещи», уроки 3–5 («У меня
есть: I've got», «У меня нет: haven't got», «Есть ли у тебя: Have you got?»).
`courses/english-elementary/mozhet-byt.ts` и `kto-to-i-chto-to.ts` — карточки
одежды.

**Берём:**

1. **готовые транскрипции, уже приведённые к условностям курса** (`r` вместо
   `ɹ`, `g` вместо `ɡ`, слоговой согласный без `ə`): `bag /bæg/`,
   `glasses /ˈglɑːsɪz/`, `umbrella /ʌmˈbrelə/`, `sock /sɒk/`,
   `t-shirt /ˈtiː ʃɜːt/`;
2. **четыре готовые записи звука** — `sock`, `bag`, `glasses`, `umbrella`.
   Проверено ПРОГОНОМ `adresSlova()` по `public/zvuk/slovo/`, а не чтением
   соседнего места;
3. **устройство трёх уроков подряд** — утверждение, отрицание, вопрос: тот же
   порядок беру я и по той же причине (вопрос требует, чтобы обе первые формы
   уже стояли).

**Не берём:**

1. **ни одного задания и ни одного вопроса работы** — правило переделки;
2. **объяснений** — ради них курс и переделывается;
3. **уроков «Мой и твой» и «Его, её, наш»** — притяжательные вводит модуль 2,
   повторяет модуль 5;
4. **урока «Чья это вещь»** — там притяжательный апостроф, которого я не беру
   (раздел 11, строка 2);
5. **строк `He's got` и `She's got`** — разбор в разделе 5.6.

---

## 2. На чём модуль стоит: дескрипторы

**Правило ступени: нет дескриптора — модуль не заводится, и исключений нет.**
У модуля 11 их три, и все три я открыл сам.

### 2.1. Дескриптор ДЕЛА — CEFR Companion 2020, с. 79, графа A1

Шкала *Information exchange*, дословно:

> Can name the colour of **clothes** or other familiar objects and can ask the
> colour of such objects.

**Проверено двумя способами, и вторая проверка обязательна.** Прогон
`npm run pdf -- materials/cefr-companion-2020.pdf --find "Can name the colour of
clothes"` даёт с. 79 — и приклеивает к строке слово `Pre-A1`, потому что первый
разбор склеивает конец графы с заголовком следующей. Поэтому я открыл второй
разбор (`materials/.md/cefr-companion-2020.md`) и прочёл блок целиком: строка
3704 открывает графу **A1** («A1 Can ask and answer questions about themselves
and other people, where they live, people they know, things they have»), наша
строка стоит под номером **3708**, а `Pre-A1` начинается только на строке 3713.
**Графа A1, а не Pre-A1.**

### 2.2. Вторая половина той же графы — «things they have»

Строка 3704 той же графы A1:

> Can ask and answer questions about themselves and other people, where they
> live, people they know, **things they have**.

Эта строка уже поделена между модулями: «where they live» — модуль 3
(`plan-modul-3.md`, строка 161), «people they know» — модуль 6
(`plan-modul-6.md`, строка 135), «things they have» — **модуль 7**
(`plan-modul-7.md`, строка 210).

**Я на неё не претендую и не спорю**, но сказать надо прямо: «things they have»
— это буквально про то, ЧТО У ЧЕЛОВЕКА ЕСТЬ, то есть про `have got`, а модуль 7
взял её под «назвать вещь» (`It is a book.`). Модуль 11 стоит на своей половине
дескриптора о цвете одежды и без этой строки обходится. **Сводящему** — раздел
11, строка 1.

### 2.3. Дескрипторы Cambridge — Young Learners, с. 9, Pre A1 Starters

*Can Do summary*, столбец **LISTENING AND SPEAKING**:

> CAN understand some very simple spoken descriptions of everyday objects – such
> as how many, **colour**, size or location.
> CAN **name** some familiar people or things – such as family, animals, and
> school or household objects.
> CAN respond to very simple questions with single words or a **'yes/no'
> response**.

Третья строка — прямая опора урока 3: короткий ответ `Yes, I have.` и
`No, I haven't.` и есть «a 'yes/no' response».

**Кусок «such as how many, colour, size or location» уже поделён** (`how many` —
модуль 8, `colour` и `size` — модуль 9, `location` — модуль 12), и я его себе не
беру: моя опора — строка CEFR о цвете ОДЕЖДЫ, а не эта.

**И четвёртая опора, ради которой стоит открыть с. 21.** Образец задания Pre A1
Starters Reading and Writing Part 3 — сличение подписи с картинкой:

> 1 **The man has got black hair and glasses.**

Cambridge на самой младшей ступени печатает `has got` вместе с вещью и цветом —
ровно строй модуля 11. Страницу проверил прогоном
`npm run pdf -- materials/cambridge-young-learners-pre-a1-a2.pdf --find "The man
has got black hair"`: одно совпадение, с. 21.

### 2.4. Чего дескриптор НЕ даёт: «что надето»

Строка программы обещает «сказать, что надето». По-английски это говорят двумя
способами, и обоих у ученика к модулю 11 нет:

- `She is wearing a red hat.` — Present Continuous. Пункты
  `1741163713121x244049823776565150` и `1741163713121x758287721695059000`
  (оба A1) розданы модулю 17 «Прямо сейчас»; сам образец Cambridge с формой
  `wearing` («What's the mother wearing? a purple shirt») стоит на **с. 43**, то
  есть в разделе **A1 Movers**, а не Pre A1 Starters. Проверено прогоном
  `--find "mother wearing"`;
- `I wear jeans.` — Present Simple, модуль 13.

**Что модуль вправду даёт вместо этого:** `I have got a red jacket.` и
`My shoes are white.` Оба строя ступень держит, оба стоят у Cambridge на Pre A1
Starters, и оба отвечают дескриптору CEFR — назвать цвет одежды и спросить о нём.

**Отсюда итог модуля**, и он не шире того, чему модуль учит:

> называть свою одежду и её цвет оборотом `have got` и спрашивать о цвете
> нескольких вещей

**Слово `wear` передаю модулю 17** — разделы 6.5 и 12.

### 2.5. Опоры уроков умений

| Урок | Опора | Дословно |
|---|---|---|
| чтение | **CEFR с. 57**, Reading correspondence, **A1** | «Can understand short, simple messages sent via social media or e-mail (e.g. proposing what to do, when and where to meet).» |
| слушание | **A2 Key, с. 29**, Listening, THE TASK | «The candidates should only write ONE word, or a number, or a date, or a time for their answer.» |
| говорение | **A2 Key, с. 42**, Speaking Part 1 | «The interlocutor asks **two short-answer questions** to each candidate … followed by **one longer** „Tell me something about …" question» |
| образец вопросов говорения | **Young Learners, с. 28**, Pre A1 Starters Speaking | «What's this? (crocodile) **What colour is it? (green)** What's your favourite animal? (cat)» |

**Опору урока чтения называю честно.** Прогон
`npm run pdf -- materials/cefr-companion-2020.pdf --find "sent via social media"`
даёт с. 57; графу A1 читал по второму разбору целиком, чтобы не приклеить
соседнюю. **Это единственная опора модуля, которую я взял под конец работы**, и
сборщику стоит перепроверить её первой (раздел 12).

### 2.6. Письма в модуле НЕТ, и это решение с опорой

Программа даёт письмо «только там, где источник называет жанр». Прошёл по всем
жанрам A1:

- **CEFR с. 67**, Creative writing, A1 — «Can use simple words/signs and phrases
  to describe certain everyday objects (e.g. the colour of a car, whether it is
  big or small)». **Этот дескриптор занят модулем 9** (`plan-modul-9.md`, раздел
  2.2 и урок 10 «Пишем записку: какая твоя вещь»);
- **CEFR с. 83**, Correspondence, A1 — открытка и короткое сообщение: модули 28
  и 29;
- **CEFR с. 84**, Notes, messages and forms, A1 — бланк (модуль 5) и записка
  (модуль 29);
- **CEFR с. 68**, Reports and essays: графы A1 и Pre-A1 — «No descriptors
  available».

**Довод не в занятости дескриптора, а в двойнике.** Урок письма модуля 11
пришлось бы строить на том же дескрипторе и о том же: описать вещь и назвать её
цвет. Образец модуля 9 («This is my box. It is not big. It is green.») и образец
модуля 11 («This is my jacket. It is red.») совпали бы тремя строками из
четырёх — ровно то, что правило проекта зовёт «один урок, а не два».

**Значит уроков восемь:** пять правил и три умения.

---

## 3. Расхождения источников: что взято и по какому источнику

Правило владельца от 6 сентября 2026 распространено на весь класс: где
руководство Cambridge даёт оборот на Pre A1 Starters, а English Grammar Profile
кладёт его на A2, курс берёт по Cambridge. **Но графа называется каждый раз
заново, и перенос вывода с соседнего случая запрещён.** Ниже — своя проверка по
каждому из четырёх случаев.

### 3.1. `have got` — Cambridge говорит, English Grammar Profile молчит

**Cambridge.** Young Learners, **с. 29**, Grammar and Structures List. Что
страница относится к Pre A1 Starters, я проверил не по памяти соседа, а по
оглавлению самого файла (`materials/.text/cambridge-young-learners-pre-a1-a2.txt`,
строки 29–50): «Pre A1 Starters … **29 Grammar and structures list**»; у A1
Movers тот же список стоит на с. 52, у A2 Flyers — на с. 77. Раздел `Verbs`
открывается заголовком

> Verbs
> (Positive, negative, question, imperative and **short-answer** forms,
> including **contractions**)

и держит строки `Present simple`, `Present continuous`, `Can for ability`,
`Can for requests/permission`, **`Have (got) for possession`**. Примеры последней
строки:

> **Have you got a pen?**
> **She hasn't got a dog.**

То есть источник печатает на самой младшей ступени и вопрос, и отрицание, а
заголовок раздела прямо называет короткий ответ и сокращения. Ещё одна строка
того же списка, `Conjunctions`, даёт короткую запись:

> **I've got a pen and a pencil.**

**English Grammar Profile.** Разобрал все 1222 записи настоящим разбором CSV
(сторож сошёлся: 1222 из 1222, уровня A1 — 109) и прошёл по всему файлу поиском
`have got`, `has got`, `'ve got`, `possession`, `have`. Нашлось:
`MODALITY / have (got) to` — обязанность, A2 и выше; `VERBS / types`, FORM:
SEMI-MODAL AUXILIARY VERBS, 'HAVE (GOT) TO' — A2; вспомогательный `have` для
перфекта — A2. **Пункта об обладании нет ни на одной ступени.**

**Формулирую честно, как требует правило: графы о `have got` в значении
обладания я не нашёл. Искал по всему файлу перечисленными словами, а не по
подкатегории.** Это не то же самое, что «её там нет», — но единственный
источник, который оборот размечает, ставит его на Pre A1 Starters.

**Отсюда решение: оборот берём, и ни один его `id` в поля `egp` и `egpPovtor` не
попадает** — ставить туда нечего, и это не забывчивость. Строка расхождения —
раздел 0.4.

### 3.2. Слова только во множественном числе

**Cambridge.** Young Learners, **с. 79**, тематический список `Clothes`, столбец
**PRE A1 STARTERS**: `bag, baseball cap, boots, clothes, dress, glasses,
handbag, hat, jacket, jeans, shirt, shoe, shorts, skirt, sock, trousers,
T-shirt, wear`. Столбец **A1 MOVERS**: `coat, helmet, scarf, sweater, swimsuit`.
Столбец **A2 FLYERS**: `belt, bracelet, costume, crown, glove, necklace,
pyjamas, pocket, ring, spot, stripe, sunglasses, trainers, umbrella, uniform`.
Читал обоими разборами: второй (`materials/.md/`, строки 3490–3500) держит
колонки таблицей, первый рассыпает их в поток, и вывод сделан по обоим сразу.

**English Grammar Profile.** `1741163711822x913552415415866000`, `NOUNS /
plural`, FORM: **NOUNS ONLY USED IN THE PLURAL**, уровень **A2**: «Can use nouns
that only have a plural form, with plural agreement.» Примеры графы:

> The **clothes** are very cheap in this shop.
> The **jeans** were expensive…
> Please wear some **shorts** and a shirt.
> I left my **glasses** in your house last night.

**Расхождение прямое и того самого вида, о котором решение владельца.** Берём по
Cambridge.

**И что из этого следует для формулировок урока 4.** Правило пишется о СВОИХ
четырёх словах, а не о языке: «у этих четырёх слов есть только форма
множественного числа». Такое утверждение подпирается словами самой графы
источника — «nouns that only have a plural form», — и потому законно. **Правила
вида «у слов об одежде так всегда» в уроке быть не должно**: `hat`, `jacket` и
`dress` опровергнут его на том же экране.

### 3.3. `What colour are your shoes?`

**English Grammar Profile:** `1741163715031x898125206259626100`, `QUESTIONS /
wh-`, FORM: MAIN VERB 'BE', **A2** — «Can use 'wh-'words + main verb 'be' +
subject to form 'wh-' questions», примеры «How was your dinner yesterday?», «How
are you, my friend?», «Where are you, men?».

**Cambridge:** Young Learners, **с. 28**, Pre A1 Starters Speaking Part 3, HOW TO
APPROACH THE TASK: «What's this? (crocodile) **What colour is it? (green)**
What's your favourite animal? (cat)».

**Расхождение то же, что у модуля 9, и решается так же — по Cambridge.**
Собственная половина модуля 11 — форма `are` при нескольких вещах; её ученик
получил в модуле 9, урок 5 (`Are the apples red?`, `The apples are red.`), а
модуль 11 соединяет её с вопросительным словом и больше ничего к разбору модуля
9 не добавляет.

**Оговорка, чтобы решение не прочли шире, чем оно есть.** English Grammar Profile
расходится сам с собой: `How are you?` стоит примером и у графы A1 (`CLAUSES /
interrogatives`, FORM: AFFIRMATIVE INTERROGATIVE, WITH 'BE'), и у графы A2.
Значит спорно ровно `wh-`-слово перед формой `be`, а не сам вопрос.

### 3.4. Два слова, взятых по младшей помете

| Слово | Oxford 3000 | Young Learners, с. 79 | Взято по |
|---|---|---|---|
| `sock` | **A2** | столбец **Pre A1 Starters** | Cambridge |
| `umbrella` | **A1** | столбец **A2 Flyers** | Oxford |

**Довод один на оба, и он не мой:** 6 сентября владелец решил ровно такой
случай — `airport`, Oxford A1 против Cambridge A2 Flyers, ответ «бери»
(`otvety-vladeltsa-6-sentyabrya.md`, ответ 5). Беру по младшей помете и называю
обе. **Цена отказа названа в разделе 11, строка 3.**

### 3.5. Слова «всегда», «только», «нельзя»: где просятся и почему их не будет

Признак опасного места по CLAUDE.md. Прошёл по каждому месту, где абсолютное
слово просится само.

| Место | Что просится сказать | Почему нельзя |
|---|---|---|
| урок 1 | «после `I` **всегда** `have got`» | правило пишется о своём случае: «при `I` стоит `have got`». Запрещающей графы нет ни одной |
| урок 1 | «`have got` значит „есть", и по-другому не говорят» | `I have a bag` — правильный английский, и он же A1 у English Grammar Profile (`NOUNS / noun phrases - grammatical functions`, FORM: AS OBJECTS, пример «I have many friends there»). **Ни одно задание модуля не объявляет эту строку ошибкой** |
| урок 2 | «отрицание — **только** `haven't got`» | `have not got` и `do not have` — правильный английский. Урок показывает свою форму и не запрещает чужой |
| урок 4 | «у слов об одежде **не бывает** единственного числа» | опровергается в том же уроке: `hat`, `jacket`, `dress`. Правило говорит о ЧЕТЫРЁХ названных словах |
| урок 5 | «о нескольких вещах **всегда** `are`» | верно, но лишнее: правило говорит о своём случае, а `is` при одной вещи ученик знает с модуля 2 |

**Проверка запрета по собственному тексту модуля** (сперва `grep` по модулю,
потом источник) делается сборщиком после сборки, по всем пяти урокам правил и по
работе.

**И вторая проверка, приписанная 6 сентября: запрет ищут в КУРСЕ целиком.**
Опаснее прочих здесь `glasses`: модуль 8 показывает `glass — glasses` о стаканах,
и любая моя строка вида «`glasses` — это всегда очки» была бы опровергнута
соседним модулем. Разбор — раздел 6.4.

---

## 4. Пункты English Grammar Profile

Ключ — поле `id`, а не заголовок: `Guideword` повторяется по всему файлу 129
раз. Файл читан настоящим разбором CSV (1222 записи, A1 — 109). **Искал по всему
файлу, а не по подкатегории**: правило приписано после модуля 29 ступени
Elementary, где вывод из одной подкатегории дал несуществующее расхождение.
Свободу каждого пункта проверил прогоном `npm run programma -- english-beginner-2`
и сверкой со списком «нерозданные пункты ступени A1».

### 4.1. Вводит модуль 11 (`egp`) — один пункт

| `id` | Категория, заголовок | Can-do и примеры источника |
|---|---|---|
| `1741163711829x787492176446619900` | `NOUNS / noun phrases - grammatical functions`, **FORM: AS OBJECTS**, A1, LR N/A | «Can use nouns and noun phrases as objects of the clause.» — «I love **my house**.» · «When I have time, I visit **my family**.» |

**Почему он мой, а не модуля 13 «Что я делаю каждый день».**

1. **Пункт впервые нужен здесь.** До модуля 11 в курсе нет ни одной строки, где
   за глаголом стоит вещь: восемь написанных модулей строятся на `be`, а после
   `be` стоит не дополнение. `I have got a bag` — первое место курса, где имя
   вещи стоит ПОСЛЕ оборота.
2. **Правило вводится там, где оно впервые нужно**, а не там, где о нём удобно
   рассказать. Модуль 13 стоит двумя модулями позже.
3. Примеры графы при этом с лексическими глаголами (`love`, `visit`), и это
   **довод против меня**, который я называю сам: сводящий вправе решить иначе.
   Цена обратного решения — раздел 11, строка 4.

**Строка, которую должен увидеть методист модуля 13** (находка о чужом модуле
сама не доезжает):

> Пункт `1741163711829x787492176446619900` (`NOUNS / noun phrases - grammatical
> functions`, FORM: AS OBJECTS, A1) взят модулем 11 «Одежда»: `I have got a bag`
> — первое место курса, где имя вещи стоит после глагола. Ставьте его в
> `egpPovtor`, а не в `egp`: `I like tea` и `I love my house` — ваши строки, но
> пункт уже введён. Оттуда же свободен и по смыслу ваш
> `1741163715288x929829619225120500` (`VERBS / types`, FORM: MAIN VERBS, A1):
> модуль 11 от него отказался (раздел 4.3, пункт 4).

### 4.2. Возвращается (`egpPovtor`)

| `id` | Что это | Ввёл | Где работает у меня |
|---|---|---|---|
| `1741163715288x539616242661052000` | `VERBS / types`, LINKING VERB 'BE' | модуль 2, **код** | `My bag is black.`, `They are white.` |
| `1741163713868x463659211645272000` | `PRONOUNS`, (SUBJECT) STATEMENTS | модуль 2, **код** | таблица форм оборота; `They are white.` |
| `1741163708329x286701804737242940` | `CLAUSES / declarative`, AFFIRMATIVE DECLARATIVE | модуль 2, **код** | все утвердительные строки |
| `1741163709005x427091401714639400` | `DETERMINERS / possessives`, WITH NOUNS | модуль 2, **код** | `my shoes`, `your jacket`, `her dress` |
| `1741163708789x105964971324936210` | `DETERMINERS / articles`, WITH NOUNS | модуль 3, **код** | `a hat`, `an umbrella` |
| `1741163708336x928067678664856700` | `CLAUSES / interrogatives`, AFFIRMATIVE INTERROGATIVE WITH 'BE' | модуль 3, **код** | `Are your shoes white?` — урок 5 |
| `1741163711821x759916598484829700` | `NOUNS / plural`, PLURAL '-S' | модуль 4, **код** | `shoes`, `hats`, `two bags` |
| `1741163708775x971363187748411900` | `CONJUNCTIONS / coordinating`, WORD + WORD | модуль 6, **код** | `a hat and a bag` — случай 9 урока 1 |
| `1741163706530x753542801715210100` | `ADJECTIVES / position`, PREDICATIVE, WITH 'BE' | модуль 9, **только замысел** | `My jacket is red.` |
| `1741163706530x479020650602328450` | `ADJECTIVES / position`, ATTRIBUTIVE (WITH NOUNS) | модуль 9, **только замысел** | `a red hat` |
| `1741163711539x318006443369482940` | `NOUNS / noun phrases`, ADJECTIVE + PLURAL NOUN | модуль 9, **только замысел** | `white shoes` |

**Восемь строк подтверждены КОДОМ** (`grep` по полю `egp` в
`courses/english-beginner-2/*.ts` плюс порядок из `index.ts`), **три последние —
только ЗАМЫСЛОМ модуля 9**, который ещё не написан. **Сборщику: если к сборке
модуль 9 в код не лёг, эти три `id` в `egpPovtor` не ставить** — проверка не
ошибётся, но запись будет говорить о том, чего нет. Правильный порядок — собирать
модуль 11 после модуля 9.

### 4.3. Чего НЕ беру, хотя соблазн прямой

1. **`1741163709005x222434593719802460`, `DETERMINERS / possessives`, FORM:
   SINGULAR NOUN + 'S, A2** — притяжательный апостроф (`It is my sister's.`).
   Модуль 6 предложил его мне или модулю 12; **не беру**, разбор — раздел 11,
   строка 2. Пункт при этом A2, и в поля `egp`/`egpPovtor` он не встал бы ни при
   каком решении.
2. **`1741163711822x913552415415866000`, NOUNS ONLY USED IN THE PLURAL, A2** — на
   этой графе стоит весь урок 4, и поставить её никуда нельзя: `npm run
   programma` объявляет ошибкой пункт чужой ступени **в любом из двух полей**
   (`scripts/programma.mts`, ветка `if (p.stupen !== stupen)` обходит `egp` и
   `egpPovtor` одинаково — прочитал код). Её место — поле `sources` и строка
   расхождения в шапке. **На этом уже обжёгся модуль 8**, и я называю ловушку
   здесь, чтобы сборщик не «починил» её обратно.
3. **`1741163715031x898125206259626100`, `QUESTIONS / wh-`, MAIN VERB 'BE', A2**
   — по той же причине: только `sources` и шапка.
4. **`1741163715288x929829619225120500`, `VERBS / types`, FORM: MAIN VERBS, A1**
   — «Can use regular and irregular main verbs in simple verb phrases», с
   указателями на present simple и past simple. **Не беру:** пункт по смыслу
   модуля 13, и взять его ради одного оборота значит закрыть модулю 13 его
   собственный предмет. Пункт свободен; называю его здесь, чтобы следующий не
   искал.
5. **`1741163706326x912859360099482800` и четыре соседних пункта о `very`** —
   модуля 9, все пять.

---

## 5. Состав модуля: восемь уроков

### 5.1. Порядок и итоги

| № | Урок | Вид | Итог |
|---|---|---|---|
| 1 | **Что у меня есть: I have got a bag** | правила | выбирать `have got` или `has got` по слову перед оборотом |
| 2 | **Чего у меня нет: I haven't got a hat** | правила | говорить, что вещи нет: `haven't got` и `hasn't got` |
| 3 | **Есть ли у тебя шапка: Have you got a hat?** | правила | спрашивать, есть ли вещь, и отвечать коротко: `Yes, I have` и `No, I haven't` |
| 4 | **Брюки и очки: одна вещь, а слово во множественном** | правила | ставить `are` при словах `trousers`, `jeans`, `glasses` и `clothes` |
| 5 | **Какого цвета твои ботинки: What colour are your shoes?** | правила | спрашивать о цвете нескольких вещей и отвечать `They are white` |
| 6 | **Читаем письмо: как меня узнать** | чтение | находить в письме, какая вещь у человека и какого она цвета |
| 7 | **Слушаем, у кого что есть** | слушание | записывать со слуха, сколько у человека вещей и какая из них названа |
| 8 | **Говорим о своей одежде** | говорение | выбирать ответ, в котором форма оборота совпадает со словом перед ним |

**Итог урока 8 назван по проверяемой половине** — правило проекта: устную речь
машина не оценивает, и обещать «говорить» нельзя. Как это обеспечено — раздел 5.4.

**Проверка каждого итога подстановкой под общий хвост «Чему научишься в модуле»:**
все восемь — неопределённая форма, все восемь называют действие, которое ученик
производит, и ни один не шире своего урока. Итог урока 4 нарочно перечисляет
четыре слова поимённо: без перечня он обещал бы правило о языке, которого модуль
не даёт (раздел 3.2).

**Все восемь имён проверены на неповторимость** `grep` по трём курсам.
Ближайшие соседи, которых надо знать сборщику: замороженный курс держит «У меня
есть: I've got», «У меня нет: haven't got», «Есть ли у тебя: Have you got?»
(`english-starter/moi-veshchi.ts`) и «Слушаем, что где есть»
(`english-elementary`). Совпадений нет ни одного, но `check:content` меряет
неповторимость ВНУТРИ курса, а не между курсами, — значит буквальный перенос
имени со склада прошёл бы молча. Я его не делаю нарочно.

**Поле `vid` проставляется у всех восьми уроков явно** (`pravila`, `chtenie`,
`slushanie`, `govorenie`). Вывод по составу ошибётся дважды: у урока 8 будет блок
`audio` с образцами, и вывод отдаст его в `slushanie`; у урока 5 запись образца
стоит в задании. `check:content` сверяет вывод с заголовком и о расхождении
спросит — поле `vid` главнее вывода, и потому оно названо здесь, а не по ходу
сборки.

### 5.2. Порядок выбран нуждой, а не удобством рассказа

- **урок 1 первый**, потому что без утвердительной формы ни отрицания, ни вопроса
  не построить;
- **урок 2 перед уроком 3**, потому что короткий ответ `No, I haven't` состоит из
  отрицательной формы: вопрос, поставленный раньше отрицания, потребовал бы
  ответа, которого ученик не умеет;
- **урок 4 после трёх уроков об обороте**, потому что его строки — `My trousers
  are black` — стоят не на обороте, а на `be`, и ставить их между уроками об
  обороте значило бы разорвать ряд;
- **урок 5 последний из правил**, потому что вопрос о цвете нескольких вещей
  требует и множественного числа (`shoes`), и слов только во множественном
  (`your jeans`), то есть всего урока 4.

**Возвращение к пройденному** (`vozvrat`, программа требует его в каждом модуле
со второго) стоит в **уроке 1**, последним заданием, и ведёт к модулю 9, урок
«Красная машина: a red car» — к заданию о прилагательном между артиклем и
названием вещи. Связь прямая: случай 6 урока 1 берёт ровно этот строй и ставит
его после оборота. **Точный `id` задания называет сборщик по коду модуля 9**,
когда тот будет написан; до тех пор в замысле стоит адрес, а не `id`.

### 5.3. Уроки 2–5 одной строкой

Подробно расписан только урок 1 (отдельный файл). Ниже — состав остальных, чтобы
сводящий видел границы и не собирал двойников. **Русские имена случаев и тексты
правил пишет редактор.**

**Урок 2 «Чего у меня нет: I haven't got a hat».** Карточки: `T-shirt`, `skirt`,
`boot`. Восемь случаев: `haven't got` при `I`; `hasn't got` при `he` и `she`; имя
вместо местоимения (`Dana hasn't got a skirt.`); вещь после отрицания стоит с
артиклем (`I haven't got a T-shirt.`); две вещи через `and` (`I haven't got a hat
and a bag.`); пара строк об одном человеке — что есть и чего нет; по-русски «нет»
меняет и глагол, и падеж вещи, а по-английски вещь не меняется вовсе; сводный
случай, где обе формы стоят таблицей.
**Начало:** русская фраза — «у меня нет шапки».
**Таблица форм:** `I haven't got` · `you haven't got` · `he hasn't got` ·
`she hasn't got` · `we haven't got` · `they haven't got`.

**Урок 3 «Есть ли у тебя шапка: Have you got a hat?».** Карточки: `shirt`,
`sock`, `umbrella`. Восемь случаев: сам вопрос (`Have you got a hat?`); порядок
слов — оборот разрывается, и `have` встаёт впереди; `Has she got …?` при `he` и
`she`; короткий ответ `Yes, I have.`; короткий ответ `No, I haven't.`; ответ о
третьем лице (`Yes, she has.` и `No, she hasn't.`); вопрос о вещи с
прилагательным (`Have you got a red umbrella?`); почему в вопросе `have you`, а в
ответе `I have` — порядок обратный, и это единственное трудное место урока (тот
же приём, каким модуль 9 разбирает `is it` и `it is`).
**Начало:** сцена — Дана собирается в школу и спрашивает Алима о его вещах.

**Урок 4 «Брюки и очки: одна вещь, а слово во множественном».** Карточки:
`trousers`, `jeans`, `glasses`, `clothes`. Восемь случаев: по-русски «брюки»,
«джинсы» и «очки» тоже стоят во множественном, и слово рядом с ними тоже
множественное — русская параллель полная; `My trousers are black.`;
`My jeans are new.`; `glasses` — то же слово, которым модуль 8 называл стаканы, и
об очках оно стоит только во множественном (раздел 6.4); `clothes` — общее слово
об одежде, и оно тоже только во множественном; `They are black.` о таких словах;
эти слова с оборотом (`I have got new jeans.`) — окончание уже стоит, и добавлять
нечего; сводный случай, где рядом `a hat` и `trousers`, `is` и `are`.
**Начало:** русская фраза — «одни брюки».
**Осторожно редактору:** правило говорит о ЧЕТЫРЁХ названных словах, а не об
одежде вообще. Строки «у слов об одежде так всегда» быть не должно: `hat` и
`dress` опровергнут её на том же экране.

**Урок 5 «Какого цвета твои ботинки: What colour are your shoes?».** Карточек нет
ни одной: урок стоит на уже данных словах — как урок 4 модуля 9. Восемь случаев:
вопрос о нескольких вещах (`What colour are your shoes?`); ответ
`They are white.`; ответ одним словом (`White.`) — образец Cambridge, с. 28;
вопрос о вещах, названных словом только во множественном (`What colour are your
jeans?`); вопрос об одной вещи рядом с вопросом о нескольких — `is` и `are` в
паре (возврат к модулю 9, урок 3); ответ о вещах другого человека (`Her shoes are
black.`); вопрос `Are your socks white?` и короткий ответ `Yes, they are.`;
сводный случай, где стоит вся строка модуля целиком (`I have got new shoes. They
are white.`).
**Начало:** сцена — Ким по телефону просит принести его вещи и не помнит, какие
они.

### 5.4. Уроки умений

**Урок 6 «Читаем письмо: как меня узнать», `genre: "email"`.** Ким прилетает и
пишет Дане, как его узнать в аэропорту. Опора — CEFR с. 57, A1 (раздел 2.5).
Порог жанра — пятнадцать слов, в письме их больше сорока.

Закрытый перечень английских строк текста:

    Hello, Dana!
    I am at the airport.
    I have got a big green bag and a black umbrella.
    My jacket is red.
    I haven't got a hat.
    My sister is here.
    She has got a blue dress and white shoes.
    Kim

**Все слова уже даны:** `at` — модуль 6, `airport` и `here` — модуль 3, `big`,
`green`, `black`, `red`, `blue`, `white` — модуль 9, `sister` — модуль 6,
`Hello` — модуль 1; остальное — карточки этого модуля.

Задания урока: найти цвет сумки; выбрать, что есть у сестры; назвать, чего у Кима
нет; отметить в тексте все названия вещей; собрать ответ на вопрос о зонте.

**Чем это не двойник соседей.** Модуль 7 читает объявление о забытых вещах — там
ученик ищет НАЗВАНИЕ вещи в списке; модуль 8 читает список у входа — там ученик
считает; модуль 9 читает две записки — там ученик сличает описание с вещью. Здесь
ученик читает связный текст об одном человеке и отвечает, ЧТО У НЕГО ЕСТЬ и какого
оно цвета. **Жанр `email` в части 2 не занят никем** — проверено по замыслам
модулей 7–12 и по коду написанных.

**Перевода у связного текста нет** — решение владельца от 6 сентября (ответ 13):
в длинных текстах незнакомое слово получит всплывающую подсказку. **Звука у
текста чтения тоже нет** — ответ 14 того же дня.

**Урок 7 «Слушаем, у кого что есть».** Формат ответа взят у Cambridge дословно:
одно слово или число (A2 Key, с. 29). Разговор на два голоса; ответы — `2` и
`socks`.

Закрытый перечень строк записи:

    — Have you got a red jacket?
    — No, I haven't. My jacket is green.
    — And your bag?
    — I have got two bags.
    — Have you got socks in the bag?
    — Yes, I have.

**Ответ ни разу не напечатан рядом** — ни латиницей, ни русскими буквами: русские
подписи записи и разборы вокруг неё не называют ни числа, ни вещи, и подпись
СЛЕДУЮЩЕЙ записи не называет ответа предыдущей. Правило приписано 6 сентября
редактором модуля 5, и оно шире подписей: **сборщику проверить, что слова `socks`
и числа `two` нет в видимом тексте на тех же экранах** — ни в условии, ни в
подсказке, ни в разборе соседнего задания.

**Урок 8 «Говорим о своей одежде».** Образец — A2 Key, с. 42: два коротких ответа
и один длинный. Короткие — `Have you got a bag?` и `What colour are your shoes?`;
длинный — «расскажи о своей одежде» одним рядом строк (`I have got a black
jacket. My shoes are white.`).

**Перед устными заданиями стоит одно задание `choice` по записи, и оно и несёт
итог урока.** Ученик слышит `My sister has got a new bag.` и выбирает из трёх
записей ту, которая прозвучала: `My sister has got a new bag.` ·
`My sister have got a new bag.` · `My sisters have got a new bag.` Машина
оценивает не произношение, а этот выбор; записи `speak` итога на себе не несут.
Это тот же ход, каким модуль 8 закрыл свой урок говорения.

**Осторожно, и это правило о неверных строках:** в задании звучит ТОЛЬКО верная
запись. Строка `My sister have got a new bag.` стоит на экране как вариант выбора
и кнопки звука не получает — неверная фраза, услышанная, запоминается наравне с
верной.

### 5.5. Вид начала модуля

**Модуль 11 — модуль-правило со сценой.** Программа требует не меньше трёх видов
начала в каждой части; часть 2 к моему приходу держит четыре: модуль 7 —
модуль-вещи, модуль 8 — модуль-правило со сценой, модуль 9 — модуль-текст, модуль
10 — модуль-разговор (все четыре объявлены в их замыслах, я открыл каждый).
Требование исполнено с запасом, и мой вид повторяет вид модуля 8 — говорю об этом
прямо, а не молчу.

**Почему не модуль-вещи, хотя одежда просит именно этого.** Главное новое в
модуле — не слова, а оборот; урок, открывшийся словарём одежды, объявил бы
предметом слова. Сцена показывает оборот работающим пять раз подряд — приём
Мёрфи, на котором стоит новый формат.

| Урок | Вид начала |
|---|---|
| 1 | сцена: Дана показывает Алиму новую сумку |
| 2 | русская фраза: «у меня нет шапки» — по-русски меняется и глагол, и падеж вещи |
| 3 | сцена: Дана собирается в школу и спрашивает Алима о его вещах |
| 4 | русская фраза: «одни брюки» — по-русски слово тоже только во множественном |
| 5 | сцена: Ким по телефону просит принести его вещи |
| 6 | текст: письмо Кима из аэропорта |
| 7 | запись: разговор двух голосов |
| 8 | сцена: собеседник спрашивает о твоей одежде |

**Ни один урок не начинается обобщением** вида «Сказать, что у тебя есть, можно
двумя способами». Правило владельца от 3 сентября.

**И проверка, которую это правило требует сделать вслух:** у каждого урока правил
названа русская фраза, которой ученик сказал бы то же самое. Там, где её нет
(уроки 1, 3, 5), стоит сцена, и «русской параллели здесь нет» я нигде не написал
как факт: у урока 1 параллель есть, но она про ПОРЯДОК слов («у меня есть сумка»
начинается с вещи, а `I have got a bag` — с человека), и разбирается она вторым
экраном, а не первым.

### 5.6. Чего в модуле нет и почему

| Что просится | Почему не здесь | Куда |
|---|---|---|
| `She is wearing a red hat.` | Present Continuous — модуль 17; образец Cambridge с формой `wearing` стоит на с. 43, ступень A1 Movers | модуль 17 |
| `I wear jeans.` | Present Simple — модуль 13 | модуль 13 |
| `He's got`, `She's got` | **правильный английский, и объявлять его ошибкой нельзя ни в одном задании.** В модуле он не ПОКАЗЫВАЕТСЯ: единственная короткая запись, напечатанная источником, — `I've got a pen and a pencil` (с. 29), а `she's` в курсе уже значит `she is` (модуль 2), и две вещи под одной записью на этой ступени развести нечем | вопрос сводящему, раздел 11, строка 5 |
| `Whose is it? — It is my sister's.` | притяжательный апостроф, A2 у English Grammar Profile | раздел 11, строка 2 |
| `I have a bag` без `got` | правильный английский; модуль учит своей форме и чужую не запрещает | не показывается |
| `Do you have a hat?` | вопрос с `do` — модули 14 и 15 | модуль 15 |
| цвет карточкой | все девять — модуля 9 | модуль 9 |
| `dark blue`, `light green` | два прилагательных подряд — A2 у обоих источников (`plan-modul-9.md`, раздел 3.2) | нет на ступени |
| `size`, `What size is it?` | `size` у Oxford — A2 (`plan-modul-9.md`, раздел 6.4) | нет на ступени |
| `shorts` | разбор в разделе 6.5 | не берётся |
| `watch` | разбор в разделе 6.5 | вопрос сводящему |
| `sweater`, `jumper` | разбор в разделе 6.5 | не берётся |
| одежда по погоде (`It is cold, take a coat`) | пустое `it` о погоде — A2, снято со ступени решением методиста программы | нет на ступени |
| цена одежды | `some`, деньги и покупка — модуль 21 | модуль 21 |

---

## 6. Словарь модуля

### 6.1. Тема `Clothes and Accessories` — счёт

Тема объявляется одна: `Clothes and Accessories`, дословное имя из Приложения 2
словника A2 Key (с. 25–32). Читал **второй разбор**
(`materials/.md/cambridge-vocab-a2-key.md`, строки 1767–1795): первый склеивает
четыре колонки в одну строку и даёт по темам 296 записей вместо 1159.

**В теме 54 записи, из них с пометой Oxford 3000 A1 — пятнадцать.** Счёт сошёлся
с `npm run programma`, который печатает `Clothes and Accessories (15)`. Все
пятнадцать выписаны своим разбором тем же правилом, каким считает проверка
(точное совпадение, без усечения основ):

`bag`, `boot`, `clothes`, `coat`, `dress`, `hat`, `jacket`, `jeans`, `shirt`,
`skirt`, `sweater`, `trousers`, `umbrella`, `watch`, `wear`.

**До модуля 11 не закрыто ни одно из пятнадцати** — тема не объявлена ни одним
модулем курса, и ни одной карточки одежды в коде нет.

**Модуль 11 закрывает одиннадцать:** `bag`, `boot`, `clothes`, `dress`, `hat`,
`jacket`, `jeans`, `shirt`, `skirt`, `trousers`, `umbrella`. **Это 11 из 15, то
есть 73 процента**, при норме программы в две трети (10) к концу курса.

**Четыре незакрытых слова — решения, а не забывчивость.** Их надо положить в
`courses/resheno.ts` с причиной, иначе `npm run programma` будет спрашивать о них
на каждом прогоне. Записать туда я не вправе — методист в `courses/` не пишет.

| Слово | Почему не берётся | Закроется ли когда-нибудь |
|---|---|---|
| `wear` | глагол, а обоих времён, в которых он живёт, у ученика нет: Present Simple — модуль 13, Present Continuous — модуль 17. Карточка без единой рабочей строки — это мёртвая карточка, о которых `check:content` сообщает сведениями | **да, модулем 17** — раздел 12, передача |
| `coat` | Young Learners, с. 79, ставит его в столбец **A1 Movers**, то есть ступенью выше `jacket`; вещь при этом та же, и второго значка для неё в Twemoji нет (единственный, `1f9e5`, отдан `jacket`). Двух слов на одну вещь модуль не тянет | нет; слово свободно для ступени Elementary |
| `sweater` | британская норма курса даёт `jumper`, а не `sweater` (CLAUDE.md, раздел о британской норме), и Young Learners тоже ставит `sweater` на **A1 Movers**. Само `jumper` при этом у Oxford не размечено вовсе и своего значка в Twemoji не имеет | нет |
| `watch` | перевод столкнулся бы с карточкой `clock` модуля 7, переведённой «часы». Модуль 7 разбирал ровно этот случай и решил: «двух слов на одно русское модуль не тянет», а скобка «(наручные)» — это сужение перевода, и оно **требует опоры**, которой у меня нет | **вопрос сводящему**, раздел 11, строка 6 |

### 6.2. Карточки модуля — шестнадцать

Все проверены `npm run slovo` поимённо: ступень по Oxford 3000, наличие в
словнике A2 Key, чтение по `ipa-en-uk`, не дано ли уже и в каком модуле.
**Ни одно слово в новом курсе раньше не вводилось** — прогон
`grep -o 'term: "[^"]*"' courses/english-beginner-2/*.ts` по каждому.

| Слово | Перевод | Ступень | Урок | Где работает |
|---|---|---|---|---|
| `have got` | есть (у меня, у тебя) | `have` — Oxford **A1**; оборот — Cambridge, с. 29, Pre A1 Starters | 1 | сцена и все девять случаев урока 1; уроки 2, 3, 5 |
| `bag` | сумка | Oxford **A1**, словник — да, YL с. 79 **Pre A1 Starters** | 1 | сцена, случаи 1, 7, 9; задания 1, 4 |
| `hat` | шапка, шляпа | то же | 1 | случаи 1, 6, 9; задания 3, 6 |
| `jacket` | куртка | то же | 1 | сцена, случаи 3, 5; задание 2 |
| `shoe` | ботинок | то же (в теме стоит запись `shoes`) | 1 | сцена, случай 7; урок 5 целиком |
| `dress` | платье | то же | 1 | случаи 3, 4; задание 2 |
| `T-shirt` | футболка | Oxford слова не знает; словник — да; YL **Pre A1 Starters** | 2 | случаи урока 2; работа, вопрос 4 |
| `skirt` | юбка | Oxford **A1**, словник — да, YL **Pre A1 Starters** | 2 | случаи урока 2 |
| `boot` | сапог | то же | 2 | случаи урока 2; урок 5 |
| `shirt` | рубашка | то же | 3 | случаи урока 3 |
| `sock` | носок | Oxford **A2**, словник — да, YL **Pre A1 Starters** — раздел 3.4 | 3 | случаи урока 3; урок 5; запись урока 7 |
| `umbrella` | зонт | Oxford **A1**, словник — да, YL **A2 Flyers** — раздел 3.4 | 3 | случаи урока 3; письмо урока 6 |
| `trousers` | брюки | Oxford **A1**, словник — да, YL **Pre A1 Starters** | 4 | случаи урока 4; урок 5 |
| `jeans` | джинсы | то же | 4 | случаи урока 4; урок 5 |
| `glasses` | очки | Oxford слова не знает; словник — да; YL **Pre A1 Starters** | 4 | случаи урока 4; раздел 6.4 |
| `clothes` | одежда | Oxford **A1**, словник — да, YL **Pre A1 Starters** | 4 | случаи урока 4; урок 8 |

**Переводы предлагает методист, а видимый текст пишет редактор** — если
редактору какой-то из них покажется узким или широким, правит он, и правит по
правилу проекта: расширение законно, сужение требует опоры.

**Три записи стоят особняком, и все три названы.** `T-shirt` и `glasses` Oxford
3000 не знает вовсе — по правилу программы такие слова «в знаменатель не входят
и не запрещаются; если карточка есть, засчитывается в числитель». Оба стоят у
Cambridge на Pre A1 Starters. `sock` и `umbrella` — раздел 3.4.

**Норма ступени.** С 29 августа она считается **по теме**, а не по числу
карточек: тема закрывается на 11 из 15. Добирать модуль словами о доме или о еде
ради тридцати было бы ровно тем добором, который запрещает уточнение владельца от
21 августа.

**Ни одна карточка не приходит позже урока, где работает её форма.** Прогнал по
всем шестнадцати: `trousers`, `jeans`, `glasses`, `clothes` нужны уроку 4 и
приходят в нём; `shirt`, `sock`, `umbrella` — уроку 3 и приходят в нём; урок 5
новых слов не вводит и стоит на данных.

**Мёртвых карточек нет:** у каждой заполнен столбец «где работает», и ни одно
слово не встречается в модуле только в самом себе. **Сборщику сверить этот
столбец с таблицами заданий строка в строку** — на этом обжёгся модуль 8, где
`carrot` был записан в задание, которого нет.

### 6.3. Чтение

**Пять записей берутся из замороженного курса дословно** — они уже приведены к
условностям владельца: `bag /bæg/`, `sock /sɒk/`, `glasses /ˈglɑːsɪz/`,
`umbrella /ʌmˈbrelə/`, `t-shirt /ˈtiː ʃɜːt/` (`english-starter/chto-eto-takoe.ts`,
`english-elementary/mozhet-byt.ts`, `kto-to-i-chto-to.ts`, `kotoryy.ts`).

**Остальные даю с применёнными условностями курса, и сборщику сверить, а не
принимать на веру:** `have got /hæv gɒt/`, `hat /hæt/`, `shirt /ʃɜːt/`,
`jacket /ˈdʒækɪt/`, `dress /dres/`, `skirt /skɜːt/`, `jeans /dʒiːnz/`,
`shoe /ʃuː/`, `boot /buːt/`, `clothes /kləʊðz/`, `trousers /ˈtraʊzəz/`.
Формы множественного: `shoes /ʃuːz/`, `boots /buːts/`, `socks /sɒks/`,
`hats /hæts/`, `bags /bægz/`.

**Три записи спорные, и я называю каждую вместо того, чтобы выбрать молча.**

1. **`trousers`.** `ipa-en-uk.txt` даёт `/tɹˈaʊsəz/` — с глухим `s`;
   `wikipron-en-uk.tsv` даёт `t ɹ aʊ z ə z` — со звонким. Источники расходятся, и
   первый собран машиной. Беру `/ˈtraʊzəz/` по второму, но это **спорное слово, и
   решает владелец как преподаватель** — раздел 11, строка 7.
2. **`clothes`.** `ipa-en-uk.txt` даёт `/klˈəʊðz/`; `wikipron-en-uk.tsv` держит
   ДВЕ записи — `k l ə ʊ z` и `k l ə ʊ ð z`. Беру `/kləʊðz/`: она есть в обоих.
3. **`have got` и `T-shirt` — записи многословные**, и знака ударения в них нет
   вовсе: решение владельца от 6 сентября (ответ 4) — «в фразах можно не уточнять
   ударение». **Запись `T-shirt` при этом взята из замороженного курса СО знаком**
   (`/ˈtiː ʃɜːt/`), и её надо привести к решению: `/tiː ʃɜːt/`. Правка одной
   строки, но она меняет имя звукового файла — значит слово надо переозвучить.

### 6.4. `glasses`: то же слово, что стаканы модуля 8

**Что стоит в коде.** `odin-i-mnogo.ts`, урок 2: карточка `glass` — «стакан», и
форма `glasses` работает там примером написания `-es` после `s`.

**Решение: карточку `glass` я не трогаю, а `glasses` ввожу своей карточкой с
переводом «очки».** Довод: это не два значения одного слова, которые надо
разводить осторожно, а два разных слова, совпавших формой, и ученик наткнётся на
совпадение сам — в первой же строке урока 4.

1. **карточка `glass` остаётся у модуля 8**, с переводом «стакан»;
2. **модуль 11 называет столкновение СЛУЧАЕМ** — урок 4, случай 4. Ровно как
   модуль 9 поступил с `orange` (плод и цвет) и модуль 7 с `desk`: «граница
   называется не в карточке, а там, где ученик на неё наткнётся»;
3. **строки случая:** `My glasses are new.` и ссылка на модуль «Один и много»,
   урок «Когда написание меняется: boxes и cities», где та же форма называла
   стаканы. Внутри одной ступени ссылки на наши модули приветствуются;
4. **чего в случае быть не должно:** правила «`glasses` — это всегда очки».
   Модуль 8 опроверг бы его на своей же странице.

**Осторожно с картинками:** в заданиях урока 4, где `glasses` — очки, значок
стакана ставить нельзя. Предлагаю `1f453`.

### 6.5. Слово `shorts`, которого в модуле нет

Young Learners, с. 79, ставит его на Pre A1 Starters; Oxford слова не знает; в
теме словника оно есть. **Не беру** — и довод не в ступени, а в курсе: модуль 9
вводит карточку `short` — «короткий», и `shorts` прочтётся как её форма
множественного числа. Это ровно та беда, которую я разбираю у `glasses`, только
здесь она ничем не окупается: слов только во множественном у урока 4 и так
четыре. **Если сводящий всё же захочет взять слово**, столкновение обязано быть
названо случаем, как у `glasses`, а не замолчано.

`watch`, `sweater` и `coat` — раздел 6.1, таблица незакрытых слов.

### 6.6. Значки

Предлагаю: `bag` — 1f45c, `hat` — 1f452, `jacket` — 1f9e5, `shoe` — 1f45e,
`dress` — 1f457, `T-shirt` — 1f455, `boot` — 1f462, `shirt` — 1f454,
`sock` — 1f9e6, `umbrella` — 2602, `jeans` — 1f456, `glasses` — 1f453,
`clothes` — 1f45a. Значок модуля — 1f455.

**Три места, где значка у меня нет, и я говорю это прямо, а не подставляю
похожий.**

1. **`skirt`** — юбки в Twemoji нет вовсе; ближайшее (`1f457`) занято платьем, а
   поставить платье юбке значит соврать картинкой.
2. **`trousers`** — единственный подходящий знак `1f456` это синие джинсы, и он
   отдан `jeans`. Двум словам один значок ставить нельзя: они стоят рядом в одном
   уроке и в одной таблице.
3. **`have got`** — служебная запись, картинки не имеет по существу. Законное
   исключение из нормы владельца: «без картинки только служебные слова».

**Что с этим делать — решает автор, а не я.** Владелец 6 сентября велел чинить
поле значка так, чтобы оно брало файлы вне `public/twemoji` (ответ 15). Как
только это сделано, `skirt` и `trousers` получают картинки из Openclipart (CC0).
**До тех пор две карточки стоят без значка**, и это лучше неверной картинки — тот
же ход, каким модуль 8 решил `bottle`.

**Сборщику проверить два условия глазами, а не чтением:** значок есть в наличии
(`npm run znachki` тянет файлы из сети и об ошибке не молчит) и значки не похожи
друг на друга на маленьком размере. **Опасных пар три:** `T-shirt` (1f455),
`shirt` (1f454) и `clothes` (1f45a) — все три о верхней одежде; `shoe` (1f45e) и
`boot` (1f462); `umbrella` (2602) — знак без цвета, и если он не заберётся,
запасной `1f302`.

**Значок модуля повторяет значок карточки `T-shirt`, и это нарочно:** карта курса
и словарь урока стоят на разных экранах, а другого узнаваемого знака одежды в
Twemoji нет. Если сборщик сочтёт повтор помехой, менять надо значок МОДУЛЯ, а не
карточки.

---

## 7. Работа модуля — двенадцать вопросов

**Раздел написан ДО заданий уроков** — правило проекта: работу пишут до заданий
или отдельно от них, с другого угла. Иначе выходит порода «375 близнецов»:
работа берёт урочное задание и меняет одно слово.

**Утверждение, которое верно только на дату, и я ставлю дату.** *6 сентября
2026:* ни одного из двенадцати углов ниже в заданиях модуля нет. Подробно
расписан пока только урок 1 (восемь заданий, отдельный файл), уроки 2–5 стоят
кратким составом. **Перед сборкой это утверждение проверяется заново, разбором
обоих документов, а не памятью** — на этом обжёгся модуль 6, где шесть вопросов
из двенадцати стали близнецами уроков, написанных ПОЗЖЕ работы.

Требование программы: 12 вопросов, больше половины — произвести ответ, по вопросу
на каждый итог, обязательно один по тексту чтения и один со звуком.

| № | Итог | Вид | Что делает ученик | Угол, которого нет в уроках |
|---|---|---|---|---|
| 1 | урок 1 | `short` | «Алим сказал о себе: `I have got a bag.` Скажи то же самое про Дану.» | перевод готовой строки с одного лица на другое. В уроке 1 ученик вписывает форму, собирает строку и правит чужую запись — переделки строки нет ни в одном задании |
| 2 | урок 1 | `choice` | «После какого слова стоит `has got`?» — `my brother` · `my parents` · `you` | ученик не пишет форму, а решает, какое слово её требует; в уроке форма всегда дана вместе со своим словом |
| 3 | урок 2 | `short` | «Дана написала: `She haven't got a jacket.` Запиши строку без ошибки.» | в уроке 2 ошибка правится при `I`, здесь при `she` |
| 4 | урок 2 | `gap` | `I ___ got a T-shirt.` Условие: футболки у тебя нет | вписывается ОТРИЦАТЕЛЬНАЯ форма; в уроке 1 в такую же рамку вписывалась утвердительная |
| 5 | урок 3 | `order` | собрать вопрос из `hat` · `you` · `Have` · `a` · `got` | сборки вопроса в уроке 3 нет: там вопрос дан, а ученик отвечает |
| 6 | урок 3 | `choice` | «Тебя спросили: `Have you got an umbrella?` **Зонта у тебя нет.** Выбери короткий ответ.» | короткий ответ выбирается по условию, а не по образцу строки |
| 7 | урок 4 | `short` | «Ким написал: `My glasses is here.` Запиши строку без ошибки.» | в уроке 4 форма `are` вписывается, здесь — узнаётся среди готового |
| 8 | урок 5 | `short` | «Ты хочешь узнать цвет джинсов собеседника. Запиши вопрос.» | в уроке 5 вопрос дан, а ученик отвечает; здесь вопрос надо ПОСТРОИТЬ |
| 9 | урок 5 | `choice` | «Тебя спросили: `What colour are your socks?` **Носки у тебя белые.** Выбери ответ.» | выбор ответа при заданном условии |
| 10 | урок 6 | `gap` | **по тексту чтения того же модуля**: `The umbrella is ___ .` | требование программы: один вопрос по тексту. Задания урока 6 спрашивают о сумке, о сестре и о том, чего у Кима нет, — о зонте не спрашивает ни одно |
| 11 | урок 7 | `short` | **со звуком**: слышит `I have got three shirts.` и записывает, сколько рубашек | требование программы: один вопрос со звуком. В уроке 7 запись — разговор ДВУХ голосов и спрашивается второе число; здесь один голос и другое число |
| 12 | урок 8 | `short` | «Скажи, что у тебя есть шапка и сумка.» | в уроке 8 ученик ГОВОРИТ, здесь пишет; и это единственное место работы, где спрашивается итог урока 8 |

**Покрытие итогов:** урок 1 — вопросы 1 и 2; урок 2 — 3 и 4; урок 3 — 5 и 6;
урок 4 — 7; урок 5 — 8 и 9; урок 6 — 10; урок 7 — 11; урок 8 — 12. **Восемь
итогов, двенадцать вопросов, ни одного итога без вопроса.**

**Доля «произвести ответ»:** производят девять из двенадцати (1, 3, 4, 5, 7, 8,
10, 11, 12), выбирают три (2, 6, 9). Требование программы «больше половины»
исполнено.

### 7.1. Ответы, принимаемые ответы и отвлекающие

Текст условия, подсказки и разбора пишет редактор; здесь стоит только то, что
решает методист. **Поле `accept` перечитывается вместе с условием и ответом —
всегда**: его не видит ни один скрипт.

| № | Верный ответ | Принимаемые | Отвлекающие | Проверено |
|---|---|---|---|---|
| 1 | `Dana has got a bag.` | `She has got a bag.`, обе строки без точки | — | **второй верный ответ есть, и он законен:** условие говорит «про Дану», а `She has got a bag.` — правильная строка по нашему же уроку 1, случай 3. Наказывать за неё нельзя |
| 2 | `my brother` | — | `my parents`, `you` | верный ровно один: при `my parents` и при `you` стоит `have got`. Оба отвлекающих настоящие — ученик ведёт форму за словом «мой», не глядя на число |
| 3 | `She hasn't got a jacket.` | `She has not got a jacket.` | — | **полная форма тоже верна**, и объявлять её ошибкой нельзя. Ошибка в условии одна и настоящая: ученик берёт `haven't` ко всем словам подряд |
| 4 | `haven't` | `have not` | — | по той же причине |
| 5 | `Have you got a hat?` | без вопросительного знака | — | верный порядок ровно один: `Got you have a hat?` и прочие перестановки по-английски не говорят |
| 6 | `No, I haven't.` | — | `No, I am not.`, `No, it is not.` | оба отвлекающих — настоящие ошибки: ученик отвечает формой `be`, потому что три модуля подряд отвечал ею. Условие называет ответ прямо и отдельным предложением |
| 7 | `My glasses are here.` | без точки | — | ошибка настоящая: слово выглядит как одна вещь, и ученик ставит `is` |
| 8 | `What colour are your jeans?` | без вопросительного знака | — | других верных строк нет: `What colour is your jeans?` неверно, `What are the colours of your jeans?` — не наша ступень |
| 9 | `They are white.` | — | `It is white.`, `They are whites.` | обе ошибки настоящие: первая — единственное число вместо множественного, вторая — окончание у слова о цвете, ровно то, против чего учит модуль 9, урок 5 |
| 10 | `black` | — | — | ответ одним словом, как у Cambridge |
| 11 | `3` | `three` | — | ответ числом или словом. **Числа `three` нет в видимом тексте на тех же экранах** — сборщику проверить |
| 12 | `I have got a hat and a bag.` | `I've got a hat and a bag.` | — | порядок задан условием («шапка и сумка»), других верных строк нет. Тот же ход, каким модуль 8 задал порядок в своём вопросе 4 |

**Два запрета на отвлекающие, действующие на всю работу и на все задания
уроков.**

1. **`I have a bag`, `Do you have a bag?`, `He's got a bag`, `have not got` —
   правильный английский.** Ни одна из этих строк не стоит неверным вариантом
   нигде: задание, объявляющее ошибкой верную фразу, наказывает за правоту. Это
   породы, на которых курс уже обжигался дважды.
2. **`glasses` о стаканах — тоже правильный английский** (модуль 8). Значит
   строка вида `My glasses are in the box` отвлекающим быть не может: она верна
   и по языку, и по нашему же курсу.

**Проверки, которые сборщик обязан прогнать по работе, когда она будет
написана:**

1. `npm run bliznetsy` — против заданий всех восьми уроков модуля И против работ
   соседних модулей своей части (проверка научена этому 6 сентября);
2. **каждый отвлекающий — по двум проверкам:** есть ли строка в собственном
   модуле и говорят ли так по-английски вообще. Вторая проверка убила у модуля 8
   четыре отвлекающих подряд;
3. распределение верных ответов по местам — `check:content`;
4. `hint` в банке вопросов не ставится вовсе — подсказка до ученика там не
   доходит, и `check:content` объявляет её ошибкой.

---

## 8. Звук: что звучит и что нет

**Что звучит.** Каждая карточка по одному слову; каждая строка примера; каждая
ячейка таблицы форм по отдельности (`I have got` одна запись, `she has got`
другая — правило владельца от 19 августа о дроблении по единицам); запись урока 7
целиком, на два голоса; образцы к заданиям `speak` урока 8.

**Разговоры звучат на два голоса.** Сцены уроков 1, 3 и 5 и запись урока 7 —
разговор; реплика на строку, тире между репликами в примерах не ставится
(`repliki()` в `scripts/ozvuchka.mts` режет примеры по переводу строки, а записи
`audio` — по тире). **Голос человека в курсе один:** Дана женский, Алим и Ким
мужские. Поле `pervyyGolos` ставится по тому, кто говорит первым: в сцене урока 1
это Дана — `"zhenskiy"`; в сцене урока 5 первым говорит Ким — `"muzhskoy"`.

**Решение владельца от 6 сентября (ответы 2 и 3) действует и здесь:** разговорная
строка внутри объяснения, врезки и таблицы тоже звучит на два голоса, а голос
выбирается по полу имени. Строк вида `Have you got a hat? — Yes, I have.` внутри
объяснений у модуля четыре — сборщику не пропустить их мимо разбора на реплики.

**Что НЕ звучит.**

1. **Строки «так сказать нельзя»** — `She haven't got a jacket.`,
   `My glasses is here.`, `It is white.` о нескольких вещах. Неверная фраза,
   услышанная, запоминается наравне с верной. Это же касается неверных вариантов
   задания `choice` урока 8: звучит только та запись, которая прозвучала верно.
2. **Артикль сам по себе** — одиночное `a` синтез прочтёт названием буквы.
3. **Русские подписи, переводы и транскрипция знаками** — их читают глазами;
   кнопка ставится рядом, а сам знак не озвучивается.
4. **Окончание `-s` отдельной строкой** — оно не слово.
5. **Текст для чтения урока 6** — решение владельца от 6 сентября, ответ 14:
   «звука у текста чтения пока не надо».

**Четыре записи уже лежат** — `sock`, `bag`, `glasses`, `umbrella`. Проверено
ПРОГОНОМ `adresSlova()` по `public/zvuk/slovo/`, а не чтением похожего места.
Остальных двенадцати карточек и всех форм множественного нет; их сделает
`npm run ozvuchka`.

**Ключ `zvuk`, которого нет в напечатанном тексте, — это кнопка, которая никогда
не появится.** Ловит `check:content`; сборщику сверять посимвольно.

**И вторая ловушка того же ряда, о которой стоит помнить именно здесь.**
Английская строка, названная в `zvuchat`, ставит кнопку ВЕЗДЕ, где она
напечатана, — и в правиле случая тоже. Значит правило говорит О строке, а не
повторяет её: не «и получается `I have got a bag`», а «после `I` стоит `have
got`». Иначе на экране встанут две одинаковые кнопки.

---

## 9. Долги модуля — все с адресами

| Долг | Адрес возврата |
|---|---|
| `She is wearing a red hat.` — сказать, что НАДЕТО | **модуль 17 «Прямо сейчас»**: Present Continuous. Там же место карточки `wear` (раздел 12) |
| `I wear jeans.` | **модуль 13 «Что я делаю каждый день»**: Present Simple |
| `He's got`, `She's got` — короткая запись при третьем лице | **вопрос сводящему**, раздел 11, строка 5. Обещания ученику при этом не возникает: строки в модуле нет ни разу |
| `Whose is it? — It is my sister's.` — притяжательный апостроф | **раздел 11, строка 2**: адрес выбирает сводящий, а по смыслу это модуль 12 |
| `Do you have a hat?` — вопрос с `do` | **модуль 15 «Спросить о делах»** |
| `some`, `a lot of` при вещах | **модуль 21 «В магазине: сколько стоит»** |
| `dark blue`, `light green` — два прилагательных подряд | не на этой ступени: A2 у обоих источников |
| слово `coat` | не на этой ступени: A1 Movers у Cambridge, и вещь занята словом `jacket` (раздел 6.1) |
| слово `watch` | **вопрос сводящему**, раздел 11, строка 6 |
| слово `shorts` | не берётся: столкновение с карточкой `short` модуля 9 (раздел 6.5) |
| цена одежды, размер одежды | модуль 21; `size` — не на этой ступени |

**Ни одной врезки «бери как есть», «разбирать не будем» и «запомни целиком» в
модуле нет.** Всё взятое готовым названо с адресом, кроме двух строк, у которых
адреса нет нарочно: решение по ним ещё не принято, и обе стоят вопросами в
разделе 11.

**Возвращение к пройденному** стоит в уроке 1 и названо в разделе 5.2.

---

## 10. Термины: где какое имя называется

| Имя | Кто вводит | Где называется в модуле 11 |
|---|---|---|
| **оборот `have got`** | модуль 11 | урок 1, случай 1, строкой внутри случая |
| слова только во множественном числе | модуль 11 | урок 4, случай 1; отдельного термина у них НЕТ — правило говорит о четырёх названных словах |
| артикль | модуль 3 | не вводится заново, только употребляется |
| множественное число | модуль 8 | ссылкой: «окончание уже встречалось в модуле „Один и много"» |
| окончание | модуль 4 | то же |
| прилагательное | модуль 9 | не вводится заново |
| местоимение | модуль 2 | не вводится заново |

**Двух имён у одной вещи в модуле нет.** Проверка, которую сборщик прогонит по
видимому тексту после сборки (`npm run tekst -- english-beginner-2 <модуль>`):
слов «конструкция», «выражение», «глагол have», «вспомогательный», «хвост»,
«признак», «предмет» в модуле быть не должно ни одного.

**И одно место, где термина НЕ будет нарочно.** Классу слов `trousers`, `jeans`,
`glasses`, `clothes` русская грамматика имя даёт («существительные, которые
употребляются только во множественном числе»), но это не имя, а описание в
шесть слов, и ученику оно не нужно: он и так скажет по-русски «одни брюки».
Правило «настоящий термин, а не своё слово» ловит ПОДМЕНУ существующего имени
выдуманным, а не требует имени там, где вещь называется словами. Здесь имени не
будет, и ярлыка тоже.

---

## 11. Спорные строки: их сводит автор, не я

### Строка 1. «things they have» — половина дескриптора у модуля 7

Модуль 7 взял этот кусок графы CEFR с. 79 под «назвать вещь» (`It is a book.`), а
буквально он про обладание, то есть про `have got`. **Спора я не поднимаю:**
модуль 11 стоит на другой половине той же графы («the colour of clothes») и без
этой строки обходится. Сводящему решать, надо ли переписать раздел 2.2 замысла
модуля 7; **цена — одна строка в его шапке**, содержание не меняется ни в одном
из двух модулей.

### Строка 2. Притяжательный апостроф остался без хозяина, и круг замкнулся

Модуль 6 отказался от него и назвал двух кандидатов — модули 11 и 12
(`plan-modul-6.md`, строки 1219–1226). **Модуль 12 отказался тоже**, и его отказ
стоит на ошибке: `plan-modul-12.md`, строка 276, пишет «`my brother's room` —
притяжательный апостроф, модуль 6 просил его себе». Модуль 6 его себе НЕ просил —
он его отдал. **Круг замкнулся: 6 → 11 или 12, 12 → 6, и слово повисло.**

**Я его тоже не беру, и вот довод.** У ученика к модулю 11 уже три способа
сказать «чьё» — `my` (модуль 2), `our` (модуль 6), `his` и `her` (модуль 5);
четвёртый, устроенный иначе, ряд не достраивает. Предмет модуля 11 — оборот
`have got` и цвет одежды, и апостроф к нему не примыкает ничем.

**Где ему место — называю, чтобы предложение не повисло второй раз:** модуль 12
«Мой дом и комната», где вещи двух людей стоят рядом (`my brother's room`,
`my sister's bed`) и апостроф работает в каждой второй строке. Это довод самого
модуля 6, и он не стал слабее оттого, что модуль 12 прочёл чужой замысел
неверно.

**Третий исход тоже законен:** не брать оборот на ступени вовсе. English Grammar
Profile ставит его на **A2** (`1741163709005x222434593719802460`), и Elementary —
его естественное место. Тогда строки о расхождении не нужно нигде.

### Строка 3. `sock` и `umbrella` — взяты по младшей помете

Разбор — раздел 3.4. **Цена отказа, если сводящий решит иначе:** без `sock` урок
5 теряет одну пару строк (`What colour are your socks?`), а запись урока 7 —
ответ, и оба чинятся заменой на `shoes`; без `umbrella` модуль теряет
единственное место, где артикль `an` работает на новом слове, и случай 5 урока 1
переезжает на `an old bag`. **Обе правки дешёвые, и я называю их сам, чтобы
решение было выбором, а не умолчанием.**

### Строка 4. Пункт `FORM: AS OBJECTS` — беру, и называю довод против себя

`1741163711829x787492176446619900`. Разбор — раздел 4.1. **Что должен увидеть
сводящий, если решит наоборот** (пункт модулю 13, мне — ничего): тогда у модуля
11 поле `egp` пустое, и в шапку встаёт строка «модуль не вводит ни одного пункта
English Grammar Profile: оборот `have got` этот источник не размечает вовсе».
Содержание модуля от такого решения не меняется ни на строку — меняется только
запись.

### Строка 5. `He's got` и `She's got` — брать ли короткую запись при третьем лице

**Вопрос не методический, а о цене.** За: форма ходовая, и ученик встретит её в
первом же живом тексте. Против: `she's` в курсе уже значит `she is` (модуль 2),
и на этой ступени развести две вещи под одной записью нечем; Cambridge печатает
короткую запись только для `I've` (с. 29).

**Что взято сейчас:** формы в модуле нет, и ошибкой она нигде не объявляется.
**Если сводящий решит взять**, это десятый случай урока 1 и одна строка в
таблице форм; переписывать нечего.

### Строка 6. `watch` — нужна выписка, которой я сделать не могу

Слово стоит в теме и у Oxford на A1, но перевод столкнётся с карточкой `clock`
модуля 7 («часы»). Чтобы взять его, нужна выписка из открытого источника о том,
чем `watch` отличается от `clock`, — ровно такая, какую сводящий сделал модулю 7
по слову `clock` (`plan-modul-7.md`, раздел 10.3). **Открытых источников я не
открываю**, поэтому вопрос идёт к нему, а не решается мной молча.

**Пока выписки нет, слово не берётся**, и тема закрывается на 11 из 15 — выше
нормы. Если выписка найдётся, карточка встаёт в урок 3 со значком `231a`, и тема
закрывается на 12.

### Строка 7. Чтение `trousers` — спорное слово, решает владелец

`ipa-en-uk.txt` даёт `/tɹˈaʊsəz/`, `wikipron-en-uk.tsv` — `/ˈtraʊzəz/`. Разбор —
раздел 6.3. **Беру звонкий вариант**; владелец как преподаватель вправе решить
иначе, и правка стоит одной строки плюс переозвучка слова.

---

## 12. Находки о чужих модулях — передаю сводящему, править не могу

Правило проекта: находка о чужом модуле в чужом модуле правится сразу или не
правится никогда. Чужих файлов у меня четыре, и вот что в них надо дописать.

**12.1. Методисту модуля 17 «Прямо сейчас» — две строки.**

> 1. **Карточка `wear` — ваша.** Слово стоит в теме `Clothes and Accessories`
>    (словник A2 Key), у Oxford 3000 — на **A1**, у Young Learners, с. 79, — в
>    столбце Pre A1 Starters. Модуль 11 его НЕ берёт: обоих времён, в которых
>    глагол живёт, у ученика к одиннадцатому модулю нет. Ваш образец у Cambridge
>    напечатан прямо: Young Learners, **с. 43** — «What's the mother wearing?
>    **a purple shirt**».
> 2. **Вся одежда к вашему приходу разобрана:** пятнадцать карточек модуля 11
>    (`hat`, `shirt`, `T-shirt`, `jacket`, `dress`, `skirt`, `trousers`, `jeans`,
>    `shoe`, `boot`, `sock`, `bag`, `glasses`, `umbrella`, `clothes`). Заводить их
>    второй раз не надо; `wear` работает на них с первой же строки.

**12.2. Методисту модуля 13 «Что я делаю каждый день» — одна строка.**

> Пункт `1741163711829x787492176446619900` (`NOUNS / noun phrases - grammatical
> functions`, FORM: AS OBJECTS, A1) взят модулем 11: `I have got a bag` — первое
> место курса, где имя вещи стоит после глагола. Ставьте его в `egpPovtor`.
> Свободен и по смыслу ваш `1741163715288x929829619225120500` (`VERBS / types`,
> FORM: MAIN VERBS, A1) — модуль 11 от него отказался нарочно (раздел 4.3).
> Карточка `wear` при этом отдана модулю 17, а не вам: `I wear jeans` и
> `She is wearing a hat` про одно и то же, и двух разборов одной строки быть не
> должно.

**12.3. Сводящему — об ошибке в замысле модуля 12.**

> `plan-modul-12.md`, строка 276, пишет: «`my brother's room` — притяжательный
> апостроф, модуль 6 просил его себе». **Это неверно:** `plan-modul-6.md`,
> строки 1219–1226, разбирает апостроф и от него ОТКАЗЫВАЕТСЯ, называя двух
> кандидатов — модули 11 и 12. Пока строка стоит как есть, слово повисло между
> тремя модулями, и каждый думает, что оно у соседа. Разбор — раздел 11,
> строка 2.

**12.4. Методисту модуля 9 — подтверждение, а не просьба.**

> Ваши три запрета исполнены: модуль 11 не заводит ни одной карточки цвета, не
> повторяет разбора `What colour is it?` и ставит ваши пункты о прилагательном в
> `egpPovtor`. Вопрос `What colour are …?` разбирается один раз и у меня.
> Ничего править в модуле 9 не надо — запись оставлена как есть нарочно.
> **Одно уточнение к разделу «Слова цвета: чьи какие»:** там сказано, что из слов
> одежды «`boots`, `glasses`, `jeans`, `shorts`, `trousers` живут только во
> множественном». Точнее так: только во множественном живут `trousers`, `jeans`,
> `glasses` и `clothes`; `boots`, `shoes` и `socks` — обычные слова, которые
> носят парой и потому чаще встречаются во множественном. Модуль 11 разводит эти
> две вещи (урок 4 против урока 5), и на границу это не влияет.

---

## 13. Что проверено и чего проверить не удалось

**Проверено по самому файлу, с цитатой и страницей:**

- **CEFR Companion 2020:** с. 79 (Information exchange — графу A1 читал сплошным
  текстом по второму разбору, строки 3704–3713, чтобы не приклеить `Pre-A1`),
  с. 57 (Reading correspondence), с. 67 (Creative writing), с. 68 (Reports and
  essays — «No descriptors available»), с. 83, с. 84;
- **руководство Young Learners:** с. 9 (Can Do summary, Pre A1 Starters), с. 21
  (Reading and Writing Part 3, «The man has got black hair and glasses» — прогон
  `--find`, одно совпадение), с. 28 (Speaking, «What colour is it?»), **с. 29**
  (Grammar and Structures List, раздел `Verbs`, строки `Have (got) for
  possession` и `Conjunctions`; принадлежность страницы ступени Pre A1 Starters
  проверена по оглавлению самого файла, а не по памяти соседа), с. 43 (`wearing`
  — ступень A1 Movers), **с. 79** (тематический список `Clothes` по трём
  столбцам, читан обоими разборами);
- **руководство A2 Key:** с. 29 (формат ответа на слух), с. 42 (Speaking Part 1);
- **словник A2 Key:** Приложение 2, тема `Clothes and Accessories` — разобрана
  вторым разбором и пересчитана своим скриптом: 54 записи, из них 15 с пометой
  Oxford A1; счёт сошёлся с `npm run programma`;
- **English Grammar Profile:** разобраны все 1222 записи настоящим разбором CSV
  (сторож сошёлся: 1222 из 1222, A1 — 109) и пройдены поиском `have`, `got`,
  `possession`, `plural`, `objects`, `wh-` **по всему файлу, а не по
  подкатегории**;
- **Oxford 3000** — построчно по выгрузке для всех 54 записей темы и для всех
  шестнадцати карточек;
- **`ipa-en-uk.txt` и `wikipron-en-uk.tsv`** — транскрипции всех шестнадцати
  карточек и пяти форм множественного;
- **`public/zvuk/slovo/`** — ПРОГОНОМ `adresSlova()`: какие записи уже лежат
  (четыре из шестнадцати). Это прогон, а не чтение соседней ветки;
- **`scripts/programma.mts`** — чтением кода: ветка `if (p.stupen !== stupen)`
  обходит `egp` и `egpPovtor` одинаково, значит пункт A2 нельзя поставить ни в
  одно из полей;
- **код курса** — `grep` по `term:`, `title:`, `outcome:`, `egp` и `have got` по
  всем восьми написанным модулям; порядок модулей брал из `index.ts`, а не из
  имён файлов;
- **замыслы модулей 2–10 и 12** и разборы к ним — целиком, а не по своему
  разделу; `otvety-vladeltsa-6-sentyabrya.md` — целиком.

**Чего проверить не удалось, и об этом лучше сказать, чем умолчать:**

1. **Опору урока чтения (CEFR с. 57) я сверял одним прогоном и одним чтением
   второго разбора**, а не двумя разборами дважды, как остальные. Это последняя
   опора, которую я брал, и сборщику стоит перепроверить её первой.
2. **Разметку столбцов на с. 79 руководства Young Learners глазами я не
   смотрел** — читал двумя разборами. Оба сходятся, и перечень с. 29 их
   подтверждает, но это не то же самое, что увидеть страницу.
3. **Наличия значков Twemoji я не проверял** — их тянет из сети
   `npm run znachki`. Про `skirt` и `trousers` я утверждаю не «значка в Twemoji
   нет», а «подходящего я не нашёл, искал среди знаков одежды 1f452–1f462,
   1f9e2–1f9e6, 1fa73, 2602, 1f302». Проверять глазами — сборщику.
4. **Отвлекающие уроков 2–5 глазами ученика я не проходил.** Подробный замысел
   есть только у урока 1; остальные расписаны кратким составом, и мой разбор их
   ровно настолько подробен, насколько подробна их строка. То же относится к
   работе модуля: углы и ответы названы, а сами вопросы напишет тот, кто будет
   собирать модуль, и породу «задание наказывает за верный ответ» на них придётся
   проверять заново.
5. **`npm run bliznetsy` по ненаписанному модулю бессмыслен.** Все утверждения о
   двойниках (разделы 5.3, 5.4, 7) сделаны чтением; после сборки прогон
   обязателен, особо по парам «модуль 11 против модуля 8» (там тоже множественное
   число) и «работа модуля 11 против работы модуля 9».
6. **Различия `watch` и `clock` в `materials/` я не нашёл.** Искал по обоим
   руководствам Cambridge и по словнику; толкований они не дают вовсе — их там и
   не бывает. Значит вопрос идёт к сводящему, у которого есть открытые источники
   (раздел 11, строка 6).
7. **Про `jumper` я утверждаю только то, что проверил:** Oxford 3000 слова не
   знает (`npm run slovo`), в словнике A2 Key оно есть, у Young Learners в списке
   `Clothes` его нет ни в одном из трёх столбцов. Британскую норму («jumper, а не
   sweater») беру из правил проекта, а не из `materials/`: наши источники пометок
   разновидности языка не дают вовсе.
8. **Утверждение «в части 2 жанр `email` не занят никем»** сделано по замыслам
   модулей 7–12 и по коду написанных. Замыслы модулей 9, 10 и 12 при этом моложе
   суток, и сборщик обязан проверить это заново перед сборкой.
9. **Строки уроков 2–5 на «не напечатан ли ответ выше» я проверял только внутри
   своего замысла.** Полная проверка возможна лишь по собранному коду
   (`check:content` сообщает об этом сведениями), и она за сборщиком.
