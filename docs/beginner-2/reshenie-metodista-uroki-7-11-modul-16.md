# Модуль 16 «Как часто», уроки 7–11 и работа: решения методиста до редактора

Дата: 14 сентября 2026. Файл модуля читан в нынешнем виде (коммит `e61964d`).
Файлы курса и замысел не правлю: строки вносит сводящий.


## Как проверялось (одно на все пункты)

- **Прогон** — скрипт `m16v3/progon.mts` в рабочей папке сессии, обходит `courses/english-beginner-2/index.ts`
  целиком. Для каждого ответа (`short` и `accept`, целая строка `gap`, сборка `order`, верный вариант `choice`) и
  каждой строки работы (ответ, `accept`, строки условия, реплики `zvuk`) ищет:
  - строки **материала** (объяснения, таблицы, примеры карточек, пример-разговор, запись, текст) модуля 16 в том же
    уроке или раньше — не больше **двух** различий; модулей 1–15 — не больше **одного**;
  - строки **заданий** модуля 16 и работ модулей 13–16 — с тем же порогом.
  Различие — слово, которого нет в другой строке (окончания `-s`, `-es`, `-ies` снимаются); рядом дан счёт по порядку
  слов. Слова строки сверяются с карточками `vocab` курса: известно, если карточка стоит в модулях 1–15 или в модуле 16
  не позже урока строки. Артикли, формы be и имена карточек не имеют по устройству курса.
- **Выбор** — для каждого `choice` урока считаются расстояния между тремя вариантами: верный не должен быть единственным,
  близким к обеим другим (правило «Как читать таблицы», замысел 18).
- **Граница:** работы частей и экзамен не сверялись; строки условий работы, ещё стоящие заглушками, прогон видит
  только там, где английская строка вписана в заглушку.

---

## 1. Четыре ответа, отличающиеся от строки материала одним словом

Порог решения один на все четыре, и он разный для урока и для работы:
- **задание урока** стоит сразу после случая, который оно отрабатывает, и близкая строка материала там законна — это
  и есть отработка. Недопустима только дословная (правило замысла 18: строка-ответ не стоит в материале);
- **вопрос работы** проверяет модуль после всех уроков. Если ответ совпадает со строкой материала всем, кроме одного
  слова, и эта строка показывает то самое преобразование, которого вопрос требует, — вопрос проходится
  воспоминанием строки. Такой вопрос правится.

### 1.1. Урок 1, сборка `z5-sobrat-my-vsegda-gotovim` и случай 6 — ОСТАВИТЬ

Ответ `We always cook in the evening.`, строка случая 6 `We always study in the evening.` — одно различие (прогон,
`razl 1`). Причины оставить:
1. Это задание урока, а не работы: оно стоит тремя экранами ниже случая и отрабатывает его место слов.
2. Куски даны ученику. Заглавная у `We` и точка у `in the evening.` ставят два куска из четырёх; ученик решает одно —
   `always` перед `cook`. Воспоминание строки случая даёт ровно то же решение, что и правило случая 2, то есть
   обхода правила нет.
3. Тот же довод принят для сборки урока 6 (`reshenie-metodista-uroki-2-6-modul-16.md`, «Проверено и оставлено»).

### 1.2. Урок 8, вставка `z3-vpisat-always-v-vopros` и урок 1, случай 2 — ОСТАВИТЬ

Строка вставки `Do they always study at school?`, строка урока 1 `They always study at school.` — одно различие.
Причины оставить:
1. Ученик пишет одно слово, и слово это задаёт условие («ВСЕГДА ли»). Строка урока 1 даёт то же `always`, что и
   условие, — второй дороги к ответу она не открывает.
2. Строка стоит семью уроками выше и в объяснении, а не в примере карточки. Правило проекта: расстояние смягчает
   повтор из объяснения.
3. В своём уроке ближайшая строка — `Do they usually eat at school?` (случай 5), два различия, и `always` в ней нет.

### 1.3. Вопрос 8 работы и урок 8, случай 3 — ПРАВКА

Ответ `Do you often cook at home?`, строка случая 3 `Do you always cook at home?` стоит в уроке 8 **дважды** и со
звуком — одно различие. Вопрос даёт строку `Do you cook at home?` и требует двух вещей: слова «часто» и места для
него. Строка случая показывает место на тех же самых словах, и ученику остаётся заменить `always` на `often`. Это
ровно порог работы, названный выше.

**Решение.** Данная строка — `Do your parents cook fish?`, ответ — `Do your parents often cook fish?` Условие
(пишет редактор): Ким спросил Дану `Do your parents cook fish?`, а хотел узнать, часто ли её родители готовят рыбу;
перепиши вопрос так, чтобы в нём было «часто».

**Опора и сверка.**
- Прогон новой пары: ни одной строки материала и заданий модуля 16, ни одной строки работ модулей 13–15 в пределах
  порога. Слова: `parents` — модуль 6 (`moya-semya.ts`), `cook` — модуль 13, `fish` — модуль 14, `your` — курс
  печатает с модуля 5; прогон «slova est».
- **Отвергнуты прогоном три строки**, и причина у каждой записана, чтобы их не предложили снова:
  `Do you often go to the library?` — данная строка `Do you go to the library?` дословно стоит в условии вопроса 2
  работы модуля 15 (`q2-otmetit-otvet-kima`); `Do you often drink juice?` — одно различие с вариантом выбора урока 8
  (`Do you often drinks coffee?`) и с таблицей урока 2; `Do you often sleep in the afternoon?` — одно различие со
  строкой отметки урока 8 (`Do you usually sleep in the afternoon?`).
- Место наречия при `your parents` урок 8 показывает до работы: строка отметки `Do your parents always eat at home?`
  (задание 2) и, после правки пункта 3.6, случай 5. Это законно: работа применяет правило к новым словам.
- Второй порядок `Do your parents cook fish often?` в `accept` не ставлю: это та же нерешённая порода, что наречие
  в конце утвердительной строки (`reshenie-metodista-urok-1-modul-16.md`, пункт 3, «ПРОВЕРИТЬ»). Решается одним
  ответом для всего модуля, не здесь.

**Кому.** Сводящему — `answer` и строка в заглушке условия `q8-perepisat-vopros-s-often`, таблица 16.2 замысла.
Редактору — условие и разбор.

### 1.4. Вопрос 3 работы: данная строка `I don't eat meat.` стоит в модуле 14 — ОСТАВИТЬ

Прогон: `I don't eat meat.` дословно стоит в модуле 14 пять раз — случаи уроков 2 и 6, таблица урока 6, запись
урока 8 — и в `accept` задания урока 8 того же модуля. Причины оставить:
1. Данная строка — не ответ, а то, что ученик преобразует. Знакомая строка прошлого модуля здесь помогает: ученик
   узнаёт отрицание, которое уже умеет, и строит из него новое.
2. Ответ `I never eat meat.` не напечатан нигде: ближайшие строки модуля 16 (`I never drink coffee.`, `They never
   eat fish.`) — два различия. Преобразование `don't` → `never` урок 3 показывает на других словах (`drink coffee`,
   случай 4; `sing at school`, свод), поэтому вопрос требует применить его к `eat meat`, а не вспомнить строку.
3. С работой модуля 14 действие не совпадает: там вопрос 2 (`We don't eat meat.` вариантом) просит выбрать верную
   короткую запись, здесь — переписать строку со словом «никогда».

**Попутно:** `accept` `I don't ever eat meat.` — у `ever` нет карточки ни в одном модуле (прогон «NET»). Это
законно: `accept` принимает неразобранное, чтобы не наказать за верный ответ. Разбор вопроса слова `ever` не
называет — редактору.

---

## 2. Вопрос 2 работы повторял сцену вступления урока 2 — ВОПРОС СТАНОВИТСЯ ЗАПИСЬЮ ОТВЕТА СО СЛОВОМ `usually`

**Что было не так, двумя сторонами.**
1. **Близнец материала.** Сцена вопроса (Дана, рано почти каждый день, в выходные позже) и верный вариант («always
   говорит, что исключений нет, а у Даны они есть») пересказывали вступление урока 2 — порода «работа списывает у
   самого материала» (записано в `reshenie-metodista-uroki-2-6-modul-16.md`, В5, попутно).
2. **Итог не спрашивался.** Итог вопроса — «называть среднюю частоту словами usually и often». Вопрос спрашивал, почему
   не подходит `always`: ни `usually`, ни `often` ученику называть было не нужно. Это та же беда, что была у
   вопроса 12, только с другой стороны. И вопрос 2 — **единственный** вопрос на этот итог.

**Решение.** Вопрос 2 — `short`, действие «ответить на вопрос строкой о себе».

| Поле | Строка |
|---|---|
| `id` | `q2-otvetit-so-slovom-usually` |
| `kind` | `short` |
| `outcome` | без изменений: «называть среднюю частоту словами usually и often» |
| условие (факты; пишет редактор) | друг спрашивает `Do you often swim in the evening?`; ответь по-английски строкой о себе, в которой будет слово «обычно» |
| `answer` | `I usually swim in the evening.` |
| `accept` | `Yes, I usually swim in the evening.` · `Yes, I do. I usually swim in the evening.` · `Usually I swim in the evening.` |
| разбор (что назвать) | верную строку и место `usually` перед глаголом. Порядок `Usually I…` разбор не называет |

**Опора и сверка.**
- Прогон: ответ — два различия со строками материала (`I always read in the evening.`, `We usually study in the
  evening.`, `I usually walk in the park.` и др.), ни одного с одним; вопрос друга — два различия со строкой таблицы
  урока 8 (`Do they often read in the evening?`) и со сборкой урока 8. Слова: `swim` — модуль 14, `evening` — модуль 13;
  «slova est».
- **`accept`, порода 14.** Урок 8, случай 4, учит отвечать короткой записью и строкой (`No, I don't. I sometimes cook at
  home.`), значит `Yes, I do. I usually swim in the evening.` — ответ по уроку, и его обязаны принять. `Yes, I usually…`
  — та же строка без короткой записи. `Usually I…` — порядок, которого курс не разбирает нигде; `accept` принимает его
  молча по правилу 3 решения по уроку 1.
- **Отвергнуты прогоном:** `I usually eat fish at home.` (рамка «I usually … at home» — два различия с ответом вопроса 11
  той же работы); `I usually drink milk in the morning.` (одно различие с образцом урока 2
  `I usually drink tea in the morning.`). Отвергнут и первый мой вариант — выбор причины ошибки в строке
  `My parents often swim in the evening.`: вариант выбора не заставляет ученика назвать слово, а итог требует назвать.
- **Близнецом урока не стал.** Письменно отвечать на вопрос строкой с наречием уроки 1–10 не просят; устное задание
  урока 11 (пункт 3.8, задание 4) делает похожее вслух, и это смена вида: урок говорит, работа пишет.
- С вопросом 11 (`I read at home.` → `I usually read at home.`) слово одно, а данное разное: там строка, здесь вопрос
  друга. Проверена замена вопроса 11 на «всегда» (`I always read in the park.`) — одно различие со случаем 2 урока 1,
  отвергнута; вопрос 11 остаётся как есть.

**Кому.** Сводящему — поля вопроса по таблице, таблица 16.2 и 16.3 замысла, шапка работы в коде (действий теперь
четыре, см. 5.3). Редактору — условие и разбор.

---

## 3. Прогон всего модуля заново и уроки 7–11 по заданию

Прогон по нынешнему `kak-chasto.ts`: 493 строки отчёта (`m16v3/modul.txt`), затем четыре партии строк-кандидатов.

### 3.1. Слова

Всё с карточками и не позже места употребления, кроме двух:
- `English` — урок 7, случаи 1 и 2. Название языка: имена собственные ступенью не меряются (решение владельца от
  15 августа), и курс уже печатает `Do you study English?` с переводом «английский» в модуле 15. Не долг.
- `ever` — `accept` вопроса 3. Законно, см. 1.4.

Карточек «позже места» нет ни одной.

### 3.2. Уроки 1–6 после правок — новых правок нет

Строки с одним различием, которые дал прогон, и почему они остаются:
- урок 4, выбор: верный `Kim is often late.` — случай 4 `He is often late.` Выбор по смыслу; правило об одинаковом
  отношении вариантов к материалу написано для сличения и к заданию на смысл не относится;
- урок 5, вставка `Nurlan often walks in the park.` — случай 3 урока 2 `We often walk in the park.`; урок 5, запись
  `Dana always cooks in the evening.` — сборка урока 1 `We always cook in the evening.` В обоих случаях воспоминание даёт
  глагол без окончания, то есть ведёт мимо ответа, а не к нему;
- урок 6, запись и сборка — решены в `reshenie-metodista-uroki-2-6-modul-16.md` («Проверено и оставлено»).

### 3.3. Строение выбора: верный не стоит серединой

Прогон считает окончание за одно слово с основой, поэтому уроки 5, 7 и 8 пересчитаны вручную по буквам.

| Урок | Верный | Близкий к обеим другим | Итог |
|---|---|---|---|
| 1 | 1 | вариант 2 | годно |
| 2 | 3 | вариант 1 | годно |
| 3 | 2 | вариант 1 | годно |
| 4 | 3 | вариант 1 | годно |
| 5 | 2 | вариант 1 (`drink` + `morning`) | годно |
| 6 | 1 | ни один | годно |
| 7 | 3 | ни один: `days`, `weeks`, `week` попарно на одно слово | годно |
| 8 | 2 | вариант 3 (`often drinks`) | годно |
| 9 | 1 | верный — единственный непохожий | правка, 3.5 |
| 10 | 2 | ни один (три слова) | годно |

### 3.4. Урок 7 «Каждую неделю»

**3.4.1. Вступление спорит с уроком 1 — ПЕРЕПИСАТЬ ФАКТ.** Заглушка: «не „часто“ и не „иногда“, а ровно раз в месяц».
По уроку 1 («Словом sometimes говорят о том, что бывает не каждый раз») встреча раз в месяц — как раз «иногда», а
отвести «часто» можно только шкалой, которую владелец снял 13 сентября. Порода та же, что В5. Сцена утверждает одно:
Айгерим видится с бабушкой каждый месяц; слова «часто» и «иногда» не говорят, сколько времени проходит между
встречами, а «каждый месяц» говорит. **Кому:** редактору — вступление; сводящему — заглушка и 9.7 замысла.

**3.4.2. Две таблицы с одними и теми же строками — СВОД МЕНЯЕТСЯ.** `tablica-slovo-posle-every-odno` и
`tablica-svod-uroka-7` держат одно и то же: `every day`, `every week`, `every month`. Урок показал бы один экран
дважды — порода, снятая в своде урока 3 (18.12). `check:content` уже называет совпавшие заглушки обеих. Свод — одна
строка с тремя оборотами:

| Строка | Перевод |
|---|---|
| `I read every day.` | Я читаю каждый день. |
| `I read every week.` | Я читаю каждую неделю. |
| `I read every month.` | Я читаю каждый месяц. |

Прогон: с материалом урока — два различия, кроме `I read every week.` против `I swim every week.` и `I work every week.`
(одно). Это материал против материала: свод показывает то же устройство на одном глаголе, ответом задания строка не
является. С ответами заданий и работы — не ближе двух. Подпись свода (пишет редактор): оборот с every стоит в конце,
и после every одно слово.

**3.4.3. У случая 1 один пример — ДОБАВИТЬ ВТОРОЙ.** `She goes to school every day. — Она ходит в школу каждый день.`
Строка дословно из модуля 13, урок 6, случай 6 (прогон: 0 различий), с тем же переводом: случай и говорит, что
`every day` уже знаком.

**3.4.4. Случай 6 `sluchay-every-i-always` — ПРАВИЛО ПО СУТИ.** Заглушка: «every week называет срок, always говорит,
что исключений нет». Но `every week` тоже говорит «каждую неделю, без исключений», и ученик прочтёт
противопоставление, которого нет. Редактору: правило говорит, что `always` срока не называет, а `every week` называет.
О том, что одно бывает чаще другого, ни случай 6, ни случай 7 не говорят (шкала снята).

**3.4.5. Задание 4, `accept` — ДОБАВИТЬ `I see friends every week.`** Порода 14: «с друзьями» не требует `my`, а
множественное число без определителя курс печатает (модуль 14: `I read books in the evening.`). Прогон: одно различие
только с самим ответом и с `accept`. Порядок `Every week I…` `accept` принимает молча, как решено (правило 3).
Запятая внутри строки сверкой не снимается (`lib/content/check.ts`, `normalizeKeepingCase` снимает только знаки в
конце), и `Every week, I see my friends.` засчитано не будет; курс учит строке без запятой (урок 6, случай 3), в
`accept` её не ставлю.

**3.4.6. Задания 1, 3, 5, 6 и сетка — ГОДНЫ.** Выбор — 3.3; вставка определена условием «КАЖДЫЙ МЕСЯЦ»; сборка
складывается одним порядком (заглавная у `Dana`, точка у `every month.`). Сетка: `check:content` называет лишние
слова только в сетках модулей 5 и 7 (`dress`, `hair`), о модуле 16 молчит. `kontrol` пишет у сетки «НИ ОДНОГО
БЛОКА»: слова сетки — карточки уроков 1, 2, 3 и 7 (`always`, `usually`, `magazine`, `never`, `month`), сетка
повторяет словарь модуля, и опора у неё есть. Сводящему — запись в `courses/resheno.ts` с этой причиной.

### 3.5. Урок 9 «Читаем: неделя Айгерим»

**3.5.1. Задание 2 угадывается по строению — ОТВЛЕКАЮЩИЕ МЕНЯЮТСЯ.** Варианты `She sees her grandmother.` (верный),
`She swims.`, `She drinks tea.`: два неверных похожи друг на друга, верный — единственный длинный и с дополнением. Тот,
кто языка не знает, выбирает непохожий и попадает. Новые неверные — пересказ строк текста той же длины:

| № | Строка | Верно |
|---|---|---|
| 1 | `She sees her grandmother.` | да |
| 2 | `She swims at the weekend.` | — |
| 3 | `She drinks tea in the morning.` | — |

Прогон: 2 и 3 — по два различия со строками текста 4 и 6; дословно в тексте нет ни одной. Место верного не меняется.
**Кому:** сводящему — варианты; редактору — разбор (плавает она иногда и в выходные, чай пьёт часто и утром, а каждый
месяц видится с бабушкой).

**3.5.2. Комментарий над заданиями урока неверен — СНЯТЬ.** «Строку `Sometimes I swim at the weekend.` задания обходят:
на ней стоит вопрос 9 работы». Задание 1 отмечает `Sometimes` именно в этой строке, а после 5.1 вопрос 9 на ней не
стоит. **Кому:** сводящему.

**3.5.3. Задания 1, 3, 4, 5 — ГОДНЫ.** Ответ из текста в уроке чтения законен (замысел 18.9).

### 3.6. Урок 8 «Спрашиваю»

**3.6.1. Случай 5 «вопрос о нескольких людях» держит строку об одном — ЗАМЕНИТЬ ВТОРУЮ СТРОКУ.** `Do you often read a
newspaper? — Ты часто читаешь газету?` переведена на «ты», то есть говорит об одном, а имя случая — о нескольких.
Строка: `Do your friends often read a newspaper? — Твои друзья часто читают газету?` Прогон: два различия только с
прежней строкой; `friends` — модуль 2. Строка ставит `your …` в материал раньше задания 2 (`Do your parents always eat
at home?`) и вопроса 8 работы (1.3); с ответом вопроса 8 — три различия.

**3.6.2. Вставка — 1.2; сборка — пункт 4. Задания 1, 2, 4, 6 — ГОДНЫ.** Выбор — 3.3; запись `Do you usually get up
early?` определена условием («ОБЫЧНО ли»). `kontrol` пишет у случая 1 «без задания»: случай напоминает вопрос с `do`
из модуля 15, и задания урока опираются на него через случаи 2–5. Сводящему — запись в `courses/resheno.ts`.

### 3.7. Урок 10 «Слушаем» — ОДНА ПРАВКА УСЛОВИЯ (3.7.1)

Ответы `never`, `magazine`, `early`, `weekend`, `usually` определены условиями; `accept` у `magazine` и `weekend`
верны; в выборе все три слова звучат в записи 2. Действие задания 1 («как часто, одним словом») совпадает с вопросом
10 работы. Рамку вопроса на слух задаёт руководство A2 Key, с. 29 («ONE word, or a number, or a date, or a time»);
опасен там ответ, а он у работы другой (5.1).

**3.7.1. Задание 2 называет второго говорящего «он», а звучит женский голос — УСЛОВИЕ ГОВОРИТ «ОНА».** Прогон
`npm run golosa -- english-beginner-2`: запись `zapis-kim-sprashivaet-o-chtenii` `[m]` — «муж. Do you often read a
newspaper?», «жен. No, I never read newspapers. I sometimes read a magazine.» Заглушка задания 2 — «что он читает
иногда». Ученик слышит женщину и читает «он». Задание 1 говорит «второй говорящий» и родом не ошибается; редактору —
условие задания 2 о ней, без имени (имя в подписи назвало бы, кто отвечает, но не ответ — решает редактор). Тот же
прогон: запись вопроса 10 работы — «жен.» спрашивает, «муж.» отвечает, поэтому «он» в условии 5.1 верно; образец урока
8, задание 6 — «жен.», «муж.».

### 3.8. Урок 11 «Говорим» — СОСТАВ ЗАДАНИЙ МЕНЯЕТСЯ

**Что не так.** Четыре устных задания повторяют устные задания уроков по одному: «что всегда» — урок 1, задание 6;
«что иногда, начав с sometimes» — урок 6, задание 6; «чего никогда» — урок 3, задание 6; «что каждую неделю» —
урок 7, задание 6 (`check:content` уже называет совпавшие заглушки уроков 7 и 11). Сборка `I never watch television
in the evening.` — близнец сборки урока 3 `We never watch television in the morning.`: два различия, то же наречие,
тот же глагол. Урок говорения из тех же шагов — пересказ уроков, а не урок.

**Чем урок 11 отличается по источнику.** Второй дескриптор модуля — «Can describe simple aspects of their everyday
life in a series of simple sentences» (CEFR, с. 63), главный — «ask and answer questions about themselves and daily
routines» (с. 72); оба выписаны в `sources` модуля. Уроки 1–7 дают по две строки с заданными наречиями. Урок 11 даёт
связный рассказ о своей части жизни, где наречия выбирает ученик, и ответ на вопрос друга.

| № | kind | Условие (факты) | Образец |
|---|---|---|---|
| 1 | speak | расскажи о своём утре тремя строками: что бывает всегда, что обычно, чего не бывает никогда | `I always drink milk in the morning. I usually cook breakfast. I never read in the morning.` |
| 2 | speak | расскажи о себе двумя строками: одну начни словом sometimes, во второй назови точную частоту оборотом every | `Sometimes I read in the afternoon. I see my cousin every month.` |
| 3 | speak | расскажи о себе двумя строками: когда у тебя всегда много дел и что ты часто делаешь вечером | `I am always busy at school. I often dance in the evening.` |
| 4 | speak, разговор на два голоса | друг спрашивает `Do you often watch television?` Ответь коротко и добавь строку о себе с наречием частоты | `Do you often watch television? — No, I don't. I sometimes watch television in the evening.` |
| 5 | order | собери строку о себе | куски `drink` · `I` · `juice in the afternoon.` · `usually`; ответ `I usually drink juice in the afternoon.`, `answer: [1, 3, 0, 2]` |

**Сверка.**
- Прогон: слова всех строк с карточками (`breakfast` — модуль 13, `cousin` — модуль 6, `dance` — модуль 14, `busy`
  — урок 4). Дословных совпадений со строками модуля нет; с одним различием стоят образцы 1–3 рядом с образцами и
  таблицами других уроков. Образец `speak` машина не проверяет, и звучит он после ответа ученика, поэтому близость
  здесь не утечка. Дословный повтор отводил: кандидат `I always get up early.` стоит в тексте урока 9 и в записи урока
  10 — снят. Образец 3, вторая строка, `I often dance in the evening.` в прогон не ставился — ПРОВЕРИТЬ сборщику тем же
  скриптом; слова `often`, `dance`, `evening` с карточками.
- Сборка: порядок один без условия. `juice in the afternoon.` — один кусок; строка `I drink usually juice…` из кусков
  складывается, но наречие между глаголом и дополнением законного чтения не имеет; `Usually I…` не складывается,
  потому что заглавная стоит только у `I`. Место `usually` сборкой ещё не спрашивалось (сборки уроков 1, 3, 4, 6, 7, 8
  — always, never, am often, Sometimes, every month, often).
- Итог урока «строить о себе строку с наречием частоты» исполняют все пять заданий: каждое о себе. Задание 4 — не
  близнец задания 6 урока 8: там ученик и спрашивает, и отвечает «Yes, I do.», здесь отвечает строкой с наречием.
- Условие 3 обходит «занят»: при обращении на «ты» краткое прилагательное родовое (CLAUDE.md, «Родовая форма живёт не
  только в прошедшем времени»). Образец при этом держит `busy` — английскому роду не нужен.
- Разборы — с формулой самопроверки без счёта строк (`reshenie-metodista-urok-1-modul-16.md`, пункт 4). Образец 4 —
  разговор, голоса по умолчанию; имён в нём нет.

**Кому.** Сводящему — пять заданий (`id` предлагаю `z1-rasskazat-pro-utro`, `z2-rasskazat-sometimes-i-every`,
`z3-rasskazat-kogda-mnogo-del`, `z4-otvetit-drugu`, `z5-sobrat-stroku-o-sebe`), комментарий «Все образцы одним
голосом» снять, 9.11 и 18.11 замысла. Редактору — условия, подсказки, разборы. После сборки — `npm run ozvuchka`.

---

## 4. Урок 8, сборка `z5-sobrat-vopros-s-often` — КУСКИ МЕНЯЮТСЯ, УСЛОВИЕ ПОРЯДОК БОЛЬШЕ НЕ ДЕРЖИТ

**Решение.** Куски: `often swim` · `Do` · `at the weekend?` · `you`. Ответ `Do you often swim at the weekend?`, порядок
номерами 2 4 1 3 (`answer: [1, 3, 0, 2]`).

**Почему не оставить условие.**
1. **Единственность держалась на утверждении без опоры.** Замысел 18.8 говорит, что `Do you swim often at the weekend?`
   «тоже верный английский», — это сказано по знанию, а не по источнику. Ровно этот вопрос (наречие частоты после
   глагола и дополнения) у меня стоит нерешённым «ПРОВЕРИТЬ» (`reshenie-metodista-urok-1-modul-16.md`, пункт 3):
   графы о нём в English Grammar Profile нет. Куски, которые второго порядка не складывают, делают вопрос ненужным:
   решать его в сборке не приходится.
2. **Условие, называющее место слова, называет ответ.** «Слово о частоте стоит перед глаголом» — это и есть то, что
   сборка проверяет.
3. **Приём уже принят в модуле:** сборка урока 4 держит `am often` одним куском по той же причине.

**Что ученик решает теперь.** `Do` первым (заглавная), `at the weekend?` последним (вопросительный знак); остаются `you`
и `often swim`. `Do often swim you` смысла не имеет, и ученик ставит `you` сразу за `Do`, перед наречием — место
наречия в вопросе после того, кто делает, и есть предмет урока 8 (случай 2). Решения «наречие до глагола или после» в
сборке больше нет — его отрабатывали уроки 1–6.

**Кому.** Сводящему — `items`, `answer`, комментарий над заданием («ОДИН ПОРЯДОК ДЕРЖИТ ТОЛЬКО УСЛОВИЕ» снять), 18.8 и
18.13 замысла (строка «сборка урока 8 держится условием» и строка таблицы отступлений). Редактору — условие без слов о
месте наречия, подсказка, разбор.

---

## 5. Работа модуля целиком

### 5.1. Каждый вопрос: итог, действие, решение

| № | Итог (урок) | Что вопрос требует | Решение |
|---|---|---|---|
| 1 | 1 | исправить `always` на `sometimes` в данной строке | **условие доопределить.** «Нурлан читает газету не каждый день» пропускает и `usually`, и `often`: по уроку 2, случаи 1 («как правило, но с исключениями») и 3 («случается много раз»), оба — «не каждый день». Порода 14. Условие называет русское слово: Нурлан хотел написать «Я иногда читаю газету», а написал `I always read a newspaper.` |
| 2 | 2 | ответить на вопрос строкой с `usually` | пункт 2 |
| 3 | 3 | переписать `I don't eat meat.` со словом «никогда» | оставить, 1.4 |
| 4 | 4 | вставить форму be | оставить: ответ `I am always busy at the weekend.` — два различия с примером карточки `I am always busy in the morning.`; хвост строки даёт условие, и ученик применяет правило, а не повторяет пример |
| 5 | 5 | вернуть окончание | оставить: два различия с ближайшими строками |
| 6 | 6 | перенести `sometimes` в начало данной строки | оставить: близких строк нет. Урок 6, задание 4, строит такую строку с русского — работа даёт английскую, вход другой (таблица 16.3) |
| 7 | 7 | исправить `every months` | оставить; пересечение с вопросом 10 снято в 5.1, вопрос 10 |
| 8 | 8 | вставить `often` в вопрос | правка, 1.3 |
| 9 | 9 | ответить по записке Айгерим | **правка, ниже** |
| 10 | 10 | записать слово со слуха | **правка, ниже** |
| 11 | 11 | вставить `usually` в данную строку | оставить: два различия; замена проверена и отвергнута (пункт 2) |
| 12 | 1 | назвать смысл строки с `sometimes` | **правка, ниже** |

**Вопрос 9 — ЗАПИСЬ ПЕЧАТАЕТСЯ В УСЛОВИИ, И ОНА НОВАЯ.** Условие ссылается на записку урока 9, а страница работы
уроков не показывает: ученик отвечает по памяти о тексте. Работы соседей так не делают — модуль 13
(`q9-prochitat-tri-stroki-i-otvetit`) и модуль 14 (`q10-nayti-kto-ne-vodit`: «Прочитай запись: Alim drives. Dana
doesn't drive…») печатают запись в условии. Печатать же текст урока 9 значит переигрывать урок чтения — порода
«работа списывает у материала». Значит запись новая.

| Поле | Строка |
|---|---|
| `id` | `q9-po-zapisi-kak-chasto-televizor` |
| запись в условии | `I always eat at school. Sometimes I watch television in the afternoon. I often go to the park.` (пишет Ким) |
| вопрос (факты) | как часто Ким смотрит телевизор днём? |
| варианты, по-русски | «всегда» · «иногда» (верный) · «часто» |

Все три слова вариантов стоят в записи, каждое в своей строке: попасть можно, только найдя строку о телевизоре и
поняв её наречие — это и есть итог «находить в записи, что человек делает всегда, а что иногда». С заданием 2 урока 9
действие не совпадает: там по сроку ищут дело, английскими строками; здесь по делу ищут частоту, русскими словами.
Прогон строк записи: не ближе двух различий ни с чем в модуле 16 и работах модулей 13–15; `go to the park` в курсе
не печатался (поиск — только резерв урока 7 в комментарии). Слова с карточками. Верный — на втором месте, как был.

**Вопрос 10 — ЗАПИСЬ И ОТВЕТ МЕНЯЮТСЯ.** Три беды в одном вопросе:
1. порода 14: на «как часто он видится с дядей? ответь одним словом» ученик пишет `every month` и получает «пока не
   так» — `accept` нет;
2. строка записи `I see my uncle every month.` отличается одним словом от `I see my grandmother every month.` — случай 3
   урока 7, пример карточки `month` и `see`, текст урока 9: ответ вспоминается, а не слышится;
3. вопрос 7 той же работы печатает `I write to my uncle every months.` — при показе обоих вопросов ответ «month» к
   «дяде» стоит на экране.

| Поле | Строка |
|---|---|
| `id` | `q10-na-sluh-kak-chasto-poyot` |
| `zvuk` | `Do you often sing? — I sing every day.` |
| вопрос (факты) | как часто он поёт? запиши слово, которое звучит после every |
| `answer` | `day` |
| `accept` | `every day` |

Прогон: `I sing every day.` — два различия со строками урока 7 (`I study English every day.`, `She cooks every day.`,
`I swim every week.`); `every day`, `every week`, `every month` стоят в уроке 7 разными строками, и по памяти одно слово
не выбрать. `Do you often sing?` — одно различие со строкой модуля 15 `Do you sing?`: это реплика-вопрос, а не ответ,
и знакомый вопрос здесь законен. Голоса: первую реплику по умолчанию читает женский голос, вторую — мужской
(`lib/content/zvuk.ts`, раскладка по умолчанию), поэтому в условии «он». Слово `every` в условии ответа не выдаёт:
после него звучит одно из трёх слов урока 7. Ответ одним словом — A2 Key, с. 29.

**Вопрос 12 — ДЕЙСТВИЕ МЕНЯЕТСЯ.** Нынешний вопрос («Ким написал `We sometimes study at home.` Что это значит?»
с вариантами «всегда», «не занимаются совсем», «не каждый день») — близнец сопоставления урока 3
(`z2-soedinit-stroku-so-smyslom`): те же три смысла (каждый раз, не каждый раз, ни разу), та же рамка «… at home»,
то же действие — перевести наречие в смысл. Вдобавок варианты говорят «они» о строке с `We`.

| Поле | Строка |
|---|---|
| `id` | `q12-pochemu-oshibka-oborot-vremeni` |
| `outcome` | без изменений (итог урока 1; урок 1, случай 6 «Как часто и когда в одной строке») |
| условие (факты) | ученик хотел сказать «Они всегда спят днём» и написал `They always in the afternoon sleep.` Почему это ошибка? |
| варианты, по-русски | 1 — always должно стоять в конце строки · 2 — перед always не хватает формы are · **3 — оборот in the afternoon должен стоять после глагола sleep** (верный) |

Ошибка настоящая: она переносит русский порядок «всегда днём спят». Неверные варианты — тоже настоящие: форма be
перед глаголом признана законным неверным вариантом решением владельца от 8 сентября, а наречие в конце строки — то,
что ученик может вынести из порядка с оборотом времени. Верный вариант не повторяет правила случая 6 дословно
(«оборот о времени стоит в конце строки») — редактору. Прогон: строка и её исправление — два различия со строками
заданий, с материалом не ближе. Верный — на третьем месте, как был.

### 5.2. Места верных ответов выбора

Вопросов выбора после правок два: 9 (верный на месте 2) и 12 (на месте 3). Одно место дважды не стоит.

### 5.3. Доля «произвести» и шапка работы

Производящих вопросов — **10 из 12** (1–8, 10, 11), больше половины. Действий у работы теперь четыре: исправить чужую
запись, переписать данную строку по условию, **ответить на вопрос строкой** (вопрос 2), записать слово со слуха; и
вопросы выбора — назвать причину или смысл. Шапка работы в коде («ТРИ ДЕЙСТВИЯ И ТОЛЬКО ОНИ», «на местах 1, 2, 3»,
«девять из двенадцати») и таблица 16.3 замысла («choice — 3 вопроса», «short без звука — 8») устаревают — сводящему.

### 5.4. Близнецы по действию с заданиями уроков — после правок

| Вопрос | Ближайшее задание урока | Совпадает ли действие |
|---|---|---|
| 1, 4, 5, 7 | исправлять чужую запись уроки не просят | нет |
| 2 | урок 11, задание 4 — устно | нет: вид другой |
| 3 | урок 3, задание 4 — с русского | нет: вход другой |
| 6 | урок 6, задание 4 — с русского | вход другой; принято в 16.3 |
| 8 | урок 8, задание 4 — вопрос с русского | вход другой; принято в 16.3 |
| 9 | урок 9, задание 2 | нет: направление и язык вариантов другие |
| 10 | урок 10, задание 1 | да, и это рамка источника (A2 Key, с. 29); запись и ответ новые |
| 11 | урок 2, задание 5 — с русского | вход другой; принято в 16.3 |
| 12 | называть причину ошибки уроки не просят | нет |

Проверено чтением всех заданий уроков 1–11 в нынешнем файле и предложенного состава урока 11; `bliznetsy` у `short` и
`gap` рамку не видит по устройству.

---

## Сводка для сводящего

| Пункт | Место | Что сделать | Кому ещё |
|---|---|---|---|
| 1.1 | урок 1, `z5-sobrat-my-vsegda-gotovim` | ничего | — |
| 1.2 | урок 8, `z3-vpisat-always-v-vopros` | ничего | — |
| 1.3 | работа, `q8-perepisat-vopros-s-often` | `answer` → `Do your parents often cook fish?`; строка в заглушке условия → `Do your parents cook fish?`; замысел 16.2 | редактор: условие, разбор |
| 1.4 | работа, `q3-perepisat-s-never` | ничего | редактор: разбор не называет `ever` |
| 2 | работа, вопрос 2 | `id` → `q2-otvetit-so-slovom-usually`; `kind: "short"`; `answer` `I usually swim in the evening.`; `accept` — три строки из пункта 2; `options` снять; замысел 16.2, 16.3 | редактор: условие, разбор |
| 3.4.1 | урок 7, вступление | заглушка — факт сцены по 3.4.1; замысел 9.7 | редактор |
| 3.4.2 | урок 7, `tablica-svod-uroka-7` | строки `I read every day.` / `every week.` / `every month.` с переводами, `zvuchat` | редактор: подпись; озвучка |
| 3.4.3 | урок 7, `sluchay-every-day-znakomo` | вторая строка `She goes to school every day. — Она ходит в школу каждый день.`, `zvuchat` | озвучка |
| 3.4.4 | урок 7, `sluchay-every-i-always` | — | редактор: правило по 3.4.4 |
| 3.4.5 | урок 7, `z4-zapisat-vizhus-s-druzyami` | `accept` + `I see friends every week.` | — |
| 3.4.6 | `courses/resheno.ts` | запись «сетка урока 7 повторяет карточки уроков 1, 2, 3, 7» (`kto: "методист"`) | — |
| 3.5.1 | урок 9, `z2-vybrat-chto-kazhdyy-mesyac` | варианты 2 и 3 по таблице | редактор: разбор |
| 3.5.2 | урок 9, комментарий над заданиями | снять строку об обходе строки о плавании | — |
| 3.6.1 | урок 8, `sluchay-vopros-o-neskolkih` | вторая строка `Do your friends often read a newspaper? — Твои друзья часто читают газету?`, `zvuchat` | озвучка |
| 3.6.2 | `courses/resheno.ts` | запись «случай 1 урока 8 напоминает модуль 15, задания стоят на случаях 2–5» | — |
| 3.8 | урок 11 | пять заданий по таблице 3.8; комментарии урока; замысел 9.11, 18.11; `resheno.ts` — урок говорения стоит на уроках 1–8 и своего материала не имеет | редактор; озвучка; ПРОВЕРИТЬ прогоном образец 3 |
| 4 | урок 8, `z5-sobrat-vopros-s-often` | `items` `["often swim", "Do", "at the weekend?", "you"]`, `answer: [1, 3, 0, 2]`, комментарий; замысел 18.8, 18.13 | редактор: условие без места наречия |
| 5.1, в. 1 | работа, `q1-ispravit-always-na-sometimes` | заглушка условия: Нурлан хотел написать «Я иногда читаю газету» | редактор |
| 5.1, в. 9 | работа | `id` → `q9-po-zapisi-kak-chasto-televizor`; запись в условии; варианты «всегда» · «иногда» (верный) · «часто»; замысел 16.2, 16.5 | редактор |
| 5.1, в. 10 | работа | `id` → `q10-na-sluh-kak-chasto-poyot`; `zvuk` `Do you often sing? — I sing every day.`; `answer` `day`; `accept` `every day`; замысел 16.2 | редактор; озвучка; `npm run golosa` |
| 5.1, в. 12 | работа | `id` → `q12-pochemu-oshibka-oborot-vremeni`; строка условия; варианты по таблице, верный третий; замысел 16.2 | редактор |
| 5.3 | работа, шапка в коде; замысел 16.3 | вопросов выбора два (места 2 и 3), производящих 10 из 12, действий четыре | — |
| `kontrol` | `sources` модуля | опоры «Cambridge, Young Learners, с. 52, строка Must for obligation…» и «Cambridge, A2 Key Handbook, с. 52, раздел Interrogatives…» назвать так, как их называют модули, которые `kontrol` сопоставляет с файлами | — |

После внесения: `npm run check:content`, `npm run kontrol -- english-beginner-2 kak-chasto`, `npm run golosa --
english-beginner-2` (вопрос 10 и образец 4 урока 11), `npm run ozvuchka`, и этот же прогон `m16v3/progon.mts modul`
по новому файлу — строки менялись, и соседство надо сверить заново.

## Что проверено и чем

- Все ответы и строки заданий уроков 1–11 и всех двенадцати вопросов работы — прогоном по нынешнему файлу (порог:
  материал модуля 16 — два различия, модулей 1–15 — одно; задания модуля 16 и работы модулей 13–16 — так же).
- Каждая названная здесь новая строка — тем же прогоном, четыре партии (`m16v3/kand1.json` … `kand4.json`), кроме
  одной, названной ниже. Отвергнутые прогоном строки названы в пунктах с причиной.
- Строение всех десяти выборов уроков — расстояниями прогона; уроки 5, 7, 8 — вручную по буквам.
- `npm run check:content` — по строкам о `kak-chasto`: ошибок, кроме незаполненных заглушек, нет; совпавшие заглушки
  уроков 7 и 11 и сетки чужих модулей учтены в 3.4 и 3.8.
- `npm run kontrol -- english-beginner-2 kak-chasto` — ошибок 0, вопросов 3; все три разобраны (разбор вопроса 1 —
  заглушка; две опоры — в сводке; «НИ ОДНОГО БЛОКА» у сетки урока 7 и у урока 11 — 3.4.6 и 3.8).
- Сверка ответа — чтением `lib/content/check.ts`, `normalizeKeepingCase`: снимаются кавычки и знаки в конце строки,
  запятая внутри не снимается.
- Раскладка голосов — чтением `lib/content/zvuk.ts` (строки 253–267) и прогоном `npm run golosa` (итог ниже).
- Как работы соседей спрашивают по чтению — поиском «Прочитай запись» по `courses/english-beginner-2/`: модули 2, 13, 14
  и работа части печатают запись в условии.
- Упоминания меняемых `id` — поиском по `courses/` и `docs/beginner-2/`: только `kak-chasto.ts` и прошлое решение.

## Чего проверить не удалось

- **Работы частей и экзамен** против новых строк не сверялись: прогон обходит работы модулей 13–16.
- **Образец 3 урока 11, вторая строка** `I often dance in the evening.` в прогон не ставилась — ПРОВЕРИТЬ.
- **Наречие частоты в конце строки** (`They walk in the park sometimes.`, `Do you swim often…?`) — по-прежнему без
  опоры и без решения (`reshenie-metodista-urok-1-modul-16.md`, пункт 3). Сборка урока 8 его теперь обходит; `accept`
  всех записей с наречием его не принимает. Нужна выписка в `docs/istochniki-norma-yazyka.md` или ответ владельца
  («ученик написал `They walk in the park sometimes.` — засчитывать ли это как ВЕРНЫЙ ответ?»).
- **Дескрипторы CEFR, с. 63 и 72** в пункте 3.8 взяты из `sources` модуля; PDF я не открывал. `kontrol` эти две опоры
  сопоставил с файлом и вопросов по ним не задал.
- **`bliznetsy`** не запускал: по модулю с заглушками он меряет рыбу, а не текст (правило проекта).
- **`Every week, I see my friends.`** не принимается — решение по уроку 6 о запятой, а не по источнику.

## Дополнение к сводке — найдено прогоном `golosa` после записи таблицы

| Пункт | Место | Что сделать | Кому ещё |
|---|---|---|---|
| 3.7.1 | урок 10, `z2-zapisat-chto-chitaet-inogda` | заглушка условия: «что она читает иногда» | редактор |

Итог прогона `npm run golosa -- english-beginner-2` по модулю 16 — в 3.7.1: запись вопроса 10 и образец урока 8 идут
«жен.» → «муж.», запись 1 урока 10 — «муж.» → «жен.».

---

## Вопросы первого редактора и хвосты круга

Дата: 15 сентября 2026. Файл модуля читан в нынешнем виде (коммит `17e6d7f`, текст редактора). Файлы курса, кроме
`courses/resheno.ts`, и замысел не правлю. Раздел пишется по пункту за раз.

Сделано всё, что было названо при заведении раздела (Р1–Р9, Р13), и вопросы второго редактора (Р10–Р12). Непроверенное —
в конце раздела.

### Р1. Вопрос 6 работы и снятое «смысл от места не меняется» — УТВЕРЖДЕНИЕ НЕ ВОЗВРАЩАЕТСЯ, РАЗБОР ОСТАЁТСЯ КАК НАПИСАН

**Решение.** Разбор вопроса 6 говорит только о форме: `sometimes` перешло в начало, пишется с заглавной, остальные
слова в прежнем порядке. Так и оставить. В урок 6 утверждение не возвращается.

**Почему.**
1. Опоры у утверждения нет, и решение В1 (`reshenie-metodista-uroki-2-6-modul-16.md`, строки 413–424) это уже
   показало: English Grammar Profile даёт оба места наречия графами о форме (ADVERBS / position, MID POSITION и
   FRONT POSITION, A1), а та же таблица держит графу FOCUS / focus, B1, «Can use adverbs in the front position for
   focus». Утверждать, что смысл один, курсу не на что.
2. Итог вопроса — «начинать строку словом sometimes» — это итог о форме. Условие вопроса («Перепиши строку так, чтобы
   наречие частоты стояло в начале») смысла не касается, поэтому разбору и нечего о нём говорить.
3. Урок 6 после В1 переводит каждую строку по её порядку («Я иногда готовлю дома» / «Иногда я готовлю дома»). Если
   разбор работы скажет «смысл тот же», он поспорит с уроком, который ученик прошёл.

**Хвост в замысле, найденный поиском.** `docs/beginner-2/plan-modul-16.md`, строка 932, таблица случаев урока 6:
«Оба места верны | смысл от места не меняется | … (перевод один)». Это прежняя редакция, отменённая В1. Заглушка
вопроса 6, которую увидел редактор, выросла, скорее всего, из неё.

**Кому.** Сводящему: строка 932 замысла — «обе строки верны, место sometimes разное; перевод по порядку слов (В1)».
Редактору: ничего, разбор верен.

**Сверено:** текст вопроса 6 (`kak-chasto.ts`, строки 2794–2803) и случай `sluchay-oba-mesta-verny` (строки
1605–1616) прочитаны в нынешнем виде; поиск «смысл» по замыслу — одно место о вопросе 6, строка 932.

### Р2. Урок 11, задание 3: «когда» или «где» — ОСТАВИТЬ «ГДЕ», КАК НАПИСАЛ РЕДАКТОР

**Решение.** Условие «где у тебя всегда много дел» и образец `I am always busy at school.` остаются. Ошибся мой пункт
3.8: «когда» при образце с `at school` — это место, а не время. Возвращать «когда» и менять образец не надо.

**Почему не «когда» с другой строкой.** Ближайшая строка о времени — `I am always busy in the morning.`, а это пример
карточки `busy` и случай `sluchay-narechie-posle-be` урока 4 (прогон `modul-v4.txt`, вопрос 4 работы: два различия с
этой строкой). Образец, повторяющий пример карточки дословно, ученик находит в словаре (правило «пример словарной
карточки — это „напечатано выше“ при любом расстоянии»). Со словом места такой беды нет.

**Сверка образца прогоном** (`m16v3/kand5.json`, долг из «Чего проверить не удалось»):
- `I am always busy at school.` — слова с карточками; с материалом модуля не ближе двух различий (`I am always busy.` —
  урок 4, случай и таблица, текст урока 9).
- `I often dance in the evening.` — слова с карточками (`dance` — модуль 14). С материалом не ближе двух различий.
  **Одно различие с образцом задания 6 урока 2** `I often read in the evening.` Оставить: образец `speak` звучит после
  ответа ученика, машина его не сверяет, а условия у заданий разные (урок 2 — «что часто», урок 11 — место дел и
  вечер). Утечки нет — это пункт 3.8, «близость здесь не утечка». Долг «ПРОВЕРИТЬ сборщику» из 3.8 закрыт.

**Попутно — подсказка задания 3** («Наречие always стоит между формой be и этим словом») верна после правки
редактора: `am always busy`.

**Кому.** Сводящему: замысел 9.11 и 18.11 — «когда» → «где». Редактору: ничего.

### Р3. Урок 11, задание 4: кто спрашивает — ДРУГ, МУЖСКОЙ ПЕРВЫЙ ГОЛОС

**Решение.** Спрашивает друг. У образца `z4-otvetit-drugu` ставится `pervyyGolos: "muzhskoy"`, условие возвращается
к «Друг спрашивает тебя:».

**Почему не «Знакомая».** Решает то, какой голос у ученика и у друга звучит в соседних местах модуля, а не имя.
1. **Урок 8, задание 6** (`golosa`, 15 сентября): «жен. Do you usually drink milk in the evening? — муж. Yes, I do.»
   По условию там ученик спрашивает, а друг («он») отвечает. Значит в модуле **друг звучит мужским голосом, а реплику
   ученика читает женский**.
2. **Урок 11, задание 4 сейчас:** «жен. Do you often watch television? — муж. No, I don't…». Спрашивающий женский,
   ученик мужской — раскладка обратна уроку 8. Условие «Знакомая» сходится с голосом, но ученик за три урока меняет
   голос.
3. **Мужской первый голос** делает оба места одинаковыми: друг мужской, ученик женский. Условие получает то же слово,
   что урок 8 и вопрос 2 работы («Друг спрашивает тебя»), и вступление урока 11 («Новый знакомый спрашивает о твоём
   дне»).

**Опора устройства.** Поле `pervyyGolos` есть у задания: `lib/content/types.ts`, строка 546, комментарий «ЗАЧЕМ У
ЗАДАНИЯ». Раскладка входит в имя записи образца (`lib/content/zvuk.ts`, `adresObrazca`, параметр `raskladka`).
Записи образцов урока 11 ещё не сделаны: `check:content` называет их среди 125 отсутствующих (журнал редактора,
«СВОДЯЩЕМУ»), поэтому переозвучки старого файла нет — нужна обычная `npm run ozvuchka`.

**Кому.** Сводящему: `pervyyGolos: "muzhskoy"` у `z4-otvetit-drugu`; `npm run golosa -- english-beginner-2
kak-chasto` — ожидается «муж.» → «жен.»; `npm run ozvuchka`. Редактору: условие «Друг спрашивает тебя: …»; остальной
текст задания рода не содержит, разбор не меняется.

### Р4. Вопрос 2 работы: факт «Вечером ты обычно плаваешь» — ФАКТ ПРИНЯТ, НО ОН ОТКРЫЛ ВТОРОЙ ВЕРНЫЙ ПОРЯДОК, И `accept` ПОПОЛНЯЕТСЯ

**Решение по факту.** Факт нужен, и редактор прав: без него «ответь о себе» принимало одну строку при множестве
верных (порода 14; CLAUDE.md, «Условие, определяющее ответ»). `answer` не меняется.

**Что факт сломал.** Русская фраза ставит «вечером» первым. Ученик, переводящий её по порядку, пишет `In the evening I
usually swim.` — и это верный английский.
- **Опора:** English Grammar Profile, FOCUS / focus, **A2**, «FORM/USE: PREPOSITIONAL PHRASES, FRONTING» — «Can use
  prepositional phrases in the front position for focus», пример «In the morning, I go to the beach.» (строка 1958
  CSV, id `1741163715820x505908632783048000`). Графа — разрешение, и её пример ставит оборот времени первым.
- **Курс это уже признаёт:** модуль 14, `ya-ne-delayu.ts`, строки 845–847 — «`In the morning I don't drink tea.` тоже
  правильный английский. Задание наказывало бы за верный ответ».
- `accept` такой строки не принимает: ученик пишет верно и получает «пока не так».

**Решение по `accept`.** Вопрос проверяет слово `usually`, а не место оборота. Порядков три (`I usually…`,
`Usually I…`, `In the evening I usually…`), начал ответа три (без «да», `Yes,`, `Yes, I do.`). Принимаются все
сочетания. Запятая внутри строки при сверке не снимается (`lib/content/check.ts`, `normalizeKeepingCase`), а пример
графы пишет её, поэтому у оборота в начале принимаются оба написания.

| `accept` вопроса `q2-otvetit-so-slovom-usually` |
|---|
| `Yes, I usually swim in the evening.` (есть) |
| `Yes, I do. I usually swim in the evening.` (есть) |
| `Usually I swim in the evening.` (есть) |
| `Yes, usually I swim in the evening.` |
| `Yes, I do. Usually I swim in the evening.` |
| `In the evening I usually swim.` |
| `In the evening, I usually swim.` |
| `Yes, in the evening I usually swim.` |
| `Yes, I do. In the evening I usually swim.` |

Прогон (`kand5.json`): `In the evening I usually swim.` — слова с карточками; дословно нигде не стоит, с материалом
не ближе двух различий.

**Редактору — на усмотрение, не обязательно.** Порядок в факте «Ты обычно плаваешь вечером» совпадает с `answer` и не
подталкивает к порядку, которого урок не разбирает. Выбор фразы — дело редактора, `accept` пополняется при любой
редакции.

**Разбор** не меняется: он называет место `usually` в строке `answer`, о других порядках молчит — `accept` вправе
принимать неразобранное (CLAUDE.md, «`accept` может принимать неразобранное, а разбор — не может»).

**Кому.** Сводящему: шесть строк `accept` по таблице; комментарий вопроса («`Usually I…` accept принимает молча») →
«другие порядки, в том числе оборот в начале, accept принимает молча — EGP FOCUS, A2».

### Р5. Найдено попутно при Р4: вопрос 12 утверждает запрет, которого нет, и три `accept` наказывают за верный ответ

Порода та же, что в Р4, и найдена тем же доводом. Первое место — моя собственная строка из 5.1.

**Р5.1. Вопрос 12, верный вариант «оборот in the afternoon должен стоять после глагола sleep» — ПЕРЕПИСАТЬ БЕЗ «ДОЛЖЕН».**
- Вариант, который ученик обязан выбрать как верный, объявляет законом то, что графа опровергает. `In the afternoon
  they always sleep.` — верный английский: English Grammar Profile, FOCUS / focus, A2, «PREPOSITIONAL PHRASES,
  FRONTING», пример «In the morning, I go to the beach.» (строка 1958 CSV). Модуль 14 признаёт то же
  (`ya-ne-delayu.ts`, 845–847). Слово «должен» — признак выдуманного запрета (CLAUDE.md, «Слово „всегда“ в правиле»).
- **Что верно на самом деле.** Ошибка ученика в том, что оборот встал **между `always` и глаголом**, туда, где «днём»
  стоит в русской фразе. Верный вариант называет это место, а не единственное законное место оборота.
- **Смысл для редактора.** Вариант 3: оборот in the afternoon встал между always и глаголом sleep. Разбор: в верной
  строке `always` стоит прямо перед `sleep` (`They always sleep in the afternoon.`), а ученик разделил их оборотом, как
  в русской фразе. Слов «должен», «только», «всегда стоит» нет ни в варианте, ни в разборе. Все три варианта
  одинакового строя, чтобы верный не выделялся описательной формой среди двух предписаний.
- Неверные варианты 1 и 2 остаются: оба ложны, и оба — настоящие ошибки (5.1). Место верного — третье, как было.
- **Выбор остаётся однозначным.** «always должно стоять в конце строки» — неверно (урок 1, `sluchay-mesto-pered-glagolom`: always стоит перед глаголом). «перед always не
  хватает формы are» — неверно: в строке есть глагол (урок 4, `sluchay-glagol-i-be-ryadom` «Строка с глаголом и строка с формой be»).

**Р5.2. Урок 1, случай 6 `sluchay-narechie-i-oborot` — ПРАВИЛО СКАЗАНО О ЯЗЫКЕ, А НЕ О СВОИХ СТРОКАХ. Редактору, одна
фраза.** «Наречие частоты стоит перед глаголом, а оборот о времени стоит в конце строки» опровергают два места:
урок 6 модуля (`Sometimes I…` — наречие не перед глаголом) и та же графа FOCUS A2. Вопрос 12 вырос из этой фразы.
Смысл правки: в строках ниже наречие стоит перед глаголом, а оборот о времени — в конце; места у них разные, поэтому
в одной строке помещаются и «как часто», и «когда». Урок 1 уже принят, но круг один, а утверждение о языке
закрепляется работой модуля (вопрос 12). Поэтому правка нужна сейчас, до выкладки, а не на общем проходе.

**Р5.3. Три `accept` — ПОПОЛНИТЬ.**

| Место | Верный ответ, который сейчас получает «пока не так» | Опора |
|---|---|---|
| работа, `q11-perepisat-s-usually` | `Usually I read at home.` | та же порода, что `Sometimes I…` в `accept` вопроса 1 и `Usually I…` в вопросе 2; EGP ADVERBS / position, FORM: FRONT POSITION, A1 (`1741163706930x279245648285079580`, пример «Sometimes I go to the beach.»). Прогон: слова с карточками, дословно нигде, два различия с материалом |
| работа, `q4-ispravit-bez-formy-be` | `I'm always busy at the weekend.` | `I'm` курс вводит в модуле 2 (`privetstvie.ts`, строки 1066–1069, `I'm Dana. — Я Дана.`); сверка апостроф не снимает и `I'm` за `I am` не считает (`lib/content/check.ts`, строки 15–17). Прогон: `i'm` без карточки — законно, это стяжение модуля 2 |
| урок 4, `z4-zapisat-oni-inogda-zanyaty` (строки 1277–1278) | `They're sometimes busy at school.` | `They're` курс печатает с модуля 6 (`moya-semya.ts`) и в модулях 8, 9, 10. Урок 4 принят, но `accept` не видимый текст — вносит сводящий |

**Р5.4. Запятая после наречия или оборота в начале строки — ВОПРОС ВЛАДЕЛЬЦУ, решать не мне.** Касается семи мест:
`accept` уроков 1 (строка 463), 2 (795), 4 (1278), 7 (2011), работы 1 (2715); `answer` урока 6 (1746) и работы 6
(2799). Сверка запятую внутри строки не снимает, поэтому `Sometimes, I walk to school.` не засчитывается. Урок 6 держит
случай «Запятая после sometimes не нужна», то есть учит строке без запятой, но ошибкой запятую не называет. Графы о
запятой после наречия в English Grammar Profile нет: пример A1 FRONT POSITION пишет без запятой, пример A2 FOCUS с
предложным оборотом — с запятой. Моё решение 3.4.5 («запятую в `accept` не ставлю») было выбором преподавателя, а не
выводом из источника. Поэтому спрашивать нужно владельца. Вопрос в том виде, в каком его надо задать:

> Ученик пишет ответ `Sometimes, I walk to school.` — с запятой после `sometimes`. Верный ответ задания —
> `Sometimes I walk to school.` без запятой. Урок 6 модуля 16 говорит «Запятая после sometimes не нужна». Засчитывать
> ли строку с запятой как ВЕРНЫЙ ответ?

Исключение уже в Р4: `In the evening, I usually swim.` там принимается, потому что пример графы, дающей этот порядок,
пишет запятую, а урока о таком порядке в курсе нет.

**Кому.** Сводящему: три строки `accept` по Р5.3; вопрос Р5.4 — в список вопросов владельцу. Редактору: вопрос 12,
вариант 3 и разбор (Р5.1); урок 1, правило случая 6 (Р5.2).

### Р6. Близнец внутри работы: вопросы 4 и 5 (75%) — ДЕЙСТВИЕ ЗАКОННО, ОБЩИЙ ХВОСТ СНИМАЕТСЯ, ВОПРОС 5 ПОЛУЧАЕТ `at home`

**Что ученик делает руками** (мерка проекта для близнецов, которых машина не отличает):
- вопросы 1, 4, 5, 7 — находит ошибку в чужой строке и пишет строку заново;
- но **ошибка у каждого своя и из своего урока**: не то наречие по смыслу (урок 1), нет формы be (урок 4), нет `-s`
  при одном человеке (урок 5), лишнее `-s` после `every` (урок 7).
Рамка «Ученик написал: … Запиши строку без ошибки» ответа не выдаёт: чтобы исправить строку 5, знание строки 4 не
помогает ничем. Порода 13 (работа проверяет память) здесь не срабатывает — вспоминать нечего. Одно действие на четыре
итога — устройство работы по 5.3 (четыре действия на двенадцать вопросов, каждое повторяется по необходимости), а не
близнец. Менять вид одному из четырёх ради отчёта значит ломать то, что работает: `gap`, `order`, `hottext` у работы
сняты нарочно (их берут уроки), а третий `choice` роняет долю производящих вопросов и требует нового места верного.

**Откуда 75%, счётом, а не на глаз.** Функция `shozhest` (`scripts/bliznetsy.mts`, строки 71–98) повторена в `node
-e`, условия взяты из файла дословно:

| Пара | Доля |
|---|---|
| 4 ↔ 5, как сейчас (`at the weekend` у обоих) | 0,75 — отчёт называет |
| 4 ↔ 7, 1 ↔ 4 (та же рамка, хвосты разные) | 0,58 — отчёт молчит |
| 4 ↔ 5 с хвостом `at home` / `in the afternoon` / `in the evening` | 0,58 |
| 5 (`at home`) ↔ 7 / ↔ 1 | 0,46 / 0,38 |

Значит одна рамка даёт 58%, а через порог пару переводит только **общий хвост `at the weekend`**. Два соседних
вопроса с одной и той же концовкой ученик читает как один и тот же вопрос — это стоит снять независимо от отчёта.

**Почему не запись в `resheno.ts`.** `bliznetsy` гасит по имени вопроса (`resheno.some((r) => q.id === r.chto)`,
строки 279, 397, 607). Запись с `q4-ispravit-bez-formy-be` или `q5-ispravit-okonchanie` заглушила бы этот вопрос во
**всех** сверках, включая сверку с заданиями уроков и с работами соседних модулей. Будущий настоящий близнец остался
бы невидим. Слишком широко для случая, который снимается одним словом.

**Решение.** Вопрос 5: строка условия `He often work at home.`, `answer` `He often works at home.` Рамка, итог, `id` и
разбор по смыслу не меняются, в разборе меняется только сама строка.

**Почему `at home`, а не другой хвост** (прогон `kand5.json`):
- `He often works at home.` — слова с карточками; с материалом модуля 16 не ближе двух различий, ближайшая строка —
  неверный вариант выбора урока 2 `Aigerim often cooks at home.` (два различия, другой глагол и другое лицо). В
  материале уроков 1–6 — ни одной близкой строки.
- `in the evening` отвергнут: два различия сразу с восемью строками, в том числе с образцами урока 11 и уроком 5,
  случай `He always works in the morning.` — рядом слишком много похожего.
- `in the afternoon` годен, но этот оборот уже стоит в вопросах 9 и 12 той же работы.
- С вопросом 11 (`I read at home.`, тоже `short`) — доля 0,31; с вопросами 6 и 3 — 0,15 и 0,23 (тот же `node -e`).

**Кому.** Сводящему: строка условия и `answer` вопроса 5; после внесения `npm run bliznetsy -- english-beginner-2` —
пара 4/5 должна уйти. Редактору: разбор вопроса 5 начинается строкой `He often works at home.`, остальной текст
разбора верен и так.

### Р7. Записи в `courses/resheno.ts` — НИ ОДНОЙ НЕ ВНОШУ: две гасили бы вопрос, которого уже нет, третья не гасит ничего

Поручение было внести три записи. Перед записью я прогнал `kontrol` по нынешнему файлу (15 сентября) и прочитал, как
отчёт применяет список (`scripts/otchyot.mts`, `skazat`, строки 32–40). Итог по каждой:

| Запись из сводки 14 сентября | Что говорит `kontrol` сейчас | Решение |
|---|---|---|
| сетка урока 7 повторяет карточки уроков 1, 2, 3, 7 (3.4.6) | «НИ ОДНОГО БЛОКА» у сетки больше нет: `z2-najti-slova-v-setke → sluchay-month (1), tablica-slovo-posle-every-odno (1)` | не вносить: вопроса нет |
| случай 1 урока 8 «без задания» (3.6.2) | строки «без задания» у урока 8 нет — ни у одного урока модуля её нет | не вносить: вопроса нет |
| урок 11 стоит на уроках 1–8 без своего материала (3.8) | «НИ ОДНОГО БЛОКА» у пяти заданий — но это **СВЕДЕНИЯ** | не вносить: см. ниже |

**Почему не вносить третью.**
1. **Запись ничего не заглушит.** `skazat`: «Сведения не гасим — они ничего не требуют» —
   `if (uroven !== "СВЕДЕНИЯ" && resheno.some(...))`. Таблица «задание → блок» целиком стоит одним сообщением уровня
   СВЕДЕНИЯ. Запись осталась бы мёртвой — ровно то, против чего написана шапка списка («свалка»).
2. **Хуже того, она заглушила бы чужое.** Сверка идёт по вхождению строки. Запись с `chto`, где стоит имя урока или
   задания, погасила бы любой будущий ВОПРОС, в тексте которого встретится то же имя, — например, о цитате или о слове
   урока 11.
3. **Это не долг, а устройство вида урока.** `npm run kontrol -- english-beginner-2` по всему курсу: уроки говорения
   модулей 12 и 13 (`govorim-o-svoey-komnate`, `govorim-o-svoyom-dne`) дают по шесть заданий «НИ ОДНОГО БЛОКА» —
   тот же строй: одни задания с образцами, опора — правила модуля. Записей о них в `resheno.ts` нет (поиск `govorim-`
   по файлу — ноль). Урок 11 модуля 16 стоит в том же строю.

**Что осталось верным из 3.8.** Урок 11 опирается на уроки 1–8 и на дескрипторы CEFR, с. 63 и 72, названные в
`sources`. Это записано в комментарии урока (строки 2604–2609), и следующему проверяющему есть с чем сверить.

**Кому.** Сводящему: строки 3.4.6, 3.6.2 и `resheno.ts` в пункте 3.8 сводки от 14 сентября — снять как исполненные
самим прогоном. Файл `courses/resheno.ts` не менялся.

### Р8. Две опоры в `sources`, которых `kontrol` не сопоставил, — ТОЧНЫЕ СТРОКИ `ref`

`kontrol` находит файл источника по началу или слову в `ref` (`scripts/kontrol.mts`, список `FAILY`, строки 82–127).
Обе опоры модуля начинаются словами «Cambridge, …», а такого образца в списке нет. Под `A2 Key` стоят
`^Cambridge English, руководство A2 Key` и `^Cambridge English, A2 Key`, под Young Learners — `Pre A1 Starters|A1
Movers|A2 Flyers|младших ступеней`. Соседние модули зовут их именно так (`Cambridge English, руководство для младших
ступеней (Young Learners), с. 29`; `Cambridge English, руководство A2 Key — с. 51`).

| Строка в `kak-chasto.ts` | Было | Стало |
|---|---|---|
| 155 | `Cambridge, Young Learners, с. 52, строка Must for obligation — опора слова homework` | `Cambridge English, руководство для младших ступеней (Young Learners), с. 52, список A1 Movers, строка Must for obligation — опора слова homework` |
| 169 | `Cambridge, A2 Key Handbook, с. 52, раздел Interrogatives — почему How often не берётся` | `Cambridge English, руководство A2 Key, с. 52, раздел Interrogatives — почему How often не берётся` |

Поле `section` у обеих не меняется.

**Проверено до внесения, тремя шагами — самого `kontrol` с новыми строками я не прогонял: файл модуля правит сводящий.**
1. **Образцы `FAILY` на новых строках** — `node -e` с двумя регулярными выражениями из списка: строка 155 → файл
   Young Learners, строка 169 → руководство A2 Key.
2. **Цитаты, которые `kontrol` вынет из `section`** (`citaty`, строки 245–259: текст в «ёлочках», не меньше четырёх
   слов, без кириллицы, без «нашего» рядом — образец `NASHE`, строка 234):
   - «He must do his homework.» — `npm run pdf -- materials/cambridge-young-learners-pre-a1-a2.pdf --find "must do his
     homework"`: с. 31 и **с. 52**; в разборе `materials/.text/…txt` строка 1365, под заголовком «The list below details
     what is new at A1 Movers level» (строка 1346). Страница и список в `ref` верны;
   - «How; How much; How many; How often; How long; etc.» — `npm run pdf -- materials/cambridge-a2-key-handbook.pdf
     --find "How often"`: **с. 52** («Who; Whose; Which How; How much; How many; How often; How long; etc. Why»); в
     `.text` строка 3648, под заголовком Interrogatives (строка 3644). Страница верна.
   - Слова рядом с обеими цитатами образец `NASHE` («наш», «в уроке», «покрывает»…) не задевают: цитаты будут искаться,
     а не пропускаться.
3. **Утверждение самой опоры 169** («строки `how often` нет во всём файле English Grammar Profile») я не перепроверял
   этим кругом — оно из прошлого круга и к имени опоры не относится.

**Кому.** Сводящему: две строки `ref` по таблице, затем `npm run kontrol -- english-beginner-2 kak-chasto`. Ожидается
«Ошибок: 0, вопросов: 0». Если вопрос останется — строку не искать по памяти, а сверить имя с `FAILY` ещё раз.

### Р9. Прогон модуля заново по нынешнему файлу (`17e6d7f`)

**Как.** Скрипт `m16v3/progon.mts` без изменений, режим `modul` → `m16v3/modul-v4.txt` (537 строк; 14 сентября было
493). Кандидаты этого раздела — `m16v3/kand5.json`, 19 строк. Пороги те же, что в «Как проверялось».

**Слова.** Без карточки три места, все известные: `English` (урок 7, случаи 1 и 2 — имя собственное, 3.1), `ever`
(`accept` вопроса 3 — законно, 1.4) и `I'm` в предложенном `accept` вопроса 4 (стяжение модуля 2, Р5.3). Карточек
«позже места» — ноль.

**Строки с одним различием или совпавшие — каждая названа, решение по каждой.**

| Место | Близкая строка | Решение |
|---|---|---|
| урок 8, `z3-vpisat-always-v-vopros` | урок 1, `They always study at school.` | оставлено в 1.2 |
| урок 8, `z4-sprosit-obychno-li-rano` | урок 9, отметка `Do you get up early?` | законно: строка урока 9 стоит **позже**, ученик её ещё не видел |
| урок 9, `z4-vpisat-usually-o-ney` | текст записки `I usually study at home in the evening.` | законно: урок чтения, ответ из текста (замысел 18.9); ученик меняет лицо, `study` → `studies` |
| работа 1, `answer` и `accept` | урок 10, запись `I sometimes read a magazine.`; урок 6, `Sometimes I read a magazine.` | оставить. Все слова ответа, кроме наречия, даны в условии (`I always read a newspaper.`), а наречие задаёт русское «иногда». Вспомненная строка даёт то же `sometimes`, что условие, — второй дороги к ответу нет (довод 1.2) |
| работа 3 | модуль 14, `I don't eat meat.` | оставлено в 1.4 |
| работа 9, строка записи `Sometimes I watch television in the afternoon.` | урок 11, образец 4 `I sometimes watch television in the evening.` | оставить. Опасность — ответить «иногда» по памяти о телевизоре, не читая записи. Но память о телевизоре в модуле раздвоена: `never` — сборка урока 3, `often` — вопрос образца урока 11, `sometimes` — ответ задания 4 урока 6 и тот же образец. Одного слова она не подсказывает, а запись напечатана в условии |
| работа 10, `accept` `every day` | урок 7, ячейка таблицы `every day` | законно: таблица держит все три оборота, выбор делает слух (5.1) |
| работа 10, запись | модуль 15, `Do you sing?`; урок 7, свод `I read every day.` | оставлено в 5.1: реплика-вопрос, а ответ `day` из свода не выводится — там три строки |
| работа 11, данная строка `I read at home.` | модуль 13, `accept` `I read at night.` | законно: это данная строка, а не ответ (довод 1.4) |

**Новые строки работы — вопросы 2, 8, 9, 10, 12.**
- 2: ответ и строка друга — не ближе двух различий; новые `accept` из Р4 — дословно нигде.
- 8: `Do your parents often cook fish?` и `Do your parents cook fish?` — ни одной близкой строки.
- 9: строки записи — одна близкая, разобрана в таблице.
- 10: разобрано в таблице.
- 12: `They always in the afternoon sleep.` и `They always sleep in the afternoon.` — не ближе двух различий.

**Урок 11 — образцы и сборка.** Разобрано в Р2 (образец 3). Образцы 1 и 2 дают одно различие с образцами уроков 1
и 3 (`I always drink water / juice in the morning.`) и с уроком 7 (`I see my grandmother every month.`). Решение 3.8:
образец `speak` звучит после ответа, машина его не сверяет, утечки нет. Сборка `I usually drink juice in the
afternoon.` — не ближе двух различий.

**Ответы уроков 7–11 против материала — новых правок нет.** Всё, что прогон назвал в уроках 7–11, — в таблице или в
пунктах 1.2, 3.4.5, 3.5, Р2.

**Чего прогон не видел.** Работы частей и экзамен (граница прежняя). Ответы `choice` по-русски: слов для сверки в
них нет, вопросы 9 и 12 разобраны чтением.

---

## Вопросы второго редактора (`priyomka-urokov-7-11-modul-16.md`), которые решает методист

### Р10. Л7-О1 и Л9: урок 7 не говорит, что `every month` не наречие частоты, а урок 9 за это наказывает — СТРОКА В УРОК 7 НУЖНА, НО НЕ ТА, ЧТО ПРЕДЛОЖЕНА

**Находка верна.** Урок 2 дал проверку «наречие частоты отвечает на вопрос «как часто?»». `every month` проходит её, а
задание 1 урока 9 (`z1-otmetit-narechiya-v-zapiske`) держит `every` и `month` отмечаемыми без `correct` (строки
2403–2404). Правило ученик впервые узнаёт из разбора — задание требует того, чего урок не давал.

**Предложенную строку «Оборот с every состоит из двух слов и стоит в конце строки, а наречие частоты — одно слово
перед глаголом» не брать: две из трёх её частей опровергает свой же модуль.**
- «наречие частоты — одно слово **перед глаголом**»: урок 4 ставит его после формы be (`I am always busy`), урок 6 —
  в начало строки (`Sometimes I cook at home`). Подсказка задания 1 урока 9 сама говорит «перед глаголом или сразу
  после формы be. Одна строка начинается таким словом».
- «оборот с every **стоит в конце строки**»: урок 7 сам принимает `Every week I see my friends.` (`accept` задания 4,
  строка 2011). English Grammar Profile даёт такой порядок на A1: PRESENT / present simple, FORM: AFFIRMATIVE, пример
  «Every day at college I learn new words and new things.» (строка 4651 CSV); на A2 — «Every day I can see the sea
  when I wake up.» (строка 5822, пример графы VERBS / phrasal, A2, FORM: NO OBJECT). Порода та же, что в Р5.2.
- Верна одна часть: **оборот из двух слов**, а наречие частоты — одно слово.

**Опора — часть речи, а не место.**
- Oxford 3000: `always adv. A1`, `usually adv. A1` — но `every det. A1`, `week n. A1`, `month n. A1`
  (`materials/.text/oxford-3000.txt`, строки 101, 2886, 942, 2952, 1703).
- Словник A2 Key: `every (det)` (`materials/.text/cambridge-vocab-a2-key.txt`, строка 755).
- English Grammar Profile кладёт `every` в DETERMINERS / quantity («quantifying determiners with singular nouns ('a',
  'every')») — графа уже названа в `sources` модуля, строки 162–166. Там же, где источник называет наречия частоты,
  стоят одиночные слова: ADVERBS / types and meanings, USE: FREQUENCY, A1 «('always', 'sometimes')», A2 «('usually',
  'often', 'never', 'weekly')» (строки 329, 348). Одно слово `weekly` источник наречием частоты считает, оборот
  `every week` — нет.
- Русская параллель держится без термина: «часто» — одно слово, «каждую неделю» — два.

**Решение.** Строка идёт в **случай 6 `sluchay-every-i-always`** («Чем every week отличается от always»), а не в
случай 4. Случай 6 — единственное место урока, где оборот и наречие стоят рядом, а случай 4 говорит о месте, и
утверждение о месте туда потянется само.

Смысл правила для редактора, два предложения:
1. `every week` тоже отвечает на вопрос «как часто?», но это не наречие частоты: в нём два слова — `every`, то есть
   «каждый», и слово о времени.
2. `always` не говорит, сколько времени проходит между разами, а `every week` говорит: неделя.

Слов «в конце строки» и «перед глаголом» в правиле нет.

**Там же — Л7-У2 (пример спорит с правилом).** `I always work in the morning.` ученик читает как «каждое утро», то
есть срок назван оборотом. Первый пример случая 6 заменить на `I always work at home. — Я всегда работаю дома.`
Прогон `kand6.json`: слова с карточками; одно различие только с парой сопоставления урока 3 `I always cook at home.`
(это строка материала, не ответ); с `I work every week.` строка делит глагол, и пара различается ровно одним —
наречием против оборота.

**Урок 9, задание 1.** `every` и `month` остаются отмечаемыми: различить оборот и наречие — настоящая проверка урока
чтения. Разбор ссылается на урок 7 полным именем («как в уроке «Каждую неделю: every week, every month»»), без «срок»
и «не считают» — формулировка за редактором. Подсказка урока 2 не меняется: в уроке 2 оборотов с every ещё нет, и
для его строк проверка верна. Урок 7 теперь её уточняет прямо.

**Попутно, того же рода — урок 7 говорит о месте оборота как о законе языка.** Имя и правило случая 4 («Оборот с every
стоит в конце строки») и подпись свода `tablica-svod-uroka-7` утверждают то, что опровергает `accept` задания 4 и
графа выше. Редактору: сказать это как указание, а не как факт языка («ставим в конец строки»). Подсказки заданий 4
и 6 («поставь в конец») уже так и сказаны. Разборы, описывающие место оборота в своей строке, верны как есть.

**Кому.** Редактору: правило случая 6 по смыслу выше; разбор задания 1 урока 9; имя, правило случая 4 и подпись
свода — «ставим». Сводящему: первая строка случая 6 → `I always work at home. — Я всегда работаю дома.` с
`zvuchat`; `npm run ozvuchka`.

### Р11. Р-У6: «точная частота» в итоге урока 7 — ИТОГ МЕНЯЕТСЯ; и того же рода, но хуже, — «средняя частота» в итоге урока 2

**Р11.1. «Точная» — не шкала, но обещает больше, чем даёт урок.**
- Шкалой, которую сняли 13 сентября (порядок «always — usually — often — sometimes — never»), «точная» не является:
  она не ставит наречия по порядку. У источника это другая ось — English Grammar Profile зовёт `always`, `usually`
  «adverbs of **indefinite** frequency» (PRESENT / present simple, A2, строка 4671 CSV; present continuous, A2,
  строка 4598). Довод первого редактора «точная против неточной — та же шкала» неверен.
- **Но итог всё равно меняется.** `every week` называет, сколько времени проходит между разами, и не называет,
  сколько раз: «точной частотой» скорее было бы «два раза в неделю», которого в модуле нет. Слова «точн» урок 7 не
  произносит ни разу (поиск по видимому тексту — только комментарий шапки, строка 5). Урок говорит своими словами:
  «дело повторяется каждый месяц» (случай 3), «между ними проходит неделя» (случай 6).

**Новая строка итога урока 7:** `называть оборотом every и словом времени, как часто повторяется дело`

Вопрос 7 (исправить `every months`) эту строку спрашивает: слово времени после `every` и есть его предмет.

**Р11.2. «Средняя частота» в итоге урока 2 — ЭТО И ЕСТЬ СНЯТАЯ ШКАЛА.** Не называлось ни одним разбором. «Средняя»
значит «посередине между всегда и никогда» — ровно порядок, которого, по решению владельца от 13 сентября, у курса
нет («Порядок наречий частоты на шкале — утверждение без опоры, и шкалы нет», CLAUDE.md). Итог стоит на экране урока
2 и в списке «Чему научишься в модуле».

**Новая строка итога урока 2** — по образцу итога урока 1 («говорить, что бывает всегда и что иногда: I always
work») и с английской строкой из имени урока 2: `говорить, что бывает обычно и что часто: I usually get up early`

**Где менять — строки сверяются точным совпадением.**

| Итог | `outcomes` модуля | `outcome` урока | `outcome` вопроса работы |
|---|---|---|---|
| урок 2 | строка 180 | строка 530 | строка 2728 (вопрос 2; после Р12 — вопрос, стоящий вторым) |
| урок 7 | строка 185 | строка 1797 | строка 2810 (вопрос 7) |

Поиск по курсу: обе прежние строки есть ещё только в `courses/english-starter/kak-chasto.ts` — это замороженный
курс, его не трогаем, а сверка идёт внутри одного курса. В замысле (`plan-modul-16.md`) обе строки меняются тоже.

**Кому.** Сводящему: шесть мест по таблице и замысел; затем `npm run check:content` (итог без вопроса — ошибка,
значит расхождение сразу будет видно). Редактору: ничего — итоги пишет методист, а слог обеих строк сверен с итогом
урока 1.

### Р12. Р-У1: вопросы 3, 8 и 11 делают одно действие — ДЕЙСТВИЕ ЗАКОННО; НО ВОПРОСЫ 2 И 11 СТОЯТ НЕ У СВОИХ ИТОГОВ И МЕНЯЮТСЯ МЕСТАМИ

**Что ученик делает руками.** Во всех трёх ему дана строка, и он пишет её заново со словом из условия. Мерка та же, что
в Р6: помогает ли знание одного ответа в другом. Нет — каждый вопрос держит своё правило:
- 3 — `never` вместо `don't`, второе «не» уходит (урок 3). Это замена с удалением, а не вставка;
- 8 — место наречия в вопросе: после `your parents`, а не после `Do` (урок 8). Ловушка `Do often your parents…`
  есть только здесь;
- 11 — место `usually` в утвердительной строке после `I`.
По 5.3 у работы четыре действия на двенадцать вопросов. «Переписать данную строку» стоит четыре раза (3, 6, 8, 11),
«исправить чужую» — четыре (1, 4, 5, 7). Поровну и по необходимости — это не близнец. Сменить вид одному значило бы
взять `gap` или `order` у уроков либо третий `choice`: Р6, тот же довод. Рамки условий 3 и 8 — дело слога (Р-У1,
предложение второго редактора), первому редактору.

**Что при этом нашлось — порода «задание не проверяет свой итог».** Разбирая, какое правило держит каждый вопрос, я
сверил их с итогами:
- **вопрос 11** помечен итогом урока 11 «строить о себе строку с наречием частоты», а делает дело урока 2: ставит
  данное `usually` в данную строку. Выбора наречия, ради которого урок 11 и построен (3.8), в нём нет;
- **вопрос 2** помечен итогом урока 2, а делает дело уроков 8 и 11: отвечает другу на вопрос строкой о себе (урок 8,
  случай 4; урок 11, задание 4).
Вопросы стоят каждый у чужого итога. Порядок работы до сих пор держал «вопрос N — урок N» (шапка работы, вопросы
1–11).

**Решение — поменять вопросы местами, а не только пометки.**

| Место | Было | Стало |
|---|---|---|
| второй вопрос | `q2-otvetit-so-slovom-usually` («Друг спрашивает…», итог урока 2) | `id` `q2-perepisat-s-usually` — нынешний вопрос 11 целиком (условие, `answer`, `accept` с Р5.3, разбор); `outcome` — новая строка итога урока 2 из Р11.2 |
| одиннадцатый вопрос | `q11-perepisat-s-usually` («Добавь «обычно»…», итог урока 11) | `id` `q11-otvetit-drugu-so-slovom-usually` — нынешний вопрос 2 целиком (условие редактора, `answer`, `accept` из Р4, разбор); `outcome` — «строить о себе строку с наречием частоты» |

Показ случайный (`ask: 10`), поэтому место в файле ученику не видно. Оно держит порядок, по которому сверяют работу, и
шапки «ВОПРОС N. Итог урока N».

**Что не меняется.** Доля производящих — 10 из 12. Места верных у выбора — 2 и 3 (вопросы 9 и 12 не трогаются).
Покрытие итогов — все одиннадцать. Близнецов с заданиями уроков новых нет: действия прежние, сверены в 5.4.

**`id` правятся поиском.** Вне файла модуля они стоят только в документах (`priyomka-urokov-7-11-modul-16.md`, этот
файл); в `resheno.ts` и скриптах — ноль.

**Кому.** Сводящему: перестановка по таблице и шапки вопросов («ВОПРОС 2. Итог урока 2. Переписать данную строку по
условию» и «ВОПРОС 11. Итог урока 11. Ответить на вопрос строкой о себе…»), шапка работы (строки 2687–2692 называют
«вопрос 2» у действия «ответить на вопрос»), замысел 16.2–16.3. Редактору: ничего нового — тексты едут целиком;
слог рамок 3 и 8 — по Р-У1.

### Р13. Прогоны после решений этого раздела (15 сентября, файл модуля `17e6d7f`, `resheno.ts` не менялся)

- `npm run kontrol -- english-beginner-2 kak-chasto` — **Ошибок: 0, вопросов: 2**. Оба вопроса — имена опор (Р8);
  сведения — таблица «задание → блок», «НИ ОДНОГО БЛОКА» только у урока 11 (Р7). После внесения Р8 ожидается «вопросов:
  0».
- `npm run check:content` — одна ошибка на курс: «нет записей (125 шт.)», её снимает `npm run ozvuchka`. Других строк
  о `kak-chasto` в отчёте нет (счёт `grep -c kak-chasto` по всему выводу — 0).
- `npm run bliznetsy -- english-beginner-2 --hudshie 60` — одна пара модуля 16, вопросы 4/5 (Р6). После внесения Р6
  ожидается ноль.
- Прогон `m16v3/progon.mts` — Р9.

---

## Сводка для сводящего — вопросы редакторов и хвосты круга

| Пункт | Место | Что сделать | Кому ещё |
|---|---|---|---|
| Р1 | `plan-modul-16.md`, строка 932 | «смысл от места не меняется … (перевод один)» → «обе строки верны, место sometimes разное; перевод по порядку слов (В1)» | — |
| Р2 | замысел 9.11, 18.11 | урок 11, задание 3: «когда» → «где» | — |
| Р3 | урок 11, `z4-otvetit-drugu` | `pervyyGolos: "muzhskoy"`; `npm run golosa` (ожидается «муж.» → «жен.»); `npm run ozvuchka` | редактор: условие «Друг спрашивает тебя: …» |
| Р4 | работа, вопрос «Друг спрашивает…» (после Р12 — одиннадцатый) | `accept` + 6 строк: `Yes, usually I swim in the evening.` · `Yes, I do. Usually I swim in the evening.` · `In the evening I usually swim.` · `In the evening, I usually swim.` · `Yes, in the evening I usually swim.` · `Yes, I do. In the evening I usually swim.`; комментарий вопроса | редактор, на усмотрение: порядок слов в факте |
| Р5.1 | работа, `q12-pochemu-oshibka-oborot-vremeni` | — | редактор: вариант 3 и разбор без «должен» (оборот встал между always и sleep), три варианта одного строя |
| Р5.2 | урок 1, `sluchay-narechie-i-oborot` | — | редактор: правило о своих строках, а не о языке |
| Р5.3 | работа, вопрос «Добавь «обычно»…» (после Р12 — второй) | `accept` + `Usually I read at home.` | — |
| Р5.3 | работа, `q4-ispravit-bez-formy-be` | `accept` + `I'm always busy at the weekend.` | — |
| Р5.3 | урок 4, `z4-zapisat-oni-inogda-zanyaty` | `accept` + `They're sometimes busy at school.` | — |
| Р5.4 | список вопросов владельцу | вопрос о запятой после `sometimes` в начале строки — дословно из Р5.4 | — |
| Р6 | работа, `q5-ispravit-okonchanie` | строка условия `He often work at home.`, `answer` `He often works at home.`; `npm run bliznetsy` | редактор: первая строка разбора |
| Р7 | `courses/resheno.ts` | ничего; строки 3.4.6, 3.6.2 и `resheno.ts` в 3.8 прежней сводки снять как исполненные прогоном | — |
| Р8 | `sources`, строки 155 и 169 | `ref` по таблице Р8; `npm run kontrol -- english-beginner-2 kak-chasto` → «вопросов: 0» | — |
| Р10 | урок 7, `sluchay-every-i-always` | первая строка → `I always work at home. — Я всегда работаю дома.`, `zvuchat`; `npm run ozvuchka` | редактор: правило случая 6 (every week — два слова, не наречие частоты; always срока не называет) |
| Р10 | урок 9, `z1-otmetit-narechiya-v-zapiske` | ничего: `every`, `month` остаются отмечаемыми | редактор: разбор со ссылкой на урок 7 по имени |
| Р10 | урок 7, случай 4 и `tablica-svod-uroka-7` | — | редактор: «ставим в конец строки» вместо «стоит в конце строки» |
| Р11.1 | итог урока 7: строки 185, 1797, 2810; замысел | → `называть оборотом every и словом времени, как часто повторяется дело` | — |
| Р11.2 | итог урока 2: строки 180, 530, 2728; замысел | → `говорить, что бывает обычно и что часто: I usually get up early` | — |
| Р12 | работа, вопросы 2 и 11 | поменять местами целиком: второй — `q2-perepisat-s-usually` (нынешний 11) с итогом урока 2; одиннадцатый — `q11-otvetit-drugu-so-slovom-usually` (нынешний 2) с итогом урока 11; шапки вопросов и шапка работы; замысел 16.2–16.3 | редактор: рамки условий 3 и 8 (Р-У1) |

**Порядок внесения**, чтобы точные совпадения не разошлись посередине: Р11 и Р12 одним заходом (итоги и
перестановка вопросов трогают одни и те же поля `outcome`), затем `accept` (Р4, Р5.3), затем прочее. После всего:
`npm run check:content`, `npm run kontrol -- english-beginner-2 kak-chasto`, `npm run bliznetsy -- english-beginner-2`,
`npm run golosa -- english-beginner-2 kak-chasto`, `npm run ozvuchka`, `npm run sosedi`.

## Что проверено в этом разделе и чем

- Вопросы первого редактора (4) и второго (3) — чтением текста в `kak-chasto.ts` (`17e6d7f`) и журналов обоих
  редакторов целиком по своим разделам.
- Прогон модуля и 22 строк-кандидатов (`kand5.json`, `kand6.json`) — `m16v3/progon.mts`.
- Доля близнецов — функция `shozhest` из `scripts/bliznetsy.mts`, повторённая в `node -e`, условия из файла дословно.
- Гашение `resheno.ts` — чтением `scripts/otchyot.mts` (`skazat`) и `scripts/bliznetsy.mts` (строки 279, 397, 607).
- Имена опор — регулярные выражения `FAILY` прогоном на новых строках; цитаты — `npm run pdf --find` по обоим PDF и
  разбор `.text` со строкой заголовка списка.
- Опоры о языке: English Grammar Profile (строки CSV 329, 348, 528, 546, 1944, 1958, 4598, 4651, 4671, 5822),
  Oxford 3000 (строки 101, 942, 1703, 2886, 2952), словник A2 Key (строка 755).
- Голоса — `npm run golosa -- english-beginner-2 kak-chasto`; поле `pervyyGolos` у задания — `lib/content/types.ts`.
- Стяжения при сверке — `lib/content/check.ts`, строки 15–40; где курс вводит `I'm` и `They're` — поиском по курсу.

## Чего проверить не удалось

- **`kontrol` с новыми именами опор** не прогонял: файл модуля правит сводящий. Проверены по отдельности образцы
  `FAILY` и цитаты (Р8).
- **Утверждение опоры 169** о том, что строки `how often` нет во всём English Grammar Profile, этим кругом не
  перепроверял.
- **Работы частей и экзамен** против новых строк (`He often works at home.`, новые `accept`) не сверялись: прогон их
  не обходит.
- **Запятая после наречия и оборота в начале строки** — без опоры и без решения (Р5.4), до ответа владельца.
- **Наречие частоты в конце строки** (`They walk in the park sometimes.`) — прежний долг
  (`reshenie-metodista-urok-1-modul-16.md`, пункт 3) не решён и в новые `accept` не взят.
- **Правило случая 6 урока 1 и имя случая 4 урока 7** даны смыслом, без готовых строк: видимый текст пишет редактор.
  После его правки их нужно перечитать против Р5.2 и Р10.
