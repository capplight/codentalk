# Модуль 3 нового Beginner: «Откуда ты и где живёшь». Замысел

6 сентября 2026. Пишет методист. **Весь видимый русский текст — вступления,
объяснения, врезки, подписи, условия и разборы заданий — пишет редактор.**
Замысел говорит, ЧТО стоит на экране и на что оно опирается.

Строка принятой программы
([programma-stupeni.md](programma-stupeni.md), часть 1):

| 3 | Откуда ты и где живёшь | сказать, откуда он и где живёт | вопрос с be, `from`, `in`, артикль | Places: Town and City | CEFR с. 79, 63 |

Подробный замысел первого урока — [plan-modul-3-urok-1.md](plan-modul-3-urok-1.md).

---

## 0. ПЕРВОЕ ДЕЛО: тот же предмет в самом курсе

Правило CLAUDE.md: **проверка по курсу идёт первой, а не второй.** Искал так:

```
grep -n "title:\|outcome:" courses/english-starter/*.ts courses/english-beginner-2/*.ts
grep -rn "I am from\|I'm from\|are you from\|live in\|I live" courses/*/*.ts
```

### 0.1. Предмет модуля целиком лежит в замороженном курсе, и не в одном месте

| Где | Что там | Сколько уроков |
|---|---|---|
| `english-starter/rasskaz-o-sebe.ts` — модуль 3 «Рассказ о себе» | `from`, страны и города, артикль `a`/`an`, занятие, `in`, порядок слов, `Where are you from?`, рассказ целиком, чтение, слушание, письмо | **11** |
| `english-starter/vopros-i-otvet.ts` — модуль 4 «Вопрос и краткий ответ» | вопрос с be перестановкой, `Yes, I am`, `No, I am not`, вопрос о другом, полный и краткий ответ | **11** |
| `english-starter/gde-eto.ts` — модуль 11 «Где это», урок «Места в городе» | `shop`, `station`, `hospital`, `park`, `school`, `hotel`, `bus stop`, `street`; врезка о `the` при месте в городе | 1 из 11 |
| `english-starter/dni-daty-vremya.ts`, урок «A и the» | определённый артикль | 1 |
| `english-starter/chto-ya-delayu.ts` | `I live in Astana` — но уже через Present Simple | 3 |

**Новый модуль 3 не пишется на пустом месте: он сводит в один модуль то, что
замороженный курс разложил на два модуля и двадцать два урока.** Это надо сказать
прямо, потому что отсюда и польза, и опасность.

### 0.2. Чем новый модуль ОТЛИЧАЕТСЯ от старого — по пунктам, а не общими словами

| | Замороженный курс | Новый модуль 3 |
|---|---|---|
| Уроков на тот же предмет | 22 (модули 3 и 4) | **8** |
| Дробность объяснения | урок «Откуда ты: from»: 2 абзаца объяснения, 1 таблица, 2 врезки, 4 строки примера | **8 названных случаев и 16 целых примеров с переводом** в одном уроке 1 |
| Начало урока | «Имя названо, здороваться уже получается. Следующее, о чём заходит речь при знакомстве, — откуда человек» — обобщение о нашем же уроке | **сцена**: первый день на курсах, четыре реплики, `from` в них пять раз |
| Порядок заданий | вперемешку | лестница «узнать, произвести, применить к себе» |
| Тема словаря | слов темы `Places: Town and City` в старом модуле 3 **три**, и стоят они там потому, что так удобно грамматике | **тема объявлена**, модуль закрывает все семь её слов A1 (раздел 3) |
| Артикль | `a`/`an` в модуле 3, `the` — в модуле 10, между ними `the` в уроках без объяснения | **`a`, `an` и `the` в одном модуле**, там, где артикль впервые нужен (раздел 4.2) |
| Вопрос | `Where are you from?` дан формулой, разбор вопроса — только в следующем модуле | вопрос с be разбирается ЗДЕСЬ, формулой остаётся только `Where are you from?` с названным адресом |

**Чего я НЕ утверждаю.** Что старый модуль плох во всём. Его разведение `from` и
`in` («родом» против «сейчас») точно, и новый модуль берёт эту пару как есть.
Отличие не в правоте, а в дробности и в том, что словарь теперь ведёт тема, а не
грамматика.

### 0.3. Что берём как есть, что переписываем, что не годится вовсе

**Берём как есть (текст, разбор, звук):**

| Что | Откуда | Почему годится |
|---|---|---|
| разведение `from` и `in` («родом» против «сейчас») | `rasskaz-o-sebe`, урок 5, блоки `zachem-in`, `tablica-from-in`, `ne-putat-from-i-in` | это и есть расхождение с русским, на котором стоит модуль |
| правило `a`/`an` по первому ЗВУКУ, а не по букве | там же, урок 3, блоки `a-ili-an`, `zvuk-a-ne-bukva` | `an hour` и `a university` — настоящие случаи, правило сформулировано верно |
| врезка «перед именем, названием страны и города артикль не ставят» | там же, блок `artikl-i-imena` | нужна модулю дословно |
| врезка о `the` при месте в городе («вокзал в разговоре один») | `gde-eto`, блок `the-o-meste-v-gorode` | лучшее объяснение `the` в замороженном курсе |
| **чтения и записи звука** слов `from`, `in`, `where`, `city`, `capital`, `town`, `street`, `station`, `park`, `market`, `here`, `country`, `home`, `road`, `airport`, `village` | `rasskaz-o-sebe`, `gde-eto`, `zdes-est`, `english-elementary/eto-bylo` | условности курса в них уже соблюдены (`r` вместо `ɹ`, `/ˈkæpɪtl/` без `ə`) |
| готовые записи фраз, дословно перенесённых: `I'm from Kazakhstan.`, `I'm from Almaty.`, `He's from Turkey.`, `She's from Astana.`, `I'm in Astana.`, `Where are you from?` | `rasskaz-o-sebe`, таблицы уроков 1, 5, 7 | имя файла считается из текста: взял строку теми же словами — запись пришла бесплатно |

**Переписываем (мысль верна, подача не годится):**

- **начала всех уроков.** «Рассказать о себе — половина разговора. Вторая
  половина — спросить» — обобщение о нашем же уроке, ровно то, на чём владелец
  споткнулся 3 сентября;
- **урок «Порядок слов в предложении»** (старый урок 6). Предмет настоящий, но на
  этой ступени он уже сделан модулем 2 (`CLAUSES declarative, FORM: AFFIRMATIVE
  DECLARATIVE`). Отдельного урока не будет, порядок работает внутри случаев;
- **таблица стран со столицами** — берём без столбца «Столица»: слово `capital`
  работает в уроке 4 (`the capital`), а в уроке 1 стало бы карточкой, которой
  негде работать.

**Не годится вовсе:**

| Что | Почему |
|---|---|
| урок «Чем ты занимаешься» (`I'm a doctor`) | занятия программа отдала **модулю 6** (`Work and Jobs`), а карточки `student`, `teacher`, `doctor`, `driver`, `artist`, `actor` уже стоят в модуле 2 нового курса |
| задания старых модулей 3 и 4 | правило CLAUDE.md: задания и работы замороженного курса не переносим — с ними переносится порода «работа повторяет урок» |
| итоги уроков | пишутся заново под новый состав |
| урок «Рассказ о себе целиком» из четырёх предложений | четвёртое там про занятие, это чужой модуль; свод модуля 3 будет из трёх |
| `I live in Astana` из `chto-ya-delayu` | Present Simple, модуль 13. Разбор — раздел 6.1 |

---

## 1. Дескриптор: опора программы проверена, и она верна

**CEFR Companion 2020, с. 79**, шкала *Information exchange*, графа **A1**,
дословно:

> Can ask and answer questions about themselves and other people, **where they
> live**, people they know, things they have.

**CEFR Companion 2020, с. 63**, шкала *Sustained monologue: describing
experience*, графа **A1**, дословно:

> Can describe themselves, **what they do and where they live**.

Обе страницы открыл сам, обе графы A1, обе называют «где живёт» прямо. **Опора
программы верна, менять её не нужно.**

**Третья опора, которой в программе нет, а стоило бы.** CEFR, **с. 79**, графа
**Pre-A1** той же шкалы: «Can ask very simple questions for information, such as
“What is this?” and understand one- or two-word/sign answers». Она подпирает
вторую половину дела — не только рассказать, но и спросить, — и делает это
ступенью ниже нашей.

**Четвёртая, самая прямая, из руководства.** A2 Key, **с. 42**, Speaking Part 1,
Phase 1 — то, что экзаменатор спрашивает первым делом:

> Where do you come from? / **Where do you live?**

Дело модуля названо источником дословно. Оговорка: экзамен спрашивает через `do`,
то есть Present Simple (модуль 15), а наш вопрос стоит на `be`. Разбор — раздел 6.

**A2 Key, с. 51**, Inventory of functions: «asking for and giving personal
details: (full) name, age, **address**…» и «**asking for and giving simple
information about places**». Это перечень ступени A2; опорой ступени он не
служит и стоит здесь подтверждением состава.

---

## 2. Состав: восемь уроков

Порядок программы (раздел 3): правила, чтение, слушание, говорение; письмо
только там, где источник называет жанр.

| № | Урок | Вид | Что в нём | Итог |
|---|---|---|---|---|
| 1 | Откуда ты: I am from Almaty | правила | `from` перед городом и страной; название после `from` не меняется; `he is from`; короткая запись; заглавная буква; английские названия стран | называть, откуда человек родом: I am from и название города или страны |
| 2 | Где ты сейчас: I am in Astana | правила | `in` перед городом и страной; `from` против `in`; `at home`, `at work`; `My home is in Astana`; `here` | говорить, где человек сейчас и где его дом: in и название места |
| 3 | Один из таких: a и an | правила | `a` перед согласным звуком, `an` перед гласным; смотрим на звук, а не на букву; перед названием города и страны артикля нет | ставить a или an по первому звуку следующего слова |
| 4 | Тот самый: the | правила | `the` о том, что в разговоре одно и о чём уже сказали; `the capital`; `the street`; `at home` без артикля | выбирать между a и the по тому, знает ли собеседник, о чём речь |
| 5 | Ты из Астаны? Are you from Astana? | правила | вопрос перестановкой; `Yes, I am`; `No, I am not`; `Is he from Turkey?`; `I am not from Astana`; `Where are you from?` формулой | спрашивать, откуда человек, и коротко отвечать да и нет |
| 6 | Читаем: три записи о себе | чтение | `genre: "message"`, три коротких записи о себе с местом | находить в короткой записи, откуда человек и где он сейчас |
| 7 | Слушаем: кто откуда | слушание | два коротких разговора, ответ одним словом | узнавать на слух название места, которое назвали |
| 8 | Говорим: откуда ты | говорение | образец по A2 Key, с. 42, Phase 1 | собирать ответ о себе из трёх строк: имя, откуда, где сейчас |

**Итоги стоят в порядке уроков** — это сверяет `check:content`.

**Письма в модуле НЕТ, и вот проверенное основание.** Четыре жанра, которые
источник называет на A1 и Pre-A1, программа раздала: бланк и записка (CEFR
с. 84) — модулям 5 и 29, открытка и короткое сообщение (с. 83) — модулям 28 и 27.
Дескриптор модуля 3 — устные шкалы (с. 79 Information exchange, с. 63 Sustained
monologue). Ближайшая письменная графа, которая подошла бы, — Correspondence
Pre-A1, с. 83, «Can convey basic personal information in short phrases and
sentences», — и её адрес программа назвала сама: модуль 27 «В сети».

**Опоры уроков умений, открыты мной:**

- **чтение** — CEFR с. 57, *Reading for information and argument*, **A1**: «Can
  get an idea of the content of simpler informational material and **short,
  simple descriptions**, especially if there is visual support»;
- **слушание** — CEFR с. 48, *Overall oral comprehension*, **A1**: «Can
  **recognise concrete information (e.g. places and times)** on familiar topics
  encountered in everyday life, provided it is delivered slowly and clearly».
  Дословно про места: лучшей опоры у урока слушания в этом модуле не будет.
  Формат ответа — A2 Key, с. 29: «The candidates should only write ONE word, or a
  number, or a date, or a time for their answer»;
- **говорение** — A2 Key, с. 42, Speaking Part 1 (цитата в разделе 1). Итог назван
  по проверяемой половине: машина устную речь не оценивает.

**Вид начала модуля.** Программа против однообразия требует не меньше трёх видов
начала на часть. В части 1 они набираются: модуль 1 — модуль-правило, модуль 2 —
модуль-разговор (открывается слушанием), модуль 5 «Бланк» — модуль-текст. **Модуль
3 открывается уроком правил, и это не нарушение**: правило требует трёх видов В
ЧАСТИ, а не разных у каждого модуля. Уводить чтение вперёд здесь нечем: три записи
о себе держатся на `from`, `in` и артикле разом, и до разбора это был бы текст из
сплошь незнакомых оборотов.

**Возвращение к пройденному** (программа обещает по одному в каждом модуле со
второго): урок 1, поле `vozvrat` со ссылкой на задание модуля 2 из урока
«Он, она, оно — is» — ученик снова выбирает `is` при имени, а потом ставит после
него `from`. Точный `id` называет сборщик: модуль 2 сейчас в правке, и имена его
заданий ещё меняются.

---

## 3. Тема словника и слова

**Тема одна: `Places: Town and City`** — дословное имя из Приложения 2 словника
A2 Key. Читал ВТОРОЙ разбор, `materials/.md/cambridge-vocab-a2-key.md`, строка
2041 и таблица под ней: `pdf-parse` склеивает четыре колонки в одну строку, и
наивное чтение врёт.

**Записей в теме двадцать:** airport, bridge, bus station, bus stop, car park,
city centre, corner, market, motorway, park, petrol station, playground, road,
roundabout, square, station, street, town, underground, railway.

**Из них Oxford 3000 ставит A1 семи:** `airport`, `market`, `park`, `road`,
`station`, `street`, `town`. **Это и есть знаменатель нормы по теме — семь.**

- A2, и потому не берём: `bridge`, `corner`, `square`, `underground`;
- Oxford не знает вовсе (составные и редкие): `bus station`, `bus stop`, `car
  park`, `city centre`, `motorway`, `petrol station`, `playground`, `roundabout`,
  `railway`. В знаменатель не входят и не запрещаются.

**Тема бедная — семь слов, — и модуль закрывает все семь.** Пять в уроке чтения,
где им есть где работать, два в уроках правил.

**Осторожно, ловушка имени темы.** Слова `city` в теме `Places: Town and City`
НЕТ — есть `city centre`. Само `city` стоит только в алфавитной части словника
(строка 373 разбора). Карточка `city` законна и нужна, но в числитель нормы по
этой теме она не идёт. Так же `capital` (алфавитная часть, строка 274), `country`
(тема `The Natural World`), `home` и `live` (тема `House and Home`), `village`
(тема `Places: Countryside`), `where` (алфавитная часть, строка 1623).

### 3.1. Карточки модуля

Чтение и запись звука берутся из замороженного курса (раздел 0.3).

| Карточка | Чтение | Перевод | Урок | Тема словника | Где работает, кроме себя |
|---|---|---|---|---|---|
| `from` | /frɒm/ | из | 1 | вне тем | сцена и все восемь случаев урока 1 |
| `in` | /ɪn/ | в | 2 | вне тем | таблица «from и in рядом», уроки 4 и 5 |
| `here` | /hɪə/ | здесь, тут | 2 | вне тем | `I am here now`, урок 5, чтение |
| `home` | /həʊm/ | дом, дома | 2 | House and Home | `at home`, `My home is in Astana`, урок 4 |
| `city` | /ˈsɪti/ | город | 3 | вне тем | `a city`, `the city`, чтение |
| `town` | /taʊn/ | город (небольшой) | 3 | **Places: Town and City** | `a small town`, чтение |
| `country` | /ˈkʌntri/ | страна | 3 | The Natural World | `a country`, урок 1 |
| `capital` | /ˈkæpɪtl/ | столица | 4 | вне тем | `the capital`, чтение |
| `street` | /striːt/ | улица | 4 | **Places: Town and City** | `the street`, `Abay Street`, чтение |
| `where` | /weə/ | где, откуда | 5 | вне тем | `Where are you from?`, говорение |
| `station` | /ˈsteɪʃn/ | станция, вокзал | 6 | **Places: Town and City** | текст чтения, слушание |
| `park` | /pɑːk/ | парк | 6 | **Places: Town and City** | текст чтения, слушание |
| `market` | /ˈmɑːkɪt/ | рынок | 6 | **Places: Town and City** | текст чтения |
| `road` | /rəʊd/ | дорога | 6 | **Places: Town and City** | текст чтения |
| `airport` | /ˈeəpɔːt/ | аэропорт | 6 | **Places: Town and City** | текст чтения, слушание |
| `village` | /ˈvɪlɪdʒ/ | деревня, село | 6 | Places: Countryside | текст чтения: не все живут в городе |

Шестнадцать карточек. Прежняя норма «тридцать на модуль» снята программой
(раздел 5.1), полнота считается по теме и по курсу.

**Столбец «Где работает» стоит здесь не для красоты.** `check:content` сообщает
сведениями о карточке, которая в модуле нигде больше не встречается, — это след
добора нормы ради счёта. Ни одной такой в списке нет.

**Названия стран и городов карточками не становятся** — решение владельца от
15 августа 2026: имена собственные ступенью не меряются. Turkey, Spain, Italy,
France, Kazakhstan, Almaty, Astana, Madrid, Rome, Ankara стоят в таблице урока 1
со своим звуком, а не в словаре.

**Чего модуль не вводит заново, потому что карточка стоит раньше:** `I`, `he`,
`she`, `you`, `my`, `friend`, `name`, `big`, `new`, `house`, `car`, `student`,
`teacher` — модули 1 и 2.

**`small` модуль не берёт.** В уроке 3 нужна пара «большой — маленький» для
`a big city` и `a small town`; `big` — карточка модуля 2, `small` — слово модуля
9 «Цвет и размер». Здесь оно живёт переводом строки. Если методист модуля 9 не
возражает, карточка может прийти сюда: строка 7.2.

---

## 4. Пункты English Grammar Profile

Ключ — поле `id`, а не заголовок (`Guideword` повторяется 129 раз). Файл читал
настоящим разбором CSV: 1222 записи, из них уровня A1 — **109**. Искал **по всему
файлу**, а не по одной подкатегории: на этом здесь уже дважды обжигались.

### 4.1. Что модуль вводит (`egp`)

| `id` | Категория, заголовок | Can-do и пример источника | Где в модуле |
|---|---|---|---|
| `1741163713112x806003192035927200` | `PREPOSITIONS / prepostions`, FORM: SIMPLE, LR **1** | «Can use a limited range of simple (single-word) prepositions.» — «**He works in Bognor Regis.**» | уроки 1 и 2: `from`, `in`, `at` |
| `1741163713112x603850914179882600` | `PREPOSITIONS / prepostions`, FORM: PREPOSITION + NOUN PHRASE | «Can form prepositional phrases with a preposition and a noun phrase.» — «When I have free time, I go **to the park** with my dog.» | уроки 1 и 2: `from Almaty`, `in Astana` |
| `1741163715608x123988758899529200` | `VERBS / linking`, FORM: 'BE' + COMPLEMENT | «Can use linking verb 'be' with complements.» — «**Martin is from Poland.**» | урок 1 целиком: пример графы и есть строка урока |
| `1741163708789x105964971324936210` | `DETERMINERS / articles`, FORM: WITH NOUNS | «Can use articles '**the**', '**a**' and '**an**' before nouns.» — «She lives in **a** small flat in **the** town — it's great!» | уроки 3 и 4 |
| `1741163708336x928067678664856700` | `CLAUSES / interrogatives`, FORM: AFFIRMATIVE INTERROGATIVE, WITH 'BE' | «Can form interrogative clauses with 'be'.» — «**How are you?**», «**Are you ok?**» | урок 5 |
| `1741163713867x150062658595893500` | `PRONOUNS / subject-object`, FORM: (SUBJECT) QUESTIONS WITH 'BE' | «Can use the pronouns 'you' and 'it' after 'be' in question forms.» — «How are you?», «Is it OK?» | урок 5: `Are you from Astana?` |
| `1741163708329x778951051617750700` | `CLAUSES / declarative`, FORM: NEGATIVE DECLARATIVE WITH 'BE' | «Can form a simple negative declarative clause with lexical 'be' + 'not' or 'n't'.» — «My college is not very big.», «**I'm not bad.**» | урок 5: `No, I am not`, `I am not from Astana` |

Семь пунктов. Все семь открыл и выписал целиком; все уровня A1; ни один не роздан
модулям 1 и 2.

### 4.2. Про артикль отдельно: почему пункт берёт модуль 3, а не модуль 7

**Пункт про артикль в источнике ОДИН**, и он покрывает `the`, `a` и `an` сразу:
`FORM: WITH NOUNS`, `Lexical Range: N/A`. Значит по правилу программы (раздел 6.2)
поставить его в `egp` может **только один модуль**. А слово «артикль» стоит в
столбце «Грамматика» у двух: у модуля 3 и у модуля 7 «Что это такое».

**Беру я, и вот счёт, а не предпочтение.**

1. **Артикль впервые нужен здесь и уже опережает.** В видимом тексте модулей 1 и
   2 слова `the` нет ни разу (проверил `npm run tekst` по обоим: ноль), а
   `a student`, `an artist`, `a car` в модуле 2 стоят и объяснения не имеют —
   план модуля 2 честно записал это долгом с адресом «модуль 3».
2. **Модуль 3 без артикля не пишется.** `Almaty is a city`, `Astana is the
   capital`, `the station` в тексте чтения — тема `Places: Town and City` сама
   приводит определённый артикль.
3. **Самая дорогая ошибка замороженного курса — ровно эта.** `the` стоял в уроках
   418 раз без единого объяснения, а урок о нём выбирался счётом: «идёт туда, где
   первое употребление». Здесь первое употребление — модуль 3.
4. **Модуль 7 при этом ничего не теряет.** Его новизна — `this`, а это три
   отдельных пункта A1 (`DETERMINERS demonstratives`: `USE: 'THIS', POINTING`,
   `USE: 'THIS', FUTURE`, `FORM: 'THIS' WITH SINGULAR NOUNS`). Артикль он ставит
   в `egpPovtor` — программа это прямо разрешает и молчит о таком.

**Цена решения.** Модуль 3 становится тяжелее на один урок правил: артикль идёт
двумя уроками (`a`/`an` и `the`), а не одним. Если владелец предпочтёт оставить
`the` модулю 7, модуль 3 обойдётся: строки чтения переписываются без `the`
(`Almaty is a big city. My street is Abay Street.`), а урок 4 отдаётся отрицанию
с be, которое сейчас втиснуто в урок 5. **Это выбор о составе курса, а не о
источнике, и потому он не мой.** Строка 7.1.

### 4.3. К чему модуль возвращается (`egpPovtor`)

| `id` | Что это | Откуда | Как работает здесь |
|---|---|---|---|
| `1741163711816x743806450459332500` | `NOUNS / types`, FORM: PROPER NOUNS — «Can use proper nouns with a capital letter…», пример источника «**In Leicester I live in an area called Hamilton.**» | модуль 1 | случай 7 урока 1: `Kazakhstan`, `Almaty`, `Spain` с заглавной |
| `1741163715288x539616242661052000` | `VERBS / types`, FORM: LINKING VERB, 'BE' | модуль 2 | форма be в каждой строке модуля |
| `1741163713868x463659211645272000` | `PRONOUNS / subject-object`, FORM: (SUBJECT) STATEMENTS | модуль 2 | `I`, `he`, `she` перед формой be |
| `1741163708329x286701804737242940` | `CLAUSES / declarative`, FORM: AFFIRMATIVE DECLARATIVE | модуль 2 | все утвердительные строки |

### 4.4. Пункт, на который я НЕ претендую, но который стоит бесхозным

`1741163708792x578027203654075000` — `DETERMINERS / articles`, **FORM:
PREPOSITION + NO ARTICLE**, A1, `Lexical Range: 1`. Can-do: «Can use no article
before a limited range of nouns in some fixed expressions with prepositions»,
пример источника — «**If you are at home**, I can come tomorrow».

Это `at home` и `at work`. **Модуль 2 их уже употребляет** (`I am at home`, `I am
at work`, карточка `work`), модуль 3 повторяет в уроке 2 и уроке 4. Хозяина у
пункта в программе нет ни у кого: по смыслу он ближе всего к модулю 7 (`House and
Home`). Я готов взять его себе — в уроке 4 он ложится ровно в разбор «а вот здесь
артикля не ставят», — но забирать молча не буду. Строка 7.3.

### 4.5. Пункт, который я НЕ беру, хотя он про `from`

`1741163711829x722500780296901000` — `NOUNS / noun phrases - grammatical
functions`, FORM: AS COMPLEMENT OF PREPOSITIONS, A1. Это то же самое, что мой
`PREPOSITION + NOUN PHRASE`, только с другого конца — со стороны
существительного. Брать оба значит забрать соседям пункт ради счёта. Оставляю
свободным.

---

## 5. Три места, где источники расходятся, и все три — не мои

### 5.1. `Where are you from?` — A1 у Cambridge, A2 у English Grammar Profile

**Что стоит у English Grammar Profile.** Вопрос со словом `where` и главным
глаголом `be` — **A2**, двумя графами сразу:

- `1741163715031x898125206259626100`, `QUESTIONS / wh-`, FORM: MAIN VERB 'BE':
  «Can use 'wh-'words + main verb 'be' + subject to form 'wh-' questions.» —
  «How was your dinner yesterday?», «Where are you, men?»;
- `1741163713115x831925659489416200`, `PREPOSITIONS`, **FORM: STRANDING**: «Can
  end a 'wh-' question with a preposition, where the preposition is stranded from
  its complement.» — «Who are you going **with**?», «So what are you waiting
  **for**?» Это ровно то `from` в конце.

Пунктов A1 о `wh-`-вопросах в файле **три**, и ни один не про `where`: я перебрал
все 109 записей A1 по подстроке `QUESTION`.

**Что стоит у Cambridge.** Young Learners, **с. 29**, Grammar and Structures List,
**Pre A1 Starters** (нижний колонтитул страницы: «Grammar and Structures List 29»,
то есть номер подтверждён самой страницей, а не оглавлением):

> Question words — Who is that man? **Where is Alex?**

Там же, **Pre A1 Starters Speaking Part 1**, HOW TO APPROACH THE TASK: «practise
identifying people, animals and things in different pictures by pointing in
response to questions such as: **Where's the chair? Where are the bananas?**»

**И CEFR, с. 79, Pre-A1**: «Can ask very simple questions for information, such
as “What is this?”».

**Три источника из четырёх ставят `wh-` + `be` НИЖЕ нашей ступени, один — выше.**

**Что я предлагаю, пока владелец не решил.** Модуль 5 разбирает вопрос, который
целиком A1 по всем источникам, — **`Are you from Astana?`** с коротким ответом.
`Where are you from?` даётся **готовым оборотом с адресом**, тем же приёмом, каким
модуль 1 взял `How do you spell that?`, а модуль 2 — `How are you?`. Адрес есть
настоящий: `wh-`-вопросы программа отдала модулю 15 «Спросить о делах».

**Это не решение, а безопасный ход.** Настоящее решение — общее для курса и оно
уже лежит у владельца: план модуля 2, раздел 6, ставит тот же вопрос про
`This is …` и `Who is this?`. **Один и тот же выбор — брать ли Pre A1 Cambridge
против A2 English Grammar Profile — не должен решаться дважды по-разному, иначе
курс повторит беду Elementary, где `Who's that?` был снят в модуле 11 как A2 и
взят в модуле 16 как Pre A1.** Строка 7.1.

### 5.2. `Where do you live?` — экзамен спрашивает через `do`

A2 Key, с. 42, Speaking Part 1 спрашивает «Where do you come from?» и «Where do
you live?» — обе строки в Present Simple, который программа отдала модулям 13–15.
Урок говорения модуля 3 берёт **вопрос экзаменатора в нашем виде** (`Where are you
from?`) и **ответ в виде экзамена** (`I am from Kazakhstan. I am in Astana now.`).
Оговорка обязана стоять в `sources` модуля: образец урезан, и вот чем.

### 5.3. `live` в модуле не появляется, хотя модуль называется «где живёшь»

`live` — Oxford A1, словник A2 Key, тема `House and Home`. Слово законно. **А
строка `I live in Astana` — нет**: это лексический глагол в Present Simple,
`1741163713626x486355013779909760`, и весь пункт роздан модулю 13. Правило
CLAUDE.md прямо про этот случай: «карточка законна, строка — нет».

**Как модуль честно исполняет «где живёшь» без `live`:**

| Что надо сказать | Чем говорим | Опора |
|---|---|---|
| откуда я родом | `I am from Almaty.` | `VERBS / linking`, 'BE' + COMPLEMENT — пример графы «Martin is from Poland» |
| где я сейчас | `I am in Astana.` | `PREPOSITIONS`, FORM: SIMPLE — пример графы «He works in Bognor Regis» |
| где мой дом | `My home is in Astana.` | та же графа + притяжательное `my` из модуля 2 |

Дескриптор при этом исполняется: CEFR с. 79 требует уметь ответить, «where they
live», а не употребить глагол `live`.

**Долг с адресом:** глагол `live` и строка `I live in Astana` — модуль 13 «Что я
делаю каждый день». Адрес называется в уроке 2 врезкой, иначе `npm run chain`
поймает обещание без адреса, и правильно поймает.

---

## 6. Долги модуля — все с адресами

| Долг или решение | Адрес |
|---|---|
| глагол `live`, строка `I live in Astana` | **модуль 13**. Врезка с адресом стоит в уроке 2 |
| `Where are you from?` берётся готовым оборотом | **модуль 15** «Спросить о делах» — там `wh-`-вопросы разбираются |
| `Where do you live?` из образца экзамена урезано | оговорка в `sources`, раздел 5.2 |
| `at home`, `at work` — обороты без артикля | берутся как есть в уроке 4; пункт источника — раздел 4.4 |
| `an hour`, `a university` — звук против буквы | остаются в уроке 3 врезкой, как в замороженном курсе; отдельного урока не требуют |
| множественное число (`two cities`) | **модули 4 и 8**. В модуле 3 город и страна всегда один |
| прилагательное перед существительным (`a big city`) | **модуль 9**. В модуле 3 сочетание `a big city` работает, но правилом не становится: `DETERMINERS articles, FORM: 'A' + ADJECTIVES` — отдельный пункт A1, и он модуля 9 |
| `this` (`This is my city`) | **модуль 7**; в модуле 3 не употребляется ни разу |
| предлоги места `on`, `under`, `next to` | **модуль 12** |
| названия стран: почему `Turkey`, а не `Türkiye` | в уроке не обсуждается вовсе. Источников о названии нет ни в одном файле `materials/`, а замороженный курс сделал из этого задание. Мы — нет |

**Отдельно: «прилагательное перед существительным» в уроке 3.** Строка `a big
city` в уроке нужна — без неё не показать, что артикль стоит перед всей группой,
а не перед существительным. Но правило о порядке «артикль, признак, предмет» тут
НЕ формулируется, и заданий на него нет. Если методист модуля 9 считает, что даже
употребление тут лишнее, строка меняется на `a city` и `a town`. Строка 7.2.

---

## 7. Строки соседям и владельцу

**7.1 — владельцу, и это не мой выбор.** Один и тот же вопрос стоит перед двумя
модулями сразу: брать ли на этой ступени то, что Cambridge кладёт на Pre A1
Starters, а English Grammar Profile — на A2. У модуля 2 это `This is …` и `Who is
this?`, у модуля 3 — `Where are you from?`. **Решение должно быть одно на весь
курс.** Если владелец берёт по Cambridge — модуль 3 разбирает `Where are you
from?` полноценно, и это шестой случай урока 5. Если по English Grammar Profile —
остаётся готовый оборот с адресом, как написано выше. Пока владелец не решил,
модуль пишется по второму, осторожному пути: его потом легко расширить, а сузить
написанное — нельзя.

**7.2 — методисту модуля 9 «Цвет и размер».** Модуль 3 хотел бы употреблять `a
big city` и `a small town` (без правила и без заданий) и, если ты не против, взять
карточку `small`. Пункт `DETERMINERS articles, FORM: 'A' + ADJECTIVES`
(`1741163708789x344483096716751800`, A1, LR 1) я НЕ беру: он твой.

**7.3 — методисту модуля 7 «Что это такое».** Два касания.

1. Пункт `DETERMINERS articles, FORM: WITH NOUNS` я забираю в `egp` (основание —
   раздел 4.2). Тебе он идёт в `egpPovtor`; твоя новизна — три пункта
   `demonstratives` про `this`, они целы.
2. Пункт `FORM: PREPOSITION + NO ARTICLE` (`at home`, `at work`) бесхозен. По теме
   он ближе к тебе (`House and Home`), по употреблению — уже работает в модулях 2
   и 3. Скажи, берёшь ли; иначе возьму я.

**7.4 — методисту модуля 5 «Бланк».** Модуль 3 не берёт слов `address`, `country`
как подписи полей и не трогает бланк вовсе. Карточка `country` в модуле 3 стоит
как обычное слово («страна»), а не как подпись поля.

**7.5 — сводящему.** В плане модуля 2 пункт `DETERMINERS / possessives, FORM:
WITH NOUNS` (`1741163709005x427091401714639400`) стоит в `egp` модуля 2, а
программа отдаёт притяжательные модулю 5. Один `id` в `egp` двух модулей — это
`ВОПРОС` у проверки. Не моё дело, но сказать надо: модуль 3 употребляет `my home`
и потому от этого пункта зависит.

---

## 8. Что проверено и чего проверить не удалось

**Проверено мной, открытием файла:**

- CEFR Companion 2020: с. 79 (Information exchange, графы A1 и Pre-A1 выписаны
  целиком), с. 63 (Sustained monologue: describing experience), с. 57 (Reading for
  information and argument), с. 48 (Overall oral comprehension), с. 55–56
  (Reading correspondence, Reading for orientation) — последние две смотрел, чтобы
  убедиться, что для модуля они не подходят: там открытки, вывески и указатели, а
  это модули 24 и 28;
- A2 Key Handbook: с. 42 (Speaking Part 1, обе фразы Phase 1 выписаны дословно),
  с. 51 (Inventory of functions), с. 29 (формат ответа на слух);
- Young Learners: с. 29 (Grammar and Structures List, Pre A1 Starters — строки
  Determiners, Question words, Verbs с коротким ответом), с. 26–27 (Starters
  Speaking Part 1, «Where's the chair?»);
- English Grammar Profile: разобрал CSV настоящим разбором, сторож сошёлся (1222
  записи, 109 на A1); перебрал все 109 записей A1 глазами; открыл целиком и
  выписал `Can-do` и примеры у одиннадцати пунктов, названных в разделе 4;
- словник A2 Key: Приложение 2, тема `Places: Town and City` (второй разбор,
  строка 2041), а также темы `House and Home`, `Places: Buildings`, `Places:
  Countryside`, `The Natural World` — чтобы знать, чьи слова я беру со стороны;
  алфавитная часть для `city`, `capital`, `where`, `here`, `now`, `country`,
  `village`, `near`, `far`, `language`, `home`, `live`;
- Oxford 3000: ступени всех 20 слов темы и ещё 26 слов, названных в разделе 3;
- курс: `courses/english-starter/*.ts` и `courses/english-beginner-2/*.ts` —
  поиском по заголовкам, итогам и английским строкам;
- счёт `the` в видимом тексте модулей 1 и 2 нового курса: **ноль** (`npm run
  tekst` по обоим модулям).

**Чего проверить не удалось, и это надо знать:**

1. **Страницы 26–27 руководства Young Learners я проверял по разбору, а не по
   самой странице.** У этого файла разборщик двоит страницы — предупреждение
   стоит в CLAUDE.md, и я его видел своими глазами: строки «с. 27 / Advice by
   task / Speaking Part 1 / A1 Movers» стоят прямо над текстом Pre A1 Starters.
   Заголовок раздела («Pre A1 Starters / Speaking») и подпись под заданием я
   привожу, но **номер страницы 26–27 у цитаты «Where's the chair?» считаю
   неподтверждённым**. Цитата со **с. 29** подтверждена нижним колонтитулом самой
   страницы и потому надёжна.
2. **Порядок слов темы внутри таблицы `Places: Town and City`** я привожу по
   разбору markitdown; сама вёрстка PDF в четыре колонки, и если в теме есть
   двадцать первая запись, съеденная разбором, я её не увижу. Сторож программы
   (25 тем, не меньше 1100 записей) на уровне отдельной темы не работает.
3. **Ступени `bus stop`, `city centre`, `car park` и прочих составных** ни Oxford
   3000, ни Oxford 5000 не знают вовсе. Я не называю их ни A1, ни выше, а просто
   не считаю в знаменатель. Утверждать, что они посильны, мне нечем.
4. **Обычаев не проверял и не мог.** Ни одной пометки о том, где как принято
   отвечать на «откуда ты» — городом или страной, — в `materials/` нет. Поэтому в
   уроках нет ни строки вида «чаще отвечают городом».
5. **Число экранов и время урока** — прикидка, а не измерение. Двенадцать минут
   у урока 1 поставлены по образцу модуля 2; настоящее время знает только тот,
   кто урок прошёл.
6. **Точный `id` задания для `vozvrat`** назвать не могу: модуль 2 сейчас правится
   по разбору, и имена его заданий меняются. Называю урок, а не задание.
7. **Не сверял модуль 3 с модулями 4, 5 и 6**, которые пишутся прямо сейчас
   тремя другими методистами. Пересечения, которые я вижу со своей стороны,
   вынесены строками 7.2–7.5; чего не вижу — сведёт координатор.
