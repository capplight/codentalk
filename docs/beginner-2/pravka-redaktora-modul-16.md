# Правка редактора: модуль 16 «Как часто», урок 1 «Всегда и иногда»

Журнал ведётся по ходу работы. Файл урока — `courses/english-beginner-2/kak-chasto.ts`.
Заход только по уроку 1 (`vsegda-i-inogda`); работа модуля и уроки 2–11 не трогаются.

## Ход работы

- Журнал заведён первым действием. Дальше — чтение уговора стиля, замысла и образца модуля 15.
- Прочитаны: уговор стиля (`stil-obyasneniy.md`, разделы 1–8), замысел (шапка, 4, 9.1, 16.6, 16.7, 17, 18.1),
  образец модуля 15, урок 1 (`sprashivayu-do-you-work`). Прочитан код `sZvukom` (`components/lesson/Ekran.tsx`)
  и `razobrat` (`lib/content/razbor-stroki.ts`).
- Что из кода следует для текста, проверено чтением функции, а не соседнего места:
  - `sZvukom` ищет каждый ключ `zvuchat` через `indexOf` в имени случая и в каждой строке правила. У случая 5
    ключи — одиночные слова `always` и `sometimes`. Значит в имени и правиле случая 5 этих английских слов быть
    не может вовсе, иначе на экране две кнопки. У остальных случаев ключи — целые строки, и отдельное слово
    `always` в правиле кнопки не даёт.
  - `razobrat` делает строку правила парой, если слева от « — » стоит латиница. В правилах тире не ставлю.
- Имена, взятые у курса: «глагол», «оборот» (модуль 13 зовёт так `in the morning` и пишет «стоит в конце
  строки»), «слово о том, кто делает». Для `in the evening` и `at the weekend` вместе в уроке одно имя —
  «оборот о времени».

## Заполненные блоки

1. **Вступление** (`vstuplenie`). Замысел 9.1: начало — русская фраза. Уговор стиля 8.6: вступление кончается
   строкой «Здесь мы разберём…». Сказано «в этих строках» и «слово с таким смыслом встаёт на то же место», а не
   «по-английски всегда там же»: слово «всегда» здесь было бы утверждением о языке (правило 4), а у `sometimes`
   есть и второе место (замысел 16.3).
2. **Случай 1** `sluchay-always`. Замысел 9.1, случай 1. В правиле нет русского перевода: его уже даёт имя случая,
   а правило говорит, что слово значит («каждый раз, без исключений»). Имя без английской строки из `zvuchat`, поэтому
   второй кнопки нет.
3. **Случай 2** `sluchay-mesto-pered-glagolom`. Замысел 9.1, случай 2. Правило называет порядок из трёх мест и
   сравнивает его с русской строкой из вступления: у английского и русского здесь один порядок, и это ученику новое.
   Место `always` после формы be здесь не называется: это урок 4.
4. **Случай 3** `sluchay-sometimes`. Замысел 9.1, случай 3. Место названо без «только»: «встаёт на то же место,
   что и always». Начало строки со словом `sometimes` законно (16.3), его разбирает урок 6, и правило этому не
   противоречит.
5. **Случай 4** `sluchay-glagol-ne-menyaetsya`. Замысел 9.1 с правкой 16.6. Правило написано так, чтобы урок 5
   («He always works») его не опроверг: глагол сравнивается не с «начальной формой», а со строкой без наречия.
   `He works` → `He always works` тоже «в том же виде, в каком стоял без этого слова». Сверено с перечнем уроков
   модуля, как требует CLAUDE.md («правило опровергает соседний урок»).
6. **Случай 5** `sluchay-narechie-chastoty`. Решение владельца от 13 сентября («Называть» одной строкой внутри
   случая) и замысел 4.1. Имя случая по заглушке должно было называть `always` и `sometimes`, но это ключи
   `zvuchat` этого блока, и каждое из них дало бы вторую кнопку в имени и в правиле. Поэтому в имени и правиле
   английских слов нет, а соответствие с русским дано через «всегда» и «иногда» и через то, что в русской грамматике
   это наречия. Термин опирается на знание из школы и не получает отдельного экрана.
   - ПРОВЕРИТЬ: утверждение о русском языке («всегда» и «иногда» относятся к наречиям) верно по школьной грамматике:
     это наречия времени. Уточнения «времени» в тексте нет нарочно, чтобы не заводить второго имени рядом с «наречием частоты».
7. **Случай 6** `sluchay-narechie-i-oborot`. Замысел 9.1, случай 6. «Оборот… стоит в конце строки» повторяет
   правило модуля 13 (урок «Утром и вечером: in the morning»). Имя «оборот о времени» объединяет `in the evening`
   и `at the weekend`, потому что «часть суток» к выходным не подходит.
8. **Случай 7** `sluchay-dve-stroki-ryadom`. Замысел 9.1, случай 7. **Правило из замысла взято не было**:
   «одним словом меняется весь смысл» строки не держат, потому что `I always drink tea.` и `I sometimes drink milk.`
   различаются ДВУМЯ словами, наречием и напитком. Такое правило лгало бы счётом. Правило переписано о том, что
   примеры действительно показывают: два дела и у каждого своё наречие.
   - МЕТОДИСТУ: если замысел хотел пару, где меняется только наречие, нужна другая вторая строка, например с тем же
     `tea`. Но `I sometimes drink tea.` против `I always drink tea.` опасно близко к своду (`I always work.` /
     `I sometimes work.`). Решает методист, английские строки я не трогал.
9. **Подпись свода** `tablica-svod-uroka-1`. Строки таблицы отличаются только наречием, и подпись говорит
   именно это. Ключи `zvuchat` — целые строки, в подпись они не попадают.
10. **З1** `z1-vybrat-bez-isklyucheniy`. Замысел 18.1, строка 1. Условие прямо называет оба определяющих
    факта: «каждое утро» и «без исключений». Сказано «в которой сказано ровно это», а не «без ошибки»: все три
    варианта — верный английский, и неверны они только против условия. Подсказка называет два места для сверки,
    но не называет слов. Разбор объясняет оба отвлекающих по признаку из 18.1.
    - МЕТОДИСТУ: верный вариант стоит первым. В модуле 15 второй редактор назвал это находкой («во всех заданиях
      выбора модуля он стоял первым»). В уроке 1 задание выбора одно, но порядок `options` не мой предмет.
11. **З2** `z2-otmetit-slovo-o-chastote`. Замысел 18.1, строка 2. Условие называет вещь термином из случая 5 и
    словом «все». Сколько строк без наречия, условие не говорит: иначе задание снова проходится счётом.
    Подсказка называет место в строке, но не слово. Разбор называет три отмеченных слова и объясняет третью
    строку по смыслу, без английской строки.
12. **З3** `z3-vpisat-always`. Замысел 18.1, строка 3. Условие: «каждый раз, без исключений». Подсказка
    отсылает к смыслу слова (случай 1), а не к его букве. Разбор оформлен как в модуле 15: «Ответ: … Целиком
    строка выглядит так: …».
13. **З4** `z4-zapisat-oni-inogda-gulyayut`. Замысел 9.1 и 16.6, уговор 8.3: русская фраза в кавычках законна,
    потому что оба верных порядка стоят в `answer` и `accept` (8.3, граница 2).
    - ПРОВЕРИТЬ: разбор говорит «начать строку словом sometimes тоже верно» и ссылается на урок «Иногда — с
      начала: Sometimes I read». Заглушка просила сказать это прямо. Правило CLAUDE.md «разбор не может содержать
      неразобранного» обходится адресом: место не разбирается здесь, а названо и отправлено в свой урок. Урок 6
      в курсе есть, имя взято из `title` дословно, но его текст ещё не написан. Если второй редактор сочтёт ссылку
      на ненаписанный урок недопустимой (уговор 8.5), фразу надо снять целиком, а `accept` оставить.
14. **З5** `z5-sobrat-my-vsegda-gotovim`. Замысел 18.1, строка 5. Условие даёт русскую фразу: выбора здесь нет,
    проверяется порядок (8.3). Подсказка называет место наречия, но не порядок остальных кусков.
15. **З6** `z6-skazat-o-sebe-always-sometimes`. Замысел 18.1, строка 6; уговор 8.3, граница 1: задание «о себе»
    держит положение, русской фразы в кавычках нет. Перевод образца дан по строкам: `drink water` — «пью воду»,
    `play football` — «играю в футбол», `at the weekend` — «в выходные», как в примере случая 3. Разбор кончается
    формулой уговора 8.1 дословно, вместе со второй её фразой («Послушай его и повтори строку следом за ним»).
    В заглушке стояла только первая фраза, а уговор и модуль 15 дают обе.

## Правки после чтения урока целиком

Урок прочитан по видимому тексту (`npm run --silent tekst -- english-beginner-2 kak-chasto`), от вступления до образца З6.

- **З2, подсказка.** Было «Ищи его на этом месте в каждой строке». Так подсказка обещала наречие в каждой строке,
  а в третьей его нет нарочно (замысел 18.1). Стало «Посмотри в каждой строке, какое слово стоит на этом месте».
- **З3, разбор.** «Наречие частоты встало перед глаголом и говорит…»: два времени в одном предложении. Стало «стоит».
- **Случай 7, правило.** Первое предложение прежней редакции («О двух похожих делах говорят двумя строками…»)
  не выдерживало проверки «убери предложение»: ученик ничего бы не потерял. Стало: «Наречие частоты говорит о деле
  из своей строки. Поэтому в двух соседних строках чай пьют каждый раз, а молоко не каждый раз.»

## Прогоны

Все после последней правки, 13 сентября 2026.

- `npx tsc --noEmit` — вывод пустой.
- `npm run --silent check:content 2>&1 | grep "vsegda-i-inogda"` — 13 строк, **все под заголовком «нет записей,
  запусти npm run ozvuchka»**: 11 примеров случаев, одна ячейка свода и образец З6. Меток «ПИШЕТ РЕДАКТОР» в уроке 1
  нет, других ошибок тоже. Звук ставит сводящий.
- `npm run --silent yazyk -- english-beginner-2` — «Ошибок: 0, вопросов: 0», строк о `kak-chasto` нет.
- `npm run --silent perevody -- english-beginner-2 --chego` — `16. kak-chasto 4 / 4 / 3`: четыре строки примеров,
  все четыре с переводом, в списке «Чего не хватает» модуля нет. «Уроков правил начато обобщением: 0 из 16».
  - Что значит «4». Столбец считает строки блоков `example` (`scripts/perevody.mts`, строка 208), а такой блок в
    модуле один: сцена урока 8. Строки объяснений проверяет другая ветка того же скрипта (строка 232, «русского
    рядом нет»), и строк `kak-chasto` в её списке нет. Значит у всех одиннадцати примеров урока 1 перевод стоит
    рядом. Подтверждено чтением скрипта и прогоном `razobrat` (раздел ниже, пункт 5).

## Проверено и чисто

По восьми правилам задания, с признаком проверки.

1. **Английские строки не тронуты.** `git diff -U0` (только чтение): все удалённые строки — строки заглушек или их
   продолжения. Ни одна строка `zvuchat`, `answer`, `accept`, `options`, `parts`, `items`, `phrase` и ни одна пара
   примера не удалена. Замечания о строках ушли пунктами «МЕТОДИСТУ» (случай 7, порядок `options` в З1).
2. **Работа модуля и уроки 2–11 не тронуты.** Последний блок правок в `git diff` стоит на строке 514, а урок 2
   начинается ниже (`slug: "obychno-i-chasto"`). `check:content` по-прежнему называет заглушки урока 2 и дальше,
   и так и должно быть.
3. **«Наречие» названо одной строкой внутри случая, шкалы нет.** Слово «наречие» стоит строкой правила случая 5
   и отдельного экрана не получает. Поиск по русскому тексту урока (`чаще|реже`) — ноль. Ни одна строка не сравнивает
   `always` с `sometimes` по частоте: «каждый раз» и «не каждый раз» — переводы, а не порядок на шкале.
4. **Нет «только», «нельзя», «не бывает».** Поиск по русскому тексту урока (вступление, строки правил, условия,
   подсказки, разборы, подписи, перевод): ноль совпадений. «всегда» встречается 9 раз, и все 9 — само русское слово в
   кавычках, в переводе или в условии, а не утверждение о языке. `sometimes` в начале строки не запрещён: разбор З4
   называет это место верным, `accept` его принимает.
5. **Правило не повторяет строку из `zvuchat`.** Прогон `zvuchashchee()` + `razobrat()` по семи объяснениям урока:
   у каждого имени случая и каждой строки правила проверено вхождение каждого ключа звука. Вторых кнопок 0, строк
   правила, разобранных парой, 0. Отдельно случай 5, где ключи — одиночные `always` и `sometimes`: в имени и правиле их
   нет. Подпись свода ключей не содержит.
6. **Подсказка не называет ответ, условие называет факт.** Прогон: ни одна из шести подсказок не содержит строки
   ответа (для `short` вместе с `accept`), латиницы в подсказках нет вовсе. Факты условий сверены с правым столбцом
   18.1: З1 — «каждое утро, без исключений», З3 — «каждый раз, без исключений», З4 — русская фраза целиком, З6 —
   «всегда» и «иногда» с именами обоих слов.
7. **Нет родовых форм.** Поиск по русскому тексту урока: `сам*`, `готов*` (кроме глагола «готовить»), `уверен*`,
   `долж*`, «ты» + прошедшее время — ноль совпадений. Формула устного задания взята дословно из уговора 8.1.
8. **Команды git, меняющие дерево, не запускались.** Использованы только `git diff` и `git diff --stat`. Временный
   скрипт проверки создан в корне проекта и удалён сразу после прогона.

---

## Правки по разбору и решениям методиста

Первый редактор, 13 сентября 2026. Основание: разбор второго редактора
`docs/beginner-2/priyomka-uroka-1-modul-16.md`, решения методиста
`docs/beginner-2/reshenie-metodista-urok-1-modul-16.md`, правка сводящего `f0595e5`.
Правился только урок 1 (`vsegda-i-inogda`), английские строки не тронуты, уроки 2–11 и
работа модуля не тронуты, команды git не запускались.

### Обязательные

- **О1, подпись свода.** Было: «Одна строка с двумя разными наречиями частоты». Стало:
  «Одна и та же строка сначала со словом always, потом со словом sometimes». От
  предложенного «Та же строка…» отошёл в одном слове: «та же» отсылает к прошлому экрану,
  а «одна и та же» говорит о двух строках таблицы между собой. Кнопок звука подпись не
  даёт: `caption` таблицы идёт через `Shapka` без `sZvukom` (`Ekran.tsx`, строка 256).
- **О2, случай 7.** Закрыта снятием экрана (методист, пункт 1). Проверил окружение:
  вступление случаев не считает и на снятый экран не ссылается; в подписи свода нет ни
  счёта случаев, ни ссылки на соседний экран.
- **О3, две формы термина.** Имя случая 5 было «Слова о частоте называются наречиями»,
  стало «Как называются слова о частоте». Имя «наречие частоты» звучит на экране один раз,
  в правиле. Латиницы в имени нет, значит второй кнопки нет. Косвенный вопрос без знака
  в именах случаев курс уже пишет: «Что меняет слово very», «Что стоит в строке о действии».

### Решения методиста

- **Задание 1, разбор (пункт 2).** Стало: «Верна строка Dana always cooks at home in the
  morning. Вторая строка со словом sometimes говорит, что утром Дана готовит не каждый раз.
  Третья строка расходится с условием дважды. В ней тоже стоит sometimes, и говорит она о
  вечере, потому что в конце стоит in the evening.» Счёт «дважды» сверен с вариантами:
  у третьего два отличия от верного, у второго одно. Подсказка «два места в каждой строке»
  верна для всех трёх вариантов, не менял.
- **Задание 4, разбор (пункт 3).** Ссылка на урок «Иногда — с начала: Sometimes I read»
  оставлена, слово поправлено по У4: «и такой порядок разбирает урок…».
- **Задание 6, формула самопроверки (пункт 4). Принята формула методиста без счёта:**
  «Ответ на это задание не проверяется, поэтому сверься с образцом. Послушай его и повтори
  следом за ним.» По-русски это звучит естественно: «повтори за мной», «повтори за ним»
  обычно говорят без дополнения, и объект ясен из того, за кем повторяют. «Его» и «за ним»
  оба относятся к образцу, двух прочтений нет. Формула верна при любом образце: в одну
  строку, в несколько и разговором. Остальные модули не трогал, правку по курсу решает
  сводящий.

### На усмотрение

- **У1, вступление — внесено.** «По-английски слова с тем же смыслом стоят на том же месте.
  Здесь мы разберём слова always и sometimes и их место в строке.» Единственное число после
  двух слов ушло, заголовок урока и вступление теперь называют одни и те же слова, «стоят» и
  «встаёт» больше не соседствуют. У вступления нет `zvuchat`, кнопок не появится.
- **У2, «в том же виде» — внесено одним предложением.** «Когда в строку добавлено always или
  sometimes, глагол остаётся таким же, каким был в строке без этого слова.» Предложенное
  разбором второе предложение («сам глагол не меняется») не взял: оно говорило то же, что
  первое, и не прошло бы проверку «убери предложение». Проверено против урока 5
  (`He always works`): правило не опровергается.
- **У3, «каждый раз» в условии задания 3 — внесено.** «Дана готовит дома каждый день, без
  исключений.» Подсказка и разбор оставлены с «каждый раз»: они говорят о значении слова
  always (случай 1), а не пересказывают условие. Ответ не меняется.
- **У4 — внесено** (см. выше).
- **У5, заглавная буква в русской фразе задания 5 — внесено:** «„Мы всегда готовим вечером"».
- **У6, «скажи» трижды — внесено иначе, чем в разборе.** Стало: «Скажи вслух о себе две
  строки. В первой поставь always и назови то, что ты делаешь всегда. Во второй поставь
  sometimes и назови то, что ты делаешь иногда.» В предложенном «В первой назови со словом
  always то, что…» слово «то» оторвано от «назови» и фраза спотыкается.
- **У7 — закрыто формулой методиста** (пункт 4), а не заменой на «повтори строки».
- **У8, один глагол для места — внесено в трёх местах:** подсказка задания 5 «встаёт» →
  «стоит»; разборы заданий 5 и 6 «идёт в конце» → «стоит в конце». Оставлены: «встаёт на то
  же место» в случае 3 (там слово ставят в строку, разбор это и советует); «Первым в строке
  идёт слово…» в случае 2 (там речь о порядке слов друг за другом, а не о месте одного).

### Прогоны после правок

- `npm run --silent check:content 2>&1 | grep "vsegda-i-inogda"` — 11 строк, все под
  заголовком «нет записей, запусти npm run ozvuchka» (9 примеров случаев, ячейка свода,
  образец задания 6). До снятия случая 7 было 13: две записи ушли вместе с экраном. Других
  строк об уроке 1 нет. Звук ставит сводящий.
- `npm run --silent yazyk -- english-beginner-2 2>&1 | grep -i "kak-chasto\|Ошибок"` —
  «Ошибок: 0, вопросов: 0», строк о `kak-chasto` нет.
- `npx tsc --noEmit` — без ошибок.
- `npm run --silent tekst -- english-beginner-2 kak-chasto` — урок 1 перечитан целиком,
  от вступления до разбора задания 6, с окружением каждой правки: вступление против
  случаев 1–3; имя случая 5 против его правила; подпись свода против двух строк таблицы;
  разбор задания 1 против трёх вариантов и подсказки; условие задания 3 против подсказки и
  разбора; условие и разбор задания 6 против образца. Расхождений нет.
- Вторые кнопки звука: в новых строках латиница есть во вступлении (нет `zvuchat`), в
  подписи свода (не идёт через `sZvukom`), в правиле случая 4 (`always`, `sometimes` при
  ключах-предложениях `I work.` и т. п., совпадения нет). В имени случая 5, где ключи
  одиночные `always` и `sometimes`, латиницы нет.
- Родовые формы в новых строках: «сам», «готов», прошедшее время при «ты» — нет.

---

## Уроки 2–6

Первый редактор, 13 сентября 2026. Уроки `obychno-i-chasto`, `nikogda`, `ya-vsegda-zanyat`,
`on-vsegda-rabotaet`, `inogda-s-nachala`. Урок 1, уроки 7–11 и работа модуля не трогаются,
английские строки не меняются, команды git, меняющие дерево, не запускаются.

### Ход работы

- Раздел заведён первым действием. Дальше — чтение разбора второго редактора, решений методиста,
  замысла (9.2–9.6, 16.7, 16.8, 16.10, 18 «Как читать таблицы», 18.2–18.6, 18.12) и уговора стиля.
- Прочитаны: разбор второго редактора по уроку 1, решения методиста (пункты 1–4 и попутные А, Б), замысел
  9.2–9.6, 16.7, 16.8, 16.10, 18 «Как читать таблицы», 18.2–18.6, 18.12, уговор стиля целиком. Сверено с курсом:
  модуль 13 зовётся «Что я делаю каждый день», окончания -es и -ies разбирает его урок «Два разных -s: boxes и he
  watches» (итог «писать -es и -ies там, где их пишут»); модуль 10 «Какой человек» зовёт `tired`, `hungry`
  «словом о состоянии» (`kakoy-chelovek.ts`, случай «По-русски „хочу есть", а по-английски слово о состоянии»),
  карточка `hungry` там же — ответ на «ПРОВЕРИТЬ СБОРЩИКУ» в шапке урока 4; `in the afternoon` курс переводит
  «днём».
- Код, от которого зависит текст, перечитан: `sZvukom` (`Ekran.tsx`) ищет ключи `zvuchat` в имени случая и в
  строках правила; `razobrat` делает строку парой, если слева от « — » латиница; подпись таблицы идёт через
  `Shapka` без звука; таблица с `naTomZheEkrane` показывается на экране ПРЕДЫДУЩЕГО объяснения (`UrokShagami.tsx`).

### Урок 2 «Обычно и часто» (`obychno-i-chasto`)

**Заполнено 37 мест** (счёт меток по `git show HEAD`): вступление; имена и правила пяти объяснений (10); перевод
строки в случае 5; подписи двух таблиц; 4 перевода свода; условия, подсказки и разборы шести заданий (18) и перевод
образца `speak`.

1. **Вступление.** Сцена по замыслу 9.2 и 16.7: «всегда» и «иногда» отводятся русскими словами и фактами сцены
   (в выходные иначе; рано почти каждый день), а не порядком на шкале. Русская фраза «я обычно встаю рано» звучит
   (уговор 8.6). Закрывающая строка «Здесь мы разберём…» называет оба слова, как в уроке 1 после У1.
2. **Случай 1.** Имя — русское соответствие первым. Правило в строю урока 1 («Словом always говорят о том, что
   бывает каждый раз, без исключений»): «как правило, но не без исключений» — толкование русского «обычно», с
   `always` оно не сравнивает частоту, а берёт признак «без исключений», который ученик уже знает. Второе
   предложение связывает слово со сценой.
3. **Случай 2.** Место названо тремя шагами, как в случае 2 урока 1; сказано «то же место, что у always и
   sometimes», без «только» и без «всегда».
4. **Случай 3.** `often` — «случается много раз» (замысел: «часто, много раз»). С `usually` не сравнивается.
5. **Случай 5.** Правило замысла стояло с двумя тире («early — рано, late — поздно»): `razobrat` сделал бы из него
   пару «early» / «рано, late — поздно…». Переписано союзом. «Оба стоят в конце строки» — правило замысла, взято
   без «всегда».
   - **Перевод `I sometimes get up late.` — «Я иногда встаю поздно.»** Дословно в замысле его нет; собран из
     перевода карточки `late` («поздно») и строки случая 1 «Я обычно встаю рано». ПРОВЕРИТЬ методисту.
6. **Подпись таблицы четырёх слов:** «Четыре наречия частоты и перевод каждого из них». Порядка по частоте не
   утверждает (16.7), к соседнему экрану не отсылает.
   - **МЕТОДИСТУ / сводящему.** У таблицы стоит `naTomZheEkrane: true`, значит она показывается на экране
     случая 5 «Слова early и late…». Экран выйдет с двумя несвязанными мыслями: слова `early`, `late` и таблица
     четырёх наречий с напитками. Подпись написана так, чтобы держаться сама, но вопрос о привязке не мой.
7. **Случай 7.** Имя — косвенный вопрос без латиницы («Как часто делают домашнюю работу»), как «Как называются
   слова о частоте» в уроке 1. Правило объясняет `do` как глагол «делать»: в модуле 15 `do` было служебным словом
   вопроса, и ученик может спросить, почему наречие стоит перед ним. Слово «оборот» для `do homework` не взял —
   в модуле оно занято «оборотом о времени».
8. **Свод.** Подпись по образцу принятой в уроке 1: «Одна и та же строка с каждым из четырёх наречий частоты»
   (не «одна строка с четырьмя…» — находка О1).
   - **Переводы свода собраны, дословно в замысле их нет** (9.2 называет строки образцом «I … work in the
     morning», 18.12 урок 2 свода не даёт): «Я всегда работаю утром» — дословно 9.1, случай 1; «Я иногда / обычно
     / часто работаю утром» — тот же перевод с переводами наречий из 16.7 и 18.12. ПРОВЕРИТЬ методисту.
9. **З1.** Условие называет оба факта (обычно, дома) и говорит «ровно это»: строка с `often` не ложна о
   Айгерим, она расходится с условием словом. Разбор по пункту 2 решения методиста: второй вариант назван
   «расходится с условием дважды», оба признака (`often` и школа) названы.
10. **З2.** Условие называет термин урока 1 и слово «все», числа строк без наречия не называет. Подсказка
    другая, чем в уроке 1 (там — место в строке): здесь вопрос «как часто?», потому что ловушка строки 3 — слово
    о времени `early`. Разбор объясняет строку 3 переводом.
11. **З3, З4.** Подсказки отсылают к толкованиям случаев 1 и 3, а не к букве слова. Разборы в строю урока 1.
12. **З5.** Разбор `Usually` в начале не называет (правило раздела 18, пункт 3 решения методиста). Подсказка и
    разбор называют окончание: `walks` ученик пишет сам, и без этого шага задание просит больше, чем урок 2
    учит (окончание дал модуль 13).
13. **З6.** Условие в строю урока 1 (после У6). Перевод образца: `in the morning` — «утром», `in the evening` —
    «вечером», как в случаях урока. Формула самопроверки — новая редакция уговора 8.1 дословно.

Прогон: `npm run --silent check:content 2>&1 | grep "kak-chasto → obychno-i-chasto"` — пусто. В общем списке
«нет записей» урок 2 назван строками примеров (звук ставит сводящий), других строк об уроке нет.

### Урок 3 «Никогда» (`nikogda`)

**Заполнено 39 мест** (счёт меток по `git show HEAD`).

1. **Вступление.** Русская фраза первой (замысел 9.3, уговор 8.6: урок стоит на расхождении с ней). «По-английски
   для этого хватает одного слова» — сказано, чего хватает, а не что второго слова быть не может (16.10).
2. **Случай 1.** Имя — соответствие; правило в строю уроков 1 и 2: «Словом never говорят о том, чего не бывает ни
   разу». «Ни разу» — то же слово, что стоит в правом столбце сопоставления З2 (замысел 16.8), поэтому ученик
   соединяет по слову, которое ему уже дали.
3. **Случай 2.** «То же место, что у других наречий частоты» — верно для всех слов модуля, которые стоят перед
   глаголом; `sometimes` в начале строки урок 6 не опровергает, потому что там случай говорит о другом месте, а
   здесь об этом не говорится ничего.
4. **Случай 3 (16.10).** Правило говорит о строках на экране: «В английских строках ниже их смысл передаёт одно
   слово never». Ни «нельзя», ни «не бывает», ни строки с `never` рядом с `don't`. Слово «ниже» — отсылка внутри
   своего экрана, а не к соседнему.
5. **Случай 4.** Имя — две русские фразы, без латиницы. Правило разводит строки по тому, что сказано в каждой,
   без тире: заглушка давала «I don't drink coffee — не пью; …», а это два ключа звука в правиле (вторая кнопка)
   и тире после латиницы (пара у `razobrat`).
   - **Перевод `I don't drink coffee.` — «Я не пью кофе.»** Дословно в замысле нет; собран из перевода соседней
     строки «Я никогда не пью кофе» без «никогда», как свод 18.12 делает с «Я не пою в школе». ПРОВЕРИТЬ методисту.
6. **Подпись таблицы пяти слов:** «Пять наречий частоты и перевод каждого из них» — в строю подписи урока 2,
   порядка не утверждает.
7. **Случай 6.** Правило одним предложением; «как и с другими наречиями частоты» отсылает к правилу, а не к экрану.
   - **Переводы `I never work in the morning.` и `We never swim at the weekend.` собраны:** «Я никогда не работаю
     утром», «Мы никогда не плаваем в выходные». В замысле строк перевода нет (9.3 даёт строки без переводов, 18.12
     этот случай не называет). `swim` — «плавать», как в уроке 1 (`They sometimes swim at the weekend. — Они иногда
     плавают в выходные`). ПРОВЕРИТЬ методисту.
8. **Случай 7 — по пункту 1.4 решения методиста: о различии `never` и `always`, а не о числе строк.** Имя «Чего не
   бывает никогда и что бывает всегда». Правило: слова противоположны по смыслу, как «никогда» и «всегда», и
   каждая строка названа тем, что в ней сказано о себе («я»), а не безличным «пьют» (урок О2 по уроку 1). Тире
   между «а вторая» и «что чай…» стояло в первой редакции и снято: слева латиница, `razobrat` сделал бы пару.
   - «Противоположны по смыслу» — это утверждение о русских «никогда» и «всегда» и о переводах; порядка среди
     пяти слов оно не вводит (16.7).
   - **Перевод `I always drink tea.` — «Я всегда пью чай.»** В нынешнем замысле его нет (снятый случай 7 урока 1
     в коде имел ровно этот перевод, `git show cc3f9cb`). ПРОВЕРИТЬ методисту.
9. **Подпись свода:** «Одна и та же строка сначала с don't, потом с never» — в строю принятой подписи урока 1.
10. **З1.** Условие называет «никогда» и «вечером»; разбор называет оба признака третьего варианта (`always` и
    утро) — пункт 2 решения методиста.
11. **З2 (`match`).** Правый столбец написан сборщиком дословно из 16.8, я его не менял. Подсказка не называет
    слово, а велит его найти; разбор называет смысл каждой пары и признак, по которому она находится.
12. **З3.** Подсказка — толкование случая 1; разбор повторяет правило случая 3.
13. **З4.** Подсказка называет то, ради чего урок: «для „никогда не" хватит одного наречия частоты». Разбор
    называет окончание: `cooks` ученик пишет сам. О строке с `doesn't` разбор молчит: он виден после верного
    ответа, а отвергнутую строку ученик тогда не писал.
14. **З5.** Условие даёт русскую фразу в кавычках (уговор 8.3: выбора нет, проверяется порядок) и число кусков.
15. **З6.** Условие в строю уроков 1 и 2. Перевод образца: `in the afternoon` — «днём», как в курсе
    (`kazhdyy-den.ts`: «Я читаю днём»).

Прогон: `check:content | grep "kak-chasto → nikogda"` — пусто; `npx tsc --noEmit` — пусто.

### Урок 4 «Я всегда занят» (`ya-vsegda-zanyat`)

**Заполнено 37 мест** (счёт меток по `git show HEAD`).

1. **Вступление.** Сцена первой (замысел 9.4), русская фраза «я всегда занят утром» вторым предложением
   (уговор 8.6). «Какой он» из заглушки не взял: владелец уже отверг слово «признак» для `busy` («признак предмету
   присущ, а `hot` и `busy` говорят о состоянии»), а модуль 10 «Какой человек» зовёт `tired` и `hungry` «словом о
   состоянии». Взято его имя. О том, что место наречия после формы be стоит сверх ступени, в тексте ни слова — это
   шапка модуля, а не ученику.
2. **Случай 1.** Имя — соответствие. Правило ставит `busy` в ряд со словами модуля 10 (`tired`, `hungry`; обе
   карточки есть — `kakoy-chelovek.ts`), поэтому место после формы be объяснено известным, а не заявлено.
3. **Случай 2.** Правило о месте и о порядке трёх слов. Без «всегда», «только».
4. **Случай 3.** Первое предложение — правило замысла союзом вместо тире (слева от тире стояла бы латиница
   `be`, и `razobrat` сделал бы пару). Второе даёт новое, чего не видно из правила: и `always`, и «всегда» стоят
   прямо перед `work` / «работаю» и `busy` / «занят», значит разница только в форме `am`, которой в русской строке
   нет. Проверено против примеров экрана.
5. **Случай 4.** «Так же, как после am и is» — отсылка к правилу, а не к экрану.
   - **МЕТОДИСТУ.** Строки случая 4 (`We are sometimes late. — Мы иногда опаздываем.`) стоят раньше случая 5,
     который объясняет, что `late` о человеке переводится глаголом «опаздывать». Ученик увидит перевод
     «опаздываем» на экран раньше объяснения. Порядок случаев не мой; перевод рядом делает место понятным, но
     случаи 4 и 5 стоило бы поменять местами или взять в случай 4 строки без `late`.
6. **Случай 5.** Замысел давал «late здесь значит „опоздал"», а переводы строк того же случая — «опаздывает»,
   «опаздываю». Правило написано по переводам: «по-русски такую строку переводят глаголом „опаздывать"».
   «Уже знакомо в значении „поздно"» — без прошедшего времени при «ты» и без ссылки на номер урока.
7. **Случай 6.** Правило в строю случая 3 урока 3 (два русских слова, одно английское). Строки `never` с отрицанием
   `do` здесь нет, 16.10 не задето.
8. **Подпись таблицы трёх лиц:** «Наречие always после форм am, is и are».
   - **Переводы собраны:** «Я всегда занят» — дословно 9.4, случай 3; «Он всегда занят», «Мы всегда заняты» —
     тот же перевод с другим лицом. В замысле строк перевода нет. ПРОВЕРИТЬ методисту.
9. **Подпись свода:** «Наречие always перед глаголом и после формы be».
   - **МЕТОДИСТУ.** Свод `tablica-svod-uroka-4` повторяет случай 3 целиком: те же две строки (`I always work.`,
     `I am always busy.`) и те же переводы. Та же находка уже чинилась в уроке 3 (18.13: «прежний свод повторял
     таблицу случая 5»). Подписи разведены, но экран выходит дважды.
10. **З1.** Выбор по смыслу (18.4, отступление 1). Условие: «Ким часто опаздывает» и «ровно это». Разбор называет
    оба признака второго варианта: `never` и `busy` (пункт 2 решения методиста).
11. **З2.** Условие называет форму be и «где она есть», числа не называет. Подсказка ведёт через наречие частоты
    к слову перед ним и перечисляет формы be — это дорога: в строке 2 перед `often` стоит `They`, и ученик сам
    видит, что формы be там нет.
12. **З3.** Условие прямо называет, что вписывается форма be (указание сводящего). Подсказка — лицо, а не ответ.
13. **З4.** `Sometimes` в начале разбор называет одной фразой с адресом урока 6 (правило раздела 18, таблица
    пункта 3 решения методиста) — дословно тем же оборотом, что разбор задания 4 урока 1.
14. **З5.** Условие — русская фраза в кавычках (8.3). Кусок `am often` один нарочно (18.4), подсказка о месте
    наречия поэтому не нужна и не дана: она говорила бы о том, что ученик не собирает.
15. **З6.** Замысел: «сказать о себе, каким бываешь часто». «Каким» — родовая форма при «ты»: для ученицы
    «какой». Условие переписано без рода: «назови состояние, в котором ты часто бываешь». Перевод образца
    «Я часто голоден днём» — мужской род у `I`, так курс переводит строки образца везде; об ученике он не
    говорит.

Прогон: `check:content | grep "kak-chasto → ya-vsegda-zanyat"` — пусто; `npx tsc --noEmit` — пусто.

### Урок 5 «Он всегда работает» (`on-vsegda-rabotaet`)

**Заполнено 38 мест** (счёт меток по `git show HEAD`).

1. **Вступление.** Сцена из замысла 9.5 и русская фраза вторым предложением (8.6). «Окончание у глагола своё» из
   заглушки не взял: окончание у обоих одно, `-s`, и фраза обещала бы разницу, которой нет. «Всегда» добавлено в
   сцену, чтобы русская фраза совпала с заголовком урока; о языке оно ничего не утверждает.
2. **Случай 1.** Ссылка на наш модуль по видимому имени — «Что я делаю каждый день» (`kazhdyy-den.ts`, `title`),
   как разрешено заданием. Правило согласовано со случаем 4 урока 1 («глагол остаётся таким же, каким был в строке
   без этого слова»): здесь «окончание остаётся на месте».
3. **Случай 2.** Место названо словами урока 1 («между словом о том, кто делает, и глаголом»).
4. **Случай 3.** Правило говорит, что наречие не меняется в строке о нём и о ней. Это готовит подсказку З2:
   у `always` и `sometimes` на конце тоже буква `s`, и ученик может принять её за окончание.
   - **Переводы собраны:** «Он часто гуляет в парке», «Она часто гуляет в парке» — из перевода `We often walk in
     the park. — Мы часто гуляем в парке` (9.2). В замысле строк перевода нет. ПРОВЕРИТЬ методисту.
5. **Случай 4.** Ссылка на урок модуля 13 полным именем «Два разных -s: boxes и he watches» (уговор 8.5). Вторая
   кнопка звука не появится: ключ блока — целая строка `He never watches television in the morning.`, в имени
   урока её нет. Проверено прогоном (раздел «Проверено и чисто», пункт 6).
6. **Случай 5.** «Имя или слова my brother» — во втором примере стоит не имя, а `My brother`, и правило называет
   оба.
   - **МЕТОДИСТУ, вопрос.** Во втором примере стоит `does his homework`. В модуле 15 `does` было служебным словом
     вопроса, а здесь это глагол «делает». Объяснять это в случае про имя значило бы вести вторую мысль на одном
     экране, и я этого не сделал. Если ученик на этом споткнётся, нужна другая строка или отдельная строка правила.
7. **Случай 6.** Правило сравнивает две строки по окончанию и по наречию. Переводы дословно из замысла: «Я всегда
   работаю утром» (9.1, случай 1), «Он всегда работает утром» (9.5, случай 1).
8. **Подпись таблицы семи строк** написана по заглушке без тире: «При I, you, we и they глагол без окончания, а
   при he, she и имени с окончанием».
9. **Подпись свода:** «В каждой паре строк наречие частоты одно и то же, а окончание стоит во второй». Проверено по
   18.12: во всех трёх парах наречие совпадает, окончание во втором столбце.
10. **З1.** Условие говорит «сказано верно», а не «ровно это»: два варианта неверны грамматически. Разбор называет
    оба признака третьего варианта: окончание и вечер (пункт 2 решения методиста).
11. **З2.** Подсказка: «Буква s на конце самого наречия окончанием не считается» — в строке 4 отмечаемо слово
    `sometimes`. Это утверждение о написании наших слов, а не правило языка. Разбор объясняет строку 2 числом
    людей.
12. **З3.** Условие называет глагол `walk` (18.5). Подсказка — вопрос о числе людей, а не ответ.
13. **З4.** Разбор называет окончание и место наречия.
14. **З5 (`match`).** Условие называет признак пары — то же дело и то же наречие, иначе `I often walk` можно было
    бы соединить с любой строкой. Разбор сначала был одним предложением в 23 слова (`check:content`), разделён
    на два.
15. **З6.** Условие без родовых форм. Перевод образца: `in the afternoon` — «днём».

Прогон: `check:content | grep "kak-chasto → on-vsegda-rabotaet"` — после разделения разбора З5 пусто;
`npx tsc --noEmit` — пусто.

### Урок 6 «Иногда — с начала» (`inogda-s-nachala`)

**Заполнено 39 мест** (счёт меток по `git show HEAD`).

Главное ограничение урока (замысел 9.6, указание сводящего): **ни «только», ни «нельзя», и об `always`, `usually`,
`often`, `never` в начале строки урок не говорит ничего.** Проверено прогоном по всему русскому тексту урока (раздел
«Проверено и чисто», пункт 6).

1. **Вступление.** Русская фраза первой: урок стоит на том, что по-русски «иногда» тоже встаёт в оба места. Названы
   две русские фразы, английское сказано о `sometimes` и больше ни о чём. «По-русски мы часто начинаем с
   „иногда"» из заглушки не взял: «часто» здесь — утверждение о русской речи без опоры, и в модуле о частоте оно
   читалось бы двусмысленно.
2. **Случай 1.** «Можно поставить первым» — правило замысла, разрешение, а не запрет. «Остальные слова идут в
   прежнем порядке» — новое: ученик видит, что двигается одно слово.
3. **Случай 2.** Переводы обеих строк — «Иногда я готовлю дома.», дословно перевод свода 18.12 («перевод один»).
   Правило объясняет, почему перевод один.
4. **Случай 3.** Имя из замысла: «не нужна», а не «нельзя». Правило опирается на русскую строку (после «иногда» в
   начале строки запятую не ставят: здесь это не вводное слово) и на то, что напечатано ниже.
   - ПРОВЕРИТЬ методисту: утверждение о русской пунктуации — моё, по школьной норме; об английской запятой урок
     говорит только «не нужна», как замысел.
5. **Случай 4.** В заглушке правила не было. Написано по примерам: после `sometimes` бывает и имя, и `my father`,
   глагол при них с окончанием, как в уроке 5. «Может стоять», а не «всегда».
6. **Случай 5.** Правило замысла: оборот о времени остаётся в конце.
7. **Случай 6.** Замысел: «Так сразу слышно, что бывает не всегда». Это читалось как утверждение о том, ЗАЧЕМ так
   говорят, и опоры у него нет. Переписано о напечатанном порядке: «первым сказано, что так бывает не каждый раз, а
   уже потом кто и что делает». Имя «Первым сказано, как часто» вместо «зачем начинать с него».
   - **МЕТОДИСТУ.** Переводы 18.12 здесь без притяжательного: `Sometimes my sister sings at home. — Иногда сестра
     поёт дома.`, `my brother` — «брат», а в случае 4 того же урока `my father` — «мой папа». Взял дословно, не
     правил; ученик может спросить, куда делось «моя».
8. **Случай 7.** Правило о смысле двух строк: утром каждый раз, вечером не каждый раз. О месте `always` ничего,
   кроме того, что вторая строка начинается словом `sometimes`: сравнение мест вывело бы к запрету, которого нет.
   - **Перевод `Sometimes I work in the evening.` — «Иногда я работаю вечером.»** собран по образцу переводов урока
     («Иногда я работаю в выходные»); «Я всегда работаю утром» — дословно 9.1. ПРОВЕРИТЬ методисту.
9. **Подпись свода:** «Два места слова sometimes и один перевод на обе строки».
10. **З1.** Разбор называет оба признака третьего варианта: `always` и утро (пункт 2 решения методиста). О месте
    `always` в третьей строке разбор не говорит.
11. **З2 (`match`).** Условие «строка, которая значит то же самое» (16.8). Разбор говорит, что обе строки пары
    верны. Первая редакция разбора (23 слова) разделена.
12. **З3.** Условие прямо говорит, что строка начинается наречием частоты (18.6). Подсказка — толкование
    `sometimes` из урока 1.
13. **З4.** **Условие называет начало строки прямо: «Начни строку наречием частоты»**, иначе `We sometimes watch
    television.` был бы верным ответом, а он не принимается (18.6). Подсказка «слова идут в том же порядке, что в
    русской фразе» верна для этой строки, сверено с ответом.
14. **З5.** Условие велит начать с наречия частоты. Подсказка — заглавная буква и точка: на них 18.6 и держит
    единственный порядок. У сборки урока 7 были те же подсказка и разбор дословно, поэтому заменял вместе с
    кусками урока 6; урок 7 не тронут.
15. **З6.** Условие велит начать каждую строку словом `sometimes`; подсказка — окончание во второй строке.

Прогон: `check:content | grep "kak-chasto → inogda-s-nachala"` — пусто; `npx tsc --noEmit` — пусто.

### Правки после чтения уроков 2–6 целиком

Прочитано по видимому тексту (`npm run --silent tekst -- english-beginner-2 kak-chasto`), от вступления урока 2 до
образца урока 6. Списка «не попало в вывод» в конце файла нет.

- **Урок 3, разбор З2.** «Поэтому пару находит наречие частоты»: наречие ничего не находит. Стало «пара находится
  по наречию частоты».
- **Урок 5, вступление.** «Разберём строки об одном человеке с наречием частоты» читалось как «человек с
  наречием». Стало «разберём, как наречие частоты стоит в строке об одном человеке».
- **Урок 5, подпись свода.** «Окончание стоит во второй»: в таблице пары стоят столбцами. Стало «окончание есть у
  глагола во втором столбце».
- **Вторая форма термина, найдена прогоном (пункт 1 ниже).** Урок 4, случай 2: «следом за наречием» — правило
  стало одним предложением «…наречие частоты стоит после неё, а слово о состоянии идёт следом». Урок 5, случай 2:
  «Место наречия…» → «Место наречия частоты…». Урок 5, подсказка З2: «на конце самого наречия» → «на конце слов
  always и sometimes».
- **Урок 3, случай 7.** Тире «а вторая — что чай…» стояло после латиницы: `razobrat` сделал бы правило парой.
  Снято сразу, прогоном подтверждено (пункт 6).

## Проверено и чисто — уроки 2–6

Все прогоны после последней правки, 13 сентября 2026.

1. **Одно имя термина.** Прогон Python по строкам кода уроков 2–6 без комментариев: всякое «наречи…», за которым не
   идёт «частоты» или английское слово, — 0 (после трёх правок выше). «Слово/слова о частоте» по русскому тексту
   уроков — 0. «Наречие always», «наречием sometimes» при английском слове оставлены: так пишет и принятый урок 1
   (разбор задания 4).
2. **Шкалы нет.** Поиск `чаще|реже|шкал|между ними` по всему русскому тексту уроков (229 кусков: вступления,
   имена, правила, переводы, подписи, условия, подсказки, разборы) — 0. Порядок строк в таблицах четырёх и пяти слов
   оставлен как у сборщика; подписи порядка не называют. `usually` и `often` разведены переводом и толкованием
   русских слов («как правило, но не без исключений», «много раз»), а не друг с другом.
3. **`short` с `accept`.** Урок 2, З5: разбор об `Usually` в начале молчит. Урок 4, З4: разбор называет
   `Sometimes` в начале одной фразой с полным именем урока 6, тем же оборотом, что урок 1. Другие `short` уроков
   2–6 `accept` не имеют.
4. **Разборы выбора.** Пять заданий `choice` сверены с вариантами в коде. У каждого вариант с двумя отличиями назван
   «расходится с условием дважды» (урок 5 — «неверна дважды»), и оба признака названы: урок 2 — `often` и школа;
   урок 3 — `always` и утро; урок 4 — `never` и `busy`; урок 5 — нет окончания и вечер; урок 6 — `always` и утро.
5. **Формула самопроверки.** Прогон по пяти `speak`: `why` кончается формулой уговора 8.1 дословно — 5 из 5.
6. **Подписи и правила не отсылают к соседнему экрану и не повторяют строку из `zvuchat`.** Прогон `tsx` через
   `zvuchashchee()` и `razobrat()` по 30 объяснениям уроков 2–6: ключ звука в имени случая — 0, в строке правила —
   0; ключей без места в английской половине пары — 0; строк правила с « — » — 0; правил, ставших парой, — 0.
   Подписи таблиц через `sZvukom` не идут. Поиск `та же|тот же|этот экран|выше|предыдущ|соседн` по русскому
   тексту: три совпадения, все законны — «одна и та же строка» в подписях двух сводов говорит о строках своей
   таблицы, «один и тот же» в разборе урока 5 — о глаголе в паре. «Ниже» в правилах урока 3 (случай 3) и урока 6
   (случай 3) отсылает к примерам своего экрана.
   - Там же: «только», «нельзя», «невозможн», «обязательн», «лишь» — 0. «Не бывает» — 4 места урока 3, все
     толкуют русское «никогда» («чего не бывает ни разу»), а не запрещают. `never` рядом с `don't` или `doesn't` —
     0.
   - Родовые формы (`сам`, `уверен`, `должен`, `готов`, `рад`, `согласен`) и «ты + прошедшее» — 0. Условие З6 урока
     4 из замысла («каким бываешь») переписано без рода.
7. **Английские строки не тронуты.** `git diff -U0` (только чтение): удалено 215 строк, из них 190 с меткой «ПИШЕТ
   РЕДАКТОР» и 25 — продолжения тех же заглушек (прочитаны все). Ни одна строка `options`, `items`, `answer`,
   `accept`, `phrase`, `parts`, `left`, `right`, `zvuchat` не удалена. 21 английская половина пары и строки
   таблицы, где заполнялся перевод, сохранена побуквенно (сверка прогоном).
8. **Урок 1, уроки 7–11 и работа не тронуты.** Все блоки правок `git diff` лежат между строками 531 и 1709 прежнего
   файла: урок 2 начинается выше 531, урок 7 — на 1716. Заглушки урока 7 с теми же словами остались на месте.
9. **Прогоны.** `npm run --silent check:content 2>&1 | grep "kak-chasto → <урок>"` по пяти урокам — пусто (строки
   «нет записей» у этих уроков стоят в общем списке без такого префикса; звук ставит сводящий).
   `npm run --silent yazyk -- english-beginner-2` — «Ошибок: 0, вопросов: 0», строк о `kak-chasto` нет.
   `npx tsc --noEmit` — пусто. Команды git, меняющие дерево, не запускались; вспомогательные скрипты лежат в
   рабочей папке сессии, не в проекте.

## Уроки 2–6: правки по разбору и решениям методиста

Основа: коммит `fd5d1ab`, разбор `priyomka-urokov-2-6-modul-16.md`, решения `reshenie-metodista-uroki-2-6-modul-16.md`.
Раздел пишется по ходу работы, 14 сентября 2026.

### Урок 2 — сделано прошлым заходом (коммит `2798947`, по `git show HEAD`)

Заход был срезан лимитом и журнала не записал; восстановлено по разнице коммита.

- **Вступление (В5, Л2-У1).** Предложение об «иногда» снято; сцена отводит только «всегда» и выводит «обычно»
  союзом «поэтому».
- **Случай 1 (Л2-У2).** «не без исключений» → «с исключениями».
- **Случай 5 (2.1, В8.2, Л2-У5).** «Оба слова стоят в конце строки» → «В обеих строках ниже эти слова стоят в
  конце, после глагола»: правило о своих строках, а не о языке.
- **Случай 7 (Л2-У3).** Имя «Как часто делают домашнюю работу» → «Слово do значит «делать»»; правило называет
  `Do you work?` модуля 15, где `do` стоит первым и не переводится, и выводит место наречия союзом «поэтому».
- **З1 (Л2-У4, О-общ).** Подсказка — «Условие называет две вещи: как часто и где»; разбор без рамки «расходится
  с условием дважды».
- **З2.** Разбор без «поэтому отмечать нечего».
- **З3 (Л2-О1, О-общ).** Подсказка без «Вспомни, каким словом…»; разбор начинается целой строкой, подлежащее —
  «Слово usually», а не «Наречие частоты».
- **З4 (Л2-О1).** Подсказка — к словам урока; подлежащее разбора — «Слово often», без «Целиком строка выглядит
  так».
- **З5 (В2).** Заглушки заполнены: условие, подсказка, разбор о `They usually read newspapers.`; `Usually` в
  начале разбор не называет.
- **З6.** Разбор: «Слова usually и often стоят сразу после I, а часть суток названа в конце строки».

### Урок 2 — доведено этим заходом

Сверено со всеми пунктами разбора и решений об уроке 2 (Л2-О1, Л2-У1–У5, О-общ, 2.1, 2.2, 2.3, В2, В5, В8.1–В8.3).
Не внесённого по существу не нашлось; поправлено два места слога.

- **З4, подсказка.** «Загляни в слова урока: там у каждого…» — двоеточие вместо союза. Стало два предложения.
- **З5, подсказка.** «между словом «они» и глаголом» ставило русское слово в английскую строку. Стало: слова идут
  в том же порядке, что в русской фразе, а «газета» (по-русски, без английского слова карточки) — во
  множественном числе.
- **Не моё, записано сводящему:** шапка урока 2 в коде (строки 514–516) держит довод «sometimes тоже не может —
  так бывает куда чаще, чем иногда», снятый из вступления решением В5. Комментарий правит сводящий.

### Урок 3

- **Вступление (В6.1, Л3-О1, путь 1).** Осталась русская фраза «я никогда не пью кофе» и обещание: как сказать это
  словом never и где оно стоит. Утверждение «по-английски хватает одного слова» ушло в случай 3.
- **Случай 3, правило (заглушка, В6.1).** «В каждой русской строке ниже стоят два слова, «никогда» и «не». В
  английской строке рядом их смысл передаёт одно слово never.» Строки `I never swim.`, `They never dance.` — по три
  слова против четырёх русских, правило о них и говорит. Тире в правиле нет, английских слов, кроме never, нет.
- **Соседство снятого случая 6.** За таблицей пяти слов теперь стоит случай 7; ни он, ни свод на снятое не
  ссылаются. Задания с оборотом в конце (З4, З5) опираются на случай 1 и урок 1 — отдельного правила не нужно.
- **Случай 7 (Л3-У2).** «кофе я не пью ни разу, а вторая говорит…» → два предложения, «не пью совсем».
- **З1 (О-общ).** Разбор без «расходится с условием дважды»: always в двух строках противоположно never, а строка
  с in the morning вдобавок говорит об утре.
- **З3 (О-общ).** Подсказка без «Вспомни, каким словом»: в пропуск встаёт одно слово на всё «никогда не». Разбор
  без «Ответ: … Целиком строка выглядит так»: перед «смотрит» два русских слова, перед watches одно never.
- **З4 (заглушки, В2).** Условие — «Запиши по-английски «Они никогда не занимаются днём»». Подсказка — дорога:
  глагол для «занимаются», перед ним наречие, «днём» в конце. Разбор называет строку и место never и оборота.
  Фразы про окончание нет: строка о they.
- **З5 (О-общ).** Подсказка о единственном выборе среди кусков — television за глаголом; разбор без рамки
  «Собралась строка… Первым стоит…»: порядок как в русской фразе, вместо «никогда не» одно never.
- **З6.** Подсказка «Для «никогда не» хватит одного слова never» оставлена: после В2 она в модуле одна.

### Урок 4

- **Вступление (Л4-У4).** Снято «По-английски слово о состоянии стоит после формы be»: оно отдавало правило случая
  1 до случая.
- **Случай о `late` (4.1).** «После формы be оно говорит о человеке» — сказано о языке, а `It is late.` законен.
  Стало: «В строке о человеке оно стоит после формы be, и по-русски такую строку переводят глаголом «опаздывать»».
  Порядок после перестановки сводящего прочитан: случай о `late` стоит перед случаем `are`, правило `are` («так же,
  как после am и is») опирается на всё выше.
- **Соседство снятого случая 6 (В8.4).** За случаем `are` сразу таблица трёх лиц; её подпись «Наречие always после
  форм am, is и are» на снятое не ссылается. «Два слова, «никогда» и «не»» в уроке больше не звучит.
- **Свод (заглушки, 4.3).** Подпись «Наречие usually перед глаголом и после формы be», заголовки «С глаголом», «С
  формой be», «Перевод пары» — как у свода урока 5.
- **З1 (О-общ).** Разбор без «расходится с условием дважды»: верная строка объяснена через `late` после `is`, две
  другие — never, и в одной вместо late стоит busy.
- **З2 (О-общ).** Разбор без «поэтому отмечать там нечего».
- **З3 (О-общ).** Разбор начинается целой строкой, без «Ответ: … Целиком строка выглядит так».
- **З4 (Л4-У2, О-общ).** Подсказка не называет `are`: «Сначала выбери форму be для слова «они»». Взята не
  предложенная вторым редактором «Вспомни, какая форма be стоит при they» — она дословно повторяла начало подсказки
  З3 («Вспомни, какая форма be стоит при he»).
- **З5 (Л4-У1, О-общ).** Подсказка о единственном выборе — месте busy; разбор без «Собралась строка… Первым
  стоит…», через порядок русской фразы «часто занят».
- **З6 (Л4-О1).** «Во второй назови человека» → «Во второй поставь never и скажи о человеке, который никогда не
  опаздывает».

### Урок 5

- **Вступление (Л5-О2).** Снято «В английской строке об одном человеке у глагола есть окончание -s»: то же говорит
  случай 1 со ссылкой на модуль 13. Обещание — «как сказать это по-английски, когда в строке об одном человеке есть
  наречие частоты».
- **Случай 1 (Л5-О2, слог).** «окончание остаётся на месте» → «В строке с наречием частоты у глагола тоже есть это
  окончание»: сказано, что есть.
- **Соседство снятых случаев 2 и 3 (В7.1, В7.2).** За случаем 1 теперь идёт случай `-es` и `-ies`; ни он, ни случай
  об имени на снятое не опираются.
- **Случай `-es`/`-ies` (Л5-О2, Л5-У1).** Условие названо: «Когда речь об одном человеке, глагол watch пишется
  watches, а study пишется studies». Ссылка называет модуль и урок: «в модуле «Что я делаю каждый день», урок «Два
  разных -s: boxes и he watches»». Отрицание «не меняет» заменено: «Наречие частоты ставим перед ними так же, как
  перед остальными глаголами».
- **Случай об имени (Л5-У2).** «стоит перед ним на прежнем месте» → «стоит перед глаголом, как и в строках с he и
  she»: «ним» больше не тянется к «окончанием».
- **Случай 6 (заглушки, В7.3).** Имя «Буква s на конце always и окончание глагола». Правило: always одно и то же в
  обеих строках, буква s есть даже в строке с I, значит это буква самого слова; окончание -s есть у eats в строке с
  he; у sometimes буква s тоже своя. «В строке о нём» не написано нигде (В7.4, порода Л5-О1).
- **З1 (О-общ).** Разбор без «неверна дважды»: верная строка через «брат один», две другие — drink без окончания,
  строка с in the evening ещё и о вечере.
- **З2 (В7.3, пункт 6 задания).** Подсказка сверена с нынешними строками: в них стоят often, usually, never,
  sometimes, а always нет. Прежняя «Буква s на конце слов always и sometimes» называла слово, которого в задании нет;
  стала «Буква s на конце sometimes принадлежит самому слову». Разбор без «отмечать там нечего».
- **З3 (О-общ).** Разбор начинается целой строкой, без «Ответ: … Целиком».
- **З4.** Не менялось: после В2 подсказка «Начни с имени… Дана одна, поэтому…» в модуле одна.
- **З5 (заглушки, В3).** Условие «…со строкой о другом человеке, где сказано о том же деле» — метод поиска в условии
  не назван, его даёт подсказка («наречие во всех строках одно, поэтому пару находит глагол»). Разбор называет три
  пары и оба вида окончания (-ies у study, -s у swim и walk). Прежний разбор с `read, walk и drink` снят вместе с
  прежними строками.
- **З6 (О-общ).** Подсказка и разбор без «речь об одном человеке, поэтому у глагола будет окончание» — эта фраза
  стояла и в уроке 6.

### Урок 6

- **Вступление (В1).** Снято «и обе строки значат одно и то же»; осталось: по-русски «иногда» ставят и так, и так,
  по-английски sometimes тоже.
- **Случай 2 (заглушка, В1, Л6-О1).** «Обе строки ниже верны. Слова в них одни и те же, а место у sometimes разное.
  В русском переводе слово «иногда» стоит там же, где sometimes в английской строке.» Ни «значат одно и то же», ни
  «поэтому перевод один».
- **Случай 3 (6.1).** Правило о напечатанном: «В русских строках ниже после «иногда» запятой нет, и в английских
  после sometimes её тоже нет». Утверждения о норме русской пунктуации больше нет. Имя «Запятая после sometimes не
  нужна» оставлено, как решил методист.
- **Случай 4 (Л6-У4).** «может стоять и имя или слова» → «может стоять имя или слова».
- **Соседство снятого случая 6 (В8.5).** За случаем об обороте времени идёт «Две строки о работе рядом», дальше свод;
  на снятое никто не ссылается.
- **Свод (заглушки, В1, Л6-У5).** Подпись «Два места слова sometimes, и у каждой строки свой перевод»; заголовки «Перед
  глаголом», «В начале строки», «Перевод пары» — ячейки держат целые строки, а не слово.
- **З1 (О-общ).** Разбор по предложению второго редактора, без «расходится с условием дважды».
- **З2 (заглушки, В4).** Условие «Соедини каждую английскую строку с её переводом». Подсказка — начало у трёх строк
  одно, пару подскажет глагол после I. Разбор называет три пары и то, что «иногда» стоит первым, как sometimes.
- **З3 (О-общ).** Подсказка без «Вспомни, каким словом»: слово значит «иногда» и открывает строку, поэтому с
  заглавной. Разбор без «Ответ: … Целиком».
- **З4 (Л6-О1).** Подсказка о порядке («в том же порядке, что в русской фразе») после В1 правде не противоречит, но
  перенесена в урок 2 (З5), где нужна дорога к порядку. Здесь — «Начни со слова, которое значит «иногда». За ним идут
  слово о том, кто смотрит, и глагол».
- **З5 (Л6-У3, О-общ).** Подсказка не о знаках, а о языке; разбор без «Собралась строка… Первым стоит…»: sometimes
  перешло в начало, остальное в прежнем порядке.
- **З6 (О-общ).** Подсказка по предложению второго редактора; разбор — «бабушка одна», без общей с уроком 5 фразы.
- **Л6-У1** решён методистом (В4) сменой правого столбца; подсказка теперь есть.

### От чего отказался и что осталось

- **Л4-У2, дословная формула второго редактора** («Вспомни, какая форма be стоит при they») не взята: она повторяла
  подсказку З3 того же урока. Смысл пункта (не называть `are`) исполнен.
- **Л6-О1, подсказка З4 «Начни со слова «иногда», дальше — …»** взята без тире.
- **Урок 6, случай 1** отчасти повторяет вступление («sometimes можно поставить первым»); оставлен: у случая есть
  своё «остальные слова идут в прежнем порядке» и примеры, у вступления — русская параллель.
- **Урок 2, подсказки З3 и З4** обе отсылают искать перевод (таблица, словарь). Места разные, рамки разные; оставил.
- **Урок 3, случай 7**: «ни разу» → «совсем» по Л3-У2. Сопоставление З2 того же урока держит «ни разу» подписью к
  строке — это не противоречие, а два слова об одном.
- **СВОДЯЩЕМУ (комментарии, не видимый текст):** шапка урока 2 — довод «так бывает куда чаще, чем иногда» (В5);
  шапка урока 4 — «ПРОВЕРИТЬ СБОРЩИКУ: карточка hungry в строке случая 6» (В8.4, случай снят, карточка есть);
  комментарий «СЛУЧАЙ 7. Перевода замысел не дал» у урока 3 и «Переводов замысел не дал — заглушки» у таблицы трёх
  лиц урока 4 устарели.
- **Английские строки не менял.** Негодных не нашёл, «МЕТОДИСТУ» нет.

### Проверено и чисто — после правок этого захода, 14 сентября 2026

1. **Заглушки.** В уроках 2–6 «ПИШЕТ РЕДАКТОР» — 0 (было 17: урок 3 — 4, урок 4 — 2, урок 5 — 5, урок 6 — 6).
2. **Шаблон (О-общ).** `grep` по строкам кода уроков 2–6: «Целиком строка выглядит так» — 0; «Вспомни, каким словом»
   — 0; «дважды» в разборах — 0; «отмечать там нечего» — 0; «Собралась строка», «Первым стоит» — 0; «поэтому у
   глагола будет окончание» — 1 (урок 5, З4, после В2 единственная). «Ответ: X.» — 1 (урок 2, З4). «к тому же» — 1
   (урок 2, З1). В уроке 1 эти рамки остаются — урок 1 не мой.
3. **«значат одно и то же»** в уроках 2–6 — 0. **«о нём»** в видимом тексте уроков 2–6 — 0.
4. **Двоеточие вместо союза** в подсказках и разборах уроков 2–6 — 0. Двоеточий по видимому тексту четыре, и все
   законны: два перед перечнем (урок 2, подсказка З1 «две вещи: как часто… и где»; урок 5, разбор З5 «Пары такие:»),
   одно внутри имени урока 6 (разбор З4 урока 4) и одно в рамке «Ответ: often.» (урок 2, З4). Первая редакция этого
   пункта называла одно — проверено прогоном по выводу `tekst` и поправлено.
5. **Английские строки и поля заданий не тронуты.** `git diff -U0 HEAD`: среди удалённых строк нет ни одной с
   `options`, `items`, `answer`, `accept`, `phrase`, `parts`, `left`, `right`, `rows`, `zvuchat`, `correct`.
6. **Урок 1, уроки 7–11 и работа не тронуты.** Все куски `git diff` лежат в строках 778–1773 нового файла; урок 2
   начинается на 525, урок 7 — на 1781.
7. **Видимый текст перечитан целиком** по `npm run --silent tekst -- english-beginner-2 kak-chasto` (вывод в рабочей
   папке сессии, уроки 2–6 — строки 266–1370). Списка «не попало в вывод» нет. После чтения поправлено два места:
   урок 3, разбор З5 («а утро названо в самом конце» — лишнее, в русской фразе утро тоже в конце); урок 5, вступление.
8. **Тире и вторые кнопки звука.** В новых правилах случаев тире нет. Английские слова в правилах — never, always,
   sometimes, eats, watch, watches, study, studies, my father, `Do you work?`, имя урока модуля 13; ни одно из них не
   совпадает подстрокой с ключами `zvuchat` своих блоков (ключи — целые строки с точкой).
9. **Прогоны.** `check:content | grep "kak-chasto → (пять уроков)"` — пусто (первый прогон назвал разбор З5 урока 6 в
   26 слов; разделён). `yazyk -- english-beginner-2` — «Ошибок: 0, вопросов: 0». `npx tsc --noEmit` — пусто.
   `npm run sosedi` — «правок, у которых сосед остался прежним, не нашлось».


---

## Уроки 7–11 и работа

Первый редактор, 14 сентября 2026. Основа — коммит `52efdaf` (решения методиста к урокам 7–11 внесены сводящим).
Уроки 1–6 не трогаются, английские строки не меняются, `sources` и `resheno.ts` не трогаются, команды git, меняющие
дерево, не запускаются. Раздел пишется по ходу работы.

### Ход работы

- Раздел заведён первым действием. Прочитаны: журнал целиком, решения методиста по урокам 7–11 и работе.
- Прочитаны также: разбор второго редактора по урокам 2–6, уговор стиля целиком, замысел 9.7–9.11, 16.2, 18 («Как
  читать таблицы», 18.7–18.12б), код уроков 7–11 и работы в нынешнем виде, принятый текст урока 6. Сверено с курсом:
  модуль 13 «Что я делаю каждый день» вводит `every day` в уроке «Утром и вечером: in the morning» с переводом «Я учу
  английский каждый день»; модуль 15 зовётся «Спросить о делах»; карточка `cousin` — «двоюродный брат, двоюродная
  сестра», `week` — «неделя»; `My name is` курс переводит «Меня зовут»; заголовки записок — «Записка от Кима».
- **Конфликт, найденный до письма.** Заглушка разбора вопроса 6 работы велит сказать, «что смысл от места не
  меняется». Решение В1 (`reshenie-metodista-uroki-2-6-modul-16.md`) сняло ровно это утверждение из урока 6: графы о
  том, что строки с `sometimes` в начале и в середине значат одно и то же, у источника нет, а графа FOCUS (B1) даёт
  начало строки ещё и средством выделения. Разбор вопроса 6 пишу о порядке слов, без утверждения о смысле.
  МЕТОДИСТУ — пункт в конце раздела.

### Урок 7 «Каждую неделю» (`kazhduyu-nedelyu`)

**Заполнено 38 мест:** вступление; имена и правила шести объяснений (12); переводы строк в случаях 1, 4, 6, 7 (7);
подписи двух таблиц и три перевода таблицы оборотов; условия, подсказки и разборы шести заданий (17, у сетки ещё пять
подписей слов) и перевод образца.

1. **Вступление — решение 3.4.1.** «Слова «часто» и «иногда» не говорят, сколько времени проходит между встречами, а
   слова «каждый месяц» говорят». Ни «не часто и не иногда», ни сравнения по частоте. Русская фраза «каждый месяц»
   звучит, закрывающая строка «Здесь мы разберём…».
2. **Случай 1.** Имя ссылается на модуль по видимому имени: «Оборот every day из модуля «Что я делаю каждый день»».
   Правило даёт то, чего модуль 13 не говорил: `every` значит «каждый». Перевод первой строки — дословно модуль 13
   («Я учу английский каждый день»).
3. **Случаи 2 и 3.** Правило «к every прибавляется слово времени» сказано как «вместо day после every можно поставить
   другое слово о времени». Правила двух случаев построены по-разному, чтобы не читались одной рамкой.
4. **Случай 4.** Правило связывает место оборота с `in the evening` урока 1 («Оборот с every стоит после них, как
   оборот о времени in the evening»). Переводы: «Я читаю газету каждую неделю», «Она готовит каждый день».
5. **Таблица оборотов.** Подпись «После every слово о времени стоит в единственном числе» — заглушка говорила «одно
   слово: every day», а отвлекающие задания 1 (`every days`, `every weeks`) и вопрос 7 работы стоят именно на числе.
   Термин «единственное число» ученик знает по русскому языку. Тем же словом говорит разбор вопроса 7.
6. **Случай 6 — решение 3.4.4.** «Слово always не говорит, сколько времени проходит между двумя разами. Оборот every
   week говорит, что между ними проходит неделя.» «Без исключений» не противопоставлено, о частоте ни слова. Первая
   редакция стояла с двоеточием вместо союза и повтором «от одного раза до другого» — переписана сразу.
7. **Случай 7.** Две строки о бабушке разведены местом (often перед глаголом, every month в конце) и переводом
   («часто» и «каждый месяц»). Порядка по частоте нет.
8. **Свод.** Подпись по 3.4.2: «Оборот с every стоит в конце строки, и после every одно слово в единственном числе».
9. **З1.** Условие «сказано верно»: два варианта неверны по форме. Разбор называет признак обоих неверных (окончание
   после every) и второй признак строки с `every days` (дни). Порядок вариантов сверен: days, weeks, week — верный.
10. **З2 (сетка).** Условие в строю сеток модулей 13 и 14 (читается слева направо или сверху вниз, отметить все
    буквы) — рамку задаёт вид. Подписи слов — переводы карточек. Подсказка — как искать, а не где лежит слово: у
    модуля 13 подсказка «Два слова лежат по строке, а три по столбцу», и повторять эту рамку не стал. Разбор назвал
    расклад по строкам и столбцам по комментарию сборщика (ALWAYS, NEVER — столбцы) — ПРОВЕРИТЬ по полю.
11. **З3.** Условие «каждый месяц» определяет ответ; подсказка о числе, а не о слове.
12. **З4.** Подсказка отсылает к словам урока (карточка `see`), не называя глагол. Разбор о `Every week I…` и
    `I see friends…` молчит (правило 3 решения по уроку 1).
13. **З5.** Условие — русская фраза в кавычках (8.3: выбора нет). Подсказка отсылает к строке случая 4 с тем же
    порядком (`I read a newspaper every week`). Заглавная и точка не названы — замечание Л4-У1 и Л6-У3.
14. **З6.** Условие «скажи» один раз (У6 урока 1). Перевод «Мой брат пьёт сок каждый день» — `juice` в курсе «сок».
    Формула самопроверки дословно.
    - **Сверено прогоном по полю** (`node`, строки `stroki` сетки): ALWAYS — столбец 9, NEVER — столбец 6, USUALLY —
      строка 9, MONTH — строка 1, MAGAZINE — строка 3. Разбор верен, «ПРОВЕРИТЬ» снято.

### Урок 8 «Спрашиваю» (`sprashivayu-o-chastote`)

**Заполнено 37 мест:** вступление; имена и правила пяти объяснений (10); переводы строк в случаях 1, 3, 4 (4);
подпись, четыре перевода и разбор разговора; подписи двух таблиц; условия, подсказки и разборы шести заданий (18) и
перевод образца.

1. **Вступление.** Сцена первой (замысел 9.8), русская фраза «ты всегда работаешь утром?» вторым предложением (8.6).
   Сцена сказана так, чтобы не спорить с разговором случая 6: Ким сначала спрашивает, работает ли Дана утром, и
   только потом — каждое ли утро.
2. **Случай 1.** Имя ссылается на модуль по видимому имени «Спросить о делах» (`sprosit-o-delah.ts`, `title`).
   Правило повторяет устройство вопроса одним предложением, без «такой» — имя случая ученик держать не обязан.
3. **Случай 2.** «Наречие частоты стоит на прежнем месте, между словом о том, кто делает, и глаголом» — слова уроков
   1 и 5. «Утверждение» из заглушки не взял: в модуле такого имени нет.
4. **Случай 3.** Переводы коротких ответов «Да.» и «Нет.» — как в своде урока 8 и в модуле 15 («Do you work? — Yes, I
   do. — Ты работаешь? — Да.»). Строка с двумя тире разбирается верно: `razobrat` пропускает тире перед чисто
   английским куском (CLAUDE.md, раздел о тире).
5. **Случай 4.** Правило говорит, что есть: короткий ответ даёт «да» или «нет», строка с наречием — как часто.
   Перевод «Нет. Я иногда готовлю дома.» — порядок по английской строке (решение В1).
6. **Случай 5 (строка 3.6.1).** Правило называет they и your friends — обе строки случая о нескольких людях. «Глагол
   без окончания» сказано о своих строках.
7. **Разговор.** Подпись в строю модуля 15: «Ким спрашивает Дану о работе. Первым говорит Ким.» Голос сверен с полем
   `pervyyGolos: "muzhskoy"`. Перевод четвёртой реплики — «Нет. Я иногда работаю вечером.», по порядку слов. Разбор
   пересказывает ход разговора одной линией: вопрос, «да», уточнение с always, «нет» и строка о вечере.
8. **Таблицы.** Подпись порядка «В вопросе сначала идёт do, потом слово о том, кто делает, наречие частоты и
   глагол»; свод — «Вопрос с наречием частоты и два коротких ответа на него».
9. **З1.** Условие называет «часто» и Дану; разбор называет оба признака: always и лишнее окончание в двух других.
   Сверено с вариантами: always drinks, often drink — верный, often drinks.
10. **З2.** Условие «все наречия частоты», числа нет. Разбор: usually, sometimes, always; во втором вопросе за they
    сразу play. Сверено с `parts`.
11. **З3.** Подсказка называет место пропуска, а не слово; слово задаёт условие («всегда»).
12. **З4.** Подсказка отсылает к вопросам урока; разбор объясняет you («на «ты»») и место usually.
13. **З5 (пункт 4 решения).** Условие «Собери вопрос из четырёх кусков» о месте наречия молчит. Подсказка отсылает к
    строке случая 2. Разбор объясняет единственное решение сборки — `often swim` после you — без рамки «Первым стоит».
14. **З6.** Условие «друг … он» и «ответь за него «да»» — родовые формы о друге, не об ученике. Перевод «Ты обычно
    пьёшь молоко вечером? — Да.» Формула дословно.

### Продолжение после обрыва лимитом — 15 сентября 2026

- Прошлый заход сохранён коммитом `958bf2b`: уроки 7 и 8 заполнены и записаны выше; урок 9 заполнен, кроме перевода
  образца `speak`, и в журнал не попал. Ниже он записан по коду, прочитанному в нынешнем виде.
- Прочитаны заново: этот журнал целиком, решения методиста по урокам 7–11, замысел 9.9–9.11, 16.2, 18.9–18.11,
  18.12б, уговор стиля (6–8), урок 10 и работа модуля 15 как соседний образец, задания урока 8 (чтобы не повторить
  их рамки). Уроки 1–8 не трогаются.

### Урок 9 «Читаем: неделя Айгерим» (`chitaem-nedelya-aygerim`)

**Заполнено прошлым заходом (по коду, коммит `958bf2b`):** вступление, условия, подсказки и разборы пяти заданий.
**Этим заходом — одно место:** перевод образца `speak`.

1. **Вступление.** Сцена первой: «Айгерим пишет записку о себе…», закрывающая строка «Здесь мы разберём…».
2. **З1 (отметка).** Условие «все наречия частоты», числа нет. Разбор называет `every month` оборотом, который
   называет срок, — тем же словом, что урок 7.
3. **З2 (выбор, решение 3.5.1).** Разбор по заданию: «Плавает Айгерим иногда и в выходные, а чай пьёт часто и утром.
   Каждый месяц она видится с бабушкой». Сверено с вариантами в коде: `She swims at the weekend.`, `She drinks tea in
   the morning.`
4. **З3–З4.** Подсказки — дорога к строке записки, а не слово. Разбор З4 называет смену `study` на `studies` в строке
   об Айгерим.
5. **З5, перевод образца.** «Здравствуй! Меня зовут Ким. Я обычно встаю поздно. Я никогда не пью молоко вечером.»
   Опоры: карточка `hello` — «здравствуй, здравствуйте» (модуль 1); `My name is` — «Меня зовут»; строка урока 2
   `I sometimes get up late. — Я иногда встаю поздно.`; «Здравствуй», а не «Привет»: «Привет» курс держит за `Hi`
   (модуль 2). Разбор З5 уже кончается формулой 8.1 дословно.

### Урок 10 «Слушаем: как часто» (`slushaem-kak-chasto`)

**Заполнено 18 мест:** вступление; подписи двух записей; условия, подсказки и разборы пяти заданий.

1. **Ответы нигде рядом не стоят (замысел 18.10).** `never`, `magazine`, `early`, `weekend`, `usually` и их русские
   слова («никогда», «журнал», «рано», «выходные», «обычно») не стоят ни во вступлении, ни в подписях. Разбор каждого
   задания называет только свой ответ и цитирует только своё предложение записи. Разбор З1 не цитирует второе
   предложение ответа (там `magazine` для З2). Разборы З3 и З4 не цитируют предложение с `usually` (ответ З5).
   Разбор З5 стоит последним в уроке и называет всё.
2. **Подпись второй записи** стоит в коде после заданий к первой (порядок сборщика) и говорит только «Нурлан
   рассказывает о своей работе. В записи говорит он один.» — никаких слов о днях, утре и о том, когда он встаёт.
3. **Голоса.** Подпись 1 «Первым говорит Ким» сверена с полем `pervyyGolos: "muzhskoy"`. З1 зовёт вторую говорящую
   «знакомая Кима», как подпись; З2 — «она» (решение 3.7.1), без имени.
4. **Вступление.** Сцена первой (оба человека и их дело), русская фраза вторым предложением («Ты часто читаешь?»),
   закрывающая строка «Здесь мы разберём…».
5. **З3.** Условие «Когда встаёт Нурлан?» не называет «рано» и «поздно». Подсказка даёт место: сразу после `get up`.
6. **З4.** «В какие дни Нурлан никогда не работает?» — вместо «Когда … никогда», которое звучит спотыкаясь. «Дни»
   ответа не называют: ответом может быть и день недели, а дней недели в модуле нет (7.3).
7. **З5.** Разбор называет, где звучат оба неверных: `always` — в предложении о том, когда он встаёт; `never` — в
   предложении о выходных.
8. **Слово для частей записи — «предложение».** Запись ученик слышит, а не читает, и «строка» в подсказках к записи
   не годится. Модуль 15 говорит в таком месте «реплика», но у Нурлана рассказ, а не разговор.
9. **Поправлено сразу:** первая редакция условия З5 кончалась местоимением («в предложении об этом», правило 5
   понятности). Стало «из предложения о работе утром».

### Урок 11 «Говорим: как часто у тебя» (`govorim-kak-chasto-u-tebya`)

**Заполнено 20 мест:** вступление; условия, переводы образцов, подсказки и разборы четырёх `speak`; условие,
подсказка и разбор сборки.

1. **Вступление (8.6).** Урок о том, какими словами рассказать о себе, поэтому первой идёт сцена («Новый знакомый
   спрашивает о твоём дне…»), русская фраза — вторым предложением. Фраза «Утром я всегда пью чай, а вечером часто
   гуляю» не повторяет ни одного образца урока. Обещание — рассказать несколькими строками и в каждой выбрать
   наречие частоты (пункт 3.8: наречия выбирает ученик). Рода нет: «сам» не написано.
2. **З1.** Условие называет три факта прямо: что бывает всегда, обычно и чего не бывает никогда. Подсказка называет
   три слова, но не порядок и не место. Разбор держится счёта образца: наречие после I в каждой строке (верно для
   всех трёх), `in the morning` «в конце строки» — верно для двух строк, где он есть.
3. **З2.** Условие по 3.8; «точную частоту» не написал: «точная» против «неточной» — та же шкала, которой в модуле нет.
   Стало «скажи оборотом с every, как часто ты что-то делаешь». Перевод `cousin` — «двоюродный брат», первое слово
   карточки модуля 6; «вижусь с…» — как в уроке 7 (`I see my grandmother every month. — Я вижусь с бабушкой каждый
   месяц.`).
4. **З3 — без родового «занят».** Условие «где у тебя всегда много дел». Методист в 3.8 написал «когда», а образец
   говорит `at school` — это место. Взял «где», чтобы условие и образец не расходились. Перевод образца «Я всегда занят
   в школе» — перевод строки от первого лица, как название урока 4 «Я всегда занят: I am always busy»; к ученику он не
   обращён. Подсказка называет `busy` и порядок «форма be, за ней always» — это правило урока 4, а не ответ: слово
   места ученик выбирает сам.
5. **З4.** Условие — английская строка после двоеточия (8.2). Подсказка не повторяет рамку подсказки З6 урока 8
   («Короткий ответ «да» берёт слово do из вопроса»). Разбор — «короткий ответ говорит «нет», строка после него — как
   часто»: мысль случая 4 урока 8, но своими словами. Перевод «Нет. Я иногда смотрю телевизор вечером.» — порядок по
   английской строке, как в уроке 8.
6. **Формула.** У всех четырёх `speak` разбор кончается дословно: «Ответ на это задание не проверяется, поэтому
   сверься с образцом. Послушай его и повтори следом за ним.»
7. **З5 (сборка).** Условие «строку о том, что ты обычно пьёшь днём» задаёт смысл, но не место. Подсказка решает за
   ученика только первый и последний кусок (I и кусок с точкой). Место `usually` остаётся ему, а это и есть предмет
   задания (3.8). Разбор без рамки «Собралась строка… Первым стоит…».

**Поправлено в уроке 11 после чтения видимого текста:** подсказка З3 «Перед ним стоит форма be, а за ней наречие
always» говорила неправду о порядке: перед `busy` стоит `always`, а не форма be. Стало «Наречие always стоит между
формой be и этим словом».

### Работа модуля (`quiz`)

**Заполнено 24 места:** условия и разборы двенадцати вопросов. Подсказок у вопросов нет: поле `hint` в банке
вопросов до ученика не доходит (CLAUDE.md, «Одно имя поля в двух местах»).

1. **Условия говорят факты прямо.** Английская строка — после двоеточия (8.2), дальше русский текст с новой строки.
   Рамка «Ученик написал: … Запиши строку без ошибки.» — та, что у соседних работ (модули 1, 3, 4, 5).
2. **В1.** Условие называет русскую фразу «Я иногда читаю газету» (решение 5.1: «не каждый день» пропускало и
   `usually`, и `often`).
3. **В2 (пункт 2 решения).** Условие даёт факт отдельным предложением: «Вечером ты обычно плаваешь». Без него ответ
   «о себе» мог быть любым, а принимается одна строка (CLAUDE.md, «Условие, определяющее ответ»). Разбор называет
   место `usually` после I и перед глаголом, порядок `Usually I…` не называет.
4. **В3.** Разбор про `ever` молчит (1.4). «Слово never значит «никогда не», и «не» в нём уже есть» — как говорит
   урок 3 («одно слово передаёт «никогда не» целиком»). Первая мысль заглушки «перепиши так, чтобы оно было» кончалась
   местоимением; стало «чтобы в ней было это слово».
5. **В6 — конфликт заглушки с решением В1 (снят с урока 6).** Заглушка велела сказать, «что смысл от места не
   меняется». Разбор этого не говорит: только то, что `sometimes` перешло в начало и пишется с заглавной, а остальные
   слова идут в прежнем порядке. Условие зовёт слово «наречием частоты», а не «словом о частоте»: у главной вещи модуля
   одно имя.
6. **В7.** «Слово о времени стоит в единственном числе» — дословно подпись таблицы урока 7.
7. **В8 (1.3).** «Ким спросил Дану: Do your parents cook fish? Он хотел узнать, часто ли её родители готовят рыбу.»
8. **В9 (5.1).** Запись напечатана в условии после двоеточия. Разбор называет строку о телевизоре и строки двух
   неверных (о школе и о парке).
9. **В10 (5.1).** «В записи мужчину спрашивают, часто ли он поёт» — «он» совпадает с голосом: первая реплика по
   умолчанию женская, ответ мужской. Условие не называет ответа: `every` стоит в условии как ориентир, после него
   в уроке 7 звучат три разных слова.
10. **В11.** Рамка «Добавь в строку слово «обычно» и запиши её целиком», чтобы не повторить дословно рамку В3 («Перепиши
    строку так…»): действие у них одно, а слова разные.
11. **В12 (5.1).** Разбор называет верную строку, объясняет ошибку русским порядком «всегда днём спят» и отводит оба
    неверных: `always` стоит на месте, форма `are` не нужна при глаголе. Формулу случая 6 урока 1 дословно не
    повторяет.
12. **Длина.** Самое длинное предложение условий — В9 вместе с английской записью; русские предложения — не длиннее
    шестнадцати слов (пересчитано прогоном, ниже).

**Поправлено в уроке 11 после прогона `golosa`:** у образца З4 первую реплику читает женский голос (`по очереди, первый
женский`), а условие говорило «Друг спрашивает тебя». Ученик слышит женщину и читает «друг» — порода 3.7.1 урока 10.
Стало «Знакомая спрашивает тебя:» — тем же словом, что в уроке 10.

### МЕТОДИСТУ

1. **Работа, В6.** Заглушка велела сказать «смысл от места не меняется»; решение В1 это утверждение сняло. Разбор
   его не говорит. Если утверждение нужно работе, нужна опора, и тогда оно возвращается и в урок 6.
2. **Урок 11, З3.** В 3.8 стоит «когда у тебя всегда много дел», образец — `at school`, то есть место. Условие
   написано «где». Если нужно «когда», образцу нужна другая строка.
3. **Урок 11, З4.** Условие стало «Знакомая», чтобы сойтись с голосом по умолчанию. Если по замыслу спрашивает друг,
   нужно `pervyyGolos: "muzhskoy"` у образца (сводящему) и тогда условие вернётся к «Друг».
4. **Работа, В2.** В условие добавлен факт «Вечером ты обычно плаваешь». Без него задание «ответь о себе» принимало
   бы одну строку при бесконечном числе верных ответов. Факт не меняет `answer` и `accept`.

### СВОДЯЩЕМУ (комментарии и звук, не видимый текст)

- Шапка файла, строка 13: «ВЕСЬ ВИДИМЫЙ РУССКИЙ ТЕКСТ СТОИТ ЗАГЛУШКАМИ» больше не верна — заглушек 0.
- Шапка урока 10 перечисляет ответы `never`, `magazine`, `early`, `weekend` без `usually` (ответ З5).
- Комментарий над работой «ЭТА ТАБЛИЦА ВИДОВ ВЕРНА НА 13 сентября 2026» — сверить заново.
- `npm run ozvuchka`: `check:content` называет 125 отсутствующих записей по курсу, среди них образцы урока 11 и запись
  вопроса 10.

### ПРОВЕРИТЬ

- `npm run bliznetsy` по модулю 16 не запускал: заглушек больше нет, и отчёт теперь меряет текст, а не рыбу.
  У `gap` и `short` он рамки не видит по устройству, поэтому рамки условий работы разведены вручную (В3 и В11).

## Проверено и чисто — уроки 9–11 и работа, 15 сентября 2026

1. **Заглушки.** `grep "ПИШЕТ РЕДАКТОР"` по файлу — одно совпадение, строка 13 шапки (комментарий). Было 63:
   урок 9 — 1, урок 10 — 18, урок 11 — 20, работа — 24.
2. **`check:content`** по префиксам `kak-chasto → chitaem-nedelya-aygerim`, `slushaem-kak-chasto`,
   `govorim-kak-chasto-u-tebya`, `работа`, `проверочная` — пусто, после последней правки. Общая ошибка курса —
   «нет записей (125 шт.)», её снимает озвучка.
3. **`yazyk -- english-beginner-2`** — «Ошибок: 0, вопросов: 0», строк о `kak-chasto` нет.
4. **`perevody -- english-beginner-2 --chego`** — `16. kak-chasto 4 / 4 / 4`; в списке «Чего не хватает» модуля 16
   нет.
5. **`npx tsc --noEmit`** — пусто.
6. **Длина предложений.** Прогон Python по выводу `tekst` (79 кусков видимого текста уроков 9–11 и работы: вступления,
   подписи, условия, подсказки, разборы, переводы) — длиннее 22 слов: 0.
7. **Утечка ответов урока 10.** Тот же прогон: в подписях записей и во вступлении ни одного из `never`, `magazine`,
   `early`, `weekend`, `usually`, «никогда», «журнал», «рано», «выходн», «обычно», «газет» — 0. Разборы проверены
   чтением по порядку: каждый называет только свой ответ (пункт 1 урока 10).
8. **Слова-признаки.** В русском тексте без переводов: «слово/слова о частоте» — 0; «сам», «самой», «самому»,
   «уверен», «должен», «готов», «согласен» — 0; «занят» — 0 (единственное совпадение — «занятиях» в подсказке урока 9);
   «значат одно», «смысл», «чаще», «реже», «шкал», «только», «нельзя» — 0; рамки «Целиком строка», «Собралась
   строка», «Вспомни, каким», «дважды» — 0; «ты» с прошедшим временем — 0. `ever` целым словом стоит только в
   `accept` вопроса 3.
9. **Формула самопроверки.** Все пять `speak` уроков 9 и 11 кончаются «Послушай его и повтори следом за ним»
   (прочитано по выводу `tekst`).
10. **Голоса.** `npm run golosa -- english-beginner-2 kak-chasto`: запись 1 урока 10 — «муж.» спрашивает, «жен.»
    отвечает, условие З2 «она» верно; вопрос 10 работы — «жен.», «муж.», условие «мужчину… он» верно; образец З4
    урока 11 — «жен.», «муж.», условие поправлено на «Знакомая».
11. **Английские строки и поля заданий не тронуты.** `git diff -U0 HEAD`: среди удалённых строк ни одной с
    `options`, `items`, `answer`, `accept`, `phrase`, `parts`, `transcript`, `zvuk`, `correct` — 0. Удалённые строки
    без метки «ПИШЕТ РЕДАКТОР» (15) — все продолжения тех же заглушек, прочитаны.
12. **Уроки 1–8 не тронуты.** Первый кусок `git diff` — строка 2468 (образец урока 9); урок 9 начинается на 2312,
    урок 8 кончается выше. `git status` — только `kak-chasto.ts` и этот журнал.
13. **`sosedi`** — одно место: у образца З5 урока 9 тронут перевод, подсказка прежняя. Подсказка «Начни так же, как
    Айгерим, со слова Hello и своего имени» с переводом сходится.
14. **Видимый текст перечитан** по `npm run --silent tekst -- english-beginner-2 kak-chasto` от урока 10 до конца
    работы. Правки после чтения — условие З5 урока 10, подсказка З3 урока 11; после `golosa` — условие З4 урока 11.

## Уроки 7–11 и работа: правки по разбору и решениям методиста

15 сентября 2026. Основание — разбор второго редактора `priyomka-urokov-7-11-modul-16.md` и решения методиста
`reshenie-metodista-uroki-7-11-modul-16.md` (Р1–Р13). Файл читан после коммита сводящего `4150f46`. Записывается
по пункту за раз.

Все пункты ниже внесены в файл модуля.

### Урок 7 «Каждую неделю»

1. **Л7-О1 и Р10 (обязательно), случай 6 `sluchay-every-i-always`.** Правило стало двумя строками. Первая:
   «Оборот every week тоже отвечает на вопрос «как часто?», но это не наречие частоты. В нём два слова: every и
   слово о времени.» (окончательная редакция, см. п. 37) Вторая: «Слово always не говорит, сколько времени проходит между двумя разами,
   а every week говорит, что проходит неделя.» Слов «в конце строки» и «перед глаголом» нет (Р10). Двоеточие стоит
   перед перечнем, а не вместо союза. Первый пример `I always work at home.` внёс сводящий, и правило с ним не
   спорит (Л7-У2 закрыт).
2. **Р10 попутно, случай 4 `sluchay-oborot-v-konce`.** Имя «Оборот с every стоит в конце строки» → «Оборот с every
   ставим в конец строки». Правило: «Сначала называем, кто делает, потом глагол и то, что к нему относится. Оборот с
   every ставим после них, так же как оборот о времени in the evening.» Теперь это указание, а не закон языка:
   `accept` задания 4 принимает `Every week I see my friends.`
3. **Р10 и Л7-У4, подпись свода `tablica-svod-uroka-7`.** «Оборот с every ставим в конец строки, а слово после
   every стоит в единственном числе». «Одно слово» ушло, союз «а» вместо «и».
4. **Л7-У3, случай 7 `sluchay-often-i-every-month`.** Правило о месте повторяло случай 4 и говорило о месте оборота
   как о законе. Стало: «Обе строки о бабушке верны, но говорят разное. Первая говорит, что встречи бывают часто, а
   вторая называет, сколько времени проходит между ними: месяц.» Снимать ли экран, решает методист. Р10 его не
   снимал, поэтому экран остаётся.
5. **Л7-У1, вступление.** «как назвать такой срок по-английски» → «как сказать по-английски «каждый месяц» и
   «каждую неделю»». Слова «срок» в уроке больше нет.
6. **Ш-3, `z1-vybrat-kazhduyu-nedelyu`.** «где это сказано верно» → «в которой это сказано верно», как в уроке 5.
7. **Л7-У5, `z6-skazat-kazhduyu-nedelyu`.** «Первая пусть говорит…, а вторая, что…» → «В первой скажи, что ты
   делаешь каждую неделю, а во второй скажи, что…». Начало «Скажи вслух две строки» оставлено, как в уроках 1–6
   (Ш-1 — решение сводящего).
8. **Попутно, `z4-zapisat-vizhus-s-druzyami`, подсказка.** «поставь в конце строки» → «поставь в конец строки»:
   глагол «поставь» требует «куда».

### Урок 8 «Спрашиваю» — все пункты на усмотрение взяты

9. **Л8-У1 и Л10-У1, вступление.** «хочет узнать, работает ли она утром и каждое ли это утро» → «Ким знает, что
   Дана работает. Он хочет узнать, работает ли она утром, и если да, то каждое ли утро.» Прямая речь получила
   двоеточие и заглавную: «По-русски он спросит: «Ты всегда работаешь утром?».»
10. **Л8-У1, разбор разговора `razgovor-kim-i-dana`.** «каждое ли это утро» → «каждое ли утро она работает».
11. **Л8-У2, случай 2 `sluchay-narechie-v-voprose`.** «стоит на прежнем месте, между…» → «стоит там же, где в
    строке без вопроса, то есть между словом о том, кто делает, и глаголом». Имя случая 3: «Короткий ответ остаётся
    прежним» → «Короткий ответ не меняется»; правило под ним и так говорит «так же, как на вопрос без него».
12. **Л8-У3, случай 4 `sluchay-otvet-s-narechiem`.** Вопрос назван в правиле: «На вопрос Do you always cook at home?
    короткий ответ говорит «нет». Следующая строка с наречием частоты говорит, как часто человек готовит дома на
    самом деле.» Ключ звука блока — только строка примера, вопроса в нём нет: второй кнопки не будет (проверено
    чтением `zvuchat` блока; прогон — ниже, в прогонах).
13. **Л8-У4, случай 5 `sluchay-vopros-o-neskolkih`.** «Наречие частоты стоит после they или your friends, а глагол
    идёт без окончания» → «Когда спрашивают о нескольких людях, вместо you стоит they или your friends. Всё остальное
    в вопросе не меняется.» Прежняя строка позволяла вычитать, что при одном человеке окончание появляется. Снимать ли
    экран — вопрос методисту; Р10–Р13 его не решали, экран остаётся.

### Урок 9 «Читаем: неделя Айгерим»

14. **Л9 и Р10 (обязательно), разбор `z1-otmetit-narechiya-v-zapiske`.** «Он называет срок, и наречием частоты его
    не считают» → «В нём два слова, поэтому это не наречие частоты. Об этом говорит урок «Каждую неделю: every week,
    every month».» Имя урока сверено с `title` дословно.
15. **Л9-У1, разбор `z2-vybrat-chto-kazhdyy-mesyac`.** «иногда и в выходные… часто и утром» → «иногда, в выходные…
    часто, по утрам».
16. **Л9-У2, `z4-vpisat-usually-o-ney`.** Условие: «Перескажи эту строку об Айгерим» → «Расскажи об этом так, будто
    говоришь о ней». Подсказка: «из строки записки о занятиях дома» → «из той строки записки, где Айгерим пишет о
    занятиях дома».

### Урок 10 «Слушаем: как часто»

17. **Л10-О1 (обязательно), `z2-zapisat-chto-chitaet-inogda`.** «Что она читает иногда?» → «Что знакомая Кима
    читает иногда?». Подсказка «её ответа» теперь относится к названному в условии человеку.
18. **Л10-У1, вступление.** «Ким спросил бы её «Ты часто читаешь?».» → «Ким спросил бы её: «Ты часто читаешь?».»
19. **Л10-У2, подсказки `z1` и `z4`.** «Нужное слово стоит в том, где звучит newspapers» → «звучит в том
    предложении, где есть newspapers»; «стоит в самом его конце» → «звучит в самом его конце».
20. **Ш-4 не взят.** Рамка разборов на слух «слово, то есть перевод. Он сказал: строка» задана видом задания: ответ
    одним словом, перевод, затем место в записи. Пять одинаковых начал идут в двух уроках, а смена рамки ради
    разнообразия сделала бы разборы менее предсказуемыми там, где ученик ищет своё слово.

### Урок 11 «Говорим: как часто у тебя»

21. **Л11-О1 (обязательно), вступление.** «как рассказать о себе несколькими строками и в каждой выбрать наречие
    частоты» → «…несколькими строками и сказать в них, как часто ты что-то делаешь». Образец задания 2 говорит
    `every month`, короткий ответ задания 4 наречия не несёт — обещание больше с ними не спорит.
22. **Р3, `z4-otvetit-drugu`.** «Знакомая спрашивает тебя» → «Друг спрашивает тебя». Голос: `pervyyGolos:
    "muzhskoy"` внёс сводящий. Вступление «Новый знакомый спрашивает» с ним сходится, поэтому Л11-У1 снят сам.
23. **Л11-У3, разбор `z1-rasskazat-pro-utro`.** «В каждой строке… а оборот in the morning стоит в конце строки» →
    «В каждой строке наречие частоты стоит после I и перед глаголом. В первой и третьей строке в конце стоит оборот
    in the morning.» Счёт сверен с образцом: `in the morning` в строках 1 и 3.
24. **Л11-У4, подсказка `z3-rasskazat-kogda-mnogo-del`.** «О делах скажи словом busy» → «Слова «много дел» скажи
    словом busy». **Перевод образца «Я всегда занят в школе» оставлен:** это перевод английской строки, а не
    обращение к ученику; урок 4 модуля называется «Я всегда занят», и перевод `I am busy` курс держит тем же словом.
25. **Л11-У2 и Ш-1, условия устных заданий.** «Строки» в условиях оставлены: так устные задания зовут их во всех
    уроках модуля, и образец с переводом ученик после ответа видит напечатанным. Довод урока 10 («запись слышат, а не
    читают») относится к записи, у которой расшифровка скрыта. Чтобы задания 2 и 3 не начинались одинаково, условие
    задания 3 переписано: «Скажи вслух, где у тебя всегда много дел и что ты часто делаешь вечером. Хватит двух
    строк.» Разбор «В первой строке… во второй» с ним сходится.
26. **Л11-У5, подсказка сборки `z5-sobrat-stroku-o-sebe`.** «Строка начинается с I, а кусок с точкой ставь
    последним» называла видимое на кнопках → «Подумай, где стоит наречие частоты в строке с глаголом.» Дорога к
    единственному выбору, а не ответ.

### Работа модуля

27. **Перестановка вопросов 2 и 11 (Р12) — перечитаны оба на новом месте.** Вопрос 2 `q2-perepisat-s-usually`:
    условие ставит `usually` в данную строку — это итог урока 2 «говорить, что бывает обычно…», сходится. Вопрос 11
    `q11-otvetit-drugu-so-slovom-usually`: ответ другу строкой о себе — итог урока 11, сходится. Разборы обоих
    говорят только о строке `answer`, от места в работе не зависят. Шапка всей работы (комментарий) по-прежнему
    называет «вопрос 2» у действия «ответить на вопрос строкой о себе» — это комментарий, СВОДЯЩЕМУ.
28. **Р4 и Р-У2, условие вопроса 11.** «…Вечером ты обычно плаваешь. Ответь ему по-английски так, чтобы в ответе
    было слово «обычно».» → «Друг спрашивает тебя: Do you often swim in the evening? Ответь ему по-английски, что ты
    обычно плаваешь вечером.» Повтор «обычно» ушёл, русский порядок «обычно плаваешь вечером» совпадает с `answer`
    (Р4, на усмотрение). Факт остаётся прямым и определяет ответ.
29. **Р-У1, рамки вопросов 2, 3 и 8.** Русского слова «в английской строке» больше нет:
    - 2: «Добавь в строку слово «обычно» и запиши её целиком» → «Добавь в неё «обычно» по-английски и запиши строку
      целиком»;
    - 3: «…слова «никогда» в ней нет. Перепиши строку так, чтобы в ней было это слово» → «…«никогда» в ней не
      сказано. Перепиши строку так, чтобы в ней было сказано «никогда»»;
    - 8: «Перепиши вопрос так, чтобы в нём было слово «часто»» → «Вставь в вопрос «часто» по-английски и запиши
      вопрос целиком».
    Вопросы 3 и 8 больше не начинают действие одной рамкой. Вопросы 2 и 8 похожи строем («…по-английски и запиши …
    целиком»), но глаголы разные, а стоят они шестью вопросами врозь; действие у них одно законно (Р12).
30. **Р6, разбор вопроса 5.** Первая строка `He often works at home.` Р-У3 взят: «у глагола work есть окончание -s.
    В строке с наречием often оно тоже есть» → «Алим один, поэтому глагол work пишется с окончанием -s, и наречие
    often перед ним этого не меняет.»
31. **Р-У3 мелкое, разбор вопроса 1.** «Нурлан хотел сказать «иногда»» → «хотел написать», как в условии.
32. **Р-У4, разбор вопроса 7.** Дословный повтор подписи таблицы урока 7 снят: «Слово после every пишется без
    окончания -s, как в уроке «Каждую неделю: every week, every month».»
33. **Р5.1 и Р-У5, вопрос 12.** Варианты одного строя, все три — утверждения о написанной строке:
    - «наречие always стоит не на своём месте» (ложно: always стоит перед глаголом);
    - «перед наречием always не хватает формы are» (ложно: в строке есть глагол);
    - «оборот in the afternoon стоит между always и глаголом sleep» (верно, место третье, как было).
    «Должен» и «должно» ушли. Первая редакция неверного варианта «always стоит не в конце строки» была отвергнута
    мной сразу: она верна как описание строки, и ученик мог бы спорить, что это тоже причина.
    Разбор: «Верна третья причина. Без ошибки строка такая: They always sleep in the afternoon. В ней always стоит
    прямо перед глаголом sleep, а ученик разделил их оборотом, как в русской фразе «всегда днём спят». Наречие always
    стоит на своём месте, и форма are перед ним не нужна, потому что в строке есть глагол.» Варианты на странице не
    перемешиваются (`lib/content/quiz.ts`, поиск `shuffle`/`random` по `lib`, `components`, `app` — 0), так что
    «третья» верно.
34. **Попутно, условие вопроса 12 — МЕТОДИСТУ ЗНАТЬ.** Условие давало русскую фразу «Они всегда спят днём», а
    разбор и решение 5.1 объясняют ошибку русским порядком: оборот встал туда, где «днём» стоит в русской фразе. В
    фразе «Они всегда спят днём» «днём» стоит в конце, то есть там же, где в верной английской строке, — объяснение
    ложно. Фраза в условии заменена на «Они всегда днём спят»: смысл тот же, порядок естественный по-русски, и
    объяснение становится правдой. Английская строка, варианты-смысл и место верного не тронуты.

### Урок 1, случай 6 (Р5.2)

35. **`sluchay-narechie-i-oborot`.** «Наречие частоты стоит перед глаголом, а оборот о времени стоит в конце строки»
    → «В строках ниже наречие частоты стоит перед глаголом, а оборот о времени стоит в конце.» Правило теперь о своих
    двух примерах (`They sometimes swim at the weekend.`, `We always study in the evening.` — в обоих так), а не о
    языке. Второе предложение прежнее. Больше в уроке 1 ничего не тронуто.

### Правки после прогонов и чтения

36. **`check:content`, случай 2 урока 8.** Правило из п. 11 вышло в 26 слов. Разделено: «В вопросе первым идёт do.
    Наречие частоты стоит там же, где в строке без вопроса, то есть между словом о том, кто делает, и глаголом.»
37. **Случай 6 урока 7, после чтения вывода `tekst`.** Из п. 1 снято «every, то есть «каждый»»: перевод every
    дан случаем 1 того же урока. Стало «В нём два слова: every и слово о времени.»

## Проверено и чисто — правки уроков 7–11, работы и случая 6 урока 1, 15 сентября 2026

1. **`npx tsc --noEmit`** — пусто.
2. **`npm run --silent check:content 2>&1 | grep kak-chasto`** — пусто, после последней правки (п. 37).
3. **`npm run yazyk -- english-beginner-2`** — «Ошибок: 0, вопросов: 0», строк о `kak-chasto` нет.
4. **`npm run bliznetsy -- english-beginner-2 --hudshie 60`** — строк о `kak-chasto` 0 (новые рамки вопросов 2, 3,
   8, 11).
5. **`npm run sosedi`** — «правок, у которых сосед остался прежним, не нашлось».
6. **Кнопки звука и тире** — `zvuchashchee()` и `razobrat()` из `lib/content/` прогоном по восьми изменённым случаям
   (урок 1 случай 6; урок 7 случаи 4, 6, 7; урок 8 случаи 2–5): ключ звука в имени или правиле — 0, ключ без текста
   — 0, кириллица в английской половине пары — 0; число пар равно числу примеров в каждом случае. В случае 6 урока 7
   две строки правила, обе показываются правилом (`Ekran.tsx`: строка без пары — правило).
7. **Запретные поля.** `git diff -U0 HEAD`: среди удалённых строк `answer`, `accept`, `items`, `zvuchat`, `phrase`,
   `parts`, `transcript`, `sources`, `pervyyGolos`, `id` — 0; одно совпадение `correct` — русский текст верного
   варианта вопроса 12, признак остался на третьем варианте. `courses/resheno.ts` не открывался на запись.
8. **Порядок вариантов выбора** на странице работы сохраняется: `lib/content/quiz.ts`, строка 72,
   `question.options.map(...)`; поиск `shuffle`, `random` по `lib`, `components`, `app` — перемешивания нет. «Верна
   третья причина» в разборе вопроса 12 верно.
9. **Видимый текст перечитан** по выводу `npm run --silent tekst -- english-beginner-2 kak-chasto`: урок 7 целиком,
   урок 8 до заданий, изменённые места уроков 9 и 10, урок 11 и работа целиком. Родовых форм в новых строках нет;
   «срок», «не считают», «должен», «прежн» в уроках 7–11 и работе — 0.
10. **Имя урока в ссылках** (разбор урока 9, разбор вопроса 7) — «Каждую неделю: every week, every month», дословно
    `title` урока 7.

### СВОДЯЩЕМУ

- Шапка работы (комментарий над `quiz`) называет «вопрос 2» у действия «ответить на вопрос строкой о себе»; после
  Р12 это вопрос 11.
- Условие вопроса 12 теперь «Они всегда днём спят» (п. 34): объяснение русским порядком без этого было ложным.
  Английские поля не тронуты.

### МЕТОДИСТУ — не решено мной

- Экраны, которые второй редактор предлагал проверить на «убери экран»: случай 7 урока 7 «Две строки о бабушке»
  (Л7-У3) и случай 5 урока 8 «Вопрос о нескольких людях» (Л8-У4). Текст обоих переписан, но снимать ли их — не моё.
- Условие вопроса 12 (п. 34): замена русской фразы сделана ради верности разбора; если методист держит прежнюю
  фразу, разбор должен перестать объяснять ошибку русским порядком.
