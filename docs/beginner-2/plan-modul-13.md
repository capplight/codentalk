# Замысел модуля 13 — «Что я делаю каждый день»

СОСТОЯНИЕ: замысел дописан 7 сентября 2026. Отдано второму методисту на разбор;
пока разбор не придёт, автор документа его не правит.

Замысел урока 1 — [plan-modul-13-urok-1.md](plan-modul-13-urok-1.md).

Курс `english-beginner-2`, часть 3 «Мой день», модуль тринадцатый.
Строка программы (`docs/beginner-2/programma-stupeni.md`, строка 593):

| № | Модуль | Итог | Грамматика | Тема | Опора |
|---|---|---|---|---|---|
| 13 | Что я делаю каждый день | рассказывать о привычных делах | Present Simple | Time + Education | CEFR с. 63, A1 |

---

## 0. Долги, переданные модулю поимённо, — что с ними

### 0.1. `I wear jeans` против `She is wearing a hat` — НЕ моё дело. Закрыто чтением.

Задание звало прочесть `plan-modul-11.md`, строку 675, и решить, моё это или
модуля 17. **Прочитал; решать нечего — методист модуля 11 уже решил и записал
решение дважды.**

`plan-modul-11.md`, раздел 12.2 (строки 1762–1768), дословно:

> **Карточка `wear` отдана модулю 17, а не вам**, и довод не в том, что
> `I wear jeans` и `She is wearing a hat` «про одно и то же»: это разные вещи —
> Present Simple говорит о привычке, Present Continuous о том, что на человеке
> сейчас… Довод в другом: вся одежда разобрана модулем 11, и в модуле 17 глагол
> работает с первой же строки.

То же в двух других местах того же документа: строка 1212 (таблица «чего не
беру») и строки 1592–1593 (таблица передач). И то же в программе ступени,
строка 697: «слово `wear` передано модулю 17, и передача записана у обоих».

**Что из этого моё.** Не слово, а РАЗЛИЧИЕ. Модуль 17 «Прямо сейчас» строит
Present Continuous на противопоставлении моему Present Simple. Значит мой
модуль обязан сказать, ЧТО ИМЕННО он утверждает строкой `I get up in the
morning` — что так бывает каждый день, а не сейчас, — иначе модулю 17 не от чего
оттолкнуться. (`at seven` не беру: час у модуля 18, раздел 5, урок 6.)
Разбор — раздел 11.2, передача методисту модуля 17.

**Карточку `wear` я не беру.** Ни она, ни одежда в моём словаре не нужны: тема
модуля — `Time` и `Education`.

### 0.2. Пункт English Grammar Profile, переданный модулем 11

`plan-modul-11.md`, раздел 12.2:

> Пункт `1741163711829x787492176446619900` (`NOUNS / noun phrases - grammatical
> functions`, FORM: AS OBJECTS, A1) взят модулем 11: `I have got a bag`…
> **Ставьте его в `egpPovtor`.**
> Свободен и по смыслу ваш `1741163715288x929829619225120500` (`VERBS / types`,
> FORM: MAIN VERBS, A1) — модуль 11 от него отказался нарочно.

СВЕРЕНО: оба `id` проверены прогоном `npm run programma -- english-beginner-2` —
оба стоят в списке нерозданных пунктов ступени A1. Раздел 6.

### 0.3. Карточки `teacher` и `student` — БЕРУ. Долг закрывается здесь.

Долг дошёл до меня третьим и записан у двоих:

- `plan-modul-10.md`, раздел 11.10: «**Карточек `teacher` и `student` в курсе нет
  НИ У КОГО.** Их давал снятый модуль `kto-eto`; модуль 6 вычистил оба слова у
  себя нарочно и записал это в шапке (`moya-semya.ts`, строки 81–97), попросив
  отдать карточки модулю 3 — а модуль 3 уже написан, и просьба повисла… **Ваша
  тема `Education` — то место, где долг закрывается.**»
- `plan-modul-10.md`, строка 932: таблица «чего не беру» — «`teacher`,
  `student` | занятия, тема модуля 6 и модуля 13; долг передан модулю 13».

**Проверено мной по КОДУ, а не по чужому документу** (правило «замысел соседа
верен только на дату»):

```
grep -n 'term: "teacher"\|term: "student"' courses/english-beginner-2/*.ts
→ courses/english-beginner-2/kto-eto.ts  (обе строки)
```

`kto-eto.ts` в `index.ts` НЕ импортируется — шапка `index.ts` зовёт его складом
нарочно, и `check:content` объявил его «файлом без дороги». Значит для ученика
карточек нет. **Счёт по видимому тексту курса:** прогон
`npm run tekst` по всем двенадцати написанным модулям (25 229 строк) даёт
`teacher` — 0 вхождений, `student` — 0.

Беру обе карточки в урок с темой `Education` (раздел 7).

---

## 1. Проверка ПО КУРСУ — сделана первой и по видимому тексту

Признак: собран `npm run --silent tekst -- english-beginner-2 <модуль>` по всем
двенадцати написанным модулям в один файл, **25 229 строк**, и поискано по нему.
Граница обхода названа честно: **двенадцать модулей и только они.** Работа
части 1 (`raboty-chastey.ts`) в этот файл НЕ попала — `tekst` берётся по имени
модуля, — и работы модулей тоже. `kto-eto.ts` не входит: он не импортируется.
Значит всё, что я говорю ниже про «в курсе этого нет», проверено по урокам, а
не по банкам вопросов, и это надо помнить.

| Что искал | Вхождений | Вывод |
|---|---|---|
| `Present Simple` | 0 | имя времени в курсе не звучало ни разу |
| `every day` | 0 | свободно |
| `get up`, `I work`, `I live`, `I go`, `I like`, `I read`, `I study` | по 0 | ни одной строки Present Simple с лексическим глаголом в курсе НЕТ |
| `always`, `usually`, `often` | по 0 | наречия частоты свободны (они у модуля 16) |
| `o'clock`, `time`, `class`, `lesson`, `homework` | по 0 | свободны |
| `school` | 19 | **занято модулем 6**: оборот `at school` разобран целиком, вместе с «артикля в нём нет вовсе». Карточка `school` там же |
| `I have` | 50 | это `have got` модуля 11 и `Have you got…?` — не лексический глагол |
| `morning` / `afternoon` / `evening` / `night` | 60 / 46 / 77 / 42 | это `good morning` и прочие приветствия модуля 2, а не части суток как время дня |

**ГЛАВНАЯ НАХОДКА ЭТОЙ ПРОВЕРКИ, и она меняет устройство модуля.**

Счёт по всем карточкам курса (285 записей, разбор поля `term:` по всем файлам):
**лексических глаголов в курсе ЧЕТЫРЕ, и все четыре — не рабочие.**

| Слово | Модуль | Чем стоит |
|---|---|---|
| `spell` | 1 | только в готовой формуле `How do you spell your name?` |
| `fill` | 5 | только в готовой формуле `Fill in the form.` (повелительное) |
| `work` | 6 | карточка стоит **существительным** (`at work`), не глаголом |
| `have got` | 11 | оборот обладания, а не действие |

То есть **модуль 13 — первое место курса, где ученик получает глагол действия
вообще.** До него весь курс говорит формой `be`, оборотом `have got` и оборотом
`there is`. Это подтверждает и разбор модуля 11 (`plan-modul-11.md`, раздел 4.1):
«до модуля 11 имя вещи после глагола стоит только в двух готовых формулах, и ни
разу не разбирается».

**Следствие для устройства:** первый урок модуля не может начинать с
третьего лица и с `-s`. Он обязан сперва показать саму мысль «в строке бывает
не только форма be, но и слово о действии». Разбор — раздел 3.

**СВЕРЕНО, и результат отрицательный не по моей воле:** замыслов модулей 14–18 в
`docs/beginner-2/` НЕ СУЩЕСТВУЕТ — проверено `ls docs/beginner-2/plan-modul-1*.md`,
там лежат только 10, 11, 12 и 13. Значит правило «замысел соседа — такая же часть
курса, как код» я исполнить не мог: читать нечего. Передачи раздела 11 написаны
в пустоту и обязаны быть прочитаны теми, кто эти модули возьмёт.

---

## 2. Опоры: что источники разрешают и где граница

### 2.1. Описания Совета Европы, с. 63 — опора программы. Прочитана сплошняком.

Шкала **Sustained monologue: describing experience**, `materials/cefr-companion-2020.pdf`,
с. 63. Читал не одну графу, а всю шкалу сверху вниз (правило «к цитате читай
соседние строки графы»).

**Графа A1 целиком, две строки, дословно:**

> Can describe themselves, **what they do** and where they live.
> Can describe simple aspects of their **everyday life** in a series of simple
> sentences, using simple words/signs and basic phrases, **provided they can
> prepare in advance**.

**Что отсюда следует и что НЕ следует.**

1. «what they do» и «everyday life in a series of simple sentences» — это
   ровно предмет модуля, и он на A1. Опора программы верна.
2. **«provided they can prepare in advance» — оговорка, и её нельзя терять.**
   Ученик A1 рассказывает о своём дне, ПОДГОТОВИВШИСЬ. Значит задание вида
   «скажи вслух прямо сейчас произвольный рассказ» ступени не отвечает, а
   задание «собери свой рассказ из данных строк, потом скажи» — отвечает.
   Задания раздела 8 написаны по этой оговорке.
3. **ГРАНИЦА, КОТОРУЮ НАДО НАЗВАТЬ ЧЕСТНО.** Соседняя графа **A2** той же шкалы
   говорит: «Can describe plans and arrangements, **habits and routines**, past
   activities and personal experiences». То есть слово «habits and routines» у
   этой шкалы стоит на **A2**, а не на A1.

   **Противоречия с итогом «рассказывать о привычных делах» тут нет, и вот
   почему.** Разрешение на само время даёт другой источник, и даёт прямо:
   English Grammar Profile, `PRESENT/present simple`, **USE: HABITS AND GENERAL
   FACTS**, ступень **A1**, `id=1741163713626x414037782388433340`:

   > Can use the present simple to talk about **repeated events or habits**, and
   > general facts.
   > «I work in a supermarket.» · «I watch TV in the evening. I read books at
   > night.» · «She goes to work by bus.»

   Шкала CEFR меряет ОБЪЁМ монолога (A2 — когда человек сводит в один рассказ
   планы, привычки, прошлое и опыт разом), EGP — разрешение на форму. Модуль
   берёт форму по EGP и объём по CEFR: **series of simple sentences**, а не
   связный рассказ о жизни.

   **Отсюда прямой запрет самому себе:** в модуле не бывает монолога длиннее
   нескольких простых предложений подряд, и не бывает рассказа, где привычки
   смешаны с прошлым или планами.

### 2.2. English Grammar Profile — искал ПО ВСЕМУ ФАЙЛУ

Признак: файл `materials/English Grammar Profile Online.csv` прочитан настоящим
разбором CSV (свой скрипт, 1222 записи, графы `id | SuperCategory | SubCategory |
Level | Lexical Range | Guideword | Can-do statement | Example`). Поиск шёл по
всей записи целиком, а не по подкатегории `present simple`: правило приписано
после модуля 29 ступени Elementary.

**Ступень A1, всё, что нашлось по слову `present simple` — шесть записей:**

| `id` | Категория, заголовок | Примеры источника | Чей |
|---|---|---|---|
| `1741163713626x486355013779909760` | `PRESENT/present simple`, **FORM: AFFIRMATIVE** | «Every day at college I **learn** new words…» · «**She plays** tennis and she **likes** going to the swimming pool.» | **мой** |
| `1741163713626x414037782388433340` | `PRESENT/present simple`, **USE: HABITS AND GENERAL FACTS** | «I **work** in a supermarket.» · «I **watch** TV in the evening. I **read** books at night.» · «**She goes** to work by bus.» | **мой** |
| `1741163715288x929829619225120500` | `VERBS/types`, **FORM: MAIN VERBS** | «**She comes** from India.» · «**He gets up** at 6 o'clock and **goes** to work.» · «You know, I **love** your cooking!» | **мой** (передан модулем 11) |
| `1741163713626x335670389512142900` | `PRESENT/present simple`, FORM: NEGATIVE | «I don't like meat and fish.» | **модуля 14** «Я не делаю» |
| `1741163716067x101148462288360720` | `NEGATION/negation`, FORM: AUXILIARY VERB 'DO', PRESENT | «I don't have a dining room…» | **модуля 14** |
| `1741163713626x606524312014730500` | `PRESENT/present simple`, FORM: WITH 'REALLY' | «I really like my kitchen…» | **не беру**, раздел 5.3 |

**САМОЕ ВАЖНОЕ ИЗ ЭТОГО РАЗБОРА — ОКОНЧАНИЕ `-s` ТРЕТЬЕГО ЛИЦА СТОИТ НА A1, И
ОТДЕЛЬНОЙ ГРАФЫ У НЕГО НЕТ ВОВСЕ.**

Искал нарочно: по строкам `third person` (нашлось 2 записи, обе C1/C2 и обе про
`let him/her/them` — к делу не относятся) и по `agreement` (8 записей, все B1 и
выше, все о согласии собеседника, а не о согласовании глагола). **Не нашёл
отдельного пункта про `-s`; искал по всему файлу двумя способами.**

Зато `-s` стоит **примерами внутри двух графов A1, которые я беру**: «She
**plays** tennis», «She **goes** to work by bus», «He **gets** up at 6 o'clock
and **goes** to work», «She **comes** from India». Правило проекта «к цитате
читай её пример» здесь работает в мою пользу: пример показывает, что засчитано.

**Отсюда: третье лицо с `-s` — законная часть этого модуля, а не превышение.**
Расхождения источников тут нет, и объявлять в шапке нечего.

### 2.3. Cambridge, Young Learners — совпадает и стоит НИЖЕ

`materials/cambridge-young-learners-pre-a1-a2.pdf`, **с. 29**, «Grammar and
Structures List», список **Pre A1 Starters**. Страницу читал целиком, а не по
одной строке (порядок страниц в этом файле местами двоится — предупреждение
программы ступени, раздел «чего проверить не удалось»).

Строки, относящиеся ко мне, дословно:

| Графа списка | Примеры источника |
|---|---|
| **Verbs** (Positive, negative, question, imperative and short-answer forms) — **Present simple** | `Nick is happy.` · `I don't like eggs.` · `Eat your lunch!` · `Is that your sister? Yes, it is.` |
| **Adverbs** | `I'm colouring it now.` · **`My grandma lives here.`** · **`She lives here too.`** |
| **Prepositions of place and time** | `Put the clock next to the picture.` · **`We go to school in the morning.`** |
| **Have + obj + inf** | `Lucy has a book to read.` |

**Две вещи, которых не видно, если читать только строку «Present simple».**

1. **Третье лицо с `-s` у Cambridge стоит на Pre A1 Starters** — `My grandma
   lives here`, `She lives here too`, `Lucy has a book`. В самой строке «Present
   simple» примеров с `-s` нет вовсе, и вывод «Cambridge даёт только первое
   лицо» сделал бы всякий, кто прочёл одну строку. Ровно та ловушка, на которой
   обжёгся разбор модуля 10.
2. **`We go to school in the morning` — готовый образец МОЕГО модуля**, и он же
   Pre A1. Оттуда берутся и `go to school`, и `in the morning`.

**Расхождения источников нет:** Cambridge Pre A1, EGP A1, ступень курса A1.
Строки в шапку модуля о расхождении не пишем — писать не о чем.

**СВЕРЕНО, и находка важная.** Список A1 Movers, **с. 52**, открывается строкой
«The list below details what is **new** at A1 Movers level» — то есть в нём стоит
только то, чего нет у Starters. Present simple там не повторяется вовсе: значит
всё время целиком, вместе с третьим лицом, лежит на Pre A1 Starters.

А ЧТО ТАМ ЕСТЬ — это границы моего модуля, и все три названы источником:

| Графа с. 52 | Пример | Чей модуль |
|---|---|---|
| Adverbs | «She **never** eats meat.» | **16** «Как часто» |
| Prepositions of time | «He plays badminton **on Saturdays**.» · «She plays with her friends **after school**.» | **18** «Дни, время, дата» |
| Question words | «**When does** school start?» | **15** «Спросить о делах» |

И там же, с. 51, раздел Speaking Part 4 A1 Movers: «Who do you play with at
school? What games do you play at school?» — вопросы с `do`, то есть тоже
модуль 15.

---

## 3. ШАПКА ЗАМЫСЛА: три вещи, названные ДО первой строки редактора

Правило приписано 6 сентября 2026 методистом модуля 8: цена промаха тут не в
решении, а в том, КОГДА оно принято. Слово, названное после того, как редактор
сел писать, стоит двадцати восьми заглушек вместо одной правки.

### 3.1. Имя главной вещи модуля — «глагол». Термин заимствованный, и адрес у него есть.

**Имя одно, других у этой вещи в модуле нет.**

Термин **настоящий** — так эту часть речи зовёт русская грамматика, и ученик
знает слово из школы. Придумывать своё («слово действия», «слово-дело») нельзя:
ровно на этом обжёгся модуль 11 Elementary со «признаком» вместо прилагательного.

**Термин не мой — он уже в курсе, и я его наследую.** Счёт по видимому тексту
(`npm run tekst` по двенадцати модулям, разбор по словам, а не по строкам):
«глагол» — 22, «глагола» — 47, **итого 69 форм**; из них 17 строк в модуле 2. Стоит оно там всегда в связке **«форма глагола be»**:

> «Между „я" и именем по-английски стоит глагол» (модуль 2, урок 1)
> «У глагола be несколько форм, и нужную выбирает слово, стоящее перед ней»

**Адрес термина: модуль 2 «Здравствуйте, меня зовут», урок 1.**

**Что делает модуль 13 с этим термином — расширяет его, а не переопределяет.**
До сих пор ученик видел в этой роли только `be`. Мой модуль показывает, что на
том же месте стоит `work`, `live`, `read`. Формулировка редактору: **глагол
бывает не только be**, а не «глагол — это слово, которое…»: определения ученику
не даём, он его знает.

### 3.2. Общее слово, которым модуль зовёт то, о чём говорит, — «действие»

**И это НЕ «дело», хотя строка программы говорит «привычные дела».** Оба
напрашивающихся слова в курсе ЗАНЯТЫ, и обе занятости я нашёл счётом, а не
припомнил.

| Слово | Счёт в видимом тексте | Чем занято |
|---|---|---|
| **«дела»** | «дела» 21, «делах» 30, «дело» 1 — **52 формы** | модуль 2, урок «Как дела»: «По-русски мы спрашиваем „как дела". По-английски для этого есть готовый вопрос из трёх слов. `How are you?`» Плюс перевод карточки `fine` — «хорошо (о делах)» и заголовок урока |
| **«занятие», «занятия»** | 27 + 32 + «занятием» 6 + «занятий» 7 = **72 формы** | **дважды занято**: модуль 6, итог урока — «называть занятие родного: слово о родном, is и занятие с артиклем» (то есть профессия); и подписи сцен модулей 1, 4, 11 — «Вечер, занятия кончились», «кто пришёл на первое занятие» (то есть школьный урок) |
| **«привычка»** | 6 | всюду о РУССКОЙ привычке как источнике ошибки: «По-русски „Стол длинный" — это два слова… потому что по-русски привычки ставить туда слово нет». Ученик, прочитав «привычное дело», пойдёт искать ошибку |
| **«действие»** | **0** («действие» 0, «действия» 0) | свободно |

**Беру «действие».** Слово настоящее, по-русски естественно стоит рядом с
«глаголом» и в курсе не значит ничего другого.

**СЛЕДСТВИЕ, КОТОРОЕ НАДО ДОВЕЗТИ ДО ПРОГРАММЫ СТУПЕНИ — раздел 12.**
Строка 593 `programma-stupeni.md` пишет итог модуля «рассказывать о **привычных
делах**». Оба слова заняты, и в видимый текст эта формулировка идти не может.
Предлагаю строку: **«рассказывать, что делаешь каждый день»**. Правку в чужой
документ вношу не я — правило проекта.

### 3.3. Заимствованные термины и адреса, где они введены

Все шесть проверены по ВИДИМОМУ тексту курса, а не по шапкам файлов: имя,
стоящее в шапке, ученику не звучит (этим курс обжёгся целой ступенью Elementary).

| Термин | Адрес | Как модуль им пользуется |
|---|---|---|
| **глагол** | модуль 2, урок 1 — «форма глагола be» (17 вхождений) | расширяю: глагол бывает не только `be` |
| **форма be** | модуль 2 (127 вхождений по курсу) | противопоставляю: либо форма be, либо глагол действия |
| **артикль** | модуль 3, урок об `a` и `an` (143 вхождения) | нужен в `a teacher`, `a book`; и в обороте `at school` его нет — это уже сказал модуль 6 |
| **предлог** | модуль 3 (10 вхождений); `at` при учреждении — модуль 6 | `in the morning`, `at night`, `at school` |
| **множественное число** | модуль 8 (32 вхождения) | **ОСТОРОЖНО, см. раздел 6.4:** окончание `-s` у глагола и `-s` у существительного — разные вещи, и ученик их спутает |
| **прилагательное** | модуль 9 (89 вхождений) | в модуле почти не нужно |

**Своего термина модуль не заводит ни одного.** Имя времени — `Present Simple` —
термином в смысле правила не является: это имя вещи, и оно даётся один раз
(раздел 6.6).

---

## 4. Состав модуля: десять уроков

Порядок «правила → чтение → слушание → говорение → письмо» — тот же, что у
модуля 12. Число уроков — ориентир (решение владельца 6 сентября); беру десять
потому, что предмет режется на шесть шагов правил, и ни один из шести не
складывается с соседом без потери.

| № | Урок | Вид | Что вводит |
|---|---|---|---|
| 1 | Я делаю: `I work` | правила | глагол действия вместо формы be; порядок «кто — глагол — остальное» |
| 2 | Мы и они: `we live`, `my parents work` | правила | те же формы при `you`, `we`, `they` и при имени во множественном |
| 3 | Он и она: окончание `-s` | правила | `he works`, `Dana lives`, `my sister reads` |
| 4 | Когда одной `-s` мало: `goes`, `watches`, `studies` | правила | три случая написания |
| 5 | Учитель и ученик: `My teacher works at school` | правила (тема `Education`) | долг курса: `teacher`, `student`, `lesson`, `learn`, `teach` |
| 6 | Утром и вечером: `in the morning` | правила (тема `Time`) | четыре оборота части суток и `every day` |
| 7 | Читаем: день Нурлана | чтение | связный текст на изученном |
| 8 | Слушаем: кто что делает | слушание | ответ одним словом |
| 9 | Говорим: мой день | говорение | дескриптор CEFR с. 63 |
| 10 | Пишем: что я делаю | письмо | дескриптор CEFR с. 67 |

### 4.1. Почему уроки 1 и 2 не сливаются, хотя форма у них одна

Соблазн прямой: при `I`, `you`, `we`, `they` глагол выглядит одинаково, и
кажется, что это один экран. **Сливать нельзя по счёту курса.**

Урок 1 делает вещь, которой в курсе не было **ни разу за двенадцать модулей**:
ставит на место формы be другое слово. Счёт из раздела 1 — лексических глаголов
в курсе четыре, и все четыре стоят готовыми формулами. Значит урок 1 — это не
«ещё одно лицо», а смена самого устройства строки, и ему нужен свой урок целиком.

Урок 2 делает вторую вещь, которой одного счёта мало: **подлежащее бывает не
местоимением.** `My parents work`, `Dana and Kim live here`. Правило проекта
требует этого шага отдельной строкой после всякой таблицы форм («таблица с
местоимениями не закрывает случай, когда вместо местоимения стоит имя»), а здесь
он ещё и решает, к чему прибавится `-s` в уроке 3.

### 4.2. Возвращение к пройденному (`vozvrat`)

Ставится сборщиком и только на уроки, которые уже есть в коде.

| Урок | Откуда возвращаем | Зачем именно здесь |
|---|---|---|
| 3 | модуль 8 «Один и много», урок об окончании `-s` | ученик увидит то же окончание в другой работе, и разводить их надо в ту же минуту |
| 5 | модуль 6 «Моя семья и знакомые», урок «Из двух слов и где работают: `a taxi driver`, `at school`» | `at school` там уже разобран целиком, вместе с «артикля нет вовсе» |
| 6 | модуль 2, урок «Доброе утро» | `good morning` ученик знает как готовую формулу; здесь `morning` впервые стоит отдельным словом |

### 4.3. НАХОДКА, ИЗМЕНИВШАЯ СОСТАВ: урок 4 едва не стал вторым уроком модуля 8

Первая редакция этого замысла ставила уроком 4 «Когда одной `-s` мало: `goes`,
`watches`, `studies`» — правило написания. **Проверка по курсу его отменила.**

Модуль 8 «Один и много», урок 2, называется **«Когда написание меняется: `boxes`
и `cities`»**, и его итог стоит в коде дословно так:

> писать `-es` после `s`, `x`, `ch`, `sh` и у слов `potato` и `tomato`,
> а `-ies` — после согласной с `y`

**Это ровно то правило, слово в слово.** `go → goes` — то же, что `potato →
potatoes`; `study → studies` — то же, что `city → cities`; `watch → watches` —
то же, что `sandwich → sandwiches`. Урок о написании был бы вторым таким же
уроком в одной ступени — порода, из-за которой модули 12 и 26 Elementary дали
ученику два разбора одной строки.

**Чем урок 4 заменён — и замена ценнее исходного.** Настоящая беда здесь не в
написании, а в том, что **одно и то же окончание `-s` делает в двух местах
противоположную работу**:

| Где стоит `-s` | Что значит | Откуда ученик знает |
|---|---|---|
| у слова о вещах: `boxes`, `books` | их **несколько** | модуль 8 |
| у глагола: `he watches`, `she reads` | человек **один** | модуль 13, урок 3 |

Ученик, прошедший модуль 8, читает `she reads books` и видит два `-s` подряд,
означающих разное. Урок 4 и есть это различение; написание в нём даётся
таблицей со ссылкой на модуль 8, а не разбирается заново.

**Заголовок урока 4: «Два разных `-s`: `boxes` и `he watches`».**

---

## 5. Уроки 2–10 — по строке каждый

Урок 1 расписан отдельным файлом: `docs/beginner-2/plan-modul-13-urok-1.md`.

Во всех уроках: восемь названных случаев — ориентир, а не норма; у каждого
случая имя, правило одной строкой и два целых примера с переводом. Вступление —
**сцена или русская фраза**, обобщением урок не начинается (правило от 3 сентября).

---

### Урок 2. «Мы и они: `we live`, `my parents work`» — правила

**Итог:** `ПИШЕТ РЕДАКТОР` — строка обязана утверждать: ставить глагол без
изменений при `you`, `we`, `they` и при названии нескольких людей.

**Вступление:** сцена. `ПИШЕТ РЕДАКТОР` — Дана называет, где живут и работают её
родители. Не обобщение, а человек и случай.

**Случаи (8):**

| № | Что утверждает | Английские строки |
|---|---|---|
| 1 | при `you` глагол тот же, что при `I` | `You work in a shop.` · `You live in Astana.` |
| 2 | при `we` глагол тот же | `We live in Almaty.` · `We work here.` |
| 3 | при `they` глагол тот же | `They live in a village.` · `They work in an office.` |
| 4 | таблица форм: четыре лица в столбик, глагол один | `I work` / `you work` / `we work` / `they work` |
| 5 | **вместо местоимения — имя**: `my parents` встаёт туда же, где `they` | `My parents work in a shop.` · `My parents live here.` |
| 6 | два имени рядом — это тоже «они» | `Dana and Kim live in Astana.` |
| 7 | врезка об ошибке: рука тянется поставить форму be, как ставила двенадцать модулей | так нельзя: `We are work.` — **звука у строки нет** |
| 8 | свод: во всех четырёх случаях слово одно и то же | сводит случаи 1–6 |

**Задания (8):** `match` (местоимение → строка), `gap` ×2, `order`, `choice`,
`hottext` (отметить в тексте глаголы), `short`, `speak`.

---

### Урок 3. «Он и она: `he works`, `Dana lives`» — правила

**Итог:** `ПИШЕТ РЕДАКТОР` — прибавлять `-s` к глаголу, когда речь об одном
человеке.

**Вступление:** русская фраза. `ПИШЕТ РЕДАКТОР` — по-русски глагол меняется при
всяком лице («я работаю — ты работаешь — он работает»), по-английски меняется
один раз.

**Случаи (8):**

| № | Что утверждает | Английские строки |
|---|---|---|
| 1 | при `he` у глагола появляется `-s` | `He works in a shop.` · `He lives in Astana.` |
| 2 | при `she` то же самое | `She works in an office.` · `She reads books.` |
| 3 | **вместо местоимения — имя одного человека** | `Dana lives in Almaty.` · `My sister works here.` |
| 4 | **вторая пара «имя вместо местоимения»** | `My brother reads books.` |
| 5 | таблица шести форм | `I work` / `you work` / `he works` / `she works` / `we work` / `they work` |
| 6 | врезка об ошибке | `He work.` и `He is works.` — обе настоящие |
| 7 | окончание слышно в речи | `works` /wɜːks/ · `lives` /lɪvz/ — **ОСТОРОЖНО, раздел 6.5** |
| 8 | свод: форму глагола решает слово ПЕРЕД ним | сводит случаи 1–5 |

**ЧТО СНЯТО САМИМ ЗАМЫСЛОМ.** Первая редакция ставила случаем 4 строку
`The shop opens at nine.` — «при `it` то же самое». Снимаю: строка требует часа
(`at nine` — предлог времени, а он у модуля 18) и глагола `open`, которого в
модуле нет. `it` о вещи без часа звучит натянуто, и случай ушёл бы на пустое.

**Задания (8):** `choice`, `gap` ×2, `match` (кто → форма), `order`, `hottext`,
`short`, `speak`.

---

### Урок 4. «Два разных `-s`: `boxes` и `he watches`» — правила

**Итог:** `ПИШЕТ РЕДАКТОР` — отличать `-s` у слова о вещах от `-s` у глагола и
писать `-es` и `-ies` там, где их пишут.

**Вступление:** русская фраза или сцена. `ПИШЕТ РЕДАКТОР` — в строке `She reads
books` два `-s`, и работают они по-разному.

**Случаи (8):**

| № | Что утверждает | Английские строки |
|---|---|---|
| 1 | у слова о вещах `-s` значит «их несколько» | `two books` · `three boxes` |
| 2 | у глагола `-s` значит «человек один» | `He reads.` · `She works.` |
| 3 | **обе работы в одной строке** | `She reads books.` · `He watches television.` |
| 4 | написание то же, что у слов о вещах | таблица `watch → watches`, `go → goes`, `study → studies` и **ссылка на модуль 8, урок «Когда написание меняется: `boxes` и `cities`»** |
| 5 | `-es` после `ch`, `sh`, `s`, `x`, `o` | `He watches television.` · `She goes to school.` |
| 6 | `-ies` после согласной с `y` | `She studies English.` · `My brother studies here.` |
| 7 | гласная перед `y` ничего не меняет | `She plays in the park.` — как `a day — days` в модуле 8 |
| 8 | врезка об ошибке | `He go.` и `She studys.` — обе настоящие |

**Задания (8):** `hottext` (отметить в тексте `-s`, который значит «много»),
`choice`, `gap` ×2, `match`, `order`, `short`, `speak`.

Задание `hottext` — предмет этого урока целиком, и оно единственное в модуле,
где ученик делает именно это движение.

---

### Урок 5. «Учитель и ученик: `My teacher works at school`» — правила, тема `Education`

**Итог:** `ПИШЕТ РЕДАКТОР` — называть учителя и ученика и говорить, что каждый из
них делает.

**Вступление:** сцена. `ПИШЕТ РЕДАКТОР` — Айгерим говорит о своей школе и о том,
кто там что делает.

**ЗДЕСЬ ЗАКРЫВАЕТСЯ ДОЛГ КУРСА** — карточки `teacher` и `student` (раздел 0.3).

**Случаи (8):**

| № | Что утверждает | Английские строки |
|---|---|---|
| 1 | кто учит и кто учится | `My teacher is Aigerim.` · `I am a student.` |
| 2 | что делает учитель | `My teacher teaches English.` · `She works at school.` |
| 3 | что делают ученики | `Students read and write.` · `We learn English.` |
| 4 | **`at school` — оборот без артикля**, прямая ссылка на модуль 6 | `My teacher works at school.` · `My son is at school.` |
| 5 | `learn` и `teach` — две стороны одного дела | `I learn English.` · `My teacher teaches English.` |
| 6 | **место учёбы** — тот же предлог учреждения | `He studies at a university.` · `My sister studies at a college.` |
| 7 | название занятия | `We have a lesson.` — **СНЯТО, разбор ниже**; вместо него `The lesson is here.` |
| 8 | свод: об одном `-s`, о нескольких без него | `My teacher works.` — `Students read.` |

**ЧТО СНЯТО САМИМ ЗАМЫСЛОМ И ПОЧЕМУ.** Строка `We have a lesson.` требует
глагола `have` в значении «есть у нас». Курс дал только оборот `have got`
(модуль 11) и дал его целым. Ставить `have` без `got` — заимствование, о котором
пришлось бы объявлять отдельно, и оно перетянуло бы на себя весь урок. Случай 7
берёт `lesson` строкой с формой be, которая ученику знакома двенадцать модулей.

**Задания (8):** `match` (человек → его действие), `gap` ×2, `choice`,
`hottext`, `setka` (поле букв со словами темы), `short`, `speak`.

**Сборщику про `setka`.** Поле собирается машиной, не глазами: `bedySetki` и
`lishnieSlova` из `lib/content/setka.ts`. Предлагаю `TEACHER`, `STUDENT`,
`LESSON`, `SCHOOL`, `LEARN`. **Прогон обязателен, и вот почему я его не заменяю
чтением:** `TEACHER` содержит внутри `HE` — карточку модуля 2, а `STUDENT`
содержит `TEN` — число, которое курс даёт. Ровно тем и кончилась сетка модуля 5:
`NAME` спрятало внутри себя `AM`. Лишнее слово внутри искомого перестановкой не
лечится — слово меняется целиком.

---

### Урок 6. «Утром и вечером: `in the morning`» — правила, тема `Time`

**Итог:** `ПИШЕТ РЕДАКТОР` — называть часть суток оборотом и говорить, что
делаешь каждый день.

**Вступление:** русская фраза. `ПИШЕТ РЕДАКТОР` — по-русски «утром» это одно
слово, по-английски их три.

**Случаи (8):**

| № | Что утверждает | Английские строки |
|---|---|---|
| 1 | русское «утром» — три английских слова | `I work in the morning.` · `Dana reads in the morning.` |
| 2 | так же днём и вечером | `I read in the afternoon.` · `We watch television in the evening.` |
| 3 | **`at night` устроено иначе**: предлог другой и `the` нет | `I sleep at night.` · `My father works at night.` |
| 4 | таблица четырёх оборотов | `in the morning` / `in the afternoon` / `in the evening` / **`at night`** |
| 5 | оборот стоит в конце строки | `My mother works in the evening.` · `I get up in the morning.` |
| 6 | `every day` — каждый день | `I study English every day.` · `She goes to school every day.` |
| 6а | еда по частям суток — три слова, переданные модулем 8 | `I eat breakfast in the morning.` · `I eat lunch in the afternoon.` · `I eat dinner in the evening.` |
| 7 | врезка об ошибке | `in night` и `in the night` — обе настоящие, обе от таблицы |
| 8 | свод: четыре оборота рядом, но НЕ сложенные в одну строку | сводит случаи 1–6; разбор запрета — раздел 6.8 |

**ГРАНИЦА С МОДУЛЕМ 18 — названа честно и передаётся обоим (раздел 11).**
Модуль 18 «Дни, время, дата, встреча» объявлен с грамматикой «предлоги времени»
и той же темой `Time`. Я беру **четыре готовых оборота части суток и `every
day`**, и больше ничего: часа (`at seven o'clock`), дней недели (`on Monday`),
месяцев и дат я не беру вовсе.

**Опора у оборотов прямая, а не по похожести.** Cambridge, Young Learners,
с. 29, список Pre A1 Starters, графа «Prepositions of place and time»:
**«We go to school in the morning.»** И English Grammar Profile,
`DETERMINERS/articles`, **FORM: PREPOSITION + 'THE' + NOUN**, ступень A1,
`id=1741163708789x819248395543273500`: «Can use 'the' in prepositional phrases
relating to time» — «I work **at the weekend**…».

**И вот чего делать НЕЛЬЗЯ, а хочется.** Модуль 3 объяснил выбор между `a` и
`the` смыслом: «Если собеседник ещё не знает, ставим `a`, а если уже знает,
ставим `the`». Под это правило `in the morning` не подводится ни с какой
стороны — никакого «уже известного утра» тут нет. **Обороты даются целыми, как
модуль 6 дал `at school`**, и редактору это надо сказать словами, иначе ученик
пойдёт применять правило модуля 3 и не сойдётся.

**Задания (8):** `gap` ×2, `choice`, `match` (часть суток → значок), `order`,
`hottext`, `short`, `speak`.

**Случаев в уроке 6 выходит девять, а не восемь**, и это не растяжка ради числа:
три карточки еды пришли передачей модуля 8 (раздел 17.1), и без своего случая
они стали бы мёртвыми — теми самыми, о которых `check:content` сообщает
сведениями. Восемь случаев — ориентир, а не норма (решение владельца).

---

### Урок 7. «Читаем: день Нурлана» — чтение

**Итог:** `ПИШЕТ РЕДАКТОР` — находить в коротком рассказе, чем человек занят и
когда.

**Жанр (`genre`): `article`** — короткая запись о человеке. Проверка требует от
`article` пятнадцати слов и больше.

**Почему не `message` и не `email`.** Урок письма этого модуля (урок 10) пишет
не письмо, а несколько простых предложений о себе — так стоит у источника
(CEFR, с. 67, Creative writing, A1: «Can produce simple phrases and sentences
about themselves and imaginary people, where they live and **what they do**»).
Правило проекта требует брать жанр урока письма из урока чтения того же модуля.
Значит и читать надо запись о человеке, а не переписку.

**Текст** — `ПИШЕТ РЕДАКТОР`, 6–8 простых предложений, ни одного слова вне
словаря модуля и курса. Строение задаёт замысел:

1. кто он и где живёт (`be` — знакомо);
2. что он делает утром;
3. где работает;
4. что делает вечером;
5. что делает его сестра — **третье лицо с `-s`**, ради чего текст и написан;
6. что они делают вместе (`we`).

**ОГРАНИЧЕНИЕ, КОТОРОЕ НЕЛЬЗЯ ПЕРЕСТУПИТЬ.** CEFR, с. 63, графа A1 — «a series
of simple sentences». Связного рассказа с причинами и последовательностью тут
быть не должно: ни `because` (модуль 20), ни `then`, ни `after that`. Между
предложениями только точка и `and` (модуль 6).

**Задания (6):** `choice` ×2 (о чём сказано), `short` ×2 (ответ одним словом),
`hottext` (отметить в тексте глаголы с `-s`), `match` (человек → его дело).

**Переводом текст не снабжается** — решение владельца от 6 сентября: у связного
текста перевода нет, незнакомое слово получит всплывающую подсказку. **Звука у
текста чтения пока не надо** — отдельным ответом того же дня.

---

### Урок 8. «Слушаем: кто что делает» — слушание

**Итог:** `ПИШЕТ РЕДАКТОР` — записывать со слуха, чем человек занят и когда.

**Форма ответа задана источником и другой быть не может.** Руководство
Cambridge A2 Key, с. 29, раздел THE TASK: «The candidates should only write ONE
word, or a number, or a date, or a time for their answer.» Значит ответы вида
`morning`, `teacher`, `school`, `evening` — и никаких предложений целиком.

**Две записи (`kind: "audio"`), обе разговоры на два голоса.**

| Запись | Кто говорит | О чём |
|---|---|---|
| 1 | `Dana` (жен.) и `Kim` (муж.) | Ким говорит, что делает утром и где работает |
| 2 | `Aigerim` (жен.) и `Bill` (муж.) | Билл говорит о сестре — третье лицо с `-s` |

**Сборщику про голоса, и это не мелочь.** У блока `audio` порядок голосов
решает поле `pervyyGolos`; без него первую реплику всегда читает женский. За
`Kim` в курсе закреплён МУЖСКОЙ голос (решение методиста модуля 2), и если
реплика Кима окажется первой, он заговорит женским. Проверяется это **только
слухом**: имя файла считается от текста и числа голосов, а порядок в него не
входит, и оба отчёта промолчат при любом раскладе.

**ЗАПРЕТ, КОТОРЫЙ НАРУШАЕТСЯ ЛЕГЧЕ ВСЕГО.** Ответ задания на слух не
напечатан рядом ни латиницей, ни РУССКИМИ буквами. Подпись записи не называет
ни `morning`, ни «утром»; подпись СЛЕДУЮЩЕЙ записи — тоже, потому что она стоит
после заданий. Правило приписано 6 сентября редактором модуля 5: там подпись
гласила «у стойки записывают Энн Бейкер», а заданием шло «запиши фамилию».

**Задания (5):** `short` ×3 (одно слово), `choice` ×2.

---

### Урок 9. «Говорим: мой день» — говорение

**Итог:** `ПИШЕТ РЕДАКТОР` — составлять о себе несколько простых предложений и
произносить их.

**Оговорка источника здесь решает устройство урока.** CEFR, с. 63, графа A1:
«…provided they can **prepare in advance**». Значит ученик сперва СОБИРАЕТ свой
рассказ, а потом произносит, и задание вида «скажи вслух прямо сейчас» ступени
не отвечает.

**Строение урока — лестница из трёх ступенек:**

1. **собрать** — `order` и `gap`: из готовых кусков сложить четыре строки о
   вымышленном человеке;
2. **заменить на своё** — `short` ×2: та же строка, но о себе;
3. **произнести** — `speak` ×3: сказать собранное вслух.

**Задание «о себе» ограничивается ЭКРАНОМ, а не полем `accept`.** Правило от
6 сентября: список принимаемых ответов ученику невидим, сузить им ничего нельзя.
Значит условие называет, из каких слов собирать: «возьми одно из слов урока».

**Ни одно задание урока 9 не проверяется машиной на смысл** — `speak` вне QTI.
Поэтому итог урока назван по проверяемой половине: «составлять… и произносить»,
а работа модуля спрашивает **составление**, а не произношение (раздел 8).

---

### Урок 10. «Пишем: что я делаю» — письмо

**Итог:** `ПИШЕТ РЕДАКТОР` — **проверять** запись о своём дне по списку: есть ли
у каждой строки глагол и стоит ли `-s` там, где надо.

**Итог назван по проверяемой половине нарочно.** Правило проекта: письмо машина
не оценивает, а каждый итог обязан спрашиваться работой. Итог, обещающий
«писать», был бы необеспеченным обещанием.

**Опора жанра.** CEFR, с. 67, шкала *Creative writing*, графа **A1**:

> Can produce simple phrases and sentences about themselves and imaginary
> people, where they live and **what they do**.

**И граница, которую надо назвать.** Соседняя графа **A2** той же шкалы:
«Can create **diary entries** that describe activities (e.g. **daily routine**,
outings, sports, hobbies)… with simple connectives like "and", "but" and
"because"». То есть **дневниковая запись о распорядке дня — это A2**, а не мой
урок. Мой урок — несколько отдельных предложений о себе, без связок, кроме `and`.

**Задание `essay`:**

- `sample` — `ПИШЕТ РЕДАКТОР`, **четыре простых предложения**;
- `checklist` — `ПИШЕТ РЕДАКТОР`, три пункта: у каждой строки есть глагол; там,
  где речь об одном человеке, у глагола `-s`; названа часть суток;
- `minWords` — **считается по самому короткому ВЕРНОМУ ответу, а не по образцу.**
  Составил его сам: `I am a student. I study every day. I work in a shop. My
  sister reads books.` — **шестнадцать слов**. Ставлю `minWords: 14`, с запасом
  вниз: 19 августа порог 12 отсекал верный ответ из 11 слов, и порода известна.

**ОБРАЗЕЦ ОБЯЗАН ПРОХОДИТЬ СОБСТВЕННЫЙ СПИСОК.** Редактору: прочитай `sample` и
`checklist` построчно друг против друга. Если в списке стоит «названа часть
суток», а образец начинается `I am a student.` и части суток не называет — это
ошибка того же вида, что «в каждом предложении есть I'm» при образце с «Hello!».

**И образец урока 10 не повторяет образца урока 9.** Проверить дословно: сравнить
`sample` урока 10 с тем, что ученик собирал в уроке 9, и с текстом урока 7.
Совпадение в трёх строках из четырёх — это один урок, а не два.

---

## 6. Пункты English Grammar Profile и решения о языке

Ключ — поле `id`, а не заголовок: `Guideword` повторяется по всему файлу.
Свободу каждого пункта проверил прогоном `npm run programma -- english-beginner-2`
и сверкой со списком «нерозданные пункты ступени A1»: их 64 из 109.

### 6.1. Что модуль вводит — `egp`, три пункта

| `id` | Категория, заголовок | Can-do и примеры | Где работает |
|---|---|---|---|
| `1741163713626x486355013779909760` | `PRESENT/present simple`, **FORM: AFFIRMATIVE**, A1 | «Can use the affirmative form with a limited range of regular and irregular verbs.» — «Every day at college I learn new words…» · «She plays tennis and she likes going to the swimming pool.» | уроки 1–4 целиком |
| `1741163713626x414037782388433340` | `PRESENT/present simple`, **USE: HABITS AND GENERAL FACTS**, A1 | «Can use the present simple to talk about repeated events or habits, and general facts.» — «I work in a supermarket.» · «I watch TV in the evening. I read books at night.» · «She goes to work by bus.» | уроки 6, 7, 9, 10 |
| `1741163715288x929829619225120500` | `VERBS/types`, **FORM: MAIN VERBS**, A1 | «Can use regular and irregular main verbs in simple verb phrases.» — «She comes from India.» · «He gets up at 6 o'clock and goes to work.» | урок 1, случай о глаголе действия |

**Третий пункт мне передан модулем 11 письменно** (`plan-modul-11.md`, раздел
4.3, п. 4 и раздел 12.2): «модуль 11 от него отказался нарочно, хотя два примера
графы из пяти и есть прямое обладание». Прогон `programma` подтверждает: пункт
стоит в списке нерозданных.

### 6.2. К чему модуль возвращается — `egpPovtor`

Все строки подтверждены **КОДОМ**: разбор полей `egp` и `egpPovtor` по всем
файлам `courses/english-beginner-2/*.ts` плюс порядок из `index.ts`. Замыслам
соседей на слово не верю — правило «замысел верен только на дату».

| `id` | Что это | Ввёл | Где работает у меня |
|---|---|---|---|
| `1741163711829x787492176446619900` | `NOUNS / noun phrases - grammatical functions`, FORM: AS OBJECTS | **модуль 11** | `I read books.`, `She studies English.` |
| `1741163708329x286701804737242940` | `CLAUSES / declarative`, AFFIRMATIVE DECLARATIVE | модуль 2 | все строки модуля |
| `1741163713868x463659211645272000` | `PRONOUNS`, (SUBJECT) STATEMENTS | модуль 2 | таблицы форм уроков 2 и 3 |
| `1741163715288x539616242661052000` | `VERBS / types`, LINKING VERB 'BE' | модуль 2 | противопоставление `I am a student` — `I study` |
| `1741163708789x105964971324936210` | `DETERMINERS / articles`, WITH NOUNS | модуль 3 | `a student`, `a teacher` |
| `1741163709005x427091401714639400` | `DETERMINERS / possessives`, WITH NOUNS | модуль 2 | `my teacher`, `my parents` |
| `1741163711821x759916598484829700` | `NOUNS / plural`, PLURAL '-S' | модуль 4 | урок 4, различение двух `-s` |
| `1741163708775x971363187748411900` | `CONJUNCTIONS / coordinating`, WORD + WORD | модуль 6 | `Dana and Kim live in Astana.` |

**ПРОВЕРИТЬ сборщику перед сборкой:** прогнать `npm run programma` заново.
Модули 14–18 пишутся параллельно, и пункт, свободный сегодня, завтра может быть
занят соседом — ровно как словарь, который «меняется под руками».

### 6.3. Пункт, который я НЕ беру, хотя он рядом

`1741163713626x606524312014730500`, `PRESENT/present simple`, **FORM: WITH
'REALLY'**, A1 — «I really like my kitchen…». Не беру: слова `really` в курсе нет
ни разу (счёт по видимому тексту двенадцати модулей — 0), а модуль 20 объявлен с
темой вкусов и `like`. Пункт вместе со словом отдаю **модулю 20** (раздел 11).

Два пункта отрицания — `1741163713626x335670389512142900` и
`1741163716067x101148462288360720` — принадлежат **модулю 14 «Я не делаю»** по
строке программы и мной не берутся.

### 6.4. Два разных `-s` — и почему это решение о ЯЗЫКЕ, а не о порядке уроков

Разобрано в разделе 4.3. Здесь только то, что должно попасть в шапку файла:
**окончание `-s` у глагола и окончание `-s` у слова о вещах — разные вещи, и
курс уже научил второму (модуль 8).** Всякая строка урока, где стоят оба
(`She reads books.`), — это место, где ученик спотыкается.

### 6.5. Про звучание окончания решено МЕНЬШЕ, чем хотелось

Соблазн был расписать три звучания: `works` /wɜːks/, `reads` /riːdz/, `watches`
/ˈwɒtʃɪz/. Все три стоят в `materials/ipa-en-uk.txt` дословно, и это утверждение
о ЗВУКЕ, а не о просодии, — значит оно законно (граница названа в CLAUDE.md:
«сказано о том, КАКОЙ звук, — законно»).

**И всё-таки беру только одно из трёх: `watches`, `studies` и `goes` звучат на
слог длиннее.** Довод не в осторожности, а в том, что этому курс уже научил —
модуль 8, разбор задания урока 2, дословно:

> «У этих двух слов прибавляется окончание `-es`, и во множественном числе они
> звучат **на слог длиннее**. У слов `city`, `day` и `potato` длина остаётся
> прежней, хотя букв на письме прибавилось.»

Ту же мысль модуль 13 повторяет о глаголе — и это связь, а не повтор.
Различение /s/ и /z/ на A1 не нужно ни одному заданию модуля и осталось бы
сведением, которое негде применить.

### 6.6. Имя времени называется один раз — `Present Simple`

Правило проекта: время называется английским именем, при первой встрече внутри
ступени, и без разговора о том, зачем имя нужно.

**Имя даётся, потому что его даёт источник, и не один.**

1. Руководство Cambridge A2 Key, **с. 52**, перечень *Tenses*, дословно:
   «**Present simple**: states, **habits**, systems and processes and with future
   meaning».
2. English Grammar Profile — подкатегория зовётся `present simple`, четыре
   записи ступени A1.
3. Young Learners, **с. 29**, список Pre A1 Starters, графа Verbs — строка
   «**Present simple**».

**Где именно называется:** урок 1, последний случай, одной строкой. Ни в одном
другом уроке модуля имя не повторяется.

**Счёт, ради которого это записано:** в видимом тексте двенадцати написанных
модулей слова `Present Simple` нет **ни одного раза**. Курс ещё не назвал ни
одного времени, и модуль 13 называет первое. Ровно на этом обжигалась ступень
Elementary: одиннадцать модулей писали имя в шапке файла и молчали на экране.

### 6.7. Разницу между `study` и `learn` модуль НЕ утверждает

Соблазн был поставить случай «`study` — учиться где-то, `learn` — узнавать
новое». **Не ставлю: подпереть нечем.** Толкований ни один файл `materials/` не
содержит; словник A2 Key даёт обе записи пометой части речи и не более —
`study (v)`, строка 1999, и `learn` в том же списке `Education`. Викисловарь
разницы такого рода тоже не проводит.

Обе карточки получают широкий перевод; **сужать без опоры нельзя** — правило от
6 сентября о карточке `clock`. А противопоставление, ради которого случай
затевался, берётся из другой пары, где оно настоящее и видно из строки:
**`learn` и `teach`** — `I learn English.` · `My teacher teaches English.`

### 6.8. Два оборота времени в одной строке — НЕ ставлю

Первая редакция ставила сводом урока 6 строку `I read books in the evening every
day.` **Снимаю.** Порядка обстоятельств в строке наши источники не дают вовсе:
искал в `English Grammar Profile Online.csv` по `ADVERBS / position` (там ПЯТЬ
граф A1 — front position, frequency mid, 'really' mid, time end, place end — и
каждая говорит о ОДНОМ обстоятельстве, ни одна о двух рядом), в
руководстве A2 Key, с. 52, перечень *Verb forms* и *Clause types*, и в списке
Young Learners, с. 29 и 52. Ни один не говорит, что за чем.

Значит утверждать порядок нельзя, а поставить строку без утверждения — значит
дать ученику образец, которого мы не объясняем. **Свод урока 6 сводит четыре
оборота части суток и `every day`, но их не складывает.**

### 6.9. Два пункта об обстоятельстве времени — БЕРУ, и вот на чём они стоят

Оба свободны по прогону `npm run programma` и оба нужны уроку 6.

| `id` | Категория, заголовок | Can-do и пример | Что покрыто у меня |
|---|---|---|---|
| `1741163708789x819248395543273500` | `DETERMINERS / articles`, **FORM: PREPOSITION + 'THE' + NOUN**, A1 | «Can use 'the' in prepositional phrases relating to time.» — «I work **at the weekend**…» | `in the morning`, `in the afternoon`, `in the evening` |
| `1741163712047x121249829588899070` | `NOUNS / noun phrases - grammatical functions`, **FORM: AS ADJUNCTS**, A1 | «Can use nouns and noun phrases as adjuncts in some time expressions.» — «…I will see you **tomorrow morning**.» | `every day` |

**Оговорка, которую делаю сам.** Пример первой графы — `at the weekend`, а не
`in the morning`. Опора здесь на СОСТАВ («предлог + `the` + слово о времени»), и
это ровно то, чему учит урок 6. Прямой образец с моими словами даёт другой
источник: Young Learners, с. 29, **«We go to school in the morning»**. Одной
графы мне бы не хватило, двух хватает.

**`at night` не покрыто ни одной из двух граф** — там нет ни `the`, ни
существительного-обстоятельства без предлога. Его опора — тот же список Pre A1
Starters (графа Prepositions of place and time) и словник A2 Key, где `night`
стоит в теме `Time`. Отдельного пункта EGP под него **не нашёл**; искал по всему
файлу по словам `night`, `PREPOSITIONS`, `time` и по подкатегории `prepositions`.

### 6.10. Пункт, который курс употребляет с модуля 3 и не объявил НИКТО

`1741163711829x722500780296901000`, `NOUNS / noun phrases - grammatical
functions`, **FORM: AS COMPLEMENT OF PREPOSITIONS**, A1 — «Can use nouns and
noun phrases as complements of prepositions in prepositional phrases.»

Прогон `npm run programma` держит его в списке нерозданных. **А курс стоит на
нём с модуля 3:** `in Astana`, `from Spain`, `at school`, `in an office`,
`in a shop`.

**В `egp` я его НЕ беру, и это не скромность.** Поле `egp` значит «модуль
ВВОДИТ», а вводит его модуль 3, написанный и лежащий в коде. Поставить его себе
значило бы соврать следующему проверяющему тем же способом, каким врёт
устаревшая шапка.

**Ставлю в `egpPovtor` и передаю находку модулю 3** (раздел 11): пункт надо
дописать в `egp` файла `otkuda-ty.ts`. Пока этого нет, счёт «роздано 44 из 109»
завышает дыру.

---

## 7. Словарь: темы, карточки, чтение, значки

### 7.1. Темы модуля — `Time` и `Education`

Обе — точные имена из Приложения 2 словника A2 Key (с. 25–32), как требует
программа ступени.

**Ни одну из двух тем к тринадцатому модулю не объявлял никто.** Прогон
`npm run programma -- english-beginner-2`, раздел «тем не объявил ни один
модуль»: `Education (31)` и `Time (31)` стоят в списке.

**Тему `Time` со мной делят ещё три модуля** — 14, 16 и 18 по строкам программы.
Границу с модулем 18 называю в уроке 6 и передаю обоим (раздел 11).

### 7.2. ДВА СЛОВА КУРСА ПОЛУЧАЮТ ВТОРУЮ РАБОТУ, И ЭТО НАДО СКАЗАТЬ ВСЛУХ

Находка счёта, а не догадки: разбор поля `term:` по живым файлам курса.

| Слово | Карточка есть | Как стоит там | Как работает у меня |
|---|---|---|---|
| **`work`** | модуль 6, `moya-semya.ts` | перевод «работа», пример `My wife is at work.` | глагол: `I work in a shop.` |
| **`watch`** | модуль 11, `odezhda.ts` | перевод «часы», пример `her new watch`, значок `231a` | глагол: `He watches television.` |

**Второй карточки ни одному из двух не завожу.** В живом курсе нет ни одного
повторяющегося `term:` — проверено разбором всех файлов, что собраны в
`index.ts` (повторы есть только с мёртвым `kto-eto.ts`), и заводить первый
повтор ради одного модуля нельзя.

**Опора у утверждения «одно слово, две работы» прямая.** Словник A2 Key,
Приложение 1, помечает часть речи у каждой записи, и у обоих слов помета
двойная:

> строка 2242: `watch (n & v)`
> строка 2311: `work (n & v)`

Соседние записи для сравнения: `read (v)`, `study (v)`, `homework (n)` — по одной
помете. То есть двойственность названа источником, а не мной.

**Что из этого следует для урока.** Модуль 6 сказал ученику «`work` — это
работа», модуль 11 — «`watch` — это часы». Правило проекта о слове `Kazakh`:
«там, где ученик встретит его дважды, разницу надо назвать словами, а не
надеяться, что он поймёт по месту». Случай об этом стоит в уроке 1 — файл
`plan-modul-13-urok-1.md`.

### 7.3. Карточки модуля — двадцать четыре здесь и ещё три в разделе 17.1

Ступень у каждого слова проверена `npm run slovo` (Oxford 3000 — A1; словник
A2 Key — есть), наличие карточки — разбором поля `term:` по живым файлам курса.

**Глаголы — семь записей вне тем модуля; они и есть предмет грамматики:**

| Слово | Перевод (широкий) | Чтение | Значок | Где работает |
|---|---|---|---|---|
| `live` | жить | `/lɪv/` | 1f3e1 | **урок 1**: случай 1 и таблица; дальше уроки 2 и 3 |
| `go` | идти, ехать | `/gəʊ/` | 1f6b6 | урок 4, случай 5 (`She goes to school.`); урок 6, случай 6 |
| `play` | играть | `/pleɪ/` | 26bd | урок 4, случай 7 (`She plays in the park.`) |
| `sleep` | спать | `/sliːp/` | 1f634 | урок 6, случай 3 (`I sleep at night.`) |
| `eat` | есть | `/iːt/` | 1f37d | урок 6, случай 6а — три строки |
| `drink` | пить | `/drɪŋk/` | 1f375 | **урок 1**: таблица случая 2 и случай 6 (`I drink tea.`) |
| `get up` | вставать | двухсловная, ударения нет | 1f6cf | урок 6, случай 5: `I get up in the morning.` |

**Тема `Education` — одиннадцать слов, все из списка Приложения 2:**

| Слово | Перевод | Чтение | Значок |
|---|---|---|---|
| `teacher` | учитель | `/ˈtiːtʃə/` | 1f9d1-200d-1f3eb |
| `student` | ученик, студент | `/ˈstjuːdənt/` | 1f9d1-200d-1f393 |
| `lesson` | урок | `/ˈlesn/` | 1f4d6 |
| `university` | университет | запись источника расходится со словарной — 7.5 | 1f3db |
| `college` | колледж | `/ˈkɒlɪdʒ/` | 1f393 |
| `learn` | учить, узнавать | `/lɜːn/` | 1f9e0 |
| `teach` | учить кого-то, преподавать | `/tiːtʃ/` | 1f469-200d-1f3eb |
| `study` | учить, учиться | `/ˈstʌdi/` | 1f4da |
| `read` | читать | `/riːd/` | 1f4d5 |
| `write` | писать | `/raɪt/` | 270d |
| `know` | знать | `/nəʊ/` | 1f4a1 |

**Тема `Time` — три слова; остальные части суток у курса уже есть:**

| Слово | Перевод | Чтение | Значок |
|---|---|---|---|
| `morning` | утро | `/ˈmɔːnɪŋ/` | 1f305 |
| `afternoon` | день, время после полудня | записи в источнике нет; взять у карточки `good afternoon` модуля 2 — 7.5 | 2600 |
| `every day` | каждый день | двухсловная, ударения нет | 1f5d3 |

**Раздача карточек по урокам — чтобы ни одна не пришла позже, чем нужна:**

| Урок | Карточки |
|---|---|
| 1 | `live`, `read`, `drink`, `study`, `know` |
| 2 | — (работают уже данные) |
| 3 | — |
| 4 | `go`, `play` |
| 5 | `teacher`, `student`, `lesson`, `university`, `college`, `learn`, `teach`, `write` |
| 6 | `morning`, `afternoon`, `every day`, `get up`, `sleep`, `eat`, `breakfast`, `lunch`, `dinner` |

**Проверка, ради которой эта таблица и составлена:** ни одно слово не работает
в задании раньше, чем его дала карточка. Это ровно та порода, которую ловит
`npm run chain` («слово, которое задание требует раньше, чем его дал словарь»), и
которая в курсе уже случалась — `meeting` и `lesson` вводились в шестом уроке
модуля, а работали с четвёртого.

**Трёх слов НЕ беру, потому что карточки уже есть, и это находка счёта:**

| Слово | Где карточка | Что это значит для меня |
|---|---|---|
| `evening` | **модуль 1**, `alfavit.ts`: «вечер», `/ˈiːvnɪŋ/` | беру как знакомое, второй карточки не завожу |
| `night` | **модуль 1**, `alfavit.ts`: «ночь», `/naɪt/` | то же; оборот `at night` строится на ней |
| `school` | **модуль 6**, `moya-semya.ts`: «школа», пример `My son is at school.` | то же; урок 5 прямо ссылается на модуль 6 |

**Счёт: 7 глаголов + 11 слов темы `Education` + 3 слова темы `Time` + 3 слова,
переданных модулем 8 (раздел 17.1), = 24 новые карточки.** Плюс `work` и
`watch` во второй работе без новой карточки.

**Нормы в тридцать штук не добиваю нарочно** — уточнение владельца от 21 августа:
«не надо форсить».

### 7.3а. ТРИ КАРТОЧКИ СНЯТЫ, ПОТОМУ ЧТО ИМ НЕГДЕ РАБОТАТЬ

Первая редакция брала 27 слов и ручалась, что у каждого назван урок. **Проверил
сам, по таблице раздачи, и ручательство оказалось ложным: у семи слов работы не
было вовсе.** Четырём я работу дал, троих снял.

| Слово | Что с ним |
|---|---|
| `write` | **дал работу**: урок 5, случай 3 — `Students read and write.` (`and` — модуль 6) |
| `get up` | **дал работу**: урок 6, случай 5 — `I get up in the morning.` |
| `lunch` | **дал работу**: урок 6, случай 6а — `I eat lunch in the afternoon.` |
| `college` | **дал работу**: урок 5, случай 6 — `My sister studies at a college.` рядом с `He studies at a university.` |
| **`today`** | **СНЯТО.** Работы в модуле нет ни одной, а место слова там, где день и дата, — **модуль 18**. Передача в разделе 11.1 |
| **`class`** | **СНЯТО.** Всякая строка выходила натянутой (`Our class is here.`), а слово вдобавок двузначно — и комната, и люди. Честная причина: **модуль не тянет**, а не «мы его отмежевали» |
| **`homework`** | **СНЯТО.** Слово живёт при глаголе `do` (`do homework`), а `do` — **модуль 15 «Спросить о делах»**. Без него строка выходит мёртвая (`My homework is here.`). Передача в разделе 11.2 |

**Порода тут известная и записана в проекте дважды:** карточка, не
встречающаяся в модуле нигде, кроме себя самой, — это след нормы, добитой ради
счёта, и `check:content` сообщает о ней сведениями. Верный исход — снять
карточку, а не дописывать под неё строку; строку я дописал только там, где она
сама собой ложится в уже написанный случай.

### 7.4. `lesson` переводится «урок», и это решение, а не недосмотр

Замороженный курс перевёл `lesson` словом **«занятие»** дважды —
`chisla-vozrast-cena.ts` и `dni-daty-vremya.ts`. **Сюда это не годится:** слово
«занятие» в новом курсе занято модулем 6, где так зовут профессию («называть
занятие родного»), и подписями сцен модулей 1, 4 и 11, где так зовут школьный
урок: **72 формы** счётом по словам. Разбор — раздел 3.2.

Беру «урок». Слово совпадает с тем, как платформа зовёт наши собственные уроки,
и это не беда, а правда: вещь одна и та же. Заменять её выдуманным словом
(«школьный час», «занятие в школе») было бы ровно тем, против чего писано
правило о настоящем термине.

### 7.5. Чего проверить не удалось — чтение четырёх записей

| Слово | Что в `materials/ipa-en-uk.txt` | Как решаю и почему не сам |
|---|---|---|
| `live` | **записи нет**; есть `lived /lˈɪvd/` | замороженный курс держит `/lɪv/` в двух модулях — беру оттуда, но это склад, а не источник |
| `read` | **записи нет** (у слова два чтения) | замороженный курс держит `/riːd/`; формы прошедшего в модуле нет — беру `/riːd/` |
| `afternoon` | **записи нет** | у курса есть карточка `good afternoon` (модуль 2) — сборщику взять чтение оттуда, а не выдумывать |
| `university` | `/jˌuːnɪvˈɜːsɪtˌi/` — два знака ударения и `ɪ` на конце | словарная запись иная; **спорное слово решает владелец как преподаватель** |

Условности курса при записи остальных чтений соблюдены: `ɛ` пишем `e`
(`/ˈlesn/`), `ɹ` пишем `r` (`/riːd/`), `ɡ` пишем `g` (`/gəʊ/`), `ɐ` на конце
пишем `ə` (`/ˈtiːtʃə/`), слоговой согласный без `ə` (`/ˈlesn/`), а `student` —
названное в CLAUDE.md исключение и держит `/ˈstjuːdənt/`.

Двухсловные записи (`get up`, `every day`) ударения не несут — решение владельца
от 6 сентября: «в фразах можно не уточнять ударение».

### 7.6. Значки: у каждой карточки

Решение владельца от 6 сентября: «желательно не оставляй без картинок ни одну
карточку». Коды в таблицах 7.3 — **предложение методиста**; забрать их обязан
сборщик прогоном `npm run znachki`, а не я строкой в замысле.

**Три места, где жду возражения сборщика:** `know` (`1f4a1`, лампочка) — вещь
непредметная, и владелец сам назвал приём для таких («писатель допустим перо»);
`every day` (`1f5d3`) и `get up` (`1f6cf`) на телефоне могут не читаться вовсе. Решает прогон, а не чтение кода.

---

## 8. Работа модуля — двенадцать вопросов

**Работа написана ДО заданий уроков** — правило проекта. Задания уроков в
разделах 5 и в файле урока 1 названы видами и предметами, а их текст пишет
редактор позже; значит близнеца надо предупредить сейчас, а не ловить потом.

**`hint` в банке вопросов не пишется вовсе.** Поле мёртвое: `forBrowser` в
`lib/content/quiz.ts` вычёркивает его у вопроса работы, и `check:content`
объявляет его ошибкой. 767 таких подсказок в курсах написано впустую.

### 8.1. Счёт по части: какие рамки в части 3 уже заняты

Часть 3 «Мой день» начинается с моего модуля — **работ модулей 13–18 нет ни
одной, и работы части 3 тоже нет.** Значит счёт я веду по части 2, откуда ученик
только что пришёл, и он назван в задании: рамка **«отметь запись без ошибки»**
стоит там **восемь раз**, **«запиши строку без ошибки»** — **пять**.

**Обеих рамок в моей работе нет ни одной.** Это не осторожность: ученик,
прошедший часть 2, узнаёт рамку раньше, чем читает условие, и отвечает по
узнаванию вида задания.

### 8.2. Двенадцать вопросов

`ПИШЕТ РЕДАКТОР` стоит на всяком видимом русском тексте. Столбец «движение» —
то, что ученик ДЕЛАЕТ руками; по нему и разводятся близнецы (мерка «назови
вслух, что делает ученик»).

| № | Итог какого урока | Вид | Движение | Английские строки замысла |
|---|---|---|---|---|
| 1 | урок 1 | `order` | собрать строку из рассыпанных слов | `I` · `live` · `in Almaty` |
| 2 | урок 2 | `short` | переписать строку об одном как строку о нескольких | дано `Dana lives here.` → `Dana and Kim live here.` |
| 3 | урок 3 | `choice` | по форме глагола выбрать, кто мог стоять перед ней | дано `… reads books.` — варианты `My sister`, `My parents`, `We` |
| 4 | урок 3 | `gap` | вписать глагол в нужной форме | `My brother ___ in a shop.` → `works` |
| 5 | урок 4 | `hottext` | отметить в строке то `-s`, которое значит «их несколько» | `She reads books.` |
| 6 | урок 5 | `match` | соединить человека с его делом | `a teacher` / `a student` ↔ `teaches English` / `learns English` |
| 7 | урок 5 | `choice` | выбрать строку, где перед `school` нет артикля | `at school` против `at a school` и `at the school` |
| 8 | урок 6 | `gap` | вписать предлог в обороте части суток | `I sleep ___ night.` → `at` |
| 9 | урок 7 | `short` | прочитать три строки и ответить одним словом | `ПИШЕТ РЕДАКТОР`, ответ — одно слово словаря модуля |
| 10 | урок 8 | `short` со `zvuk` | послушать разговор и записать одно слово | ответ — одно слово; **разговор на два голоса** |
| 11 | урок 9 | `order` | собрать о себе строку из данных кусков | `I` · `study` · `every day` |
| 12 | урок 10 | `choice` | назвать, чего не хватает в чужой записи | `He work in a shop.` — не хватает `-s` |

**Порог зачёта — `passRatio: 0.8`**, как у всех работ курса. Ставится явно:
умолчание в `seed-courses.mts` даёт 0,7, и четыре работы курса из-за этого
молча требовали меньше остальных.

**Показывается `ask: 12`** — все двенадцать, как в работах модулей 2 и 5 после
правки 7 сентября.

### 8.3. Почему ни один из двенадцати не близнец задания урока

Мерка та же, что у скрипта не выходит: **назови вслух, ЧТО ДЕЛАЕТ ученик.**

| Вопрос | Ближайшее задание урока | Чем движение другое |
|---|---|---|
| 2 | урок 2, `short` | в уроке ученик СТРОИТ строку о нескольких людях с нуля; здесь ПЕРЕДЕЛЫВАЕТ данную строку об одном |
| 3 | урок 3, `match` «кто → форма» | в уроке дана пара и надо соединить; здесь дана ФОРМА и надо назвать возможного хозяина — ход от ответа к вопросу, в модуле не занятый |
| 5 | урок 4, `hottext` | в уроке отмечаются глаголы в связном куске; здесь одна строка с двумя `-s`, и выбор между ними |
| 11 | урок 9, `order` | в уроке собирается строка о вымышленном человеке; здесь — о себе, и условие называет, из каких слов |

**ГРАНИЦА МОЕГО РУЧАТЕЛЬСТВА НАЗВАНА, и она не «по модулю».** Я сверял двенадцать
вопросов с **замыслом** заданий уроков 1–10 этого модуля (разделы 5 и файл урока
1) и с **кодом** работ модулей 7–12, то есть всей части 2, прогоном
`npm run bliznetsy -- english-beginner-2 --hudshie 30`. **Чего я НЕ обошёл:**
текста заданий уроков — его ещё не существует, его пишет редактор. Значит
близнец, родившийся в тексте редактора, этой сверкой не пойман, и второй прогон
`bliznetsy` после написания текста обязателен.

### 8.4. Отвлекающие варианты: обе проверки, а не одна

**Первая: не подтверждает ли отвлекающий мой собственный модуль.**

| Вопрос | Отвлекающие | Проверка по тексту модуля |
|---|---|---|
| 3 | `My parents`, `We` | оба в модуле стоят с формой БЕЗ `-s` (уроки 2 и 3) — значит при `reads` они неверны, и модуль это подтверждает |
| 7 | `at a school`, `at the school` | модуль 6 сказал «у оборотов `at school`, `at work` и `at home` артикля нет вовсе»; ни одна строка курса их не содержит — проверено счётом по видимому тексту, 0 вхождений |
| 12 | «не хватает артикля», «не хватает формы be» | обе — настоящие ошибки учеников, и обе модуль разбирает врезками (уроки 1 и 3) |

**Вторая: а говорят ли так вообще?** Эта проверка на первую не сводится, и
именно на ней обожглись модули 8 и 25.

- `at the school` и `at a school` — **говорят**, и это надо знать: `He works at
  a school in Almaty` по-английски верно. **Поэтому условие вопроса 7 обязано
  спрашивать не «что верно», а «где перед `school` нет слова»** — иначе работа
  накажет за правоту. `ПИШЕТ РЕДАКТОР`, и это ограничение на условие, а не
  пожелание.
- `He work in a shop.` (вопрос 12) — не говорят никто и никогда, и это самая
  частая ошибка на этом правиле. Отвлекающий настоящий.
- Строки `I am work`, `We are work`, `He is works` — настоящие ошибки, и они
  стоят в уроках врезками. В работу их не беру: они уже отработаны, а работа
  спрашивает не память об уроке.

### 8.5. Строки, занятые работой: границу называю честно

Правило приписано 7 сентября: ручательство «уроки этих строк не употребляют»
ложно чаще всего потому, что автор сверял не то — по своему уроку, а близнец лежал
в соседнем.

**Что я обошёл:** все английские строки, названные в разделах 4–7 этого замысла
и в файле `plan-modul-13-urok-1.md`, — то есть весь замысел модуля, а не свой
урок.

**Строки работы, которых в замысле уроков нет ни в одном месте:**
`I live in Almaty.` · `Dana and Kim live here.` · `My brother works in a shop.` ·
`I sleep at night.` — стоит в уроке 6, случай 3, **и это надо развести**:
сборщику взять в вопрос 8 другую пару, например `My father works at night.`
переделанную под `___ night`. **Нашёл сам, перечитывая; правку вносит сборщик.**

**Чего я НЕ обошёл — и это половина беды:** текста, который напишет редактор.
Второй прогон `npm run bliznetsy` после написания текста заменить этой сверкой
нельзя.

---

## 9. Виды начал уроков — против однообразия

Порода известна: однородные уроки подряд повторяют один промах столько раз,
сколько их. Шесть уроков правил подряд легко получают одно и то же начало.

| Урок | Вид начала | Чем начинается |
|---|---|---|
| 1 | **русская фраза** | «я работаю» — по-русски одно слово, по-английски два |
| 2 | **сцена** | Дана говорит, где живут и работают её родители |
| 3 | **русская фраза** | по-русски глагол меняется при всяком лице, по-английски один раз |
| 4 | **сцена** | ученик видит `She reads books` и два `-s` подряд |
| 5 | **сцена** | Айгуль говорит о своей школе |
| 6 | **русская фраза** | «утром» — одно слово, а по-английски три |

**Обобщением («Present Simple — это время, которое…») не начинается ни один.**
Правило от 3 сентября: определение нашего же слова — не начало урока.

**И проверка, которой я проверял сам себя:** для каждого урока названа русская
фраза, которой ученик сказал бы то же самое. «Русской параллели здесь нет» —
это вывод, а не факт, и делать его, не назвав фразы, нельзя.

---

## 10. Что должно стоять в шапке файла модуля

Сборщику — дословно, это не пересказ:

1. **Имя главной вещи модуля — «глагол».** Термин заимствован у модуля 2, где он
   стоит в связке «форма глагола be». Модуль 13 его расширяет: глагол бывает не
   только `be`. Других имён у этой вещи в модуле нет.
2. **Общее слово модуля — «действие».** НЕ «дело» (занято модулем 2: `How are
   you?`), НЕ «занятие» (занято модулем 6: профессия), НЕ «привычка» (занято
   шестью врезками об ошибках: «русская привычка»).
3. **Имя времени `Present Simple` называется РОВНО ОДИН РАЗ** — урок 1, последний
   случай. Источники: A2 Key, с. 52, перечень *Tenses*; Young Learners, с. 29;
   подкатегория EGP.
4. **Расхождения источников по составу модуля НЕТ.** Present Simple с третьим
   лицом стоит у Cambridge на Pre A1 Starters (с. 29, графа Adverbs: `My grandma
   lives here`), у EGP на A1 (`FORM: AFFIRMATIVE`, пример `She plays tennis`).
   Строку о расхождении в шапку писать не надо — писать не о чем.
5. **Границы модуля, за которые он не заходит:** отрицание — модуль 14; вопрос с
   `do` — модуль 15; наречия частоты — модуль 16; Present Continuous — модуль 17;
   час, день недели, дата — модуль 18. Ни одного из пяти в модуле нет.
6. **`work` и `watch` работают глаголами при карточках-существительных** модулей
   6 и 11. Вторых карточек нет нарочно; помета источника — `work (n & v)`,
   `watch (n & v)`, словник A2 Key, строки 2311 и 2242.
7. **Долг курса закрыт здесь:** карточки `teacher` и `student`, переданные
   модулем 6 → модулю 3 → модулю 10 → сюда.

---

## 11. Находки о ЧУЖИХ модулях — передаю сводящему, править не могу

Правило проекта: находка о чужом модуле правится в чужом модуле сразу или не
правится никогда. Записка в шапке СВОЕГО файла бесполезна — её читают, работая
над своим.

### 11.1. Методисту модуля 18 «Дни, время, дата, встреча» — граница по времени

> Модуль 13 берёт из темы `Time` **только четыре готовых оборота части суток** —
> `in the morning`, `in the afternoon`, `in the evening`, `at night` — и оборот
> `every day`. Карточки: `morning`, `afternoon`, `every day`, `get up`;
> `evening` и `night` уже стояли в модуле 1.
> **Не беру и оставляю вам:** час (`at seven o'clock`), дни недели (`on Monday`),
> месяцы, даты, `tomorrow`, `yesterday`, `week`, `weekend`, `hour`, `minute`.
> **И карточку `today`** — я её снял, потому что в модуле 13 ей негде работать
> (раздел 7.3а); Oxford 3000 ставит её на A1, словник A2 Key держит в теме
> `Time`, карточки в живом курсе нет.
> Пункты EGP, которые я занял: `1741163708789x819248395543273500`
> (`the` в обстоятельстве времени) и `1741163712047x121249829588899070`
> (существительное обстоятельством времени). Оба вам придётся ставить в
> `egpPovtor`, а не в `egp`.
> **И предупреждение о выдуманном запрете:** я НЕ говорю ученику, что оборот
> части суток «всегда стоит в конце». Порядка обстоятельств источники не дают
> вовсе (раздел 6.8), и запрета в модуле 13 нет.

### 11.2. Методистам модулей 14, 15, 16, 17 — что уже занято

> Модуль 13 вводит **только утвердительную форму** Present Simple и три пункта
> EGP: `1741163713626x486355013779909760` (FORM: AFFIRMATIVE),
> `1741163713626x414037782388433340` (USE: HABITS AND GENERAL FACTS),
> `1741163715288x929829619225120500` (VERBS/types, MAIN VERBS).
> Ваши пункты свободны и мной не тронуты: отрицание —
> `1741163713626x335670389512142900` и `1741163716067x101148462288360720`
> (модуль 14); наречия частоты — `1741163706722x604132732561016200` и
> `1741163706930x543836854317855550` (модуль 16); Present Continuous —
> `1741163713121x244049823776565150` и `1741163713121x758287721695059000`
> (модуль 17).
> **Модулю 15 отдельно: карточка `homework` ваша.** Я её снял: слово живёт при
> глаголе `do` (`do homework`), а `do` — ваша строка программы. Oxford 3000 —
> A1, словник A2 Key — тема `Education`, карточки в живом курсе нет.
> **Двадцать четыре карточки модуля 13** — раздел 7.3; заводить их второй раз не
> надо. Тринадцать из них глаголы, и они работают у вас с первой же строки.
> **Модулю 17 отдельно:** карточка `wear` ваша — так решил модуль 11
> (`plan-modul-11.md`, раздел 12.1), и я её не беру. Ваш контраст
> `She is wearing a hat` строится против моего `I wear jeans`; **строки
> `I wear jeans` в модуле 13 при этом нет** — глагола `wear` у меня нет вовсе,
> и опереться вам придётся на любую мою строку Present Simple.

### 11.3. Методисту модуля 20 — пункт про `really` вместе со словом

> `1741163713626x606524312014730500`, `PRESENT/present simple`, **FORM: WITH
> 'REALLY'**, A1 — «I really like my kitchen…». Модуль 13 его не берёт: слова
> `really` в курсе нет ни разу (счёт по видимому тексту двенадцати модулей — 0),
> а `like` объявлен вашей строкой программы. Пункт свободен по прогону
> `npm run programma`.

### 11.4. Сводящему — о модуле 3 и пункте, которого не объявил никто

> `1741163711829x722500780296901000`, `NOUNS / noun phrases - grammatical
> functions`, **FORM: AS COMPLEMENT OF PREPOSITIONS**, A1. Прогон держит его в
> нерозданных, а курс стоит на нём с модуля 3 (`in Astana`, `from Spain`) и с
> модуля 6 (`at school`, `in an office`). **Дописать его в `egp` файла
> `otkuda-ty.ts`.** Модуль 13 ставит его себе в `egpPovtor` — вводит не он.
> Разбор — раздел 6.10.

### 11.5. Сводящему — части 3 в коде нет

> `courses/english-beginner-2/index.ts`, поле `parts`, держит **две части из
> пяти**: `ya-i-moi-lyudi` и `veshchi-i-mesto`. Модуль 13 открывает **часть 3
> «Мой день»** (модули 13–18 по программе), и её в перечне нет вовсе.
> Пока части нет, ни одна проверка о ней не спрашивает — то самое устройство,
> из-за которого экзамен Elementary сутки лежал невключённым. Работы части 2
> тоже нет, и модуль 12 её закрывает.

### 11.6. Сводящему — мёртвый файл держит две карточки долга

> `kto-eto.ts` не импортируется из `index.ts` и держит карточки `teacher`,
> `student`, `work`, `car`, `house` и ещё девятнадцать. Модуль 13 берёт `teacher`
> и `student` заново — **не копированием**, а своими примерами по теме
> `Education`. Про сам файл: находка модуля 12 (раздел 9.3 его замысла) о нём уже
> записана, и я её только подтверждаю счётом.

---

## 12. Что довезти до программы ступени — правку вношу не я

`docs/beginner-2/programma-stupeni.md`, **строка 593**, столбец «Итог»:

> | 13 | Что я делаю каждый день | **рассказывать о привычных делах** | … |

**Оба слова этой формулировки в курсе заняты, и заняты не мной** (счёт —
раздел 3.2):

- **«дела»** — модуль 2, урок «Как дела»: `How are you?`, плюс перевод карточки
  `fine` — «хорошо (о делах)». 52 формы: «дела» 21, «делах» 30, «дело» 1;
- **«привычка»** — шесть врезок об ошибках, все о РУССКОЙ привычке как источнике
  ошибки: «по-русски привычки ставить туда слово нет».

Ученик, прочитав «привычное дело», пойдёт искать либо `How are you?`, либо
ошибку. **Предлагаю строку: «рассказывать, что делаешь каждый день».**

**Урок отсюда тот же, что записан в самой программе после поправки к модулю 11:**
программа ступени устаревает от работы над модулями так же, как шапка файла, и
находка о ней не доезжает сама.

---

## 13. Что проверено — и каким признаком

Список сдаётся нарочно: круг на модуль один, а пород в проекте три десятка. Без
такого списка следующий проверяющий откроет то же заново либо не откроет вовсе.
И он же делает мою проверку **оспоримой**: назван признак, его можно перепрогнать.

| Порода | Каким признаком проверял | Что нашлось |
|---|---|---|
| предмет модуля уже есть в курсе | `npm run tekst` по **всем двенадцати** написанным модулям в один файл (25 229 строк) и поиск по нему 26 строк | `Present Simple` — 0, `every day` — 0, `I work`/`I live`/`I go` — по 0. Предмет свободен |
| урок повторяет соседний | сверка каждого урока с ПЕРЕЧНЕМ уроков всех двенадцати модулей по заголовкам | **урок 4 совпадал с уроком 2 модуля 8** — состав изменён (раздел 4.3) |
| слово требуется раньше карточки | разбор поля `term:` по живым файлам + порядок из `index.ts` | `hospital` — карточки нет ни у кого, строки заменены; `TV` — в курсе 0, взято `television` (карточка модуля 12); `football`, `film`, `music` — нет, строки заменены |
| вторая карточка тому же слову | `term:` по всем файлам, поиск повторов | `evening` уже в модуле 1, `school` в модуле 6 — не завожу; `work` и `watch` получают вторую РАБОТУ без карточки (раздел 7.2) |
| ярлык вместо термина | поиск каждого кандидата в видимом тексте курса | «дело», «занятие», «привычка» — все три заняты; взято «действие» (0 вхождений) |
| ступень слова | `npm run slovo` по каждому из 24 слов: Oxford 3000 и словник A2 Key | все 24 — A1 у Oxford и есть в словнике |
| ступень грамматики | разбор CSV `English Grammar Profile Online.csv` (1222 записи) **по всему файлу**, не по подкатегории | шесть записей A1 про present simple; отдельной графы про `-s` третьего лица **нет вовсе**, `-s` стоит примерами внутри графы FORM: AFFIRMATIVE |
| выдуманный запрет | поиск слов «всегда», «только», «нельзя» в собственном замысле | снят порядок обстоятельств (раздел 6.8) и разница `study`/`learn` (раздел 6.7) |
| цитата из соседней графы | у каждой цитаты прочитаны примеры и СОСЕДНИЕ строки графы | CEFR с. 63: графа A1 даёт «what they do», а «habits and routines» стоит на A2 — сказано вслух (раздел 2.1) |
| свобода пункта EGP | `npm run programma -- english-beginner-2`, список нерозданных | три пункта в `egp` свободны; один нерозданный курс употребляет с модуля 3 (раздел 6.10) |
| близнецы работы | `npm run bliznetsy -- english-beginner-2 --hudshie 30` по написанным работам + сверка с замыслом своих уроков | одно совпадение поймано самим замыслом: вопрос 8 и урок 6, случай 3 (раздел 8.5) |
| отвлекающий оказался правдой | каждый прочитан как утверждение и поискан в модуле И проверен по языку | `at a school` и `at the school` — верный английский; условие вопроса 7 переписано (раздел 8.4) |
| ответ на слух напечатан рядом | требование записано уроку 8 прямо, вместе с запретом на РУССКУЮ подпись | правило исполнено замыслом; проверить придётся редактору по готовому тексту |
| долг словаря | цепочка передач прочитана у всех троих (модули 6, 10, программа) и сверена по коду | `teacher` и `student` — только в мёртвом `kto-eto.ts`; долг закрывается уроком 5 |

## 14. Две правки имён, сделанные при последней сверке

Обе нашлись счётом по видимому тексту курса, а не чтением, и обе стояли в первой
редакции этого замысла.

### 14.1. Имени `Aigul` в живом курсе нет ни разу — взято `Aigerim`

Первая редакция ставила учительницу `Aigul` (урок 5) и её же вторым голосом
записи урока 8. **Счёт: `Aigul` — 0 вхождений в видимом тексте двенадцати
модулей.** Больше того, модуль 10 снял это имя нарочно и написал причину прямо в
коде (`kakoy-chelovek.ts`, строки 765–766): «имени `Aigul` в живом курсе нет ни
разу — все девять вхождений в выведенном файле».

Имена собственные ступенью не меряются, и завести новое было бы законно. **Но
заводить новое там, где готовое уже работает, незачем:** `Aigerim` стоит в
модуле 5 девять раз (бланк, образец письма, записи), и это женское имя из списка
голосов курса.

**Взято `Aigerim`** — урок 5, случай 1, и второй голос записи урока 8.

### 14.2. `Abay` в курсе — это УЛИЦА, а не человек

Первая редакция звала урок 7 «Читаем: день Абая». Счёт по видимому тексту:
**`Abay` встречается 21 раз, и все двадцать один — в сочетании `Abay Street`**
(модуль 5, адрес в бланке). Человека с таким именем курс не знает.

Строка `Abay reads books.` заставила бы ученика, прошедшего модуль 5, искать
улицу. **Взято `Nurlan`** — 72 вхождения, мужское имя, человек.

**Урок 7 зовётся «Читаем: день Нурлана».**

**Порода тут общая, и её стоит назвать:** имя, встречающееся в курсе часто, ещё
не значит «имя человека». Проверяется одной командой — не «есть ли слово», а
**в каком сочетании оно стоит**.

### 14.3. Ещё одно, что попалось по дороге, — находка о чужих модулях

`npm run bliznetsy -- english-beginner-2 --hudshie 30`, прогон 7 сентября, даёт
среди прочего пару, которой нет в записке дня:

> `[privetstvie-2 · q-vmesto-imeni-she]` ↔ `[kakoy-chelovek · q1-vpisat-mestoimenie]`
> `gap`, общих слов 33 %, совпало дословно: `she`
> модуль 2: «Grace — твоя подруга. Дальше о ней говорят, не повторяя имени.
> Впиши слово, которое стоит вместо имени.»
> модуль 10: «Ким пишет о своей тёте и во второй строке имени не повторяет.
> Впиши недостающее слово.»

**Движение одно и то же: вписать местоимение вместо имени в готовую рамку.**
Заменено имя и родственница — ровно то, о чём правило от 2 сентября говорит
«замена слова близнеца не снимает». Это два ВОПРОСА РАБОТ разных модулей, то
есть для ученика два соседних экрана.

Модули 2 и 10 — чужие, правку туда вношу не я. Передаю сводящему.

---

## 15. Оговорка о самом способе счёта — чтобы следующий не повторил моей ошибки

Все счёты этого замысла сделаны по одному файлу: вывод
`npm run --silent tekst -- english-beginner-2 <модуль>` по всем двенадцати
написанным модулям, склеенный в один, **25 229 строк**.

**Две вещи, о которых надо знать, читая эти числа.**

1. **Счёт по СЛОВАМ, а не по строкам.** Первая редакция считала `grep -c`, и это
   даёт число СТРОК с совпадением, а не число вхождений: слово «глагол» вышло
   65 вместо 69. Все числа разделов 3.2, 7.4 и 12 пересчитаны разбором на слова.
2. **Вывод `tekst` содержит служебные подписи разбора** — `_итог урока:_`,
   `_разбор задания:_`, `_подсказка:_`. Ученик их не видит. На счёты этого
   замысла они не влияют (ни «глагол», ни «занятие», ни «дела» в подписях не
   стоят), **но слово «урок» считать по этому файлу без разбора нельзя**: из 433
   вхождений форм 212 — это служебные подписи `_итог урока:_` и
   `_вступление урока:_` (по 106), а ещё 58 — подпись словаря «Слова урока»,
   которую ученик видит. То есть служебных 212, видимых 221, и мерить надо
   вычтя первые.

   **Числа тут я привожу пересчитанными**: первая редакция этого абзаца писала
   «340 из 431 — подпись `_итог урока:_`», и это было неверно вдвое. Поймал
   сам, прогнав `grep -c` по самой подписи вместо того, чтобы поверить своей же
   прикидке. Порода известная — сошедшийся счёт не доказывает, что считали то
   самое.

**И третье, общее.** Ни один из этих счётов не видит модулей 14–30: они не
написаны. Значит всякое «в курсе этого нет» здесь означает «нет в двенадцати
написанных модулях на 7 сентября 2026», и через сутки может устареть.

---

## 16. Дескриптор CEFR с. 63 ДЕЛИТСЯ С МОДУЛЕМ 3 — и это надо было проверить

Нашлось при последней сверке, поиском `Present Simple` и `I live` **по коду**, а
не по видимому тексту: обе строки стоят в комментариях, и первая проверка их не
видела.

**`courses/english-beginner-2/otkuda-ty.ts`, поле `sources` модуля 3, дословно:**

> «с. 63, шкала *Sustained monologue: describing experience*, графа A1: „Can
> describe themselves, **what they do and where they live**." Обе графы называют
> „где живёт" прямо — **это опора дела всего модуля и итога урока 1**.»

**То есть моя опора уже занята — наполовину.** Модуль 3 взял из этой строки
половину «where they live», модуль 13 берёт половину «what they do». Обе
половины стоят в одном предложении графы, и следующий проверяющий, открыв
программу, вправе спросить, отчего один дескриптор обслуживает два модуля.

**Ответ у модуля 3 приготовлен, и он верный.** Тот же `sources`, следующая
строка:

> «ВАЖНО: дескриптор требует уметь ответить, ГДЕ ЧЕЛОВЕК ЖИВЁТ, а не употребить
> глагол `live`. **Глагол — Present Simple и модуль 13**.»

И шапка того же файла, комментарий перед `const modul`:

> «ЧЕГО В МОДУЛЕ НЕТ И ПОЧЕМУ: глагола `live` и строки `I live in Astana` — это
> Present Simple, модуль 13… занятий (`student`, `teacher`) — тема `Work and
> Jobs` целиком у модуля 6.»

**Что из этого следует.**

1. **Карточка `live` и строка `I live in Astana` переданы мне модулем 3 прямо, в
   коде.** Я их беру (урок 1, случай 1 и словарь урока). Передача записана у
   обоих — у него в шапке, у меня здесь.
2. **Дескриптор с. 63 A1 делится по половинам предложения**, и это надо сказать
   в шапке моего файла одной строкой: модуль 3 — «where they live» через форму
   be; модуль 13 — «what they do» через глагол. Без такой строки проверяющий
   снимет мою опору как занятую.
3. **Модуль 3 ошибается в одном:** он пишет, что занятия (`student`, `teacher`)
   «у модуля 6», а модуль 6 их вычистил и передал дальше — модулю 3, потом 10,
   потом мне. Цепочка разобрана в разделе 0.3. **Это находка о чужом файле;
   правку в `otkuda-ty.ts` вношу не я** — передаю сводящему вместе с разделом
   11.4.

**И урок, который стоит записать отдельно.** Проверка по курсу, сделанная по
ВИДИМОМУ тексту, не видит того, что соседи написали о МОЁМ модуле: они пишут это
в комментариях и в `sources`. Оба поиска нужны, и они отвечают на разные
вопросы: видимый текст говорит, что ученик уже видел; код говорит, что соседи
мне отдали.

---

## 17. Ещё три передачи, найденные ПОИСКОМ ПО КОДУ И ПО ЗАМЫСЛАМ СОСЕДЕЙ

Найдены тем же поздним поиском, что и раздел 16: `grep` по строке «модуль 13» во
всех файлах курса **и во всех замыслах** `docs/beginner-2/plan-modul-*.md`.
Первая проверка по видимому тексту их не видела — они стоят в комментариях и в
чужих документах.

### 17.1. `breakfast`, `lunch`, `dinner` — БЕРУ, передача модуля 8

`docs/beginner-2/plan-modul-8.md`, раздел 10 «Долги модуля — все с адресами»:

> | `breakfast`, `lunch`, `dinner` | модуль 13 (тема `Time`) |

и раздел 5.1 того же документа:

> «`drink` и `eat` — глаголы, а Present Simple приходит только в модуле 13;
> `breakfast`, `lunch`, `dinner` — там же, где распорядок дня (модуль 13, тема
> `Time`).»

**Беру все три.** Ступень проверена `npm run slovo`: Oxford 3000 — A1, словник
A2 Key — есть у всех трёх. Карточек в живом курсе нет (разбор `term:`).
Работают в уроке 6: `I eat breakfast in the morning.` · `I eat dinner in the
evening.`

| Слово | Перевод | Чтение | Значок |
|---|---|---|---|
| `breakfast` | завтрак | `/ˈbrekfəst/` | 1f373 |
| `lunch` | обед | `/lʌntʃ/` | 1f371 |
| `dinner` | ужин | `/ˈdɪnə/` | 1f37d |

**Счёт карточек модуля становится 27**, а не 24: раздел 7.3 читать вместе с этой
таблицей.

**И поправка к чужому документу, которую передаю сводящему:** модуль 8 пишет,
что эти три слова относятся к теме `Time`. **Это неверно.** Все три стоят в теме
**`Food and Drink`** — прогон `npm run programma -- english-beginner-2` называет
их поимённо в списке непокрытых слов этой темы: «…**lunch**, break, **dinner**,
meal, **breakfast**, drink…». В моём модуле они пойдут карточками ВНЕ объявленной
темы, и это законно (у модуля 12 таких девять, у модуля 11 — пять), но счёт темы
`Food and Drink` они закроют, а не `Time`.

### 17.2. `live` и `I live in Astana` — передача модуля 3, БЕРУ

Разобрано в разделе 16. Здесь только вторая половина находки.

**Замысел модуля 3 ручается за врезку, которой в коде НЕТ.**
`docs/beginner-2/plan-modul-3.md`, строка 786:

> «Долг с адресом: глагол `live` и строка `I live in Astana` — модуль 13 «Что я
> делаю каждый день». **Адрес называется в уроке 2 врезкой**, иначе
> `npm run chain` поймает обещание без адреса, и правильно поймает.»

**Врезки в модуле 3 нет.** Прогон `npm run tekst -- english-beginner-2
otkuda-ty` и поиск по нему: слова `live` в видимом тексте модуля 3 — **0
вхождений**, названия «Что я делаю каждый день» — 0.

**Беды из этого не вышло** — обещания нет, значит и обещания без адреса нет, и
`chain` промолчит верно. Но ручательство замысла ложно, и следующий, кто на него
обопрётся, ошибётся. Порода известная: «проверено разбором» проверяется тем же
вопросом, что и всё.

### 17.3. `live` — то же от модуля 12

`docs/beginner-2/plan-modul-12.md`, строки 404 и 784:

> | `I live in a flat.` | Present Simple — модуль 13. Замороженный курс брал
> `I live in` готовым и сам назвал это «решением уровня преподавателя»; я не беру |
> | `live` | глагол, требует Present Simple | модуль 13 |

Подтверждает передачу модуля 3. **Карточка `live` — моя, и это записано у троих.**

---

## 18. Что осталось сделать другим — коротко, по ролям

**Второму методисту (разбор этого замысла):**

1. **Проверить границу с модулем 18** (раздел 5, урок 6; раздел 11.1). Я беру
   четыре оборота части суток и `every day`; можно возразить, что и это уже
   предлоги времени.
2. **Проверить решение снять урок о написании `-es`** (раздел 4.3) — я заменил
   его уроком о двух разных `-s`. Возражение возможно: написание глагола ученик
   иначе не увидит нигде.
3. **Проверить, не мал ли урок 2** (раздел 4.1). Форма при `I`, `you`, `we`,
   `they` одна, и урок держится на двух вещах: лицах и подлежащем-имени.
4. **Проверить счёт «лексических глаголов в курсе четыре»** (раздел 1) —
   он решает устройство урока 1.
5. **Перепрогнать ручательство раздела 8.5** о строках, занятых работой:
   границу я назвал, но обходил замысел, а не текст.

**Сборщику:**

1. `npm run programma` заново перед сборкой — пункты EGP могли занять соседи;
2. `npm run znachki` по 27 значкам раздела 7.3 и 17.1;
3. `bedySetki` и `lishnieSlova` по полю сетки урока 5 — глазами его строить
   нельзя;
4. `pervyyGolos` мужской у примера-разговора урока 1 (`Kim` говорит первым) и у
   записей урока 8;
5. развести вопрос 8 работы и случай 3 урока 6 — обе строки сейчас `I sleep at
   night.` (раздел 8.5);
6. **части 3 в `index.ts` нет вовсе** — раздел 11.5.

**Редактору — после написания текста:**

1. прогон `razobrat` по видимому тексту: тире внутри строки правила превращает
   её в пару «строка — перевод»;
2. прогон `sZvukom`: английская строка в правиле даёт вторую кнопку звука;
3. образец урока 10 против его же списка самопроверки — построчно;
4. образец урока 10 против того, что ученик собирал в уроке 9, и против текста
   урока 7.

**Сводящему — находки о чужих модулях:** разделы 11.1–11.6, 12, 14.3, 16 (п. 3),
17.1 (тема слов у модуля 8), 17.2 (ложное ручательство замысла модуля 3).

---

## Чего проверить НЕ удалось

Честнее назвать, чем умолчать.

1. **Чтение четырёх записей** — `live`, `read`, `afternoon`, `university`.
   Первых двух в `ipa-en-uk.txt` нет вовсе; у `afternoon` записи нет; у
   `university` запись расходится со словарной. Разобрано в 7.5, `university`
   уходит владельцу.
2. **Замыслов модулей 14–18 не существует** — проверено `ls
   docs/beginner-2/plan-modul-1*.md`. Значит правило «замысел соседа — такая же
   часть курса, как код» я исполнить не мог: читать нечего. Передачи раздела 11
   написаны в пустоту и должны быть прочитаны теми, кто эти модули возьмёт.
3. **Второй разбор PDF (`materials/.md/`) я открывал только для словника** —
   через `npm run programma`. Страницы 29 и 52 руководства Young Learners и
   с. 63, 67 описаний Совета Европы читал по ПЕРВОМУ разбору
   (`materials/.text/`). Оба разбора расходятся в разметке таблиц, и спорное
   место второго я не смотрел. Если проверяющий усомнится в строке «My grandma
   lives here» из графы Adverbs — её надо открыть вторым разбором.
4. **Порядок страниц в `cambridge-young-learners-pre-a1-a2.pdf` местами
   двоится** — предупреждение программы ступени. С. 29 я читал по самому тексту
   страницы (заголовок «Grammar and Structures List 29» стоит в конце блока), а
   не по оглавлению; с. 52 — по строке «The list below details what is new at A1
   Movers level». Но за нумерацию этого файла я не ручаюсь.
5. **Тексты уроков 7 и 10 не написаны** — их пишет редактор, и обе проверки
   («образец проходит свой список», «порог не отсекает верного») делаются по
   готовому тексту. Я назвал `minWords: 14` по САМОМУ КОРОТКОМУ верному ответу,
   который составил сам (16 слов), — но образца редактора я не видел.
6. **Значки не забирал** — `npm run znachki` не прогонял ни разу. Коды в
   разделе 7.3 предложены по каталогу Twemoji и могут не забраться.
7. **Поле сетки урока 5 не строил** — у методиста нет прогона `bedySetki` и
   `lishnieSlova`, и строить его глазами запрещено правилом от 6 сентября.
8. **Работа модуля сверена с заданиями уроков по ЗАМЫСЛУ, а не по тексту** —
   граница названа в 8.3. Текста ещё нет.
9. **Проверка по курсу шла двумя способами, и первый был неполон.** Сперва я
   искал по ВИДИМОМУ тексту (`npm run tekst`, 25 229 строк) — так найдено всё,
   что ученик уже видел. Позже пришлось искать ПО КОДУ (`grep` по
   `courses/english-beginner-2/*.ts`) и **по замыслам соседей** (`grep` по
   `docs/beginner-2/plan-modul-*.md`) — и только тогда нашлись четыре передачи
   мне: `live` от модулей 3 и 12, `eat`/`drink` и `breakfast`/`lunch`/`dinner`
   от модуля 8, половина дескриптора с. 63 у модуля 3. **Все четыре стоят в
   комментариях и в чужих документах, и видимый текст их не показывает.**
   Разделы 16 и 17.

   Значит правило «проверка по курсу идёт первой» надо читать как **три поиска,
   а не один**: видимый текст, код и замыслы. Я сделал первый вовремя, а два
   других поздно — и потому состав модуля пришлось править после того, как
   уроки были расписаны.
10. **Работы модулей и работы части в счёт «в курсе этого нет» не входили** —
    `npm run tekst` берётся по имени модуля и банки вопросов не отдаёт. Проверил
    выборочно `grep` по коду (`Present Simple`, `every day`, `really`, `I work`,
    `in the morning` — все нули вне комментариев), но сплошного обхода банков
    вопросов не делал.
