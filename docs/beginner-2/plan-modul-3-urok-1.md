# Модуль 3, урок 1 «Откуда ты: I am from Almaty». Замысел

6 сентября 2026. Пишет методист. **Весь видимый русский текст — вступление,
правила случаев, врезка, подписи, условия и разборы заданий — пишет редактор.**
Здесь сказано, ЧТО стоит на экране, какая английская строка, какой ответ и на что
это опирается.

Замысел модуля целиком — [plan-modul-3.md](plan-modul-3.md).

---

## 1. Шапка урока

```
slug: "otkuda-ty-2"
title: "Откуда ты: I am from Almaty"
vid: "pravila"
estimatedMinutes: 13
outcome: "называть, откуда человек родом: I am from и название города или страны"
```

**`slug` с хвостом `-2`** — так же, как `alfavit-2` и `zdravstvuyte-2`: в
замороженном курсе урок `otkuda-ty` уже есть, и хвост разводит адреса. Хвост
значит «второй курс», а не «второй модуль».

**`vstuplenie` — поле обязательное.** `lib/content/types.ts`, строка 626:
«Только у пошагового вида урока (`format: "shagi"`); там он обязателен, и это
проверяет `npm run check:content`». Именно на этой строке владелец споткнулся
3 сентября: на экране печатался `outcome`, написанный для сверочного скрипта.

**Чем вступление начинается: РУССКОЙ ФРАЗОЙ, а не обобщением.** По-русски мы
говорим «я из Алматы», «я из Казахстана» — и название при этом меняется: Алматы —
из Алматы, Казахстан — из Казахстана, Испания — из Испании. По-английски меняться
нечему: слово перед названием одно на все случаи, а само название остаётся, как в
словаре. Вступление называет эту параллель и говорит, что разберём: как сказать,
откуда ты, и откуда другой человек.

**Чего во вступлении быть НЕ ДОЛЖНО:** ни слова «предлог» (он тут не нужен: слово
одно, и правило говорится о нём самом), ни ссылки на наш прошлый урок вида «имя
названо, здороваться уже получается» — это обобщение о нашей же кухне, и на нём
владелец споткнулся 3 сентября, ни перечисления того, что будет дальше в модуле.

**Опора итога.** CEFR Companion 2020, **с. 79**, шкала *Information exchange*,
графа **A1**: «Can ask and answer questions about themselves and other people,
**where they live**, people they know, things they have». И **с. 63**, шкала
*Sustained monologue: describing experience*, графа **A1**: «Can describe
themselves, what they do and **where they live**».

**Опора состава.** English Grammar Profile, A1, `VERBS / linking`, **FORM: 'BE' +
COMPLEMENT** (`1741163715608x123988758899529200`): «Can use linking verb 'be'
with complements», и **пример самой графы — «Martin is from Poland.»** Это
дословно строка нашего урока. Второй пункт — `PREPOSITIONS / prepostions`, **FORM:
SIMPLE** (`1741163713112x806003192035927200`), пример «He works in Bognor Regis».
Третий — `FORM: PREPOSITION + NOUN PHRASE` (`1741163713112x603850914179882600`).

**Возвращение к пройденному.** Поле `vozvrat` с ссылкой на задание модуля 2 из
урока «Он, она, оно — is»: ученик снова выбирает `is` при имени, а здесь ставит
после него `from`. Точный `id` называет сборщик — модуль 2 сейчас правится, и
имена его заданий меняются.

---

## 2. Экраны: двадцать

| Экран | Что стоит |
|---|---|
| 1 | вступление |
| 2 | сцена, `example`, `razgovor: true`, два голоса |
| 3 | случай 1: `from` значит «из» |
| 4 | случай 2: название после `from` не меняется |
| 5 | случай 3: `from` одинаково перед городом и перед страной |
| 6 | словарь урока: карточка `from` |
| 7 | задание **З1** `gap` |
| 8 | случай 4: `he is from`, `she is from` |
| 9 | случай 5: вместо `he` и `she` ставят имя |
| 10 | задание **З2** `order` |
| 11 | врезка `mistake`: без формы be фразы нет |
| 12 | задание **З3** `short` |
| 13 | случай 6: короткая запись `I'm from`, `she's from` |
| 14 | задание **З4** `choice` |
| 15 | случай 7: название пишут с заглавной буквы |
| 16 | задание **З5** `hottext` |
| 17 | случай 8: английское название часто не похоже на русское, и таблица `naTomZheEkrane: true` |
| 18 | задание **З6** `gap` |
| 19 | задание **З7** `speak` |
| 20 | «готово» |

Случаев восемь, целых примеров с переводом шестнадцать — мерка формата
выполнена.

**Словарь стоит на экране 6, до первого задания.** Карточка `from` нужна заданию
З1; поставить её после было бы тем самым «задание требует того, чего в уроке не
было».

**Карточка в уроке одна, и это не недосмотр.** Остальные пятнадцать карточек
модуля стоят там, где их слова работают (замысел модуля, раздел 3.1). Названия
стран и городов карточками не становятся вовсе: имена собственные ступенью не
меряются, решение владельца от 15 августа 2026.

```
{ term: "from", translation: "из", example: "I am from Almaty.", hint: "/frɒm/" }
```

Чтение взято из `english-starter/rasskaz-o-sebe.ts`, строка 218: условности курса
там соблюдены.

---

## 3. Сцена (экран 2)

`id: "scena-pervyy-den-na-kursah"`, `kind: "example"`, `razgovor: true`.
`caption` называет случай: первый день на курсах, говорят, кто откуда.

```
— Hello! I am Dana. I am from Astana.
— Hi! I am Kim. I am from Almaty.
— Sue is from Spain. Tom is from Italy.
— My friend Bill is from France.
```

Перевод у каждой реплики, по правилу 9 формата.

**Форма живёт в сцене пять раз** — `from` стоит в пяти предложениях. Ученик видит
вещь целиком раньше, чем её разбор: приём Мёрфи, тот же, что в модуле 2.

**Голоса.** `pervyyGolos` не ставим: по умолчанию первую реплику читает женский
голос (`scripts/ozvuchka.mts`, строка 377), дальше чередование. Выходит Дана (ж),
Ким (м), Дана (ж), Ким (м). Ким мужской и в модуле 1, и в модуле 2 — расхождения
нет.

**Имена.** `Kim`, `Sue`, `Tom`, `Bill` — Young Learners, **с. 81**, графа Names,
колонка **Pre A1 Starters** (там же `Alex`, `Pat`, `Sam`, `Nick`, `Lucy`).
`Dana` — наше, звучало в модулях 1 и 2.

**КАРТА ЛЮДЕЙ УРОКА, и её надо держать во всех примерах.** Дана — Астана, Ким —
Алматы, Сью — Испания и Мадрид, Том — Италия и Рим, Билл — Франция, Алим — Турция
и Анкара. Все шестнадцать примеров ниже ей подчиняются: одно лицо нигде не
оказывается родом из двух мест. Правишь строку — сверься с картой.

`explain` под сценой пишет редактор. Сказать надо одно: в каждой строке после
формы be стоит `from`, а за ним название; меняется только название.

---

## 4. Восемь случаев

У каждого случая: имя, **правило одной строкой** и два целых примера с переводом.
Правило пишет редактор — здесь сказано, ЧТО оно утверждает.

**ЛОВУШКА РАЗБОРА СТРОКИ.** `razobrat()` в `components/lesson/Ekran.tsx`,
строка 74, режет строку по первому тире с пробелами, если слева два латинских
знака подряд, а справа два русских, — на этом и держится показ примера. В строке
ПРАВИЛА тире с пробелами по обе стороны ставить нельзя: правило разъедется надвое.

**ЗАПРЕТОВ В ПРАВИЛАХ НЕТ НИ ОДНОГО.** Ни «всегда», ни «только», ни «нельзя».
Единственное место, где сказано «так не говорят», — врезка на экране 11, и она
про пропуск формы be: графа `VERBS / linking`, `FORM: 'BE' + COMPLEMENT` держит
глагол в каждом своём примере.

### Случай 1 (экран 3). `from` значит «из»

Правило утверждает: чтобы сказать, откуда человек, после формы be ставим `from`,
а за ним название.

- `I am from Kazakhstan.` — Я из Казахстана.
- `I am from Turkey.` — Я из Турции.

### Случай 2 (экран 4). Название после `from` не меняется

Правило утверждает: по-русски название меняет окончание (Испания, из Испании), а
по-английски слово остаётся тем же, каким было.

- `Spain. I am from Spain.` — Испания. Я из Испании.
- `Italy. I am from Italy.` — Италия. Я из Италии.

**Примеры нарочно из двух предложений:** только так на одной строке видно, что
слово до `from` и после `from` одно и то же. Тире внутри английской части нет.

### Случай 3 (экран 5). `from` одинаково перед городом и перед страной

Правило утверждает: слово `from` одно и то же и перед названием города, и перед
названием страны.

- `Kim is from Almaty.` — Ким из Алматы.
- `Kim is from Kazakhstan.` — Ким из Казахстана.

**Человек в обоих примерах один нарочно:** меняется только название, и видно, что
слово перед ним не меняется.

### Случай 4 (экран 8). О другом человеке: `he is from`, `she is from`

Правило утверждает: о другом человеке говорят `he is from` и `she is from`;
меняется форма be, а `from` остаётся.

- `He is from Rome.` — Он из Рима.
- `She is from Madrid.` — Она из Мадрида.

`he` и `she` — карточки модуля 2, `is` разобрано там же.

### Случай 5 (экран 9). Вместо `he` и `she` ставят имя

Правило утверждает: на месте `he` и `she` может стоять имя человека, а форма be
при нём та же, `is`.

- `Bill is from France.` — Билл из Франции.
- `Alim is from Ankara.` — Алим из Анкары.

**Это возвращение к модулю 2 в новой работе, а не новый случай**, и так его надо
подать: там ученик ставил `is` при имени, здесь ставит и добавляет `from`. Пункт
источника стоит у модуля 3 в `egpPovtor`.

### Случай 6 (экран 13). Короткая запись: `I'm from`, `she's from`

Правило утверждает: `I am` и `she is` записывают короче, `I'm` и `she's`, и `from`
при этом не меняется.

- `I'm from Almaty.` — Я из Алматы.
- `She's from Astana.` — Она из Астаны.

Обе строки дословно есть в `english-starter/rasskaz-o-sebe.ts` (таблицы уроков 1
и 5) — **запись звука приходит бесплатно**: имя файла считается из текста.

Короткая запись разобрана в модуле 2. Апострофа заново не объясняем.

### Случай 7 (экран 15). Название пишут с заглавной буквы

Правило утверждает: названия стран и городов — такие же имена, как имена людей, и
пишутся с заглавной буквы, где бы в предложении ни стояли.

- `He's from Turkey.` — Он из Турции.
- `I'm from Kazakhstan.` — Я из Казахстана.

Обе строки тоже есть в замороженном курсе, звук бесплатный.

Опора: English Grammar Profile, A1, `NOUNS / types`, **FORM: PROPER NOUNS**
(`1741163711816x743806450459332500`): «Can use proper nouns with a capital letter
to refer to specific people, **places**, days and things», пример графы —
«**In Leicester** I live in an area called Hamilton». Модуль 1 этот пункт ввёл,
модуль 3 к нему возвращается.

### Случай 8 (экран 17). Английское название часто не похоже на русское

Правило утверждает: у многих стран и городов английское название расходится с
русским, и такое название запоминают отдельно.

- `I am from France.` — Я из Франции.
- `Tom is from Rome.` — Том из Рима.

**Таблица на том же экране** (`kind: "table"`, `naTomZheEkrane: true`):

```
caption: «Как это пишется по-английски»
head:    ["По-английски", "По-русски"]
rows:    Kazakhstan/Казахстан, Turkey/Турция, Spain/Испания, Italy/Италия,
         France/Франция, Almaty/Алматы, Astana/Астана, Madrid/Мадрид,
         Rome/Рим, Ankara/Анкара
zvuchat: все десять названий, каждое своей записью
```

`zvuchat`, а не `zvuk`: произносится ровно то, что напечатано. Все десять записей
уже есть в `english-starter/rasskaz-o-sebe.ts` (блок `tablica-stran`, строки
311–313) — **бесплатны**. Чтение `Almaty` и `Astana` задано
`lib/content/proiznoshenie.ts`: по-казахски, а не так, как их прочёл бы англичанин.

**Чего в таблице НЕТ.** Столбца «Столица»: слово `capital` работает в уроке 4
(`the capital`), а здесь стало бы карточкой, которой негде работать. И ни слова о
том, почему `Turkey`, а не `Türkiye`: пометок о названиях в `materials/` нет ни
одной, а замороженный курс сделал из этого задание с вариантом
`I'm from Türkiye.` Мы — нет.

---

## 5. Врезка (экран 11)

`id: "bez-formy-be-frazy-net"`, `kind: "note"`, `tone: "mistake"`.

Утверждает: «Bill from France» — без формы be, и так по-английски не говорят.
По-русски глагола в такой фразе нет вовсе («Билл из Франции»), поэтому его легко
потерять. Правильно `Bill is from France`.

**Почему ошибка настоящая, а не выдуманная.** Она прямо следует из расхождения с
русским: в русском предложении «Билл из Франции» глагола нет ни одного, и
русскоязычный ученик переносит это устройство в английский. Замороженный курс
ловил ту же ошибку и тем же способом (`bez-glagola-nelzya`,
`rasskaz-o-sebe.ts`, строка 204).

**Врезка стоит ПОСЛЕ случая 5 и ПЕРЕД заданием З3 нарочно.** Задание З3 просит
вернуть форму be — и ответ во врезке не напечатан: там разобрана строка с `Bill`
в утверждении, а З3 даёт другую запись. Проверка «верный ответ задания напечатан
во врезке выше» на этом месте должна молчать; если она заговорит, менять надо
задание, а не врезку.

**Одной врезки достаточно.** Второй, про заглавную букву, не будет: это случай 7,
у него своё правило и своё задание.

---

## 6. Семь заданий: лестница «узнать, произвести, применить к себе»

| № | id | Вид | Что делает ученик | Случай | Ступенька |
|---|---|---|---|---|---|
| З1 | `o-dopisat-from` | `gap` | вписывает `from` | 1, 3 | произвести |
| З2 | `o-sobrat-o-ney` | `order` | собирает `She is from Madrid.` | 4 | произвести |
| З3 | `o-vernut-formu-be` | `short` | возвращает потерянную форму be | 5, врезка | произвести |
| З4 | `o-korotkaya-zapis` | `choice` | узнаёт короткую запись среди трёх | 6 | узнать |
| З5 | `o-oshibka-v-nazvanii` | `hottext` | отмечает строки, где название со строчной буквы | 7 | узнать |
| З6 | `o-dopisat-turkey` | `gap` | пишет английское название страны | 2, 8 | произвести |
| З7 | `o-skazat-o-sebe` | `speak` | говорит вслух, откуда он сам | 1, 6 | применить к себе |

Требующих произвести — четыре из семи.

**Лестница идёт не по номерам, а по экранам.** Задание стоит сразу за своим
случаем, поэтому «узнать» приходит на экране 14, а «произвести» уже было на 7.
Это не нарушение: лестница считается внутри одного случая (сперва узнать форму,
потом произвести её), а случаи идут по своему порядку. Иначе ученик пять экранов
читал бы без единого задания.

**Слово «ступенька» на экран не выносится.** Стоит «задание 3 из 7» — лестница
это наша кухня, и в курсе это уже поймала проверка.

### З1 (экран 7) — `gap`

```
prompt: смысл — «Ким родом из Алматы. Допиши недостающее слово.»
before: "Kim is "
after:  " Almaty."
answer: "from"
hint:   ведёт, а не отвечает: «Четыре буквы. Это слово стоит в словаре урока.»
why:    Kim is from Almaty. После формы be ставят from, а за ним название.
```

**Что ещё сюда подходит?** Ничего: `in` ученику ещё не давали, а по-русски условие
говорит «родом», а не «сейчас». Ответ один.

### З2 (экран 10) — `order`

```
prompt: смысл — «Собери предложение: „Она из Мадрида".»
items:  ["Madrid", "She", "from", "is"]
answer: [1, 3, 2, 0]
hint:   «Сначала тот, о ком речь, потом форма be.»
why:    She is from Madrid. Порядок один: кто, форма be, from, название.
```

**Проверка на порода 14 («сколько верных порядков»).** Верный порядок один:
`From Madrid she is` и `Madrid she is from` по-английски не строятся. Задание не
наказывает за верный ответ.

### З3 (экран 12) — `short`

```
prompt: смысл — «Ученик написал: Sue from Spain. Запиши строку без ошибки.»
answer: "Sue is from Spain."
accept: ["Sue is from Spain", "Sue's from Spain.", "Sue's from Spain"]
exact:  НЕ ставить — ошибка не в регистре, а в пропущенном слове
hint:   «Потеряно одно слово, и по-русски его в этой фразе нет вовсе.»
why:    Sue is from Spain. Годится и короткая запись Sue's from Spain.
```

**`accept` перечитан вместе с условием** — правило, приписанное 22 августа
2026. Короткая запись `Sue's from Spain` верна и потому принимается: решение
владельца «несколько верных решений можно, если они правильные». Имя в `accept`
то же самое, что в условии, — `Sue`, а не `Dana`: ровно на этом расхождении
проверяющий поймал работу модуля 15 Elementary.

**Строка `Sue is from Spain.` во врезке не напечатана** (там `Bill`), и в примерах
случаев её нет: в сцене стоит `Sue is from Spain. Tom is from Italy.` одной
строкой. Если сборщик решит, что и это совпадение, задание меняет имя на `Tom` —
но тогда меняются и `accept`, и разбор, и условие разом.

### З4 (экран 14) — `choice`

```
prompt: смысл — «Какая строка значит то же, что I am from Italy?»
options: "I'm from Italy."  ← верно
         "I from Italy."
         "Im from Italy."
hint:   «Форма be никуда не делась, она просто срослась со словом I.»
why:    I'm from Italy. Апостроф стоит там, где убрали букву a.
```

**Оба неверных варианта — настоящие ошибки, а не выдумка.** `I from Italy` —
пропуск формы be, ровно та ошибка, о которой врезка; `Im from Italy` — потерянный
апостроф, самая частая описка в короткой записи. Ученика, который делает каждую
из двух, представить легко.

### З5 (экран 16) — `hottext`

```
prompt: смысл — «Отметь строки, где название написано с ошибкой.»
parts:  "I am from spain."     selectable, correct
        " · "
        "He is from Turkey."   selectable
        " · "
        "She is from madrid."  selectable, correct
        " · "
        "Kim is from Almaty."  selectable
hint:   «Смотри на первую букву названия.»
why:    Spain и Madrid — названия, и начинаются они с заглавной буквы.
        Turkey и Almaty записаны верно.
```

**Прочитано глазами того, кто языка не знает: попасть нельзя.** Все четыре строки
одинаковой длины и устройства, английские все четыре, и различие только в регистре
одной буквы — без правила случая 7 выбор не сделать.

### З6 (экран 18) — `gap`

```
prompt: смысл — «Алим родом из Турции. Допиши название страны по-английски.»
before: "Alim is from "
after:  "."
answer: "Turkey"
exact:  true
hint:   «Шесть букв. Слово стоит в таблице урока.»
why:    Alim is from Turkey. По-русски Турция, по-английски Turkey, и название
        пишется с заглавной буквы.
```

**`exact: true` стоит нарочно.** Без него сверка снимает регистр, и `turkey`
засчиталось бы верным — а урок только что учил заглавной букве. Это тот самый
случай, о котором предупреждает комментарий в `lib/content/check.ts`.

**Алим и Турция — по карте людей урока** (случай 5). Строка `Alim is from Turkey.`
в уроке нигде не напечатана: в случае 5 стоит `Alim is from Ankara.` Списать
неоткуда, таблица даёт только слово.

### З7 (экран 19) — `speak`

```
prompt: смысл — «Произнеси вслух, откуда ты родом. Подставь свой город или свою
        страну.»
phrase: "I am from Almaty."
translation: "Я из Алматы."
hint:   «Скажи два раза: сначала с городом, потом со страной.»
why:    Это первое, что скажешь при знакомстве. Проговори, подставляя своё.
```

**Почему «применить к себе» стоит `speak`, а не `short`.** Задание «напиши о
себе» имеет бесконечно много верных ответов, а в `answer` стоял бы один — та самая
порода, где задание наказывает за верный ответ. Устная строка машиной не
оценивается, и подвоха в ней нет.

**Строка `I am from Almaty.` стоит и в карточке `from` как `example`.** Это
законно и нарочно: образец для произнесения должен быть тем, что ученик уже видел.

---

## 7. Значки Twemoji

Поле `znak` стоит у случая, врезки, таблицы и словарного слова. Своё рисование
прекращено, библиотеки и лицензии — `docs/beginner-2/kartinki-otkuda.md`, файлы
забирает `npm run znachki`.

| Где | Код | Что это |
|---|---|---|
| модуль | `1f5fa` | карта мира |
| случай 1 | `1f30d` | глобус |
| случай 3 | `1f3d9` | город |
| случай 4 | `1f46b` | двое: он и она |
| случай 8 | `1f6c2` | паспортный контроль (разные страны) |
| карточка `from` | `27a1` | стрелка: движение откуда-то |

**Правило, которое здесь легко нарушить:** значок не повторяет смысл значка,
уже занятого другим блоком, и не ставится там, где различить его нельзя. У
случаев 2, 5, 6 и 7 значка нет нарочно: «название не меняется», «вместо he имя»,
«короткая запись» и «заглавная буква» — вещи не предметные, и значок к ним будет
шумом. Так же поступил модуль 2 (случай 2 без значка).

Коды проверяет сборщик по набору Twemoji: **я их не открывал**, и это стоит в
разделе 8.

---

## 8. Что проверено и чего проверить не удалось

**Проверено мной, открытием файла:**

- CEFR Companion 2020, с. 79 и с. 63 — обе графы A1 выписаны дословно;
- English Grammar Profile — разбор CSV, 1222 записи, 109 на A1; открыл целиком
  `VERBS / linking` FORM: 'BE' + COMPLEMENT, `PREPOSITIONS` FORM: SIMPLE и FORM:
  PREPOSITION + NOUN PHRASE, `NOUNS / types` FORM: PROPER NOUNS — у каждого
  выписаны `Can-do statement` и примеры;
- Young Learners, с. 81, графа Names, колонка Pre A1 Starters — все двадцать имён
  колонки;
- замороженный курс: `rasskaz-o-sebe.ts` целиком (уроки 1–7), `gde-eto.ts` урок
  «Места в городе», `vopros-i-otvet.ts` по заголовкам; чтение `/frɒm/` и все
  строки, объявленные бесплатными по звуку, найдены в файлах;
- `lib/content/types.ts`, строка 626 — обязательность `vstuplenie`;
- `components/lesson/Ekran.tsx`, строка 74 — `razobrat()` и правило про тире;
- `courses/english-beginner-2/kto-eto.ts` — что модуль 2 уже дал: `he`, `she`,
  `my`, `friend`, `I'm`, `he's`, `she's`, `is` при имени.

**Чего проверить не удалось:**

1. **Кодов Twemoji я не открывал.** Все шесть названы по памяти о наборе, и любой
   может оказаться не тем значком или не существовать вовсе. Сборщик обязан
   сверить их с `public/twemoji` после `npm run znachki`; если кода нет, поле
   `znak` просто не ставится — оно необязательное.
2. **Строка 377 `scripts/ozvuchka.mts`** (женский голос первым) названа по разбору
   модуля 2, сам файл я на этой строке не открывал. Если номер уехал, вывод о
   порядке голосов от этого не меняется: он проверен в модулях 1 и 2 слухом.
3. **Что записи звука для десяти названий и шести фраз действительно лежат в
   `public/`,** я не проверял — проверял, что дословно те же строки стоят в
   замороженном курсе. Имя файла считается из текста, поэтому вывод «запись
   бесплатна» верен, только если строка совпадает **посимвольно**. Сборщику надо
   прогнать `npm run ozvuchka` и посмотреть, сколько записей он делает заново:
   если больше нуля из названных, строка где-то разошлась.
4. **Двадцать экранов и тринадцать минут** — прикидка по образцу модуля 2, а не
   измерение.
5. **Не сверял этот урок с уроками модулей 4, 5 и 6**, которые пишутся сейчас
   тремя другими методистами: их замыслов я не читал. Пересечения, которые видны
   с моей стороны, вынесены в раздел 7 замысла модуля.
6. **Обычаев не проверял и не мог.** Пометок о том, чем чаще отвечают на «откуда
   ты» — городом или страной, — в `materials/` нет ни одной, и в уроке такой
   строки нет.
