# Разбор замысла модуля 15 (второй методист)

Дата: 11 сентября 2026. Файл заведён первым действием, пишется по ходу.

Разбор закончен: находки 1–16, список проверенного и приговор — ниже.

## Ход работы (пишется по мере проверки)

Замысел прочитан целиком (2127 строк). Дальше — проверки по источникам и по коду,
каждая с признаком.

Все перечисленные ниже проверки сделаны; чего сделать не удалось — отдельным разделом в конце.

---

## Находка 1. Раздел 5.4 разводит работу с УРОКАМИ, КОТОРЫХ В ЗАМЫСЛЕ НЕТ

Признак: сверка каждой строки таблицы 5.4 со списками заданий разделов 6 и 7
того же файла, построчно, все двенадцать строк.

Пять строк таблицы называют задание урока, которого в разделах 6–7 не существует:

| Строка 5.4 | Что там названо | Что в разделе 6 на самом деле |
|---|---|---|
| 3 | урок 3, `order` собрать `Do we work here?` | урок 3, З3 собирает `Do Dana and Kim study English?` |
| 4 | урок 4, `gap` `Does she ______ at a school?` → `work` | урок 4, З3 — `______ your mother work at a school?` → **`Does`** |
| 5 | урок 4, `short` «построить вопрос об Алиме» | урок 4, З4 переделывает `She studies English.`; З6 исправляет `Does he works…` |
| 7 | урок 6, `choice` «какой вопрос задать» | урок 6, З1 выбирает верную ЗАПИСЬ из трёх |
| 8 | урок 7, `short` «спросить, где человек живёт» | урок 7, З4 — «Ты хочешь узнать у Нурлана, помнит ли он твою фамилию. Запиши вопрос» |

Разделы 6–7 писались ПОСЛЕ раздела 5 (сказано в 5.1), и правки в них ушли, а
таблица 5.4 осталась описывать первую редакцию. Это ровно порода «правка ломает
соседа» и «свод о собственном документе, составленный по памяти, доказывает
только усердие составителя».

**Само по себе это не беда — беда в том, что разведение из-за этого не
состоялось.** Разбор в находках 2 и 3.

**Что сделать:** таблицу 5.4 пересобрать по разделам 6–7 заново, а не подправить
пять строк. Проверка: у каждой строки назвать `id` задания и его строку в
разделе 6.

## Находка 2. Работа, вопрос 4 — близнец задания урока 4 (З3) по всем признакам

Признак: сверка по ДЕЙСТВИЮ, рамке и ответу.

```
урок 4, З3 (раздел 6.4):  `______ your mother work at a school?`   → Does
работа,  в.4 (раздел 5.2): `______ your teacher live in Astana?`   → Does
```

Вид один (`gap`), пропуск на одном и том же месте (начало строки), ответ ДОСЛОВНО
один и тот же (`Does`), рамка одна: «пропуск + мой родственник/учитель + глагол +
место». Заменены имя человека и глагол — ровно то, о чём правило проекта говорит
«замена слова близнеца не снимает: снимает смена того, что ученик СДЕЛАЕТ».

Разведение в 5.4 (строка 4) опирается на несуществующее задание (находка 1) и
потому ложно: «в уроке вписывают ГЛАГОЛ, в работе — служебное слово» — в уроке
вписывают то же служебное слово.

**Что сделать:** сменить ВИД действия у вопроса 4 работы. Годится «исправить
чужую запись» — но оно занято модулем 14 трижды (сам замысел это и пишет в 5.3).
Предложение: вопрос 4 делает то, чего в уроках модуля нет, — по данному
КОРОТКОМУ ОТВЕТУ и данному хвосту строки восстановить начало
(`____ your teacher live in Astana? — Yes, she does.`), либо снять вопрос 4
вовсе: итог «ставить does…» уже спрошен вопросом 5, а замысел сам называет
двойной спрос роскошью («самое трудное место модуля»).

## Находка 3. Вопрос 8 работы и урок 7 З4 — одна рамка условия слово в слово

```
урок 7, З4:  «Ты хочешь узнать у Нурлана, помнит ли он твою фамилию. Запиши вопрос»
работа, в.8: «Ты хочешь узнать у Даны, знает ли она твою сестру. Запиши вопрос»
```

Вид один (`short`), рамка условия совпадает дословно до имени и придаточного,
действие одно: построить вопрос с `do` по русскому положению дел. Заменены имя и
глагол — это и есть порода 13 («работа проверяет память о заданиях»).

Разведение в 5.4 (строка 8) сравнивает вопрос 8 с заданием, которого в уроке 7
нет («спросить, где человек живёт»), и потому молчит о настоящем близнеце.

**Что сделать:** развести ВИДОМ. У урока 7 остаётся `short` по русскому
положению дел; вопросу 8 работы дать другое действие — например, `hottext` по
трём строкам «о чём спрашивают» или сборку из кусков (но `order` в работе уже
дважды, см. находку 5).

## Находка 4. Вопрос 11 работы — близнец заданий З1 и З3 урока 9, включая набор вариантов

```
урок 9, З3:  по записи 2 отметить прозвучавший ответ:
             `No, she doesn't.` · `No, she isn't.` · `Yes, she does.`
работа, в.11: отметить ответ, который прозвучал:
             `Yes, he does.` · `Yes, he is.` · `No, he doesn't.`
```

Одно действие (послушать разговор и отметить прозвучавший короткий ответ), один
вид (`choice`), и даже набор вариантов собран по одному образцу: верный ответ с
`does`, отвлекающий с формой be, отвлекающий обратной полярности. Сменены только
лицо (she → he) и порядок кнопок.

Разведение в 5.4 (строка 11) сравнивает вопрос 11 с заданием З2 урока 9
(«записать одним словом») и настоящего близнеца не видит — снова узкая граница
ручательства.

**Что сделать:** вопрос 11 работы сменить действие. Формат ответа на слух
позволяет: A2 Key, с. 29 — одно слово, число, дата или время. Годится «запиши
одним словом, где работает мама» — но это занято З4 урока 9. Предложение: в
записи звучат ДВА человека, и ученик отмечает, кто из них ответил «да», либо
записывает одним словом занятие того, о ком спросили.

## Находка 5. Требование владельца от 11 сентября (раздел 14а) В СОСТАВ УРОКОВ НЕ ВНЕСЕНО

Признак: чтение всех случаев разделов 6.1–6.7 подряд и поиск случая, который
показывает одно слово в трёх местах.

Раздел 14а называет требование дословно и говорит, что оно «ложится в раздел 6».
**В разделе 6 такого случая нет ни одного.** У урока 1 восемь случаев, у урока 2
восемь — и ни один не ставит рядом `I don't work.`, `Do you work?`, `No, I don't.`

Сборщик собирает скелет по таблицам раздела 6, а не по разделу 14а: требование
владельца до ученика не доедет.

**Что сделать.** Вписать случай в таблицу урока 2 девятым — после случая 3
(«Глагола в коротком ответе нет»): к этой минуте ученик видел все три места, а
раньше — нет. Строки те, что названы в 14а, столбец «Где стоит слово» —
«перед глаголом» · «перед you» · «одно, глагола нет». Итог урока 2 при этом не
меняется: случай объясняет устройство короткого ответа, а не новое умение.

Заодно поправить раздел 4.4: у урока 2 в лестнице пять заданий, и случай 9 даёт
опору, которой у заданий пока нет, — либо опереть на него З1, либо сказать, что
случай заданием не спрашивается (это законно, но должно быть названо).

## Находка 6. Урок 8 печатает `The lesson is at five.` — час, который модуль 13 СНЯЛ с этой ступени нарочно

Признак: `grep` по коду модуля 13 (`kazhdyy-den.ts`, строки 1659–1660 и 3040).

```
ЧТО СНЯТО РАНЬШЕ: строка `The shop opens at nine.` Она требует часа
(`at nine` — предлог времени, а он у модуля 18) и глагола `open`
```

Текст урока 8 несёт ровно эту конструкцию — `at five`. Раздел 7.1 ручается «ни
одного слова вне курса», и ручательство верно БУКВАЛЬНО: `five` карточка есть.
Но заимствование тут не словарное, а грамматическое — предлог времени с часом, —
и правило проекта на это прямое: «Пройди по каждой английской строке урока и
спроси: эта конструкция уже вводилась? Незаявленное заимствование хуже
заявленного».

**Что сделать:** строку с часом снять, как её снял модуль 13, а не объявлять
заимствованием. Жанр `notice` требует шести слов, и без неё их остаётся с
избытком. Если место в объявлении нужно занять, годится строка из разобранного —
например, о том, где клуб (`in the classroom` уже стоит).

## Находка 7. Слово `club`, на котором стоят урок 8 и вопрос 10 работы, курсом не дано и в карточки модуля не взято

Признак: `grep -rl 'term: "club"' courses/english-beginner-2/*.ts` — пусто;
`npm run slovo -- club` — **Oxford 3000: A1, словник A2 Key: да**, «уже дано:
english-elementary → davay-ya» (чужая ступень, а между ступенями нет ссылок на
память).

Слово стоит в заголовке текста (`THE SCHOOL CLUB`), в двух его строках, в
условии задания З1, в условии З4 и в вопросе 10 работы — то есть это опорное
слово урока чтения. Карточек в разделе 8.2 пятнадцать, и `club` среди них нет.

Ручательство раздела 7.1 («Все слова — карточки модулей 1–15») этим опровергнуто.

**Что сделать:** взять `club` шестнадцатой карточкой. Тему объявлять из-за него
не надо — по тому же доводу, каким взят `message` (раздел 8.1): карточка ставится
там, где слово работает, а тема объявляется та, чей список модуль закрывает.

## Находка 8. Свод урока 5 говорит «ВСЕГДА … do, does или форму be», а курс даёт четвёртое начало вопроса и четвёртое слово ответа

Признак: `grep` по коду модуля 11 (`odezhda.ts`) — `Have you got a hat?`,
`Have you got an umbrella?`, ответы `Yes, I have.` / `No, I haven't.`, и там же
прямая строка: «Спросили оборотом, значит и отвечать надо им же, а не формой be».

Случай 8 урока 5: «Слово ответа **всегда** берут из начала вопроса — do, does или
форму be». Случай 8 урока 6: «Спрашиваешь, кто человек, — начинай формой be;
спрашиваешь о действии — словом do или does».

Оба перечня закрытые, и оба опровергаются не источником, а **собственным курсом,
модулем 11**, который ученик прошёл четырьмя модулями раньше. Это ровно признак
выдуманного запрета: слово «всегда» при перечне, у которого нет графы,
запрещающей четвёртый член.

**Что сделать.** Слово «всегда» снять, а перечень оставить открытым: «Слово
ответа берут из начала вопроса: начался с do — отвечают do». Правило от этого не
слабеет, а становится верным. В уроке 6 то же: правило говорит о ДВУХ началах,
которым учит модуль, и не объявляет, что других не бывает.

**И это же место — законный адрес ссылки на наш модуль:** `Have you got a hat?` —
`Yes, I have.` стоит в модуле 11, написанном целиком, и одна строка о нём делает
правило полным. Решает методист; я называю беду и оба выхода.

## Находка 9. Объявление раздела 5.1 ложно по двум пунктам из трёх — и оба дают близнеца

Раздел 5.1 объявляет с датой: работа берёт три действия, «которых в уроках модуля
нет ни одного». Признак проверки: сверка каждого из трёх с таблицами заданий
разделов 6 и 7 того же файла.

| Объявлено «в уроках нет» | Что стоит в уроках |
|---|---|
| «собрать разговор целиком» (в. 12) | **урок 10, З2, `order`: собрать разговор из четырёх кусков `Do you` · `study English?` · `Yes,` · `I do.`** — то же действие, тот же вид |
| «кому подходит объявление» (в. 10) | **урок 8, З4, `choice`: «Нияз плавает, но не поёт и не танцует. Отметь, подходит ли ему клуб»** — то же соотнесение человека с прочитанным |
| «на какой вопрос отвечают вот этим ответом» (в. 6) | в уроках вправду нет — держится |

Замысел сам предупреждает, что такое объявление устаревает без единой правки
(случай модуля 6), — здесь оно оказалось ложным в день написания: разделы 6–7
писались после раздела 5.

Разведение 5.4 обе беды пропустило: строка 12 сравнивает работу с З4 урока 10
(`speak`, машиной не проверяется), а З2 того же урока не видит; строка 10
объясняет разницу «своим объявлением», хотя порода снимается не заменой слов, а
сменой ДЕЙСТВИЯ.

**Что сделать.** Вопрос 12 работы и урок 10 З2 делают одно дело — оставить надо
одно. Предложение: у урока 10 З2 сменить вид (собрать не разговор, а ОТВЕТ по
услышанному вопросу), а сборку разговора оставить работе. Вопрос 10 и урок 8 З4:
у урока оставить «подходит ли ему клуб», а работе дать действие, которого в
уроках нет, — например, выбрать, о чём НЕ спрашивает объявление.

## Находка 10. Граница ручательства 5.3 узка: работа модуля 11 держит обе рамки работы модуля 15

Раздел 5.3 называет границу прямо: «сверено с работами модулей 13 и 14 … и с
работами модулей 3 и 7». Модуль 11 в границу не вошёл, а в его работе
(`odezhda.ts`, вопросы 5 и 6) стоит:

```
q5, short:   «Ты хочешь узнать, есть ли у собеседника очки. Запиши свой вопрос
             целиком.»                      → Have you got glasses?
q6, choice:  «Тебя спросили: Have you got an umbrella? Зонта у тебя нет.
             Выбери короткий ответ.»        → No, I haven't.
             отвлекающие: No, I am not. · No, it is not.  (ответ формой be)
```

Работа модуля 15: вопрос 8 — та же рамка, что q5 (русское положение дел →
записать вопрос); вопрос 2 — та же рамка, что q6, включая и вид отвлекающего
(ответ формой be вместо служебного слова).

Это не довод снять вопросы: предмет у модуля 11 другой (`have got`). Это довод
**назвать границу шире и развести действием**, потому что ученик проходит работы
подряд и делает одно и то же в третий раз.

**Что сделать:** границу ручательства переписать как «работы модулей 3, 7, 11, 13
и 14» и пересверить по ней; рамку «Тебя спросили … Ответь коротко» в работе не
брать вовсе — она занята модулями 3, 9, 10 и 11 (счёт `grep "Тебя спросили"` по
курсу даёт одиннадцать мест).

## Находка 11. Раздел 9.2: второй довод отвода ложен — пункт модулю 7 НЕ роздан

Замысел пишет об `…x281198469131771500` (PRONOUNS, FORM: (OBJECT) 'IT'): «К тому
же пункт уже роздан модулю 7 „Что это такое", и двойная раздача — ошибка сборки».

Признак проверки: не `grep` по `id`, а чтение ПОЛЯ, где строка стоит.
`chto-eto-takoe.ts`, строка 362 — это поле `sources`, раздел «ЧЕГО МОДУЛЬ НЕ
БЕРЁТ, ХОТЯ ОНО РЯДОМ»:

> FORM: (OBJECT) 'IT' (1741163713867x281198469131771500) — у нас it всегда
> подлежащее, **место пункта в модуле 20**.

То есть модуль 7 пункт прямо ОТВЁЛ и назвал чужой адрес. В полях `egp` и
`egpPovtor` модуля 7 его нет (проверено чтением обоих списков, строки 275–284).

Раздел 13 записывает это ручательство признаком «`grep` по `id` во всех файлах
курса» — и это ровно проверка не тем признаком: `grep` находит строку, но не
отвечает, в каком она поле.

**Вывода это не меняет:** главный довод отвода (графа о дополнении `it`, а не о
вопросе; ступень из ПРИМЕРА не выводится) верен и подтверждён чтением графы в
CSV. Убрать надо только второй довод, иначе следующий проверяющий пойдёт искать
двойную раздачу, которой нет.

## Находка 12. Граница «досуг — это A2» опровергается собственным уроком 8 этого же модуля

Раздел 1.1 ставит `Do you play football at the weekend?` на A2 («what they do …
in their free time»), раздел 4.3 отдаёт «вопрос о досуге» модулям 17 и 19, а
раздел 0.2 на этом же доводе снимает тему `Hobbies and Leisure`.

А урок 8 — целиком о досуге: объявление клуба с тремя вопросами `Do you swim?`,
`Do you dance?`, `Do you sing?`, и задание З5 требует ПРОИЗВЕСТИ ещё один такой:
`Do you play football?`

Одно из двух неверно, и я называю, какое. **Неверна граница, а не урок.** Графа
источника — разрешение, а не запрет: из «A2: can ask about free time» не следует
«A1 нельзя», а графа A1 («about themselves and other people») вопрос о занятиях
человека покрывает. Довод раздела 0.2 при этом остаётся в силе там, где он и
работает: тема как ДЕЛО модуля — не то же самое, что отдельный глагол в строке.

**Что сделать:** в разделе 1.1 оставить признак, который автор и сам назвал
работающим («если к вопросу просится „всегда", „каждый день", „по выходным" — он
ушёл в A2»), а строку-запрет о досуге снять; в разделе 4.3 адрес модулям 17 и 19
оставить за ВОПРОСОМ О РАСПИСАНИИ досуга, а не за глаголами `swim`, `sing`,
`dance`, которые курс дал карточками ещё в модуле 14.

Иначе выйдет ровно то, чего замысел боится в другом месте: следующий проверяющий
откроет урок 8, прочтёт раздел 1.1 и снимет законное.

## Находка 13. Карточки `science` и `history` работают впервые в уроке 4, а довод седьмого урока говорит обратное

Раздел 4.1 держит седьмой урок правил в том числе доводом: «Урок 7 к тому же
единственное место, где работает словарь темы `Education`».

По собственной таблице 8.2 это неверно: `science` — «уроки 4 и 7», `history` —
«уроки 4, 5, 6, 7», `lesson` и `classroom` — урок 8. Признак: чтение столбца «Где
работает» у всех пятнадцати строк.

Само по себе это довод, а не беда, — но за ним стоит настоящее требование:
**слово вводится карточкой не позже, чем впервые понадобилось.** Значит `science`
и `history` встают в словарь УРОКА 4 (случаи 1 и 1 урока 5), а не урока 7, иначе
ученик прочтёт `She teaches science.` за три урока до карточки.

**Что сделать:** в разделе 8.2 назвать урок, где карточка СТОИТ, отдельным
столбцом от того, где слово работает; довод раздела 4.1 переписать («урок 7 —
место, где словарь темы работает целиком»), потому что в нынешнем виде он ложен.

## Находка 14. Урок 4, З6 проходится копированием врезки того же урока

```
случай 5 (врезка): «В строке `Does he works?` окончание осталось у глагола …
                    Верно: `Does he work?`»
З6, short:          исправить `Does he works at a school?`  → Does he work at a school?
```

Ученик берёт исправленную строку из врезки и дописывает хвост. То же лицо, тот
же глагол, та же ошибка. Проверка утечки в `check:content` тут промолчит: у неё
порог `DLINA_UTECHKI = 4`, а `Does he work?` короче.

К тому же З1 того же урока опирается на тот же случай 5 и спрашивает то же самое
выбором.

**Что сделать:** в З6 сменить и лицо, и глагол — например, `Does she studies
English?` Тогда ученик применяет правило, а не переписывает строку.

## Находка 15. Новое имя `Niyaz` — в курсе его нет ни разу, и чтения для синтеза у него нет

Признак: `grep -rho "Niyaz" courses/english-beginner-2/*.ts` — ноль (при `Dana`
355, `Kim` 289, `Alim` 221, `Nurlan` 145); `grep Niyaz lib/content/proiznoshenie.ts`
— пусто.

Нияз говорит в сцене урока 7, стоит в условии урока 8 (З4), в образце урока 10 и
в двух вопросах работы (7 и 10). Каждая строка `proiznoshenie.ts` — решение
владельца как преподавателя, и без неё синтез прочтёт имя как придётся; проверка
звука этого не видит, потому что она смотрит, есть ли файл.

**Что сделать:** либо взять уже работающее имя (`Nurlan`, `Alim`), либо внести
чтение `Niyaz` пятым вопросом владельцу в раздел 12. Вопрос сейчас не задан
вовсе, а сборщику в 11.2 велено прогнать только `golosa` — голос он выберет, а
чтение не возьмётся ниоткуда.

## Находка 16. Дыра КУРСА, которую модуль 15 углубляет: `the` не введён нигде

Признак: `npm run --silent tekst` по каждому из четырнадцати написанных модулей,
счёт `\bthe\b` в видимом тексте — **455 вхождений** (модуль 12 — 237, модуль 13 —
96, модуль 3 — 36, модуль 14 — 35, модуль 9 — 28, модуль 8 — 15, модуль 5 — 6,
модули 7 и 11 — по 1). Карточки `the` нет ни в одном модуле; объяснения нет ни
одного — при этом разборы заданий модуля 12 уже говорят ученику «слово the» и
«артикль the» как о знакомом. В программе ступени определённого артикля нет ни в
одной строке грамматики.

Модуль 15 добавляет свои: `Do you go to the library?` (работа, в. 2), `Our club
is in the classroom.` и `The lesson is at five.` (урок 8).

**Это не вина модуля 15 и чинится не в нём.** Но назвать дыру обязан тот, кто её
увидел: ровно так `the` простоял в прежнем курсе 418 раз, и владелец спросил сам.

**Что сделать:** записать находку в раздел 11.1 адресом «программа ступени» —
там решается, какой модуль вводит артикль. По счёту первое место, где он нужен, —
модуль 3 (36 вхождений, `Astana is the capital`), но модули 1–14 уже у ученика,
значит решение о месте принимает методист программы, а не модуль 15.

## Мелкое, но проверяемое

1. **Раздел 12 не помечен закрытым.** Владелец ответил на все четыре вопроса
   11 сентября (`otvety-vladeltsa-11-sentyabrya.md`, разделы 3–6). Пока пометки
   нет, следующий круг понесёт их заново — порода «находка не доезжает сама».
2. **Разделы 8.3 и 12 называют РАЗНЫЕ пары расхождений с `ipa-en-uk`** (8.3 —
   `exam` и `history`, вопрос 2 — `exam` и `geography`). На деле их три; это уже
   записано владельцем в ответах 11 сентября. Свести оба раздела к трём словам.
3. **Раздел 8.1 отводит тему `Work and Jobs` неверным доводом.** «Модуль,
   взявший одно из шести слов, сделает отчёт хуже» — проверка устроена иначе:
   `scripts/programma.mts`, строки 306–326, спрашивает полноту у КУРСА, а не у
   модуля, порог — две трети, и у `Work and Jobs` уже 21 из 27 (78 %), то есть
   тема давно в СВЕДЕНИЯХ. Решение не объявлять тему верное, довод — нет.
4. **`surname` и `last name` даны не «обе в модуле 5»** (раздел 6.7, З4):
   `surname` — карточка модуля 1 (`alfavit.ts`), `last name` — модуля 5
   (`blank.ts`). Сам `accept` от этого не меняется, ссылка — да.
5. **Передача модуля 11 не записана обратно.** `plan-modul-11.md`, строки 1132 и
   1596, и шапка `odezhda.ts`, строки 167–168, отдают модулю 15 оборот
   `Do you have a hat?`. Модуль 15 его отклоняет (раздел 4.3) — решение
   разумное, но в разделе 11.1 его нет, и адрес у соседа останется ложным.
   Обещания ученику там нет (строка только в шапке файла), значит правка не
   срочная, но записать её надо.
6. **Урок 6, З1 проходится счётом, не языком.** Варианты: `Are you a teacher?`
   (верный) · `Do you a teacher?` · `Do you teacher?` — два неверных начинаются
   одинаково, верный единственный не такой. Ученик отмечает «непохожий», не читая.
   Лечится заменой одного отвлекающего на строку с другим началом (например,
   `You are a teacher?`).
7. **Работа, в. 7: `Are you a singer?` может оказаться верным ответом.** Условие
   говорит «Нияз — учитель, и он поёт». Человек, который поёт, на вопрос
   «ты певец?» отвечает и «да». Разводит их только то, что урок 6 учит
   спрашивать формой be о ЗАНЯТИИ, — а условие называет и занятие, и действие.
   Надёжнее взять отвлекающим профессию, которой у Нияза нет вовсе
   (`Are you a driver?`).

## Добавление к опорам: на с. 52 Cambridge стоят две строки сильнее той, что взята

Признак: чтение страницы 52 руководства Young Learners целиком
(`materials/.text/cambridge-young-learners-pre-a1-a2.txt`, строки 1345–1390), а не
одной строки списка.

Замысел подпирает третье лицо строкой `When does school start?` — а она стоит под
подзаголовком **Question words**, то есть это вопрос со словом вопроса, который
сам замысел объявляет A2 и отдаёт модулю 18. Опора работает, но выбрана неудачно.

На той же странице, в списке «new at A1 Movers … in addition to the list at Pre A1
Starters level», стоят две строки прямее:

```
Have (got) to/had to      Do I have to go to bed now?
Past simple ...           Did you go to the cinema? Yes, I did.
```

Первая — вопрос ДА-НЕТ со служебным `do` (и с первым лицом); вторая — вопрос со
служебным и краткий ответ со служебным словом, а именно краткого ответа с `do`
замыслу и не хватало: раздел 1.2 подпирает его только заголовком с. 29 и примером
с формой be (`Is that your sister? Yes, it is.`).

Что сделать: в шапке модуля и в `sources` назвать все три строки с. 52, а не
одну. Опора от этого становится прямой, а не выведенной из заголовка.

---

## ЧТО ПРОВЕРЕНО И НАЙДЕНО ЧИСТЫМ — с признаком и границей

| Что проверял | Признак (можно перепрогнать) | Граница | Итог |
|---|---|---|---|
| дескриптор модуля | чтение шкалы Information exchange целиком, `materials/.text/cefr-companion-2020.txt`, строки 3830–3905 | обе графы, A1 и A2, дословно | сходится: A1 «Can ask and answer questions about themselves and other people, where they live, people they know, things they have»; A2 «habits and routines» и «what they do at work and in their free time» |
| строка 595 программы ступени | чтение `docs/beginner-2/programma-stupeni.md`, строки 590–600 | одна строка, все пять столбцов | сходится: три столбца из пяти неверны, как и сказано в 0.2 |
| «Starters с. 29 — грамматический список» | чтение с. 29 руководства Young Learners, строки 1249–1300 | вся страница | сходится: заголовок страницы «Grammar and Structures List» |
| формат ответа на слух | цитата A2 Key с. 29 (метки страниц: строка 1790 «с. 29», строка 1881 «с. 30»; цитата на 1853) | раздел THE TASK | сходится дословно |
| есть ли на A1 графа о вопросе с `do` и о кратком ответе | свой разбор CSV: перебор всех записей A1 по образцу interrogat/question/auxiliar/short answer/ellips в четырёх полях. Признак ШИРЕ авторского — он искал слово question в Can-do | все 1222 записи, девять совпавших A1 | сходится: графы о форме вопроса с `do` на A1 нет, краткого ответа нет, `QUESTIONS/yes/no FORM: MODAL VERBS` — только модальные |
| ступень и текст шести пунктов egp и egpPovtor | выборка по id из CSV с чтением Level, Can-do и всех примеров | шесть id поимённо | сходится: четыре пункта возврата A1, отведённый …x661087028434845600 — A2 |
| поправка 0.3: у графы …x609205291409504700 три примера | чтение поля Example целиком | одна графа | сходится: «Can I help you?», «What do you think about that?», «Can we have a chat?» |
| поправка 9.3: на A1 есть своя графа о be-вопросах | перебор CSV по всему файлу | все записи A1 | сходится и шире: кроме PRONOUNS …x150062658595893500 есть CLAUSES / interrogatives, FORM: AFFIRMATIVE INTERROGATIVE, WITH BE, A1 — именно он покрывает `Is this a key?`, где нет ни you, ни it. Оба розданы: модуль 3 в egp, модули 7 и 9 в egpPovtor — проверено чтением полей, а не grep |
| «третье лицо графой не покрыто» | сравнение графы с соседями: все записи семьи PRONOUNS subject-object со словом QUESTION в Guideword | все ступени | сходится с поправкой: таких граф всего две, обе A1, разведены по типу служебного слова (be против auxiliary/modal), а не по лицу. Источник о лице в вопросе молчит везде, а не кладёт третье лицо выше |
| раздача пунктов | grep по id в `courses/` и в `docs/beginner-2/*.md` | весь курс и все замыслы | …x609205291409504700 свободен; …x281198469131771500 не роздан — находка 11 |
| темы и норма словаря | прогон `npm run programma -- english-beginner-2` | весь курс | сходится: Education 11 из 31, Work and Jobs 21 из 27; двадцать слов темы без карточки, из них двенадцать берёт модуль 15, восемь остаются — список замысла совпал со списком прогона слово в слово |
| ступень и словник пятнадцати карточек | прогон `npm run slovo` по каждому слову | пятнадцать слов | сходится: все тринадцать знаменательных — Oxford A1 и в словнике A2 Key |
| чтения карточек против `ipa-en-uk` | сверка каждой строки с выводом slovo по условностям курса | пятнадцать строк | сходится: /ˈlesn/, /ˈlaɪbrəri/, /ˈdɪkʃənəri/, /rɪˈmembə/, /dʒɪˈɒgrəfi/ выведены верно |
| слово `together` | grep по term во всех файлах курса | весь курс | сходится: карточки нет, правка урока 3 верна |
| словарь прочих строк уроков | grep по term для 34 слов, которые несут строки разделов 6 и 7 | весь курс, кроме неподключённого `kto-eto.ts` | чисто у всех, кроме `club` (находка 7) |
| нет ли предмета модуля уже в курсе | grep по outcome и title во всех файлах курса | весь курс | чисто: вопрос со служебным do не занят никем; короткий ответ с формой be — модули 3 и 7, с have got — модуль 11 (отсюда находка 8) |
| термин «вспомогательный глагол» | grep по всем файлам курса и счёт по ВИДИМОМУ тексту всех четырнадцати модулей прогоном `npm run tekst` | весь курс | сходится: три вхождения, все в шапках, в видимом тексте ноль |
| имена людей | grep по коду и чтение `lib/content/proiznoshenie.ts` | весь курс | Dana, Kim, Alim, Nurlan на месте; Niyaz — находка 15 |
| вопросы владельца раздела 12 | чтение `otvety-vladeltsa-11-sentyabrya.md` | все четыре | закрыты, переоткрывать не стал |

## ЧЕГО ПРОВЕРИТЬ НЕ УДАЛОСЬ

1. Толкований английских слов не проверял: Викисловарь и Википедия с этой машины
   не открываются — то же ограничение назвал автор замысла. Значит вторая
   проверка отвлекающего, «говорят ли так вообще», у меня сделана разбором
   строки, а не выпиской. Это прямо касается замечания про `Are you a singer?`:
   говорю его как соображение, а не как опору.
2. Сплошного чтения уроков модулей 3, 7, 9, 11, 13 и 14 не делал. Сверял grep-ом
   по итогам, заголовкам, вопросам работ и отдельным строкам. Совпадение
   отдельной строки примера я мог пропустить; его закроет `npm run bliznetsy`
   после подключения модуля.
3. `npm run kontrol`, `check:content` и `bliznetsy` по модулю 15 не гонял —
   модуля в коде нет, а по неподключённому молчат все проверки без исключения.
4. Число экранов и время уроков не считал: мерки объявлены ориентирами.
5. Поле сетки (урок 7, З5) не проверял — `bedySetki` и `lishnieSlova` требуют
   собранного задания. Слова названы разумно, но короткие слова внутри длинных я
   искал глазами, а не прогоном.
6. Замыслов модулей 16–18 по-прежнему не существует (проверено `ls
   docs/beginner-2/`). Передачи `homework`, `How often…?` и урока письма записаны
   только у модуля 15 и до тех пор ничьи.

## ПРИГОВОР

Доработать. К сборке замысел не годен — и не из-за ступени и не из-за источников:
разбор источников в нём сделан честнее обычного, три самопоправки (8.1, 9.2, 9.3)
устояли по существу, а две из них подтверждены моим собственным признаком.

Чинить до сборки — шесть мест:

1. находка 5 — требование владельца от 11 сентября не внесено в состав уроков:
   сборщик соберёт модуль без него;
2. находки 2, 3, 4, 9, 10 — работа модуля списывает у уроков и у работ соседей:
   пять вопросов из двенадцати повторяют действие. Таблицу 5.4 пересобрать по
   разделам 6 и 7, границу 5.3 расширить до модуля 11;
3. находка 6 — снять час (`at five`) из текста урока 8: модуль 13 снял ту же
   конструкцию нарочно;
4. находка 7 — взять карточку `club`;
5. находка 8 — снять «всегда» из свода урока 5 и открыть перечень урока 6:
   модуль 11 даёт четвёртое начало вопроса и четвёртое слово ответа;
6. находка 12 — снять границу-запрет о досуге, оставив признак по времени: иначе
   модуль противоречит собственному уроку 8.

Находки 11, 13, 14, 15 и мелкое правятся одной вычиткой и сборку не держат.
Находка 16 (`the`) чинится не здесь: это долг программы ступени, и его надо
записать в раздел 11.1, чтобы он доехал.
