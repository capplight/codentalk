# Модуль 4, уроки 2–9. Подробный замысел

6 сентября 2026. Пишет методист. Весь видимый русский текст — вступления,
объяснения, врезки, подписи, условия и разборы заданий — пишет **редактор**.

Замысел модуля целиком — [plan-modul-4.md](plan-modul-4.md); урок 1 —
[plan-modul-4-urok-1.md](plan-modul-4-urok-1.md), он прошёл круг и собран в код.
Здесь расписаны остальные восемь.

**ВТОРАЯ РЕДАКЦИЯ, по разбору второго методиста**
([razbor-planov-modul-4-uroki.md](razbor-planov-modul-4-uroki.md): 16 ошибок,
6 вопросов, 7 сведений). Что сделано по каждой строке разбора и где я с ним не
согласился — раздел 16. Три места из его списка меняли не строку, а работу, и
они внесены первыми: свод раздела 13 теперь собирается **скриптом**, банк
вопросов работы сведён в **один раздел** (12) и покрывает все девять итогов, а
три вида упражнений, о которых просил владелец 5 сентября, поставлены на места
(раздел 14).

---

## 0. Правило, ради которого этот файл устроен так, а не как серия

**Находка разбора модуля 2, и она стоила дорого:** там семь замыслов писали
серией, и **пять уроков подряд пошли одним строем заданий**, а четыре пары
заданий вышли близнецами — у двух условие совпало дословно. Круг после первого
урока такого не ловит: одинаковость видна только на серии.

**Отсюда четыре правила этого файла.**

1. **Строй заданий разводится по делу урока**, а не берётся от урока 1. Таблица
   строёв — раздел 1.
2. **Ни одно задание не повторяет вид и предмет другого сразу.**
3. **Свод собирается разбором документа, а не глазами.** Первая редакция
   считала долю «произвести ответ» руками и соврала в четырёх строках из девяти:
   в счёт шли `choice`, `match` и `hottext`, которых мерка кода не считает
   (`lib/content/types.ts:570`, `PRODUCTIVE_KINDS = ["gap", "short", "order",
   "code", "essay", "speak"]`). Правило проекта: **свод, составленный вручную,
   доказывает только усердие составителя.** Скрипт и его вывод — раздел 13.
4. **Правило одного урока проверяется соседними уроками и соседними модулями.**
   Порода найдена 6 сентября и в старых списках её нет: правило, верное в своём
   уроке, отменяется тем, что курс скажет позже. Прогон девяти уроков одного
   против другого — раздел 13.4; одна находка этой породы внесена (случай 2
   урока 2, см. ниже).

**И пятое, из того же разбора модуля 2:** где предмет урока живёт на слух —
задание на слух; где в письме — письмо. Урок 6 диктует номер, значит его задания
слушают и записывают; урок 5 говорит о себе, значит его задания производят
строку.

---

## 1. Строй заданий: составлено заранее, пересобрано по разбору

| Урок | Дело урока | Строй заданий по порядку | Чего в строе НЕТ нарочно |
|---|---|---|---|
| 1 | числа 1–10 | `choice`(звук) · `match` · `short`(звук) · `gap` · `short` · `speak` | сборки, исправления чужой записи |
| 2 | `-s` и `How many` | `hottext` · `gap` · `short`(исправить чужую запись) · `choice`(звук) · `short`(ответ числом) · `speak` | звука в первых заданиях: предмет урока письменный |
| 3 | 11–20, хвост `-teen` | `short`(исправить) · `order`(буквы вразброс) · `gap` · `choice`(звук) · `speak` | `match`: он был в уроке 1 на тех же числах |
| 4 | десятки, пара `-teen`/`-ty` | `choice`(звук) · `short`(звук, цифрой) · `gap`(дефис) · `setka` · `speak` | сборки слова: она была в уроке 3 |
| 5 | возраст | `short`(исправить) · `gap` · `match` · `short`(о себе) · `speak` | звука вовсе: возраст ученик говорит, а не слышит |
| 6 | номер по цифрам | `short`(звук) · `choice`(звук) · `gap` · `short`(картинка) · `speak` | `hottext`: прежнее задание проходилось поиском запятой, разбор 19 |
| 7 | чтение | `short`(`about`) · `choice`(`about`) · `short`(`about`) | звука: это урок чтения |
| 8 | слушание | `short`(звук) ×3 · `choice`(звук) | `speak`: он в уроке 9 |
| 9 | говорение | `gap` · `speak` ×2 | письменных заданий сверх одного |

**Как читать таблицу.** Совпадение ВИДА между уроками законно и неизбежно: видов
восемь, уроков девять. Запрещено совпадение **вида и предмета сразу** — и
столбец «чего нет нарочно» назван для того, чтобы при написании соблазн вернуть
привычное был виден.

**Пара уроков 5 и 6 пересобрана — это ошибка 7 разбора.** Было: у обоих
`short · gap · ? · short · speak`, то есть четыре места из пяти совпадали видом
и порядком, при том что свод первой редакции утверждал обратное. Стало:
урок 6 разводится вторым и третьим местом (`choice` со звуком вместо `gap`,
`gap` вместо `match`), и — что важнее вида — **весь звук модуля собран в уроке 6
и уроках 8–9, а в уроке 5 его нет ни одного.** Остаток сходства (первое,
четвёртое и пятое места) назван честно: `short` в начале и `speak` в конце стоят
у пяти уроков из шести, и разводить их дальше значило бы ломать дело урока ради
таблицы.

**Долю «произвести ответ» держим больше половины в каждом уроке.** Счётчик
`check:content` смотрит только банк работы, поэтому внутри уроков её держит
методист; считает её скрипт раздела 13, а не рука.

---

## 2. Урок 2 «Два предмета: буква -s»

```
slug: "dva-predmeta"
title: "Два предмета: буква -s"
vid: "pravila"
estimatedMinutes: 14
outcome: "прибавлять -s к предмету, когда их больше одного"
vozvrat: задание модуля 1 на запись слова со слуха (alfavit-2 / h2-zapisat-slovo, слово night)
```

**Итог сужен — это ошибка 1 разбора.** Было: «прибавлять `-s` … **и спрашивать о
количестве через `How many`**». Спросить ученик не может нигде: условие 3
сводящего стоит в шапке модуля прямо в коде
(`courses/english-beginner-2/chisla-i-telefon.ts`, строки 57–61) — «ни одно
задание модуля не требует построить `there are` самому: ученик отвечает
ЧИСЛОМ». Половина обещания не производилась ни одним заданием.

**Почему связка снята целиком, а не заменена на «отвечать числом».** Разбор
предлагал и такой выход, и он выглядит мягче. Но итог обязан спрашиваться
работой, а единственное, что ученик ДЕЛАЕТ с `How many`, — пишет число; вопрос
работы об этом был бы близнецом задания D5 по действию, а правило от 2 сентября
требует смены самого действия, а не слова. Случай `How many` остаётся в уроке
(случаи 7 и 8) и работает заданием D5 — итог просто не обещает того, чего не
проверяет.

**Это главный урок модуля: в нём стоит его грамматика.** `NOUNS / plural`,
FORM: PLURAL `-S`, `1741163711821x759916598484829700`, A1 — «Can form plurals by
adding -s to common countable nouns», второй пример графы «You have to walk just
**5 minutes** down the road». И `DETERMINERS / quantity`, FORM: WITH PLURAL
NOUNS, `1741163709012x117638123076284200`, A1, пример «My house has got **three
rooms**».

**`vozvrat` стоит здесь, и это единственный возврат модуля.** Задание
существует: `courses/english-beginner-2/alfavit.ts:2531`, `h2-zapisat-slovo`,
`zvuk: "night"`, ответ `night`; модуль в поле `iz` зовётся `alfavit-2`.

**Описание возврата исправлено — это вопрос 18 разбора.** Первая редакция
обещала, что ученик «снова записывает слово со слуха, а потом ставит к нему
число: `two nights`». Такой связки не бывает: `vozvrat` разворачивается как показ
ЧУЖОГО задания (`VozvratRef`, `lib/content/types.ts:644–653`) и ничего к нему не
добавляет. Связь сделана иначе и по-настоящему: **строка `two nights` стоит
вторым примером случая 3**, и ученик встречает то же слово в счёте на том же
уроке. Форма проверена по источнику: `nights /nˈaɪts/`, `materials/ipa-en-uk.txt`,
строка 37823.

### 2.1. Вступление

Русской фразой. Требования: по-русски «книга» и «книги» — слово меняется на
конце; по-английски меняется тоже, и почти у всех слов одинаково. Что ученик
научится делать: ставить `-s`, когда предметов больше одного.

**Чего во вступлении быть не должно:** определения слов «множественное число»
(ученик знает их по русскому языку — решение владельца от 4 сентября); обещания
`-es` и особых форм — их в уроке нет.

### 2.2. Сцена (экран 2)

`id: "scena-razdayut-knigi"`, `example`, `razgovor: true`, два голоса. Случай:
перед занятием раздают книги.

```
— One book, two books, three books.   — Одна книга, две книги, три книги.
— Sue?                                — Сью?
— Yes.                                — Да.
— Four books. Good.                   — Четыре книги. Хорошо.
```

Голоса: ж — м — ж — м.

**Сцена собиралась трижды, и ловушка тут общая.** Первые две редакции держали
`Four books, three students` и `Sue is here` — слов `student` и `here` в курсе на
тот час не было. Итоговая сцена стоит на `book`, `yes`, `good` (все три —
карточки модуля 1), на имени `Sue` (`privetstvie.ts:1297`) и на числах урока 1.

**И приписка второй редакции, которая важнее самой правки: вывод про `here`
устарел за один день.** Сегодня `here` — карточка модуля 3
(`courses/english-beginner-2/otkuda-ty.ts:1134`, урок «Где ты сейчас»), то есть
слово стоит РАНЬШЕ модуля 4 и запрещённым больше не является. Сцену это не
меняет — она уже написана без него, — но **правило видно целиком: словарь курса
меняется под руками, и всякий вывод «такого слова в курсе нет» верен на дату, а
не вообще.** Перед сборкой словарь сверяется заново по коду.

`explain` под сценой: книги считают по одной, и у слова `book` в счёте
появляется `-s`.

### 2.3. Восемь случаев

**Случай 1. Один предмет — слово как в словаре.** По-русски «одна книга»,
по-английски `one book`: слово не меняется.
`one book` — одна книга · `one key` — один ключ. `zvuchat` обе.

**Случай 2. Два и больше — в конце `-s`.** От двух и дальше к слову прибавляется
`-s`.
`two books` — две книги · `ten keys` — десять ключей. `zvuchat` обе.

**ТРЕБОВАНИЕ К РЕДАКТОРУ, и оно не стилистическое.** В правиле не должно стоять
ни «всегда», ни «у всех слов», ни «больше в слове не меняется ничего». Первая
редакция писала последнее — и это выдуманный запрет, который **опровергается не
источником, а нашим же курсом**: модуль 8 «Один и много» даёт `boxes`, `buses`,
`children`, `mice`, и его шапка объявляет это своим предметом
(`courses/english-beginner-2/odin-i-mnogo.ts`, пункт 3 шапки). Правило говорит о
том, что делаем мы и с нашими словами: к этим словам прибавляется `-s`.

**Случай 3. Число впереди, предмет за ним.** Расхождения с русским тут нет, и
случай короткий: он нужен, чтобы ученик не начал писать `books two`.
`three tickets` — три билета · `two nights` — две ночи.

**Случай 4. Само число не меняется.** По-русски «два ключа», но «двух ключей» —
слово «два» меняется; по-английски `two` одно на всё.
`two keys` — два ключа · `two books` — две книги.
**Расхождение с русским полное**, и это тот вид случая, который метод считает
главным. Прямое продолжение случая 7 урока 1 (`one` не меняется по роду); ссылка
на свой модуль внутри ступени законна и приветствуется.

**Случай 5. `-s` прибавляется и к единицам измерения.**
`five minutes` — пять минут · `two hours` — два часа.
Второй пример графы источника — «You have to walk just **5 minutes** down the
road» — ровно этот случай.

**Случай 6. Окончание слышно на конце слова.** Правило говорит О звучании, а не
повторяет строку примера: когда предметов больше одного, в конце слова слышен
лишний звук.
Таблица `naTomZheEkrane`, столбцы «Один · Много · Как звучит»:
`one key` · `two keys` · `/kiː/ — /kiːz/`; `one book` · `two books` ·
`/bʊk/ — /bʊks/`. `zvuk` у обеих строк.
Примеры: `Two keys, please.` — Два ключа, пожалуйста. · `Ten books.` — Десять
книг.

**Случай переписан целиком — это ошибка 12 разбора.** Было: «пишется `-s`
одинаково, а слышится по-разному», и заданием к нему ученик выбирал между
`books` и `keys`. Задание своего случая не проверяло: `/bʊks/` и `/kiːz/`
различаются каждым звуком, и решается оно по первому согласному. Хуже того, сам
случай давал наблюдение без правила — применить ученику было нечего.

**Что осталось от наблюдения: врезка без задания.** «В `books` конец звучит
`/s/`, в `keys` — `/z/`. Буква одна, а звук выходит разный сам собой —
запоминать тут нечего.» Ни «всегда после глухого», ни «только»: опора у
наблюдения есть, но она сама говорит «normally», а не «always» — Википедия,
статья English plurals, раздел Regular plurals: «When the singular form ends in a
voiceless consonant other than a sibilant, the plural is **normally** formed by
adding [s]»; «For a singular noun ending on a non-sibilant voiced consonant, the
plural adds [z]». Читал через программный интерфейс Википедии 6 сентября, своими
глазами.

**Выписку надо перенести в
[docs/istochniki-norma-yazyka.md](../istochniki-norma-yazyka.md)** — там её место,
и оттуда её сверяет `npm run kontrol`. Сам туда не писал нарочно: 6 сентября в
файле работает другой проверяющий (`git status` показывает его правку), а правило
проекта запрещает открывать на запись файл, в котором кто-то работает. Место для
неё — рядом с разделом 26 «Лишний слог даёт звук перед окончанием»: там уже стоит
выписка из ТОЙ ЖЕ статьи, и оба места об одном — о звуке перед окончанием и после
него. Наши десять записей остаются главной опорой.

**Случай 7. `How many` спрашивает о количестве.** Вопрос начинается с `How
many`, дальше стоит предмет с `-s`.
`How many books are there?` — Сколько книг? · `How many keys are there?` —
Сколько ключей? `zvuchat` обе целиком.
**Расхождение источников объявлено в шапке модуля** (раздел 0.6 замысла модуля):
English Grammar Profile ставит вопросительное `many` на B1, Cambridge — на
Pre A1 Starters (Young Learners, с. 23, с. 27, с. 9). Взято по Cambridge,
решение владельца от 6 сентября.
**`there are` взято ГОТОВЫМ**, третья строка шапки, адрес — модуль 12. В правиле
про него ровно одна строка: эти два слова стоят в вопросе, а разбирает их модуль
«Мой дом и комната». **Разбирать здесь нельзя** — это чужой урок.

**Случай 8. На вопрос отвечают числом.** В ответе довольно одного числа.
`How many books are there? — Four.` — Сколько книг? — Четыре.
`How many keys are there? — Two.` — Сколько ключей? — Два.
Опора — Young Learners, **с. 23**, Pre A1 Starters, Reading and Writing Part 5:
«How many children are there? … **two**»; и A2 Key, **с. 29**: «only ONE word, or
a number, or a date, or a time».

**Восемь случаев, шестнадцать примеров, звук назван у каждого.**

### 2.4. Словарь урока: шесть карточек

| Слово | Перевод | Пример | Чтение | Значок |
|---|---|---|---|---|
| `minute` | минута | `five minutes` | `/ˈmɪnɪt/` | нет |
| `hour` | час | `two hours` | `/ˈaʊə/` | нет |
| `day` | день | `seven days` | `/deɪ/` | нет |
| `week` | неделя | `two weeks` | `/wiːk/` | нет |
| `second` | секунда | `ten seconds` | `/ˈsekənd/` | нет |
| `moment` | момент | `One moment, please.` — Минутку, пожалуйста. | `/ˈməʊmənt/` | нет |

Все шесть — Oxford **A1**, все из темы `Measurements`. **`second` даёт только
«секунда»:** порядковых в модуле нет, и «второй» увело бы в модуль 18.
Карточка `key` стоит в уроке 1 — здесь она работает, но не вводится заново.

**Перевод `moment` расширен обратно — это вопрос 20 разбора.** Стояло «минутка»,
а «минутка» переводит оборот `one moment`, а не слово; толкований `materials/` не
держит, значит опоры у сужения нет. Правило проекта: расширение перевода законно,
сужение требует опоры. Карточка даёт прямое соответствие «момент», а **граница
называется там, где ученик на неё наткнётся** — переводом самой строки
`One moment, please.` в «Минутку, пожалуйста».

**Значков у этих шести карточек нет, и это решение, а не пропуск.** Владелец
просил упражнение «отгадать вещь на картинке» именно для новых слов, но
`minute`, `hour`, `second` и `week` картинкой не различаются: у Twemoji на все
четыре идут часы и календарь, и значок сказал бы неправду. Единственное новое
слово модуля, которое картинка называет точно, — `phone`; там упражнение и стоит
(урок 6, задание H4, раздел 14).

### 2.5. Шесть заданий

| № | id | Вид | Что делает ученик | Случай |
|---|---|---|---|---|
| D1 | `d1-otmetit-mnogo` | `hottext` | в ряду `book · books · key · keys` отмечает те, где предметов больше одного | 2 |
| D2 | `d2-dopisat-s` | `gap` | `two book_` → дописывает `s` | 2 |
| D3 | `d3-ispravit-zapis` | `short` | «Ученик записал: `four card`. Запиши строку без ошибки.» → `four cards` | 2, 3 |
| D4 | `d4-uslyshat-konec` | `choice` + `zvuk: "keys"` | слышит слово и выбирает: `key` или `keys` | 6 |
| D5 | `d5-otvetit-chislom` | `short` | «`How many tickets are there?` Билетов семь. Ответь одним словом.» → `Seven`, `accept: ["7"]` | 7, 8 |
| D6 | `d6-skazat-vsluh` | `speak` | произносит `two books` и `five minutes` | 2, 5 |

**D3 и D5 переписаны — это ошибка 14 разбора.** Ответы обоих стояли примерами
выше дословно: `three tickets` — первый пример случая 3, `How many books are
there? — Four.` — первый пример случая 8. Менялись задания, а не примеры: пример
нужен случаю. Теперь ответ D3 — `four cards` (эта пара в модуле не печатается
нигде), а D5 спрашивает про билеты и отвечает `Seven`.

**D4 переписан вместе со случаем 6** (ошибка 12): пара `key` — `keys`
различается только окончанием, и решить её можно, лишь услышав конец слова.
Отвлекающий `key` — не выдумка: пропуск окончания на слух и есть та ошибка, ради
которой случай стоит.

**D5 требует ОТВЕТА, а не построения вопроса** — третье условие сводящего.
Условие прямо говорит, сколько билетов, чтобы верный ответ был один: без этого у
задания их бесконечно много. Цифра засчитывается наравне со словом — правило
модуля из раздела 0.1 замысла (Young Learners, с. 16).

**Чего в заданиях быть не должно:** слова `box` во множественном (`boxes` — не
наш случай); сборки вопроса из слов; `match` — он был в уроке 1 и на числах.

---

## 3. Урок 3 «От одиннадцати до двадцати: хвост -teen»

```
slug: "ot-11-do-20"
title: "От одиннадцати до двадцати: хвост -teen"
vid: "pravila"
estimatedMinutes: 13
outcome: "называть и записывать числа от одиннадцати до двадцати"
```

**Ступень:** Young Learners, **с. 81**, строка `Numbers`, колонка **Pre A1
Starters** — `1–20`.

### 3.1. Вступление и сцена

Вступление русской фразой: по-русски после десяти счёт идёт «одиннадцать,
двенадцать, тринадцать» — и там слышен один и тот же хвост «-надцать»;
по-английски хвост свой, `-teen`, и он делает семь чисел из семи уже знакомых.

**Сцена — очередь к окошку**, называют номера.

```
— Eleven.         — Одиннадцать.
— Twelve.         — Двенадцать.
— Thirteen.       — Тринадцать.
— Thirteen? Yes.  — Тринадцать? Да.
```

Голоса ж — м — ж — м. Ни одного нового слова, кроме самих чисел.

### 3.2. Восемь случаев

1. **`eleven` и `twelve` не похожи ни на что** — их учат как есть.
   `Eleven.` — Одиннадцать. · `Twelve.` — Двенадцать. Таблица 11–12 со звуком,
   `naTomZheEkrane`.
2. **С тринадцати работает хвост `-teen`.**
   `Sixteen.` — Шестнадцать. · `Seventeen.` — Семнадцать. Таблица 13–19 на том
   же экране, столбцы «Цифрой · Словом · Как звучит · Перевод», `zvuchat` у
   каждой строки.
3. **Две особые части: `thir-` и `fif-`.** В `thirteen` и `fifteen` начало
   другое, чем в `three` и `five`.
   `Thirteen.` — Тринадцать. · `Fifteen.` — Пятнадцать.
   **Особых частей ДВЕ, а не три** — правка первого круга: `eighteen` устроено
   иначе, см. случай 4.
4. **В `eighteen` выпадает одна буква.** `eight` и `teen` вместе дали бы
   `eightteen`, а пишется `eighteen` — одна `t` вместо двух.
   `Eighteen.` — Восемнадцать. · `Eight, eighteen.` — Восемь, восемнадцать.
5. **`twenty` закрывает счёт до двадцати** и хвоста `-teen` не имеет.
   `Twenty.` — Двадцать. · `Nineteen, twenty.` — Девятнадцать, двадцать.
6. **Хвост `-teen` звучит долго:** гласный в нём тянется.
   `Thirteen.` — Тринадцать. · `Nineteen.` — Девятнадцать. `zvuk` с ключами
   `/θɜːˈtiːn/` и `/naɪnˈtiːn/`, напечатанными в правиле дословно.
   **Ударение в правиле НЕ называется** — источника у него нет (раздел 15), и
   правило говорит только о долготе гласного.
   **Переводы дописаны — это ошибка 16 разбора:** обе строки стояли без русского
   рядом, а правило 9 (`docs/format-uroka.md`) этого не допускает. Оговорка про
   «строку со своим тире» тут не работает: тире в этих строках нет.
7. **Число цифрой и словом — то же, что в уроке 1**, но на двузначном.
   `Number fifteen.` — Номер пятнадцать. · `Number 15.` — Номер пятнадцать.
   `zvuk` ставит второй строке то же произнесение, что первой: правило говорит,
   что вслух они одинаковы, значит и звучать должны одинаково.
8. **Числа с хвостом `-teen` не меняются перед предметом.**
   `thirteen books` — тринадцать книг · `fifteen minutes` — пятнадцать минут.
   Работает `-s` из урока 2.

### 3.3. Пять заданий

| № | id | Вид | Что делает ученик | Случай |
|---|---|---|---|---|
| E1 | `e1-ispravit-threeteen` | `short` | «Ученик записал: `threeteen`. Запиши число без ошибки.» → `thirteen` | 3 |
| E2 | `e2-sobrat-iz-bukv` | `order` | собирает `eighteen` из восьми отдельных букв | 4 |
| E3 | `e3-dopisat-hvost` | `gap` | `six___` → `teen` | 2 |
| E4 | `e4-uslyshat` | `choice` + `zvuk: "fifteen"` | `fifteen` · `fifty` · `five` | 6 |
| E5 | `e5-skazat-vsluh` | `speak` | считает вслух от одиннадцати до двадцати | 1, 2, 5 |

**E1 — настоящая ошибка**, а не выдумка: `threeteen` и `fiveteen` ученики пишут,
и замороженный курс её ловит (`chisla-vozrast-cena.ts:330, 349, 1711`; запись в
`courses/resheno.ts`).

**E2 переписан — это ошибка 13 разбора и первое из трёх упражнений владельца.**
Было: собрать `eighteen` из кусков `eigh` + `teen`. Ученик, выучивший случай 3,
ищет кусок `eight` и не находит, а `eigh` встаёт третьей «особой частью» ровно
там, где случай 3 настаивает, что их две. Стало: **буквы вразброс** —
`items: ["E", "I", "G", "H", "T", "E", "E", "N"]`, по одной букве, как в образце
модуля 1 (`alfavit.ts:778`, `items: ["D", "E", "B"]`). При сборке по буквам
вопрос о разрезе не возникает вовсе, а выпавшая `t` из случая 4 проверяется
прямо: лишней буквы в наборе нет.

**Повторяющиеся буквы задание не ломают** — проверено чтением сверки, а не
догадкой по соседней ветке: `lib/content/check.ts`, ветка `order`, сравнивает
СОБРАННОЕ, а не номера («Сверка по собранному верна и для сборки фразы: два
одинаковых куска взаимозаменимы всегда»). Три `E` в `eighteen` дают один и тот же
ответ в любом порядке.

**Требование к подсказке:** она говорит, на что смотреть (сколько букв `t` в
слове), а не какое слово собирают. Подсказка-ответ была самой частой находкой
приёмки модуля 1 — девять из двадцати восьми.

**E4 берёт `fifty` отвлекающим нарочно:** десятки приходят уроком 4, и слово
здесь не требуется к производству — только к отбрасыванию по звуку. Это не
заимствование: ученик его не пишет и не говорит.

**`order` есть только в этом уроке** — в остальных семи его нет ни разу.

---

## 4. Урок 4 «Десятки и сотня: хвост -ty»

```
slug: "desyatki-i-sotnya"
title: "Десятки и сотня: хвост -ty"
vid: "pravila"
estimatedMinutes: 14
outcome: "называть числа от двадцати до ста и не путать -teen с -ty"
```

**Ступень:** Young Learners, **с. 81**, колонка **A1 Movers** — `21–100`,
`hundred`. И **с. 36**, совет к A1 Movers: «make sure children are confident with
numbers **up to 100** in English». Потолок назван источником дважды.

### 4.1. Восемь случаев

1. **Десятки заканчиваются на `-ty`** — таблица 20–90, `zvuchat` у каждой
   строки.
   `Twenty, thirty.` — Двадцать, тридцать. · `Eighty, ninety.` — Восемьдесят,
   девяносто.
2. **Те же две особые части: `thir-` и `fif-`.**
   `Thirty.` — Тридцать. · `Fifty.` — Пятьдесят.
3. **`forty` пишется без `u`**, хотя `four` — с `u`. И `eighty` теряет `t`, как
   `eighteen` в уроке 3.
   `Forty.` — Сорок. · `Eighty.` — Восемьдесят.
4. **Составное число пишется через дефис.**
   `twenty-one` — двадцать один · `thirty-five` — тридцать пять.
   **Осторожно со звуком:** дефис синтез читает слитно (решение владельца от
   5 сентября), и здесь это верно — `twenty-one` и есть одно слово. Диктовка по
   цифрам через дефис не пишется нигде: там запятая.
5. **Сотня — `one hundred`.**
   `One hundred.` — Сто. · `Ninety, one hundred.` — Девяносто, сто.
6. **Пара `thirteen` — `thirty`: разный хвост.**
   `Thirteen.` `/θɜːˈtiːn/` — Тринадцать. · `Thirty.` `/ˈθɜːti/` — Тридцать.
   Правило говорит про хвост и про долготу гласного: в `-teen` гласный тянется, в
   `-ty` короткий. **Про ударение не говорит ничего** — источника нет.
   **Переводы дописаны — ошибка 16 разбора**, как и в случае 6 урока 3.
7. **Пара работает на всех десятках.**
   `fourteen` — `forty` · `fifteen` — `fifty`.
   (Строки со своим тире перевода не получают — правило 2 оговорок к переводу:
   перевод выводится через тире и слился бы с ними. Разницу называет столбец
   таблицы случая 1 и словарь.)
8. **Десятки считают предметы так же.**
   `twenty books` — двадцать книг · `fifty metres` — пятьдесят метров.

**Довод против `a hundred` переписан, и старый был неверен — находка второго
круга.** Первая редакция писала: «`a hundred` не берём: артикль курс не
объяснял». Это утверждение о курсе, сделанное по памяти, и оно ложно: модуль 3
«Откуда ты и где живёшь» разбирает артикль ДВУМЯ уроками — «Один из таких: a и
an» (`otkuda-ty.ts:1404`) и «Тот самый: the» (там же, 1786), и оба стоят раньше
модуля 4. Настоящая причина другая и мельче: **у всех чисел модуля один строй —
число называется числом**, и `one hundred` этот строй держит. `a hundred` —
правильный английский, и **ошибкой его не объявляет ни одно задание**; там, где
ученик может его написать, он стоит в `accept`.

### 4.2. Словарь: две карточки

`metre` `/ˈmiːtə/` (`fifty metres`), `kilometre` `/ˈkɪləˌmiːtə/`
(`twenty kilometres`). Оба Oxford A1, оба из темы `Measurements`, оба работают
только с десятками — на числах до десяти им негде.

**Знак второстепенного ударения в `kilometre` возвращён — это сведение 26
разбора.** Источник даёт `/kˈɪləmˌiːtɐ/` (`materials/ipa-en-uk.txt`, строка
31410), первая редакция писала `/ˈkɪləmiːtə/` и знак теряла. Решение владельца от
6 сентября: «можешь показывать, не надо объяснять» — знак стоит так, как его даёт
словарь, а строки о самом знаке в уроке нет. `ɐ` заменено на `ə` по условности
курса.

**Про `kilometre` остаётся вопрос владельцу:** двух чтений (`/ˈkɪləˌmiːtə/` и
`/kɪˈlɒmɪtə/`) наши источники не различают, `ipa-en-uk.txt` даёт одно. Раздел 17.

### 4.3. Пять заданий

| № | id | Вид | Что делает ученик | Случай |
|---|---|---|---|---|
| F1 | `f1-teen-ili-ty` | `choice` + `zvuk: "thirty"` | `thirteen` · `thirty` | 6 |
| F2 | `f2-zapisat-cifroy` | `short` + `zvuk: "sixty"` | записывает услышанное цифрой → `60`, `accept: ["sixty"]` | 1 |
| F3 | `f3-defis` | `gap` | `twenty_one` → `-` | 4 |
| F4 | `f4-setka` | `setka` | находит в поле букв `THIRTEEN` и `THIRTY` | 6 |
| F5 | `f5-skazat-vsluh` | `speak` | называет `twenty-one`, `forty`, `one hundred` | 3, 4, 5 |

**F2 — единственное место модуля, где условие сужает форму ответа**, и причина
названа прямо в условии: двузначное число со слуха проще записать цифрой.
`accept` при этом держит и слово — иначе верный ответ наказывается.

**F3 — задание на дефис, и вида «дописать знак» в модуле больше нет нигде.**

**F4 — сетка букв, второе из трёх упражнений владельца, и она ЗАМЕНИЛА
`hottext`, а не добавилась.** Разбор верно посчитал: если сетку просто дописать,
доля «произвести ответ» в уроке падает до половины (`setka` в
`PRODUCTIVE_KINDS` не входит), и правило «больше половины» ломается. Замена
прежнего `hottext` («отметить десятки в ряду») сохраняет долю 3/5 и снимает
задание, которое проверяло ровно то же самое узнаванием.

**Три оговорки сетки исполнены** (разбор, раздел о трёх видах):

1. **чисел до десяти в поле нет ни одного.** `ONE`, `TWO`, `SIX`, `TEN` в три-
   четыре буквы возникают в случайном поле сами, ученик обводит настоящее слово и
   получает «пока не так». Искомые — `THIRTEEN` (8 букв) и `THIRTY` (6);
   **заполнение поля сборщик подбирает, а не берёт случайным, и прогоняет по
   словарю модуля**, чтобы в строках и столбцах не сложилось ни одного слова
   курса;
2. **в счёт «произвести ответ» сетка не идёт** — см. выше про замену;
3. **список даёт русские подписи** (`podpis`): «тринадцать» и «тридцать», а не
   сами английские слова. Иначе ученик ищет напечатанную рядом строку, и задание
   меряет зоркость, а не память о написании.

**Пара выбрана `THIRTEEN`/`THIRTY`, а не `SEVENTEEN`/`SEVENTY`, как предлагал
разбор.** Причина счётная: `seventy` — ответ вопроса работы (раздел 12, вопрос
8), и печатать его в сетке значило бы напечатать ответ. А `thirteen`/`thirty` —
главная пара самого урока (случай 6), и второе задание на неё, но письменное,
работает на предмет урока, а не мимо него.

**Сетка не ставится в банк вопросов работы** — запрет записан в самом типе
(`lib/content/types.ts:519–538`) и проверяется `check:content`: список искомых
слов обязан уйти в браузер, а вместе с ним ушёл бы ответ.

---

## 5. Урок 5 «Сколько тебе лет»

```
slug: "skolko-let"
title: "Сколько тебе лет"
vid: "pravila"
estimatedMinutes: 13
outcome: "называть свой возраст и спрашивать о возрасте"
```

**Опора:** CEFR Companion, **с. 79**, Information exchange, **Pre-A1** — «Can
tell people their age and ask people about their age». Оборот `How old are you?`
— Young Learners, **с. 28** (Speaking Part 4) и **с. 16** (Listening Part 2:
«How old is the new girl? … 8»), обе Pre A1 Starters. Расхождение с English
Grammar Profile объявлено в шапке модуля (раздел 1.5 замысла модуля).

**`egpPovtor`:** `1741163715288x539616242661052000`, `VERBS / types`, FORM:
LINKING VERB, BE — первый пример графы дословно «**I am 22 years old.**»

### 5.1. Сцена

```
— How old are you?   — Сколько тебе лет?
— I am twenty.       — Мне двадцать.
— Twenty? Good.      — Двадцать? Хорошо.
```

Голоса ж — м — ж. Все слова свои: `good` — карточка модуля 1, `twenty` — урок 4,
`How old are you?` — формула этого урока.

**Строка `Me too` из первой редакции снята.** Она требовала объектного `me` —
пункт чужой (модуль 6), и заимствование ради одной реплики того не стоит.
`Good` уже работает в сценах уроков 1 и 2, и повтор здесь законен: это отклик, а
не предмет разбора.

### 5.2. Восемь случаев

1. **Возраст говорят через `am`.** По-русски «мне двадцать лет» — без глагола
   вовсе; по-английски глагол обязателен.
   `I am twenty years old.` — Мне двадцать лет. · `I am ten years old.` — Мне
   десять лет.
   **Это расхождение с русским и главный случай урока.**
2. **`years` — то же `-s`, что в уроке 2.**
   `twenty years` — двадцать лет · `one year` — один год.
3. **Короткий ответ — без `years old`.**
   `I am twenty.` — Мне двадцать. · `I am ten.` — Мне десять.
4. **Вопрос — целой формулой.** Три слова стоят вместе, разбирать их не нужно.
   `How old are you?` — Сколько тебе лет? · `How old is she?` — Сколько ей лет?
5. **О другом человеке — `is`.**
   `She is ten.` — Ей десять. · `He is thirty.` — Ему тридцать.
6. **`old` в этом обороте не значит «старый».** Карточка даёт «старый», и без
   этой строки ученик прочтёт вопрос как «насколько ты стар». Ловушка наша
   собственная: слово он получил в этом же уроке.
   `How old are you? — I am ten.` — Сколько тебе лет? — Мне десять. ·
   `He is thirty.` — Ему тридцать.
7. **Возраст пишут цифрой в бланке.**
   `Age: 20` — Возраст: 20. · `Age: 15` — Возраст: 15.
   Долг с адресом: бланк разбирает модуль 5 «Бланк: имя, страна, адрес».
   **Сборщику знать:** шапка модуля 5 обещает, что закрывает половину дескриптора
   CEFR с. 84 полем `Age`, а в замысле его урока 4 поля `Age` нет ни в одной
   строке (записка дня 6 сентября, раздел о модуле 5). Долг остаётся адресован
   модулю 5, но обещание там надо либо исполнить, либо снять.
8. **Ответить можно и одним числом.**
   `How old are you? — Twenty.` — Сколько тебе лет? — Двадцать. ·
   `How old is she? — Ten.` — Сколько ей лет? — Десять.
   Опора — Young Learners, **с. 16**, Pre A1 Starters, Listening Part 2: в
   образце ответом на «How old is the new girl?» стоит **`8`**, одно число.
   Правило говорит «можно и так, и так», а не «только так»: обе формы верны, и
   обе стоят в `accept` задания G4.

**Случай 8 переписан целиком — это ошибка 9 разбора.** Стояло «`one year` — один
год · `twenty years` — двадцать лет», то есть та же пара строк, что в случае 2,
переставленная местами: восьмой случай набирался повтором, и разных примеров в
уроке было четырнадцать вместо шестнадцати. Теперь случай 8 говорит своё, стоит
на прямом примере источника и работает на задание G4.

### 5.3. Словарь: три карточки

`year` `/jɪə/`, `age` `/eɪdʒ/`, `old` `/əʊld/`. Все Oxford A1.

### 5.4. Пять заданий

| № | id | Вид | Что делает ученик | Случай |
|---|---|---|---|---|
| G1 | `g1-ispravit-vozrast` | `short` | «Ученик написал: `I fifteen years old`. Запиши строку без ошибки.» → `I am fifteen years old` | 1 |
| G2 | `g2-dopisat-old` | `gap` | `He is thirty years ___` → `old` | 1, 5 |
| G3 | `g3-vopros-i-otvet` | `match` | `How old are you?` ↔ `I am twenty.` · `How old is she?` ↔ `She is ten.` | 4, 5 |
| G4 | `g4-o-sebe` | `short` | «Ответь о себе на вопрос `How old are you?`» → образец `I am twenty.`, `accept` — обе рамки со всеми числами модуля | 3, 8 |
| G5 | `g5-skazat-vsluh` | `speak` | отвечает вслух на `How old are you?` | 1, 3 |

**G1 — настоящая ошибка**, пропуск связки: её делают те, чей родной язык
обходится без глагола. **Число заменено на `fifteen` — это ошибка 14 разбора:**
ответ `I am twenty years old` стоял первым примером случая 1 дословно.

**G2 переписан по той же причине:** рамка `I am ten years ___` дословно
повторяла второй пример случая 1. Строка `He is thirty years old` в уроке не
печатается, и задание заодно проверяет случай 5.

**G3 близнецом не является, и это проверено вопросом «что ещё сюда подходит»:**
`I am twenty.` не может ответить на `How old is she?`, а `She is ten.` — на
`How old are you?`. Пар две, решение одно.

**G4 переписан — это ошибки 15 и 22 разбора, и это две разные беды.**

- Было: «Напиши, сколько тебе лет, целой строкой» с `accept` «та же рамка со
  всеми числами модуля». **Случай 3 ТОГО ЖЕ УРОКА учит короткому ответу
  `I am twenty.`** — верная по нашему же уроку строка, которую `accept` не
  держал. Ученик отвечал правильно и получал «пока не так»;
- и ответ-образец G4 совпадал с ответом G1 дословно: тот, кому двадцать, писал
  строку, которую переписывал двумя экранами выше.

Стало: условие не навязывает длину («Ответь о себе»), а `accept` держит **обе**
рамки — `I am <число>.` и `I am <число> years old.` — со всеми числами модуля,
словом и цифрой. Решение владельца: несколько верных решений можно, если они
правильные. Ответ G1 (`I am fifteen years old`) при этом больше не совпадает с
образцом G4 (`I am twenty.`) ни рамкой, ни числом.

**Звука в заданиях урока 5 нет вовсе** — возраст ученик говорит и пишет, а не
опознаёт на слух. Это единственный урок правил без звука в заданиях, и это
нарочно: слух работает в уроках 6, 8 и 9.

**Чего в отвлекающих быть НЕ должно, и оба случая проверены:**
`I have twenty years` — калька романских языков, а не русская; `My age is twenty`
— **правильный английский**, и объявить его ошибкой значило бы наказать за верный
ответ.

---

## 6. Урок 6 «Номер по одной цифре»

```
slug: "nomer-po-cifram"
title: "Номер по одной цифре"
vid: "pravila"
estimatedMinutes: 14
outcome: "диктовать длинный номер по одной цифре, а короткий целиком"
```

**Опора:** CEFR Companion, **с. 79**, Pre-A1 — «Can ask for and give a **phone
number**». **Этого дескриптора не исполнил ни один из 56 модулей замороженного
курса** (раздел 0 замысла модуля), значит урок пишется с нуля.

**Решение владельца от 2 сентября 2026:** короткий номер называют целиком
(`sixty-three`), по одной цифре диктуют длинный.

### 6.1. Сцена

```
— Your phone number, please?      — Ваш номер телефона, пожалуйста.
— Seven, seven, one, two, three.  — Семь, семь, один, два, три.
— Seven, seven, one, two, three?  — Семь, семь, один, два, три?
— Yes.                            — Да.
```

**Строка `What is your phone number?` СНЯТА ИЗ ВСЕГО МОДУЛЯ — это ошибка 2
разбора, и правка сделана не так, как он предлагал.** Разбор нашёл, что оборот
стоял в уроках 6, 8 и 9 пятым, необъявленным заимствованием, и предлагал дописать
пятую строку в шапку модуля с адресом (модуль 7). Ступень при этом вправду не
превышена — проверил сам: CEFR Companion, **с. 79**, графа Pre-A1, та же шкала
Information exchange, что держит три дескриптора модуля: «Can ask very simple
questions for information, such as “**What is this?**” and understand one- or
two-word/sign answers» (поиск по файлу дал ровно одно совпадение, страница 79).

**И всё-таки оборот снят, по трём доводам, и ни один из них не про ступень.**

1. **Курс уже решил этот вопрос — в модуле 2, и решил обратно.**
   `courses/english-beginner-2/privetstvie.ts:2550–2560`, шапка урока говорения:
   цитата источника берётся «ТОЛЬКО ДО ПЕРВОЙ ТОЧКИ: дальше в той же строке стоят
   `and this is …` и `What's your name?`, обе конструкции A2. **Спрашивать имя
   ученик умеет строкой модуля 1 — `Your name, please?`**» Взять `What is …?` в
   модуле 4 значило бы дать одному классу оборотов два решения внутри одной
   ступени — ровно та беда, из-за которой `Who is that?` получил на Elementary
   две ступени в двух модулях.
2. **Слова `what` ученик к четвёртому модулю не получал ни в каком виде.**
   Карточка — `chto-eto-takoe.ts:861`, то есть модуль 7. Прогон по всему курсу
   даёт вне модуля 7 только комментарии и поля `sources`: **в видимом тексте
   курса слова `what` нет ни разу.** Объявление в шапке этого не меняет — оно
   объясняет заимствование, но не даёт ученику слова.
3. **Первую встречу отбирали бы у соседа.** Шапка модуля 5 (`blank.ts:42–46`)
   называет `What is your name?` своим оборотом и своим уроком 8; модуль 7 зовёт
   `What is this?` предметом своего первого урока. Оба стоят позже четвёртого.

**Замена ничего не стоит и стоит на строке самого курса.** `Your phone number,
please?` собран из слов, которые у ученика есть: `your` — карточка модуля 1
(`alfavit.ts:565`), `please` — там же (579), `phone` и `number` — карточки этого
модуля. Рамка `Your …, please?` знакома ученику с первого модуля
(`alfavit.ts:473`, сцена у стойки) и работает в модуле 5 (`Your form, please.`,
`Your surname, please.`). Дескриптор «Can ask for and give a phone number»
исполняется целиком: спросить ученик умеет этой строкой.

**Записи звука для новой строки в `public/zvuk/` нет** — её озвучит
`npm run ozvuchka` при сборке.

**`phone number` взято ГОТОВЫМ** — четвёртая строка шапки модуля, адрес —
модуль 5 (раздел 1.4 замысла модуля). Ученик его не собирает ни в одном задании.

**Разделитель — запятая.** Дефис синтез читает слитно (владелец, 5 сентября), а
тире с пробелами означает смену голоса (`razgovorLi`). **Это правило сборщика, а
не случай урока** — см. ниже.

### 6.2. Восемь случаев

1. **Длинный номер диктуют по одной цифре.**
   `Seven, seven, one.` — Семь, семь, один. · `Four, two, five.` — Четыре, два,
   пять.
2. **Короткий номер называют целиком, и граница названа числом знаков.**
   Номер из двух знаков говорят одним словом, из трёх и больше — по цифрам.
   `Sixty-three.` — Шестьдесят три. · `Number sixty-three.` — Номер шестьдесят
   три.
   Решение владельца от 2 сентября 2026 дословно: «Комната 63 — это
   `sixty-three`… По одной цифре диктуют ДЛИННЫЙ номер: комнату из трёх цифр,
   рейс, счёт». **Границу надо напечатать ученику, а не подразумевать:** без неё
   задание H2 и вопрос работы про комнату не имеют одного верного ответа.
3. **Ноль в номере — `zero`.**
   `Four, zero, five.` — Четыре, ноль, пять. · `Zero, seven.` — Ноль, семь.
4. **Слово `number` называет и число, и номер.**
   `Number ten.` — Номер десять. · `My number is ten.` — Мой номер десять.
5. **Свой номер называют формулой.**
   `My phone number is seven, seven, one.` — Мой номер телефона: семь, семь,
   один. · `My phone number is four, zero, five.` — Мой номер телефона: четыре,
   ноль, пять.
6. **Переспрашивают повтором.**
   `Seven, seven? — Yes.` — Семь, семь? — Да. · `Zero, five? — Yes.` — Ноль,
   пять? — Да.
7. **Номер записывают цифрами, а читают по одной.**
   `77123` — Семь, семь, один, два, три. · `9018` — Девять, ноль, один, восемь.
   Опора — Young Learners, **с. 16**: «encouraged to write numbers as digits
   rather than words to avoid spelling mistakes». `zvuk` задаётся словами:
   `{"77123": "seven, seven, one, two, three"}`.
8. **Номер просят строкой `Your phone number, please?`**
   `Your phone number, please?` — Ваш номер телефона, пожалуйста. ·
   `Your number, please?` — Ваш номер, пожалуйста.
   Рамка та же, что в модуле 1 при вопросе об имени; ссылка на свой модуль внутри
   ступени законна и приветствуется.

**Прежний случай 8 снят — это ошибка 11 разбора.** Он говорил: «Между цифрами —
запятая, а не дефис. Диктуя, ставим запятую: так пауза слышна». Обе причины —
наши: дефис синтез читает слитно (решение владельца об ОЗВУЧКЕ), а тире с
пробелами означает смену голоса (`razgovorLi()`, `lib/content/zvuk.ts`). Правила
английского здесь нет вовсе — раздел 15 честно пишет, что о чтении номера
источники молчат. Ученику о нашей кухне не рассказывают: **запятая остаётся
правилом сборщика** (раздел 4 замысла модуля), а место случая заняло то, чего
модулю вправду недоставало, — как номер ПРОСЯТ.

**Урок добирает мерку — это ошибка 10 разбора.** Было тринадцать примеров: у
случая 8 не было ни одного, у случая 7 — один. Стало шестнадцать при восьми
случаях.

### 6.3. Словарь: две карточки

| Слово | Перевод | Пример | Чтение | Значок |
|---|---|---|---|---|
| `zero` | ноль | `Four, zero, five.` | `/ˈzɪərəʊ/` | нет |
| `phone` | телефон | `My phone number is …` | `/fəʊn/` | `1f4f1` |

`zero` — **Oxford A2**, превышение объявлено в шапке модуля: без нуля номер не
продиктовать, а телефон — прямой дескриптор Pre-A1. `phone` — Oxford A1; Young
Learners, **с. 81**, список «The home», колонка **Pre A1 Starters**. Карточка
`number` стоит в уроке 1.

**Значок `1f4f1` — это третье из трёх упражнений владельца** (задание H4). В
`public/twemoji` его сегодня нет (154 файла), файл забирает `npm run znachki` по
коду, названному в содержании: **сборщик обязан прогнать команду и убедиться, что
файл лёг**, а не поверить этой строке. Правило проекта: утверждение о том, что
делает код, проверяется прогоном.

### 6.4. Пять заданий

| № | id | Вид | Что делает ученик | Случай |
|---|---|---|---|---|
| H1 | `h1-zapisat-nomer` | `short` + `zvuk: "seven, four, two"` | записывает номер цифрами → `742` | 1, 7 |
| H2 | `h2-celikom-ili-po-cifram` | `choice` + `zvuk: "Sixty-three."` | как записали услышанное: `63` · `six, three` | 2 |
| H3 | `h3-dopisat-zero` | `gap` | `four, ___, five` → `zero` | 3 |
| H4 | `h4-chto-na-kartinke` | `short` + `znak: "1f4f1"` | «Что на картинке? Запиши по-английски.» → `phone` | словарь |
| H5 | `h5-skazat-vsluh` | `speak` | диктует вслух номер по цифрам и называет комнату `sixty-three` | 1, 2, 5 |

**Прежнее третье задание снято — это вопрос 19 разбора, и он верен трижды.** Было
`hottext`: в ряду `seven, seven · seventeen · one, two · twelve` отметить
прочитанные по цифрам. Пара `one, two` и `twelve` работала, пара `seven, seven` и
`seventeen` — нет: это 77 и 17, разные числа, и противопоставления между ними
не существует. Отметить надо было ровно те строки, где стоит запятая, — английский
для этого не нужен. И третье, чего разбор назвать не мог, потому что урок этого не
печатал: **у задания не было границы «длинный — короткий»**, а `77` и `12` по
случаю 2 читаются целиком. Граница теперь напечатана (случай 2), а H2 проверяет
её одним верным ответом.

**H2 отвлекающим берёт настоящую ошибку:** диктовку двузначного номера по цифрам
(`six, three`) — именно её делает ученик, выучивший случай 1 и не заметивший
случая 2. Замороженный курс обжигался на обратном: объявлял `sixty-three` ошибкой
и наказывал за верный ответ.

**H4 — картинка в задании, а не в вариантах**, и это осознанный выбор. Владелец
просил «отгадать вещь на картинке»; вариантами это было бы узнаванием, а `short`
со значком заставляет слово ПРОИЗВЕСТИ. Слово выбрано единственное годное: из
тринадцати новых карточек модуля картинкой точно называется только `phone`
(разбор — раздел 2.4).

**Ни одно задание не требует собрать `phone number`** — второе условие сводящего:
сочетание приходит готовой строкой в условии и в образце, а ученик производит
цифры.

---

## 7. Урок 7 «Читаем записку с числами»

```
slug: "chitaem-zapisku"
title: "Читаем записку с числами"
vid: "chtenie"
estimatedMinutes: 12
outcome: "находить в записке номер телефона и число"
```

**Опора:** CEFR Companion, **с. 55**, Reading correspondence, графа **Pre-A1**:
«Can recognise times and places in very simple notes and text messages from
friends or colleagues … **provided there are no abbreviations**».

**Оговорка источника становится требованием к тексту: НИКАКИХ СОКРАЩЕНИЙ.** Ни
`Rm 12`, ни `tel.`, ни `min`. Это то самое место, где оговорка графы важнее самой
графы.

**Итог сведён с замыслом модуля — это вопрос 17 разбора.** `plan-modul-4.md`
обещал «находить в записке номер комнаты и номер телефона», а строка про комнату
из текста снята (слова `room` в курсе нет ни одной карточкой). Два документа
давали уроку разные предметы поиска, и сборщик взял бы тот, что попался.
Правильный — «номер телефона и число»; замысел модуля исправлен.

### 7.1. Текст

`kind: "text"`, `genre: "message"`, `id: "zapiska-ot-dany"`.

```
Hi Alim!                                          Привет, Алим!
My phone number is nine, one, eight.              Мой номер телефона: девять, один, восемь.
Two books. One key.                               Две книги. Один ключ.
Ten minutes.                                      Десять минут.
Dana                                              Дана
```

**Текст собирался пять раз, и каждая правка снимала чужое слово.** Записываю все,
потому что ловушка общая и повторится в любом уроке чтения.

| Что стояло | Почему снято |
|---|---|
| `I am in room twelve.` | слова `room` в курсе нет ни одной карточкой |
| `I have two books for you.` | глагола `have` курс не вводил |
| `Two books and one key for you.` | `and` вводит модуль 6 |
| `Ten minutes and I am ready.` | слова `ready` в курсе нет: оно жило в выведенном модуле `kto-eto.ts` |
| `Two books for you. One key for you.` | **предлог `for` — сведение 24 разбора** |

**О `for` решено, а не отложено.** Первая редакция писала: «пятое заимствование,
если сводящий сочтёт нужным» — то есть ставка, а не решение. Грамматически он
закрыт (`PREPOSITIONS`, FORM: SIMPLE, `1741163713112x806003192035927200`, стоит в
`egp` модуля 3, `otkuda-ty.ts:97`), но СЛОВОМ ученик его не получал, а
`npm run kontrol` промолчал бы по устройству: `what`, `how`, `for`, `there` лежат
в списке `SLUZHEBNYE`. Значит либо пятая строка в шапке, либо слово вон. **Слово
вон:** записка ничего не теряет — `Two books. One key.` для жанра `message` даже
естественнее, а модуль остаётся при четырёх объявленных заимствованиях вместо
шести.

**Итог: все слова свои.** `hi` — карточка модуля 2 (`privetstvie.ts:350`);
`book`, `key` — модулей 1 и 4; `minute` — урок 2 этого модуля; `phone number` —
урок 6, взято готовым; числа — уроки 1 и 3. Имена ступенью не меряются.

### 7.2. Три задания

| № | id | Вид | Что делает ученик | Случай |
|---|---|---|---|---|
| K1 | `k1-nomer-telefona` | `short` + `about: "zapiska-ot-dany"` | выписывает номер цифрами → `918` | текст |
| K2 | `k2-skolko-knig` | `choice` + `about` | сколько книг: `one` · `two` · `ten` | текст |
| K3 | `k3-skolko-minut` | `short` + `about` | сколько минут → `ten`, `accept: ["10"]` | текст |

**`about` здесь законен, и это единственный урок модуля, где он стоит:** блок
`text` — один из двух видов, которые принимает проверка
(`check-content.mts`, строка 1154). В уроках 1–6 его нет ни у одного задания.

**K2 берёт отвлекающими числа из самой записки** (`one` от ключа, `ten` от
минут) — оба настоящие: ученик, читающий невнимательно, возьмёт любое.

---

## 8. Урок 8 «Слушаем числа»

```
slug: "slushaem-chisla"
title: "Слушаем числа"
vid: "slushanie"
estimatedMinutes: 12
outcome: "записывать со слуха названное число"
```

**Формат взят у Cambridge дословно:** A2 Key, **с. 29** — «The candidates should
only write ONE word, or a number, or a date, or a time for their answer»; Young
Learners, **с. 16** — «Each answer is either a name or a **number** … The numbers
dictated **can be written as digits or words**»; **с. 13** — «Note-taking.
**Write numbers and names in gaps**» (проверено вторым методистом по второму
разбору, строка 443).

### 8.1. Записи

Три блока `audio`, каждый — короткий разговор. **Поле `voice` обязательно у
каждого:** без него разговор читается одним голосом, и эту беду в проекте ловили
дважды — сорок примеров ступени Elementary и двадцать четыре блока `audio`.

1. `How old are you? — I am thirteen.`
2. `Your phone number, please? — Four, zero, five.`
3. `How many books are there? — Twelve.`

**Первые два держатся в двадцатке нарочно** — Young Learners, с. 16:
«Candidates will only hear numbers 1–20». Третья запись тоже: десятки приходят
только четвёртым заданием, где ответ выбирается, а не пишется.

### 8.2. Четыре задания

| № | id | Вид | Что делает ученик | Случай |
|---|---|---|---|---|
| L1 | `l1-vozrast-so-sluha` | `short` + запись 1 | → `13`, `accept: ["thirteen"]` | запись 1 |
| L2 | `l2-nomer-so-sluha` | `short` + запись 2 | → `405` | запись 2 |
| L3 | `l3-skolko-knig` | `short` + запись 3 | → `12`, `accept: ["twelve"]` | запись 3 |
| L4 | `l4-teen-ili-ty` | `choice` + `zvuk: "eighty"` | `eighteen` · `eighty` | запись 4 |

**Все четыре ответа — одно число**, как требует источник. `speak` в уроке 8 нет:
он весь в уроке 9.

**Числа записей и L4 пересчитаны — это вопрос 21 разбора.** Было: `fifteen` стоял
ответом задания E4 урока 3, ответом записи 3 и ответом двух вопросов работы, а на
пару `-teen`/`-ty` приходилось три вопроса работы из двенадцати. Теперь каждая
пара работает в одном месте: `thirteen`/`thirty` — урок 4 (F1 и сетка F4),
`fifteen`/`fifty` — урок 3 (E4), `eighteen`/`eighty` — урок 8 (L4),
`seventeen`/`seventy` — работа модуля (вопрос 8). Запись 3 отдана числу `twelve`.

**L2 и H1 из урока 6 — ближайшая пара модуля**, и разведены они длиной номера и
местом нуля: в уроке 6 три цифры без нуля и вне разговора, здесь три цифры с
нулём в середине и внутри разговора. **При написании перечитать оба условия
рядом** (раздел 13.3).

---

## 9. Урок 9 «Говорим о числах»

```
slug: "govorim-o-chislah"
title: "Говорим о числах"
vid: "govorenie"
estimatedMinutes: 12
outcome: "отвечать на вопрос о возрасте и на просьбу назвать номер"
```

**Итог возвращён к тому, который выбрал первый круг, — это вопрос 17 разбора, и
он был самым дорогим из шести.** В прошлой редакции здесь стояло «различать на
слух `-teen` и `-ty`»: итог, который первый круг СНЯЛ как повтор урока 8
«Слушаем числа», и который замысел модуля уже заменил на «отвечать на вопросы о
своём возрасте и своём номере» (`plan-modul-4.md`, строки 799–802). Файл при
этом объявлял себя правкой по разбору — и ставил снятое обратно. **Два документа
модуля давали уроку 9 разные итоги, а сборщик взял бы тот, что попался.**

**Вместе с итогом снята и «разминка на слух»** — три пары своими записями. Она и
делала половину урока говорения вторым уроком слушания подряд. Пара
`-teen`/`-ty` остаётся там, где она объявлена главным случаем (урок 4), и
переспрашивается в уроке 8 и в работе.

**Поле `vid` обязательно:** в уроке стоят блоки `audio` (образцы), а вывод по
составу отдал бы такой урок в `slushanie`. `check:content` сверяет вывод с
заголовком и о расхождении спросит.

**Образец — A2 Key, с. 42, Speaking Part 1:** «The interlocutor asks **two
short-answer questions** to each candidate … followed by **one longer „Tell me
something about …" question**». Программа читает эту страницу так же: два
коротких ответа и один длинный, с записью-образцом.

### 9.1. Строение

- **два коротких ответа с записями-образцами:** на `How old are you?` — образец
  `I am twenty years old.`; на `Your phone number, please?` — образец
  `My phone number is seven, seven, one.`;
- **один длинный ответ:** «расскажи о числах своего дня» — сколько книг, сколько
  минут дорога, какой номер. Запись-образец обязательна;
- разбор под образцами: в ответе о возрасте стоит `I am`, в ответе о номере —
  `My phone number is`, дальше цифры.

### 9.2. Три задания

| № | id | Вид | Что делает ученик | Случай |
|---|---|---|---|---|
| M1 | `m1-otvetit-nomerom` | `gap` | «Твой номер — `412`. Ответь на просьбу.» `My phone number is ___.` → `four, one, two` | образец 2 |
| M2 | `m2-svoy-vozrast` | `speak` | отвечает вслух на `How old are you?`, образец `I am twenty years old.` | образец 1 |
| M3 | `m3-svoi-chisla` | `speak` | длинный ответ по образцу: `Two books. Ten minutes. My phone number is seven, seven, one.` | образец 3 |

**M1 — единственное машинно проверяемое задание урока, и рамка выбрана так,
чтобы условие сводящего не нарушалось.** Ученик вписывает ЦИФРЫ словами, а
`My phone number is` приходит готовой строкой: собирать составное существительное
он не должен нигде в модуле. Прежнее M1 (`choice` на паре `fourteen`/`forty`)
снято вместе с разминкой.

**Итог назван по проверяемой половине наполовину, и это надо сказать честно.**
Машина оценивает M1 (номер); ответ о возрасте проверяется вопросом работы
(раздел 12, вопрос 15), а не заданием урока: `speak` не оценивается. Обе половины
связки — настоящие дела урока, и обе спрашиваются: одна заданием, другая работой.

---

## 10. Что берётся готовым во всём модуле — сводка для шапки

Четыре строки, все с адресами. Тексты выписаны дословно в замысле модуля и уже
стоят в коде (`chisla-i-telefon.ts`, строки 38–75).

| Оборот | Где работает | Адрес разбора |
|---|---|---|
| `How many …?` | урок 2, случаи 7 и 8 | взят по Cambridge, разбора не требует; расхождение источников — строка в шапке |
| `there are` внутри вопроса | урок 2, случаи 7 и 8; урок 8, запись 3 | модуль 12 «Мой дом и комната» |
| `phone number` | уроки 6, 7, 9 | модуль 5 «Бланк: имя, страна, адрес» |
| `How old are you?` | урок 5, случай 4; уроки 8 и 9 | взят по Cambridge; расхождение — строка в шапке |

**Пятой строки НЕ БУДЕТ, и оба кандидата закрыты снятием, а не объявлением:**
`What is …?` (урок 6, раздел 6.1) и предлог `for` (урок 7, раздел 7.1). Правило
проекта говорит, что незаявленное заимствование хуже заявленного, — но
несуществующее лучше обоих, а обе замены ничего не стоили курсу.

**Строку про `What is …?` надо вычеркнуть из записки дня**
(`zapiska-6-sentyabrya.md`, раздел «Модуль 4») с пометой, чем закрыта: оборот
снят, вместо него стоит `Your phone number, please?`. Записка без вычеркнутых
строк через неделю превращается в свалку.

---

## 11. Долги модуля с адресами — дополнение к разделу 6 замысла модуля

| Долг | Где обещан ученику | Адрес |
|---|---|---|
| два предмета и больше | урок 1, конец случая 2 | урок 2 этого модуля |
| устройство `there is/are` | урок 2, случай 7 | модуль 12 |
| устройство `phone number` | урок 6, случай 5 | модуль 5 |
| бланк с полем `Age` | урок 5, случай 7 | модуль 5 |
| `-es`, особые формы, неисчисляемое | **ученику не обещаны вовсе** | модуль 8 |

**Ни одной врезки «бери как есть» без адреса в модуле нет.** Последняя строка
стоит здесь нарочно: это долг курса, а не обещание ученику, и в тексте уроков о
нём нет ни слова. Обещать то, чего ученик не спрашивал, — значит заводить долг на
пустом месте.

---

## 12. Работа модуля: пятнадцать вопросов на девять итогов

**Раздел заведён во второй редакции, и это правка устройства, а не строки.**
Прежде вопросы работы стояли врозь — по два в конце каждого урока, — и увидеть
банк целиком было негде. Оттого прошли незамеченными три вещи сразу: три итога
не спрашивались вовсе (ошибка 3 разбора), два вопроса оказались близнецами
заданий своих же уроков (ошибки 4 и 5), а два — близнецами друг друга.

**Почему без этого не собрать модуль.** `scripts/check-content.mts`, строки
1939–1941: каждый `lesson.outcome` обязан быть покрыт хотя бы одним вопросом
банка, иначе **ошибка сборки**. Уроков девять, вопросов было двенадцать, итогов
без вопроса — три (уроки 7, 8 и 9).

**Правило, по которому банк написан:** работа спрашивает то же умение ДРУГОЙ
дорогой. Близнец чинится сменой того, что ученик ДЕЛАЕТ, а не заменой имени или
числа.

| № | id | Вид | Итог | Что делает ученик |
|---|---|---|---|---|
| 1 | `chisla-4-slovo-v-cifru` | `short` | 1 | «Ученик записал число словом: `nine`. Запиши то же число цифрой.» → `9` |
| 2 | `chisla-4-naiti-propusk` | `gap` | 1 | «Записали: `four, five, ___, eight`. Одно число пропущено.» → `six`, `accept: ["6"]` |
| 3 | `chisla-4-shest-dney` | `short` | 2 | «По-русски: шесть дней. Запиши по-английски.» → `six days` |
| 4 | `chisla-4-najti-oshibki` | `hottext` | 2 | в ряду `four keys · three pen · two tickets · one books` отмечает записанные с ошибкой |
| 5 | `chisla-4-cifra-v-slovo` | `short` | 3 | «Запиши словом: 17» → `seventeen` |
| 6 | `chisla-4-vernoe-napisanie` | `choice` | 3 | `fourteen` · `forteen` |
| 7 | `chisla-4-sorok-bez-oshibki` | `short` | 4 | «Ученик записал сорок так: `fourty`. Запиши число без ошибки.» → `forty` |
| 8 | `chisla-4-semdesyat` | `choice` | 4 | «Записано `70`. Какое это слово?» `seventy` · `seventeen` |
| 9 | `chisla-4-vozrast-drugogo` | `gap` | 5 | `She ___ ten years old.` → `is` |
| 10 | `chisla-4-sprosit-vozrast` | `short` | 5 | «Ты хочешь узнать возраст. Запиши вопрос.» → `How old are you?` |
| 11 | `chisla-4-prodiktovat` | `short` | 6 | «Номер `6042`. Запиши, как его называют вслух, — по одной цифре, через запятую.» → `six, zero, four, two` |
| 12 | `chisla-4-kak-nazvat` | `choice` | 6 | «Комната 19. Как её называют?» `nineteen` · `one, nine` |
| 13 | `chisla-4-nomer-bez-oshibki` | `short` | 7 | записка: `Hi Alim! Ten books. My phone number is six, zero, two. Kim` — Алим записал номер как `620`. → `602` |
| 14 | `chisla-4-dva-chisla-na-sluh` | `short` + звук | 8 | разговор `How old are you? — I am sixteen. Your phone number, please? — Four, one, zero.` → возраст цифрой: `16`, `accept: ["sixteen"]` |
| 15 | `chisla-4-kakoy-vopros` | `choice` | 9 | «Ученик ответил: `I am twelve.` На какой вопрос?» `How old are you?` · `Your phone number, please?` · `How many books are there?` |

### 12.1. Что заменено и почему — по каждой находке разбора

**Вопрос 4 разбора: `chisla-4-para-na-sluh` был дословным близнецом E4.** Тот же
`choice`, та же запись `fifteen`, тот же верный ответ, тот же отвлекающий
`fifty`. Совпадало даже имя звукового файла: оно считается от текста
(`lib/content/zvuk.ts`). Снят целиком; итог 4 спрашивают вопросы 7 и 8, и оба —
письменные.

**Вопрос 5 разбора: `chisla-4-nomer-so-sluha` был близнецом L2**, а довод «в
уроке ноль дописывался в `gap`» сверялся не с тем заданием. Заменён вопросом 11:
ученик не слушает, а **переводит цифровую запись в диктовку** — действие, которого
нет ни в одном уроке (в уроке 6 диктует H5, но вслух, и машина этого не
оценивает).

**Вопрос 6 разбора: оба вопроса на итог урока 2 были близнецами D3 и D2.**
«Ученик записал: `one books`» против D3 «Ученик записал: `three ticket`» — та же
рамка, то же действие; `ten minute_` → `s` против D2 `two book_` → `s` — тот же
вид и та же строка ответа. Оправдания «ошибка обратная» и «предмет другой» —
ровно то, что правило от 2 сентября объявляет недостаточным. Заменены вопросами
3 (производство английской строки от русской — дороги, которой в уроках нет
вовсе) и 4 (сличение чужой записи в ряду).

**И одна находка второго круга, которой в разборе нет.** `chisla-4-slovo-desyatka`
(«Запиши словом: 40» → `forty`) был близнецом `chisla-4-cifra-v-slovo` («Запиши
словом: 17» → `seventeen`) — **внутри самого банка**: та же рамка, то же действие,
заменено только число. Порода та же, что разбор ловил в паре «урок — работа», и
она пряталась ровно потому, что вопросы стояли в разных разделах файла. Стал
вопросом 7: исправить чужое написание `fourty`, а это ошибка, которую случай 3
урока 4 и разбирает.

### 12.2. Три места банка, которые надо перечитать при написании

**Вопросы 6 и 7 — это одна ловушка с двух сторон, и так задумано.** `forteen`
против `fourteen` и `fourty` против `forty` держатся на том, что модуль сам и
показывает: `forty` пишется БЕЗ `u`, хотя `four` — с `u` (урок 4, случай 3).
Ученик, выучивший случай, ровно так и переносит потерю буквы на соседнее число.
Вопросы стоят на разных итогах (3 и 4) и требуют разного: один узнаёт написание,
другой его чинит. Выдуманным ни один не является — оба выведены из правила самого
модуля.

**Вопрос 12 держится на границе из случая 2 урока 6.** «Комната 19» называется
целиком (`nineteen`), потому что знаков два; отвлекающий `one, nine` — то, что
сделает ученик, запомнивший только диктовку по цифрам. **Пока граница не
напечатана в уроке, у этого вопроса два верных ответа** — это и есть довод, по
которому она в урок дописана.

**Вопрос 15 проверен вопросом «что ещё сюда подходит».** Строка `I am twelve.`
отвечает только на `How old are you?`: на просьбу назвать номер отвечают цифрами,
на `How many books are there?` — одним числом без `I am`. Верный ответ один.

### 12.3. Проверки, которые банк обязан пройти, и как он их проходит

- **все девять итогов покрыты** (`check-content.mts:1939`): 1 → вопросы 1–2,
  2 → 3–4, 3 → 5–6, 4 → 7–8, 5 → 9–10, 6 → 11–12, 7 → 13, 8 → 14, 9 → 15;
- **доля «произвести ответ» больше половины** (`check-content.mts:1961`):
  считает скрипт раздела 13, не рука;
- **сетки в банке нет** — запрет типа (`types.ts:519–538`);
- **вопрос со звуком в работе есть ровно один** — вопрос 14, и стоит он на итоге
  урока слушания, где услышанное число и есть ответ;
- **разговор вопроса 14 звучит на два голоса**: четыре реплики, границы по тире,
  спрашивает один голос (реплики 1 и 3), отвечает другой (2 и 4);
- **в задании на сличение (вопрос 4) все четыре строки стоят в одинаковом
  отношении к материалу:** ни одна из них — ни верные `four keys` и
  `two tickets`, ни ошибочные `three pen` и `one books` — в уроках не печатается.
  Первая редакция этого ряда держала `ten books`, а такая строка стоит примером
  случая 6 урока 2: два верных ответа были бы эхом урока, а два неверных — нет, и
  ученик отличал бы их по памяти об экране, а не по языку. Порода найдена
  6 сентября на модуле 3;
- **ни один ответ банка не напечатан в уроках дословно**: `9`, `six`, `six days`,
  `seventeen`, `forty`, `seventy`, `is`, `How old are you?`, `six, zero, four,
  two`, `nineteen`, `602`, `16`. Числа-слова стоят в таблицах уроков 3 и 4 — это
  парадигма, её работа и есть печатать все формы; врезок с ответами нет ни одной.

---

## 13. Свод, собранный СКРИПТОМ

**Раздел пересобран целиком — это ошибки 7 и 8 разбора, и они об устройстве
проверки, а не о строке.** Прежний свод считался руками, по собственной памяти о
том, что написано двадцатью экранами выше, и врал дважды:

- **доля «произвести ответ» была неверна в четырёх строках из девяти** (уроки 4,
  5, 6, 8): в счёт шли `choice`, `match` и `hottext`, которых мерка кода не
  считает — `lib/content/types.ts:570`, `PRODUCTIVE_KINDS = ["gap", "short",
  "order", "code", "essay", "speak"]`;
- **утверждение «ни один урок не повторяет строй другого» было ложно**: уроки 5
  и 6 совпадали четырьмя местами из пяти, и ближайшей парой свод называл другую.

Правило проекта: **если свод нельзя получить разбором документа, его нельзя и
проверить; свод, составленный вручную, доказывает только усердие составителя.**
Скрипт разбирает таблицы заданий этого файла и файла урока 1, берёт вид из
столбца «Вид» и считает. Его вывод ниже — дословно.

```
СТРОЙ И ДОЛЯ «ПРОИЗВЕСТИ ОТВЕТ» ПО УРОКАМ

урок 1: choice · match · short · gap · short · speak  → произвести 4/6
урок 2: hottext · gap · short · choice · short · speak  → произвести 4/6
урок 3: short · order · gap · choice · speak  → произвести 4/5
урок 4: choice · short · gap · setka · speak  → произвести 3/5
урок 5: short · gap · match · short · speak  → произвести 4/5
урок 6: short · choice · gap · short · speak  → произвести 4/5
урок 7: short · choice · short  → произвести 2/3
урок 8: short · short · short · choice  → произвести 3/4
урок 9: gap · speak · speak  → произвести 3/3

ВИД × УРОК
вид      1 2 3 4 5 6 7 8 9
choice   + + + + — + + + —
gap      + + + + + + — — +
short    + + + + + + + + —
order    — — + — — — — — —
match    + — — — + — — — —
hottext  — + — — — — — — —
setka    — — — + — — — — —
speak    + + + + + + — — +
  order стоит один раз на весь модуль — урок 3
  hottext стоит один раз на весь модуль — урок 2
  setka стоит один раз на весь модуль — урок 4

ПАРЫ УРОКОВ: СОВПАДЕНИЕ СТРОЯ ПО МЕСТАМ
  уроки 3 и 6: 3 мест из 5
  уроки 5 и 6: 3 мест из 5
  уроки 1 и 2: 3 мест из 6
  уроки 7 и 8: 2 мест из 4
  уроки 3 и 4: 2 мест из 5

БАНК РАБОТЫ
  вопросов 15, произвести ответ 10/15
  по видам: short 8, gap 2, hottext 1, choice 4
  итогов покрыто 9 из 9
```

### 13.1. Что из этого следует

- **доля «произвести ответ» больше половины везде**, и самая низкая — у урока 4
  (3/5): там стоит сетка, которая в счёт не идёт. Это и был довод против того,
  чтобы сетку ДОПИСАТЬ, а не заменить ею прежний `hottext`;
- **ни одна пара уроков не совпадает строем больше чем на три места из пяти.**
  Было четыре из пяти у пары 5 и 6; после правки худших пар три, и все по три
  места. Совпадают в них первое, четвёртое и пятое места — `short` в начале и
  `speak` в конце, — и разводить их дальше значило бы ломать дело урока ради
  таблицы;
- **три вида стоят по одному разу на весь модуль** (`order`, `hottext`, `setka`),
  и это ровно те три места, где живут упражнения, о которых просил владелец;
- **банк работы: 10 из 15 требуют произвести ответ, все девять итогов покрыты.**

### 13.2. Сам скрипт

Кладётся рядом с замыслом, чтобы следующий круг пересчитал, а не поверил. Если
таблицы заданий изменятся, свод меняется прогоном, а не правкой руками.

```js
const PRODUCTIVE = ["gap", "short", "order", "code", "essay", "speak"];
const KINDS = ["choice","gap","short","order","match","hottext","setka","speak","essay","code"];
const vid = (cell) => [...cell.matchAll(/`([a-z]+)`/g)].map(m => m[1]).filter(k => KINDS.includes(k))[0] ?? null;
// строки таблиц: у уроков — «| D1 | id | Вид | …», у банка — «| 7 | id | Вид | Итог | …»
// дальше: строй по урокам, доля PRODUCTIVE, вид × урок, совпадение строёв по местам,
// покрытие итогов вопросами банка.
```

Полный текст лежит рядом с замыслом: **`docs/beginner-2/svod-modul-4.mjs`**,
запуск — `node docs/beginner-2/svod-modul-4.mjs`. Он читает этот файл и файл
урока 1 сам, путей в нём не правят.

**Скрипт испытан настоящей ошибкой, а не чистым отчётом.** Прогнал его по ПРЕЖНЕЙ
редакции этого файла (`git show HEAD:docs/beginner-2/plan-modul-4-uroki-2-9.md`) —
и он сам, без подсказки, назвал обе находки разбора:

```
урок 4: choice · short · gap · hottext · speak  → произвести 3/5
урок 6: short · gap · hottext · short · speak  → произвести 4/5
урок 9: choice · speak · speak  → произвести 2/3
  уроки 5 и 6: 4 мест из 5  ← смотреть
  вопросов 0 … итогов покрыто 0 из 9  ← БЕЗ ВОПРОСА: 1, 2, 3, 4, 5, 6, 7, 8, 9
```

Последняя строка — про устройство прежнего файла, и она поучительна сама по себе:
пока вопросы работы стояли врозь по урокам, **свод не мог их даже сосчитать.**
Правило проекта о чистом отчёте работает и наоборот: отчёт, которому нечего
читать, выглядит так же, как отчёт, которому нечего сказать.

### 13.3. Пары, которые проверены особо и остаются близкими

| Пара | Чем различаются |
|---|---|
| D4 (урок 2) и F1 (урок 4) — оба `choice` со звуком | D4 различает окончание (`key`/`keys`), F1 — хвост числа (`thirteen`/`thirty`) |
| E4 (урок 3), F1 (урок 4), L4 (урок 8), вопрос 8 работы — все на паре `-teen`/`-ty` | пары разные у каждого: `fifteen`/`fifty`, `thirteen`/`thirty`, `eighteen`/`eighty`, `seventeen`/`seventy`. **Это самое густое место модуля.** При написании перечитать все четыре условия подряд |
| G1 (урок 5) и D3 (урок 2) — оба «исправь запись» | D3 чинит пропущенное `-s`, G1 — пропущенную связку. **Рамка условия общая, и это законно: сказать иначе нельзя**, а предмет разный |
| H1 (урок 6) и L2 (урок 8) — оба «запиши номер со слуха» | H1 в уроке правил, три цифры без нуля, вне разговора; L2 в уроке слушания, ноль в середине, внутри разговора |
| M1 (урок 9) и вопрос 11 работы — оба «цифры словами» | M1 вписывает их в готовую рамку ответа на просьбу; вопрос 11 переводит цифровую запись в диктовку без всякой рамки |
| K3 (урок 7) и L3 (урок 8) — оба «сколько» | K3 читает, L3 слушает; разные умения и разные виды урока |

**`npm run bliznetsy` ни одну из этих пар не увидит** — кода уроков ещё нет, а у
`gap` и `short` поблажка стоит нарочно: рамку там задаёт сам вид задания. Поэтому
они названы здесь, а не оставлены отчёту.

### 13.4. Правило против правила: прогон девяти уроков друг о друга

Порода найдена 6 сентября: правило, верное в своём уроке, отменяется соседним
уроком или соседним модулем. Прошёл по всем правилам модуля; нашлось три места.

1. **Урок 2, случай 2 против модуля 8.** Формулировка «больше в слове не
   меняется ничего» опровергается своим же курсом: `boxes`, `children`, `mice` —
   модуль 8. Снято, требование к редактору стоит прямо в случае.
2. **Урок 3, случай 7 против урока 6, случая 7.** Урок 3 говорит: цифрой и словом
   — одно и то же (`Number 15.` = `Number fifteen.`). Урок 6 говорит: номер
   записывают цифрами, а читают по одной (`77123` = `seven, seven, one, two,
   three`). Друг друга они не отменяют **только потому, что урок 6 печатает
   границу** — два знака целиком, три и больше по цифрам. Пока границы не было,
   два правила модуля противоречили друг другу на числе `15`. Это второй довод в
   пользу правки случая 2 урока 6, и он сильнее первого.
3. **Урок 4, случай 5 против модуля 3.** Довод «`a hundred` не берём, артикль
   курс не объяснял» ложен: модуль 3 разбирает `a`/`an` и `the` двумя уроками
   раньше. Причина переписана, а `a hundred` нигде не объявляется ошибкой.
   **Следствие для редактора шире одного случая:** ученик модуля 4 уже знает
   артикль, и писать «здесь артикля не нужно, мы его не проходили» нельзя ни в
   одной врезке.

---

## 14. Три упражнения, о которых просил владелец 5 сентября

Владелец просил три вида — отгадать вещь на картинке, собрать слово из
рассыпанных букв, найти знакомые слова в поле случайных букв — и просил их
**именно для новых слов**. В первой редакции этих восьми уроков их было **ноль,
ноль и ноль**; разбор посчитал и назвал (свой раздел «Три вида упражнений»).

| Вид | Где стоит | На чём | Что заменило |
|---|---|---|---|
| буквы вразброс (`order` из одиночных букв) | урок 3, E2 | `eighteen` | прежнюю сборку из кусков `eigh` + `teen` (ошибка 13) |
| сетка букв (`setka`) | урок 4, F4 | `THIRTEEN` и `THIRTY` | прежний `hottext` «отметить десятки в ряду» |
| картинка (`znak` у задания) | урок 6, H4 | `phone`, значок `1f4f1` | прежний `hottext`, проходившийся поиском запятой (вопрос 19) |

**Все три ЗАМЕНИЛИ задания, а не добавились**, и это не экономия. Сетка не входит
в счёт «произвести ответ» (`PRODUCTIVE_KINDS`), и дописать её к пяти заданиям
урока 4 значило бы уронить долю до половины ровно — то есть сломать правило.
Замена держит долю 3/5 (раздел 13).

**Оговорки, без которых эти упражнения вредят, и все три исполнены.**

1. **Чисел до десяти в сетке нет ни одного.** `ONE`, `TWO`, `SIX`, `TEN`, `NINE`
   в три-четыре буквы складываются в случайном поле сами; ученик обводит
   настоящее слово, которого составитель не задумывал, и получает «пока не так» —
   порода «задание наказывает за верный ответ» в чистом виде. Годятся только
   длинные, и взяты `THIRTEEN` (8 букв) и `THIRTY` (6). **Заполнение поля
   сборщик подбирает и прогоняет по словарю модуля**, а не берёт случайным.
2. **Список сетки даёт русские подписи** (`podpis`), а не сами английские слова:
   иначе ученик ищет напечатанную рядом строку, и задание меряет зоркость.
3. **Сетка не ставится в банк вопросов работы** — запрет записан в самом типе
   (`lib/content/types.ts:519–538`).

**Картинка стоит в ЗАДАНИИ, а не в вариантах, и это выбор, а не небрежность.**
Вариантами со значками задание было бы узнаванием; `short` со значком заставляет
слово произвести — «отгадать вещь на картинке» в прямом смысле просьбы.

**И честная граница: больше трёх мест в этом модуле для них нет.** Картинка
называет вещь, а тринадцать новых карточек модуля — это единицы времени
(`minute`, `hour`, `day`, `week`, `second`, `moment`), меры (`metre`,
`kilometre`), возраст (`year`, `age`, `old`) и номер (`zero`, `phone`). Картинкой
из них точно называется одна — `phone`. У Twemoji на минуту, час, секунду и
неделю идут одни и те же часы с календарём, и значок сказал бы неправду; правило
проекта — **значок отвечает «какая именно вещь» точнее скобки в переводе**, и
ставить его там, где он не различает, значит обменять точность на украшение.

**Сетка годится уроку 3 или 4 и не годится урокам 5 и 6** — вывод разбора,
принятый целиком: в уроках 3 и 4 предмет и есть НАПИСАНИЕ (хвост `-teen` против
`-ty`, выпавшие буквы), а в уроках 5 и 6 предмет — формула возраста и диктовка
цифрами, и написание слова там ни при чём.

---

## 15. Что проверено и чего проверить не удалось

**Проверено по файлам и прогоном (второй круг, 6 сентября):**

- **словарь курса на сегодня — по КОДУ, а не по замыслам.** Восемь модулей,
  `grep 'term:'` по всем файлам; порядок модулей — `index.ts`. Каждое английское
  слово каждой строки этих восьми уроков сверено с ним. **Курс вырос между
  кругами:** модуль 3 держит теперь пятнадцать карточек (`from`, `in`, `here`,
  `home`, `city`, `town`, `country`, `airport`, `capital`, `street`, `where`,
  `park`, `road`, `zoo`, `village`), а не одну, и девять уроков вместо одного;
- **`what` в видимом тексте курса не встречается ни разу** — поиск по всем
  файлам курса; вне модуля 7 попадаются только комментарии и поля `sources`;
- **модуль 3 разбирает артикль двумя уроками** — «Один из таких: a и an»
  (`otkuda-ty.ts:1404`) и «Тот самый: the» (1786), оба раньше модуля 4;
- **модуль 2 отказался от `What's your name?` и назвал замену** —
  `privetstvie.ts:2550–2560`;
- **CEFR Companion, с. 79** — «Can ask very simple questions for information,
  such as “What is this?”»: поиск по всему файлу дал одно совпадение, страница
  79, та же графа Pre-A1, что держит три дескриптора модуля;
- **`lib/content/check.ts`, ветка `order`** — сверка идёт по СОБРАННОЙ строке, а
  не по номерам: повторяющиеся буквы в `eighteen` задание не ломают. Прочитано в
  самом файле, а не выведено по похожему месту;
- **`lib/content/types.ts:570`** — `PRODUCTIVE_KINDS`; **519–538** — запрет сетки
  в банке; **644–653** — `VozvratRef` показывает чужое задание и ничего к нему не
  добавляет;
- **`scripts/check-content.mts:1939–1941`** — каждый итог обязан быть покрыт
  вопросом банка; **1961–1968** — доля «произвести ответ» в банке;
- **транскрипции карточек** — `materials/ipa-en-uk.txt` построчно:
  `age`, `books`, `card(s)`, `day`, `hour`, `key(s)`, `kilometre` (31410,
  `/kˈɪləmˌiːtɐ/` — со знаком второстепенного ударения), `metre`, `minutes`,
  `moment`, `night(s)` (37823), `old`, `pen(s)`, `phone`, `ticket(s)`, `week`,
  `year`, `zero`;
- **`minute` и `second` в `ipa-en-uk.txt` отсутствуют** (есть только `minutes`) —
  это сведение 25 разбора, и ссылка дописана: обе записи подтверждает
  `materials/wikipron-en-uk.tsv`, `minute` → `m ɪ n ɪ t`; `second` там стоит
  **тремя вариантами** — `s ə k ɒ n d`, `s ɛ k n d`, `s ɛ k ə n d`, — и берётся
  третий, `ɛ` → `e` по условности курса: `/ˈsekənd/`;
- **`public/twemoji`** — прогоном: 154 файла, `1f4f1` среди них НЕТ, значок
  заберёт `npm run znachki` при сборке;
- **Википедия, English plurals** — читал через программный интерфейс: «the
  plural is **normally** formed by adding [s]». Слово «normally», а не «always»,
  и потому наблюдение остаётся наблюдением.

**Чего проверить не удалось:**

1. **Ударение в числительных на `-teen`** — источника нет ни одного:
   `ipa-en-uk.txt` собран машиной, `wikipron-en-uk.tsv` ударений не несёт,
   руководства молчат. Уроки 3, 4 и 8 строят пару на ХВОСТЕ и долготе гласного, а
   не на ударении. Если владелец решит иначе, меняются три правила.
2. **Два чтения `kilometre`** — раздел 17, владельцу.
3. **Как читают номер телефона по-британски** — раздел 17, владельцу.
4. **Отвлекающие уроков 2–9 глазами ученика я не проходил.** Каждый проверен
   двумя вопросами — «представь ученика, который так ошибается» и «а говорят ли
   так вообще», — и выдуманных среди них нет; но это проверка замысла, а не
   прохождение урока. Владелец нашёл три такие беды, пройдя один урок.
5. **`npm run bliznetsy` по модулю прогнать нечем** — кода уроков 2–9 нет.
   Близнецы названы чтением; у `gap` и `short` отчёт их и не увидел бы.
6. **Словарь курса меняется под руками.** Между первым и вторым кругом модуль 3
   вырос с одной карточки до пятнадцати, и вывод первой редакции «слова `here` в
   курсе нет» устарел за день. **Перед сборкой КАЖДОГО урока словарь сверяется
   заново по коду**, а не по этому файлу: он верен на 6 сентября и не позже.
7. **Замыслов модулей 12, 18, 21 и 27 не читал** — их нет; границы с ними сведены
   с программой. Замыслы и код модулей 3, 5, 6, 7 и 8 читал.

---

## 16. Где я не согласился с разбором

Второй методист докладывает, решает методист, — но расхождение называется вслух и
подпирается. Из шестнадцати ошибок, шести вопросов и семи сведений внесены все;
в пяти местах внесены не так, как предлагал разбор, и один раз я нашёл сверх
него.

**1. Ошибка 2 (`What is …?`): находка принята, выход другой.** Разбор предлагал
пятую строку в шапке модуля с адресом. Я оборот СНЯЛ и заменил на
`Your phone number, please?`. Довод разбора о ступени верен, я его проверил сам
(CEFR, с. 79) — но ступень тут и не была вопросом. Объявление в шапке объясняет
заимствование и не даёт ученику слова `what`, которого в видимом тексте курса нет
ни разу до модуля 7; а модуль 2 уже отказался от `What's your name?` теми же
словами и назвал ту же замену. Разбор до этого места не дошёл: он смотрел, где
слово ВВОДИТСЯ, и не смотрел, где курс уже РЕШИЛ. Замена ничего не стоила и
исполняет дескриптор целиком.

**2. Ошибка 1 (итог урока 2): взял более узкий из двух выходов.** Разбор
предлагал заменить вторую половину на «отвечать числом на вопрос о количестве». Я
снял её целиком: итог обязан спрашиваться работой, а вопрос об этом был бы
близнецом задания D5 по действию — сам разбор в ошибке 6 требует менять действие,
а не слово. Обещание сузилось до того, что урок и производит, и проверяет.

**3. Ошибка 12 (D4 и случай 6): согласен с находкой, не согласен с приговором
случаю.** Разбор писал, что случай даёт наблюдение без правила и «задания,
проверяющего этот случай, построить нельзя в принципе». Построить нельзя задание
на то, ЧЕМ различаются `/s/` и `/z/`, — и такого задания больше нет. Но на
ступени Pre A1 проверяемо другое и полезное: **что окончание вообще слышно**.
Случай нацелен туда, пара `key` — `keys` различается только концом, наблюдение о
`/s/` и `/z/` осталось врезкой без задания, и у него дописана опора со словом
«normally».

**4. Сетка: место принято, арифметика — нет.** Разбор считал, что добавление
сетки уронит долю урока 4 до 3/6, и требовал заодно менять узнающее задание на
производящее. Я сетку не добавил, а заменил ею `hottext` — доля осталась 3/5, и
второй правки не понадобилось. Пара взята `THIRTEEN`/`THIRTY`, а не предложенная
`SEVENTEEN`/`SEVENTY`: `seventy` — ответ вопроса 8 работы, и печатать его в сетке
значило бы напечатать ответ.

**5. Сведение 23 (суффикс `-2` в именах уроков): условность не соблюдаю, и
комментарий в коде надо снять, а не исполнять.** Имена уникальны по всем 36
`slug` курса (разбор проверил сам), ученику `slug` не виден, а условность уже
нарушена не мной: `ya-iz-almaty`, `gde-ty-seychas`, `a-i-an`, `tot-samyy-the`,
`mama-i-papa`, `prosto-yabloki`, `blank-imya-i-familiya` её не несут, и держится
она у одного `otkuda-ty-2`. Дописывать `-2` восьми новым именам ради устаревшего
комментария — шум. **Сборщику: снять утверждение из `chisla-i-telefon.ts:406–411`,
что суффикс несут все уроки нового курса.** Файл не мой, правка не моя.

**6. Сведение 24 (`for`): согласен, что это ставка, и закрываю её снятием слова,
а не объявлением.** Пятая строка в шапке стоила бы дешевле, но предлог всё равно
не был бы ученику ДАН, а записка без него не теряет ничего.

**И то, чего в разборе нет, — четыре находки второго круга.**

- **Близнец ВНУТРИ банка работы:** `chisla-4-slovo-desyatka` («Запиши словом: 40»)
  и `chisla-4-cifra-v-slovo` («Запиши словом: 17») — та же рамка, то же действие,
  заменено число. Разбор ловил близнецов между уроком и работой и этой пары не
  видел, потому что вопросы стояли в разных разделах файла. Отсюда и раздел 12:
  банк собран в одном месте.
- **Правило урока 2 опровергается модулем 8 своего же курса** («больше в слове не
  меняется ничего» против `boxes` и `children`).
- **Довод против `a hundred` («артикль курс не объяснял») ложен:** модуль 3
  разбирает артикль двумя уроками раньше.
- **Правила уроков 3 и 6 противоречили друг другу на числе `15`**, пока урок 6 не
  напечатал границу «два знака целиком, три и больше по цифрам».

---

## 17. Что оставлено владельцу

Два места, и оба — решения преподавателя, а не выбор из источников: источники
молчат либо дают одно там, где жизнь даёт два.

1. **Два чтения `kilometre`.** Наши источники различают одно:
   `materials/ipa-en-uk.txt`, строка 31410 — `/kˈɪləmˌiːtɐ/`, то есть с ударением
   на первом слоге. Второе чтение, `/kɪˈlɒmɪtə/`, в наших файлах не стоит вовсе.
   Модуль пишет `/ˈkɪləˌmiːtə/` (`ɐ` → `ə` по условности курса, знак
   второстепенного ударения сохранён по решению владельца от 6 сентября
   «показывать, не объяснять»). **Если владелец как преподаватель выберет второе
   чтение или захочет дать оба — цена одна строка** (карточка урока 4) и одна
   переозвучка.
2. **Как читают номер телефона по-британски.** Ни `double seven` вместо `seven,
   seven`, ни `oh` вместо `zero` источники не подтверждают: искал по обоим
   руководствам Cambridge словами `phone number`, `telephone`, `digit` — молчат;
   второй методист повторил тот же поиск с тем же итогом. Модуль держится решения
   владельца от 2 сентября 2026 (короткий номер целиком, длинный по цифрам) и
   ничего сверх него не пишет. **Если владелец захочет дать `double seven` или
   `oh`, это отдельный случай урока 6 и отдельная запись звука.**

**И одно место, которое владельцу уже адресовано и решения ждёт с первого
круга** (стоит в шапке кода, `chisla-i-telefon.ts`): **слово `key`.** Oxford даёт
`key n., adj. A1`, а Young Learners, с. 81, список «The home», ставит его в
колонку **A2 Flyers** — на той самой странице, откуда модуль берёт ступень чисел.
Карточка стоит в уроке 1, и уроки 2, 4 и 7 на ней работают. Замена готова и
ничего не стоит: считать можно `book`, `card`, `letter`, `pen`, `ticket` — все
карточки модуля 1.
