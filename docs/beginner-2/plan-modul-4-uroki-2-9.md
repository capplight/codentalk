# Модуль 4, уроки 2–9. Подробный замысел

6 сентября 2026. Пишет методист. Весь видимый русский текст — вступления,
объяснения, врезки, подписи, условия и разборы заданий — пишет **редактор**.

Замысел модуля целиком — [plan-modul-4.md](plan-modul-4.md); урок 1 —
[plan-modul-4-urok-1.md](plan-modul-4-urok-1.md), он прошёл круг и служит
образцом строения. Здесь расписаны остальные восемь.

---

## 0. Правило, ради которого этот файл устроен так, а не как серия

**Находка разбора модуля 2, и она стоила дорого:** там семь замыслов писали
серией, и **пять уроков подряд пошли одним строем заданий**, а четыре пары
заданий вышли близнецами — у двух условие совпало дословно. Круг после первого
урока такого не ловит: одинаковость видна только на серии.

**Отсюда два правила этого файла.**

1. **Строй заданий разводится по делу урока**, а не берётся от урока 1. Таблица
   строёв — раздел 1, и она составлена ДО того, как расписан хоть один урок:
   иначе образец воспроизводится сам.
2. **Ни одно задание не повторяет вид и предмет другого сразу.** Сверка —
   раздел 12: таблица видов по урокам, разбор ближайших пар, прогон по ответам.

**И третье, из того же разбора:** где предмет урока живёт на слух — задание на
слух; где в письме — письмо. Урок 6 диктует номер, значит его задания слушают и
записывают; урок 5 говорит о себе, значит его задания производят строку.

---

## 1. Строй заданий: восемь разных, составлено заранее

| Урок | Дело урока | Строй заданий по порядку | Чего в строе НЕТ нарочно |
|---|---|---|---|
| 1 | числа 1–10 | `choice`(звук) · `match` · `short`(звук) · `gap` · `short` · `speak` | сборки, исправления чужой записи |
| 2 | `-s` и `How many` | `hottext` · `gap` · `short`(исправить чужую запись) · `choice`(звук) · `short`(ответ числом) · `speak` | звука в первых заданиях: предмет урока письменный |
| 3 | 11–20, хвост `-teen` | `short`(исправить) · `order` · `gap` · `choice`(звук) · `speak` | `match`: он был в уроке 1 на тех же числах |
| 4 | десятки, пара `-teen`/`-ty` | `choice`(звук) · `short`(звук, цифрой) · `gap`(дефис) · `hottext` · `speak` | сборки слова: она была в уроке 3 |
| 5 | возраст | `short`(исправить) · `gap` · `match` · `short`(о себе, с `accept`) · `speak` | звука вовсе: возраст ученик говорит, а не слышит |
| 6 | номер по цифрам | `short`(звук) · `gap` · `hottext` · `short` · `speak` | `choice`: узнавание тут ничего не меряет |
| 7 | чтение | `short`(`about`) · `choice`(`about`) · `short`(`about`) | звука: это урок чтения |
| 8 | слушание | `short`(звук) ×3 · `choice`(звук) | `speak`: он в уроке 9 |
| 9 | говорение | `choice`(звук) · `speak` ×2 | письменных заданий |

**Как читать таблицу.** Совпадение ВИДА между уроками законно и неизбежно: видов
девять, уроков девять. Запрещено совпадение **вида и предмета сразу** — и
столбец «чего нет нарочно» назван для того, чтобы при написании соблазн вернуть
привычное был виден.

**Долю «произвести ответ» держим больше половины в каждом уроке.** Счётчик
`check:content` смотрит только банк работы, поэтому внутри уроков её держит
методист; посчитана она в разделе 12.4.

---

## 2. Урок 2 «Два предмета: буква -s»

```
slug: "dva-predmeta"
title: "Два предмета: буква -s"
vid: "pravila"
estimatedMinutes: 14
outcome: "прибавлять -s к предмету, когда их больше одного, и спрашивать о количестве через How many"
vozvrat: задание модуля 1 на запись слова со слуха (h2-zapisat-slovo, слово night)
```

**Это главный урок модуля: в нём стоит его грамматика.** `NOUNS / plural`,
FORM: PLURAL `-S`, `1741163711821x759916598484829700`, A1 — «Can form plurals by
adding -s to common countable nouns», второй пример графы «You have to walk just
**5 minutes** down the road». И `DETERMINERS / quantity`, FORM: WITH PLURAL
NOUNS, `1741163709012x117638123076284200`, A1, пример «My house has got **three
rooms**».

**`vozvrat` стоит здесь, и это единственный возврат модуля** (программа требует
по одному со второго модуля). Ученик снова записывает слово со слуха — `night` из
модуля 1, — а потом ставит к нему число: `two nights`. Возврат выбран не для
галочки: урок 2 стоит на существительных модуля 1, и другого источника предметов
для счёта у модуля нет. Точный `id` называет сборщик по нынешнему коду.

### 2.1. Вступление

Русской фразой. Требования: по-русски «книга» и «книги» — слово меняется на
конце; по-английски меняется тоже, и почти у всех слов одинаково. Что ученик
научится делать: ставить `-s` и спрашивать, сколько чего.

**Чего во вступлении быть не должно:** определения слов «множественное число»
(ученик знает их по русскому языку — решение владельца от 4 сентября); обещания
`-es` и особых форм — их в уроке нет.

### 2.2. Сцена (экран 2)

`id: "scena-razdayut-knigi"`, `example`, `razgovor: true`, два голоса. Случай:
перед занятием раздают книги.

```
— One book, two books, three books.   — Одна книга, две книги, три книги.
— Sue?                                — Сью?
— Yes.                                — Да.
— Four books. Good.                   — Четыре книги. Хорошо.
```

Голоса: ж — м — ж — м.

**Сцена собиралась трижды, и это стоит записать, потому что ловушка общая.**
Первые две редакции держали `Four books, three students` и `Sue is here` — а
слов `student` и `here` в курсе НЕТ: `student` жил в выведенном модуле
`kto-eto.ts`, `here` не вводился вовсе. **Проверять надо не «звучит ли
естественно», а по словарю курса, слово за словом.** Итоговая сцена стоит на
`book`, `yes`, `good` — все три карточки модуля 1 — и на числах урока 1. Ни
одного нового слова.

`explain` под сценой: книги считают по одной, и у слова `book` в счёте
появляется `-s`.

### 2.3. Восемь случаев

**Случай 1. Один предмет — слово как в словаре.** По-русски «одна книга»,
по-английски `one book`: слово не меняется.
`one book` — одна книга · `one key` — один ключ. `zvuchat` обе.

**Случай 2. Два и больше — в конце `-s`.** От двух и дальше к слову прибавляется
`-s`, и больше в слове не меняется ничего.
`two books` — две книги · `ten keys` — десять ключей. `zvuchat` обе.

**Случай 3. Число впереди, предмет за ним.** Расхождения с русским тут нет, и
случай короткий: он нужен, чтобы ученик не начал писать `books two`.
`three tickets` — три билета · `five pens` — пять ручек.

**Случай 4. Само число не меняется.** По-русски «два ключа», но «двух ключей» —
слово «два» меняется; по-английски `two` одно на всё.
`two keys` — два ключа · `two books` — две книги.
**Расхождение с русским полное**, и это тот вид случая, который метод считает
главным. Прямое продолжение случая 7 урока 1 (`one` не меняется по роду); ссылка
на свой модуль внутри ступени законна и приветствуется.

**Случай 5. `-s` прибавляется и к единицам измерения.**
`five minutes` — пять минут · `two hours` — два часа.
Второй пример графы источника — «You have to walk just **5 minutes** down the
road» — ровно этот случай.

**Случай 6. Пишется `-s` одинаково, а слышится по-разному.** В `books` конец
звучит `/s/`, в `keys` — `/z/`; буква в обоих одна.
`books` `/bʊks/` — книги · `keys` `/kiːz/` — ключи.
`zvuk` с ключами `/bʊks/` и `/kiːz/` — обе транскрипции печатаются в правиле
дословно, иначе кнопки не будет.
**Опора — наши записи**, `materials/ipa-en-uk.txt`; десять форм выписаны в
`sources`. **Никаких «всегда после глухого»:** это наблюдение по десяти записям,
а не закон из источника.

**Случай 7. `How many` спрашивает о количестве.** Вопрос начинается с `How
many`, дальше стоит предмет с `-s`.
`How many books are there?` — Сколько книг? · `How many keys are there?` —
Сколько ключей? `zvuchat` обе целиком.
**Расхождение источников объявлено в шапке модуля** (раздел 0.6 замысла модуля):
English Grammar Profile ставит вопросительное `many` на B1, Cambridge — на
Pre A1 Starters (Young Learners, с. 23, с. 27, с. 9). Взято по Cambridge,
решение владельца от 6 сентября.
**`there are` взято ГОТОВЫМ**, вторая строка шапки, адрес — модуль 12. В правиле
про него ровно одна строка: эти два слова стоят в вопросе всегда, а разбирает их
модуль «Мой дом и комната». **Разбирать здесь нельзя** — это чужой урок.

**Случай 8. На вопрос отвечают числом.** В ответе довольно одного числа.
`How many books are there? — Four.` — Сколько книг? — Четыре.
`How many keys are there? — Two.` — Сколько ключей? — Два.
Опора — Young Learners, **с. 23**, Pre A1 Starters, **Reading and Writing
Part 5** (не Listening — ошибка первой редакции замысла урока 1): «How many
children are there? … **two**»; и A2 Key, **с. 29**: «only ONE word, or a number,
or a date, or a time».

**Восемь случаев, шестнадцать примеров, звук назван у каждого.**

### 2.4. Словарь урока: шесть карточек

| Слово | Перевод | Пример | Чтение |
|---|---|---|---|
| `minute` | минута | `five minutes` | `/ˈmɪnɪt/` |
| `hour` | час | `two hours` | `/ˈaʊə/` |
| `day` | день | `seven days` | `/deɪ/` |
| `week` | неделя | `two weeks` | `/wiːk/` |
| `second` | секунда | `ten seconds` | `/ˈsekənd/` |
| `moment` | минутка | `one moment` | `/ˈməʊmənt/` |

Все шесть — Oxford **A1**, все из темы `Measurements`. **`second` даёт только
«секунда»:** порядковых в модуле нет, и «второй» увело бы в модуль 18.
Карточка `key` стоит в уроке 1 — здесь она работает, но не вводится заново.

### 2.5. Шесть заданий

| № | id | Вид | Что делает ученик | Случай |
|---|---|---|---|---|
| D1 | `d1-otmetit-mnogo` | `hottext` | в ряду `book · books · key · keys` отмечает те, где предметов больше одного | 2 |
| D2 | `d2-dopisat-s` | `gap` | `two book_` → дописывает `s` | 2 |
| D3 | `d3-ispravit-zapis` | `short` | «Ученик записал: `three ticket`. Запиши строку без ошибки.» → `three tickets` | 2, 3 |
| D4 | `d4-uslyshat-konec` | `choice` + `zvuk: "keys"` | слышит слово и выбирает: `books` или `keys` | 6 |
| D5 | `d5-otvetit-chislom` | `short` | «`How many books are there?` Книг четыре. Ответь одним словом.» → `Four` | 7, 8 |
| D6 | `d6-skazat-vsluh` | `speak` | произносит `two books` и `five minutes` | 2, 5 |

Произвести ответ — четыре из шести (D2, D3, D5, D6).

**D3 — исправление чужой записи, и вида этого в уроке 1 нет.** `exact` не
ставится: ошибка не в регистре.

**D5 требует ОТВЕТА, а не построения вопроса** — третье условие сводящего.
Условие прямо говорит, сколько книг, чтобы ответ был один: без этого у задания
верных ответов бесконечно много.

**Чего в заданиях быть не должно:** слова `box` во множественном (`boxes` — не
наш случай); сборки вопроса из слов; `match` — он был в уроке 1 и на числах.

### 2.6. Вопросы работы модуля на итог урока 2

| id | Вид | Что | Почему не близнец |
|---|---|---|---|
| `chisla-4-ubrat-lishnee` | `short` | «Ученик записал: `one books`. Запиши строку без ошибки.» → `one book` | урок исправлял ПРОПУЩЕННОЕ `-s`, работа — ЛИШНЕЕ; ошибка обратная |
| `chisla-4-skolko-minut` | `gap` | `ten minute_` → `s` | предмет другой: единица измерения, а не вещь |

---

## 3. Урок 3 «От одиннадцати до двадцати: хвост -teen»

```
slug: "ot-11-do-20"
title: "От одиннадцати до двадцати: хвост -teen"
vid: "pravila"
estimatedMinutes: 13
outcome: "называть и записывать числа от одиннадцати до двадцати"
```

**Ступень:** Young Learners, **с. 81**, строка `Numbers`, колонка **Pre A1
Starters** — `1–20`.

### 3.1. Вступление и сцена

Вступление русской фразой: по-русски после десяти счёт идёт «одиннадцать,
двенадцать, тринадцать» — и там слышен один и тот же хвост «-надцать»;
по-английски хвост свой, `-teen`, и он делает семь чисел из семи уже знакомых.

**Сцена — очередь к окошку**, называют номера.

```
— Eleven.         — Одиннадцать.
— Twelve.         — Двенадцать.
— Thirteen.       — Тринадцать.
— Thirteen? Yes.  — Тринадцать? Да.
```

Голоса ж — м — ж — м. Ни одного нового слова, кроме самих чисел.

### 3.2. Восемь случаев

1. **`eleven` и `twelve` не похожи ни на что** — их учат как есть.
   `Eleven.` — Одиннадцать. · `Twelve.` — Двенадцать. Таблица 11–12 со звуком,
   `naTomZheEkrane`.
2. **С тринадцати работает хвост `-teen`.**
   `Sixteen.` — Шестнадцать. · `Seventeen.` — Семнадцать. Таблица 13–19 на том
   же экране, столбцы «Цифрой · Словом · Как звучит · Перевод», `zvuchat` у
   каждой строки.
3. **Две особые части: `thir-` и `fif-`.** В `thirteen` и `fifteen` начало
   другое, чем в `three` и `five`.
   `Thirteen.` — Тринадцать. · `Fifteen.` — Пятнадцать.
   **Особых частей ДВЕ, а не три** — правка по разбору второго методиста:
   `eighteen` устроено иначе, см. случай 4.
4. **В `eighteen` выпадает одна буква.** `eight` и `teen` вместе дали бы
   `eightteen`, а пишется `eighteen` — одна `t` вместо двух.
   `Eighteen.` — Восемнадцать. · `Eight, eighteen.` — Восемь, восемнадцать.
5. **`twenty` закрывает счёт до двадцати** и хвоста `-teen` не имеет.
   `Twenty.` — Двадцать. · `Nineteen, twenty.` — Девятнадцать, двадцать.
6. **Хвост `-teen` звучит долго:** `/tiːn/`, гласный тянется.
   `Thirteen.` · `Nineteen.` — `zvuk` с ключами `/θɜːˈtiːn/` и `/naɪnˈtiːn/`,
   напечатанными в правиле дословно.
   **Ударение в правиле НЕ называется** — источника у него нет (раздел 13), и
   правило говорит только о долготе гласного.
7. **Число цифрой и словом — то же, что в уроке 1**, но на двузначном.
   `Number fifteen.` — Номер пятнадцать. · `Number 15.` — Номер пятнадцать.
   `zvuk` ставит второй строке то же произнесение, что первой: правило говорит,
   что вслух они одинаковы, значит и звучать должны одинаково.
8. **Числа с хвостом `-teen` не меняются перед предметом.**
   `thirteen books` — тринадцать книг · `fifteen minutes` — пятнадцать минут.
   Работает `-s` из урока 2.

### 3.3. Пять заданий

| № | id | Вид | Что делает ученик |
|---|---|---|---|
| E1 | `e1-ispravit-threeteen` | `short` | «Ученик записал: `threeteen`. Запиши число без ошибки.» → `thirteen` |
| E2 | `e2-sobrat-chislo` | `order` | собирает `eighteen` из частей `teen` · `eigh` |
| E3 | `e3-dopisat-hvost` | `gap` | `six___` → `teen` |
| E4 | `e4-uslyshat` | `choice` + `zvuk: "fifteen"` | `fifteen` · `fifty` · `five` |
| E5 | `e5-skazat-vsluh` | `speak` | считает вслух от одиннадцати до двадцати |

**E1 — настоящая ошибка**, а не выдумка: `threeteen` и `fiveteen` ученики пишут,
и замороженный курс её ловит (запись в `courses/resheno.ts`).

**E4 берёт `fifty` отвлекающим нарочно:** десятки приходят уроком 4, и слово
здесь не требуется к производству — только к отбрасыванию по звуку. Это не
заимствование: ученик его не пишет и не говорит.

**`order` есть только в этом уроке** — в остальных семи его нет ни разу.

### 3.4. Вопросы работы модуля

| id | Вид | Что | Почему не близнец |
|---|---|---|---|
| `chisla-4-cifra-v-slovo` | `short` | «Запиши словом: 17» → `seventeen` | урок исправлял чужую запись, работа переводит цифру в слово |
| `chisla-4-vernoe-napisanie` | `choice` | `fiveteen` · `fifteen` · `fifthteen` | вида «выбрать верное написание» в уроке нет: там исправляли записью |

---

## 4. Урок 4 «Десятки и сотня: хвост -ty»

```
slug: "desyatki-i-sotnya"
title: "Десятки и сотня: хвост -ty"
vid: "pravila"
estimatedMinutes: 14
outcome: "называть числа от двадцати до ста и не путать -teen с -ty"
```

**Ступень:** Young Learners, **с. 81**, колонка **A1 Movers** — `21–100`,
`hundred`. И **с. 36**, совет к A1 Movers: «make sure children are confident with
numbers **up to 100** in English». Потолок назван источником дважды.

### 4.1. Восемь случаев

1. **Десятки кончаются на `-ty`** — таблица 20–90, `zvuchat` у каждой строки.
   `Twenty, thirty.` — Двадцать, тридцать. · `Eighty, ninety.` — Восемьдесят,
   девяносто.
2. **Те же две особые части: `thir-` и `fif-`.**
   `Thirty.` — Тридцать. · `Fifty.` — Пятьдесят.
3. **`forty` пишется без `u`**, хотя `four` — с `u`. И `eighty` теряет `t`, как
   `eighteen` в уроке 3.
   `Forty.` — Сорок. · `Eighty.` — Восемьдесят.
4. **Составное число пишется через дефис.**
   `twenty-one` — двадцать один · `thirty-five` — тридцать пять.
   **Осторожно со звуком:** дефис синтез читает слитно (решение владельца от
   5 сентября), и здесь это верно — `twenty-one` и есть одно слово. Диктовка по
   цифрам через дефис не пишется нигде: там запятая.
5. **Сотня — `one hundred`.** `One hundred.` — Сто. · `Ninety, one hundred.` —
   Девяносто, сто.
   **`a hundred` не берём:** артикль курс не объяснял.
6. **Пара `thirteen` — `thirty`: разный хвост.**
   `Thirteen.` `/θɜːˈtiːn/` · `Thirty.` `/ˈθɜːti/`. Правило говорит про хвост и
   про долготу гласного: в `-teen` гласный тянется, в `-ty` короткий. **Про
   ударение не говорит ничего** — источника нет.
7. **Пара работает на всех десятках.**
   `fourteen` — `forty` · `fifteen` — `fifty`.
8. **Десятки считают предметы так же.**
   `twenty books` — двадцать книг · `fifty metres` — пятьдесят метров.

### 4.2. Словарь: две карточки

`metre` `/ˈmiːtə/` (`fifty metres`), `kilometre` `/ˈkɪləmiːtə/`
(`twenty kilometres`). Оба Oxford A1, оба из темы `Measurements`, оба работают
только с десятками — на числах до десяти им негде.

**Про `kilometre` оговорка:** двух чтений наши источники не различают, решает
владелец (раздел 13).

### 4.3. Пять заданий

| № | id | Вид | Что делает ученик |
|---|---|---|---|
| F1 | `f1-teen-ili-ty` | `choice` + `zvuk: "thirty"` | `thirteen` · `thirty` |
| F2 | `f2-zapisat-cifroy` | `short` + `zvuk: "sixty"` | записывает услышанное цифрой → `60`, `accept: ["sixty"]` |
| F3 | `f3-defis` | `gap` | `twenty_one` → `-` |
| F4 | `f4-otmetit-desyatki` | `hottext` | в ряду `fifteen · fifty · seventeen · seventy` отмечает десятки |
| F5 | `f5-skazat-vsluh` | `speak` | называет `twenty-one`, `forty`, `one hundred` |

**F2 — единственное место модуля, где условие сужает форму ответа**, и причина
названа прямо в условии: двузначное число со слуха проще записать цифрой.
`accept` при этом держит и слово — иначе верный ответ наказывается.

**F3 — задание на дефис, и вида «дописать знак» в модуле больше нет нигде.**

### 4.4. Вопросы работы модуля

| id | Вид | Что | Почему не близнец |
|---|---|---|---|
| `chisla-4-slovo-desyatka` | `short` | «Запиши словом: 40» → `forty` | ловит `fourty`; урок писал цифрой, работа — словом |
| `chisla-4-para-na-sluh` | `choice` + `zvuk: "fifteen"` | `fifteen` · `fifty` | пара другая, чем в уроке (там `thirteen`/`thirty`) |

---

## 5. Урок 5 «Сколько тебе лет»

```
slug: "skolko-let"
title: "Сколько тебе лет"
vid: "pravila"
estimatedMinutes: 13
outcome: "называть свой возраст и спрашивать о возрасте"
```

**Опора:** CEFR Companion, **с. 79**, Information exchange, **Pre-A1** — «Can
tell people their age and ask people about their age». Оборот `How old are you?`
— Young Learners, **с. 28** (Speaking Part 4) и **с. 16** (Listening Part 2:
«How old is the new girl? … 8»), обе Pre A1 Starters. Расхождение с English
Grammar Profile объявлено в шапке модуля (раздел 1.5 замысла модуля).

**`egpPovtor`:** `1741163715288x539616242661052000`, `VERBS / types`, FORM:
LINKING VERB, BE — первый пример графы дословно «**I am 22 years old.**»

### 5.1. Сцена

```
— How old are you?   — Сколько тебе лет?
— I am twenty.       — Мне двадцать.
— Twenty? Good.      — Двадцать? Хорошо.
```

Голоса ж — м — ж. Все слова свои: `good` — карточка модуля 1, `twenty` — урок 4,
`How old are you?` — формула этого урока.

**Строка `Me too` из первой редакции снята.** Она требовала объектного `me` —
пункт чужой (модуль 6), и заимствование ради одной реплики того не стоит.
`Good` уже работает в сценах уроков 1 и 2, и повтор здесь законен: это отклик, а
не предмет разбора.

### 5.2. Восемь случаев

1. **Возраст говорят через `am`.** По-русски «мне двадцать лет» — без глагола
   вовсе; по-английски глагол обязателен.
   `I am twenty years old.` — Мне двадцать лет. · `I am ten years old.` — Мне
   десять лет.
   **Это расхождение с русским и главный случай урока.**
2. **`years` — то же `-s`, что в уроке 2.**
   `twenty years` — двадцать лет · `one year` — один год.
3. **Короткий ответ — без `years old`.**
   `I am twenty.` — Мне двадцать. · `I am ten.` — Мне десять.
4. **Вопрос — целой формулой.** Три слова стоят вместе, разбирать их не нужно.
   `How old are you?` — Сколько тебе лет? · `How old is she?` — Сколько ей лет?
5. **О другом человеке — `is`.**
   `She is ten.` — Ей десять. · `He is thirty.` — Ему тридцать.
6. **`old` в этом обороте не значит «старый».** Карточка даёт «старый», и без
   этой строки ученик прочтёт вопрос как «насколько ты стар». Ловушка наша
   собственная: слово он получил в этом же уроке.
   `How old are you? — I am ten.` — Сколько тебе лет? — Мне десять. ·
   `He is thirty.` — Ему тридцать.
7. **Возраст пишут цифрой в бланке.** `Age: 20` — Возраст: 20.
   Долг с адресом: бланк разбирает модуль 5 «Бланк: имя, страна, адрес».
8. **`year` без `-s`, когда год один.**
   `one year` — один год · `twenty years` — двадцать лет.

### 5.3. Словарь: три карточки

`year` `/jɪə/`, `age` `/eɪdʒ/`, `old` `/əʊld/`. Все Oxford A1.

### 5.4. Пять заданий

| № | id | Вид | Что делает ученик |
|---|---|---|---|
| G1 | `g1-ispravit-vozrast` | `short` | «Ученик написал: `I twenty years old`. Запиши строку без ошибки.» → `I am twenty years old` |
| G2 | `g2-dopisat-old` | `gap` | `I am ten years ___` → `old` |
| G3 | `g3-vopros-i-otvet` | `match` | `How old are you?` ↔ `I am twenty.` · `How old is she?` ↔ `She is ten.` |
| G4 | `g4-o-sebe` | `short` | «Напиши, сколько тебе лет, целой строкой.» → `I am twenty years old`, `accept` — та же рамка со всеми числами модуля |
| G5 | `g5-skazat-vsluh` | `speak` | отвечает вслух на `How old are you?` |

**G1 — настоящая ошибка**, пропуск связки: её делают те, чей родной язык
обходится без глагола.

**Чего в отвлекающих быть НЕ должно, и оба случая проверены:**
`I have twenty years` — калька романских языков, а не русская (разбор второго
методиста, вопрос 19); `My age is twenty` — **правильный английский**, и объявить
его ошибкой значило бы наказать за верный ответ.

**G4 — задание о себе, и у него бесконечно много верных ответов.** Спасает рамка
в условии («целой строкой») и `accept` со всеми числами модуля в той же рамке.
Без этого задание наказывает за верный ответ — порода, которую владелец нашёл
сам.

**Звука в заданиях урока 5 нет вовсе** — возраст ученик говорит и пишет, а не
опознаёт на слух. Это единственный урок правил без звука в заданиях, и это
нарочно: слух работает в уроках 8 и 9.

### 5.5. Вопросы работы модуля

| id | Вид | Что | Почему не близнец |
|---|---|---|---|
| `chisla-4-vozrast-drugogo` | `gap` | `She ___ ten years old.` → `is` | урок дописывал `old`, работа — связку |
| `chisla-4-sprosit-vozrast` | `short` | «Ты хочешь узнать возраст. Запиши вопрос.» → `How old are you?` | урок спрашивал ОТВЕТ, работа — ВОПРОС |

---

## 6. Урок 6 «Номер по одной цифре»

```
slug: "nomer-po-cifram"
title: "Номер по одной цифре"
vid: "pravila"
estimatedMinutes: 14
outcome: "диктовать длинный номер по одной цифре, а короткий целиком"
```

**Опора:** CEFR Companion, **с. 79**, Pre-A1 — «Can ask for and give a **phone
number**». **Этого дескриптора не исполнил ни один из 56 модулей замороженного
курса** (раздел 0 замысла модуля), значит урок пишется с нуля и списывать его
неоткуда.

**Решение владельца от 2 сентября 2026:** короткий номер называют целиком
(`sixty-three`), по одной цифре диктуют длинный.

### 6.1. Сцена

```
— What is your phone number?      — Какой у тебя номер телефона?
— Seven, seven, one, two, three.  — Семь, семь, один, два, три.
— Seven, seven, one, two, three?  — Семь, семь, один, два, три?
— Yes.                            — Да.
```

**`phone number` взято ГОТОВЫМ** — третья строка шапки модуля, адрес — модуль 5
(раздел 1.4 замысла модуля). Ученик его не собирает ни в одном задании.

**Разделитель — запятая.** Дефис синтез читает слитно (владелец, 5 сентября), а
тире с пробелами означает смену голоса (`razgovorLi`).

**`What is your phone number?` — вопрос с `be`**, того же вида, что `How old are
you?`; `your` — карточка модуля 1.

### 6.2. Восемь случаев

1. **Длинный номер диктуют по одной цифре.**
   `Seven, seven, one.` — Семь, семь, один. · `Four, two, five.` — Четыре, два,
   пять.
2. **Короткий номер называют целиком.**
   `Sixty-three.` — Шестьдесят три. · `Number sixty-three.` — Номер шестьдесят
   три.
   Решение владельца; врезка объясняет разницу на комнате 63.
3. **Ноль в номере — `zero`.**
   `Four, zero, five.` — Четыре, ноль, пять. · `Zero, seven.` — Ноль, семь.
4. **Слово `number` называет и число, и номер.**
   `Number ten.` — Номер десять. · `My number is ten.` — Мой номер десять.
5. **Свой номер называют формулой.**
   `My phone number is seven, seven, one.` — Мой номер телефона: семь, семь,
   один. · `What is your phone number?` — Какой у тебя номер телефона?
6. **Переспрашивают повтором.**
   `Seven, seven? — Yes.` — Семь, семь? — Да. · `Zero, five? — Yes.` — Ноль,
   пять? — Да.
7. **Номер записывают цифрами, и это не ошибка.**
   `77123` — Семьдесят семь сто двадцать три? Нет: вслух это `seven, seven, one,
   two, three`. Правило говорит: запись цифрами короче, а читают её по одной.
   Опора — Young Learners, **с. 16**: «encouraged to write numbers as digits
   rather than words to avoid spelling mistakes».
8. **Между цифрами — запятая, а не дефис.** Диктуя, ставим запятую: так пауза
   слышна.

### 6.3. Словарь: две карточки

`zero` `/ˈzɪərəʊ/` — **Oxford A2**, превышение объявлено в шапке модуля: без нуля
номер не продиктовать, а телефон — прямой дескриптор Pre-A1. `phone` `/fəʊn/` —
Oxford A1; Young Learners, **с. 81**, список «The home», колонка **Pre A1
Starters**. Карточка `number` стоит в уроке 1.

### 6.4. Пять заданий

| № | id | Вид | Что делает ученик |
|---|---|---|---|
| H1 | `h1-zapisat-nomer` | `short` + `zvuk: "seven, four, two"` | записывает номер цифрами → `742` |
| H2 | `h2-dopisat-zero` | `gap` | `four, ___, five` → `zero` |
| H3 | `h3-po-cifram-ili-celikom` | `hottext` | в ряду `seven, seven · seventeen · one, two · twelve` отмечает прочитанные по цифрам |
| H4 | `h4-korotkiy-nomer` | `short` | «Комната 63. Как её называют? Запиши словами.» → `sixty-three` |
| H5 | `h5-skazat-vsluh` | `speak` | диктует вслух номер `seven, seven, one` |

**H4 проверяет решение владельца прямо** и снимает беду замороженного курса, где
`sixty-three` объявлялось ошибкой, а ученик, ответивший верно, получал «пока не
так».

**Ни одно задание не требует собрать `phone number`** — третье условие сводящего.

### 6.5. Вопросы работы модуля

| id | Вид | Что | Почему не близнец |
|---|---|---|---|
| `chisla-4-nomer-so-sluha` | `short` + `zvuk: "one, zero, three"` | → `103` | номер другой, ноль стоит в середине, а в уроке он дописывался в `gap` |
| `chisla-4-kak-nazvat` | `choice` | «Комната 15. Как её называют?» — `fifteen` · `one, five` | урок требовал записать, работа — выбрать; и число другое |

---

## 7. Урок 7 «Читаем записку с числами»

```
slug: "chitaem-zapisku"
title: "Читаем записку с числами"
vid: "chtenie"
estimatedMinutes: 12
outcome: "находить в записке номер телефона и число"
```

**Опора:** CEFR Companion, **с. 55**, *Reading correspondence*, графа **Pre-A1**:
«Can recognise times and places in very simple notes and text messages from
friends or colleagues … **provided there are no abbreviations**».

**Оговорка источника становится требованием к тексту: НИКАКИХ СОКРАЩЕНИЙ.** Ни
`Rm 12`, ни `tel.`, ни `min`. Это то самое место, где оговорка графы важнее
самой графы.

### 7.1. Текст

`kind: "text"`, `genre: "message"`, `id: "zapiska-ot-dany"`.

```
Hi Alim!                                          Привет, Алим!
My phone number is seven, seven, one, two, three. Мой номер телефона: семь, семь, один, два, три.
Two books for you. One key for you.               Две книги для тебя. Один ключ для тебя.
Ten minutes.                                      Десять минут.
Dana                                              Дана
```

**Текст собирался четырежды, и каждая правка снимала чужое слово.** Записываю
все, потому что ловушка тут общая и повторится в любом уроке чтения.

| Что стояло | Почему снято |
|---|---|
| `I am in room twelve.` | слова `room` в курсе нет ни одной карточкой |
| `I have two books for you.` | глагола `have` курс не вводил |
| `Two books and one key for you.` | `and` вводит модуль 6; заимствовать чужой пункт ради одной строки не стоит — строка разбита на две |
| `Ten minutes and I am ready.` | **слова `ready` в курсе нет:** оно жило в выведенном модуле `kto-eto.ts`. Та же ловушка, что со `student` и `here` в сцене урока 2 |

**Проверять надо по файлам курса, а не по памяти, и файлы меняются под руками.**
Словарь модуля 2 я смотрел дважды за один день: утром в `privetstvie.ts` не было
ни одной карточки, к вечеру там стояли `hi`, `good morning`, `good afternoon`,
`good evening`. Значит `Hi` в записке законно — но проверять это надо было
второй раз, а не помнить первый.

**Итог: все слова свои.** `hi` — карточка модуля 2; `book`, `key` — модулей 1 и
4; `minute` — урок 2 этого модуля; `phone number` — урок 6, взято готовым.
`for` — простой предлог, `PREPOSITIONS, FORM: SIMPLE`, A1, пункт модуля 3, и
здесь он **употребляется, а не разбирается**. Числа — уроки 1 и 3.

### 7.2. Три задания

| № | id | Вид | Что делает ученик |
|---|---|---|---|
| K1 | `k1-nomer-telefona` | `short` + `about: "zapiska-ot-dany"` | выписывает номер цифрами → `77123` |
| K2 | `k2-skolko-knig` | `choice` + `about` | сколько книг: `one` · `two` · `ten` |
| K3 | `k3-skolko-minut` | `short` + `about` | сколько минут → `ten`, `accept: ["10"]` |

**`about` здесь законен, и это единственный урок модуля, где он стоит:** блок
`text` — один из двух видов, которые принимает проверка (`check-content.mts`,
строка 1154). В уроках 1–6 его нет ни у одного задания.

**K2 берёт отвлекающими числа из самой записки** (`one` от ключа, `ten` от
минут) — оба настоящие: ученик, читающий невнимательно, возьмёт любое.

---

## 8. Урок 8 «Слушаем числа»

```
slug: "slushaem-chisla"
title: "Слушаем числа"
vid: "slushanie"
estimatedMinutes: 12
outcome: "записывать со слуха названное число"
```

**Формат взят у Cambridge дословно:** A2 Key, **с. 29** — «The candidates should
only write ONE word, or a number, or a date, or a time for their answer»; Young
Learners, **с. 16** — «Each answer is either a name or a **number** … The numbers
dictated **can be written as digits or words**»; **с. 13** — «Note-taking.
**Write numbers and names in gaps**».

### 8.1. Записи

Три блока `audio`, каждый — короткий разговор. **Поле `voice` обязательно у
каждого:** без него разговор читается одним голосом, и эту беду в проекте ловили
дважды — сорок примеров ступени Elementary и двадцать четыре блока `audio`.

1. `How old are you? — I am thirteen.`
2. `What is your phone number? — Four, zero, five.`
3. `How many books are there? — Fifteen.`

**Первые два держатся в двадцатке нарочно** — Young Learners, с. 16:
«Candidates will only hear numbers 1–20». Третий берёт пятнадцать, а десятки
приходят только четвёртым заданием, где ответ выбирается, а не пишется.

### 8.2. Четыре задания

| № | id | Вид | Что делает ученик |
|---|---|---|---|
| L1 | `l1-vozrast-so-sluha` | `short` + запись 1 | → `13`, `accept: ["thirteen"]` |
| L2 | `l2-nomer-so-sluha` | `short` + запись 2 | → `405` |
| L3 | `l3-skolko-knig` | `short` + запись 3 | → `15`, `accept: ["fifteen"]` |
| L4 | `l4-teen-ili-ty` | `choice` + `zvuk: "seventy"` | `seventeen` · `seventy` |

**Все четыре ответа — одно число**, как требует источник. `speak` в уроке 8 нет:
он весь в уроке 9.

**L2 и H1 из урока 6 — ближайшая пара модуля**, и разведены они длиной номера и
местом нуля: в уроке 6 три цифры без нуля и вне разговора, здесь три цифры с
нулём в середине и внутри разговора. **При написании перечитать оба условия
рядом** (раздел 12.2).

---

## 9. Урок 9 «Говорим о числах»

```
slug: "govorim-o-chislah"
title: "Говорим о числах"
vid: "govorenie"
estimatedMinutes: 12
outcome: "различать на слух -teen и -ty"
```

**Поле `vid` обязательно:** в уроке стоят блоки `audio` (образцы), а вывод по
составу отдал бы такой урок в `slushanie`. `check:content` сверяет вывод с
заголовком и о расхождении спросит.

**Образец — A2 Key, с. 42, Speaking Part 1:** «The interlocutor asks **two
short-answer questions** to each candidate … followed by **one longer „Tell me
something about …" question**». Программа читает эту страницу так же (раздел 3):
два коротких ответа и один длинный, с записью-образцом.

**Первая редакция замысла шла не по этому образцу** — ставила в урок говорения
отметку услышанных пар, то есть второй урок слушания подряд. Правка по разбору
второго методиста.

### 9.1. Строение

- **разминка на слух** — три пары своими записями: `thirteen`/`thirty`,
  `fourteen`/`forty`, `fifteen`/`fifty`;
- **два коротких ответа:** `How old are you?` и `What is your phone number?`;
- **один длинный:** «расскажи о числах своего дня» — сколько книг, сколько минут
  дорога, какой номер. Запись-образец обязательна.

### 9.2. Три задания

| № | id | Вид | Что делает ученик |
|---|---|---|---|
| M1 | `m1-para-na-sluh` | `choice` + `zvuk: "forty"` | `fourteen` · `forty` |
| M2 | `m2-svoy-vozrast` | `speak` | отвечает вслух на `How old are you?`, образец `I am twenty years old.` |
| M3 | `m3-svoi-chisla` | `speak` | длинный ответ по образцу: `Two books. Ten minutes. My phone number is seven, seven, one.` |

**Итог назван по проверяемой половине.** Машина оценивает только M1; M2 и M3 —
`speak`, итога на себе не несут. Поэтому в итоге стоит «различать на слух», а
умение назвать свой возраст спрашивается вопросом работы модуля на итог урока 5,
а не заданием урока 9.

**M1 берёт пару `fourteen`/`forty`, а не `thirteen`/`thirty`** — та стоит в
уроке 4 (F1) и в работе модуля.

---

## 10. Что берётся готовым во всём модуле — сводка для шапки

Четыре строки, все с адресами. Тексты выписаны дословно в замысле модуля.

| Оборот | Где работает | Адрес разбора |
|---|---|---|
| `How many …?` | урок 2, случаи 7 и 8 | взят по Cambridge, разбора не требует; расхождение источников — строка в шапке |
| `there are` внутри вопроса | урок 2, случаи 7 и 8; урок 8, запись 3 | модуль 12 «Мой дом и комната» |
| `phone number` | уроки 6, 7, 9 | модуль 5 «Бланк: имя, страна, адрес» |
| `How old are you?` | урок 5, случай 4; уроки 8 и 9 | взят по Cambridge; расхождение — строка в шапке |

**Пятое, если сводящий сочтёт нужным:** предлог `for` в тексте чтения — пункт
`PREPOSITIONS, FORM: SIMPLE`, A1, принадлежит модулю 3 (`plan-modul-3.md`,
строка 296). Модуль 4 его употребляет, не объясняя; строка в шапке стоит копейки,
а `npm run chain` без неё промолчит только потому, что предлог служебный.

---

## 11. Долги модуля с адресами — дополнение к разделу 6 замысла модуля

| Долг | Где обещан ученику | Адрес |
|---|---|---|
| два предмета и больше | урок 1, конец случая 2 | урок 2 этого модуля |
| устройство `there is/are` | урок 2, случай 7 | модуль 12 |
| устройство `phone number` | урок 6, случай 5 | модуль 5 |
| бланк с полем `Age` | урок 5, случай 7 | модуль 5 |
| `-es`, особые формы, неисчисляемое | **ученику не обещаны вовсе** | модуль 8 |

**Ни одной врезки «бери как есть» без адреса в модуле нет.** Последняя строка
стоит здесь нарочно: это долг курса, а не обещание ученику, и в тексте уроков о
нём нет ни слова. Обещать то, чего ученик не спрашивал, — значит заводить долг на
пустом месте.

---

## 12. Сверка на близнецов: прогон по всем девяти урокам

**Ради этого раздела и писался раздел 0.** Считал руками, по своей же таблице
строёв, до того как расписал уроки.

### 12.1. Вид задания по урокам

| Вид | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9 |
|---|---|---|---|---|---|---|---|---|---|
| `choice` | + | + | + | + | — | — | + | + | + |
| `short` | + | + | + | + | + | + | + | + | — |
| `gap` | + | + | + | + | + | + | — | — | — |
| `match` | + | — | — | — | + | — | — | — | — |
| `hottext` | — | + | — | + | — | + | — | — | — |
| `order` | — | — | + | — | — | — | — | — | — |
| `speak` | + | + | + | + | + | + | — | — | + |

**Ни один урок не повторяет строй другого целиком.** Ближайшие — уроки 3 и 4
(оба `choice` + `short` + `gap` + `speak`), и они разведены пятым видом
(`order` против `hottext`) и порядком: урок 3 начинается исправлением записи,
урок 4 — слушанием.

`order` стоит один раз на весь модуль, `match` — дважды и на разных предметах
(числа против пар «вопрос — ответ»).

### 12.2. Пары, которые я проверил особо

| Пара | Чем различаются |
|---|---|
| D4 (урок 2) и F1 (урок 4) — оба `choice` со звуком | D4 различает КОНЕЦ слова (`books`/`keys`), F1 — хвост числа (`thirteen`/`thirty`) |
| E4 (урок 3), F1 (урок 4), L4 (урок 8), M1 (урок 9) — все на паре `-teen`/`-ty` | пары разные у каждого: `fifteen`/`fifty`, `thirteen`/`thirty`, `seventeen`/`seventy`, `fourteen`/`forty`. **Это самое густое место модуля**, и держится оно на том, что пара — предмет урока 4, а остальные три ею пользуются. При написании перечитать все четыре условия подряд |
| G1 (урок 5) и D3 (урок 2) — оба «исправь запись» | D3 чинит пропущенное `-s`, G1 — пропущенную связку. **Рамка условия у них общая, и это законно: сказать иначе нельзя**, а предмет разный |
| H1 (урок 6) и L2 (урок 8) — оба «запиши номер со слуха» | H1 в уроке правил, три цифры без нуля, вне разговора; L2 в уроке слушания, ноль в середине, внутри разговора. **Вторая близкая пара**, разведена длиной и местом нуля |
| T5 (урок 1) и D5 (урок 2) — оба «ответь числом» | T5 считает имена в условии по-русски, D5 отвечает на английский вопрос |
| K3 (урок 7) и L3 (урок 8) — оба «сколько» | K3 читает, L3 слушает; разные умения и разные виды урока |

### 12.3. Ответы заданий: прогон по всему модулю

`eight` · `ten` · `four` (урок 1) · `s` · `three tickets` · `Four` (урок 2) ·
`thirteen` · `eighteen` · `teen` (урок 3) · `60` · `-` (урок 4) ·
`I am twenty years old` · `old` (урок 5) · `742` · `zero` · `sixty-three`
(урок 6) · `77123` · `ten` (урок 7) · `13` · `405` · `15` (урок 8).

**Совпадение одно: `ten` в T4 (урок 1) и K3 (урок 7).** Считаю законным: в уроке
1 это дописанное следом число, в уроке 7 — число, найденное в записке. Разные
действия, разные виды, шесть уроков между ними. **Записано здесь, чтобы следующий
не искал заново.**

**Совпадений между заданиями уроков и вопросами работы модуля нет ни одного** —
каждый вопрос разобран в своём разделе колонкой «почему не близнец», и везде
меняется ВИД действия, а не слово. Это то правило, на котором две прежние правки
провалились: замена существительного близнеца не снимает.

### 12.4. Доля «произвести ответ» по урокам

| Урок | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9 |
|---|---|---|---|---|---|---|---|---|---|
| произвести | 4/6 | 4/6 | 4/5 | 4/5 | 5/5 | 5/5 | 2/3 | 4/4 | 2/3 |

Больше половины везде. В уроках 5 и 6 доля полная: там ученик либо пишет строку,
либо говорит.

---

## 13. Что проверено и чего проверить не удалось

**Проверено по файлам и прогоном:**

- **словарь курса на сегодня** — `alfavit.ts` и `privetstvie.ts`; каждое
  английское слово каждой строки этих восьми уроков сверено с ним. Найдено и
  починено пять мест: `students`, `here`, `have`, `room`, `ready` — все в первых
  редакциях сцен и текста чтения, все заменены строками из слов курса;
- `check-content.mts`, строка 1154 — `about` принимается только на `text` и
  `audio`: отсюда `about` есть в уроке 7 и нет в уроках 1–6;
- `check-content.mts`, строки 972–976 — ключ `zvuk` сверяется вхождением в
  абзац: отсюда требование печатать транскрипции в правилах дословно;
- ступени всех числительных — Young Learners, с. 81; потолок в сто — там же и
  с. 36;
- формат ответа на слух — A2 Key, с. 29; Young Learners, с. 13 и с. 16;
- образец говорения — A2 Key, с. 42;
- опоры возраста — Young Learners, с. 28 и с. 16; чтения — CEFR с. 55, Pre-A1.

**Чего проверить не удалось:**

1. **Ударение в числительных на `-teen`** — источника нет ни одного (разбор в
   замысле модуля). Уроки 3, 4, 8 и 9 строят пару на ХВОСТЕ и долготе гласного,
   а не на ударении. Если владелец решит иначе, меняются четыре правила.
2. **Два чтения `kilometre`** — решает владелец.
3. **Предлог `for` в тексте чтения** — брать ли его строкой в шапку пятым
   заимствованием, решает сводящий (раздел 10).
4. **Отвлекающие уроков 2–9 глазами ученика я не проходил.** Каждый проверен
   вопросом «представь ученика, который так ошибается», и выдуманных среди них
   нет; но это проверка замысла, а не прохождение урока. Владелец нашёл три
   такие беды, пройдя один урок, — и ни один замысел их не поймал.
5. **Четвёрка E4 / F1 / L4 / M1 и пара H1 / L2 названы в разделе 12.2 как
   ближайшие.** Сейчас их различие держится на замысле; когда появятся настоящие
   условия, перечитать все шесть подряд. Отчёт `npm run bliznetsy` до написания
   кода прогнать нечем.
6. **Словарь курса меняется под руками.** `privetstvie.ts` за один день прошёл
   путь от нуля карточек до четырёх. Значит перед сборкой каждого урока словарь
   сверяется ЗАНОВО, а не по этому файлу: он верен на 6 сентября и не позже.
