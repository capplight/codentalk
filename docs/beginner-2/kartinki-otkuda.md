# Откуда берём картинки для уроков

Заведено 3 сентября 2026. Владелец: «уроки должны иметь картинки, сделанные тобой
картинки плохие, должна быть какая-то общедоступная библиотека картинок и
элементов для уроков, найди, и используй».

Своё рисование прекращаем. Ниже — четыре открытые библиотеки, лицензии проверены
по их же страницам, цитаты дословные.

## 1. Twemoji — основа. Предметы, занятия, состояния

**Лицензия:** графика — **CC-BY 4.0**, код — MIT. Дословно со страницы проекта:
«Graphics licensed under CC-BY 4.0». Указание авторства нужно, но простое: сам
проект пишет, что «will accept a mention in a project README or an "About"
section or footer on a website». То есть одна строка в подвале сайта.

**Что это.** Около 3600 значков в одном стиле: люди по занятиям (врач, учитель,
студент, водитель), предметы, еда, места, погода, состояния. Для ступени A1 это
почти весь нужный словарь.

**Чем удобнее рисования от руки.** Один стиль на все 3600 картинок. Своё
рисование этого не даст никогда: у меня каждая схема выходит своя.

**Как брать.** Имя файла — это код знака:

```
https://cdn.jsdelivr.net/gh/jdecked/twemoji@15.1.0/assets/svg/1f697.svg   🚗 машина
https://cdn.jsdelivr.net/gh/jdecked/twemoji@15.1.0/assets/svg/1f3e0.svg   🏠 дом
https://cdn.jsdelivr.net/gh/jdecked/twemoji@15.1.0/assets/svg/1f9d1-200d-1f3eb.svg  🧑‍🏫 учитель
```

**Важно для боевого сайта:** на своём сайте эти файлы надо положить к себе, а не
тянуть с чужого адреса. Правило проекта уже действует для шрифтов — их забирают
во время сборки, а не грузят со стороны. Для образца адрес чужой, чтобы картинки
было видно сразу.

## 2. Openclipart — всё, чего нет у Twemoji

**Лицензия:** **CC0 1.0**, общественное достояние. Дословно: «there is no need to
cite Openclipart», художники отказываются от всех прав.

Решение владельца от 22 августа уже называло Openclipart источником предметов —
оно в силе, и теперь у него есть напарник.

## 3. Open Peeps — люди для сцен

**Лицензия:** **CC0**. Дословно: «Free for commercial and personal use under CC0
License», «you can copy, modify, distribute, remix… even for commercial purposes,
without asking permission». Автор — Pablo Stanley, называть его не обязательно.

Рисованные от руки люди, которых можно собирать по частям. Годится там, где нужна
сцена с человеком, а не значок.

## 4. unDraw — большие рисунки под сцену

**Лицензия:** коммерческое использование разрешено, авторство не требуется —
«You do not need to ask permission from or provide credit».

**Но два запрета, и один из них прямо про нас:** нельзя раздавать иллюстрации
пачками и нельзя «any automated processing of the assets for AI/ML model
development»; скачивание скриптом без согласия тоже запрещено. **Значит качать
unDraw пачкой нельзя** — только брать по одной руками, когда нужна большая сцена.

## Что берём для чего

| Нужно | Откуда | Указание авторства |
|---|---|---|
| предмет, занятие, еда, погода, состояние | Twemoji | строка в подвале сайта |
| чего у Twemoji нет | Openclipart | не нужно |
| человек в сцене | Open Peeps | не нужно |
| большая сцена на весь экран | unDraw, по одной руками | не нужно |

## Чего мы больше не делаем

**Не рисуем сами.** Владелец сказал прямо: «сделанные тобой картинки плохие». Своё
рисование остаётся только там, где картинки нет ни в одной библиотеке и это
схема, а не предмет, — например разбор строки по частям.

---

# Как это устроено на деле — 6 сентября 2026

Второй источник понадобился в тот день, когда владелец потребовал картинку
почти у каждой карточки: **«Желательно не оставляй без картинок ни одну
карточку»**. Сборщик раздал 52 значка Twemoji и честно назвал слова, которым
значка в наборе НЕТ. Первым в том списке стоял `table` — та самая карточка, с
которой владелец и начал замечание: «table и door нуждаются в картинке».

Дверь у Twemoji есть (`1f6aa`), стола нет вовсе.

## Где что лежит и как курс отличает одно от другого

| Источник | Папка | Как записывается в уроке |
|---|---|---|
| Twemoji | `public/twemoji` | `znak: "1f697"` — код знака шестнадцатеричными |
| Openclipart | `public/kartinki` | `znak: "stol"` — имя файла |

Различает их одна строка — `adresZnachka()` в `lib/content/znaki.ts`: код
Twemoji это шестнадцатеричные числа через дефис, всё прочее — наша картинка.
Правило живёт в одном месте нарочно: его спрашивают и страница, и скрипт
забора, и проверка. Разойдись они — ученик получил бы сломанную картинку при
чистом отчёте, а на этом проект уже обжигался с компасом вступления.

`npm run znachki` за нашими картинками не ходит и в отчёте называет их
отдельной строкой: Openclipart адреса по имени слова не даёт, там выбирают
глазами.

## Два правила, без которых картинка Openclipart выглядит плохо

**1. Её надо ПОСМОТРЕТЬ, а не выбрать по имени файла.** Правило проекта
«оформление проверяется глазами» относится и сюда. Кандидаты рисуются в один
PNG и разглядываются рядом с соседними значками Twemoji — так видно, выбивается
картинка стилем или нет. Из восьми столов взят плоский и тёплого дерева: он
стоит рядом со стулом `1fa91` и дверью `1f6aa`, не споря с ними.

**2. Её надо ОБРЕЗАТЬ по рисунку** — `node scripts/obrezat-kartinku.mjs <файл>`.
У Twemoji `viewBox` у всех 3600 значков ровно «0 0 36 36», рисунок занимает весь
квадрат, и страница ставит картинку квадратом. Openclipart отдаёт файл как есть:
у стола `viewBox` был «0 0 612 792», то есть страница формата Letter, внутри
которой рисунок занимает четверть. Без обрезки значок вышел бы крошечным и
сдвинутым в угол.

Скрипт не разбирает путей внутри файла: рисует его крупно, обрезает поля и
пересчитывает `viewBox` по смещениям обрезки, добивая до квадрата. Работает с
любым файлом, как бы он ни был устроен внутри.

## Что взято

| Слово | Файл | Openclipart | Лицензия |
|---|---|---|---|
| `table` | `public/kartinki/stol.svg` | `openclipart.org/detail/211444` | CC0 |
| `desk` | `public/kartinki/pismennyy-stol.svg` | `openclipart.org/detail/193203` | CC0 |
| `skirt` | `public/kartinki/yubka.svg` | `openclipart.org/detail/192036` | CC0 |
| `trousers` | `public/kartinki/bryuki.svg` | `openclipart.org/detail/168770` | CC0 |
| `coat` | `public/kartinki/palto.svg` | `openclipart.org/detail/234422` | CC0 |
| `sweater` | `public/kartinki/sviter.svg` | `openclipart.org/detail/213608` | CC0 |
| `shirt` | `public/kartinki/rubashka.svg` | `openclipart.org/detail/177426` | CC0 |

Пять нижних строк взял сборщик модуля 11 «Одежда» 7 сентября 2026. Все пять
страниц Openclipart печатают «Public Domain»; все пять файлов обрезаны
`scripts/obrezat-kartinku.mjs`.

**Четыре из пяти назвал замысел, пятую нашёл сборщик, и вот чем.** Замысел просил
картинки для `skirt`, `trousers`, `coat` и `sweater` — тех слов, которых в Twemoji
нет вовсе. Карточке `shirt` он предлагал значок `1f454`, честно оговорив, что
глазами его не смотрел: «в Unicode это NECKTIE, а рисует рубашку с галстуком».
Сборщик посмотрел — **знак рисует крупный красный галстук на синем квадратном
фоне**, рубашка там только воротник. Слова «галстук» в курсе нет ни одного, и это
единственный из четырнадцати значков модуля с собственным фоном: он выбивался бы
из ряда. Карточка ушла в Openclipart.

**А `1f9e5` остался за `jacket`, и это тоже решено глазами.** В Unicode он зовётся
COAT, то есть по имени просился карточке `coat`, — а рисует стёганую синюю
куртку. Имя Unicode тут врёт, и менять два значка местами не надо.

**Приём, которым это смотрели.** Все кандидаты — и коды Twemoji, и файлы
Openclipart — собраны в одно контактное полотно PNG (`sharp`, сетка с подписями
кодов) и просмотрены одним изображением, рядом друг с другом. Так видно сразу две
вещи: врёт ли значок и не сливаются ли два соседних на маленьком размере. По
одному открывать дороже, и соседства при этом не видно вовсе.

CC0 ссылаться не требует — дословно со страницы лицензии: «there is no need to
cite Openclipart». Строки выше стоят не ради лицензии, а чтобы следующий знал,
откуда картинка и чем её заменить.

## Чему картинки так и не нашлось

Сборщик назвал тринадцать слов, и по каждому сказал почему. Одиннадцать из них —
не про Twemoji, а про то, что **вещь не рисуется значком вовсе**: `sister`,
`brother`, `aunt`, `uncle`, `cousin`, `grandparents` — это РОДСТВО, а любой
кандидат оказывается уже занятым 👩 или 👨; `town` отличается от `city` размером,
а размер картинкой не передаётся; `street` и `kilometre` упираются в 🛣, отданный
карточке `road`; `company` — в 🏢, отданный `office`; `thing` это понятие, и 📦
сказал бы «коробка».

Openclipart тут не помогает: беда не в наборе значков, а в том, что у слова нет
предмета. Правило владельца — «желательно», и служебных слов оно не касается.

## Что взято для модуля 12 «Мой дом и комната» — 7 сентября 2026

| Слово | Файл | Openclipart | Вес |
|---|---|---|---|
| `flat` | `public/kartinki/kvartira.svg` | `/detail/170439/apartment-building` | 136 КБ |
| `kitchen` | `public/kartinki/kuhnya.svg` | `/detail/253185/kitchen-counter-and-sink-from-glitch` | 49 КБ |
| `bedroom` | `public/kartinki/spalnya.svg` | `/detail/245249/bedroom` | 184 КБ |
| `garden` | `public/kartinki/sad.svg` | `/detail/302561/cartoon-nature-scene` | 67 КБ |
| `floor` | `public/kartinki/pol.svg` | `/detail/20735/isometric-floor-tile` | 143 КБ |

Все пять выбраны глазами: кандидаты качались пачками, собирались в один снимок
и разглядывались рядом со значками Twemoji; штриховые отброшены. Все обрезаны
`node scripts/obrezat-kartinku.mjs`.

**У `kuhnya` обрезка сперва падала:** файл задавал только `width` и `height`, а
`viewBox` не задавал вовсе, и скрипту нечего было пересчитывать. `viewBox`
дописан по этим же размерам.

### Три слабых места, названные вслух

1. **`pol` на сорока четырёх точках читается хуже прочих.** Годная по смыслу
   картинка (доски во всю клетку, `/detail/264728/pine-laminate`) весит **1,5 МБ**
   — вдесятеро больше самого тяжёлого значка курса. Отвергнута по весу, а не по
   виду, и это решение стоит пересмотреть, если найдётся лёгкая.
2. **У `sad` и `kuhnya` есть собственный фон** — небо и стена. На экране они
   встают цветной плиткой, а не рисунком на просвет, как значки Twemoji.
3. Оформление проверяется глазами, и это не заменить чтением кода: три пункта
   выше — к владельцу.

## Что взято для модуля 11 «Одежда» — 7 сентября 2026

| Слово | Файл | Openclipart |
|---|---|---|
| `skirt` | `public/kartinki/yubka.svg` | `/detail/192036` |
| `trousers` | `public/kartinki/bryuki.svg` | `/detail/168770` |
| `coat` | `public/kartinki/palto.svg` | `/detail/234422` |
| `sweater` | `public/kartinki/sviter.svg` | `/detail/213608` |
| `shirt` | `public/kartinki/rubashka.svg` | `/detail/177426` |

Первые четыре взяты потому, что значков этих вещей в Twemoji нет вовсе. Пятая —
отступление сборщика, и оно поучительно: **знак `1f454` в Unicode зовётся
NECKTIE и рисует не рубашку, а крупный красный галстук на синем квадратном
фоне** — единственный из четырнадцати значков модуля с собственным фоном. Слова
«галстук» в курсе нет.

Замысел этого не видел и честно писал, что судит по имени знака, а не по
картинке. Обратное там же подтвердилось: **`1f9e5` (в Unicode COAT) рисует
стёганую куртку**, и он оставлен за `jacket` — менять их местами не надо.

**Урок из этих двух строк один: имя знака в Unicode не описывает картинку.**
Смотреть надо контактным листом, а не перечнем имён.

## Что взято по решению владельца от 8 сентября 2026

Он отменил довод, которым тринадцать карточек оставили без значка:

> «можно использовать эмоджи типа карточик, брат-мальчик, сестра-девочка,
> aunt-женщина, grandparents - бабушка с дедушкой, **но только когда знакомишь
> ученика со словом. потому что в упражнениях по картинке фиг угадаешь что
> это.** … town может быть картинка маленьких зданий, city больших. street
> картинка с табличкой baker st. 228 например.»

**Разделение, на котором стоит его решение, устройство уже держит** — проверено
чтением кода, а не предположением: значок карточки берётся из `item.znak`
(`components/lesson/Ekran.tsx`), значок варианта задания — из `option.znak`
(`TaskCard.tsx`). Это разные поля, и одно в другое не перетекает. Значит повтор
значка между карточками ученику не мешает: в словаре он читает слово рядом с
картинкой и не угадывает ничего.

| Слово | Значок | Откуда |
|---|---|---|
| `sister` | `1f467` 👧 | Twemoji, тот же, что у `girl` |
| `brother` | `1f466` 👦 | Twemoji, тот же, что у `boy` |
| `aunt` | `1f469` 👩 | Twemoji, тот же, что у `woman` |
| `uncle` | `1f468` 👨 | Twemoji, тот же, что у `man` |
| `grandparents` | `babushka-i-dedushka` | два значка Twemoji в одном файле |
| `street` | `tablichka-ulicy` | Openclipart `203557`, CC0 |
| `bottle` | `butylka` | Openclipart `315806`, CC0 |

### Пара значков в одном файле — приём новый, и вот его цена

У «бабушки и дедушки» одного значка нет ни в Twemoji, ни где-либо: это ДВА
человека, а владелец просил именно пару. Файл собран `scripts/tmp-para.mjs` из
`1f475` и `1f474` — оба уже лежат у нас, оба под той же лицензией CC-BY, и
упоминание Twemoji в подвале их покрывает.

**Первая сборка была негодной, и увидеть это можно было только глазами:** при
масштабе 24/36 фигуры наложились, и левая почти спряталась за правой. Взято
20/36 с разводкой — обе читаются и на сорока точках, где значок и стоит в
словаре.

**Своим рисованием это не является** и запрета от 3 сентября не нарушает: ни
одной новой линии не нарисовано, два готовых значка расставлены рядом.

### Слабое место таблички улицы названо вслух

`203557` — тёмно-синяя табличка с надписью «1 Old Street». Это ровно то, что
просил владелец, и это же **единственный значок курса с собственным тёмным
фоном**: все Twemoji стоят без подложки. Прежде такой довод уже решил судьбу
значка — `1f454` ушёл из карточки `shirt` в том числе потому, что рисует
галстук на синем квадрате.

Здесь довод владельца сильнее довода о стиле: у таблички фон и есть предмет,
табличка без подложки перестаёт быть табличкой. Записано, чтобы следующий не
счёл это недосмотром.

### Чему картинки по-прежнему нет, и почему это не лень

Восемь слов остались без значка, и у каждого причина своя. Кандидаты искались
по Openclipart и сводились в контактное полотно — тем же приёмом, что и раньше.

| Слово | Что нашлось | Почему не взято |
|---|---|---|
| `town` | «маленькие здания» просил владелец | 🏘 `1f3d8` — это именно они, но значок занят карточкой `village`, а `town` и `village` в курсе противопоставлены. Openclipart по запросам «town buildings», «small town houses», «row of houses» отдаёт рамки-орнаменты и чёрно-белые гравюры |
| `college` | Openclipart `298280` | флаг здания с английской надписью «COLLEGE LIBRARY» внутри рисунка; на сорока точках надпись нечитаема, а без неё это просто здание, неотличимое от 🏫 `school` |
| `kilometre` | 📏 `1f4cf` занят карточкой `metre` | одна и та же линейка у метра и километра ничего не различает |
| `company` | 🏢 `1f3e2` занят карточкой `office`, 🏭 рисует завод | «фирма» и «контора» стоят в одном модуле, и один значок на оба сотрёт разницу |
| `thing` | — | у понятия нет предмета: 📦 сказал бы «коробка», а `box` — карточка того же курса |
| `cousin` | — | двоюродный брат ИЛИ сестра: любой один рисунок половину значения отсекает |
| `neighbour` | — | сосед отличается от прочих людей не видом, а тем, где живёт |
| `teenager` | Openclipart, запрос «teenager» | подростка от взрослого рисунок не отличает; Twemoji его не знает вовсе |

**Правило владельца — «желательно», и здесь оно упирается не в набор значков, а
в то, что у слова нет предмета.** Картинка, которая врёт, хуже её отсутствия:
этим доводом курс уже снял `1f454` с карточки `shirt`.
