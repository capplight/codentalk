# Разбор замысла модуля 10 «Какой человек»

6 сентября 2026. Пишет **второй методист** — тот, кто замысла не писал.
Разбираются `docs/beginner-2/plan-modul-10.md` (1475 строк) и
`docs/beginner-2/plan-modul-10-urok-1.md` (577 строк).

**Приговор: доработать.**

Замысел сделан добросовестно, и в нём много верного: все опоры на CEFR, на
руководство Young Learners и на A2 Key я открыл постранично, и **ни одна цитата
не оказалась выдуманной** — расхождение нашлось ровно одно (пункт 3). Ступень
двадцати пяти слов, разбиение тематических списков по столбцам, счёт нерозданных
пунктов, свобода нового пункта EGP — всё сошлось с прогонами.

Но проверка по курсу, которую замысел объявляет первой и главной, **сделана не
до конца**, и это стоило модулю урока 2, двух вопросов работы и трёх слов,
которых в курсе нет.

---

## ОШИБКИ

### 1. Урок 2 и его итог дублируют модуль 4, урок 5 — вместе с «расхождением источников»

**Где:** `plan-modul-10.md`, шапка (строки 29–33), раздел 3.1, раздел 5.3
(«Урок 2»), раздел 5.2 (итог урока 2).

Замысел объявляет расхождение по `How old is he?` **главной находкой модуля** и
ставит его отдельной строкой в шапку. Он же пишет в разделе 5.3: «Осторожно:
модуль 4 уже учит `How old are you? — I am ten years old.` Урок 2 берёт это как
известное, о СЕБЕ не переучивает».

Модуль 4 учит гораздо больше. Вот его видимый текст
(`npm run tekst -- english-beginner-2 chisla-i-telefon`, урок «Сколько тебе
лет», итог «называть свой возраст и спрашивать о возрасте»):

> **Вопрос о возрасте начинается словами How old**
> Слова How old стоят вместе и не меняются, о ком бы ни шла речь. Дальше стоит
> are you или **is she**, и меняется в вопросе только эта часть.
> How old are you? — Сколько тебе лет?
> **How old is she? — Сколько ей лет?**
>
> **О другом человеке говорят через is**
> О себе мы ставим am, а о нём и о ней ставим is. Число за глаголом стоит на том
> же месте, и меняется только глагол.
> **She is ten. — Ей десять.**
> **He is thirty. — Ему тридцать.**
>
> **В ответе можно обойтись без years old**
> В ответе довольно глагола и числа, а слова years old можно не говорить.

Сверка с восемью случаями урока 2 (раздел 5.3):

| Случай урока 2 | Есть ли в модуле 4 |
|---|---|
| возраст другого человека (`He is ten years old.`) | да: `He is thirty.` плюс `I am ten years old.` |
| вопрос `How old is he?` | да: `How old is she?` — тот же строй, тот же разбор |
| ответ одним числом (`Ten.`) | нет |
| ответ строкой (`She is nine years old.`) | да |
| почему в вопросе `is he`, а в ответе `he is` | нет — но это случай 6 урока 1 модуля 10, см. пункт 17 |
| возраст без `years old` (`She is nine.`) | да, отдельным случаем |
| `young` и `old` о человеке | нет |
| `baby` и `teenager` | нет |

**Пять случаев из восьми — второй разбор одной строки.** Итог урока 2
(«называть возраст другого человека и спрашивать о нём») повторяет итог урока 5
модуля 4 («называть свой возраст и спрашивать о возрасте») почти дословно.

**И расхождение источников модуль 4 объявил первым.**
`courses/english-beginner-2/chisla-i-telefon.ts`, строки 75–82:

> РАСХОЖДЕНИЕ ИСТОЧНИКОВ: `How old are you?` взято по руководству Cambridge
> (Young Learners, с. 28 и с. 16 — Pre A1 Starters). English Grammar Profile
> держит две графы на один оборот: `CLAUSES / interrogatives` … A1, и
> `QUESTIONS / wh-`, FORM: MAIN VERB 'BE'
> (`1741163715031x898125206259626100`) — A2. Решение владельца от 6 сентября
> 2026 то же: берём по Cambridge. **Оборот приходит уроком 5 целой формулой.**

Те же две графы, те же страницы, то же решение. Раздел 3.1 замысла модуля 10 —
не находка, а повторное открытие; раздел 1.1 «Чего в курсе НЕТ» его пропустил.

**Что сделать.** Урок 2 переписать так, чтобы он не разбирал `How old …?`
заново: его собственное содержание — **возраст словом** (`baby`, `teenager`,
`young`, `old` о человеке) и ответ одним числом. Итог переписать так, чтобы он
не совпадал с итогом урока 5 модуля 4. Строку расхождения в шапке заменить
ссылкой: «оборот введён модулем 4, урок „Сколько тебе лет"; здесь он работает
повтором» — иначе следующий проверяющий решит, что оборот приходит сюда.

Это ровно та порода, ради которой записано «ПРОВЕРКА ПО КУРСУ ИДЁТ ПЕРВОЙ»:
модули 12 и 26 Elementary порознь написали `Why don't we…?`.

---

### 2. Два вопроса работы — близнецы работы модуля 4

**Где:** `plan-modul-10.md`, раздел 8.3, вопросы 3 и 4.

Раздел 8.2 честно перечисляет углы, отданные работе модуля 9, и сверяет с ними
свои. **Работа модуля 4 не открывалась вовсе.** А в ней стоит (тот же прогон
`tekst`, раздел проверочной работы):

> _условие задания:_ Ты хочешь узнать, сколько лет собеседнику. Запиши этот
> вопрос по-английски. → _ответ:_ `How old are you?`
>
> _условие задания:_ Речь о девочке десяти лет, но в записи пропущен глагол.
> Допиши его. → `She ___ ten years old.`, _ответ:_ `is`

Против них:

- **вопрос 3 модуля 10:** «Ким хочет узнать, сколько лет Дане… запиши вопрос» →
  `How old is she?`. Действие то же, заменён человек. Это порода «замена слова
  близнеца не снимает»;
- **вопрос 4 модуля 10:** выбор из `She is ten years old.` · `She ten years
  old.` · `She is ten year old.`. Верный вариант — дословно строка вопроса
  модуля 4, а первый отвлекающий — та же строка с незаполненным пропуском.

**Что сделать.** Оба вопроса переписать, сменив **действие**, а не человека, и
раздел 8.2 дополнить работой модуля 4 наравне с работой модуля 9.

---

### 3. CEFR, с. 67: цитата взята из соседней строки той же графы

**Где:** `plan-modul-10.md`, раздел 2.4, врезка «Оговорка о соседней графе».

Написано:

> CEFR, **с. 67**, шкала *Creative writing*, графа A1 даёт описывать **комнату**
> и **вещь** … — **человека там нет**; описания людей на письме приходят графой
> A2.

Открываю `materials/.text/cefr-companion-2020.txt`, страница 67 (метка `с. 67` —
строка 3253), графа A1 целиком, строки 3266–3271:

> A1
> **Can produce simple phrases and sentences about themselves and imaginary
> people, where they live and what they do.**
> Can describe in very simple language what a room looks like.
> Can use simple words/signs and phrases to describe certain everyday objects
> (e.g. the colour of a car, whether it is big or small).

**Первая строка графы A1 — прямо о людях.** Замысел прочитал вторую и третью и
сделал вывод об отсутствии по ним. Это та порода, о которой правило говорит
«никогда не писать „в источнике этого нет"»: цитаты верны, а вывод обратен тому,
что стоит в графе.

Урок письма от этого не рушится — жанр `message` подпёрт с. 83, и это верно.
Рушится **оговорка**: она сужает урок без нужды и оставляет следующему
проверяющему ложное утверждение об источнике.

**Что сделать.** Оговорку переписать: графа A1 с. 67 описание человека
разрешает; жанр выбран сообщением по с. 83 и потому, что открытка занята
модулем 28, а бланк — модулем 5.

---

### 4. Карточки `teacher` в курсе нет ни у кого, а модуль 10 стоит на ней дважды

**Где:** `plan-modul-10-urok-1.md`, раздел 5 (пример карточки `young` — `Our
teacher is young.`) и раздел 9 («проверено… что `teacher` уже дан РАНЬШЕ модуля
10 — поиск по `term:` во всех файлах курса»); `plan-modul-10.md`, раздел 5.4,
урок 6 «Читаем заметку о **новом учителе**».

Проверка по коду:

```
grep -rn 'term: "teacher"' courses/english-beginner-2/*.ts
```

даёт одно совпадение — `kto-eto.ts`, а этот файл выведен из курса, и **сам
замысел это знает** (раздел 1.1, находка 1: «файл выведен из курса… И ЭТО НЕ
СЧИТАЕТСЯ»).

Больше того, долг известен и записан в коде соседа —
`courses/english-beginner-2/moya-semya.ts`, строки 81–97:

> КАРТОЧЕК `teacher` И `student` В КУРСЕ СЕЙЧАС НЕТ НИ У КОГО — их давал снятый
> модуль `kto-eto`. … СЛОВА `teachers` В УРОКЕ БОЛЬШЕ НЕТ НИ РАЗУ, и ни одна
> строка модуля 6 не зависит от карточек `teacher` и `student`.

Модуль 6 нарочно вычистил у себя это слово, а модуль 10 строит на нём пример
карточки и **весь предмет урока чтения**.

**Та же порода задела ещё два места того же раздела:**

- **`Aigul`** — «имя, встречающееся в курсе девять раз». Все девять — в
  `kto-eto.ts`; в живом курсе имени нет ни разу
  (`grep -rc Aigul courses/english-beginner-2/*.ts`). Ступенью имя не меряется,
  но довод ложен;
- **`very`** — стоит в разделе 9 среди слов, «проверенных поиском по `term:` во
  всех файлах курса». Карточки `very` в коде нет: её даёт модуль 9, которого в
  `index.ts` ещё нет. Сам замысел это знает (раздел 10, последняя строка), и два
  его утверждения противоречат друг другу.

**Что сделать.** Либо модуль 10 берёт карточку `teacher` (она вне его тем, а
карточка вне объявленной темы законна всегда), либо пример карточки `young` и
предмет урока 6 переписываются на слово, которое у ученика есть. И раздел 9
перепроверить по коду, а не по `kto-eto.ts`.

---

### 5. Глагол `know` в первой английской строке модуля — и он же опровергает довод модуля

**Где:** `plan-modul-10-urok-1.md`, раздел 2, четвёртая реплика сцены:
`I know his sister. She is tall too.`

Проверка: `grep -rn "\bknow\b" courses/english-beginner-2/*.ts` даёт три
совпадения, и **все три внутри поля `sources`** — в цитате дескриптора CEFR
(«…people they know, things they have»). В видимом тексте курса слова нет ни
разу, карточки нет.

Хуже другое: замысел трижды опирается на утверждение **«во всём модуле 10 глагол
один, и он `be`»** — им он отказывается от `love` (раздел 6.6, п. 2), от
объектных местоимений (раздел 4.3, п. 5) и от половины темы `Health` (раздел
6.1, п. 2). Первая же сцена модуля это утверждение отменяет.

Перечень раздела 2 («Все прочие слова уже даны: `this is` — модуль 7, `my
friend` — модуль 2…») слова `know` не называет вовсе — оно проскочило мимо
собственной проверки.

**И вторая половина той же строки.** Замысел записал в «чего проверить не
удалось»: «Есть ли в курсе карточка `too`… искал по `term:` и не нашёл».
**Закрываю вопрос: карточки нет.** Единственное вхождение — `kto-eto.ts`,
выведенный из курса. Значит строка меняется не «если», а обязательно.

**Что сделать.** Запасная строка замысла — `His sister is tall. She is
beautiful.` — снимает обе беды разом. Поправить и соседнюю строку: «Новых слов в
сцене два — `tall` и `strong`» станет тремя (`beautiful` — карточка того же
урока, ступень не превышена).

---

### 6. `minWords` 12 отсекает верный ответ из одиннадцати слов

**Где:** `plan-modul-10.md`, раздел 5.4, урок 9.

Замысел считает сам:

> Самый короткий ответ, отвечающий условию…: `This is Dana. She is ten. She is
> kind and funny.` — **двенадцать слов**.

Считаю тем же признаком, каким считает страница
(`components/lesson/TaskCard.tsx`, строка 697: `text.trim().split(/\s+/)`):
This(1) is(2) Dana.(3) She(4) is(5) ten.(6) She(7) is(8) kind(9) and(10)
funny.(11) — **одиннадцать**.

Ученик, написавший ровно то, что просили, прочтёт «Слов написано: 11, а нужно от
12. Допиши и сравни снова». Это дословно случай 19 августа, записанный в
правилах проекта.

**Что сделать.** Порог 11 или ниже.

---

### 7. Ответ вопроса на слух напечатан латиницей в соседнем вопросе той же работы

**Где:** `plan-modul-10.md`, раздел 8.3, вопросы 11 и 12; раздел 8.4, врезка
«Вопрос 12 проверен на то, что он о слухе».

Вопрос 11 печатает в условии: `This is Ben. He is twelve. He is tall and
friendly.` Вопрос 12 — следующий — просит записать со слуха одно слово, и это
слово **`tall`**. Оно же стоит ответом вопроса 2 (`She is tall.`).

Замысел пишет: «Искомое слово не напечатано ни в подписи записи, ни в разборе,
ни русскими буквами». Проверены подписи и разборы — а слово стоит в **условии
соседнего вопроса**. Это буквально та порода, которую 6 сентября нашёл методист
модуля 5: предыдущая проверка смотрела только подписи и пропустила два задания,
проходимые не слушая.

**Что сделать.** Либо сменить искомое слово вопроса 12 (например, на `nine` или
`tired`, которых работа нигде не печатает), либо убрать `tall` из вопроса 11.

---

### 8. Вопрос 13 работы: верных ответов два

**Где:** раздел 8.3, вопрос 13; раздел 8.4, последняя строка таблицы.

Напечатанный вопрос — `How old is your friend?`. Варианты — `She is nine.` ·
`She is nine years.` · `He is nine.`

Замысел объясняет: «`He is nine.` — да, но по условию, а не по языку: **в
вопросе друг назван словом `she`**». В вопросе `How old is your friend?` слова
`she` нет. Пол друга ничем не задан, и `He is nine.` — такой же верный ответ.

Порода 14 в чистом виде: ученик отвечает правильно и получает «пока не так».

**Что сделать.** Доопределить условие («у тебя спросили о Дане») либо сменить
отвлекающий.

---

### 9. Урок 1, задание 5: второй верный ответ

**Где:** `plan-modul-10-urok-1.md`, раздел 6, задание 5; раздел 6.1, мерка 5.

Условие: «Ким пишет об Алиме: `Alim is tall. She is young.` Запиши вторую строку
без ошибки». Ответ — `He is young.`

**`Alim is young.` — тоже строка без ошибки и тоже об Алиме.** Задания 3 и 4
задают первое слово прямо («начав с имени», «начав со слова `She`»), а задание 5
не задаёт ничего. Мерка 5 разобрала только вариант «оставить `She`».

**Что сделать.** Либо назвать первое слово в условии, либо поставить
`Alim is young.` в `accept`.

---

### 10. Вида задания `bukvy` в коде не существует

**Где:** `plan-modul-10-urok-1.md`, раздел 6, задание 1 и врезка «Почему `bukvy`
первым».

`lib/content/types.ts`, строки 741–752, перечень `TASK_KINDS`: choice, gap,
hottext, order, match, short, essay, code, speak, setka. Имени `bukvy` там нет.

«Буквы вразброс» — это `order`, у которого все `items` в одну букву:
`courses/english-beginner-2/alfavit.ts`, строки 778–781 (`kind: "order"`,
`items: ["D", "E", "B"]`), а вид сборки страница выбирает по признаку
`task.items.every((k) => k.length === 1)` (`components/lesson/TaskCard.tsx`,
строка 376).

Отсюда следствие, которого замысел не учёл: **у урока 1 выходит два задания
`order`** — первое и восьмое. Довод «виды заданий модуля 10 не должны повторить
порядок модуля 9» и счёт «производящих семь из девяти» надо пересчитать заново:
`order` входит в `PRODUCTIVE_KINDS` (`types.ts`, строка 759).

**Что сделать.** Назвать вид его настоящим именем и пересобрать строй заданий
урока 1.

---

### 11. Отвлекающий `He is and tall funny.` — выдуманная строка

**Где:** раздел 8.3, вопрос 10; раздел 8.4.

Довод замысла: «ошибка того, кто выучил, что `and` идёт после `is`». Такого
ученика представить нельзя: правила «`and` идёт после `is`» не даёт ни один урок
курса, и порядок `and` с двумя прилагательными вразнобой никто не производит.

**Замена называется прямо, чтобы её не заменили другой выдумкой.** Настоящая
ошибка этого места — бессоюзная запись по русскому образцу: по-русски «он
высокий, весёлый» законно и без союза, и ученик переносит запятую:
**`He is tall, funny.`** Второй отвлекающий (`He is tall and he funny.`)
настоящий, его трогать не надо.

---

## ВОПРОСЫ — решает методист или владелец

### 12. Уроки умений модулей 9 и 10 — близнецы по действию

Модули идут подряд, и ученик проходит их встык. Сверка итогов (раздел 5.2 обоих
замыслов):

| Модуль 9 | Модуль 10 | Что делает ученик |
|---|---|---|
| урок 9 «выбирать верный ответ на вопрос о цвете» | урок 8 «выбирать верный ответ на вопрос о человеке» | выбирает ответ, отвечающий на заданный вопрос |
| урок 10 «проверять записку о вещи: **стоит ли форма `be` при прилагательном**» | урок 9 «проверять сообщение о человеке: **стоит ли форма `be` при слове о нём**» | ищет пропущенную форму `be` в чужой записи |

Второе совпадение — не только действие, но и формулировка. Работы повторяют его
же: модуль 9, вопрос 16 («в записке Кима находит строку, где пропущена форма
`be`») против модуля 10, вопрос 14 («не хватает одного слова… запиши вторую
строку целиком»).

Туда же **урок 1 против урока 1**: модуль 9, задание 5 — «Дана говорит о своей
ручке: она не новая. Запиши её строку целиком, **начав со слова My**», ответ
`My pen is not new.`; модуль 10, задание 4 — «Дана не старая. Запиши это,
**начав со слова She**», ответ `She is not old.` Одна рамка, одна ветвь правила,
один человек.

`npm run bliznetsy` этого не увидит: общих слов мало, ответы разные. Мерка здесь
человеческая — «назови вслух, что делает ученик».

**Что решить.** Развести хотя бы урок письма и его вопрос работы: у модуля 10
есть незанятое действие — **дописать недостающую строку сообщения по списку
самопроверки**, а не искать пропуск.

### 13. Вопрос 14 нарушает собственный запрет замысла

Раздел 8.2 пишет: четыре угла «в работе модуля 10 НЕ будет, потому что их взяла
работа модуля 9», и первый из них — **«исправить чужую запись целиком»**. Вопрос
14 («В сообщении написано: `My friend Dana. She tall.` … Запиши вторую строку
целиком») делает ровно это. Запрет и его нарушение стоят в одном документе.

### 14. Задание 2 урока 1 и вопрос 1 работы — одна и та же строка

Задание 2: `Alim is my friend. ___ is tall.` — выбрать `He` · `She` · `It`.
Вопрос 1 работы: `Alim is my friend. ___ is young.` — вписать `He`.

Вид сменён (выбор на вставку), а строка совпадает слово в слово, кроме
прилагательного, и ответ один и тот же. Правило прямое: замена слова близнеца не
снимает. Менять надо строку, а не прилагательное.

### 15. Задание 6 урока 1 стоит на строке сцены

Ответ — `Yes, he is.`, и третья реплика сцены начинается ровно этими словами
(`Yes, he is. He is very strong.`). Замысел это назвал (раздел 6.1, мерка 1) и
оставил «сборщику проверить прогоном `bliznetsy`».

**Прогон здесь не поможет и решать не должен.** Проверка «ответ напечатан выше»
(`scripts/check-content.mts`, строка 598 и далее) копит **только врезки** — в
обход идут блоки вида `note`, — и молчит о примерах и сценах; короткий ответ к
тому же не доходит до порога длины. Решать надо в замысле: сменить лицо вопроса
(`Is she young? — Yes, she is.`) или сменить реплику сцены.

### 16. Случай урока 4 про `I am fine` рядом с `I am well` опоры не имеет

Раздел 5.3, урок 4, шестой случай: «`I am fine` — известная формула модуля 2, и
чем она рядом с `I am well`».

Разница между ними — утверждение о том, что слово значит и как принято, а таких
утверждений наши источники не дают вовсе. Проверил: в
`docs/istochniki-norma-yazyka.md` нет ни одной записи со словами `fine`, `well`,
`ill`, `sick` — поиск по всем четырём даёт ноль совпадений при 39 пунктах файла.
Редактор возьмёт то, что напечатано, и напишет утверждение из головы.

**Что решить.** Либо случай снимается, либо вопрос идёт владельцу — и тогда одной
строкой вместе с вопросом про `ill` и `sick` (раздел 11.8, п. 1): это один и тот
же вопрос о британском обиходе.

### 17. Один разбор дважды внутри модуля

Урок 1, случай 6: «в вопросе форма `is` идёт вперёд, а в ответе возвращается на
место». Урок 2, случай 5: «почему в вопросе `is he`, а в ответе `he is` — порядок
обратный». Это одно правило, разобранное дважды через урок. Если урок 2
переписывается по пункту 1, случай уходит сам.

### 18. Расхождение по `How old is he?` записано неполно

Замысел пишет: пунктов A1 о `wh-`-вопросе с `be` нет, «`wh-`-слово в них не
описано». По **описанию** это верно — я перебрал все 109 записей уровня A1
разбором CSV и подтверждаю. Но правило проекта требует читать к цитате её пример,
а пример графы A1 `CLAUSES / interrogatives`
(`1741163708336x928067678664856700`) — **«How are you?»**, то есть `wh-`-слово,
форма `be` и подлежащее на A1. Та же строка стоит примером и у графы A2
(`1741163715031x898125206259626100`: «How are you, my friend?»).

Источник расходится сам с собой, и об этом прямо предупреждает CLAUDE.md.
Решение (берём по Cambridge) от этого только крепче, но строка шапки, говорящая
«EGP кладёт на A2», следующего проверяющего собьёт: он откроет графу A1 и увидит
там ровно такой вопрос.

---

## Мелкие неточности счёта и ссылок

Ни одна из них урока не ломает, но каждая — утверждение, по которому следующий
будет сверяться.

1. **Тема `Personal Feelings, Opinions and Experiences (adjectives)` держит 56
   слов A1, а не 55** (`npm run programma -- english-beginner-2`, раздел «тем не
   объявил ни один модуль»). Счёт по теме `Health` — 36 — сошёлся точно.
2. **`notice` в курсе шесть, а не пять** (`grep -c` по всем файлам).
   `message` — восемь, `article` — ноль: тут замысел верен.
3. **`teenager` — не «вне тем».** Он стоит в списке `Family and Friends`
   Приложения 2 (`materials/.md/cambridge-vocab-a2-key.md`, строка 1876, рядом с
   `family`, `grandpa`, `mother`). На норму модуля 10 это не влияет — тема
   чужая, — но графа «Тема» в таблице карточек неверна.
4. **«Сообщений ученик прочёл четыре — модули 2, 4, 6 и 9».** Поле
   `genre: "message"` стоит в модулях 2 (дважды), 3 (трижды), 4, 6 и 7 — восемь
   текстов в пяти модулях. Модуля 9 в коде нет.
5. **«Занятыми стоят `v1-pered-formoy` и `v5-zapisat-s-nulya`».** Возвращений в
   курсе семь: к ним добавьте `g3-gorod-i-strana`, `h2-zapisat-slovo`,
   `a-dopisat-a`, `c5-skolko-imyon`, `a3-zapisat-imya`. Вывод при этом верен:
   `v3-o-kom-rech` существует (`privetstvie.ts`, строка 1545) и как возвращение
   не занят.
6. **«Ответ одним числом (`Ten.`) — дословный образец Cambridge, с. 16».** На
   с. 16 стоит запись в бланк ответов задания на слух — «How old is the new
   girl? … 8», цифрой, — а не устный краткий ответ. Опора на формат существует
   (A2 Key, с. 29, строка 1853: «only write ONE word, or a number»), но
   «дословным образцом» устной реплики она не является.
7. **Строка модулю 6 про `married` (раздел 11.7)** называет Oxford A1 и словник,
   но умалчивает, что Young Learners, с. 80, кладёт слово в столбец **A2
   Flyers** (строка `Family & friends`). Методисту модуля 6 это надо знать до
   того, как он возьмёт карточку.
8. **Значок `1f9d1`** — тут замысел прав, проверил: это карточка `person` модуля
   8 (`odin-i-mnogo.ts`, строка 2072). Подбирать другой действительно придётся.

---

## Что проверено и оказалось чистым

По каждой породе назван признак, которым проверял, — чтобы следующий круг мог
это оспорить, а не открывать заново.

**Опоры и страницы — все открыты постранично, ни одной подмены не нашлось.**
Метки страниц в разборах стоят внутри управляющих байтов, поэтому наивный поиск
по началу строки их не находит; я сопоставлял номер строки с ближайшей меткой
разбором байтов файла.

- **Young Learners, с. 9**, Can Do summary Pre A1 Starters: «CAN understand some
  very simple spoken descriptions of people – such as name, gender, age, mood,
  appearance or what they are doing» — есть дословно (строки 417–419). A1
  Movers: «CAN understand very simple spoken descriptions about people and
  objects» и «CAN read and understand some short factual texts with the help of
  pictures» — есть. **И честная оговорка замысла о производстве подтвердилась:**
  в столбце говорения Pre A1 Starters стоит только «CAN give very basic
  descriptions of some **objects and animals**» — человека там нет.
- **Young Learners, с. 16**: «How old is the new girl? … 8» — есть (строка 700).
  **с. 28**, Speaking Part 4: «How old are you? What's your friend's name?» —
  есть (строка 1243). **с. 29**: «Nick is happy.» под строкой Verbs / Present
  simple, «He's a small boy.» и «His name is Yusef.» под строкой Adjectives —
  есть (строки 1263 и 1282). Замысел верно предупреждает про след оглавления на
  строках 507 и 526 — вывода по ним я тоже не делал.
- **Young Learners, с. 52**: «Comparative and superlative adjectives — Your
  house is bigger than mine» в перечне «what is new at A1 Movers level» — есть
  (строка 2253). Довод раздела 11.6 верен.
- **Young Learners, с. 79**, список `The body and the face`: столбцы разобраны
  верно, и **оба разбора дают одно разбиение** — сверил `materials/.text/`
  (строки 3531–3564, поток) с `materials/.md/` (строки 3482–3489, таблица).
  Pre A1 Starters — тринадцать слов, ровно те, что названы; `fat` и `thin` — A1
  Movers.
- **Young Learners, с. 80**: `tired`, `sick`, `ill`, `fine` — столбец **A1
  Movers** строки `Health` (столбец Pre A1 Starters у неё пуст); `hungry` и
  `thirsty` — A1 Movers строки `Food & drink`; `baby`, `young`, `old` — Pre A1
  Starters строки `Family & friends`. Все пометы замысла сошлись.
- **CEFR, с. 63**, Sustained monologue: describing experience, графа **Pre-A1**:
  «Can express how they are feeling using simple adjectives like "happy" or
  "tired"» — есть (строка 3033, страница подтверждена меткой на строке 3011).
  Строка «Can describe themselves, what they do and where they live» и вправду
  лежит в графе A1 — замысел её не берёт, и правильно делает.
- **CEFR, с. 79**, Information exchange: A1 — «Can ask and answer questions
  about themselves and other people…» (строка 3881), Pre-A1 — «Can tell people
  their age and ask people about their age» (строка 3891). Обе на своих местах.
- **CEFR, с. 49** (Understanding conversation between other people, A1),
  **с. 54** (Overall reading comprehension, A1), **с. 66** (Overall written
  production, A1) — все три цитаты дословны и на названных страницах.
  **A2 Key, с. 29** — «only write ONE word, or a number, or a date, or a time»
  (строка 1853, страница подтверждена разбором меток).
- **English Grammar Profile**: файл разобран настоящим разбором CSV — 1222
  записи, из них A1 — **109**, оба сторожа сошлись. Пункты
  `1741163706316x926291459998112000`, `1741163716067x824562184770361200`,
  `1741163708775x971363187748411900`, `1741163715031x898125206259626100`
  открыты по `id`: категория, уровень, Can-do и все примеры совпадают с
  выписками замысла **дословно**, включая «The teachers are very nice and
  friendly» и «I know you are not well».
- **Пунктов A1 о вопросе с `be` действительно два**, и обе графы описывают строй
  без `wh-`-слова: `CLAUSES / interrogatives` и `PRONOUNS / subject–object,
  FORM: (SUBJECT) QUESTIONS WITH 'BE'` (`1741163713867x150062658595893500`).
  Перебирал по всему файлу, а не по подкатегории. Оговорка — пункт 18 выше.

**Проверка по курсу — по коду и по замыслам соседей:**

- **Замысел модуля 9 открыт в рабочем дереве сегодня**, а не пересказан по чужой
  строке: он и вправду взял `1741163706530x753542801715210100` (раздел 4.1,
  урок 1), взял `1741163716067x824562184770361200` (раздел 4.3, п. 2 — «БЕРУ.
  Прежняя редакция отказывалась, и отказ был ошибкой») и **письменно передал**
  модулю 10 пункт `ADJECTIVES / combining` (раздел 4.3, п. 1, вместе со строкой
  методисту модуля 10). Все три утверждения замысла модуля 10 о соседе верны на
  сегодня.
- **Столбец «Грамматика» программы для модуля 10 и вправду неисполним как
  написан:** у обоих пунктов `ADJECTIVES / position` стоит `Lexical Range = 1`, а
  программа (раздел 6.2) разрешает двум модулям вводить один `id` только при
  диапазоне больше единицы. Решение «пункт остаётся у модуля 9, модуль 10 ставит
  его в `egpPovtor`» законно, и проверка о нём молчит.
- **Новый пункт свободен:** `npm run programma -- english-beginner-2` печатает
  `1741163706316x926291459998112000` первым в списке нерозданных, и нерозданных
  ровно **81** — как и сказано в замысле.
- **Восемь пунктов `egpPovtor`, помеченных «код», сверены с полями `egp` в самих
  файлах:** `privetstvie.ts` держит LINKING VERB BE, (SUBJECT) STATEMENTS,
  AFFIRMATIVE DECLARATIVE и DETERMINERS / possessives, WITH NOUNS;
  `otkuda-ty.ts` — NEGATIVE DECLARATIVE WITH BE и AFFIRMATIVE INTERROGATIVE WITH
  BE; `chisla-i-telefon.ts` — NOUNS / plural, PLURAL -S; `moya-semya.ts` —
  CONJUNCTIONS / coordinating, WORD + WORD. Все восемь у модулей, идущих раньше
  десятого по `index.ts`.
- **Ни одной строки с `and` между двумя прилагательными в курсе нет.** Проверял
  не по коду, а по **видимому тексту**: собрал `npm run tekst` по всем восьми
  модулям и искал образец «форма be, слово, and, слово» — два совпадения, оба с
  существительными (`are doctors and my`, `are sisters and they`).
- **Слово `it` о человеке в курсе не стоит нигде.** Искал `It is` с заглавным
  именем, сокращённую запись с апострофом и `Who is it` по всем файлам, кроме
  выведенного, — ноль совпадений. Запрет замысла законен.
- **Слова, объявленные известными, проверены по коду и по порядку `index.ts`:**
  `mouth` (`alfavit.ts`, строка 2456, перевод «рот», знак `1f444`), `old`
  (`chisla-i-telefon.ts`), `fine` (`privetstvie.ts`), `boy`, `girl`, `child`,
  `best friend`, `doctor`, `nurse`, `brother`, `father`, `sister`, `our`
  (`moya-semya.ts`), `friend` (`privetstvie.ts`), `man`, `woman`, `person`,
  `people` (`odin-i-mnogo.ts`), `this` (`chto-eto-takoe.ts`), `his`
  (`blank.ts`). Исключения — `teacher`, `Aigul`, `very`, `too`, `know`: пункты 4
  и 5.
- **Неправильных множественных в курсе ровно четыре пары.** Счёт по видимому
  тексту: `children` 37, `men` 20, `people` 23, `women` 20; `feet` и `teeth` —
  ноль. Отказ от `foot` и `tooth` обоснован.
- **Имена девяти уроков уникальны.** Искал каждое по `title:` во всех трёх
  курсах: точных совпадений нет. Ближайшие — «Слушаем описание» в двух
  замороженных курсах и три «Читаем заметку о …» на Elementary; строки разные.
- **Виды уроков умений выведутся верно:** `lib/content/vid-uroka.ts`,
  `vidPoZagolovku` ловит начала «Читаем», «Слушаем», «Пишем», «Говорим» — все
  четыре заголовка модуля 10 под это подходят, поле `vid` не понадобится.
- **Модуль 12 модулю 10 не соперник:** его замысел прямо отказывается от `happy`
  и `new`, сославшись на то, что прилагательные розданы модулю 9
  (`plan-modul-12-urok-1.md`, строки 113–114).

**Словарь и ступень:**

- **Все двадцать пять слов** прогнаны через `npm run slovo` по одному: Oxford
  3000 — **A1** у всех двадцати пяти, словник A2 Key — «да» у всех двадцати
  пяти. Записи в `ipa-en-uk` нет ровно у одного слова — `nice`; замысел это
  назвал верно.
- **Темы карточек сверены с Приложением 2 по второму разбору**
  (`materials/.md/cambridge-vocab-a2-key.md`): список Health, Medicine and
  Exercise (строки 1918–1934) содержит `hair`, `hand`, `head`, `ear`, `eye`,
  `face`, `leg`, `nose`, `baby`, `tired` и **запись `well` с пометой
  прилагательного** — сужение перевода до «здоровый» подпёрто источником, как и
  сказано. Список Personal Feelings (строки 1982–2009) содержит `angry`,
  `beautiful`, `friendly`, `funny`, `happy`, `hungry`, `kind`, `nice`, `strong`,
  `tall`, `tired`, `well`, `young`. Исключение — `teenager`, пункт 3 мелочей.
- **Отказ от `neck`, `stomach`, `thin` по ступени верен:** Oxford ставит всем
  трём **A2** (`npm run slovo`, каждое отдельно). `fat` и вправду A1, и отказ от
  него — не ступень, а оценка; вопрос владельцу поставлен правильно. `sick` —
  A1, `ill` — A2: тоже сошлось.
- **Записей об `ill` и `sick` в `docs/istochniki-norma-yazyka.md` нет** — искал
  по обоим словам пары и по `well` с `fine`, ноль совпадений при 39 пунктах
  файла. Утверждение замысла «не нашёл» подтверждаю.

**Код и проверки:**

- **`check:content` и вправду требует вопроса на каждый итог каждого урока,
  включая уроки умений:** `scripts/check-content.mts`, поле `mustCover`
  собирается обходом по всем урокам модуля. Номер строки у меня вышел другой —
  1971, а не 1898, — но утверждение верно, и строка методисту модуля 9
  (раздел 11.1) тоже.
- **`PRODUCTIVE_KINDS`** — `gap`, `short`, `order`, `code`, `essay`, `speak`
  (`lib/content/types.ts`, строка 759); `match` туда не входит. Счёт
  «двенадцать производящих из четырнадцати» верен, порог пройден.
- **Запрет `hint` в банке вопросов существует** (`scripts/check-content.mts`,
  строки 2150 и 2188, со ссылкой на `forBrowser`). Решение замысла не ставить
  подсказок верно.
- **Порог `article` — пятнадцать слов** (`scripts/check-content.mts`, строка
  1094: у `notice` шесть, у прочих пятнадцать). Открытый вопрос замысла (пункт 7
  в его «чего проверить не удалось») закрыт.
- **Проверка «ответ напечатан во врезке выше» копит только врезки** и молчит о
  примерах и сценах (`scripts/check-content.mts`, строка 598 и далее) — отсюда
  пункт 15.

---

## Чего проверить не удалось

1. **Есть ли в Twemoji восемнадцать названных знаков.** Прогона `npm run
   znachki` я не делал. В `public/twemoji` сейчас лежат `1f4aa`, `1f9d1`,
   `1f642`, `1f464`, `270b`, `1f442`, `1f62a` — семь из девятнадцати; остальные
   двенадцать просто не забирались, и это **не доказательство их отсутствия в
   библиотеке**. Проверять сборщику прогоном, как и просит замысел.
2. **Как выглядят `1f641` и `1f620` на маленьком размере.** Это глазами.
3. **Чтение `nice`.** Подтверждаю только, что записи в `ipa-en-uk` нет
   (`npm run slovo -- nice`); выбор знака — владельцу.
4. **Транскрипции со склада `kakoy-on.ts`** построчно не сверял: смотрел таблицу
   замысла против `npm run slovo`, а не против замороженного файла. Просьба
   замысла к сборщику остаётся в силе.
5. **Поле сетки букв.** `bedySetki` и `lishnieSlova` не прогонял — состав слов
   раздела 6.8 разбирал глазами, как и автор замысла. Вложенность `A` в `FACE`,
   `HE` в `HEAD`, `AN` и `AND` в `HAND` подтверждаю чтением, но это не проверка.
6. **Близнецы прогоном.** `npm run bliznetsy` по модулю 10 сейчас бессмыслен —
   модуля нет. Всё, что о близнецах сказано выше (пункты 2, 12, 13, 14), найдено
   чтением и сверкой действий, а не отчётом.
7. **Замысла модуля 11 «Одежда» не существует** — файла `plan-modul-11.md` в
   `docs/beginner-2/` нет. Значит утверждения замысла о границе с модулем 11
   (раздел 1.5 и строки 11.2) я мог сверить только с программой ступени, где
   модулю 11 и вправду отданы `have got` и тема Clothes and Accessories.
8. **Полноту тем Personal Feelings и Health после прихода модуля 10** не
   пересчитывал: модуля в коде нет, а `programma` считает по коду. Довод замысла
   («одним модулем их не закрыть, добирать не буду») по существу верен — счёт тем
   я подтвердил, — но цифры «плюс 13, итого 20» и «плюс 12, итого 14» проверятся
   только прогоном после сборки.
9. **Уроки 3, 4 и 5 разобраны по одной строке каждый** — подробных замыслов у них
   нет, есть только раздел 5.3. Значит проверка их случаев, заданий и
   отвлекающих ещё предстоит; урок 2 по пункту 1 придётся писать заново.
