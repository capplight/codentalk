# Модуль 11, урок 1 «Что у меня есть: I have got a bag». Подробный замысел

6 сентября 2026. Пишет методист. **Весь видимый русский текст пишет редактор:**
вступление, имена случаев, тексты правил, врезки, подписи, условия, подсказки и
разборы заданий. Здесь стоят состав, порядок, английские строки, опоры и
запреты — и ничего из того, что ученик прочтёт по-русски.

Замысел модуля — [plan-modul-11.md](plan-modul-11.md).

**Итог урока:** выбирать `have got` или `has got` по слову перед оборотом.

**Проверка итога подстановкой под общий хвост «Чему научишься в модуле»:**
«выбирать `have got` или `has got` по слову перед оборотом» — правда, урок этому
учит (случаи 1, 3, 4, 5 и **6**), задания 1, 2, 3 и 5 этого требуют, и работа
модуля спрашивает это вопросами 1 и 2. **Случай 6 добавлен именно ради итога:**
без строки со множественным словом перед оборотом выбор формы всегда сходился с
«он / она», и правило было не видно на своих строках, а вопрос 2 работы требовал
невиданного. **Слов «говорить, что у тебя есть» в итоге нет
нарочно:** это шире урока — так ученик умеет и после случая 1, а урок держится на
выборе формы.

**Итог не говорит «по подлежащему» и не говорит «по тому, о ком речь».** Первое —
термин, которого курс не вводил; второе прямо неверно, и на этом ступень
Elementary уже обожглась: форму выбирает СЛОВО, стоящее перед оборотом, а не
смысл. `My sister has got a bag` — речь об одном человеке, и слово перед оборотом
одно; `Dana and Alim have got new bags` — речь о двоих, и форма другая. **Эта
строка теперь в уроке стоит** (случай 6): прежде довод жил только здесь, в
замысле, а на экране его не было ни разу.

---

## 1. Чем этот урок НЕ является

**Это не урок об артикле.** Артикль ввёл модуль 3 двумя уроками; здесь он стоит в
каждом примере и не разбирается ни разу. Редактору: строки «перед названием вещи
ставится `a`» в уроке быть не должно как НОВОГО правила. **И ни один отвлекающий
задания не различается артиклем** — прежняя редакция ставила таким отвлекающим
`I have got jacket.`, то есть спрашивала ровно то, чем урок не является; та же
ось уже разобрана заданием `k1-vybrat-s-artiklem` модуля 9 (`It is a new phone.`
· `It is new phone.` · `It is an new phone.`).

**Это не урок о цвете.** Цвет вводит модуль 9; случай 6 берёт готовый строй
`a red bag` и добавляет к нему ровно одно — что он встаёт после оборота.
Ссылка на модуль 9 обязательна, разбор — нет.

**Это не урок о множественном числе.** Окончание `-s` ввёл модуль 4, исчезновение
артикля при нескольких вещах — модуль 8. Случай 7 берёт и то, и другое готовым:
`I have got two bags`. **Ни одного правила об окончании в уроке нет.**

**Чем урок вправду нов — тремя вещами, и все три названы:**

1. **сам оборот `have got`** — до модуля 11 имя вещи после глагола стоит в курсе
   только в двух готовых формулах (модуль 1 `How do you spell your name?`, модуль
   5 `Fill in the form.`) и ни разу не разбирается. Прежняя редакция писала «нет
   ни одной строки, где за глаголом стоит вещь», и это опровергалось видимым
   текстом собственного курса;
2. **выбор между `have got` и `has got`** — по слову перед оборотом;
3. **расхождение с русским**: по-русски «у меня», по-английски просто `I`.

**Термин модуля называется здесь, до первого задания:** `have got` зовётся
**оборотом**. Другого имени у этой вещи в модуле нет ни разу — ни «конструкция»,
ни «выражение», ни «глагол have».

---

## 2. Вступление: сцена, а не обобщение

**Сцена.** Дана показывает Алиму новую сумку, и разговор сам собой идёт о том, у
кого что есть.

Разговор, два голоса, реплика на строку, тире между репликами НЕ ставится.
`pervyyGolos: "zhenskiy"` — первой говорит Дана. Голос человека в курсе один:
Дана женский, Алим мужской.

| Английская строка | Перевод рядом |
|---|---|
| Dana: I have got a new bag. | У меня новая сумка. |
| Alim: It is very big. My bag is old. | Она очень большая. Моя сумка старая. |
| Dana: You have got a new jacket! | А у тебя новая куртка! |
| Alim: Yes, I have got a new jacket and new shoes. | Да, у меня новая куртка и новые ботинки. |
| Dana: My sister has got a red jacket. | У моей сестры красная куртка. |

**Оборот работает в сцене четыре раза** — приём Мёрфи: сперва ученик видит вещь
целиком, потом узнаёт, из чего она сделана.

**Все слова уже даны:** `new`, `big`, `very`, `red` — модуль 9; `old` — модуль 4
(карточка `old`, `chisla-i-telefon.ts`, там она о возрасте, а модуль 9 разбирает
второе употребление — о вещи);
`sister` — модуль 6; `yes` — модуль 1; `it`, `my`, `your` — модули 7 и 2;
`and` — модуль 6; `bag`, `jacket`, `shoes`, `have got` — карточки этого урока
(раздел 5).

**Почему сцена, а не русская фраза.** Русская параллель здесь есть и очень
сильная, но она про РАСХОЖДЕНИЕ («у меня» против `I`), а расхождение объясняют
после того, как ученик увидел строку целиком. Значит русская фраза встаёт вторым
экраном — случаем 2, — а первым идёт сцена.

**«Русской параллели здесь нет» я нигде не написал:** она есть, названа и
разобрана.

**Чего во вступлении быть не должно:** перечисления того, что будет разобрано
(«разберём оборот, формы, цвет…»). Владелец сказал об этом прямо, и так
открывался последний урок каждого модуля старого курса.

**Осторожно, ловушка кода:** сцена ставится блоком `example` с подписью, а не
блоком `text`. `vidPoSostavu()` (`lib/content/vid-uroka.ts`) возвращает `chtenie`
для любого урока, где есть блок `text`, и вид урока съедет. Поле `vid:
"pravila"` у урока проставляется всё равно — оно главнее вывода. **Утверждение о
коде сделано чтением, и сборщику проверить его прогоном**, а не чтением соседней
ветки.

---

## 3. Десять названных случаев и двадцать примеров

Мерка нового формата: не меньше восьми названных случаев и шестнадцати целых
примеров с переводом; у случая имя, правило ОДНОЙ СТРОКОЙ и два примера. Мерка с
6 сентября — ориентир, а не обязательство (ответ владельца 18), и растягивать
урок ради числа не надо; здесь случаев вышло десять, потому что столько вещей в
уроке вправду разбирается.

**Случаев было девять, стало десять: добавлен случай 6.** Довод стоит при самом
случае.

**Русские имена случаев и тексты правил пишет редактор** — ниже стоит только то,
ЧТО в случае разбирается и какие английские строки в нём работают.

### Случай 1 — при `I` стоит `have got`

Что разбирается: сам оборот. Вещь называется после него. **Здесь же, строкой
внутри случая, называется имя вещи: `have got` — это оборот.**

| Строка | Перевод |
|---|---|
| I have got a bag. | У меня есть сумка. |
| I have got a hat. | У меня есть шапка. |

### Случай 2 — по-русски «у меня», по-английски просто `I`

Что разбирается: расхождение с русским, и оно главное в уроке. По-русски вещь
принадлежит «мне» — «у меня есть», — а по-английски строка начинается с того же
слова `I`, что и `I am`.

| Строка | Перевод |
|---|---|
| I have got a jacket. | У меня есть куртка. |
| You have got a hat. | У тебя есть шапка. |

**Редактору: связь с модулем 2 назвать прямо** — `I am` ученик уже говорил, и
слово то же самое. Это ссылка на наш модуль, а такие внутри ступени
приветствуются.

### Случай 3 — при `we` и `they` оборот тот же

Что разбирается: форма `have got` не меняется при четырёх словах из шести.

| Строка | Перевод |
|---|---|
| We have got a car. | У нас есть машина. |
| They have got a bag. | У них есть сумка. |

**Имя случая сужено, и вот почему.** Прежняя редакция звала его «при `you`, `we`
и `they` оборот тот же», а примеры давали только `we` и `they`: правило на своих
строках видно не было. Слово `you` показано случаем 2, и редактору стоит назвать
это одной строкой — но в ИМЕНИ случая его нет.

### Случай 4 — при `he` и `she` оборот другой: `has got`

Что разбирается: единственная развилка урока. **Таблица форм стоит на ЭТОМ
экране** — поле `naTomZheEkrane: true`, раздел 4.

| Строка | Перевод |
|---|---|
| He has got a jacket. | У него есть куртка. |
| She has got a dress. | У неё есть платье. |

### Случай 5 — вместо местоимения стоит имя

Что разбирается: таблица с местоимениями случая не закрывает — ученик спотыкается
на первом же примере с именем. Правило проекта требует отдельной строки, и вот
она.

| Строка | Перевод |
|---|---|
| Dana has got a hat. | У Даны есть шапка. |
| My brother has got a jacket. | У моего брата есть куртка. |

**Редактору: правило говорит о СЛОВЕ перед оборотом, а не о смысле.** «Дана» —
это «она», и соблазн сказать «выбирай по тому, о ком речь» прямой. Так писать
нельзя: `My parents have got a car` — речь о двоих, и форма другая.

### Случай 6 — когда людей несколько, форма `have got`

Что разбирается: слово перед оборотом стоит во множественном — и форма та же,
что при `we` и `they`, хотя перед ней не местоимение, а имя или названия людей.

| Строка | Перевод |
|---|---|
| My parents have got a car. | У моих родителей есть машина. |
| Dana and Alim have got new bags. | У Даны и Алима новые сумки. |

**Этого случая в прежней редакции не было, и без него урок не проверял своего же
итога.** Итог урока — «выбирать `have got` или `has got` по слову перед
оборотом», а при прежних примерах выбор всегда сходился с «он / она»: ни одной
строки со множественным словом перед оборотом урок не показывал. При этом урок
ПРЯМО ЗАПРЕЩАЕТ рассуждать «по смыслу» (случай 5), и вопрос 2 работы («После
какого слова стоит `has got`?» — `my brother` · `my parents` · `you`) требовал
того, чего ученик не видел ни разу. Довод замысла против «по смыслу» — строка
`My sister and Dana have got bags` — в уроке тоже не стояла нигде.

**Все слова даны:** `parents` — карточка модуля 6; `car` — карточка модуля 9;
`and` — модуль 6 строками и пунктом EGP (раздел 8); `new` — модуль 9.

### Случай 7 — цвет вещи стоит между артиклем и названием

Что разбирается: готовый строй модуля 9 встаёт после оборота и ничего в нём не
меняет. Разбора строя здесь нет — только ссылка на модуль «Цвет и размер», урок
«Красная машина: a red car».

| Строка | Перевод |
|---|---|
| I have got a red bag. | У меня есть красная сумка. |
| She has got a blue dress. | У неё есть синее платье. |

### Случай 8 — вещей несколько: артикль уходит, у названия окончание

Что разбирается: готовое правило модуля 8 работает и после оборота. Заново оно
не объясняется — называется ссылкой на модуль «Один и много», урок «Яблоко и
яблоки: a уходит».

| Строка | Перевод |
|---|---|
| I have got two bags. | У меня есть две сумки. |
| Alim has got new shoes. | У Алима новые ботинки. |

### Случай 9 — короткая запись `I've got`

Что разбирается: апостроф на месте выпавших букв. Опора — Young Learners, с. 29,
строка `Conjunctions`: «I've got a pen and a pencil».

| Строка | Перевод |
|---|---|
| I've got a dress. | У меня есть платье. |
| We've got a car. | У нас есть машина. |

**Редактору: строк `he's got` и `she's got` в уроке нет ни одной**, и ошибкой они
нигде не объявляются. Причина — раздел 5.6 замысла модуля.

### Случай 10 — две вещи через `and`

Что разбирается: перечисление после оборота. Опора — та же строка Cambridge, с.
29. Возврат к модулю 6, где `and` соединяет двоих.

| Строка | Перевод |
|---|---|
| I have got a jacket and a bag. | У меня есть куртка и сумка. |
| She has got a dress and shoes. | У неё есть платье и ботинки. |

---

## 4. Таблица форм: она делает работу, которой не сделать словами

Ставится на одном экране со случаем 4, полем `naTomZheEkrane: true`. Строка на
слово, столбец на оборот — по правилу владельца от 20 августа: «надо разделить
каждое местоимение на одну линию, в стандартах так объясняют».

| Слово впереди | Оборот |
|---|---|
| I | I have got |
| you | you have got |
| he | he has got |
| she | she has got |
| we | we have got |
| they | they have got |

**Объяснение идёт ПОСЛЕ таблицы и коротко.** Проверка, о которой предупреждает
CLAUDE.md: если после таблицы убрать все слова, потеряет ли урок что-нибудь?
Потеряет — таблица отвечает КАКАЯ форма, а объяснение говорит, что развилка одна
и она у `he` и `she`.

**Строки `it has got` в таблице нет нарочно.** Ни одной строки с `it has got` в
модуле не встречается: вещи в нём не обладают вещами. Строка в таблице, которой
нет в уроке, — это обещание без примера.

**Звучит каждая ячейка правого столбца по отдельности:** `I have got` одна
запись, `she has got` другая. Правило владельца от 19 августа о дроблении звука
по единицам; пара в одной записи — ошибка.

---

## 5. Карточки урока — шесть

| Слово | Перевод | Ступень | Где работает |
|---|---|---|---|
| `have got` | есть (у меня, у тебя) | `have` — Oxford **A1**; оборот — Young Learners, с. 29, **Pre A1 Starters** | сцена и все десять случаев; задания 1–7 |
| `bag` | сумка | Oxford **A1**, словник A2 Key — да, YL с. 79 **Pre A1 Starters** | сцена, случаи 1, 3, 6, 7, 8, 10; задание 2 |
| `hat` | шапка, шляпа | то же | случаи 1, 2, 5; задания 1, 4, 5, 6, 7 |
| `jacket` | куртка | то же | сцена, случаи 2, 4, 5, 10; задание 3 |
| `shoe` | ботинок | то же (в теме словника стоит запись `shoes`) | сцена, случаи 8, 10; задание 7 |
| `dress` | платье | то же | случаи 4, 7, 9, 10; задание 2 |

**Столбец «где работает» сверен с таблицей заданий (раздел 6) строка в строку** —
на этом обжёгся модуль 8, где карточка была записана в задание, которого нет.

**Пример у карточки не повторяет ни одной строки урока.** Правило приписано
6 сентября: в словарь ученик заходит нарочно, и пример карточки считается
напечатанным выше ВСЕГДА, сколько бы экранов ни лежало между ним и заданием.
Предлагаемые примеры:

| Карточка | Пример | Почему такой |
|---|---|---|
| `have got` | `We have got new bags.` | ни одна строка урока так не звучит; форма множественного даёт слову вторую работу |
| `bag` | `my old bag` | сочетание, а не предложение: строк урока не повторяет |
| `hat` | `a black hat` | `black` — карточка модуля 1 |
| `jacket` | `her new jacket` | `her` — карточка модуля 5 |
| `shoe` | `two shoes` | показывает пару и окончание |
| `dress` | `a green dress` | `green` — карточка модуля 9 |

**Чтение.** `bag /bæg/` берётся из замороженного курса дословно. Остальные пять
даю с применёнными условностями курса, **сборщику сверить**: `have got /hæv gɒt/`
(знака ударения в многословной записи нет — решение владельца от 6 сентября,
ответ 4), `hat /hæt/`, `jacket /ˈdʒækɪt/`, `dress /dres/`, `shoe /ʃuː/`.
Формы: `bags /bægz/`, `hats /hæts/`, `shoes /ʃuːz/`.

**Звук.** Запись `bag` уже лежит в `public/zvuk/slovo/` — проверено прогоном
`adresSlova()`. Записей `have got`, `hat`, `jacket`, `dress`, `shoe` и всех форм
нет; их сделает `npm run ozvuchka`.

**Значки.** `bag` — 1f45c, `hat` — 1f452, `jacket` — 1f9e5, `shoe` — 1f45e,
`dress` — 1f457; сцене — 1f45a. **Карточка `have got` идёт без значка**, и это
законное исключение из нормы владельца: служебная запись картинки не имеет по
существу. Все пять кодов существуют и ни один не занят другой карточкой ни в
одном из трёх курсов — проверено прогоном. **Оговорка о `1f9e5`:** в Unicode этот
знак зовётся COAT, и в модуле 11 есть карточка `coat` (урок 2); он оставлен
`jacket`, потому что `jacket` работает здесь с первой строки, а сборщику стоит
посмотреть глазами и, если картинка читается пальто, поменять их местами
(раздел 6.6 замысла модуля).

**Опасных пар среди значков урока нет** — сумка, шапка, куртка, ботинок и платье
различаются формой и цветом. **Сборщику всё же проверить глазами**, а не
чтением: значок в наличии (`npm run znachki` тянет файлы из сети и об ошибке не
молчит) и знаки не сливаются на маленьком размере.

---

## 6. Задания: семь лестницей и одно возвращение

Порядок ступенек — узнать, произвести, применить к себе. Подписей «ступенька 2»
на экране нет: лестница — наша кухня, и слово «ступень» в курсе занято ступенями
A1 и A2.

**Заданий семь, а не восемь.** Возвращение к пройденному — это **поле УРОКА**
`vozvrat` (`VozvratRef[]`, `lib/content/types.ts`, строка 833), а не блок среди
заданий; прежняя редакция звала его «заданием 8», и сборщик положил бы его в
`blocks`.

| № | Вид | Что делает ученик | На каком блоке стоит |
|---|---|---|---|
| 1 | `choice` | выбирает строку, где форма оборота подходит к слову перед ней: `He has got a hat.` · `He have got a hat.` · `He is got a hat.` | случай 4 и таблица |
| 2 | `hottext` | отмечает строки, где стоит `has got`: `My sister has got a bag.` · `I have got a dress.` · `Dana has got a car.` · `We have got a jacket.` · `My friends have got hats.` | случаи 4, 5, 6 и таблица |
| 3 | `gap` | вписывает форму: `Kim ___ got a new jacket.` | случаи 4, 5 |
| 4 | `order` | собирает `I have got a red hat.` из `hat` · `a` · `have` · `I` · `red` · `got` | случаи 1, 7 |
| 5 | `short` | «Алим написал: `My friend have got a hat.`» — исправить чужую запись | случай 5 |
| 6 | `short` | «Перепиши строку короче: `I have got a hat.`» | случай 9 |
| 7 | `speak` | называет вслух две свои вещи через `and`; образец записью — `I have got a hat and shoes.` | случай 10 |

**Поле урока `vozvrat`:** модуль 9 «Цвет и размер», урок `krasnaya-mashina`
(«Красная машина: a red car»), задание **`k3-vpisat-slovo-o-veshchi`** — `gap`,
«Машина новая. Впиши прилагательное на его место». **Модуль 9 написан, и `id`
называется точно, а не адресом.** Соседнее задание `k4-sobrat-stroku-s-an` брать
нельзя: это `order` о строе «артикль + прилагательное + название», а задание 4
этого урока — тоже `order` о том же строе. Два таких подряд — близнец в чистом
виде.

**Блоков без задания в уроке нет, и заданий без блока тоже:** столбец «на каком
блоке стоит» заполнен у всех семи.

### 6.1. Ответы, принимаемые ответы и отвлекающие

**Точка в `accept` не нужна:** `normalizeKeepingCase()` (`lib/content/check.ts`)
срезает `[.!?]+` в конце строки. **А апостроф НЕ срезается** — он только
приводится к прямому. Значит задание 6 без апострофа не засчитает, и правильно
сделает: апостроф там и есть предмет.

| № | Верный ответ | Принимаемые | Отвлекающие | Что проверено |
|---|---|---|---|---|
| 1 | `He has got a hat.` | — | `He have got a hat.`, `He is got a hat.` | **обе ошибки настоящие:** первую делает всякий, кто ведёт одну форму ко всем словам подряд, вторую — тот, кто девять модулей подряд ставил после `he` форму `is`. **Ни один из трёх вариантов не напечатан выше по уроку** — сверено построчно с закрытым перечнем раздела 8: случай 4 стоит на `He has got a jacket.`, случай 1 на `I have got a hat.`, пары `He` + `hat` в уроке нет |
| 2 | `My sister has got a bag.` и `Dana has got a car.` | — | — (`hottext`) | верных ровно два. **Все пять строк в одинаковом отношении к материалу:** ни одна не повторяет примера урока, и различить их можно только по слову перед оборотом. Пятая строка (`My friends have got hats.`) добавлена вместе со случаем 6: без неё задание не трогало множественного слова вовсе |
| 3 | `has` | — | — (`gap`) | другого ответа нет: `Kim` — одно слово об одном человеке |
| 4 | `I have got a red hat.` | — | — (`order`) | **верный порядок ровно один.** `A red hat I have got` по-английски не говорят, других перестановок из этих шести слов не складывается |
| 5 | `My friend has got a hat.` | — | — | ошибка в строке одна и настоящая. **Строка `My friend has got a hat.` не напечатана нигде выше** — случай 5 стоит на `Dana` и `my brother` |
| 6 | `I've got a hat.` | — | — | **строка `I've got a hat.` в уроке не печатается:** случай 9 стоит на `I've got a dress.` и `We've got a car.` Иначе задание проходилось бы списыванием |
| 7 | — (`speak`) | — | — | машина не оценивает; итога на себе не несёт |

**ЧТО ЗДЕСЬ БЫЛО НЕВЕРНО И ИСПРАВЛЕНО 7 сентября 2026.** Прежняя редакция задания
1 давала варианты `I have got a jacket.` (верный) · `I have got jacket.` ·
`I has got a jacket.` и ручалась: «**Ни один из трёх вариантов не напечатан выше
по уроку** — проверено построчно по разделам 2 и 3». **Ручательство было ложным, и
ложным в документе, который сам печатает опровержение:** строка
`I have got a jacket.` стоит первым примером случая 2, а закрытый перечень раздела
8 печатает её дважды — среди случаев и среди заданий. Задание первое в уроке,
ступень «узнать», и верный вариант отмечался бы по узнаванию строки, прочитанной
двумя экранами раньше. К соседней клетке замысел применял более строгую мерку сам
(задание 6: «Иначе задание проходилось бы списыванием»).

**Заодно сменён и второй отвлекающий.** `I have got jacket.` отличался от верного
АРТИКЛЕМ, а урок сам объявляет (раздел 1): «Это не урок об артикле». Тот же
различитель уже стоит у модуля 9 — `k1-vybrat-s-artiklem`, варианты
`It is a new phone.` · `It is new phone.` · `It is an new phone.` Теперь оба
отвлекающих — об обороте, ради которого урок и написан.

**Проверки, прогнанные по каждому заданию.**

1. **«Что ещё сюда подходит?»** — порода 14. У задания 1 подходит ровно одна
   строка, у задания 3 — одна форма, у задания 4 — один порядок. У задания 5
   вторых верных ответов нет: `My friend has a hat.` тоже правильный английский,
   но условие велит ИСПРАВИТЬ написанное, а не переписать своими словами, и
   `have` в нём уже стоит. **Сборщику: если он всё-таки положит `My friend has a
   hat.` в принимаемые, это не ошибка, а щедрость** — принимаемые вправе
   принимать неразобранное.
2. **«Представь ученика, который делает эту ошибку»** — порода 15. Оба
   отвлекающих задания 1 представимы, и оба названы поимённо в таблице.
   `I am got a jacket` и `I have got a me` не берутся: так не пишет никто.
3. **Не проходится ли задание, не читая урока.** Задание 2 требует различить
   `has got` и `have got` в пяти правильных строках — угадать нельзя;
   задание 4 требует порядка, задание 6 — записи, которой нет на экране.
4. **Не проверяет ли задание память о формулировке.** Ни одно условие не
   спрашивает «как называется» и «что мы сказали в правиле».
5. **Верный ответ не напечатан выше** — проверено построчно по сцене, случаям,
   таблице и примерам карточек, и проверено ЗАНОВО после правки задания 1. Это та
   самая порода, где правка ПЕРЕНОСИТ совпадение вместо того, чтобы снять его;
   сборщику перечитать столбец заново после любой правки строк.
6. **Задание не требует слова, которого ученик не получал.** Слова заданий:
   `jacket`, `bag`, `dress`, `car`, `hat`, `shoes`, `sister`, `friend`, `friends`,
   `Dana`, `Kim`, `new`, `red`, `my`, `we`, `he`, `I` — все шесть карточек урока
   плюс слова модулей 2, 5, 6, 9. **`car` даёт модуль 9, `friend` — модуль 2,
   `sister` — модуль 6.** Сборщику проверить это заново по коду.

---

## 7. Звук урока: что звучит поимённо

**Звучит:**

- **сцена целиком, на два голоса**, `pervyyGolos: "zhenskiy"` — первой говорит
  Дана. Реплика на строку, тире между репликами не ставится;
- **каждая из двадцати строк случаев** по отдельности;
- **каждая ячейка правого столбца таблицы** — шесть записей, а не одна;
- **шесть карточек по одному слову** и их примеры;
- **образец к заданию 7** — `I have got a hat and shoes.`

**НЕ звучит:**

1. **отвлекающие задания 1** — `He have got a hat.` и `He is got a hat.`
   Неверная фраза, услышанная, запоминается наравне с верной;
2. **строка задания 5 `My friend have got a hat.`** — по той же причине. Звучит
   только исправленная, и только там, где она стоит ответом;
3. **артикль сам по себе** — одиночное `a` синтез прочтёт названием буквы;
4. **русские подписи, переводы и транскрипция знаками** — их читают глазами;
5. **окончание `-s` отдельной строкой** — оно не слово.

**Ключ `zvuk`, которого нет в напечатанном тексте, — это кнопка, которая никогда
не появится.** Ловит `check:content`; сборщику сверять посимвольно.

**И ловушка второй кнопки.** Английская строка, названная в `zvuchat`, ставит
кнопку ВЕЗДЕ, где она напечатана, — и в правиле случая тоже. Значит правило
говорит О строке, а не повторяет её: не «и получается `I have got a bag`», а
«после `I` стоит оборот, а вещь называют за ним». Иначе на одном экране встанут
две одинаковые кнопки, и ученик не поймёт, чем они различаются.

---

## 8. Закрытый перечень английских строк урока

Нужен затем, чтобы сверка словаря шла по перечню, а не по прозе замысла: правило
приписано методистом модуля 8, у которого скрипт разобрал 183 слова, из которых
строками урока не было ни одно.

**Сцена:** `I have got a new bag.` · `It is very big.` · `My bag is old.` ·
`You have got a new jacket!` · `Yes, I have got a new jacket and new shoes.` ·
`My sister has got a red jacket.`

**Случаи 1–10:** `I have got a bag.` · `I have got a hat.` ·
`I have got a jacket.` · `You have got a hat.` · `We have got a car.` ·
`They have got a bag.` · `He has got a jacket.` · `She has got a dress.` ·
`Dana has got a hat.` · `My brother has got a jacket.` ·
`My parents have got a car.` · `Dana and Alim have got new bags.` ·
`I have got a red bag.` · `She has got a blue dress.` · `I have got two bags.` ·
`Alim has got new shoes.` · `I've got a dress.` · `We've got a car.` ·
`I have got a jacket and a bag.` · `She has got a dress and shoes.`

**Таблица:** `I` · `you` · `he` · `she` · `we` · `they` · `I have got` ·
`you have got` · `he has got` · `she has got` · `we have got` · `they have got`

**Примеры карточек:** `We have got new bags.` · `my old bag` · `a black hat` ·
`her new jacket` · `two shoes` · `a green dress`

**Задания:** `He has got a hat.` · `He have got a hat.` · `He is got a hat.` ·
`My sister has got a bag.` · `I have got a dress.` · `Dana has got a car.` ·
`We have got a jacket.` · `My friends have got hats.` ·
`Kim ___ got a new jacket.` · `has` · `I have got a red hat.` ·
`My friend have got a hat.` · `My friend has got a hat.` · `I have got a hat.` ·
`I've got a hat.` · `I have got a hat and shoes.`

**ПРОВЕРКА, РАДИ КОТОРОЙ ПЕРЕЧЕНЬ И НУЖЕН, СДЕЛАНА ЗАНОВО 7 сентября 2026.** Ни
одна строка-ответ задания не стоит среди строк сцены, случаев, таблицы и примеров
карточек. Прежняя редакция это ручательство давала, а перечень его тут же
опровергал: верный вариант задания 1 был `I have got a jacket.` — первый пример
случая 2, и в перечне он стоял дважды. Строки задания 1 сменены (раздел 6.1).

**Все слова этих строк проверены по коду:** `bag`, `hat`, `jacket`, `shoe`,
`dress`, `have got` — карточки урока; `new`, `big`, `very`, `red`, `blue`,
`green`, `car` — модуль 9, **и он теперь в коде** (`cvet-i-razmer.ts`, курс
подключён в `index.ts`); `old` — модуль 4; `black` — модуль 1; `sister`,
`brother`, `parents` — модуль 6; `friend` — модуль 2; `her` — модуль 5; `my`,
`your`, `I`, `you`, `he`, `she`, `we`, `they`, `it`, `is`, `yes` — модули 1, 2,
6, 7; число `two` даёт модуль 4 таблицей, а не карточкой (числа в курсе карточек
не получают); `Dana`, `Alim`, `Kim` — имена, а имена собственные ступенью не
меряются.

**Одна запись прежней редакции была неверна: `and` карточки НЕ имеет.** Здесь
стояло «`sister`, `brother`, `and` — модуль 6». Прогон
`grep 'term: "and"' courses/english-beginner-2/*.ts` даёт ноль: модуль 6 вводит
союз пунктом English Grammar Profile (`1741163708775x971363187748411900`, стоит в
его `egp`) и строками, но словарём — нет. **Для служебного слова это законно**, и
`and` в строках урока 1 стоит правомерно; неверна была именно запись «карточка
модуля 6».

**Оговорка прежней редакции о семи словах модуля 9 снята:** модуль 9 лежит в
коде, и слова проверены по нему, а не по замыслу.

---

## 9. Что проверено и чего проверить не удалось

**Проверено:**

- **все двадцать примеров случаев, шесть строк сцены и шестнадцать строк
  заданий** — по словам, против КОДА курса (`grep` по `term:`), а не против
  замыслов: модуль 9 написан;
- **ни один верный ответ задания не напечатан выше по уроку** — построчным
  сличением с перечнем раздела 8, и сличением ЗАНОВО после правки задания 1;
- **ни одно правило урока не опровергается его же примерами** — прошёл по всем
  десяти случаям вопросом «показывают ли примеры то, что сказано в правиле». На
  этом снялось имя случая 3: оно называло `you`, которого в его примерах нет;
- **итог урока проверяется своими случаями** — случай 6 добавлен ровно поэтому:
  без множественного слова перед оборотом выбор формы всегда сходился с «он /
  она», и правило было не видно на своих строках;
- **ни одного слова «всегда», «только», «нельзя» в замысле урока нет** —
  проверено поиском по этому файлу; места, где они просятся, разобраны в разделе
  3.5 замысла модуля;
- **`have got` у Cambridge на Pre A1 Starters** — Young Learners, с. 29, раздел
  `Verbs`, строка `Have (got) for possession`; принадлежность страницы ступени
  проверена по оглавлению файла;
- **короткая запись `I've got`** — там же, строка `Conjunctions`: «I've got a pen
  and a pencil»;
- **записи звука** — прогоном `adresSlova()`: из шести карточек урока лежит одна,
  `bag`; `have got`, `hat`, `jacket`, `dress`, `shoe` и все формы делает
  `npm run ozvuchka`;
- **`vozvrat` — поле урока, а не блок**, и `id` задания модуля 9 назван точно
  (`k3-vpisat-slovo-o-veshchi`) — чтением кода `cvet-i-razmer.ts` и
  `lib/content/types.ts`;
- **`normalizeKeepingCase()`** — чтением `lib/content/check.ts`: точка срезается,
  апостроф нет.

**Чего проверить не удалось:**

1. **Наличие значков Twemoji глазами** — их тянет из сети `npm run znachki`. Что
   коды существуют и не заняты, проверено прогоном; что они рисуют — нет.
   Отдельно `1f9e5`: в Unicode он зовётся COAT (раздел 5).
2. **Отвлекающие глазами ученика я не проходил** — прошёл вопросами «что ещё
   сюда подходит» и «представь ученика, который делает эту ошибку», а это не то
   же самое, что пройти урок. Особо это касается нового отвлекающего
   `He is got a hat.`: я считаю его настоящей ошибкой ученика, девять модулей
   ставившего после `he` форму `is`, но проверить это может только тот, кто
   видел учеников.
3. **`npm run bliznetsy` по ненаписанному уроку бессмыслен**; сличение заданий
   урока 1 с работой модуля и с заданиями модулей 7–9 сделано чтением видимого
   текста.
