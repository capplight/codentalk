# Модуль 8, уроки 2–8. Подробный замысел

6 сентября 2026. Пишет методист. **Весь видимый русский текст пишет редактор:**
вступления, объяснения случаев, врезки, подписи, условия и разборы заданий.
Здесь стоят состав, порядок, английские строки, опоры и запреты.

Замысел модуля — [plan-modul-8.md](plan-modul-8.md), урок 1 —
[plan-modul-8-urok-1.md](plan-modul-8-urok-1.md).

**Круг по уроку 1 уже пройден, и его находки внесены сюда заранее.** Так и должно
быть: правило проекта зовёт проверяющих после ПЕРВОГО урока серии, чтобы один
промах не повторился семь раз. Внесено:

1. **слова «хвост» в этих уроках нет ни разу** — окончание зовётся окончанием;
2. **пример словарной карточки не повторяет ни одной строки своего урока**;
3. **у каждой формы, которую урок показывает, есть хотя бы одна запись**;
4. **правило сверено со своими примерами** — порода, найденная редактором на
   паре «яйцо — яйца»;
5. **в задании на сличение все варианты стоят в одном отношении к
   напечатанному** — либо все повторяют, либо ни один;
6. **условие, определяющее ответ, названо отдельным предложением**, а не вставкой
   внутрь длинной фразы.

---

## 0. Правило, из-за которого пятая находка в уроках 2 и 3 работает иначе
**Урок о написании не может не показать той формы, которую потом спрашивает.**
В уроке 2 предмет — как пишется `boxes` и `cities`; в уроке 3 — как выглядит
`men` и `children`. Показать эти формы обязан сам урок, значит любое задание на
производство воспроизводит показанное, и «ни одна строка не повторяет
напечатанное» там недостижимо.

**Разделяю ТРИ вида заданий, а не два — правка 6 сентября по разбору второго
методиста.** Первая редакция клала `hottext` рядом с `choice` и тем объявляла
мерку нарушенной во всех четырёх уроках правил разом.

| Вид | Слово берётся | Почему |
|---|---|---|
| **выбор из наших строк** (`choice`) | из слов, которых урок в нужной форме НЕ печатал | иначе задание решается памятью, а не правилом. Находка 14.1 |
| **отметка внутри данной строки** (`hottext`) | любое | мерка к нему неприменима **по устройству**: все слова стоят в одной строке и напечатаны одинаково, выбирать «знакомое» не из чего |
| **производство** (`gap`, исправить, сборка) | можно из показанных | памятью его не решить: форму надо написать, а видеть её раньше — и есть смысл урока |

**Но у `hottext` своя мерка, и она строже.** Признак, отличающий верные слова от
неверных, обязан быть **языковым**. Первая редакция урока 3 давала строку
`man, cup, child, box, woman`, где верные — слова этого урока, а неверные — слова
чужих: ученик отвечает по тому, откуда слово, а не по тому, какое у него
множественное. **Строки всех четырёх `hottext` пересобраны так, чтобы все слова
были напечатаны в СВОЁМ уроке.**

**Слова для выбора взяты из прежних модулей** — они подпадают под правило, но в
уроке не печатаются. Список выправлен: первая редакция называла `office`, `zoo`
и `taxi`, а ни одного задания с ними в уроках 2–8 нет. **Осталось одно, и оно
работает:** `company` (модуль 6). **Карточка пришла 6 сентября вечером** — `moya-semya.ts`, модуль 6 стоит шестым в `index.ts`, — и `kontrol` даёт по модулю ноль ошибок. Слово `address` из раздела ушло: заданий с ним в уроках 2–8 нет ни одного, а развилку `y` оно не проверяет вовсе.

**Слово `zoo` из раздела ушло, и вот почему это важнее списка.** Оно годилось бы
для выбора, но правила о букве `o` в уроке нет — там список из двух слов, — и
задание на `zoos` требовало бы от ученика того, чего урок не давал. Хуже того:
карточка `zoo` **уже стоит в коде** (модуль 3), и правило «слова на `-o` берут
`-es`» опровергалось бы ею. Разбор — урок 2, случай 3.

---

## Урок 2. Когда написание меняется: boxes и cities

**Итог:** писать `-es` после `s`, `x`, `ch`, `sh` и у слов `potato` и `tomato`, а
`-ies` — после согласной с `y`.

### Вступление: сцена

Дана заказывает, Алим записывает и слышит, что слово стало длиннее.

| Строка | Перевод | Голос |
|---|---|---|
| Dana: Two glasses, please. | Два стакана, пожалуйста. | женский |
| Alim: Glasses? | Стаканы? | мужской |
| Dana: Yes. One glass, two glasses. | Да. Один стакан, два стакана. | женский |

`pervyyGolos: "zhenskiy"`, реплика на строку, тире между репликами нет. Все слова
даны: `please`, `yes` — модуль 1; числа — модуль 4; `glass` — карточка этого
урока.

**Почему сцена, а не русская фраза.** Русская параллель тут обманчива: по-русски
написание тоже меняется («книга — книги», «нож — ножи»), но по другим причинам.
Сказать «как по-русски» значило бы дать ложную опору. Сцена показывает то, ради
чего урок и нужен: слово стало ДЛИННЕЕ на слух, и ученик слышит это раньше, чем
видит.

### Восемь случаев, девятнадцать примеров

**Счёт исправлен 6 сентября вечером и собран РАЗБОРОМ собранного урока, а не
глазом:** 2 + 2 + 2 + 2 + 3 + 2 + 2 + 4 = девятнадцать. Прежние «семнадцать»
считали случай 5 за две строки вместо трёх и случай 8 — за две пары вместо
четырёх.

| № | Что разбирается | Примеры |
|---|---|---|
| 1 | слово кончается на `s` или `x` — пишем `-es` | `a glass — glasses` (стакан — стаканы) · `a box — boxes` (коробка — коробки) |
| 2 | кончается на `ch` или `sh` — тоже `-es` | `a sandwich — sandwiches` (бутерброд — бутерброды) · `a dish — dishes` (блюдо — блюда) |
| 3 | **у двух слов модуля** — `potato` и `tomato` — тоже пишется `-es`, и их запоминают | `a potato — potatoes` (картофелина — картофелины) · `a tomato — tomatoes` (помидор — помидоры) |
| 4 | перед `y` согласная — `y` уходит, пишем `-ies` | `a city — cities` (город — города) · `a family — families` (семья — семьи) |
| 5 | перед `y` гласная — `y` остаётся, пишем `-s` | `a day — days` (день — дни) · `a key — keys` (ключ — ключи) · `a boy — boys` (мальчик — мальчики) |
| 6 | выбирая написание, смотрим на конец слова: `s`, `x`, `ch`, `sh` — или `y`, и тогда на букву перед ним | `a country — countries` (страна — страны) · `a taxi — taxis` (такси — такси) |
| 7 | в ряду из нескольких слов окончание нужно каждому | `two glasses and three dishes` (два стакана и три блюда) · `four cities and two days` (четыре города и два дня) |
| 8 | и только теперь — звучание: у слов после `s`, `x`, `ch`, `sh` появляется лишний слог, у остальных нет | `box — boxes` · `glass — glasses` · `city — cities` · `potato — potatoes` |

**Случай 3 переписан 6 сентября: правила о букве `o` в уроке нет.** Правило
опровергается словом собственного курса — `zoo` даёт `zoos`, и карточка `zoo`
**уже стоит в коде** (`otkuda-ty.ts`, модуль 3). Ему вторит `zero` из модуля 4
(`zeros`). Выписка источника это подпирает дословно: Википедия говорит «o
**preceded by a consonant** in many cases», а у `zoo` перед `o` стоит гласная, то
есть под графу оно не подпадает вовсе; но `zero` подпадает и всё равно берёт
`-s`. **Значит случай 3 — список из двух слов, и в таблице у них стоит не «на
`o`», а «эти два слова».** Мой же раздел 6 замысла модуля этого и требовал, а
первая редакция урока 2 требование нарушила.

**Случай 6 переписан там же и по той же породе.** Он говорил «смотрим на
последнюю букву», то есть выглядел общим порядком выбора для любого слова. Тогда
`person` (кончается на `n`) остаётся без ответа, а `fish` (кончается на `sh`)
получает по правилу `fishes`. **После правки случай называет ровно четыре конца и
`y`, а всё прочее уходит строкой-обещанием:** слова, у которых множественное
своё, разбираются в уроке «Свои формы: men, women, children, people».

**Случай 7 переписан: он приносил примеры из случаев 1 и 4, а не свои.** Отсюда и
расхождение в числе — заголовок обещал восемнадцать примеров, в перечне их было
двенадцать. Теперь у случая свои две строки, и заголовок называет **семнадцать**:
пятнадцать целых строк плюс две пары в случае 8, где показано звучание.

**Случай 8 стоит последним нарочно** — разбор в замысле модуля, раздел 4.1, и
выписка `docs/istochniki-norma-yazyka.md`, пункт 26: лишний слог даёт шипящий
перед окончанием, а не буква. Ставить его третьим, как в первой редакции,
значило дать правило, которое врёт на половине своих же примеров.

**Проверка правил своими примерами** — порода, найденная редактором в уроке 1:

| Правило | Примеры | Сходится? |
|---|---|---|
| «`y` уходит, пишем `-ies`» | cities, families, countries | да, во всех трёх `y` вправду исчезает |
| «перед `y` гласная — `y` остаётся» | days, keys, boys | да |
| «появляется лишний слог» | boxes, glasses | да; и тут же названы `cities` и `potatoes`, где не появляется |
| «два слова урока на `-o`» | potatoes, tomatoes | да, и это СПИСОК, а не правило о букве |

**Три слова курса, которыми правило урока 2 опровергалось до правки** — их нашёл
второй методист, и все три надо держать в голове при письме:

| Слово | Что давало правило | Что на деле | Где живёт |
|---|---|---|---|
| `zoo` | `zooes` по столбцу «на `o`» | `zoos` | карточка модуля 3, **уже в коде** |
| `zero` | `zeroes` | `zeros` | карточка модуля 4 |
| `fish` | `fishes` по правилу о `sh` | своя форма | **из модуля 8 выведено**, см. урок 3 |
| `person` | ничего: правило его не покрывает | своя форма | урок 3 |

Первые два сняты правкой случая 3, третье — выводом слова `fish` из модуля,
четвёртое — строкой-обещанием в случае 6.

### Таблица форм

Едет вместе с объяснением случая 6 (`naTomZheEkrane: true`): она отвечает «какая
форма», объяснение — «по какой букве её выбирают».

| Об одном | О нескольких | На что смотрим |
|---|---|---|
| a glass | glasses | на `s` |
| a box | boxes | на `x` |
| a sandwich | sandwiches | на `ch` |
| a dish | dishes | на `sh` |
| a potato | potatoes | это слово запоминают |
| a city | cities | согласная перед `y` |
| a day | days | гласная перед `y` |

**Звучат все четырнадцать ячеек по отдельности.** Столбец «на что смотрим» —
русский, его читают глазами.

### Карточки урока — четыре

| Слово | Перевод | Пример карточки | Ступень |
|---|---|---|---|
| `glass` | стакан | `my glass` | Oxford A1, словник A2 Key, YL с. 80 A1 Movers |
| `sandwich` | бутерброд | `a sandwich and tea` | Oxford A1, YL с. 80 A1 Movers |
| `dish` | блюдо | `my dish` | Oxford A1, словник A2 Key — да, в теме `Food and Drink` |
| `potato` | картофелина | `a potato and an onion` | Oxford A1, YL с. 80 Pre A1 Starters |
| `tomato` | помидор | `a tomato in the box` | Oxford A1, YL с. 80 Pre A1 Starters |

**Ни один пример карточки не повторяет строки урока** — проверено против
восемнадцати примеров, трёх реплик сцены и четырнадцати ячеек таблицы.

**Сочетание `a glass of` запрещено прямо:** это «существительное + of +
существительное», модуль его не берёт нигде (раздел 3.4 замысла модуля).
Редактору: ни одной строки с `of` в уроке 2.

**Расхождения по `dish` НЕТ, и объявлять в шапке нечего.** Первая редакция писала,
что тематический список Young Learners, с. 80, держит `dish` в колонке A2 Flyers.
**Это неверно: слова `dish` в руководстве нет вовсе.** Второй методист искал тремя
способами и выписал строку `Food & drink` целиком — biscuit, butter, cereal,
chopsticks, cookie, flour, fork, honey, jam, knife, meal, olives, pepper, piece,
pizza, salt, smell, snack, spoon, strawberry, sugar, taste; `dish` там нет.

**Я сослался на графу, которой не открывал** — та самая порода, которую сам ловлю
у других. Слово при этом законно и без оговорок: Oxford даёт `dish n. A1`,
словник A2 Key держит его и в алфавитной части, и в теме `Food and Drink`.

**И довод, ради которого `dish` был взят, тоже был ложен:** «на `sh` в модуле нет
больше ни одного слова» — было `fish`. **После вывода `fish` из модуля довод стал
верным**, и это совпадение, а не оправдание: правильный порядок был обратный —
сперва проверить, потом писать.

### Задания — семь, лестницей

| № | Вид | Что делает ученик | На каком блоке | Слово |
|---|---|---|---|---|
| 1 | `hottext` | в строке `box, city, day, glass, potato` отмечает слова, у которых во множественном станет на слог больше | случай 8 | показанные |
| 2 | `choice` | выбирает множественное к слову `company`: `companies` · `companys` · `company` | случаи 4, 6 | **не показано** |
| 3 | `gap` | вписывает форму: `Three ___ .` при слове `glass` | случаи 1, 8 | показано |
| 4 | `gap` | вписывает форму: `Three ___ .` при слове `city` | случай 4 | показано |
| 5 | исправить чужую запись | «Алим написал: `four potatos`» → `four potatoes` | случай 3 | показано |
| 6 | исправить чужую запись | «Дана написала: `two boxs and three citys`» → `two boxes and three cities` | случаи 1, 4, 7 | показано |
| 7 | `speak` | называет вслух, что стоит на столе; образец записью — `Two glasses and three dishes.` | случай 7 | — |

**Мерка «все варианты в одном отношении к напечатанному»:**

- **задание 2** — ни одна из трёх строк в уроке не печаталась, слова `company`
  урок не показывает вовсе. Решается правилом, не памятью;
- **задание 6 переведено в производство 6 сентября.** Оно было выбором из трёх
  строк, и второй методист показал, чем оно решалось: верная строка состояла
  только из напечатанных форм, а каждая неверная несла одну ненапечатанную —
  узнавание вело прямо к ответу. Правило починить было нечем, и задание сменило
  ВИД;
- **задания 3, 4, 5, 6** — производство, правило к ним не применяется (раздел 0);
- **задание 1 — `hottext`, и раздел 0 к нему тоже не применяется.** Разбор ниже.

**Второй верный ответ — прогнал по каждому:**

- задание 1: лишний слог появляется у `box` и `glass`; у `city`, `day` и
  `potato` не появляется — по выписке (пункт 26) и по нашим записям. Верных ровно
  два. **Условие обязано звучать «станет НА СЛОГ БОЛЬШЕ», а не «звучит иначе»:**
  иначе верны все пять;
- задание 2: `companies` единственная форма;
- задания 3 и 4: `glasses` и `cities` единственные;
- задание 5: `four potatoes` единственный. **`four potato` в принимаемые не
  кладётся** — это не другой верный ответ, а другая ошибка;
- задание 6: `two boxes and three cities` — единственный ответ. **`two box and
  three city` в принимаемые не кладётся:** это не другой верный ответ, а другая
  ошибка;
- задание 7 машиной не оценивается.

### Все английские строки урока 2 — закрытый перечень

**Сцена:** `Two glasses, please.` · `Glasses?` · `Yes. One glass, two glasses.`

**Примеры:** `a glass — glasses` · `a box — boxes` · `a sandwich — sandwiches` ·
`a dish — dishes` · `a potato — potatoes` · `a tomato — tomatoes` ·
`a city — cities` · `a family — families` · `a day — days` · `a key — keys` ·
`a boy — boys` · `a country — countries` · `a taxi — taxis` · `box — boxes` ·
`glass — glasses` · `city — cities` · `potato — potatoes`

**Таблица:** `a glass` · `glasses` · `a box` · `boxes` · `a sandwich` ·
`sandwiches` · `a dish` · `dishes` · `a potato` · `potatoes` · `a city` ·
`cities` · `a day` · `days`

**Карточки:** `my glass` · `a sandwich and tea` · `my dish` ·
`a potato and an onion` · `a tomato in the box`

**Задания:** `box, city, day, glass, potato` · `companies` · `companys` ·
`company` · `Three ___ .` (при слове `glass`) · `Three ___ .` (при слове `city`) · `four potatos` · `four potatoes` ·
`two boxs and three citys` · `two boxes and three cities` ·
`Two glasses and three dishes.`

**Слова, карточка которых не в этом уроке:** `box`, `key`, `pen`, `tea`
(модуль 1); `city`, `country` (модуль 3); `day` (модуль 4); `family`, `boy`,
`company` (модуль 6); `onion` (урок 1). Четыре из них — `city`, `country`, `day`,
`boy` — сегодня в коде отсутствуют: раздел 15 замысла модуля.

**Звук.** Не звучат: `boxs`, `citys`, `companys`, `four potatos` и `company` в
столбце неверных; русские подписи; столбец «на что смотрим». Звучат: сцена
целиком на два голоса, все примеры, четырнадцать ячеек таблицы, пять карточек,
образец `Two glasses and three dishes.`, строка задания 1.

---

## Урок 3. Свои формы: men, women, children, people

**Итог:** называть несколько людей словом со своей формой и ставить при нём
`are`.

### Вступление: сцена

Дана показывает Алиму снимок семьи.

| Строка | Перевод | Голос |
|---|---|---|
| Dana: My family. | Моя семья. | женский |
| Alim: Two women and one man? | Две женщины и один мужчина? | мужской |
| Dana: Yes. And two children. | Да. И двое детей. | женский |

**Русскую параллель урок 3 НЕ берёт, и это решение, а не забывчивость.** Пара
«человек — люди» ушла в урок 1: редактор поставил её там вместо «яйцо — яйца»,
и второй раз она сказала бы то же самое. Ссылка на урок 1 внутри одной ступени
законна и уместна: «по-русски бывает так же — помнишь „человек — люди"?»

### Восемь случаев, шестнадцать примеров

Счёт сверен разбором перечня строк, а не глазом.

| № | Что разбирается | Примеры |
|---|---|---|
| 1 | `man — men`: внутри слова другая буква | `one man — two men` (один мужчина — двое мужчин) · `The men are here.` (Мужчины здесь.) |
| 2 | `woman — women`: пишется через `o`, а звучит через `i` — /ˈwʊmən/ и /ˈwɪmɪn/ | `one woman — two women` (одна женщина — две женщины) · `The women are here.` (Женщины здесь.) |
| 3 | `child — children`: прибавляется окончание `-ren` | `one child — two children` (один ребёнок — двое детей) · `The children are here.` (Дети здесь.) |
| 4 | `person — people`: слово другое совсем | `one person — two people` (один человек — двое людей) · `Ten people are here.` (Десять человек здесь.) |
| 5 | у этих четырёх слов множественное своё, и его запоминают: правила тут нет | `a man — men` (мужчина — мужчины) · `a child — children` (ребёнок — дети) |
| 6 | число впереди работает как обычно — и у этих слов, и у остальных | `three children` (трое детей) · `three cups` (три чашки) |
| 7 | при одном стоит `is`, при нескольких `are` | `The child is here.` (Ребёнок здесь.) · `The children are here.` (Дети здесь.) |
| 8 | слово `people` само по себе о нескольких, и глагол при нём во множественном | `People are here.` (Люди здесь.) · `Two people and one child.` (Двое людей и один ребёнок.) |

**Общего правила о том, ЧТО именно меняется, в уроке быть не должно** — это
находка круга по уроку 1 (замысел модуля, раздел 14.7). Правило «слово меняется
внутри» верно для двух пар из пяти: у `child` прибавляется окончание, у `person`
слово другое. **Правило урока одно: у этих четырёх слов
множественное своё, и его запоминают.** Каждая пара — свой случай со своим
примером.

**И слово «всегда» в случае 8 запрещено.** Викисловарь: «The word *people* today
takes a plural verb **in most senses**» (`docs/istochniki-norma-yazyka.md`,
пункт 21). Правило говорит о нашем случае, а не обо всём языке.



### Таблица форм

| Об одном | О нескольких |
|---|---|
| a man | men |
| a woman | women |
| a child | children |
| a person | people |

Восемь ячеек, восемь записей. Таблица делает то, чего не сказать словами: четыре
пары стоят рядом, и видно, что общего правила у них нет — у каждой своё.

### Карточки урока — четыре

| Слово | Перевод | Пример карточки | Ступень |
|---|---|---|---|
| `man` | мужчина | `a man in the city` | Oxford A1, YL с. 80 Pre A1 Starters (`man/men`) |
| `woman` | женщина | `a woman in the park` | Oxford A1, YL с. 80 Pre A1 Starters (`woman/women`) |
| `person` | человек | `a person in the street` | Oxford A1, YL с. 80 Pre A1 Starters (`person/people`) |
| `people` | люди | `people in the city` | Oxford A1, тот же список |


**Опора двойная и названа в шапке модуля:** Young Learners, с. 29 («including
irregular plural forms», «Pat has six mice») и с. 80, список `Family & friends`,
колонка Pre A1 Starters, где пары стоят целиком — `child/children`, `man/men`,
`person/people`, `woman/women`.

**Слова `city`, `park`, `street` — карточки модуля 3.** Ни один пример карточки
не повторяет строки урока.

**`people` берёт карточку рядом с `person`, а не вместо неё** — иначе случай 4
показывает пару, половина которой ученику не дана.

### Задания — семь, лестницей

| № | Вид | Что делает ученик | На каком блоке |
|---|---|---|---|
| 1 | `hottext` | в строке `man, cup, child, woman` отмечает слова, у которых множественное своё | случаи 1–3, 5, 6 |
| 2 | исправить чужую запись | «Алим написал: `two mans`» → `two men` | случай 1 |
| 3 | `gap` | `One child, two ___ .` → `children` | случай 3 |
| 4 | `gap` | `One woman, three ___ .` → `women` | случай 2 |
| 5 | исправить чужую запись | «Дана написала: `The children is here.`» → `The children are here.` | случай 7 |
| 6 | `gap` | `Ten ___ are here.` при слове `person` → `people` | случаи 4, 8 |
| 7 | `speak` | называет вслух, сколько людей в его семье; образец записью — `Four people. Two men and two women.` | случаи 4, 6 |

**Задание 2 возвращено 6 сентября вечером, и вот чем оно куплено.** Сборщик снял
его, разводя однообразие, и назвал цену честно: **форму `men` ученик не
производит нигде** — она стоит примером, парой, ячейкой таблицы и образцом
говорения. Слово `men` стоит первым в названии урока; итог обещает называть
несколько людей словом со своей формой. Умение дороже строя, и заданий в уроке 3
снова семь.

**Мерка «все варианты в одном отношении к напечатанному».** Задание 2 —
единственное с выбором, и все три строки собраны из показанных кусков: `The
children are here.` стоит примером случая 3 и 7, `The children is here.` и `The
child are here.` — нет. **Отношение неодинаково, и я это называю прямо: здесь
правило нарушено, и починить его в уроке 3 нечем.** Слов со своей формой в курсе
ровно пять, все пять урок показывает, взять неказанное слово неоткуда.

**Что делаю вместо починки:** задание переводится в производство — исправить
чужую запись «Дана написала: `The children is here.`» → `The children are here.`
В порядке 6 сентября (вечера) оно стоит ПЯТЫМ Тогда мерка к нему не применяется (раздел 0), а предмет остаётся тот
же. **Итого заданий с выбором в уроке 3 нет ни одного**, и это честнее, чем
выбор, решаемый памятью.

**Второй верный ответ:**

- задание 1: своя форма у `man`, `child`, `woman`; у `cup` нет. Верных ровно
  три. **Строка пересобрана 6 сентября:** первая редакция ставила туда `box` из
  чужого урока, и верные отличались от неверных не языком, а тем, из какого они
  урока. Теперь все четыре слова напечатаны в СВОЁМ уроке — `cup` стоит примером
  случая 6, — и признак остаётся один: какое у слова множественное;
- задание 3: `children` единственное;
- задание 4: `women` единственное;
- задание 2: `two men` единственный ответ;
- задание 6: `people` верный ответ, **и `persons` КЛАДЁТСЯ В ПРИНИМАЕМЫЕ.**
  Первая редакция писала «не кладётся, но и отвлекающим не берётся» — это
  полумера: форма законна, ученик, написавший её, прав, и молчание принимаемых
  наказало бы его за правоту;
- задание 5: `The children are here.` единственный ответ.

### Все английские строки урока 3 — закрытый перечень

**Сцена:** `My family.` · `Two women and one man?` · `Yes. And two children.`

**Примеры:** `one man — two men` · `The men are here.` ·
`one woman — two women` · `The women are here.` · `one child — two children` ·
`The children are here.` · `one person — two people` · `Ten people are here.` ·
`a man — men` · `a child — children` · `three children` · `three cups` ·
`The child is here.` · `People are here.` · `Two people and one child.`

**Таблица:** `a man` · `men` · `a woman` · `women` · `a child` · `children` ·
`a person` · `people`

**Карточки:** `a man in the city` · `a woman in the park` ·
`a person in the street` · `people in the city`

**Задания:** `man, cup, child, woman` · `The children is here.` ·
`The children are here.` · `One child, two ___ .` · `One woman, three ___ .` ·
`two mans` · `two men` · `Ten ___ are here.` ·
`Four people. Two men and two women.`

**Слова, карточка которых не в этом уроке:** `family`, `child` (модуль 6);
`city`, `park`, `street`, `here`, `the` (модуль 3); `cup` (урок 1); `box`
(модуль 1); `rice` (урок 4 — **осторожно, идёт ПОЗЖЕ**).

**Слово `fish` из модуля 8 выведено целиком — правка 6 сентября.** Оно кончается
на `sh`, и правило урока 2 давало бы `fishes`, а множественное у него своё:
урок 2 опровергался бы уроком 3 внутри одного модуля. Карточка уходит **модулю
22 «Еда и кафе»** — тема `Food and Drink` объявлена и у него.

**Строка, которую должен увидеть модуль 22:**

> Карточка `fish` свободна: модуль 8 её не берёт, потому что слово кончается на
> `sh` и опровергает правило его урока 2. Oxford ставит `fish n. A1`, словник
> A2 Key держит слово в теме `Food and Drink`, Young Learners, с. 80, — колонка
> Pre A1 Starters. Множественное у него совпадает с единственным, а `fishes` не
> ошибка (`docs/istochniki-norma-yazyka.md`, пункт 20).

**И перечень строк нашёл до этого ещё одно, помельче:** пример карточки `fish`
был `fish and rice`, а `rice` приходит карточкой только уроком позже. Обе находки
сделаны не чтением, а сверкой перечня — сам по себе он не читается.

**Звук.** Не звучат: `The children is here.`, `The child are here.`, `two mans`;
русские подписи. Звучат: сцена на два голоса, шестнадцать примеров, восемь ячеек
таблицы, четыре карточки, образец говорения, строка задания 1.

---

## Урок 4. Когда называют саму еду: rice, bread, milk

**Итог:** называть саму еду без `a` и без `-s`.

### Вступление: русская фраза

Здесь русская параллель полная, и потому урок начинается ею, а не сценой:
по-русски словами «рис», «хлеб», «молоко» называют саму еду. По-английски так же,
и слово тогда стоит голым — без артикля и без окончания.

**Правка 6 сентября: вступление переписано с отрицания на утверждение.** Первая
редакция открывала урок словами «по-русски не говорят „два риса"» — определение
через отрицание, один из трёх признаков машинного объяснения. И оно спорило с
моим же разделом 7 замысла модуля, где `two rice` взято настоящим отвлекающим
именно потому, что по-русски «два риса» в кафе говорят.

**Редактору: параллель ставится ДО английского**, и английская строка приходит
после неё.

### Восемь случаев, шестнадцать примеров

| № | Что разбирается | Примеры |
|---|---|---|
| 1 | когда называют саму еду, впереди не ставят `a` | `This is milk.` (Это молоко.) · `This is bread.` (Это хлеб.) |
| 2 | и окончания `-s` у неё нет | `Milk and bread.` (Молоко и хлеб.) · `Rice and meat.` (Рис и мясо.) |
| 3 | при таком слове стоит `is` | `Milk is food.` (Молоко — еда.) · `Rice is food.` (Рис — еда.) |
| 4 | ответ на вопрос — одно слово без артикля | `What is this? — Milk.` (Что это? — Молоко.) · `What is this? — Cheese.` (Что это? — Сыр.) |
| 5 | рядом с предметом разница видна сразу | `an egg and bread` (яйцо и хлеб) · `an apple and milk` (яблоко и молоко) |
| 6 | вода ведёт себя так же | `This is water.` (Это вода.) · `Water is here.` (Вода здесь.) |
| 7 | и сыр с мясом | `This is cheese.` (Это сыр.) · `Meat and rice.` (Мясо и рис.) |
| 8 | в списке такие слова стоят просто именем, а предметы — с числом | `Bread, milk, rice.` (Хлеб, молоко, рис.) · `Three apples and cheese.` (Три яблока и сыр.) |

**Ни слова о том, чего с этими словами «не бывает».** Числа перед ними в
английском стоят — `two coffees` тому доказательство
(`docs/istochniki-norma-yazyka.md`, пункт 22), — и запрещать это курс не вправе.
Разница показывается на том, что в модуле ЕСТЬ.

**Правило показывается на шести словах из тринадцати** — `rice`, `bread`, `milk`,
`water`, `meat`, `cheese`: у них порционное употребление редкое. Слова `coffee`,
`juice`, `soup`, `salad`, `food`, `fruit`, `chocolate`, `vegetable` в модуле есть
карточками, но правило на них не показывается ни разу.

**Слово «неисчисляемое» ученику не говорится** (замысел модуля, раздел 4.1).

**Проверка правил своими примерами:** правило 1 («не ставят `a`») показано на
`milk` и `bread` — у обоих `a` вправду нет; правило 3 («стоит `is`») — на `Milk
is food.` и `Rice is food.`, оба с `is`. Расхождений нет.

### Таблица: что отвечают на `What is this?`

| Предмет — с артиклем | Еда — без артикля |
|---|---|
| An apple. | Milk. |
| An egg. | Bread. |
| A cup. | Rice. |
| A bottle. | Water. |

Восемь ячеек, восемь записей. Таблица делает то, чего не сделать словами: два
ответа на один вопрос стоят рядом, и разница видна глазом, а не из объяснения.

### Карточки урока — девять

| Слово | Перевод | Пример карточки |
|---|---|---|
| `rice` | рис | `rice in the dish` |
| `milk` | молоко | `milk in the bottle` |
| `water` | вода | `water in the glass` |
| `meat` | мясо | `meat and potatoes` |
| `cheese` | сыр | `cheese and tomatoes` |
| `coffee` | кофе | `coffee, please` |
| `juice` | сок | `juice in the glass` |
| `soup` | суп | `soup and bread` |
| `food` | еда | `food in the box` |

**Все девять — Oxford A1 и словник A2 Key.** По тематическому списку Young
Learners, с. 80: `rice`, `milk`, `water`, `meat`, `food` — Pre A1 Starters;
`cheese`, `coffee`, `soup` — A1 Movers; `juice` — Pre A1 Starters.

**Ни один пример карточки не повторяет строки урока** — проверено против
шестнадцати примеров и восьми ячеек таблицы. Слова примеров: `bottle`, `glass` (урок 1 и урок 2), `potatoes`, `tomatoes` (урок 2), `bread`
(модуль 1), `please` (модуль 1), `box` (модуль 1), `in`, `the` (модуль 3).

**Девять карточек в одном уроке — много.** Если сводящий сочтёт перебором,
`juice` и `soup` уходят в урок 5: правило на них всё равно не показывается.

### Задания — семь, лестницей

| № | Вид | Что делает ученик | На каком блоке |
|---|---|---|---|
| 1 | `hottext` | в строке `an apple, milk, three eggs, bread` отмечает то, где называют саму еду | случаи 1, 5 |
| 2 | `choice` | выбирает, **в какой строке говорят, ЧТО ЭТО ЗА ЕДА**: `This is cheese.` · `This is a cup.` · `These are eggs.` | случаи 1, 4, 5 |
| 3 | `gap` | при картинке хлеба: `What is this? — ___ .` → `Bread.` | случаи 1, 4 |
| 4 | `order` | собирает `Rice is food.` из `is` · `Rice` · `food` | случай 3 |
| 5 | разложить по двум столбцам | **строки, а не слова**: `an apple` · `milk` · `three eggs` · `bread` · `a cup` · `water` — где слово стоит с артиклем или числом, а где голым | случай 5 |
| 6 | исправить чужую запись | «Алим написал: `Milk are food.`» → `Milk is food.` | случай 3 |
| 7 | `speak` | называет вслух три еды из тех, что стоят в уроке голыми; образец записью — `Bread, milk and cheese.` | случай 8 |

**Все три задания на исправление сняты, кроме одного, и это правка по существу.**
Второй методист показал, что `This is a bread.`, `Rices are food.` и
`Cheeses are food.` — строки, которые модуль сам объявил правильным английским:
раздел 7 замысла снял `a bread` из ОТВЛЕКАЮЩИХ именно потому, что она законна, а
задание ставило её на место ошибки, которую ученик обязан исправить. Это хуже
отвлекающего: там строку можно было не выбрать, здесь ученику прямо говорят, что
так писать нельзя. Викисловарь даёт `cheese` как countable/uncountable с формой
`cheeses`; `rices` — то же самое о сортах.

**Осталась одна строка на исправление — `Milk are food.`**, и она неверна при
любом чтении: голое слово о еде глагола во множественном не берёт ни как еда, ни
как порция (порция была бы `Two milks are…`, и там слово стоит с числом).

**Мерка «все варианты в одном отношении к напечатанному».** Задание 2 —
единственное с выбором, и оно переустроено: **все три строки правильны
по-английски, а выбирают по СМЫСЛУ** — где говорят, что это за еда. Тогда мерка
исполняется сама собой: ни одна строка не может быть «неверной», и узнавать
нечего.

**Второй верный ответ:**

- задание 1: саму еду называют `milk` и `bread`; `an apple` и `three eggs` — нет.
  Верных ровно два;
- задание 2: **условие переписано 6 сентября вечером — прежнее давало два верных
  ответа.** Оно звучало «отметь строку, в которой говорят, ЧТО ЭТО ЗА ЕДА», а
  яйца — тоже еда, и ученик, отметивший `They are eggs.`, был бы прав. Условие
  теперь называет разницу прямо: «В двух строках названо то, что считают
  штуками, а в одной — сама еда. Отметь строку, где названа сама еда.» После
  этого верный один: `This is cheese.`, и неверные неверны **по условию**, а не
  по языку;
- задание 3: `Bread.` верный ответ. **В принимаемые кладутся `It is bread.` и
  `This is bread.`** — оба правильные и полные ответы на тот же вопрос, и
  наказывать за них нельзя. Вторая строка при этом дословно повторяет пример
  случая 1, и это законно: она не ЕДИНСТВЕННЫЙ верный ответ, а один из трёх;
- задание 4: `Rice is food.` — единственный осмысленный порядок трёх слов;
- задание 5: три и три, других разложений нет. **Раскладываются СТРОКИ, а не
  слова** — правка 6 сентября: рамка «что считают штуками, а что нет» была снята
  разделом 5.2 замысла модуля ещё утром, а в задании уцелела. Строка `milk`
  голая, строка `three eggs` с числом — это про то, чему учит урок, и спорных
  слов тут нет вовсе;
- задание 6: `Milk is food.` единственный ответ;
- задание 7 машиной не оценивается.

### Все английские строки урока 4 — закрытый перечень

**Примеры:** `This is milk.` · `This is bread.` · `Milk and bread.` ·
`Rice and meat.` · `Milk is food.` · `Rice is food.` · `What is this? — Milk.` ·
`What is this? — Cheese.` · `an egg and bread` · `an apple and milk` ·
`This is water.` · `Water is here.` · `This is cheese.` · `Meat and rice.` ·
`Bread, milk, rice.` · `Three apples and cheese.`

**Таблица:** `An apple.` · `Milk.` · `An egg.` · `Bread.` · `A cup.` · `Rice.` ·
`A bottle.` · `Water.`

**Карточки:** `rice in the dish` · `milk in the bottle` · `water in the glass` ·
`meat and potatoes` · `cheese and tomatoes` · `coffee, please` ·
`juice in the glass` · `soup and bread` · `food in the box`

**Задания — перечень пересобран 6 сентября вечером РАЗБОРОМ собранного урока,
а не глазом.** Прежний остался от снятой редакции: он держал `Cheese is food.`,
`A cheese is food.`, `Cheeses are food.`, `This is a bread.` и `Rices are food.` —
все пять сняты вместе с тремя исправлениями, — и не знал новых строк задания 2.

`an apple` · `milk` · `three eggs` · `bread` · `a cup` · `water` ·
`This is a cup.` · `They are eggs.` · `This is cheese.` ·
`What is this? — ___` · `Bread.` · `It is bread.` · `This is bread.` · `bread` ·
`is` · `Rice` · `food.` · `Rice is food.` · `Milk are food.` · `Milk is food.` ·
`Bread, milk and cheese.`

**Слова, карточка которых не в этом уроке:** `this`, `what`, `it` (модуль 7);
`is`, `are` (модуль 2); `here`, `in`, `the` (модуль 3); `bread`, `box`, `please`
(модуль 1); `apple`, `egg`, `cup`, `bottle` (урок 1); `glass`, `potatoes`,
`tomatoes` (урок 2). **Ни одного слова из уроков 5–8** —
проверено перечнем.

**Звук.** Не звучат: `This is a bread.`, `Rices are food.`, `A cheese is food.`,
`Cheeses are food.`; русские подписи и заголовки столбцов. Звучат: шестнадцать
примеров, восемь ячеек таблицы, девять карточек, образец говорения, строка
задания 1 и строка задания 5.

---

## Урок 5. Когда их несколько рядом: these apples

**Итог:** ставить `these` перед словом во множественном числе.

### Вступление: сцена

Алим спрашивает про то, что лежит на виду.

| Строка | Перевод | Голос |
|---|---|---|
| Alim: What is this? | Что это? | мужской |
| Dana: An apple. | Яблоко. | женский |
| Alim: And these? | А эти? | мужской |
| Dana: Apples. Six apples. | Яблоки. Шесть яблок. | женский |

`pervyyGolos: "muzhskoy"` — первым говорит Алим.

**Строка `And these?` — это `these` вместо существительного, и ученик её только
СЛЫШИТ и ЧИТАЕТ.** Разбор и обе строки для шапки — замысел модуля, раздел 3.4.
Ни одно задание урока не требует построить `these` в одиночку.

### Восемь случаев, пятнадцать примеров

Счёт сверен разбором перечня строк: у случая 1 обе строки — пара `this apple` и
`these apples`, и второй раз они же стоят в таблице форм.

| № | Что разбирается | Примеры |
|---|---|---|
| 1 | по-русски у слова «этот» тоже две формы — «этот» и «эти»; по-английски их тоже две | `this apple` (это яблоко) · `these apples` (эти яблоки) |
| 2 | `this` — про одну вещь рядом | `this egg` (это яйцо) · `this cup` (эта чашка) |
| 3 | `these` — про несколько рядом | `these eggs` (эти яйца) · `these cups` (эти чашки) |
| 4 | `these` не меняет слово: окончание всё равно нужно | `these apples` (эти яблоки) · `these carrots` (эти морковки) |
| 5 | при `these` стоит `are` | `These apples are here.` (Эти яблоки здесь.) · `These cups are here.` (Эти чашки здесь.) |
| 6 | слова со своей формой работают так же | `these children` (эти дети) · `these men` (эти мужчины) |
| 7 | написание `-es` и `-ies` работает так же | `these boxes` (эти коробки) · `these cities` (эти города) |
| 8 | ответ о нескольких вещах рядом | `What are these? — Apples.` (Что это? — Яблоки.) · `What are these? — Eggs.` (Что это? — Яйца.) |

**Случай 1 стоит первым нарочно.** После снятия `those` случаев оставалось семь,
и восьмым встал не добор, а то, с чего урок и должен начинаться: у русского
«этот — эти» две формы, и `this — these` ложится на них слово в слово. **Это
единственное место модуля, где английское устройство СОВПАДАЕТ с русским**, и
сказать об этом стоит прямо: остальные четыре урока говорят о расхождениях.

**Слова `those` в уроке нет ни разу** — у него нет опоры Cambridge вовсе,
разбор в замысле модуля, раздел 3.3, вопрос владельцу — раздел 8, строка 9.

**Строки случая 8 ученик читает и слышит, но не строит** — это то же условие, что
у сцены.

**Случай 6 обязан сказать про ЛЮДЕЙ вслух — правка 6 сентября вечером.** Урок 5
зовёт то, о чём говорит, «вещью» (решение C, шапка модуля), а строки случая 6 —
`these children` и `these men`. Правило случая 6 говорит: то же самое работает и
о людях. Без этой строки правило уже своих примеров, и это порода 14.7.

**Проверка правил своими примерами:** правило 4 («окончание всё равно нужно»)
показано на `these apples` и `these carrots` — у обоих окончание стоит; правило 7
(«написание работает так же») — на `these boxes` и `these cities`, то есть на
обоих написаниях урока 2, а не на одном.

### Таблица форм

| Об одном | О нескольких |
|---|---|
| this apple | these apples |
| this egg | these eggs |
| this box | these boxes |
| this child | these children |

Восемь ячеек, восемь записей.

### Карточки урока — пять

| Слово | Перевод | Пример карточки |
|---|---|---|
| `grape` | виноградина | `a grape` |
| `vegetable` | овощ | `a vegetable in the box` |
| `fruit` | фрукты | `fruit and water` |
| `salad` | салат | `salad and bread` |
| `chocolate` | шоколад | `chocolate and milk` |

**`grape` — Oxford слова не знает вовсе**, и по правилу программы такие слова в
знаменатель нормы не входят и не запрещаются. Оно нужно потому, что на нём стоит
образец Cambridge «These are grapes.» (Young Learners, с. 21); тематический
список, с. 80, ставит `grape` в колонку Pre A1 Starters.

**Пример карточки `grape` — `a grape`, и это нарочно:** форма `grapes` в задании
2 не должна быть напечатана раньше него (мерка 14.1).

### Задания — семь, лестницей

| № | Вид | Что делает ученик | На каком блоке |
|---|---|---|---|
| 1 | `hottext` | в строке `this apple, these grapes, this cup, these boxes` отмечает то, где говорят о нескольких | случаи 2, 3 |
| 2 | `choice` | на столе несколько виноградин; выбирает подпись: `these grapes` · `this grapes` · `these grape` | случаи 3, 4 |
| 3 | `gap` | `___ vegetables are here.` → `These` | случаи 3, 5 |
| 4 | `order` | собирает `These children are here.` из `are` · `These` · `children` · `here` | случаи 5, 6 |
| 5 | исправить чужую запись | «Алим написал: `these box`» → `these boxes` | случаи 4, 7 |
| 6 | `gap` | `this apple — ___ apples` → `these` | случай 1 |
| 7 | `speak` | показывает на две вещи рядом и называет их; образец записью — `These books are here.` | случаи 3, 5 |

**Мерка «все варианты в одном отношении к напечатанному».** Задание 2 —
единственное с выбором, и ни одна из трёх строк не печаталась: слово `grape` в
уроке стоит только карточкой и только в единственном числе. Правило исполнено
вторым способом.

**Второй верный ответ:**

- задание 1: о нескольких говорят `these grapes` и `these boxes`. Верных ровно
  два;
- задание 2: `these grapes` единственный;
- задание 3: `These` верный ответ. **В пропуск встают и `The`, и `My`, и `Two` —
  все три дают правильный английский**, и одним условием их не отсечь. Значит
  условие называет прямо: «овощи лежат прямо перед тобой, и ты показываешь на
  них», — а `The`, `My` и `Two` кладутся в принимаемые. Наказывать за
  правильную строку нельзя, а условие тут сужает не до одного ответа, а до
  четырёх верных;
- задание 4: `These children are here.` — единственный порядок четырёх слов,
  дающий осмысленную строку;
- задание 5: `these boxes` единственный;
- задание 6: `these` единственное;
- задание 7 машиной не оценивается. **Образец `These books are here.` —
  определитель перед словом, а не `these` в одиночку:** правило раздела 3.4
  держится и в образце.

### Все английские строки урока 5 — закрытый перечень

**Сцена:** `What is this?` · `An apple.` · `And these?` · `Apples. Six apples.`

**Примеры:** `this apple` · `these apples` · `this egg` · `this cup` ·
`these eggs` · `these cups` · `these carrots` · `These apples are here.` ·
`These cups are here.` · `these children` · `these men` · `these boxes` ·
`these cities` · `What are these? — Apples.` · `What are these? — Eggs.`

**Таблица:** `this apple` · `these apples` · `this egg` · `these eggs` ·
`this box` · `these boxes` · `this child` · `these children`

**Карточки:** `a grape` · `vegetables in the box` · `fruit and water` ·
`salad and bread` · `chocolate and milk`

**Задания:** `this apple, these grapes, this cup, these boxes` · `these grapes` ·
`this grapes` · `these grape` · `___ vegetables are here.` ·
`These children are here.` · `these box` · `these boxes` ·
`this apple — ___ apples` · `These books are here.`

**Слова, карточка которых не в этом уроке:** `this`, `what` (модуль 7); `are`
(модуль 2); `here`, `in`, `the`, `city` (модуль 3); `book`, `box` (модуль 1);
`apple`, `egg`, `cup`, `carrot` (урок 1); `child`, `man` (урок 3 и модуль 6);
`water`, `milk`, `bread` (урок 4 и модуль 1). **Ни одного слова из уроков 6–8.**

**Звук.** Не звучат: `this grapes`, `these grape`, `these box`; русские подписи.
Звучат: сцена на два голоса, пятнадцать примеров, восемь ячеек таблицы, пять
карточек, образец `These books are here.`, строка задания 1.

---

## Урок 6. Читаем список у входа

**Вид:** `chtenie`, `genre: "notice"`. **Итог:** находить в списке, что там есть
и чего сколько.

**Опора — CEFR Companion, с. 56**, Reading for orientation, **A1**: «Can
recognise familiar names, words/signs and very basic phrases on simple notices in
the most common everyday situations».

**Формат заданий — Young Learners, с. 22**, Pre A1 Starters Reading and Writing
Part 4. Слово «дословно» из первой редакции снято: там ученик СПИСЫВАЕТ слово из
рамки, а у нас образует. Берётся **совет учителю**: «help them to identify words
or grammatical forms that will indicate whether an answer should be plural or
not, e.g. **if a gap is preceded by a**». Отсюда устройство нашего пропуска:
перед пропуском стоит `a` или число, и по нему ученик решает форму.

### Текст объявления

```
FOOD
two sandwiches
soup and salad
three cakes
apples and grapes
tea and milk
coffee and juice
fruit and chocolate
```

**Две последние строки дописаны 6 сентября вечером, и это не добор.** Оговорка 2
раздела 5.4 замысла модуля обещает, что слова урока 5 и лишние слова урока 4
работают в тексте для чтения; на деле `coffee`, `juice`, `fruit` и `chocolate` не
встречались в модуле нигде, кроме собственных карточек. Список еды у входа —
ровно то место, где эти четыре слова работают сами собой, а не в сочинённой ради
них строке. Задания 1, 5 и 6 от прибавки не меняются: числа в новых строках нет,
значит верных ответов у задания 5 по-прежнему два.

Все слова даны: `food` (урок 4), `sandwich` (урок 2), `soup`, `salad`, `milk`
(уроки 4 и 5), `cake`, `apple` (урок 1), `grape` (урок 5), `tea` (модуль 1),
`and` (модуль 6), числа (модуль 4).

**Заголовок был `FOOD TODAY`, и слово `today` снято.** Карточки у него нет ни у
кого — проверил по коду и по замыслам модулей 2–7. **Это находка перечня строк, а
не чтения:** заголовок я написал не глядя, и увидел это, только выписав все
строки урока подряд.

**Строк с артиклем `a` в тексте нет ни одной, и это нарочно:** пропуск с `a`
появляется только в задании, где он и работает по совету источника.

### Задания — шесть

| № | Вид | Что делает ученик | На чём стоит |
|---|---|---|---|
| 1 | `choice` | чего в списке несколько: `sandwiches` · `soup` · `tea` | текст |
| 2 | `gap` | отвечает числом: сколько в списке пирожных | текст |
| 3 | `gap` | вставляет форму в строку `a ___` при слове `cake` | совет с. 22 |
| 4 | `hottext` | отмечает в тексте всё, что стоит С ЧИСЛОМ | уроки 1, 2 |
| 5 | `choice` | какой строки в списке НЕТ: `four apples` · `soup and salad` · `tea and milk` | текст |

**Заданий стало пять: прежнее четвёртое снято 6 сентября вечером.** Оно просило
вписать форму в строку `three ___` при слове `glass` — то есть делало ровно то
же, что задания 3 и 4 урока 2, той же рамкой «число плюс пропуск». Смена слова
близнеца не снимает. Совет источника (с. 22: «if a gap is preceded by a») держит
задание 3, и держит его сильнее: там ученик, наоборот, СНИМАЕТ окончание, потому
что в тексте стоит `three cakes`, а в пропуске перед ним `a`.

**Мерка «все варианты в одном отношении к напечатанному».** Задания 1 и 6 — с
выбором, и в обоих все три строки взяты из самого текста либо построены из его
слов: в задании 1 все три слова стоят в списке, в задании 6 две строки стоят
дословно, а третья построена из слов списка. Отношение одинаково, решается
чтением — а чтение тут и есть предмет.

**Задания 4 и 5 переустроены 6 сентября.**

- **Задание 4** просило форму слова `sandwich`, а `sandwiches` напечатано в самом
  объявлении на том же экране: ученик списывал, а не образовывал. Взято слово
  `glass` — оно из урока 2, в объявлении его нет, а развилка та же. Задание 3
  (`a ___` при слове `cake`) устроено верно и остаётся: там ответ единственное
  число, и текст его не содержит.
- **Задание 5 стояло на снятой рамке.** Оно просило отметить, «что не считают
  штуками», а `soup`, `salad` и `tea` — ровно те слова, которые я сам вывел из
  показа правила, потому что их считают порциями. Пятым верным был бы и заголовок
  `FOOD`. **Теперь ученик отмечает то, что стоит С ЧИСЛОМ** — `two sandwiches` и
  `three cakes`, — и спорных слов в задании нет ни одного.

**Второй верный ответ:** в задании 1 несколько только у `sandwiches` — `soup` и
`tea` стоят без числа и без окончания. В задании 5 верных ровно два. В задании 6
нет только `four apples`.

**Граница с модулем 24 «Вывески, этажи, указатели»:** он ищет в вывеске место,
время и цену (CEFR с. 56, Pre-A1 — «places, times and prices»), модуль 8 — что и
сколько. **Граница с модулем 22 «Еда и кафе»:** меню и заказ его, слов `menu`,
`meal`, `main course`, `dessert` в модуле 8 нет ни разу. **Список у входа не
меню:** в нём нет ни цен, ни заказа, ни разделов.

---

## Урок 7. Слушаем, чего сколько

**Вид:** `slushanie`. **Итог:** записывать со слуха, сколько названо вещей и
в какой форме стоит слово.

**Опора — Young Learners, с. 9**, Pre A1 Starters: «CAN understand some very
simple spoken descriptions of everyday objects – such as **how many**, colour,
size or location». **Формат ответа — A2 Key, с. 29:** «The candidates should only
write ONE word, or a number, or a date, or a time for their answer».

### Пять записей и пять вопросов

| № | Запись | Что спрашивают | Ответ |
|---|---|---|---|
| 1 | `How many apples are there? — Three.` | сколько яблок | `3` |
| 2 | `Two sandwiches and one cake.` | сколько бутербродов | `2` |
| 3 | `These are eggs. Six eggs.` | сколько яиц | `6` |
| 4 | `This is a sandwich.` **или** `These are sandwiches.` | об одном говорят или о нескольких | картинка, см. ниже |
| 5 | `Bread, milk and four apples.` | сколько яблок | `4` |

**Записи 1 и 3 — разговоры**, границу реплик задаёт тире, число голосов — поле
`voice`. Записи 2, 4 и 5 читает один голос.

**Вопрос 4 переписан против первой редакции, и переписан дважды.** Первая
редакция спрашивала «об одной вещи говорят или о нескольких», а ответом было бы
русское слово — формат Cambridge такого не допускает. Второй ход был «пусть
пишет число» — но числа в записи нет, и придумывать его ученик не должен.
**Верное устройство: `choice` с ТРЕМЯ картинками** — один бутерброд, два и
четыре, — и ученик отмечает ту, что слышит. Двух мало: вопрос угадывается в
половине случаев, а у Cambridge в этой части три. Отвлекающим тут служит
картинка, а не строка, — значит породы «выдуманный неверный вариант» здесь нет
вовсе.

**Цифра сама по себе звука не получает** — прогон модуля 4 показал, что цифровая
строка уходит синтезу обычным текстом. В записях числа записаны словами; ответы
ученика — цифрами, и в `accept` у каждого стоит и цифра, и слово (правило модуля
4 по Young Learners, с. 16: «can be written as digits or words»).

**Слово `there` в записи 1 приходит готовым** — это формула модуля 4
`How many … are there?`, и ученик её не строит, а отвечает числом.

---

## Урок 8. Говорим о том, что на картинке

**Заголовок:** «Говорим, что это и сколько». **Вид:** `govorenie`. **Итог:**
выбирать форму слова, отвечая на вопрос о числе вещей.

**Заголовок и итог переписаны 6 сентября вечером, и вместе с ними — устройство
всего урока.** Слов «на картинке» в них больше нет, и вот почему.

**ЧИСЛО НАЗЫВАЮТ СЛОВА, А НЕ ЗНАЧОК.** Поле `znak` рисует ОДИН значок 72×72
(`components/lesson/TaskCard.tsx`, строки 158–167 — смотрел код, а не соседнее
место). Значит «на картинке четыре чашки» ученик прочтёт, а увидит одну чашку, и
это прямое расхождение того, что он видит, с тем, что он читает. Мой замысел
просил картинку в каждом задании урока 8 и в двух вопросах работы; ни одна из
них не строится.

Отсюда правило для всего модуля: **значок стоит ПРЕДМЕТОМ разговора, а сколько
их — говорит запись или условие словами.** Условия заданий 1, 3 и 4 называют,
что стоит на столе, и число в них стоит словом; значок остаётся картинкой
предмета.

**У УРОКА ПОЯВЛЯЮТСЯ ДВА БЛОКА МАТЕРИАЛА** — до правки их не было ни одного, и
`kontrol` говорил об этом четырежды: «задание проверяет то, чего в уроке нет».

1. `example` `obrazec-dvuh-voprosov`, знак `1f4e6`, `razgovor: true`, звучит на
   два голоса — дословный образец источника (Young Learners, с. 27):

   | Строка | Перевод |
   |---|---|
   | What is this? — A box. | Что это? — Коробка. |
   | How many boxes are there? — Three. | Сколько там коробок? — Три. |

   **Предмет взят другой, чем в заданиях, нарочно:** заданиям 2 и 3 отвечать про
   чашку, и печатать их ответы образцом нельзя. Слово `box` — карточка модуля 1,
   форма `boxes` разобрана уроком 2.

2. `note` — правило одной строкой: на вопрос «что это» отвечают названием, а
   впереди ставят артикль; на вопрос «сколько» отвечают числом, и у названия в
   самом вопросе стоит окончание. Пишет редактор.

**Опора — Young Learners, с. 27**, Pre A1 Starters Speaking: «Candidates should
practise answering simple questions about a picture (with one-word answers). For
example: **What's this? (plate) How many plates are there? (four)**». Это и есть
предмет урока: вопросы о картинке.

**Строение — A2 Key, с. 42**, Speaking Part 1: «The interlocutor asks **two
short-answer questions** to each candidate … followed by **one longer** „Tell me
something about …" question».

**Опоры переставлены 6 сентября по разбору второго методиста.** Первая редакция
ставила первой A2 Key, с. 42, а тамошний длинный вопрос — про быт, интересы и
что нравится; про картинку там нет ничего. Ступень дела задаёт Young Learners,
с. 27, а A2 Key даёт только число и порядок вопросов.

| № | Вид | Что делает ученик | Оценивается машиной |
|---|---|---|---|
| 1 | `choice` по записи | условие называет: на столе три апельсина. Слышит `How many oranges are there?` и выбирает подпись: `Three oranges.` · `Three orange.` · `An orange.` | **да — это и есть проверяемая половина** |
| 2 | `speak` | условие называет: на столе одна чашка. Отвечает на `What is this?`; образец записью — `A cup.` | нет |
| 3 | `speak` | условие называет: чашек на столе четыре. Отвечает на `How many cups are there?`; образец записью — `Four.` | нет |
| 4 | `speak` | длинный ответ: условие называет, что на столе три яблока, два яйца и молоко; ученик называет это одним рядом; образец записью — `Three apples, two eggs and milk.` | нет |

**Число в каждом условии названо СЛОВОМ, и это не украшение.** До правки задание
1 не давало ученику узнать, что апельсинов три, ни из чего: значок показывает
один апельсин, а в записи числа нет. Строка `Three oranges.` выбиралась
отбрасыванием двух других, а не по числу. Теперь число даёт условие, и выбор
проверяет ровно то, что обещает итог, — форму слова при числе.

**Задание 1 дописано по разбору собранного урока 1** (замысел модуля, раздел 4):
пока в уроке одни `speak`, итог «выбирать форму слова» проверять нечем, а
программа требует называть итог урока говорения по проверяемой половине.

**Мерка «все варианты в одном отношении к напечатанному»:** ни одна из трёх строк
задания 1 в модуле не печаталась. Слово `orange` во множественном стоит ячейкой
таблицы урока 1 (`oranges`), но целой строки с ним нет ни одной.

**Второй верный ответ:** `Three oranges.` единственный. `Three orange.` —
забытое окончание, настоящая ошибка; `An orange.` — ответ на другой вопрос («что
это», а не «сколько»), и её ученики дают постоянно.

**`A three oranges.` из первой редакции снято:** такой строки не пишет никто, и
ни один из восьми настоящих отвлекающих раздела 7 замысла модуля на неё не
похож. Проверка «представь ученика, который так ошибается» её не проходит.

**`vid: "govorenie"` ставится явно:** в уроке будут блоки `audio` с
записями-образцами, а вывод по составу отдаёт такой урок в `slushanie`.

---

## Что этот замысел проверил и чего не проверил

**Проверено:**

- **закрытый перечень английских строк у каждого из уроков 2–5** и у текста
  урока 6. Он нашёл две вещи, которых чтением я не видел: пример карточки `fish`
  тянул слово `rice` из БУДУЩЕГО урока 4, а заголовок объявления тянул `today`,
  которого нет ни у кого. Обе строки исправлены;
- **каждое правило против своих же примеров** — порода, найденная редактором в
  уроке 1. В уроках 2, 4 и 5 расхождений нет, а **правило урока 3 пришлось снять
  целиком:** «слово меняется внутри» верно для двух пар из пяти;
- **каждое задание с выбором против мерки «все варианты в одном отношении к
  напечатанному»**. В уроке 3 мерка не исполняется в принципе — слов со своей
  формой в курсе ровно пять, и все пять урок показывает; сказано прямо, а
  задание переведено в производство, где мерка не применяется;
- **второй верный ответ у каждого задания**, включая три места, где правильную
  строку нельзя брать отвлекающим: `persons` (урок 3), `It is bread.` (урок 4),
  `Two vegetables are here.` (урок 5);
- **все слова каждого урока по правилу «есть ли карточка РАНЬШЕ этого урока»** —
  скриптом, раздел 15 замысла модуля.

**Чего проверить не удалось:**

1. **Уроков 6–8 глазами ученика я не проходил**, и расписаны они короче уроков
   2–5 нарочно: в уроках умений предмет задаёт источник, а не я. Породу 14
   («задание наказывает за верный ответ») я проверил там только чтением.
2. **Число слогов в `sandwiches` и `dishes`** считал по нашим записям, а не по
   внешнему словарю. Для `boxes`, `glasses`, `cities` и `potatoes` выписка есть
   (`docs/istochniki-norma-yazyka.md`, пункт 26), для этих двух — нет. **Если
   окажется, что `sandwich` /ˈsænwɪdʒ/ во множественном слога не прибавляет,
   случай 8 урока 2 показывается только на `boxes` и `glasses`** — на них выписка
   есть, и урок от этого не рушится.
3. **Семь слов из раздела 15 замысла модуля по-прежнему без карточки в коде:**
   `city`, `country`, `here`, `day`, `child`, `boy`, `girl`. Уроки 2, 3 и 5 стоят
   на всех семи, и сборщик обязан прогнать список ПЕРЕД сборкой.
4. **Работа модуля (раздел 8а замысла) — это углы, а не вопросы.** Написать их и
   прогнать `bliznetsy` придётся заново, когда уроки будут собраны.
5. **Значки уроков 2–8 я не называл вовсе.** У урока 1 они предложены и одна
   ошибка уже нашлась чтением (1f37e — открытое шампанское). Для уроков 2–5
   значки выбирает сборщик по `npm run znachki`, и опасных пар тут больше:
   `potato` и `onion`, `cheese` и `cake`, `soup` и `salad`, `man` и `person`.

---

## Правка 6 сентября по разбору второго методиста

Восемнадцать находок, шесть из них — «нельзя собирать до правки». **Все шесть
внесены в текст выше**, остальные разобраны ниже. Одна общая черта у половины:
я нашёл породу («правило опровергается собственным примером»), записал её
проверкой — и сам же нарушил её через два урока.

### Шесть блокирующих — что стало

| № | Находка | Где исправлено |
|---|---|---|
| 1 | урок 4 объявлял ошибкой `This is a bread.` — строку, которую мой же раздел 7 снял из отвлекающих как законную | задания 2, 3, 4, 6 урока 4 переустроены: осталось одно исправление, и его строка (`Milk are food.`) неверна при любом чтении |
| 2 | правило урока 2 опровергалось словами `zoo`, `zero`, `fish`, `person` | случаи 3 и 6 переписаны, столбец таблицы у `potato` сменён, `fish` выведен из модуля |
| 3 | цитата про `dish` со с. 80 — графы нет, слова в руководстве нет вовсе | ложное расхождение снято, `dish` стоит на Oxford и словнике без оговорок |
| 4 | шесть близнецов работы и уроков, три «угла» ложны | раздел 8а и 8б замысла модуля переписаны, вопросы 4, 6, 7, 8 и 12 сменены |
| 5 | заголовки называли не то число примеров | пересчитано разбором перечней: урок 2 — семнадцать, урок 3 — шестнадцать, урок 5 — пятнадцать |
| 6 | снятая рамка «что не считают штуками» уцелела в трёх заданиях | задание 5 урока 4 раскладывает СТРОКИ, задание 5 урока 6 отмечает то, что стоит с числом |

### Двенадцать остальных

| № | Находка | Что сделано |
|---|---|---|
| 7 | раздел 0 объявлял мерку и для `hottext`, а все четыре его нарушали | раздел 0 переписан: видов заданий ТРИ, у `hottext` своя мерка — признак должен быть языковым; строка урока 3 пересобрана |
| 8 | три вторых верных ответа без принимаемых | `persons` (урок 3), `The`/`My`/`Two` (урок 5), `This is bread.` (урок 4) — все положены в принимаемые |
| 9 | два документа дают разную раскладку карточек | раскладка сведена: **единственная таблица — раздел 5.4 замысла модуля**, таблицы уроков её повторяют и правятся только вместе с ней |
| 10 | вступление урока 4 начиналось отрицанием и спорило с моим же отвлекающим | переписано на утверждение |
| 11 | итоги уже материала, итог урока 8 не спрашивается работой | вопрос 12 работы переустроен; итоги сверены заново, разбор ниже |
| 12 | урок 6, задание 4: `sandwiches` напечатано в объявлении на том же экране | слово сменено на `glass` — оно из урока 2 и в объявлении не стоит |
| 13 | урок 7, вопрос 4: две картинки вместо трёх | стало три: один бутерброд, два и четыре |
| 14 | `A three oranges.` — выдуманный отвлекающий | заменён на `An orange.` — ответ на другой вопрос, настоящая ошибка |
| 15 | однообразие: `hottext` первым и `speak` седьмым во всех четырёх уроках | разбор ниже — правится порядком, и это решение сводящего |
| 16 | раздел 0 предупреждал о словах, которых нет ни в одном задании | список сокращён до `company` и `address`, `zoo` разобран отдельно |
| 17 | `vegetable` стоял в «словах о самой еде», а урок 5 ставит его во множественном | пример карточки сменён, слово переведено в считаемые |
| 18 | опоры урока 8 стояли в обратном порядке | переставлены: Young Learners, с. 27, первой |

### Находка 11 разобрана отдельно: итоги против материала

Прошёл по всем семи заново, подставляя каждый глагол под общий хвост.

| Урок | Итог | Сходится с материалом? |
|---|---|---|
| 2 | писать `-es` после `s`, `x`, `ch`, `sh` и у слов `potato` и `tomato`, а `-ies` — после согласной с `y` | да; случай 5 (гласная перед `y`) — половина той же развилки, отдельным умением не является |
| 3 | называть несколько людей словом со своей формой и ставить при нём `are` | да, после вывода `fish`: раньше итог обещал людей, а урок показывал рыбу |
| 4 | называть саму еду без `a` и без `-s` | да |
| 5 | ставить `these` перед словом о нескольких вещах | да |
| 6 | находить в списке, что там есть и чего сколько | да, после правки задания 5 |
| 7 | записывать со слуха, сколько названо вещей и в какой форме стоит слово | да; форму спрашивает вопрос 4 |
| 8 | выбирать форму слова, отвечая на вопрос о числе вещей | да, и **теперь спрашивается работой** — вопрос 12. Слов «на картинке» в итоге больше нет: одним значком нескольких вещей не показать, разбор в разделе решений |

### Находка 15: однообразие — предлагаю порядок, решает сводящий

Свод по четырём урокам правил был одинаков: `hottext` первым, `choice` вторым,
`speak` седьмым, заданий семь. **Предлагаю развести, не ломая лестницы:**

| Урок | Порядок | Заданий |
|---|---|---|
| 2 | `hottext` → `choice` → `gap` → `gap` → исправить → исправить → `speak` | 7 |
| 3 | `hottext` → исправить → `gap` → `gap` → `gap` → `speak` | 6 |
| 4 | разложить → `choice` → `gap` → `order` → исправить → `speak` | 6 |
| 5 | `choice` → `hottext` → `gap` → `order` → исправить → `gap` → `speak` | 7 |

`speak` остаётся последним во всех четырёх, и иначе быть не может: ступенька «к
себе» по устройству последняя. Разводится всё остальное — первый вид, состав
середины и число заданий.

**И два образца `speak` были об одном и том же** — «вещи из своей сумки» в уроках
1 и 2. Образец урока 2 сменён на «что стоит на столе».

### Три места от сборщика — решения методиста

**A. Решение A печатало в уроке верный ответ задания 6 — виноват я, и выбираю
раздел 6.** Пара `a carrot — carrots` в случае 5 ставила форму `carrots` в
объяснение, а раздел 6 замысла урока 1 прямо обещает: «слова `carrot`, `onion`,
`orange` и форма `cakes` в объяснениях не печатаются вовсе». Два моих решения
спорили друг с другом, и выбирать сборщику нельзя было.

**Случай 5 берёт `a cup — cups` и `a banana — bananas`.** Обе пары из слов урока,
обе правилу подчиняются, ни одно из двух слов в заданиях 1, 2, 4, 6 не работает.
Правило и обещание раздела 6 после этого не спорят.

**B. Заголовок второго столбца — «Несколько вещей».** Довод редактора в силе:
голое «Несколько» пары не составляет, ученик читает и спрашивает «несколько
чего». Столбцы: `| Одна вещь | Несколько вещей |`. То же во всех таблицах форм
модуля — уроки 1, 2, 3, 5.

**C. Обещание с адресом: беру ход сборщика.** Заглушка «дальше в этом же модуле»
обратима и не создаёт ссылок на несуществующие уроки; имена подставятся, когда
уроки 2 и 3 лягут в код. **Условие одно: подстановка не забудется** — она
записана здесь и в замысле урока 1, раздел 11A, а не в комментарии кода.

**И о цене решения C, которую сборщик назвал.** Замена «предмета» на «вещь» и
«короткого слова» на «артикль» стоила двадцати восьми заглушек — то есть урока,
переписанного целиком, при том что до правки незаполненных заглушек не было ни
одной.

**Цена настоящая, и решение я не отменяю:** два имени у одной вещи — то, за что
владелец снял «признак» в модуле 11 Elementary и «полную мысль» в модуле 24
Beginner, и стоит это дороже одного урока. **Но ошибка была не в решении, а в
том, КОГДА оно принято.** Имя главной вещи объявляется в шапке замысла ДО того,
как редактор напишет первую строку, — правило это записано в CLAUDE.md, и мой
замысел его нарушил: он объявил термином одно «множественное число» и промолчал
про артикль и про общее слово для вещей.

**Отсюда для уроков 2–8 и для следующих модулей:** шапка замысла называет три
вещи, а не одну — имя главной вещи, имена ЗАИМСТВОВАННЫХ терминов с адресом, где
они введены, и общее слово, которым модуль зовёт то, о чём говорит. Для модуля 8
все три теперь стоят в шапке `plan-modul-8.md`.

---

## Правка 6 сентября (вечер) по разбору собранных уроков 2–8

Сборщик назвал пятнадцать мест, три своих решения и вопрос о строе. **Решения
целиком стоят в замысле модуля, раздел 16** (`plan-modul-8.md`); здесь — то, что
уже внесено в тело этого документа, и три места, которых сборщик не называл.

**Внесено выше:** заголовки всех таблиц форм («Об одном / О нескольких»);
заголовок и итог урока 5; счёт примеров урока 2 (девятнадцать); задание 3 урока 2
(`Three ___ .`); седьмое задание урока 3 (`two mans` на `two men`); пример
карточки `rice`; закрытый перечень урока 4; две строки объявления урока 6; снятие
задания 4 урока 6; заголовок, итог, два блока материала и число словом в уроке 8.

### Три места, которых сборщик не называл

1. **Задание 2 урока 4 давало ДВА верных ответа.** Условие «отметь строку, в
   которой говорят, ЧТО ЭТО ЗА ЕДА» верно и для строки про яйца: яйца — тоже еда.
   Ученик, отметивший `They are eggs.`, был бы прав и получил бы «пока не так».
   Условие переписано, разбор — в теле урока 4.
2. **Вопрос 11 работы держал рамку записи 1 урока 7 слово в слово** — там
   `How many apples are there? — Three.`, здесь было
   `How many eggs are there? — Five.`: поменяны слово и число, а это близнеца не
   снимает. Запись сменена на `Three cups and two dishes.` одним голосом, и
   спрашивается ВТОРОЕ число.
3. **Случай 6 урока 5 говорит о людях, а правило вокруг зовёт всё «вещью».**
   Строки случая — `these children` и `these men`. Правило обязано сказать про
   людей вслух, иначе оно уже своих примеров.

### Что осталось сборщику проверить после правок

`npm run check:content` (заголовок урока 8 менялся — имя урока должно остаться
неповторимым во всём курсе), `npm run kontrol` (у урока 8 появились блоки
материала: строк «НИ ОДНОГО БЛОКА» должно стать меньше на четыре) и
`npm run bliznetsy` по всей работе — она переустроена в четырёх вопросах.
