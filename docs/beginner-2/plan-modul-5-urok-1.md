# Модуль 5, урок 1 «Бланк: имя и фамилия» — подробный замысел

Методист, 6 сентября 2026, **вторая редакция** — после разбора вторым методистом
(`razbor-plana-modul-5.md`). Замысел модуля целиком — `plan-modul-5.md`.

**Весь видимый текст пишет редактор.** Здесь сказано, ЧТО стоит на экране:
случай, английская строка, перевод, задание, ответ, опора. Русские формулировки
ниже — содержание, а не текст урока.

**Что изменилось против первой редакции:** имя главной вещи стало «полем» (было
два имени сразу); итог расширен до пяти подписей; С4 и С7 говорят проверяемое, а
не утверждение об обычае без источника; С6 переписан на противопоставление и
получил задание З7; задания разведены с примерами по именам; счёт слов в образце
исправлен; `voice` заменён на `pervyyGolos`; реплика `Sorry?` заменена.

---

## Служебное

| Поле | Значение |
|---|---|
| `slug` | `blank-imya-i-familiya` |
| `title` | Бланк: имя и фамилия |
| `vid` | `pravila` |
| `estimatedMinutes` | 25 |
| `outcome` | различать подписи полей бланка для имени и фамилии и вписывать в каждое поле свою часть имени |

**ИМЯ ГЛАВНОЙ ВЕЩИ МОДУЛЯ — «ПОЛЕ».** Оно стоит отдельной строкой в шапке файла
и проходит через все девять итогов, все случаи и работу модуля.

Правка по разбору: первая редакция объявляла именем «строку бланка» и тут же
двадцать раз писала «поле», а в одном итоге держала оба слова в одном
предложении. Выбрано «поле» не по вкусу, а по счёту: **модуль 1 уже напечатал это
слово ученику** — `courses/english-beginner-2/alfavit.ts`, строка 2872, заголовок
столбца `head: ["Поле", "Запись"]`. Возьми модуль 5 «строку», и одна вещь
получила бы в курсе два имени.

Слово вводится редактором один раз, при первом появлении, строкой с примером.

**Итог расширен по разбору.** Прежний перечислял четыре подписи и забывал
`Last name`, у которого в уроке случай С5, карточка и целая врезка N1. Новый итог
называет подписи не поимённо, а по делу: он не уже материала и не шире его.

**`vstuplenie` (обязательное поле).** Сцена, а не оглавление: Дана записывается
на курсы английского. У стойки ей дают лист, в нём несколько полей, и все подписи
английские. По самим полям не видно, куда писать имя, а куда фамилию, — надо
прочитать подпись. В уроке разбираем подписи полей и заполняем такой лист.

Обратная проверка сделана: про страну, адрес и возраст здесь ни слова (урок 4),
про my и your ни слова (урок 2). Обещаны подписи полей и заполнение — это в уроке
есть.

---

## Что ученик уже получил и на что урок опирается

- **модуль 1 «Алфавит и своё имя» (`alfavit-2`), девять уроков:** карточки
  surname, my, your, name, letter, word, spell, please, sorry, good, evening,
  hello; заглавная буква у имён, стран и городов (урок 6); диктовка по буквам и
  оборот `How do you spell that?` (уроки 1 и 5); объявление English со списком
  Alex Green, Alice Green, Ann Baker, Ben Baker, Kim Baker, Sue Green и бланк
  `Name: Kim` / `Surname: Bakker` (**урок 9**, а не 10 — десятым он был в
  замороженном курсе);
- **модуль 2 «Здравствуйте, меня зовут» (`privetstvie-2`):** оттуда приходят he,
  she, you, fine и имя Alim. Там же вводится притяжательное строкой `Dana is my
  friend` — урок 1 его не разбирает, он только употребляет уже данное слово your;
- **имена и фамилии.** Правка по разбору: Dana, Kim, Ann, Ben, Alice, Sue, Green,
  Baker приходят из списка модуля 1; **Alim и Alimov — из модуля 2**, а не из
  первого. Порядок не нарушен, но говорить «все имена из модуля 1» неточно.

**ОБЪЯВЛЯЮ ЗАИМСТВОВАНИЕ.** Слово your стоит в сцене и в случае С10
(`Your form, please.`), а разбирается уроком 2. Заимствованием в строгом смысле
это не является — карточка your стоит в модуле 1, — но **ни одно задание урока 1
на your не строится**.

**ПОВЕЛИТЕЛЬНОГО В УРОКЕ НЕТ, И ЭТО СЦЕНА, А НЕ ОСТОРОЖНОСТЬ.** Владелец
6 сентября решил повелительное взять (CLAUDE.md), и `Fill in the form.` законно —
оно стоит в уроке 4. Здесь у стойки просят `Your form, please.`: администратор не
командует гостю.

**РЕШЕНИЕ О РАСХОЖДЕНИИ ИСТОЧНИКОВ УРОКА 1 НЕ КАСАЕТСЯ — сверил построчно.** В
уроке нет ни одного вопросительного слова, ни одного `this`, ни притяжательного
апострофа: сцена построена на именных строках с please, разбор — на подписях
полей. `What is your name?` стоит в уроке 8, и там же объявляется.

---

## Связный кусок в начале (приём Мёрфи): два экрана

**Б1. `example`, `id: blank-na-stoyke`, `znak: 1f4c4`, caption «Лист, который
дали Дане».** Сам бланк, четыре напечатанные подписи, справа перевод:

| Английская строка | Перевод |
|---|---|
| `Form` | Бланк |
| `First name:` | Имя: |
| `Surname:` | Фамилия: |
| `Full name:` | Полное имя: |

`zvuchat`: `Form`, `First name`, `Surname`, `Full name` — четыре записи, а не
одна.

**Б2. `example`, `id: razgovor-u-stoyki`, `razgovor: true`,
`pervyyGolos: "muzhskoy"`**, caption «Что говорят у стойки». Реплика на строку:

| Реплика | Перевод |
|---|---|
| `Good evening. Your form, please.` | Добрый вечер. Ваш бланк, пожалуйста. |
| `Sorry, your form?` | Простите, бланк? |
| `Your form. Your first name, your surname.` | Ваш бланк. Ваше имя, ваша фамилия. |
| `Dana Nurlanova.` | Дана Нурланова. |

**ДВЕ ПРАВКИ ПО РАЗБОРУ, обе о коде и обе проверены чтением `types.ts` и
`ozvuchka.mts`, а не соседней ветки.**

1. **Поля `voice` у блока `example` НЕ СУЩЕСТВУЕТ.** Оно стоит у `AudioBlock`
   (`lib/content/types.ts`, строка 273) — там оно и понадобится, в уроке 7. У
   примера два голоса даёт `razgovor: true` (строка 122): в
   `scripts/ozvuchka.mts` (строки 108–120) `dvaGolosa: true` стоит жёстко при
   `block.razgovor`, а `voice` читается только в ветке `kind === "audio"`. Первая
   редакция требовала `voice` у примера — сборщик, исполнив это буквально,
   получил бы ошибку типов.
2. **`pervyyGolos: "muzhskoy"` обязателен.** `repliki()` чередует голоса по
   строкам, а первым по умолчанию читает ЖЕНСКИЙ (`types.ts`, строка 139 и
   комментарий над ней). В Б2 первая реплика — администратор, вторая — Дана. Без
   поля **Дана заговорит мужским голосом** — ровно то, что владелец услышал
   5 сентября 2026 и просил не повторять.

**ТРЕТЬЯ ПРАВКА — реплика `Sorry?`.** Первая редакция ставила её отдельной
репликой в значении «не расслышала». Модуль 1 такой случай СНЯЛ нарочно
(`alfavit.ts`, строки 1767–1769): восьмым стоял случай про то, что `Sorry?` даёт
повтор, и он снят целиком — «источник такого не говорит, а утверждать без
источника нельзя». Обойти чужое решение молча нельзя, поэтому сцена берёт форму,
которую курс уже дал: **`Sorry, your form?`** Карточка `sorry` модуля 1 стоит с
примером `Sorry, how do you spell that?`

*(Для сводящего: источник тут ближе, чем думал модуль 1 — Young Learners, с. 26,
советы к устной части Pre A1 Starters говорят, что ребёнку нужна практика
употребления `Sorry` и `I do not understand`, когда он не понял. Это не про обмен
«Sorry? — повтор» и не про одиночную вопросительную реплику. Пересматривать
решение модуля 1 — не моя работа.)*

Слова and в сцене нет нарочно: соединение перечисления вводит модуль 6.
Разделяет запятая.

---

## Десять названных случаев

У каждого имя, правило одной строкой и ДВА целых примера с переводом. Двадцать
примеров при мерке в шестнадцать.

**Имена в случаях и в заданиях РАЗВЕДЕНЫ** — правка по разбору: в первой редакции
ответ задания З4 (`Ann`) стоял примером в С1, и ученик отвечал узнаванием экрана.
Случаи держат Dana, Kim, Sue, Alice, Alim, Nurlanova, Alimov, Green; задания —
Ben, Ann, Baker.

### С1. `sluchay-first-name` — First name

Правило: в поле First name пишут имя, которым человека зовут.

| Строка | Перевод |
|---|---|
| `First name: Dana` | Имя: Дана |
| `First name: Alim` | Имя: Алим |

### С2. `sluchay-surname` — Surname

Правило: в поле Surname пишут фамилию.

| Строка | Перевод |
|---|---|
| `Surname: Nurlanova` | Фамилия: Нурланова |
| `Surname: Alimov` | Фамилия: Алимов |

### С3. `sluchay-full-name` — Full name

Правило: в поле Full name пишут имя и фамилию вместе, в одну строку.

| Строка | Перевод |
|---|---|
| `Full name: Dana Nurlanova` | Полное имя: Дана Нурланова |
| `Full name: Alim Alimov` | Полное имя: Алим Алимов |

**Таблица `T1` едет тем же экраном** (`naTomZheEkrane: true`), три строки,
столбцы «Поле — что вписывают — пример»: First name / имя / Dana; Surname /
фамилия / Nurlanova; Full name / имя и фамилия / Dana Nurlanova. Объяснение стоит
ПОСЛЕ таблицы и коротко.

### С4. `sluchay-name` — Name

**Случай переписан второй раз, и теперь он короче.** Первая редакция утверждала о
настоящих бланках то, чего источник не говорит. Вторая говорила только о нашем же
бланке и разводила мнимое противоречие с модулем 1. **Владелец решил 6 сентября
2026: короткая подпись `Name` значит ИМЯ, а не имя с фамилией** (CLAUDE.md,
раздел о подписях полей бланка). Образца бланка в `materials/` нет ни одного —
искал form, registration, application по трём руководствам, — поэтому решал
владелец как преподаватель.

**Отсюда: расхождения с модулем 1 нет вовсе.** Его бланк `Name: Kim` верен и
правки не требует; из раздела «находки о чужих модулях» этот пункт снят. Есть
просто короткая подпись и полная, и случай объясняет обычную вещь.

Правило: подпись `Name` значит имя; а где нужны обе части, полей два — `First
name` и `Surname`.

| Строка | Перевод |
|---|---|
| `Name: Kim` | Имя: Ким |
| `Name: Dana` | Имя: Дана |

Ссылка на наш урок «Читаем список и бланк» остаётся: там ученик такую подпись уже
видел, и ссылка внутри ступени приветствуется.

### С5. `sluchay-last-name` — Last name

Правило: поле Last name — то же самое поле, что Surname.

| Строка | Перевод |
|---|---|
| `Last name: Green` | Фамилия: Грин |
| `Last name: Alimov` | Фамилия: Алимов |

### С6. `sluchay-odno-slovo-dva-slova` — одно слово и два слова об одном

**Случай переписан по разбору.** Прежний («подпись бывает из двух слов, а строка
одна») повторял рамки С1 и С3 и о собственном пункте модуля не говорил ничего.
Новый строится на противопоставлении, ради которого пункт и берут.

Правило: `Surname` — подпись из одного слова, `Last name` — из двух, а поле у них
одно.

| Строка | Перевод |
|---|---|
| `Surname: Green` | Фамилия: Грин |
| `Last name: Green` | Фамилия: Грин |

**Это и есть пункт English Grammar Profile, который вводит модуль** — NOUNS /
types, «FORM: COMPOUND NOUNS», A1, id `1741163711817x590984810392161200`:
«one-word, two-word and hyphenated compounds… adjective + noun combinations».
`first name`, `last name`, `full name` — это `adjective + noun`.

**Оговорка, снятая по разбору:** `surname` компаундом ПО ФОРМУЛЕ ГРАФЫ не
является — это приставочное образование, а граф говорит о `verb + noun`,
`noun + noun`, `adjective + noun`. В тексте урока `surname` зовётся просто
подписью из одного слова; графу за него выдавать не надо.

Слов «всегда», «только», «нельзя» здесь быть не должно: графа разрешает, а не
запрещает.

### С7. `sluchay-poryadok` — в поле Full name имя стоит первым

**Правило сужено по разбору.** Первая редакция говорила «в английской записи
сначала идёт имя» — утверждение о языке вообще, а порядок «фамилия, имя» в
английских списках существует (`Baker, Ann`), и на широком правиле стояло
задание, объявлявшее верную запись ошибкой. Теперь правило говорит о БЛАНКЕ, и
это проверяется нашим же бланком и подсказкой модуля 1 («Имя стоит в строке
первым, а фамилия после него», `alfavit.ts`, строка 2853).

Правило: в поле Full name сначала пишут имя, потом фамилию.

| Строка | Перевод |
|---|---|
| `Full name: Sue Green` | Полное имя: Сью Грин |
| `Full name: Kim Baker` | Полное имя: Ким Бейкер |

Русская параллель называется прямо: в наших списках привычен обратный порядок —
сначала фамилия.

### С8. `sluchay-zaglavnaya` — заглавная буква

Правило: и имя, и фамилия начинаются с заглавной буквы.

| Строка | Перевод |
|---|---|
| `First name: Alice` | Имя: Элис |
| `Surname: Green` | Фамилия: Грин |

Возвращение к уроку 6 модуля 1 — то же правило, теперь в деле.

### С9. `sluchay-diktovka` — подпись называют, запись диктуют

Правило: у стойки называют подпись поля, а саму запись диктуют по буквам.

| Строка | Перевод |
|---|---|
| `First name: Dana. D-A-N-A.` | Имя: Дана. |
| `Surname: Nurlanova. N-U-R-L-A-N-O-V-A.` | Фамилия: Нурланова. |

**Строки взяты дословно из склада — записи звука уже готовы** (проверено
отпечатком, замысел модуля, раздел 7). Диктовка русскими буквами не переводится.

### С10. `sluchay-u-stoyki` — как просят у стойки

Правило: у стойки называют, что нужно, и добавляют please.

| Строка | Перевод |
|---|---|
| `Your form, please.` | Ваш бланк, пожалуйста. |
| `Your surname, please.` | Вашу фамилию, пожалуйста. |

---

## Две врезки

**N1. `note`, `tone: "info"`, `id: dve-podpisi-familii`.** Surname и Last name —
одно и то же поле, подписанное по-разному. Пишем surname, а узнавать нужно обе:
бланк выбирает не ученик.

Опора — решение владельца (CLAUDE.md, раздел о британской норме): surname
британская подпись, last name американская и более распространённая. **Слов
«чаще», «принято», «вежливее» в тексте быть не должно:** пометок разновидности
языка и частотности нет ни в одном нашем источнике, это решение владельца, а не
выписка.

**N2. `note`, `tone: "mistake"`, `id: kazhdomu-polyu-svoyo`.** Каждое поле берёт
свою часть имени: в First name идёт имя, в Surname — фамилия, в Full name — обе.
Показать типичную запись `First name: Dana Nurlanova` и рядом верную
`First name: Dana`.

**Два требования к этой врезке, оба проверяются глазами.**

1. **Звука у неверной строки быть не должно.** Услышанная неверная запись
   запоминается наравне с верной.
2. **Врезка обязана взять именно Дану.** Задание З4 стоит на той же ошибке с
   другим человеком (Ann Baker); возьми врезка Энн — ответ задания окажется
   напечатан над ним.

---

## Словарь урока

`vocab`, четыре карточки. Больше не нужно: остальные подписи и слова вводит
урок 4 (`address`, `email`, `passport`, `nationality`, `fill`).

| `term` | Перевод | Пример | Транскрипция | `znak` |
|---|---|---|---|---|
| `form` | бланк | `Your form, please.` | `/fɔːm/` | `1f4c4` |
| `first name` | имя | `First name: Dana` | `/ˈfɜːst neɪm/` | `1f464` |
| `last name` | фамилия (то же поле, что surname) | `Last name: Green` | `/ˈlɑːst neɪm/` | `1f4c4` |
| `full name` | имя и фамилия вместе | `Full name: Dana Nurlanova` | `/ˈfʊl neɪm/` | `1f4c3` |

**Карточки surname здесь НЕТ, и это решение.** Она стоит в модуле 1
(`alfavit.ts`, строка 2669), а слово закрыто, если карточка есть в этом модуле
или в любом более раннем.

**Ударение: выбрал единообразие, и это моё решение, а не выписка.** Владелец
третий вопрос пока не решал, и до его слова все три подписи несут ударение на
первом слове: `/ˈfɜːst neɪm/`, `/ˈlɑːst neɪm/`, `/ˈfʊl neɪm/`. Первые две так
записаны в замороженном курсе; третью я в первой редакции написал с ударением на
втором слове (`/fʊl ˈneɪm/`) — и сам же назвал главной бедой то, что внутри
одного словаря урока выходят две схемы. Правило проекта тут однозначно:
расхождение внутри курса хуже неудачно выбранного знака.

Источника под любым из трёх нет: `ipa-en-uk.txt` знает только одиночные слова —
first `/fˈɜːst/`, last `/ˈlɑːst/`, full `/fˈʊl/`, name `/nˈeɪm/`. **Запись ждёт
слова владельца**, и если он решит иначе, меняются три строки в одном словаре.

Знака второстепенного ударения здесь нет: при ударении на первом слове он не
нужен. Общее решение владельца от 6 сентября — второстепенное ударение
показываем, но не объясняем — соблюдено само собой.

Условности курса соблюдены: r вместо другого знака для этого согласного, g
обычной буквой, краткий гласный через e.

---

## Семь заданий лестницей

**Заданий стало семь, а не шесть** — правка по разбору: собственный пункт модуля
(составные существительные) в уроке не проверялся ничем. Его проверяет З7.

### Ступень «узнать»

**З1. `hottext`, `id: z1-pole-familii`.** Условие: отметь поле, в которое
вписывают ТОЛЬКО фамилию.

`parts`: `First name: Ben` · `Full name: Ben Baker` · `Surname: Baker` (верная).

**«Что ещё сюда подходит»** — проверил: `Full name: Ben Baker` фамилию тоже
содержит, и без слова «только» верных ответов было бы два. Слово «только» в
условии обязательно. `Last name` в варианты НЕ ставится: он и Surname — одно
поле, и верных снова стало бы два.

Имена взяты не из случаев (правка по разбору): в С1–С10 стоят Dana, Alim, Sue,
Kim, Alice, Green, Nurlanova, Alimov, а здесь Ben и Baker.

**З2. `match`, `id: z2-podpis-i-zapis`.** Слева подписи, справа то, что в них
идёт.

- `left`: `First name` · `Surname` · `Full name` · `Form`
- `right`: имя · фамилия · имя и фамилия вместе · сам лист с полями

**«Что ещё сюда подходит»** — проверил: `Last name` слева НЕ ставится, иначе у
«фамилии» два хозяина. У каждой строки ровно одна пара.

### Ступень «произвести»

**З3. `gap`, `id: z3-vpisat-familiyu`, `exact: true`.** Условие: на курсы
записывается Ben Baker; впиши в поле Surname то, что в него идёт.
`before`: `Surname: `, `after` пустой, `answer`: `Baker`.

`exact: true` обязателен: проверка упирается в заглавную букву, а сверка по
умолчанию регистр снимает. Ученик выбирает ЧАСТЬ имени, а не переписывает условие
целиком.

**З4. `short`, `id: z4-ispravit-pole`, `exact: true`.** Условие: в бланке в поле
First name записано `Ann Baker`; запиши, что должно стоять в этом поле.
`answer`: `Ann`.

**«Что ещё сюда подходит»** — проверил: поле First name берёт имя, и оно здесь
одно. Второго разумного ответа нет.

**Правка по разбору:** пара `Ann` / `Ann Baker` в случаях урока больше не стоит
(в С1 теперь Dana и Alim), значит ответ не напечатан выше ни примером, ни
врезкой.

**З5. `order`, `id: z5-sobrat-full-name`.** Условие: имя Ann, фамилия Baker;
собери поле Full name так, как его заполняют в бланке.
`items`: `Full name:` · `Baker` · `Ann`; `answer`: `[0, 2, 1]`.

**«Что ещё сюда подходит»** — проверил: подпись может стоять только первой, а
порядок имени и фамилии задан случаем С7, который теперь говорит **о бланке**, а
не об английской записи вообще. Запись `Baker, Ann` в списках существует, но
задание спрашивает не список, а поле бланка, и условие это называет. Верный
порядок один.

**З7. `short`, `id: z7-dve-podpisi-odno-pole`.** Условие: подпись Surname состоит
из одного слова; запиши подпись из двух слов, которая значит то же самое.
`answer`: `Last name`. `exact` не ставится — заглавная здесь не предмет проверки.

**Это задание на собственный пункт модуля** (составные существительные) и на С5 с
С6 разом. **«Что ещё сюда подходит»** — проверил: другой двухсловной подписи с
тем же значением урок не даёт; `Full name` значит другое.

### Ступень «применить к себе»

**З6. `essay`, `id: z6-tri-polya-o-sebe`, `minWords: 8`.** Условие: запиши три
поля бланка о себе — First name, Surname, Full name.

`sample` — три строки: `First name: Aigerim`, `Surname: Sultanova`,
`Full name: Aigerim Sultanova`.

`checklist`, пять пунктов:

1. в поле First name стоит только имя;
2. в поле Surname стоит только фамилия;
3. в поле Full name сначала имя, потом фамилия;
4. и имя, и фамилия начинаются с заглавной буквы;
5. имя записано латиницей — так, как оно стоит в удостоверении или паспорте.

**Пятый пункт переписан по разбору.** Прежний требовал «латиницей, а не русскими
буквами», а курс латинской записи имени не даёт: модуль 1 говорит это прямо
(`alfavit.ts`, строки 1103–1106: «ученик может не знать латинской записи своего
имени, а курс её не давал и проверить не может») и в задании `e4-svoyo-imya`
отсылает к документу. Пункт теперь ведёт туда же — иначе ученик получает мерку,
по которой ему нечем себя мерить.

**Образец проходит собственный список — сверил построчно, все пять пунктов.**

**Счёт порога, исправленный по разбору.** Самый короткий ВЕРНЫЙ ответ:
`First name: Kim` (3 слова) плюс `Surname: Baker` (2) плюс `Full name: Kim Baker`
(4) — **девять слов**. **Образец даёт ровно столько же, девять, а не
одиннадцать**, как я написал в первой редакции: `First name: Aigerim` (3) плюс
`Surname: Sultanova` (2) плюс `Full name: Aigerim Sultanova` (4). Способ счёта
проверен по коду: `components/lesson/TaskCard.tsx`, строка 572, режет ответ по
пробелам, двоеточие своим словом не считается.

Порог **`minWords: 8`** верный ответ не отсекает — это и остаётся его
основанием; довод «порог берётся по короткому ответу, а не по образцу» здесь
ничего не решает, потому что они равны.

---

## Таблица «задание — блок»

| Задание | На что опирается |
|---|---|
| З1 | С2 (Surname), С3 (Full name) и таблица T1 |
| З2 | С1, С2, С3, С10 и карточка form |
| З3 | С2 (Surname), С8 (заглавная буква) |
| З4 | С1 (First name), врезка N2 |
| З5 | С3 (Full name), С7 (порядок в поле Full name) |
| З7 | С5 (Last name), С6 (одно слово и два слова об одном) |
| З6 | таблица T1, С7, С8 |

Строк «ни одного блока» нет. Блоки без задания: С4 (Name) и С9 (диктовка) — и
это нарочно: оба спрашиваются работой модуля. **Правка по разбору:** прежде без
задания оставались ещё С5 и С6, то есть собственный пункт модуля не проверялся
ничем; теперь на них стоит З7.

---

## Звук

| Что звучит | Как |
|---|---|
| Б1, бланк | `zvuchat`: Form, First name, Surname, Full name — четыре записи, а не одна |
| Б2, разговор у стойки | одной кнопкой и на два голоса: `razgovor: true` плюс **`pervyyGolos: "muzhskoy"`**. Поля `voice` у примера НЕ существует — оно только у блока `audio` |
| С1–С8, С10 | каждая английская строка примера |
| С9 | `First name: Dana. D-A-N-A.` и `Surname: Nurlanova. N-U-R-L-A-N-O-V-A.` — записи на диске есть, если взять строки дословно. Буквы внутри реплики отдаются синтезу звуком, а не знаком |
| словарь | четыре слова по одному |

**Записей нет и придётся озвучить:** `Full name`, `Form`, `Your form, please.`
(проверено отпечатком, замысел модуля, раздел 7).

**Звука НЕ получают:** неверная строка `First name: Dana Nurlanova` во врезке N2;
русские подписи и транскрипция; подписи с двоеточием, если редактор поставит их
отдельными ячейками таблицы.

---

## Чего проверить не удалось (по этому уроку)

- **ударение в двухсловных подписях** — источника нет ни под одной из трёх
  записей; владелец вопроса пока не решал. До его слова держу ударение на первом
  слове у всех трёх, ради единообразия (см. словарь урока);
- **порядок полей в настоящем бланке** (что идёт раньше — First name или
  Surname) источниками не задан. Порядок в Б1 наш, и утверждать в тексте «в
  бланке сначала идёт…» нельзя;
- **как синтез прочитает имена Ann, Alice, Sue, Ben** —
  `lib/content/proiznoshenie.ts` я не открывал. Сборщику проверить;
- **звучание записей не слушал** — ключа синтеза нет. Проверено только наличие
  файлов по отпечатку, а «файл есть» не значит «звучит верно»;
- **сколько экранов выйдет на самом деле** — считал на глаз: два связных куска,
  десять случаев, таблица, две врезки, словарь и семь заданий, около двадцати
  трёх экранов. Мерка «один экран — одна мысль» проверяется на собранном уроке.
