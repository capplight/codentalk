# Разбор замысла модуля 7 вторым методистом

6 сентября 2026. Замыслы — [plan-modul-7.md](plan-modul-7.md) и
[plan-modul-7-urok-1.md](plan-modul-7-urok-1.md), писал первый методист.
Разбирал второй, который их не видел.

**Приговор: доработать.** Восемь ошибок обязательны, двенадцать вопросов.

**Что в замысле сильно, и это надо сказать до упрёков.** Разведка по источникам
самая честная из всех, что я разбирал. Я открыл каждую названную страницу и
**ни одной выдуманной цитаты не нашёл**: дескриптор CEFR с. 79 стоит дословно,
`What's this?` вправду напечатано у Cambridge дважды и оба раза внутри раздела
Pre A1 Starters, список «The home» на с. 81 держит все четыре спорных слова в
столбце PRE A1 STARTERS, все пятнадцать `id` English Grammar Profile существуют,
лежат на объявленных ступенях и с объявленным `Lexical Range`. Счёт темы
`House and Home` — 60 записей, 27 с пометой A1 — сошёлся у меня до записи.
Утверждение «записей о `that` уровня A1 в файле нет ни одной» я перепроверил
перебором всех 109 и подтверждаю. Аудит склада звука (24 слова и 20 строк есть,
`computer` нет) пересчитал тем же отпечатком, каким считает код, — он верен до
единицы.

**Где замысел слаб — и это НЕ источники.** Все восемь ошибок лежат внутри самого
урока: правило, опровергаемое собственным примером; правило, опровергаемое
собственной сценой; задание, решаемое сличением строк; обещание итога, которого
не делает ни одно задание. Ровно то, чего не видит ни один скрипт и о чём
`materials/` не скажет.

**И одно предупреждение о порядке работы.** Файл `plan-modul-7.md` правился,
пока я его читал: на первом чтении в нём было 879 строк, на втором — 1034.
Раздел 8.5 за это время переписан из находки в снятую находку, раздел 3.4
дописан целиком, восьмой случай урока 5 добавлен. Часть моей работы ушла на
разбор текста, которого больше нет. Правило проекта («проверяющие ходят по
очереди, если один из них правит файл») здесь не исполнено. Разбор ниже сделан
по редакции в 1034 строки.

---

## Ошибки

### 1. Задание З1 в описанном виде построить нельзя, а построенное решается без английского

`plan-modul-7-urok-1.md`, раздел 8, З1:

> **З1. `choice` со значками.** «Ким показал на вещь и сказал: `It's a chair.`
> На какую вещь он показал?» Варианты — три значка: `1fa91` (стул, верный),
> `23f0` (часы), `1f4bb` (компьютер).

**Значок вариантом слово не заменяет.** Открыл код, а не соседнюю ветку.
`lib/content/types.ts`, `ChoiceOption`: поле `text: string` обязательное,
`znak?: string` — необязательное; комментарий там же: «Нет значка — вариант
стоит словом, ничего не ломается». А `components/lesson/TaskCard.tsx`, строки
197–210, говорит прямо, и это решение, а не случайность:

> // Картинка не заменяет слово, а стоит над ним: ученик выбирает
> // вещь, но ЧИТАЕТ И ЕЁ НАЗВАНИЕ — иначе задание перестаёт быть
> // языковым.

Значит у каждого из трёх вариантов будет подпись, и замысел обязан назвать,
какая. Он её не называет ни здесь, ни в разделе 9, ни в разделе 11.

**И оба возможных ответа портят задание.**

- Подпись английская (`chair`, `clock`, `computer`) — верный вариант **дословно
  содержится в условии** (`It's a chair.`). Ученик сличает две одинаковые строки
  и не читает по-английски ничего. Это порода «прочитай варианты глазами того,
  кто языка не знает: можно ли попасть?» — попасть можно с закрытыми глазами.
- Подпись русская («стул», «часы», «компьютер») — задание становится переводом
  английской строки, а не выбором вещи, и опора на A2 Key, с. 29 («each with a
  question and three images»), перестаёт его описывать: там под картинками
  подписей нет вовсе.

Замысел утверждает обратное: «все три вещи названы в уроке словами, значит
верный вариант — не единственный знакомый (мерка 2)». Мерка проверена не для
того признака: опасен здесь не «знакомый», а ТОЖДЕСТВЕННЫЙ УСЛОВИЮ.

Чинится одним из трёх: подпись русская, а условие перестаёт печатать
`It's a chair.`; либо условие русское, а подписи английские; либо у вида
просится пустая подпись — но это правка типа, и решать её не мне.

**То же касается урока 7** («задание с картинкой вариантом стоит здесь») и
урока 2 («слово `CHAIR`, значок обязателен»): везде надо назвать подпись.

**Ошибка.**

### 2. Случай 3 опровергается собственными примерами, и на нём стоит задание З3

`plan-modul-7-urok-1.md`, случай 3:

> **Правило:** по-русски стол — он, дверь — она, окно — оно, а по-английски обо
> всех трёх говорят одним словом `it`.
> `It is a door.` — Это дверь.
> `It is a window.` — Это окно.

**Правило обещает соответствие `it` — «он / она / оно», а оба примера дают
`it` — «это».** По-русски «Она дверь» не говорят вовсе, говорят «Это дверь».
Ученик читает правило, смотрит на два примера и не находит в них ни «он», ни
«она», ни «оно».

Это не придирка к переводу. В строках отождествления (`It is a door`) английское
`it` отвечает русскому «это», а соответствие «он / она / оно» работает в другом
строе — `The door is white. It is white.` Такого строя в модуле 7 нет и быть не
может: прилагательные розданы модулям 9 и 10 (раздел 1.1 того же замысла).

**И третье имя тому же слову даёт карточка:** `it` — «это, оно» (раздел 7). То
есть в одном уроке `it` получает три разных русских соответствия: «это, оно» в
словаре, «он, она, оно» в правиле, «это» в примерах и переводах.

**Хуже, что на этом стоит задание.** З3 требует отметить `this` и `It` как «два
слова, которыми по-английски сказано русское „это“». Ученик, поверивший правилу
случая 3, отметит одно место и получит «пока не так» за то, что запомнил урок.

Мерка 11 не выдержана, и опровергает правило не источник, а сам урок.

Чинится либо снятием случая 3 (его предмет — не этот модуль), либо переписью
правила под то, что показывают примеры: «по-русски мы говорим „это“ и о столе, и
о двери, и об окне; по-английски в ответе тоже одно слово — `it`». Тогда случай
становится настоящим расхождением с русским и сходится с З3.

**Ошибка.**

### 3. Случай 4 опровергается сценой своего же урока

Случай 4, правило: «В РАЗГОВОРЕ те же строки говорят короче: `What's this?` и
`It's a book.`»

Сцена того же урока (раздел 3) — разговор, `razgovor: true`, и в нём обе строки
стоят полными:

| Строка сцены | Что в ней |
|---|---|
| `What is this?` | полная, а не `What's this?` |
| `It is a computer.` | полная |
| `What is this?` | полная второй раз |
| `It is a clock.` | полная |

Ученик читает «в разговоре говорят короче» и тут же вспоминает разговор этого
урока, где не сократили ни разу. Это мерка 11, и здесь она бьёт по самому
заметному месту: сцена стоит первой и читается дважды.

Источник против сокращения ничего не имеет — Young Learners, с. 29, строка
Verbs: «(Positive, negative, question, imperative and short-answer forms,
including contractions)»; цитата в замысле верна дословно, я её открыл (строка
1279 разбора). Спорит с правилом не источник, а наша сцена.

Чинится либо тем, что одна пара реплик сцены становится короткой (тогда случай 4
разбирает то, что ученик уже слышал, — это и есть приём Мёрфи), либо тем, что
правило перестаёт говорить «в разговоре».

**Ошибка.**

### 4. Врезка называет звуком транскрипцию целого слова, и сосед-модуль пишет иначе

`plan-modul-7-urok-1.md`, раздел 5:

> Смысл: `this` начинается со звонкого звука /ðɪs/ — того самого, что стоит в
> `mother`, `father` и `brother`.

`/ðɪs/` — запись всего слова `this`, а не звука. Звук в начале — `/ð/`, и именно
он стоит в `mother` и `brother`; `/ðɪs/` там не стоит и стоять не может.

**Модуль 6 то же место пишет верно**, и это не моя догадка —
`plan-modul-6-urok-1.md`, случай 8:

> **Правило:** в `mother`, `father` и `brother` буквы `th` звучат с голосом —
> это звук /ð/.
> `She is my mother. — Она моя мама.` (кнопка звука у /ˈmʌðə/)

Там звук назван `/ð/`, а транскрипция целого слова служит только КЛЮЧОМ кнопки.
В модуле 7 ключ и звук слиты в один знак, и слиты неверно.

Правило CLAUDE.md («звук объясняется словом, а потом значком») нарушено вдвойне:
значок назван вместо звука и назван не тот значок.

**Осторожно при починке.** Ключ поля `zvuk` обязан дословно стоять в тексте, а
записи для `/ð/` в складе нет — синтезу её не прочесть. Значит устройство то же,
что у модуля 6: правило говорит «звук /ð/», кнопка висит у `/ðɪs/`, и оба знака
стоят в тексте по отдельности.

**Ошибка.**

### 5. Итог урока 1 обещает «спрашивать», а ни одно из восьми заданий этого не делает

`outcome` урока 1: «спрашивать о вещи рядом: What is this? — и отвечать: It's a
chair».

Прошёл по всем восьми заданиям раздела 8 и по таблице «задание — блок» раздела 9:

| Задание | Что ученик делает | Спрашивает ли `What is this?` |
|---|---|---|
| З1 | выбирает картинку | нет |
| З2 | соединяет три слова с переводами | нет |
| З3 | отмечает два слова в готовой строке | нет |
| З4 | вписывает `a` | нет |
| З5 | собирает `It is a pen.` | нет |
| З6 | пишет `It's a door.` | нет |
| З7 | пишет `A clock.` | нет |
| З8 | дописывает `a book` | нет |

**Ноль из восьми.** Все восемь — про ОТВЕТ. Замысел это признаёт сам (раздел 9):
«случай 1 (сам вопрос)… заданий не имеют», «случай 1 спрашивается вопросом
работы модуля».

Правило программы «каждый итог спрашивается работой», может быть, и исполнено, —
но работы ещё нет, а мерка требует трёх сходящихся сторон: чему учит урок, что
делают задания, что спрашивает работа. Вторая сторона пуста, и обещание,
стоящее под заголовком «Чему научишься», ученик в уроке не исполняет ни разу.

**И то же место сломано вторым способом: итог записан двумя разными строками.**

| Где | Что стоит |
|---|---|
| `plan-modul-7.md`, раздел 2, строка 182 | «спрашивать о вещи рядом `What is this?` и отвечать **`It's a book`**» |
| `plan-modul-7-urok-1.md`, раздел 1, `outcome` | «спрашивать о вещи рядом: What is this? — и отвечать: **It's a chair**» |

Сводящему нечем выбрать. И обе строки называют пример из РАЗНЫХ случаев:
`It's a book.` — случай 4, `It is a chair.` — случай 2.

Чинится либо заданием, где ученик производит вопрос (собрать `What` · `is` ·
`this?` — вид `order`, в уроке ещё не занятый), либо сужением итога до
«отвечать на вопрос о вещи рядом: `It's a chair`». Второе честнее: тогда
обещание и восемь заданий сходятся.

**Ошибка.**

### 6. У З8 условие открытое, а `accept` закрытый, и полнота списка объявлена неверно

З8: «Посмотри, что лежит перед тобой прямо сейчас. Возьми слово из словаря этого
урока ИЛИ ИЗ ПРОШЛЫХ МОДУЛЕЙ и допиши строку.»

`accept`: `a table`, `a chair`, `a window`, `a door`, `a clock`, `a computer`,
`a pen`, `a box`, `a bed`, `a card`, `a ticket`, `a key`, `a phone` — тринадцать
строк плюс `a book` в `answer`.

Замысел утверждает: «Это все вещи, которые курс дал карточкой к этому месту, и
ни одной сверх того». Я выписал карточки модулей 1–5 из кода
(`grep 'term:' courses/english-beginner-2/*.ts`), и утверждение не подтвердилось.
Снято с объяснением только двое — `paper` и `light`. Не названы вовсе:

| Карточка | Где | Перевод в курсе | Годится ли в ответ |
|---|---|---|---|
| `cat` | модуль 1, строка 626 | «кошка» | да: кошка перед человеком бывает чаще компьютера |
| `form` | модуль 5, строка 704 | «бланк» | да: модуль 5 целиком про бланк, лежащий перед учеником |
| `letter` | модуль 1, строка 1856 | «буква» | нет — переведено «буква», а не «письмо» |
| `word`, `night`, `mouth`, `out`, `black`, `tea`, `sea`, `bread` | модуль 1 | — | нет |

Двое годных — `a cat` и `a form` — ученик напишет и получит «пока не так» за
верный английский при верном переводе нашей же карточки. Это порода 14.

**Но глубже не список, а устройство.** Условие приглашает «из прошлых модулей»,
то есть открывает весь словарь курса, а `accept` его закрыть не может в
принципе. Либо условие сужается до словаря ЭТОГО урока (тогда `accept` — девять
строк и он полон), либо задание перестаёт быть `gap` с одним ответом.

**Ошибка.**

### 7. Список `House and Home` стоит на с. 28, а не на с. 29, и расхождения разборов нет

`plan-modul-7.md`, раздел 3.1:

> **Осторожно, страница у двух разборов разная.** Первый разбор ставит список на
> **с. 29** (его собственный колонтитул «Page 29 of 32» стоит сразу под таблицей
> и согласуется с меткой страницы `с. 29`), второй — на с. 28.

Открыл оба разбора и сам способ разметки.

**Метка страницы ставится ПЕРЕД текстом страницы, а не после него.**
`scripts/kontrol.mts`, строка 132:

    syroy = result.pages.map((p: any) => `\nс. ${p.num}\n${p.text}`).join("\n");

Значит всё, что идёт ПОСЛЕ метки `с. N`, — это страница N. В
`materials/.text/cambridge-vocab-a2-key.txt`: метка `с. 28` — строка 2508,
колонтитул «© UCLES 2025 Page 28 of 32» — 2510, `House and Home` — 2541,
последняя строка списка (`chair fridge living room towel`) — 2555, метка
`с. 29` — 2559, колонтитул «Page 29 of 32» — 2560. Список целиком лежит между
метками 28 и 29, то есть НА СТРАНИЦЕ 28. Колонтитул «Page 29 of 32» стоит не
«сразу под таблицей», а уже за границей страницы: pdf-parse выдаёт колонтитул
первой строкой своей страницы, и это видно на всех семи страницах словника
подряд — 2419, 2465, 2510, 2559, 2609, 2656, 2706.

**Второй разбор говорит то же самое.** `materials/.md/cambridge-vocab-a2-key.md`,
строка сразу под таблицей: `| © UCLES 2025 | | Page 28 of 32 | A2 Key and Key
for |`. Колонтитул стоит внизу СВОЕЙ страницы, и это страница 28.

**Разборы не расходятся ни на строку.** Расхождение выведено из того, что метку
страницы прочли задом наперёд.

Правится в трёх местах: раздел 3.1 (с. 29 → с. 28, абзац о расхождении
снимается), раздел 11, первый пункт «чего проверить не удалось» (проверять
нечего), и `plan-modul-7-urok-1.md`, раздел 7: «в теме `House and Home` словника
A2 Key, с. 29».

Цена невелика, но она ровно та, ради которой номера и пишут: следующий
проверяющий откроет с. 29 и увидит там `Measurements`.

**Ошибка.**

### 8. Правка про `clock` дошла до случая 7 и не дошла до таблицы раздела 10

`plan-modul-7-urok-1.md`, случай 7 (строки 197–206):

> **Уточнение сужало то, что источник даёт широким, и снято: перевод карточки —
> просто «часы».**

`plan-modul-7-urok-1.md`, раздел 10, последняя строка таблицы (строка 470):

> | `watch` («часы наручные») | в модуле нет вовсе; **перевод карточки `clock`
> уточнён скобкой заранее** |

Один файл, два взаимоисключающих утверждения об одной карточке. Это порода
«правка доходит до врезки и не доходит до подсказок».

Словарь урока (раздел 7) и раздел 3.2 замысла модуля поправлены верно — там
«часы». Осталась одна строка, и видна она только чтением всего файла подряд.

**Ошибка.**

---

## Вопросы

### 9. У З4 есть второй верный ответ, и его подсказывает правило нашего же модуля 3

З4: «Ким показал на окно и назвал его. Допиши недостающее слово.»
`It is ` __ ` window.` → `a`.

Замысел проверил одного соперника: «`an` перед `window` не ставится по правилу
модуля 3 — по первому звуку». Верно. **Но соперник не `an`, а `the`.**

`plan-modul-3.md`, строка 221, урок 4 «Тот самый: the»:

> `the` о том, что в разговоре одно и о чём уже сказали… итог: **выбирать между
> a и the по тому, знает ли собеседник, о чём речь**

Ученик модуля 7 этот урок прошёл. В условии Ким ПОКАЗЫВАЕТ на окно — собеседник
видит, о чём речь, окно в комнате одно. По правилу модуля 3 ученик вправе
написать `the`, и `It is the window.` — верная английская строка.

Задание при этом законно: в ответе на `What is this?` естественно `a`. Но условие
этого не задаёт, а правило соседнего модуля толкает в другую сторону. Выход
обычный — доопределить условие («Ким называет вещь впервые») или поставить `the`
отдельной строкой в разбор задания.

Заодно сводящему: `the` в модуле 7 не упомянут ни разу — ни в списке долгов, ни
в разделе 0.2. Модуль, где каждая строка стоит на артикле, обязан сказать хотя
бы, почему `the` в нём не встречается.

**Вопрос.**

### 10. Условие З7 требует артикля, а `accept` принимает ответ без артикля

З7: «Ответь ему ОДНИМ СЛОВОМ С АРТИКЛЕМ.» → `A clock.`, `accept`: `Clock.`,
`Clock`, `A clock`.

Наказания за верный ответ здесь нет — принимается и то, и другое, — но условие
говорит о задании неправду, а случай 5 повторяет её правилом: «если вещь просто
называют, довольно одного слова с артиклем».

Источник, на который замысел ссылается, даёт обратное: Young Learners, с. 27,
ответ экзамена напечатан как «(plate)» — голое слово, без артикля. Я открыл
страницу, цитата верна.

То есть правило у нас уже, чем источник, а `accept` шире правила. Свести надо в
одну сторону: либо правило говорит «довольно одного слова» и примеры дают обе
записи, либо `accept` голое слово не принимает. Нынешнее сочетание учит одному,
а засчитывает другое.

**Вопрос.**

### 11. Случай 8 стоит на цитате про «ложных друзей», а `computer` ложным другом не является

Случай 8, обоснование: «Ровно тот случай, о котором Young Learners, с. 20, пишет:
„Concentrate on words which are likely to be confused, or which have ‘false
friends’ in the candidates’ first language“».

Цитата верна дословно — открыл, строки 811–813. **Но она о другом.** Абзац
целиком (с. 20, «Tips for preparing learners for the Reading and Writing paper»)
советует сравнивать слова, СПУТЫВАЕМЫЕ ПО ЗНАЧЕНИЮ, и даёт пример `photo/camera`.
«Ложный друг» в этом ремесле — слово, похожее на родное, но значащее другое.
`computer` и «компьютер» значат одно и то же; расходится у них ударение, а не
значение.

Это порода «подмена источника»: цитата настоящая, вывод чужой. Сам факт при этом
верен — `ipa-en-uk.txt` даёт `/kəmpjˈuːtɐ/`, ударение на втором слоге, и наша
запись `/kəmˈpjuːtə/` выводится из него по обеим условностям курса. Но опоры под
случаем нет, и её надо либо найти, либо честно записать: «источника нет, случай
наш».

**И вторая половина того же места.** Тот же абзац с. 20 замысел цитирует ВТОРОЙ
раз — в уроке 2, для пары `desk` / `table`. Там цитата на месте: это ровно
«слова, спутываемые по значению». Одна страница подпирает два случая, и один из
двух подпирает.

**Вопрос.**

### 12. Довод «письма в модуле нет» берёт с. 83–84 и обходит с. 67, где A1 прямо про вещи

`plan-modul-7.md`, раздел 2:

> Письма в модуле нет: источник называет жанры письма на A1 (открытка, короткое
> сообщение, бланк — CEFR с. 83–84), и ни один из них не про «назвать вещь».

Обе страницы я открыл, жанры названы верно. **Но письмо на A1 источник даёт не
только жанрами.** CEFR, **с. 67**, шкала *Creative writing*, графа **A1**
(строки 3230–3232 разбора):

> Can describe in very simple language what a room looks like.
> Can use simple words/signs and phrases to describe **certain everyday objects**
> (e.g. the colour of a car, whether it is big or small).

Это дескриптор письма про ВЕЩИ, и он единственный на ступени, который вещи
вообще называет. Мимо него довод модуля 7 проходить не должен — тем более что
принятая программа ссылается на него сама (`programma-stupeni.md`, строки
277–281).

**Я не говорю, что урок письма модулю 7 нужен.** Скорее нет: дескриптор просит
цвет и размер, а прилагательные розданы модулям 9 и 10, «what a room looks like»
— модулю 12. Но именно это и надо написать: «дескриптор с. 67 требует
прилагательных, которых модуль не даёт», а не «ни один жанр не про вещи».
Нынешний довод неполон, и следующий проверяющий найдёт с. 67 и решит, что её не
смотрели.

**Вопрос.**

### 13. У «указательного» два имени, и ни одно нигде не вводится

`plan-modul-7.md`, шапка:

> **И второе имя, которое модуль обязан держать неизменным, — «указательное
> слово»**… Настоящий термин русской грамматики — «указательное местоимение»…
> Укорочение до «указательное слово» допустимо там, где строка длинна.

Мерка 9 требует у главной вещи ОДНО имя. Здесь их объявлено два и разрешено
пользоваться обоими по длине строки. Образец, на который замысел ссылается
(решение владельца от 22 августа о «наречиях меры»), устроен иначе: там курс
держит ОДНО укороченное имя и полного не употребляет нигде, потому что слово
«степень» занято сравнением. Здесь ничего не занято — «местоимение» курс ввёл в
модуле 2, — и укорачивать не от чего.

**И вторая половина, которая дороже.** Правило проекта требует, чтобы термин был
объяснён НА МЕСТЕ, до первого задания. Ни замысел урока 1, ни строки уроков 2–5
не говорят, где термин вводится и каким примером. В уроке 1 он вообще не нужен:
`this` там стоит только внутри `What is this?`. Значит либо адрес называется
(урок 2, где `this` становится предметом), либо слова в модуле нет вовсе — и
тогда шапка не должна объявлять его именем главной вещи.

**Вопрос.**

### 14. `vozvrat` назван именем урока, которого нет

`plan-modul-7.md`, раздел 2.2:

> Модуль 7 возвращается к **модулю 3, урок 3 «Артикль a и an»**.

`plan-modul-3.md`, строка 220: урок 3 называется «**Один из таких: a и an**».
Заголовка «Артикль a и an» в курсе нет и не будет. Правило CLAUDE.md: «ссылка на
наш модуль называет его тем именем, которое видит ученик», и `check:content` это
проверяет. Пока замысел — документ, беды нет; но редактор возьмёт строку отсюда.

**Заодно две вещи о том же поле.** Первая: `vozvrat` ссылается на урок, которого
в коде ещё нет — у модуля 3 написан один урок из девяти. Вторая: поле `iz` берёт
`slug` МОДУЛЯ, а `slug` модуля 3 — `otkuda-ty` (`otkuda-ty.ts`, строка 59), не
`otkuda-ty-2`. На этом уже споткнулся замысел модуля 5 (разбор, п. 28.1), и
стоит назвать `slug` прямо, чтобы сводящий не выводил его по образцу `alfavit-2`.

**Вопрос.**

### 15. Звучание восемнадцати примеров не расписано нигде

Решение владельца от 19 августа: звучат примеры и таблицы, дроблением по
единицам. Замысел расписывает `zvuchat` у таблицы (раздел 6, пять записей), у
сцены (разговор целиком, два голоса), у врезки и у случая 8.

**А восемнадцать строк девяти случаев не названы ни разу.** Раздел 4 перечисляет
их как примеры с переводом, но `zvuchat` у них не объявлен. Сборщик будет решать
сам, а решает он по образцу соседнего модуля, то есть наугад.

Счёт при этом важен. Посчитал отпечатком: из восемнадцати строк записи есть у
трёх — `What is this?`, `What's this?`, `It's a book.`. Нет у пятнадцати:
`What is this, Kim?`, `It is a table.`, `It is a chair.`, `It is a door.`,
`It is a window.`, `A key.`, `A pen.`, `It's a ticket.`, `It's a box.`,
`It's a clock.`, `It's a computer.`, `It is a computer.`, `A computer.`,
`It's a bed.`, `It's a phone.` Плюс две строки таблицы — `It's a chair.` и
`A window.` Раздел 6.1 обещает бесплатными только слова, и это верно, но
масштаб озвучки урока 1 стоит назвать: семнадцать новых записей.

**Вопрос.**

### 16. З5 решается пунктуацией, а не языком

З5, `order`: собрать `It` · `is` · `a` · `pen.`

Замысел гордится тем, что порядок один, и обеспечен он верно: заглавная `It`
может стоять только первой, точка в `pen.` — только последней. Порода 14 здесь
и вправду снята.

**Но тем же приёмом снята и половина задания.** Из четырёх кусков два закреплены
знаками, и ученику остаётся выбрать между `is a` и `a is` — угадать из двух.
Заданию на ступеньке «произвести» этого мало.

Либо задание меняет вид (`short` вместо `order`), либо признаётся заданием на
узнавание и переезжает к первым трём — и тогда лестница «три узнать, четыре
произвести» пересчитывается.

**Вопрос.**
