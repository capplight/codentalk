# Правка редактора: долги 15 сентября 2026

Журнал ведётся по ходу работы.

## 1. Модуль 14 «Я не делаю», работа, вопрос 1 `q1-soedinit-s-otricaniem-i-bez`

Опора: `reshenie-metodista-bliznetsy-15-sentyabrya.md`, место 9.

**Условие сверено со строками.** «Разложи четыре английские строки по русским. В двух из них есть
отрицание, а в двух его нет.» Строк четыре, с отрицанием две (`I don't drink milk.`, `She doesn't dance.`),
без него две (`I eat bread.`, `She dances.`). Ответ `[2, 0, 1, 3]` сверен по индексам: каждая русская
строка получает свою английскую. Условие не правлено.

**Заглушка `why` заменена. Стало:**

> В строках I don't drink milk. и She doesn't dance. стоят слова don't и doesn't, и по ним видно
> отрицание. В строках I eat bread. и She dances. этих слов нет, поэтому в их переводе нет и слова «не».

Разбор называет только то, что напечатано в строках вопроса. Тире внутри нет, родовых форм нет.
Строй тот же, что у разборов соседних вопросов работы: запись названа, признак назван словом.

## 2. Модуль 16 «Как часто», урок 7 `kazhduyu-nedelyu`, случай `sluchay-every-i-always`

Опора: `reshenie-metodista-dolgi-15-sentyabrya.md`, пункт 1.1 (прочитан целиком).

**Правка одна, во второй строке правила.** Было: «Слово always не говорит, сколько времени проходит
между двумя разами, а every week говорит, что проходит неделя.» Стало:

> Слова always и often не говорят, сколько времени проходит между двумя разами, а every week говорит,
> что проходит неделя.

Имя случая «Чем every week отличается от always» и примеры (`I always work at home.`, `I work every week.`)
не тронуты: без новой строки обошлось. Слово `often` ученику известно из урока 2 «Обычно и часто».

**Перечитано против правки:**

- вступление урока 7 — «Слова «часто» и «иногда» не говорят, сколько времени проходит между встречами, а
  слова «каждый месяц» говорят». Теперь «часто» сопоставлено в случае 6 с `often`. Сходится;
- случаи 1–4 и таблица `tablica-slovo-posle-every-odno` — о `often` не говорят, спорить нечему;
- свод `tablica-svod-uroka-7` — о порядке и единственном числе, с правилом не расходится;
- задания `z1`–`z6` урока 7 — ни одно не опирается на сопоставление наречия с оборотом;
- разбор `z1-otmetit-narechiya-v-zapiske` урока 9 — «оборот every month. В нём два слова, поэтому это не
  наречие частоты» опирается на первую строку правила случая 6, которая не менялась. Сходится;
- кнопки звука: в `zvuchat` случая нет строк `always` или `often` отдельно, значит новое слово в правиле
  второй кнопки не даёт; тире « — » в строке правила нет, пары «строка — перевод» не выйдет.

**Вопрос методисту (не правка).** Вступление называет два русских слова, «часто» и «иногда». После
правки `often` сопоставлен с оборотом, а «иногда» (`sometimes`, урок 6) — нет. Если нужна полная пара
со вступлением, правило можно довести до «Слова always, often и sometimes не говорят…» — утверждение
того же рода, но методист его не называл, и без его слова я его не ставлю.

## Прогоны (после обеих правок)

- `npx tsc --noEmit` — без вывода, чисто.
- `npm run --silent check:content` — «Ошибок нет». По обоим местам замечаний нет. Единственное сведение о
  модуле 14 — «проверочная: показываются все вопросы банка» — старое и к правке не относится.
- `npm run yazyk -- english-beginner-2` — «Ошибок: 0, вопросов: 0».
- `npm run sosedi` — «Против HEAD: правок, у которых сосед остался прежним, не нашлось». Оба файла в истории,
  так что скрипт их видит.
- `git diff` — ровно две правки: строка правила в `kak-chasto.ts` и поле `why` в `ya-ne-delayu.ts`.

# Второй заход — 15 сентября 2026

Задание: двенадцать заглушек в работах модулей 9–12 (часть А), перевод карточек `isn't` и `is not` в модуле 7
(часть Б), две правки об артикле `the` в модулях 7 и 8 (часть В). Пишу по ходу.

Все три части сделаны, отмечено в заголовках разделов.

## Часть А. Двенадцать заглушек в работах модулей 9–12 — СДЕЛАНО

Опора по каждому вопросу — `reshenie-metodista-bliznetsy-15-sentyabrya.md`, место указано. Английские строки
заглушек перенесены дословно, ответы, `accept` и варианты не тронуты.

### Модуль 9, `q13-cvet-knigi-nurlana` (место 5)

Условие: «Дане оставили две записки. Первая: Hello, Dana! My book is big. It is red. The small book is not my
book. Ben. Вторая: Hello, Dana! My book is small. It is blue. The big book is not my book. Nurlan. Ответь одним
словом, какого цвета книга Нурлана.»
Разбор: «blue. Нурлан подписал вторую записку. В ней он пишет, что его книга маленькая, и следом называет её
цвет. Красная книга у Бена.»
Сверено: цвет ответа по-русски в условии не назван; «красная» стоит только в разборе, после ответа. Рамка
«Ответь одним словом» не совпадает с соседним вопросом 14 («Запиши одним словом цвет машины»). Второго верного
нет: сверка снимает регистр (`Blue` засчитается). Разбор называет только напечатанное в условии.

### Модуль 10, `q1-ispravit-zapis-o-dyade` (место 11)

Условие: «Ученик написал о своём дяде строку с одной ошибкой: My uncle is a strong. Запиши эту строку верно.»
Разбор: «My uncle is strong. Артикль ставится перед названием человека, а слово strong говорит о том, какой
дядя, поэтому перед ним артикля нет. Правило разобрано в уроке «Молодой и старый: He is an old man».»
Сверено: разбор не называет `a strong man` из `accept` — законно: `accept` может принимать, разбор не учит.
Правило урока 2 (`sluchay-artikl-pri-nazvanii`) пересказано своими словами, не дословно — проверка «разбор
работы не повторяет урок». «Запиши её без ошибки» не взято: «её» читалось бы и как «ошибку». Адрес урока —
видимым именем, по `title:` модуля. Рамка модуля 9 «В этой строке одна ошибка. Запиши строку без ошибки» не
повторена.

### Модуль 10, `q14-nazvat-nedostayushchee-svedenie` (место 6)

Условие: «Алим написал Киму сообщение о брате: Hi, Kim! This is my brother Nurlan. He is eight years old.
Alim. Проверь это сообщение по списку из урока «Пишем сообщение о человеке» и отметь, чего в сообщении Алима не
хватает.»
Разбор: «Слов о том, какой человек. Имя Нурлана и его возраст в сообщении названы, а слов о том, какой он, нет
ни одного.»
Варианты не тронуты, верный вторым. Слова «список самопроверки» заглушки не взяты: урок зовёт это «списком»
(«сверь своё сообщение по списку ниже»), «самопроверка» — наше слово. «В нём» после имени урока не поставлено:
урок мужского рода, прочлось бы «в уроке». Номер пункта списка из разбора убран — ученик нумерации не видит.

### Модуль 11, `q8-zapisat-vopros-o-cvete` (место 3)

Условие: «Ты спрашиваешь Дану о цвете её брюк, и она отвечает: They are blue. Запиши свой вопрос и назови в нём
брюки.»
Разбор: «What colour are your trousers? Слово trousers стоит во множественном числе, поэтому Дана отвечает
строкой с are, и в вопросе стоит та же форма. Перед названием стоит your, потому что ты обращаешься к самой
Дане.»
Оговорки методиста исполнены: спрашивает сам ученик у Даны (снимает `her trousers`), брюки названы в вопросе
(снимает `What colour are they?`), настоящее время вместо «ты спросил». «Целиком» не взято: так уже говорит
вопрос 5 той же работы.
**Отступление от заглушки, с доводом.** Заглушка велела сказать «форма be показывает, что вещей несколько». Брюки
— одна вещь, и урок 4 того же модуля так и называется: «Брюки и очки: одна вещь, а слово во множественном».
Разбор, сказавший «вещей несколько», спорил бы с уроком; взято имя урока — «множественное число».

### Модуль 12, `q11-ispravit-stroku-o-kartinah` (место 1)

Условие: «Ученик написал о кухне: There is two pictures in the kitchen. Картин на кухне и правда две, поэтому
число оставь прежним. Запиши строку без ошибки.»
Разбор: «There are two pictures in the kitchen. Картин две, а при нескольких вещах оборот начинается словами
There are. Окончание у слова pictures уже стоит, поэтому исправить надо одно слово.»
Разбор соседнего вопроса 10 («поэтому в обороте стоит форма для нескольких вещей») не повторён дословно.

### Модуль 12, `q12-zapisat-stroku-o-stule` (место 10)

Условие: «Ким спрашивает, где стул. Стул стоит перед дверью. Ответь Киму строкой, которая начинается словами The
chair.»
Разбор: «The chair is in front of the door. Ким спрашивает о самом стуле, поэтому строка начинается с его
названия. За названием идёт is, а место названо в конце строки.»
«Ким спросил» заменено настоящим временем — не ради рода (Ким не ученик), а ради ровного строя с условием.

### Вопросы методисту по части А (ответы и `accept` не правил)

1. Модуль 12, `q11`: ученик, написавший число цифрой, `There are 2 pictures in the kitchen.`, получит «пока не
   так». Условие велит «число оставь прежним», и цифра это число. Вписывать ли в `accept`?
2. Модуль 12, `q12`: `The chair's in front of the door.` начинается словами The chair и верна; в модуле 9 и в
   вопросе 1 модуля 12 стяжение принимается (`The table's big.`, `There's a book.`). Здесь `accept` нет.
3. Модуль 11, `q8`: `What colour are your trousers, Dana?` и `Dana, what colour are your trousers?` — верные
   вопросы с обращением, не принимаются. Решать ли условием или `accept`?
4. Модуль 12, комментарий над `q12` («ЭТО ВТОРОЙ ВОПРОС НА ИТОГ 4, и вещи у них разные: там радио и телевизор,
   здесь коробка и окно») устарел после смены предмета: теперь стул и дверь. Комментарий не мой — сборщику.

## Часть Б. Модуль 7, урок 3, словарь `slova-uroka-eto-ne-ruchka` — УТВЕРЖДЕНО без правки

`isn't` — «не», `is not` — «не (полная запись)». Опора — `reshenie-metodista-dolgi-15-sentyabrya.md`, пункт 6.3.

Доводы, проверенные по видимому тексту, а не по замыслу:
- **«не» у `isn't`.** Случай перед словарём переводит `It isn't a clock.` как «Это не часы», а случай 1 урока
  прямо говорит: «По-русски мы вставляем в поправку слово «не». По-английски на его месте стоит слово not». Русское
  соответствие одно, и оно названо первым. Форма `is` в «не» не переводится — так же, как у `don't` в модуле 14.
- **«(полная запись)» у `is not`.** Слово не новое для модуля 7: урок 2 уже говорит «Из полной записи It is a
  door. выпадает буква i» (`chto-eto-takoe.ts`, строка 1301), а разбор задания того же урока 3, стоящий после
  словаря, — «В полной записи после формы is стоит слово not. В короткой записи…» (строка 2185). Модуль 14 зовёт
  пару так же. Одно имя вещи по курсу держится.
- Скобка расширяет, а не сужает: говорит о записи, а не о значении, и значение у обеих карточек одно, как и сказано
  в случае («Значение строки при этом не меняется»).

## Часть В. Артикль — СДЕЛАНО

Опора — `razbor-programma-i-the.md`, раздел 2.4, пункты 2 и 3.

### Модуль 7 «Что это такое», урок 1, случай `sluchay-artikl-pered-veshchyu`

Было: «По-русски мы говорим просто «это билет». По-английски перед названием вещи встаёт ещё одно слово, артикль.
У всех вещей этого урока это a.»
Стало: «По-русски мы говорим просто «это билет». По-английски перед названием вещи стоит артикль. Он разобран в
уроке «Один из таких: a и an» модуля «Откуда ты и где живёшь». У всех вещей этого урока это a.»

Адрес — урок «Один из таких: a и an», а не «Тот самый: the», названный в задании: случай говорит об `a`, и имя
«артикль» вместе с `a` и `an` дано именно там (разбор методиста, 2.1, пункт 2; `otkuda-ty.ts`, `title` на строке
1671). Так же ссылаются модули 6, 10 и 11 (`moya-semya.ts:2405`, `kakoy-chelovek.ts:1229`, `odezhda.ts:1205`).
«Ещё одно слово» снято: оно и подавало артикль новым. Английской строки в правило не добавлено — второй кнопки
звука нет; тире в строке нет. Устаревший комментарий над случаем («Поля нет, пока урок… не написан») — сборщику.

### Модуль 8 «Один и много», урок 1 «Яблоко и яблоки: a уходит»

Поиск по видимому тексту (`npm run tekst -- english-beginner-2 odin-i-mnogo`, все строки со словом «артикл»):
в уроке 1 **обобщённых мест пять в четырёх строках, а не три** — вступление говорит «артикль» дважды, и тело
русской параллели («пропадает артикль, который стоял впереди») обобщает так же, как её заголовок.

1. Вступление. Было: «Пока вещь одна, перед её названием стоит артикль. Как только вещей становится несколько,
   артикль пропадает…» Стало: «Пока вещь одна, перед её названием стоит артикль a или an. Как только вещей
   становится несколько, этот артикль пропадает…» («a или an» — потому что сцена сразу за вступлением говорит
   `an apple`, а тело случая 3 так и пишет «артикль a или an».)
2. Заголовок `russkaya-parallel`. Было: «…а по-английски уходит ещё и артикль». Стало: «…уходит ещё и артикль a».
3. Тело `russkaya-parallel`. Было: «…а вдобавок пропадает артикль, который стоял впереди». Стало: «…пропадает
   артикль a или an, который стоял впереди».
4. Заголовок `sluchay-neskolko-s-i-a-uhodit`. Было: «Когда вещей несколько, артикль уходит». Стало: «Когда вещей
   несколько, артикль a уходит».

**Заголовок урока не менялся:** он уже «Яблоко и яблоки: a уходит», значит ссылок по курсу править не нужно.
Не тронуто, с доводом: «Место перед названием занимает само число, и артикля там уже нет» (случай 4) говорит о
строке с числом, и заголовок над ним уже «название стоит без a»; «Что значит артикль a» и разборы с «артиклем a»
верны.

**Вопросы методисту (вне порученного, не правил):**
- модуль 8, урок 4 «Когда называют саму еду: rice, bread, milk»: «Когда называют саму еду, артикль перед
  названием не ставят», заголовок «Перед словом о самой еде артикля нет», подпись таблицы «Еда — без артикля».
  Это правило о всяком артикле, а `the bread` и `the milk` — законный английский. Поиск `the milk|bread|water|
  cheese|meat|juice|tea|coffee|rice|soup` по видимому тексту курса пуст — собственный курс его пока не опровергает,
  но порода та же, что у урока 1; сужать ли до «артикля a»?
- модуль 8, урок 9, случай «На вопрос о самой вещи отвечают её названием, а впереди названия ставят артикль» и
  разбор вопроса работы `q1` «артикль впереди ушёл» — оба о строке с `a`, методист второй счёл необязательным.

## Прогоны после всех правок

- `npx tsc --noEmit` — без вывода, чисто.
- `npm run --silent check:content` — «Ошибок нет». По изменённым местам замечаний нет; о уроке
  `yabloko-i-yabloki` только старое сведение о минутах. «Разобрано раньше… 63».
- `npm run yazyk -- english-beginner-2` — «Ошибок: 0, вопросов: 0».
- `npm run sosedi` — «Против HEAD: правок, у которых сосед остался прежним, не нашлось».
- `npm run --silent bliznetsy -- english-beginner-2 --hudshie 40` — «0 худших из 0», «Близнецов: 0. Разобрано
  раньше и потому пропущено: 14». То есть шесть заполненных вопросов теперь сверяются как текст, а не пропускаются
  заглушками, и ни одного совпадения не дали.
- `git diff --stat` — шесть файлов курса и этот журнал. Заглушек «ПИШЕТ РЕДАКТОР:» в `cvet-i-razmer`,
  `kakoy-chelovek`, `odezhda`, `moy-dom` — по нулю.
- Попутно: одной командой был запрошен `git stash list` — это только чтение, дерево не тронуто.

Звук: условия и разборы работ звука не имеют; в правках модулей 7 и 8 английские строки с кнопками не менялись,
переозвучка не нужна.

## Третий заход (16 сентября 2026)

Задание: три места по решению методиста (`plan-rabota-chasti-2.md`, раздел 12,
пункты 1, 4, 5). Работа идёт по ходу, записи ниже дописываются.

### Место 1. Модуль 12, работа, `q11-ispravit-stroku-o-kartinah` (`moy-dom.ts`)

Решение 12.1: отказ от цифры должен быть честным — условие говорит, что меняется
одно слово.

Было: «…Картин на кухне и правда две, поэтому число оставь прежним. Запиши строку
без ошибки.»

Стало: «…Картин на кухне и правда две, поэтому слово two остаётся как есть.
В строке неверно одно слово, и поменять надо только его. Запиши строку целиком.»

Почему: «число оставь прежним» не говорит, ЧЕМ его записывать, и ученик вправе
поставить `2`. Названное слово `two` закрывает цифру, а «неверно одно слово»
делает отказ от `2` невыполненным условием, а не наказанием за правоту. Ответ и
`accept` не тронуты.

### Место 2. Модуль 8, урок 4 `sama-eda-rice-bread-milk` (`odin-i-mnogo.ts`)

Решение 12.4: «артикль» сужается до «артикль a», и говорится, что речь о еде
вообще. Английские строки не тронуты ни одной.

**Мест вышло 14, а не 11.** Список методиста собран поиском по строчной букве, и
три места с ЗАГЛАВНОЙ «Артикл…» в него не попали: вступление урока, правило
случая «На вопрос отвечаем одним словом» и правило случая «Слово water».
Плюс одно место вне урока 4 — разбор `q7-goloe-slovo-v-ryadu` работы модуля, где
то же правило сказано дословно тем же оборотом.

Дословно по каждому месту:

1. вступление: «Артикля **a** впереди него нет».
2. имя случая 1: было «Перед словом о самой еде артикля нет» → стало «Перед
   словом о самой еде нет артикля **a**».
3. правило случая 1: было «Когда называют саму еду, артикль перед названием не
   ставят» → стало «Когда называют саму еду **вообще**, артикль **a** перед
   названием не ставят». Слово «вообще» — та самая граница, которую назвал
   методист графой EGP `FORM/USE: NO ARTICLE`.
4. имя случая «ответ одним словом»: «…и артикля **a** в ответе нет».
5. правило того же случая: «Артикль **a** впереди не появляется и здесь».
6. имя случая water: «Слово water стоит без артикля **a**».
7. правило того же случая: «Артикля **a** перед словом water нет…».
8. правило случая cheese и meat: «…поэтому артикля **a** впереди нет…».
9. подписи столбцов таблицы: было «Вещь — с артиклем» и «Еда — без артикля» →
   стало «Вещь — с артиклем **a или an**» и «Еда — без артикля **a**». В левом
   столбце три строки из четырёх с `an`, поэтому там названы оба.
10. правило случая «в списке»: «…без числа и без артикля **a**».
11. условие задания `e1-razlozhit-stroki`: было «без артикля и без числа
    впереди» → стало «без артикля **a или an** и без числа впереди». Оба названы
    нарочно: среди строк стоит `an apple`, и «без артикля a» дало бы ученику
    повод счесть её голой.
12. разбор того же задания: «…впереди стоит артикль **a или an**…».
13. разбор задания `e3-otvetit-odnim-slovom`: «…поэтому артикля **a** впереди
    нет…».
14. подсказка задания `e6-nazvat-tri-edy`: «Ни артикля **a**, ни числа впереди
    этих названий не ставят».
15. разбор `q7-goloe-slovo-v-ryadu` работы модуля: «…у слова нет ни артикля
    **a** впереди, ни окончания на конце».

Строка 2341 (случай «Рядом с вещью разница видна сразу») не тронута — там
«артикль» сказано о вещи, как и велел методист.

Имя урока, его итог и итоги модуля правки не требовали: там с самого начала
стоит «называть саму еду **без a** и без -s». Свода, повторяющего это правило,
в модуле нет — блоков `note` два, и оба о другом (перевод списка, два вида
ответа).

### Место 3. Модуль 12, урок 2 `dva-stula`, случай `sluchay-obe-formy-v-odnoy-zapisi`

Решение 12.5: форму берут по вещи сразу за оборотом, а при перечне через `and`
вещь может быть не одна. Английские строки урока не тронуты, новая строка взята
у методиста дословно.

Было (правило случая): «В записи о комнате обе формы стоят рядом. Форму выбирают
не для всей записи, а для каждой строки отдельно, по числу вещей, о которых эта
строка говорит.»

Стало: «В записи о комнате обе формы стоят рядом, и для каждой строки форму
выбирают заново. Берут её по той вещи, которая названа сразу за оборотом.
В строке про кровать и стол сразу за оборотом названа одна кровать, поэтому
форма там is, а стол идёт после and.»

Третьим примером встала строка методиста с моим переводом:
«There is a bed and a table. — Есть кровать и стол.» Строка добавлена и в
`zvuchat` случая.

**Записи звука это НЕ требует, и проверено прогоном, а не чтением.** Имя файла
считается от текста, а `There is a bed and a table.` дословно звучит в
замороженном курсе `english-starter` (`zdes-est.ts`), поэтому файл
`public/zvuk/slovo/d94c18392e46cd35.mp3` уже лежит. `check:content` молчит о
записи по той же причине.

**Шесть мест методиста перечитаны, ни одно правки не потребовало:** записка
урока 5, объяснение того же урока, текст чтения `obyavlenie-o-kvartire` и
условие `q8-poschitat-komnaty-v-obyavlenii` — везде стоят английские строки и
русский текст рядом с ними о форме оборота не говорит.

**А правило оказалось повторено ещё в пяти местах, и все пять поправлены:**

1. случай 6 того же урока (`sluchay-svoi-formy`): было «форму выбирают по числу
   вещей, а не по букве на конце» → стало «форму выбирают **по тому, сколько
   их**, а не по букве на конце». Заодно ушло слово «вещи» о людях.
2. вступление урока письма: было «форма оборота в строке должна сойтись с числом
   вещей» → стало «форма **в каждой строке** должна сойтись с вещью, которая
   названа **сразу за оборотом**».
3. разбор образца записи Кима: было «форма оборота у него сходится с числом
   вещей в строке» → стало «…сходится с вещью, которая названа сразу за
   оборотом».
4. пункт самопроверки: было «При одной вещи стоит There is, при нескольких There
   are…» → стало «При одной вещи **сразу за оборотом** стоит There is, при
   нескольких There are…».
5. разбор задания-сочинения: было «форма оборота в каждой сходится с числом
   вещей» → стало «в каждой форма сходится с вещью, которая названа сразу за
   оборотом».

Без этих пяти список самопроверки урока письма объявлял бы ошибкой строку
`There is a bed and a table.`, которой учит урок 2 и которую печатают уроки 5
и 7, — то есть наказывал бы за верный ответ на самопроверке.

Итоги уроков и модуля («стоит ли There is при одной вещи и There are при
нескольких») не тронуты: они сверяются точным совпадением в четырёх местах
каждый, а новому правилу не противоречат.

### Прогоны

- `npx tsc --noEmit` — чисто.
- `npm run --silent check:content` — по моим модулям ошибок нет. Единственная
  ошибка курса о записях двух вопросов работы части 2 — файла сборщика, не моего.
- `npm run yazyk -- english-beginner-2` — ошибок по моим модулям нет. Одна
  ошибка курса («признак») стоит в заглушке работы части 2.
- `npm run sosedi` — правок, у которых сосед остался прежним, не нашлось.

### Вопросы методисту (третий заход)

1. **Модуль 8, урок 8, подсказка и разбор задания `g4-rasskazat-o-kartinke`:**
   «У того, что считают штуками, впереди стоит число, а у самой еды впереди не
   стоит ничего» и «Слово milk стоит голым, потому что названа сама еда». Слова
   «артикль» там нет, поэтому в список сужения место не попало, но утверждение
   того же объёма: «впереди не стоит ничего» закрывает и `the`. Сужать ли его
   тем же доводом или оставить как описание печатной строки — решать методисту.
2. **Список методиста собран поиском по строчной букве.** Три места в уроке 4
   начинались с заглавной «Артикл…» и в него не попали. Если сужение пойдёт
   дальше по курсу, искать надо обе буквы: `grep -i` в Git Bash кириллицу не
   складывает.

## Четвёртый заход (17 сентября 2026)

Решение методиста по моему вчерашнему вопросу — `plan-rabota-chasti-2.md`,
раздел 13.5: сузить одно место, подсказку `g4-rasskazat-o-kartinke`.

### Модуль 8, урок 8, подсказка `g4-rasskazat-o-kartinke` (`odin-i-mnogo.ts`)

Было: «У того, что считают штуками, впереди стоит число, а у самой еды впереди
не стоит ничего.»

Стало: «У того, что считают штуками, впереди стоит число, а **перед названием
еды не стоит ни числа, ни артикля a**.»

Слова «ничего» в подсказке больше нет, а сказанное сошлось по объёму с правилами
урока 4 после вчерашней правки. Английских строк не менял. Разбор того же
задания не тронут: «стоит голым» — наш термин, и границу ему дало вчерашнее
условие `e1-razlozhit-stroki` («без артикля a или an и без числа»).

### Четыре места с «не стоит ничего»: с тремя согласен, с одним нет

Проверил каждое по коду, номера после вчерашних правок сошлись.

**Согласен, это описание напечатанного:**

- строка 2528, разбор `e1-razlozhit-stroki`: «Перед этими тремя названиями не
  стоит ничего» — о трёх вариантах, которые ученик видит на экране;
- строка 3184, разбор `r4`: «В остальных строках впереди не стоит ничего» — о
  строках списка того же задания;
- строка 3901, разбор `q8` работы: «Перед словами cheese и bread не стоит
  ничего» — о двух словах строки `Cheese and bread.`

**Не согласен: строка 2342, правило случая `sluchay-ryadom-s-veshchyu`.**
Стоит: «Когда в одном ряду стоят вещь и еда, у вещи впереди стоит артикль, а у
еды **впереди не стоит ничего**.» Это не описание напечатанной строки, а правило:
подлежащее у него общее («вещь и еда»), а не «в строках ниже», и условие задано
словом «когда». По объёму оно ровно то же, что сужённая сегодня подсказка, и
«ничего» закрывает там и `the`. Примеры под ним — `an egg and bread` и `an apple
and milk`.

Сам не правил: методист назвал это место законным, а довод «описывает
напечатанные строки» к нему, по-моему, не подходит. Если решит сузить, правка
одна: «…а перед названием еды артикля a нет». Первую половину строки трогать
по-прежнему не надо — там «артикль» сказано о вещи.

### Прогоны

- `npx tsc --noEmit` — чисто.
- `npm run --silent check:content` — по модулю 8 ошибок нет. Единственная ошибка
  курса — записи двух вопросов работы части 2, файл сборщика.
- `npm run yazyk -- english-beginner-2` — ошибок по модулю 8 нет. Одна ошибка
  курса («признак») по-прежнему в заглушке работы части 2.
- `npm run sosedi` — правок, у которых сосед остался прежним, не нашлось.

## Пятый заход (17 сентября 2026): спор решён в мою пользу, и место сужено шире

Методист согласился, что `sluchay-ryadom-s-veshchyu` — правило, а не описание
(пометка к 13.5 от 17 сентября), и потребовал сузить ОБЕ половины. Довод против
первой половины я не увидел вчера, а он верный: «у вещи впереди стоит артикль»
опровергается собственным модулем дважды — `Three apples and cheese.` (тот же
урок, строка 2410) и `Three apples, two eggs and milk.` (урок 8, строка 3597):
перед вещью там стоит число.

### Модуль 8, урок 4, случай `sluchay-ryadom-s-veshchyu` (строки 2341–2343)

Было: «Когда в одном ряду стоят вещь и еда, у вещи впереди стоит артикль, а у
еды впереди не стоит ничего. Обе половины строки видно разом, и разницу между
ними искать не приходится.»

Стало: «Когда в одном ряду стоят **одна** вещь и еда, впереди вещи стоит
**артикль a или an**, а перед названием еды **такого артикля нет**. Обе половины
строки видно разом, и разницу между ними искать не приходится.»

Все три требования исполнены: речь об одной вещи, артикль назван поимённо, слова
«ничего» нет. Имя случая, оба примера (`an egg and bread`, `an apple and milk`)
и `zvuchat` не тронуты.

### Окружение перечитано

- **Имя случая** «Рядом с вещью разница видна сразу» держит то, что под ним:
  вещь там единственного числа, и правило теперь тоже.
- **Таблица `tablica-otvet-na-vopros` следом** — подписи «Вещь — с артиклем a или
  an» и «Еда — без артикля a», в строках `An apple.`, `An egg.`, `A cup.`,
  `A bottle.` против `Milk.`, `Bread.`, `Rice.`, `Water.`: везде одна вещь, с
  новым правилом сходится.
- **Случай `sluchay-v-spiske`** («У вещей в том же списке впереди стоит число»,
  пример `Three apples and cheese.`) теперь не спорит с соседом: ряд с числом
  разобран там, ряд с одной вещью — здесь.
- **Итог урока и итоги модуля** («называть саму еду без a и без -s») правки не
  требовали. Свода, повторяющего это правило, в модуле нет.
- Три места с «не стоит ничего» (2528, 3184, 3901) оставлены, как решено.

**Одно наблюдение без правки.** В уроке 8 «артикль» дважды назван без имени —
врезка `pravilo-dvuh-otvetov` («впереди названия ставят артикль») и подсказка с
разбором `g2-chto-eto` («при одной вещи впереди стоит артикль»). Оба места о
вещи, и оба о ней одной, так что ложного там ничего нет; но после сужения урока 4
курс называет артикль поимённо везде, кроме этих двух строк. Сам не правил —
методист назвал круг мест, и этих в нём нет.

### Прогоны

- `npx tsc --noEmit` — чисто.
- `npm run --silent check:content` — по модулю 8 ошибок нет. Единственная ошибка
  курса — записи двух вопросов работы части 2, файл сборщика.
- `npm run yazyk -- english-beginner-2` — **ошибок 0, вопросов 0** по всему курсу.
- `npm run sosedi` — правок, у которых сосед остался прежним, не нашлось.
