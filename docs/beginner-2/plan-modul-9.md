# Замысел модуля 9 «Цвет и размер»

6 сентября 2026. Пишет методист. **Весь видимый русский текст пишет редактор.**
Здесь стоят состав, порядок, английские строки, опоры, запреты и решения — и
ничего из того, что ученик прочтёт по-русски.

Подробный замысел первого урока — [plan-modul-9-urok-1.md](plan-modul-9-urok-1.md).

Строка программы ([programma-stupeni.md](programma-stupeni.md), раздел 8,
часть 2 «Вещи и место»):

| № | Модуль | Итог | Грамматика | Тема | Опора |
|---|---|---|---|---|---|
| 9 | Цвет и размер | назвать цвет и размер и спросить о цвете | прилагательное, `very` | Colours | CEFR с. 79, 67 |

**Обе опоры программы проверены постранично и обе верны.** Разбор — раздел 2.

---

## 0. Шапка: три вещи, названные ДО первой строки редактора

Правило приписано 6 сентября методистом модуля 8: два имени назвали после того,
как редактор написал весь текст, и в уроке стало двадцать восемь заглушек вместо
замены слова.

### 0.1. Имя главной вещи модуля — **прилагательное**

Одно на модуль, других имён у этой вещи нет ни разу. Ни «признак», ни «слово о
вещи», ни «описание», ни «качество».

**Почему настоящий термин, а не своё слово.** У вещи есть имя в русской
грамматике, и ученик его знает из школы. Правило проекта прямое: «есть ли у этой
вещи имя в русской грамматике? Если есть — берём его и объясняем на месте».
Курс так же ввёл «местоимение», «предлог», «артикль», «множественное число».

**И это ровно то место, где курс однажды обжёгся.** Модуль 11 замороженного
курса звал прилагательное **признаком**, и владелец споткнулся первым: признак
предмету присущ, а `new`, `small`, `red` говорят о том, что меняется.

**Где имя называется:** урок 1, случай 1, строкой внутри случая — не отдельным
экраном. Правило от 6 сентября: «настоящий термин называется всегда, но
отдельного экрана не получает». Экран без новой английской строки ученик
проходит как пустой.

### 0.2. Заимствованные термины и адреса, где они введены

| Термин | Кто дал | Как модуль 9 им пользуется |
|---|---|---|
| **артикль** | модуль 3 «Откуда ты и где живёшь», уроки «Один из таких: a и an» и «Тот самый: the» | берём как известное. Урок 4 добавляет ОДНО: что стоит между артиклем и названием вещи |
| **множественное число** | модуль 8 «Один и много», урок 1 | берём как известное. Урок 5 добавляет ОДНО: прилагательное окончания `-s` не получает |
| **местоимение** | модуль 2 «Здравствуйте, меня зовут» | нужно уроку 2: `it` вместо названия вещи |
| **наречие** | **никто, и модуль 9 его НЕ вводит** | разбор — раздел 7.2 |

### 0.3. Общее слово, которым модуль зовёт то, о чём говорит, — **вещь**

Как у модуля 7 и по его же доводу, записанному дословно: «„предмет" по-русски
значит ещё и школьный предмет, а тема `Education` придёт модулем 13».

**Осторожно, модуль 8 взял «предмет» — двадцать с лишним раз.** Расхождение между
модулями 7 и 8 уже есть, и чинить его не моё дело; я держу слово модуля 7,
потому что модуль 9 стоит на его вещах (`car`, `box`, `table`, `clock`, `door`)
и на его слове `thing`, у которого карточка так и переведена — «вещь».

**Строка сводящему:** расхождение «вещь» (модуль 7) против «предмет» (модуль 8)
надо свести на всю часть 2 разом, а не помодульно. Я выбираю «вещь» и называю
это здесь, чтобы следующий не выбирал заново.

**И слово «размер» — русское, служебное, в объяснениях не работает.** Оно стоит
в названии модуля и больше нигде: английского `size` модуль не даёт (раздел
6.4), а по-русски о вещи говорят «большая», «маленькая», «длинная», «короткая».

---

## 1. Сперва курс, потом источник

Правило проекта: проверка по курсу идёт ПЕРВОЙ. Она нажита на четырёх случаях
подряд, и здесь она сработала пять раз.

### 1.1. Что нашлось в коде нового курса

Прогоны:

```
grep -n "outcome:\|title:" courses/english-beginner-2/*.ts | grep -iE "цвет|размер|какой"
grep -rn 'term: "' courses/english-beginner-2/*.ts | grep -iE "colour|big|small|red|blue|orange|new|old|light"
grep -rn "размер|прилагательн|наречи|степен" courses/english-beginner-2/*.ts
```

**Находка 1. `black` — УЖЕ КАРТОЧКА, и не там, где ждёшь.**
`alfavit.ts`, урок «Когда буквы читаются вместе»:
`{ term: "black", translation: "чёрный", example: "B-L-A-C-K. Black.", hint: "/blæk/", znak: "2b1b" }`.
Слово взято ради сочетания `ck`, а перевод у него полный — «чёрный». Значит
модуль 9 карточку `black` **не даёт**, а употребляет как известную. Это же
закрывает долг модуля 7: обещанная им строка `It is black.` стоит на слове,
которое ученик получил ещё в модуле 1.

**Находка 2. `light` — карточка модуля 1 со значением «свет».** В словнике A2 Key
`light` стоит в теме `Colours` (там оно значит «светлый»). Второе значение модуль
9 **не берёт**: `light blue` — два прилагательных подряд, а это A2 (раздел 3.2).

**Находка 3. `old` — карточка модуля 4** (`chisla-i-telefon.ts`), перевод
«старый», пример `I am ten years old.` Модуль 9 карточку не повторяет, а
разбирает второе употребление того же слова: `The car is old` — не о возрасте
человека, а о вещи. Это законный случай урока 1: «граница называется не в
карточке, а там, где ученик на неё наткнётся».

**Находка 4. `orange` — карточка модуля 8 со значением «апельсин», и модуль 8
прямо запретил себе второе значение.** Разбор и решение — раздел 6.3.

**Находка 5. Слов «размер», «прилагательное», «наречие», «степень» в ВИДИМОМ
тексте курса нет ни разу.** Все совпадения `grep` лежат в полях `sources` и в
комментариях. Значит все четыре имени свободны.

**Чего в курсе НЕТ:** ни одного урока о цвете, ни одного о размере, ни одной
строки с прилагательным перед существительным. Прилагательное после `be` один
раз взято формулой — `privetstvie.ts`, `I am fine`, — и замысел модуля 2 сам
объявил, что разбирают его модули 9 и 10 (`plan-modul-2-uroki-1-3-8.md`,
строки 107 и 780).

### 1.2. Что нашлось в замыслах соседей

Пока модуль не написан, его замысел И ЕСТЬ модуль. Прочитаны целиком
`plan-modul-2.md` … `plan-modul-8.md` и разборы к ним. Модулю 9 адресованы
**восемь** строк, и все восемь я принимаю или отвечаю на них.

| Откуда | Что сказано | Мой ответ |
|---|---|---|
| `plan-modul-2-uroki-1-3-8.md`, стр. 107, 780 | прилагательное после `be` роздано модулям 9 и 10, в модуле 2 взято формулой | принято, разбирает урок 1 |
| `plan-modul-3.md`, стр. 151, 453, 891 | `big` и `small` модуль 3 НЕ берёт; пункты `'A' + ADJECTIVES` и `'A' + 'VERY' + ADJECTIVES` — мои; просьба о карточке `small` отозвана | принято, карточки мои, пункты беру (раздел 4.1) |
| `plan-modul-6-urok-1.md`, стр. 90, 171 | `big` снят из модуля 6 нарочно, «она нужна модулю 9» | принято |
| `plan-modul-6.md`, стр. 524, 749 | `teenager`, `love`, `married`, `friendly`, `group` — модулям 9 и 10; пункт `ADJECTIVES combining` — модулям 9 и 10 | все пять слов о людях — модулю 10; пункт `combining` тоже модулю 10, разбор в разделе 4.3 |
| `plan-modul-7.md`, стр. 234, 266 | цвет и размер вещи — модули 9 и 10; графа `FORM: 'A' + ADJECTIVES` роздана модулю 9; дескриптор CEFR с. 67 — не модуля 7 | принято целиком, включая урок письма (раздел 5.4) |
| `plan-modul-7-urok-1.md`, стр. 181 | **долг с адресом:** `it` в значении «он / она / оно» приходит в модуле 9, где появляется `It is black.` | закрывает урок 2; строка `It is black.` стоит там дословно |
| `plan-modul-7.md`, стр. 575 | карточка `it` переведена «это» нарочно; «оно» работает в строе, розданном модулю 9 | принято; карточку `it` модуль 9 не повторяет, второе соответствие называет случаем |
| `plan-modul-8.md`, стр. 1151–1188 | пункт `ADJECTIVE + PLURAL NOUN` (A1) предложен модулю 8, тот отказался: «пункт свободен и по смыслу ваш: `red apples`, `big boxes`» | **беру**, урок 5 (раздел 4.1) |

### 1.3. Что берём со склада замороженного курса

`courses/english-starter/kakoy-on.ts` — модуль 11 «Какой он», 11 уроков. Он
покрывает предмет модулей 9 И 10 нового курса сразу, поэтому берём половину.

**Берём:**

1. **Готовые транскрипции** — они уже приведены к условностям курса (`r`, `g`,
   слоговой согласный, конечное `ə`): `big /bɪg/`, `small /smɔːl/`,
   `new /njuː/`, `long /lɒŋ/`, `short /ʃɔːt/`, `colour /ˈkʌlə/`, `red /red/`,
   `blue /bluː/`, `green /griːn/`, `yellow /ˈjeləʊ/`, `white /waɪt/`,
   `brown /braʊn/`, `orange /ˈɒrɪndʒ/`, `pink /pɪŋk/`, `grey /greɪ/`,
   `very /ˈveri/`, `favourite /ˈfeɪvrɪt/`, `car /kɑː/`.
2. **Три выписки из источников, проверенные заново** (раздел 2): CEFR с. 79 и
   с. 67 и перечень пунктов A1 о прилагательных.
3. **Устройство пары «чтение → письмо»**: там урок чтения даёт жанр, а урок
   письма просит написать такой же текст. Правило проекта требует ровно этого.

**Не берём:**

1. **Ярлык «признак»** — снят владельцем, и это главная причина, по которой
   шапка модуля 9 называет термин первой строкой.
2. **`FOR SALE` и жанр объявления о продаже.** `sale` у Oxford стоит на **A2**
   (`npm run slovo -- sale`), а курс на A1. Жанр модуля 9 — записка, разбор в
   разделе 5.4.
3. **Уроки о человеке** («Какой человек», «Мой любимый», «Описать целиком») —
   это модуль 10, и брать их сюда значит написать второй такой же урок.
4. **Врезку `pochemu-the-i-a`** — она объясняет то, что в новом курсе уже
   объяснено модулем 3 двумя уроками.
5. **Ни одного задания и ни одного вопроса работы** — правило переделки.

### 1.4. Столкновение с модулем 11: оба «спрашивают о цвете»

**Это самое опасное место замысла, и найдено оно проверкой по курсу, а не по
источнику.** Программа даёт модулю 11 «Одежда» итог **«сказать, что надето, и
спросить о цвете»** — то есть тот же вопрос, что и модулю 9.

Ровно так курс уже обжёгся: модули 12 и 26 ступени Elementary порознь написали
`Why don't we…?`, и ученик получил два несовместимых разбора одной строки.

**Граница, которую предлагаю, и её надо записать ОБОИМ** (правило: передачу
записывает и тот, кто отдал, и тот, кто взял):

| Модуль 9 | Модуль 11 |
|---|---|
| вводит все карточки цвета | не вводит ни одной, пользуется данными |
| вводит и разбирает `What colour is it?` | берёт вопрос как известный, ставит его в `egpPovtor`, устройства не разбирает |
| вводит прилагательное: место, множественное, `very` | добавляет своё — `have got` и одежду |
| вещи: машина, коробка, стол, часы, дверь, еда | одежда, и только она |

**Если модуль 11 решит иначе — это спор методистов, и его сводит автор**
(раздел 10, строка 1). Но `What colour is it?` дважды в одной ступени быть не
должно ни при каком исходе.

---

## 2. На чём модуль стоит: дескрипторы

**Правило ступени: нет дескриптора — модуль не заводится.** У модуля 9 их
четыре, и все четыре я открыл сам.

### 2.1. Дескриптор ДЕЛА — CEFR Companion 2020, с. 79, графа A1

Шкала *Information exchange*, дословно:

> **Can name the colour of clothes or other familiar objects and can ask the
> colour of such objects.**

Это точное описание дела модуля: назвать цвет вещи и спросить о цвете.

**СТРАНИЦА ОПАСНАЯ, И ЧИТАЛ Я ЕЁ ДВУМЯ РАЗБОРАМИ.** CLAUDE.md прямо
предупреждает: на с. 79 два разбора ставят пометку ступени в разных местах.
Первый разбор выдаёт строку так:

```
с. 79: … can ask the colour of such objects. Pre-A1
```

то есть приклеивает к ней **Pre-A1**. Это ложь разборщика. Второй разбор
(`materials/.md/cefr-companion-2020.md`, строки 3704–3717) показывает устройство
графы: метка `A1` стоит В НАЧАЛЕ своего блока (строка 3704, «A1 Can ask and
answer questions about themselves…»), метка `Pre-A1` — в начале следующего
(строка 3713). Строка про цвет — пятая строка блока **A1**.

**Ступень дескриптора: A1.** Возьми я её у первого разбора — модуль получил бы
Pre-A1, и вся его грамматика поехала бы ступенью ниже.

### 2.2. Дескриптор ПИСЬМА — CEFR Companion 2020, с. 67, графа A1

Шкала *Creative writing*, дословно:

> **Can use simple words/signs and phrases to describe certain everyday objects
> (e.g. the colour of a car, whether it is big or small).**

Тут та же ловушка и тот же ответ: первый разбор приклеивает «Pre-A1 No
descriptors available», второй (строки 3103–3107) показывает, что `A1` открывает
блок из двух строк, а `Pre-A1` — «No descriptors available».

**Источник называет прямо и цвет, и размер, и даже машину** — обе половины
названия модуля стоят в одной графе. Соседняя строка того же блока («Can describe
in very simple language what a room looks like») — модуля 12, и замысел модуля 7
это уже записал.

### 2.3. Дескрипторы Cambridge — Young Learners, с. 9, Pre A1 Starters

*Can Do summary*, столбец **LISTENING AND SPEAKING**, дословно две строки:

> **CAN understand some very simple spoken descriptions of everyday objects –
> such as how many, colour, size or location.**
>
> **CAN give very basic descriptions of some objects and animals – such as how
> many, colour, size or location.**

Те же две строки стоят на **с. 8**, в таблице шкалы Cambridge English («Pre A1 /
Below 100»). Проверено по обеим страницам, а не по оглавлению: разборщик этого
файла местами подмешивает в страницу-оглавление содержание страницы, на которую
она ссылается, и программа ступени об этом предупреждает.

**Строку делят три модуля, и это уже записано соседом.** `plan-modul-8.md`,
строка 1220: «`how many` мой, `colour` и `size` модуля 9, `location` модуля 12».
Принимаю.

**Столбец READING AND WRITING той же графы описания вещей не даёт вовсе** — там
только алфавит, копирование и правописание. Значит опора урока письма у модуля 9
одна: CEFR с. 67. Говорю это прямо, чтобы следующий проверяющий не искал у
Cambridge того, чего там нет.

### 2.4. Опоры уроков умений

| Урок | Опора |
|---|---|
| чтение | CEFR **с. 54**, Overall reading comprehension, A1: «Can understand very short, simple texts a single phrase at a time, picking up familiar names, words and basic phrases and rereading as required». И **с. 56**, Reading for orientation, A1 |
| слушание | CEFR **с. 49**, Understanding conversation between other people, A1. Формат ответа — A2 Key, **с. 29**: «The candidates should only write ONE word, or a number, or a date, or a time for their answer» |
| говорение | Young Learners, **с. 28**, Pre A1 Starters Speaking Part 3 и Part 4 (раздел 3.1) |
| письмо | CEFR **с. 67**, A1 (раздел 2.2). И **с. 66**, Overall written production, A1: «Can produce simple isolated phrases and sentences» |

**Оговорка о соседней графе, чтобы её не искали заново:** CEFR, **с. 68**, шкала
Reports and essays, графы A1 и Pre-A1 — «No descriptors available». Сочинения на
ступени нет, и урок письма модуля 9 — записка из четырёх строк, а не текст.

---

## 3. Расхождение источников: берём по Cambridge

**Правило владельца от 6 сентября 2026** распространено на весь класс: где
руководство Cambridge даёт оборот на Pre A1 Starters, а English Grammar Profile
кладёт его на A2, курс берёт по Cambridge. **Но графа называется каждый раз
заново**, и перенос вывода с соседнего случая запрещён. Ниже — своя проверка.

### 3.1. What colour is it? — расхождение есть, берём по Cambridge

**English Grammar Profile, `1741163715031x898125206259626100`,
`QUESTIONS / wh-`, FORM: MAIN VERB 'BE', уровень A2:** «Can use 'wh-'words +
main verb 'be' + subject to form 'wh-' questions», примеры «How was your dinner
yesterday?», «How are you, my friend?», «Where are you, men?».

**Руководство Cambridge, Young Learners, с. 28, Pre A1 Starters Speaking Part 3,
HOW TO APPROACH THE TASK**, дословно:

> Candidates should practise answering simple questions about a picture (with
> one-word answers). For example:
> What’s this? (crocodile) **What colour is it? (green)** What’s your favourite
> animal? (cat)

Строка напечатана у Cambridge как есть, вместе с ответом одним словом, в
описании самой младшей ступени. Берём.

**Там же, с. 28, Speaking Part 4** — вторая строка, и она моя же:

> …can answer questions such as: How old are you? What’s your friend’s name?
> **Is your house/apartment big or small?** Can you play table tennis?
> **What’s your favourite colour**/animal/sport/food?

То есть у Cambridge на Pre A1 Starters стоят все три вопроса модуля: о цвете
вещи, о размере вещи и о любимом цвете.

**И тот же файл держит грамматику — с. 29, Grammar and Structures List,
Pre A1 Starters:**

- строка `Adjectives` — «**He’s a small boy.**» (артикль, прилагательное,
  название — это урок 4);
- строка `Nouns` — «**Lemons are yellow.**» (несколько вещей и прилагательное
  после `are` — это урок 5);
- строка `Determiners` — «**These books are blue.**» (то же).

**Расхождение объявляется в шапке модуля отдельной строкой** — иначе следующий
проверяющий снимет оборот как превышение ступени и будет прав по своему
источнику.

**И одна оговорка, чтобы решение не прочли шире, чем оно есть.** English Grammar
Profile расходится сам с собой: `How are you?` стоит примером и у графы A1
(`CLAUSES / interrogatives`, FORM: AFFIRMATIVE INTERROGATIVE, WITH 'BE'), и у
графы A2 (`QUESTIONS / wh-`). Значит вопрос с `be` на A1 у источника есть, а
спорно ровно `wh-`-слово перед ним.

### 3.2. Чего расхождением НЕ является: два прилагательных подряд

`a big red car`, `light blue`, `dark green` — **на ступени их нет, и спора тут
нет вовсе**: A2 стоит у обоих источников.

**English Grammar Profile, `1741163711543x798815849394851200`,
`NOUNS / noun phrases`, FORM: NOUN PHRASES WITH ADJECTIVES, A2:** «Can pre-modify
noun phrases with a limited range of **more than one** adjective», пример «I
bought a beautiful pink skirt and a white top».

**Young Learners, с. 29**, строка `Adjectives` даёт ровно одно прилагательное:
«He’s a small boy». Двух подряд у Cambridge на Pre A1 Starters нет.

Отсюда три следствия:

1. `dark` и `light` как цветовые слова модуль не берёт — они работают только в
   паре (`dark blue`), и `pale` тоже (раздел 6.1);
2. **ни одна строка модуля не содержит двух прилагательных подряд** — это запрет
   на весь модуль, и его надо проверить `grep` перед сборкой;
3. **правило «двух прилагательных подряд не ставят» в тексте урока не пишется.**
   Графа A2 — это разрешение для A2, а не запрет для A1. Модуль учит своему
   случаю, а не отрицанию чужого. Ровно на этом курс обжёгся семь раз за день.

### 3.3. Слова «всегда», «только», «нельзя»: где просятся и почему их не будет

Прошёл по каждому месту, где абсолютное слово просится само:

| Место | Что просится сказать | Почему нельзя |
|---|---|---|
| урок 1 | «`is` ставят **всегда**» | графы, запрещающей опустить `be`, я не нашёл ни одной; и `How big?` без `be` — правильный английский |
| урок 4 | «прилагательное стоит **только** перед названием» | урок 1 учит обратному строю, и оба верны |
| урок 5 | «прилагательное **никогда** не получает `-s`» | пишем о СВОИХ словах: «`red` так и остаётся `red`», а не о языке вообще |
| урок 6 | «`very` стоит **только** перед прилагательным» | графа `ADVERBS / adverbs as modifiers`, FORM: 'VERY' + TIME ADVERBS (A1) разрешает `very often`; запрета нет |

**Проверка запрета по собственному тексту** (правило: сперва `grep` по модулю,
потом источник) делается сборщиком после сборки, по всем шести урокам правил.

---

## 4. Пункты English Grammar Profile

Ключ — поле `id`, а не заголовок: `Guideword` повторяется 129 раз. Файл читан
настоящим разбором CSV — 1222 записи, из них уровня A1 сто девять. **Искал по
всему файлу, а не по подкатегории `ADJECTIVES`**: правило приписано после модуля
29 Elementary, где вывод из одной подкатегории дал несуществующее расхождение.
Поэтому в списке ниже стоят и `ADVERBS`, и `DETERMINERS`, и `NOUNS`.

Свободу каждого пункта проверил прогоном `npm run programma -- english-beginner-2`
и сверкой с восемью написанными модулями: все десять числятся в списке
«нерозданные пункты ступени A1».

### 4.1. Вводит модуль 9 (`egp`) — десять пунктов

| `id` | Категория, заголовок | Can-do и пример источника | Урок |
|---|---|---|---|
| `1741163706530x753542801715210100` | `ADJECTIVES / position`, FORM: PREDICATIVE, WITH 'BE', LR 1 | «Can use a limited range of adjectives predicatively, after 'be'.» — «I like my home because **my bedroom is big**.» | 1 |
| `1741163706530x479020650602328450` | `ADJECTIVES / position`, FORM: ATTRIBUTIVE (WITH NOUNS), LR 1 | «Can use a limited range of adjectives attributively, before a noun.» — «We have **a big garden** and **a small swimming pool**.» | 4 |
| `1741163708789x344483096716751800` | `DETERMINERS / articles`, FORM: 'A' + ADJECTIVES, LR 1 | «Can use 'a' and 'an' before adjectives in a noun phrase.» — «It’s **an old house**, about 50 years old.» | 4 |
| `1741163711539x336320676887564860` | `NOUNS / noun phrases`, FORM: DETERMINER + ADJECTIVE + NOUN | «Can form simple noun phrases by pre-modifying singular and plural nouns with an adjective after a determiner.» — «**Her favourite colours are blue and black.**» | 3 и 4 |
| `1741163711539x318006443369482940` | `NOUNS / noun phrases`, FORM: ADJECTIVE + PLURAL NOUN | «Can form simple noun phrases by pre-modifying plural nouns with an adjective and no determiner.» — «I have **new neighbours**.», «They’re **nice people**.» | 5 |
| `1741163706326x912859360099482800` | `ADJECTIVES / modifying`, FORM: WITH 'VERY' | «Can use 'very' with a limited range of common gradable adjectives.» — «I like Croydon because it’s **very quiet** and **very nice**.» | 6 |
| `1741163706728x100067556201106640` | `ADVERBS / adverbs as modifiers`, FORM: 'VERY' + ADJECTIVES | «Can use 'very' to modify common gradable adjectives.» — «**My home is very nice.**» | 6 |
| `1741163706722x502415631169033540` | `ADVERBS / types and meanings`, USE: 'VERY' WITH ADJECTIVES | «Can use 'very' to make adjectives stronger.» — «**He’s a very good man.**» | 6 |
| `1741163706930x543760633232157630` | `ADVERBS / position`, FORM: DEGREE, BEFORE ADJECTIVES, LR 1 | «Can use a limited range of degree adverbs before an adjective.» — «**My home is very light** and very comfortable.» | 6 |
| `1741163708789x174288205596050180` | `DETERMINERS / articles`, FORM: 'A' + 'VERY' + ADJECTIVES | «Can use 'a' + 'very' + adjectives in basic noun phrases.» — «**It’s a very nice town.**» | 6 |

**Пять последних пунктов — об одном и том же `very`, и это устройство источника,
а не мой добор.** English Grammar Profile описывает `very` с четырёх сторон
(прилагательное, наречие как определитель, значение, место) плюс отдельной
графой про артикль. Замороженный модуль 11 записал то же самое словами «То же с
двух сторон». Урок 6 при этом один, и разбирает он одну вещь.

**Уроки 2 и 3 своего пункта НЕ вводят, и это сказано нарочно.** Урок 2 (цвет)
стоит на пункте урока 1 плюс девять карточек; урок 3 (вопрос о цвете) — на
расхождении источников из раздела 3.1 и на `DETERMINER + ADJECTIVE + NOUN`
(`my favourite colour`). Пункт A2 в `egp` не ставится: проверка называет чужую
ступень ошибкой, и правильно делает. Так же поступил модуль 7 с `What is this?`

### 4.2. Возвращается (`egpPovtor`) — восемь пунктов

Каждый проверен по коду: все восемь введены модулями, которые идут **раньше**
девятого.

| `id` | Что это | Ввёл | Где работает |
|---|---|---|---|
| `1741163715288x539616242661052000` | `VERBS / types`, LINKING VERB 'BE' | модуль 2 | `is` и `are` во всех шести уроках правил |
| `1741163713868x463659211645272000` | `PRONOUNS`, (SUBJECT) STATEMENTS | модуль 2, `it` добавил модуль 7 | урок 2: `It is red.` |
| `1741163708329x286701804737242940` | `CLAUSES / declarative`, AFFIRMATIVE DECLARATIVE | модуль 2 | все утвердительные строки |
| `1741163708789x105964971324936210` | `DETERMINERS / articles`, WITH NOUNS | модуль 3 | `the box`, `a car`, `an old car` |
| `1741163708329x778951051617750700` | `CLAUSES / declarative`, NEGATIVE DECLARATIVE WITH 'BE' | модуль 3 | урок 1: `The car is not new.` |
| `1741163708336x928067678664856700` | `CLAUSES / interrogatives`, AFFIRMATIVE INTERROGATIVE WITH 'BE' | модуль 3 | урок 1: `Is the box big?` |
| `1741163713867x150062658595893500` | `PRONOUNS`, (SUBJECT) QUESTIONS WITH 'BE' | модуль 3 | урок 3: `Is it red? — Yes, it is.` |
| `1741163711821x759916598484829700` | `NOUNS / plural`, PLURAL '-S' | модуль 4 | урок 5: `red apples`, `The apples are red.` |

### 4.3. Чего НЕ беру, хотя соблазн прямой

**1. `1741163706316x926291459998112000`, `ADJECTIVES / combining`, FORM:
COMBINING TWO ADJECTIVES WITH 'AND', A1 — отдаю модулю 10.**

Замысел модуля 6 роздал его «модулям 9 и 10» — то есть решения не принял, а
назвал двоих. Принимаю его модулю 10, и вот на чём:

- **пример графы о людях**: «The teachers are very nice and friendly»;
- **модуль 6 УЖЕ ввёл соединение прилагательных другой графой**:
  `1741163708775x971363187748411900`, `CONJUNCTIONS / coordinating`, FORM:
  WORD + WORD, A1 — «Can use single word conjunctions ('and, but, or') to connect
  single nouns **and adjectives**», пример «My street is quiet and friendly».
  То есть `and` между прилагательными ученику уже показан, и вторая графа —
  углубление, а не введение;
- **у модуля 9 шесть уроков правил и десять пунктов**; седьмой урок ради
  одиннадцатого пункта был бы добором.

**Строка, которую должен увидеть методист модуля 10** (находка о чужом модуле
сама не доезжает):

> Пункт `1741163706316x926291459998112000` (`ADJECTIVES / combining`, TWO
> ADJECTIVES WITH 'AND', A1) свободен и по смыслу ваш: пример графы —
> «The teachers are very nice and friendly», то есть люди и оценка. Модуль 6
> роздал его «модулям 9 и 10», модуль 9 отказался (`plan-modul-9.md`, раздел
> 4.3) и **ни одной строки с двумя прилагательными через `and` не содержит** —
> проверено запретом на весь модуль. Прилагательные, `very` и место
> прилагательного к вашему приходу разобраны целиком. Оттуда же ваши пять слов:
> `teenager`, `love`, `married`, `friendly`, `group` — модуль 6 роздал их
> «модулям 9 и 10», все пять о людях, и модуль 9 ни одного не берёт.

**2. `1741163716067x824562184770361200`, `NEGATION / negation`, FORM: MAIN VERB
'BE', A1 — не беру, хотя оба примера графы буквально о размере**
(«My college is not very big», «This place isn’t big»). Отрицание с `be` уже
введено модулем 3 другой графой (`CLAUSES / declarative`, NEGATIVE DECLARATIVE
WITH 'BE'), и модуль 7 разобрал сокращённую запись `isn’t`. Взять эту графу
значит ввести отрицание второй раз. Ставлю в `egpPovtor` ту, что модуль 3 и ввёл.
**Пункт остаётся свободным; называю его здесь, чтобы следующий не искал.**

**3. `PRONOUNS`, (OBJECT) 'IT' и соседние объектные пункты — не беру:** объектное
местоимение требует переходного глагола, а во всём модуле 9 глагол один, и он
`be`.

**4. `1741163708998x842376822392022500`, 'THIS' WITH SINGULAR NOUNS — не беру
в `egpPovtor`, хотя `this box` в модуле встречается.** Пункт ввёл модуль 7 и
повторил модуль 8; третий повтор ничего не добавляет, а `egpPovtor` — не список
всего употреблённого.

---

## 5. Состав модуля: десять уроков

### 5.1. Почему десять, а не восемь

Программа (раздел 3) даёт норму: «Восемь уроков, девять там, где есть письмо» —
пять уроков правил и умения. **Модуль 9 берёт шесть уроков правил и четыре урока
умений, и это надо объявить, а не провести молча.**

Довод — счёт, а не желание:

1. **шесть уроков правил в курсе уже норма**, а не исключение: столько же у
   модуля 3 и у модуля 6 (по девять уроков каждый, без письма);
2. **пунктов у модуля десять**, и делить их на пять уроков значит по два на урок
   при уроке в восемь случаев;
3. **урок письма обеспечен дескриптором** (раздел 2.2), а урок говорения
   обязателен по программе.

**Обратный путь, если сводящий или владелец решит держать девять:** склеить
уроки 2 и 3 (цвет и вопрос о цвете) в один. Цена — девять карточек цвета, вопрос,
короткий ответ и `favourite` на одном уроке; это выше того, что несут уроки
соседей. Решение назвал, выбор оставил (раздел 10, строка 3).

### 5.2. Порядок и итоги

| № | Урок | Вид | Итог |
|---|---|---|---|
| 1 | Большой и маленький: The box is big | правила | говорить, какая вещь: `The box is big` |
| 2 | Какого цвета: The car is red | правила | называть цвет вещи и говорить о ней словом `it` |
| 3 | Какого цвета? — What colour is it? | правила | спрашивать о цвете и отвечать одним словом или строкой |
| 4 | Красная машина: a red car | правила | ставить слово о признаке между артиклем и названием вещи |
| 5 | Красные яблоки: red apples | правила | говорить о признаке нескольких вещей сразу |
| 6 | Очень: very big | правила | усиливать признак словом `very` |
| 7 | Читаем две записки: чья коробка | чтение | находить в записке, какая вещь и чья |
| 8 | Слушаем: какая из трёх | слушание | узнавать на слух, о какой из вещей идёт речь |
| 9 | Говорим: какого цвета твоя вещь | говорение | выбирать верный ответ на вопрос о цвете |
| 10 | Пишем записку: какая твоя вещь | письмо | проверять записку о вещи: стоит ли форма `be` при слове о признаке |

**Итоги 9 и 10 названы по проверяемой половине** — правило проекта: устную речь
и письмо машина не оценивает, и обещать «говорить» и «писать» нельзя.

**Все десять имён уникальны во всём курсе** — проверено `grep` по трём курсам.
Ближайшие соседи: модуль 7 «Слушаем, что это за вещь» и «Говорим: спроси о вещи
и назови её», модуль 8 «Слушаем, чего сколько». Совпадений нет, но сборщику
прогнать `npm run bliznetsy` по паре 7–9 обязательно.

**Строка, которую надо держать в уме всем шести урокам правил** (правило
«правило урока сверяется с перечнем уроков модуля»): урок 1 учит строю
`The box is big`, а урок 4 — строю `a big box`. Значит **правило урока 1 не
вправе сказать «слово о признаке стоит после `is`»** как утверждение о языке:
через три урока сам модуль покажет обратное. Правило урока 1 говорит о СВОЁМ
случае, а второй строй придёт с адресом.

### 5.3. Уроки 2–6 одной строкой

Подробно расписан только урок 1 (отдельный файл). Ниже — состав остальных, чтобы
сводящий видел границы и не собирал двойников.

**Урок 2 «Какого цвета: The car is red».** Девять карточек цвета. Восемь случаев:
цвет после `is`; `it` вместо названия вещи (**закрывает долг модуля 7**, строка
`It is black.` стоит здесь дословно); по-русски на месте `it` три слова — «он»,
«она», «оно», и выбирают их по русскому слову, а не по английскому; цвет у еды
(`The apple is green.`); `orange` — плод и цвет одним словом (раздел 6.3);
`The car is not red.`; `black` и `white` — слова, которые ученик уже знает
(возврат к модулю 1); цвет вещи, которую только что назвали
(`This is a box. It is brown.`).
**Начало:** русская фраза — «какого цвета» по-русски два слова, по-английски
одно `colour`.

**Урок 3 «Какого цвета? — What colour is it?».** Карточки: `colour`, `favourite`.
Восемь случаев: сам вопрос; ответ одним словом (`Red.`) — дословный образец
Cambridge, с. 28; ответ строкой (`It is red.`); вопрос о названной вещи
(`What colour is the car?`); короткий ответ `Yes, it is` и `No, it is not` на
`Is it red?` (возврат к модулю 3); `What’s your favourite colour?` — вторая
строка Cambridge, с. 28, Speaking Part 4; ответ `My favourite colour is blue.`;
почему в вопросе `is it`, а в ответе `it is` — порядок обратный, и это
единственное трудное место урока.
**Начало:** сцена — Ким спрашивает по телефону, какую коробку взять.

**Урок 4 «Красная машина: a red car».** Карточек нет ни одной: урок стоит на уже
данных словах. Восемь случаев: слово о признаке перед названием вещи; артикль
остаётся впереди (`a red car`); `an` перед гласным звуком **самого
прилагательного** — `an old car`, и это расхождение, которого модуль 3 показать
не мог (там гласный был у существительного); `the` тоже остаётся (`the red
car`); притяжательное вместо артикля (`my new phone`); по-русски порядок тот же,
и этим урок 4 легче урока 1; два строя об одной вещи (`The car is red.` и
`a red car`) — когда какой; вещь без артикля не называют (возврат к модулю 3).
**Начало:** сцена — на стоянке две машины, и Дана объясняет, какая её.

**Урок 5 «Красные яблоки: red apples».** Карточек нет. Восемь случаев:
`red apples` — прилагательное перед несколькими вещами и без артикля (пункт,
отданный модулем 8); прилагательное окончания `-s` не получает — **расхождение
с русским, главное в уроке** («красные яблоки» — окончание есть у обоих слов);
`The apples are red.` — прилагательное после `are`; `They are red.`; число
впереди (`three green apples`); вещи, а не только еда (`The boxes are big.`);
строй `Lemons are yellow.`, напечатанный у Cambridge, с. 29; вопрос о нескольких
(`Are the apples red?`).
**Начало:** русская фраза — «красные яблоки», где по-русски меняются оба слова.
**Осторожно:** слова `lemon` в курсе нет, строку Cambridge берём устройством, а
не дословно.

**Урок 6 «Очень: very big».** Карточка одна — `very`. Восемь случаев: `very`
перед прилагательным; `very` в строе с `is` (`The box is very big.`); `very` в
строе с артиклем (`a very big box`) — порядок «артикль, `very`, признак,
название»; `very` перед словом о цвете (`a very red car`); `very` с отрицанием
(`It is not very big.`); что `very` меняет в смысле; `very` при слове о размере
против `very` при слове о цвете — чем разница; сводный случай, где стоит вся
строка модуля целиком.
**Начало:** русская фраза — «очень большая» ложится на `very big` слово в слово.

### 5.4. Уроки умений

**Урок 7 «Читаем две записки: чья коробка», genre `message` дважды.** Две
короткие записки: Ким пишет, какая коробка его, Дана — какая её. Ученик по
описанию понимает, чья какая. **Чем это не двойник модуля 7:** там ученик искал
в списке НАЗВАНИЕ вещи, здесь — сличает ОПИСАНИЕ с вещью, и без цвета и размера
задание не решается. Порог `message` — пятнадцать слов, в каждой записке их
больше двадцати.

**Урок 8 «Слушаем: какая из трёх».** Разговор на два голоса: Ким описывает вещь,
Дана переспрашивает о цвете. Ответы — **одно слово**: `red`, `big`, `new`.
Формат из A2 Key, с. 29. **Русская подпись записи и разборы вокруг неё не
называют ни цвета, ни размера** — правило от 6 сентября: у задания на слух
русские подписи читаются как часть условия, и «Ким описывает КРАСНУЮ коробку»
отменило бы задание. И **подпись следующей записи не называет ответа
предыдущей** — та же находка редактора модуля 5.

**Урок 9 «Говорим: какого цвета твоя вещь».** Образец — Cambridge, с. 28:
`What’s this? — A box. What colour is it? — Red.` Два коротких ответа и один
длинный, с записью-образцом. Итог проверяется тем, что машина проверить может:
выбором верного ответа на вопрос о цвете.

**Урок 10 «Пишем записку: какая твоя вещь», задание вида `essay`.** Жанр взят из
урока чтения того же модуля — правило проекта. Образец:

    Hello, Kim!
    This is my box. It is not big.
    It is green. My favourite colour is green.
    Dana

**Список самопроверки (пишет редактор, состав мой) — четыре пункта:** цвет
назван; сказано, большая вещь или маленькая; там, где ты говоришь, какая вещь,
стоит `is`; вещь названа с артиклем или со словом `my`.

**Образец проверен по собственному списку построчно:** цвет — `green`; размер —
`not big`; `is` стоит в трёх строках из трёх, где есть признак; `my box` —
притяжательное вместо артикля. Строки `Hello, Kim!` и `Dana` признака не
содержат и под третий пункт не подпадают — **и редактор обязан написать пункт
так, чтобы это было видно** («в каждой строке, где ты говоришь, какая вещь»).
Пункт вида «в каждом предложении есть…» здесь запрещён: ровно на нём курс
обжёгся в уроках письма прошлой ступени.

**`minWords` — семь.** Считано по самому короткому ВЕРНОМУ ответу, а не по
образцу. Самый короткий ответ, отвечающий условию «назови вещь и её цвет»:
`This is my box. It is green.` — семь слов. Если условие потребует и размера,
самый короткий станет `This is my box. It is green. It is small.` — десять слов,
и **порог всё равно остаётся семь**: правило проекта требует брать по самому
короткому верному. Порог в двенадцать, как на прошлой ступени, отсекал бы
правоту.

**Чего в уроке письма быть не должно:** строки `It is green and small.` — `and`
между двумя прилагательными отдан модулю 10 (раздел 4.3), и образец, который
печатает то, чему модуль не учит, наказал бы ученика за подражание.

### 5.5. Виды начала уроков — против однообразия

**Модуль 9 — модуль-текст.** Программа (раздел 3) требует не меньше трёх видов
начала в каждой части; модуль 7 объявил себя модулем-вещами, модуль 8 —
модулем-правилом со сценой, и его замысел прямо пишет: «Третий вид
(модуль-разговор или модуль-текст) остаётся модулям 9–12». Беру текст.

**Как это выглядит и чем отличается от соседей.** Первый экран модуля — не
разговор и не показ вещей, а **написанная записка**, которую ученик читает
глазами: Ким уехал и оставил Дане записку о двух одинаковых на вид телефонах.
Дальше урок разбирает её строки.

| Урок | Вид начала |
|---|---|
| 1 | текст: записка на столе о двух телефонах |
| 2 | русская фраза: «какого цвета» — по-русски два слова, по-английски одно |
| 3 | сцена: Ким спрашивает по телефону, какую коробку взять |
| 4 | сцена: на стоянке две машины, Дана объясняет, какая её |
| 5 | русская фраза: «красные яблоки», где меняются оба слова |
| 6 | русская фраза: «очень большая» ложится слово в слово |
| 7 | текст: две записки |
| 8 | запись: разговор двух голосов |
| 9 | сцена: собеседник показывает вещь и спрашивает о цвете |
| 10 | сцена: Дане надо оставить записку о своей коробке |

**Ни один урок не начинается обобщением** вида «Сказать, какая вещь, можно
двумя способами». Правило владельца от 3 сентября.

**ЛОВУШКА КОДА, КОТОРУЮ ОБЯЗАН ЗНАТЬ СБОРЩИК.** `vidPoSostavu()`
(`lib/content/vid-uroka.ts`) возвращает `chtenie` для ЛЮБОГО урока, где есть
блок `kind: "text"`. Значит записка урока 1 **не должна быть блоком `text`** —
её место в блоке `example` с подписью, и тогда вид урока выведется верно. Если
сборщик всё-таки захочет `text`, уроку 1 нужно поставить `vid: "pravila"` явно,
иначе `check:content` спросит о расхождении вывода с заголовком. **Утверждение
это сделано ЧТЕНИЕМ кода, а не прогоном, — сборщику проверить одной строкой.**

---

## 6. Словарь модуля

### 6.1. Тема `Colours` — счёт

Тема объявляется одна: `Colours`, дословное имя из Приложения 2 словника A2 Key,
с. 25–32. `npm run programma` считает в ней **13 слов ступени A1** из
шестнадцати записей.

Все шестнадцать: `black`, `blue`, `brown`, `dark`, `golden`, `green`,
`grey / gray`, `light`, `orange`, `pale`, `pink`, `purple`, `red`, `silver`,
`white`, `yellow`. Не A1 у Oxford: `golden`, `pale`, `silver` — их проверка и не
считает.

| Слово A1 темы | Карточка | Где |
|---|---|---|
| `black` | **уже есть** | модуль 1, `alfavit.ts` |
| `light` | **уже есть**, в значении «свет» | модуль 1 |
| `orange` | **уже есть**, в значении «апельсин» | модуль 8 |
| `blue`, `brown`, `green`, `grey`, `pink`, `purple`, `red`, `white`, `yellow` | модуль 9, урок 2 | девять новых |
| `dark` | **не берётся** | разбор ниже |

**После модуля 9 тема закрыта на 12 из 13.** Единственное незакрытое слово —
`dark`, и я его не беру нарочно:

1. в теме `Colours` оно работает только в паре — `dark blue`, `dark green`, — а
   два прилагательных подряд стоят на A2 (раздел 3.2);
2. отдельно `It is dark` — это пустое `it` о времени суток, а пустое `it` снято
   со ступени решением методиста программы (раздел 0.1 программы, English Grammar
   Profile, FORM: (SUBJECT) EMPTY IT, A2);
3. **в тематическом списке Cambridge его нет вовсе:** Young Learners, с. 79,
   строка `Colours`, столбец Pre A1 Starters — `black, blue, brown, colour,
   gray (UK grey), green, grey, orange, pink, purple, red, white, yellow`;
   столбец A2 Flyers — `gold, silver, spot, spotted, stripe, striped`. Ни `dark`,
   ни `light`, ни `pale` там не стоят.

**Сводящему: это решение надо положить в `courses/resheno.ts`**, иначе
`npm run programma` будет спрашивать о `dark` на каждом прогоне. Записать я его
туда не вправе — методист в `courses/` не пишет.

### 6.2. Ступень каждого слова — по двум источникам, а не по одному

**Все тринадцать цветов стоят у Cambridge на самой младшей ступени.** Young
Learners, **с. 79**, тематические списки, строка `Colours`, столбец
**PRE A1 STARTERS**. Читал двумя разборами: первый (`materials/.text/`)
рассыпает три столбца в один поток, второй (`materials/.md/`, строки 3501–3508)
держит их таблицей, и в столбце A2 Flyers стоят только `gold`, `silver`, `spot`,
`spotted`, `stripe`, `striped`. **Границы столбцов в этом файле разборщик местами
рвёт**, поэтому вывод я делаю по обоим разборам сразу, а не по одному.

**Второй источник — Cambridge называет цвета перечнем, и перечень тот же.**
Young Learners, **с. 13**, Pre A1 Starters Listening, задание 4:

> Colouring. Carry out instructions, locate objects, and colour correctly.
> **(Range of colours is: black, blue, brown, green, grey, orange, pink, purple,
> red, yellow)**

Десять цветов, названных источником поимённо для самой младшей ступени.

**`white` в этот перечень не входит, и я его всё равно беру.** Довод: перечень
описывает задание, где надо ЗАКРАСИТЬ предмет на белой бумаге, — белый там
бесполезен по устройству задания, а не по ступени. Слово при этом стоит и в
тематическом списке той же ступени (с. 79), и в словнике A2 Key, и у Oxford 3000
на A1. **Графа — это разрешение, а не запрет**, и вычитать из перечня задания
запрет на слово нельзя.

### 6.3. `orange`: слово занято, и модуль 9 его НЕ перезанимает

**Что стоит в коде.** `odin-i-mnogo.ts`, урок 1: карточка `orange`, перевод
«апельсин», пример `an orange and a banana`, чтение `/ˈɒrɪndʒ/`, значок `1f34a`.
Замысел модуля 8 (`plan-modul-8-urok-1.md`, строка 533) пишет прямо: «значения
„оранжевый" в переводе быть не должно ни словом: иначе модуль 9 придёт к
занятому имени, а ученик получит два перевода одной строки».

**Решение: второй карточки `orange` в курсе не будет.**

1. **карточка остаётся одна и остаётся у модуля 8**, с переводом «апельсин».
   Правило проекта: расширение перевода законно, сужение требует опоры, — но
   правит перевод тот, чей модуль, а не сосед;
2. **модуль 9 называет второе значение СЛУЧАЕМ** — урок 2. Ровно как модуль 7
   поступил с `desk` и `thing`: «граница называется не в карточке, а там, где
   ученик на неё наткнётся»;
3. **строки случая:** `The box is orange. — Коробка оранжевая.` и
   `This is an orange. — Это апельсин.` Одно слово, два дела, и оба ученику уже
   знакомы;
4. **`kontrol` промолчит** — слово введено карточкой раньше по курсу, и проверка
   долга словаря удовлетворена.

**Строка, которую должен увидеть сборщик модуля 8** (находка о чужом модуле сама
не доезжает):

> Ваша оговорка исполнена: модуль 9 второй карточки `orange` не заводит и
> перевода «апельсин» не трогает. Он вводит второе значение случаем урока 2.
> Ничего править в модуле 8 не надо — запись оставлена как есть нарочно.

**Осторожно с картинками:** в задании модуля 9, где `orange` — цвет, значок
`1f34a` (апельсин) ставить НЕЛЬЗЯ. Цвет показывается квадратом `1f7e7`.

### 6.4. Слова `size` в модуле нет, и это решение

`npm run slovo -- size` даёт **Oxford 3000: A2**. Слово в словнике A2 Key стоит,
но ступень у него чужая. Значит:

- карточки `size` нет;
- ни одна английская строка модуля слова `size` не содержит;
- по-русски о размере говорят «большая» и «маленькая», а слово «размер» стоит
  только в названии модуля.

**Это же снимает вопрос о вопросе `What size is it?`** — его в модуле нет вовсе.

**И оговорка о строке Cambridge `Is your house big or small?`** (с. 28,
Speaking Part 4). Соблазн взять её прямой: источник называет её на Pre A1
Starters, и она о размере. **Не берём.** Она соединяет два прилагательных через
`or`, а пункт `CONJUNCTIONS / coordinating`, FORM: WORD + WORD принадлежит
модулю 6 и введён там на существительных и на `and`; вдобавок пункт
`ADJECTIVES / combining` я отдал модулю 10 (раздел 4.3). Размер спрашивается тем
же вопросом с `be`, что и всё остальное: `Is the box big? — Yes, it is.`

Прежняя редакция этого раздела предлагала взять строку «готовой формулой и
объявить». Правлю это здесь, пока замысел не ушёл редактору: **помета
«редактору решить» починкой не является**, редактор берёт то, что напечатано.

### 6.5. Карточки модуля — восемнадцать

| Слово | Перевод | Ступень | Тема | Урок |
|---|---|---|---|---|
| `big` | большой | Oxford **A1**, словник — да | Personal Feelings (adjectives) | 1 |
| `small` | маленький | Oxford **A1**, словник — да | Personal Feelings (adjectives) | 1 |
| `long` | длинный | Oxford **A1**, словник — да | вне тем | 1 |
| `short` | короткий | Oxford **A1**, словник — да | вне тем | 1 |
| `new` | новый | Oxford **A1**, словник — да | вне тем | 1 |
| `car` | машина | Oxford **A1**, словник — да | Travel and Transport | 1 |
| `red` | красный | Oxford **A1**, словник — да, YL с. 79 Pre A1 Starters | **Colours** | 2 |
| `blue` | синий | то же | **Colours** | 2 |
| `green` | зелёный | то же | **Colours** | 2 |
| `yellow` | жёлтый | то же | **Colours** | 2 |
| `white` | белый | то же | **Colours** | 2 |
| `brown` | коричневый | то же | **Colours** | 2 |
| `pink` | розовый | то же | **Colours** | 2 |
| `grey` | серый | то же | **Colours** | 2 |
| `purple` | фиолетовый | то же | **Colours** | 2 |
| `colour` | цвет | Oxford **A1**, словник — да, YL с. 79 Pre A1 Starters | **Colours** | 3 |
| `favourite` | любимый | Oxford **A1**, словник — да | Personal Feelings (adjectives) | 3 |
| `very` | очень | Oxford **A1**, словник — да | вне тем | 6 |

**Трёх слов в этой таблице НЕТ нарочно, чтобы их не завели второй раз:** `black`
(модуль 1), `old` (модуль 4), `thing` (модуль 7).

**Норма ступени.** Норма в тридцать карточек с 29 августа считается **по теме**,
а не по числу. Тема `Colours` закрывается на 12 из 13 — это лучшая доля среди
всех восьми написанных модулей. Добирать модуль словами о еде или о городе ради
тридцати было бы ровно тем добором, который запрещает уточнение владельца от
21 августа.

**Ни одно слово ступень не превышает.** Восемнадцать из восемнадцати стоят у
Oxford 3000 на A1 и в словнике A2 Key — проверено `npm run slovo` по каждому
отдельно.

**И одна строка отчёта, которая пугает зря.** По всем восемнадцати словам
`slovo` пишет «уже дано: english-starter → kakoy-on» (у `car` — `chto-eto-takoe`).
Это ЗАМОРОЖЕННЫЙ курс, а не наш: в `english-beginner-2` ни одного из восемнадцати
слов нет. Проверено `grep` по `term:` во всех девяти файлах курса. Пометы «уже
дано: english-beginner-2» нет ни у одного. Объявлять в шапке нечего, кроме расхождения по `What colour is it?`
(раздел 3.1).

**Чтение** берётся со склада замороженного курса, где условности уже применены
(раздел 1.3). Своей рукой считаю только `purple`: `ipa-en-uk` даёт `/pˈɜːpəl/`,
условность курса про слоговой согласный (`/ˈæpl/`, `/ˈkæpɪtl/`) даёт
**`/ˈpɜːpl/`**. **Сборщику сверить.**

**Чего модуль употребляет, но карточкой не даёт:** `box`, `pen`, `book`, `cat`,
`ticket`, `paper` (модуль 1), `it`, `this`, `that`, `table`, `chair`, `door`,
`window`, `clock`, `thing` (модуль 7), `apple`, `banana`, `cup`, `bottle`,
`tomato`, `carrot` (модуль 8), `phone`, `number`, `old` (модуль 4), `my`, `your`
(модули 1 и 5), `his`, `her` (модуль 5), `the`, `a`, `an`, `in`, `from`
(модуль 3), `black` (модуль 1). Все проверены по коду и все стоят РАНЬШЕ
девятого модуля — правило «у слова спрашивают не „есть ли карточка", а „есть ли
карточка раньше этого модуля"».

### 6.6. Значки

Цвет показывается **цветным квадратом**, а не предметом этого цвета: предмет
увёл бы ученика к названию предмета.

| Слово | Значок | Занят? |
|---|---|---|
| `red` | `1f7e5` | свободен |
| `blue` | `1f7e6` | свободен |
| `orange` (цвет, в случае урока 2) | `1f7e7` | свободен |
| `yellow` | `1f7e8` | свободен |
| `green` | `1f7e9` | свободен |
| `purple` | `1f7ea` | свободен |
| `brown` | `1f7eb` | свободен |
| `white` | `2b1c` | свободен |
| `black` (карточка модуля 1) | `2b1b` | **занят ею же, и это верно** |
| `grey` | **нет** | серого квадрата в Twemoji нет |
| `pink` | **нет** | розового квадрата в Twemoji нет |
| `car` | `1f697` | свободен (стоял в выведенном из курса `kto-eto.ts`) |
| модуль | `1f308` (радуга) | свободен во всём курсе |

**У `grey` и `pink` значка не будет, и это решение, а не пропуск.** Правило взято
у методиста модуля 8 дословно: «неверная картинка хуже её отсутствия».
Подставлять слону серый цвет, а поросёнку розовый — значит дать ученику предмет
вместо цвета.

**Сборщику проверить прогоном `npm run znachki`**, что все восемь квадратов и
`1f697` в Twemoji есть: чтением этого не проверить, а ключ без файла — это
кнопка, которой не будет.

**Опасная пара на маленьком размере:** `1f7e6` (синий) и `1f7ea` (фиолетовый). В
одном задании с картинками их вместе не ставить — то же правило, что у пары
`1f305` и `1f307` в модуле 2.

### 6.7. Сетка букв: два слова прячут в себе слова курса

Владелец просил три новых вида упражнений 5 сентября, и счёт по курсу через
сутки дал: **сетка со словами в поле букв — ноль заданий во всём курсе**.
Модуль 9 — самое подходящее место: девять коротких слов одной темы.

**Но два из них прячут в себе карточки курса, и ловится это только прогоном:**

- **`WHITE` содержит `IT`** — карточка модуля 7;
- **`PINK` содержит `IN`** — карточка модуля 3.

Порода известна: методист модуля 5 нашёл, что `NAME` содержит `AM`, и
перестановкой букв это не лечится — лишнее слово лежит ВНУТРИ искомого.

**Отсюда состав сетки, который предлагаю:** `RED`, `BLUE`, `GREEN`, `BROWN`,
`YELLOW`, `PURPLE`, `GREY`. Слова `WHITE` и `PINK` в сетку не ставим.
**Поле собирает сборщик прогоном** `bedySetki` и `lishnieSlova` из
`lib/content/setka.ts`: у методиста прогона нет, и поле, собранное глазами,
проверкой не является.

---

## 7. Термины и имена

### 7.1. «Прилагательное» — где и как называется

Урок 1, случай 1, **строкой внутри случая**, до первого задания. Отдельного
экрана термин не получает: экран без новой английской строки ученик проходит как
пустой.

**Порядок слов в объяснении задаёт правило владельца от 4 сентября:** есть
русское соответствие — оно называется ПЕРВЫМ. Значит сперва «большая», «новая»,
«красная», и только потом имя класса.

**Ссылок на модуль 10 в тексте нет.** Правило от 6 сентября: о ненаписанном
модуле не утверждаем ничего, даже мимоходом.

### 7.2. `very`: класс НЕ называется, и вот почему

**Прошлая ступень звала `very`, `too`, `quite` наречиями меры** — укороченным
термином, потому что слово «степень» было занято сравнением (модули 14 и 15
Elementary). **В новом курсе сравнения нет ни в одном из тридцати модулей**, и
слово «степень» свободно: `grep` по видимому тексту даёт ноль.

Казалось бы, значит термин можно дать полностью — «наречие степени». **Не даю, и
довод не в занятости, а в цене.**

1. **«Наречие степени» требует сперва объяснить «наречие»**, а наречий в модуле
   ровно одно. Термин, введённый ради одного слова, — это экран без работы.
2. **Правило требует настоящего термина там, где мы ПОДМЕНЯЕМ его своим
   ярлыком.** Ярлыка здесь нет: урок говорит «слово `very`», а не «усилитель»,
   не «слово меры», не «степень». Не назвать класс — не то же самое, что назвать
   его выдуманно. Правило записано в памяти проекта отдельно: «правило о
   настоящем термине ловит подмену, а не требует имени там, где его нет».
3. **Класс придёт тогда, когда в нём будет больше одного слова** — модуль 16
   «Как часто» (`always`, `often`, `sometimes`) и модуль 24 (наречия места). Там
   наречие и надо называть.

**Это решение, а не умолчание, и оно спорное.** Ставлю его в раздел 10 строкой
для сводящего: если он или второй методист решит, что термин обязателен, цена —
одна строка в уроке 6.

### 7.3. Три имени, которых в модуле не будет

`признак`, `описание`, `качество`. Первое снято владельцем на прошлой ступени;
второе занято целым текстом («описание вещи» — это записка урока 10); третье
ничего не объясняет и класса в русской грамматике не называет.

---

## 8. Работа модуля — двенадцать вопросов

**Работа пишется ДО заданий уроков** и с другого угла. Правило нажито дорого:
пятьдесят восемь мест в десяти модулях прошлой ступени, где вопрос работы был
близнецом задания урока.

**И осторожно с утверждением «ни одного такого вида в уроках нет».** Замысел
модуля 6 написал это правдой, и оно стало ложным без единой правки, когда уроки
дописали. Поэтому пишу с датой: **на 6 сентября 2026 заданий уроков модуля 9 ещё
не существует, кроме урока 1**; перед сборкой пары «вопрос — задание»
проверяются заново прогоном `npm run bliznetsy`.

**Углы, отданные работе и запрещённые урокам** (запрет записывается и в замыслы
уроков 2–10): исправить чужую запись целиком; назвать ПРИЧИНУ ошибки; собрать
вопрос из рассыпанных кусков; усилить готовую строку.

| № | Вид | Что делает ученик | Итог урока | Ответ |
|---|---|---|---|---|
| 1 | `choice` | по картинке (маленькая зелёная машина) выбирает верную строку из трёх | 1 | `The car is small.` |
| 2 | `short` | «Дана написала: `The box big.` Запиши строку без ошибки» | 1 | `The box is big.` |
| 3 | `choice` | называет ПРИЧИНУ: чем плоха строка `The box big.` | 1 | «пропущено `is`» |
| 4 | `gap` | `The apple is ___ .` при картинке зелёного яблока | 2 | `green` |
| 5 | `match` | соединяет три цвета с тремя русскими словами | 2 | — |
| 6 | `short` | отвечает на `What colour is it?` при картинке красной коробки | 3 | `Red.` |
| 7 | `order` | собирает `What colour is the car?` из четырёх кусков | 3 | — |
| 8 | `choice` | выбирает верный порядок: `a red car` · `a car red` · `red a car` | 4 | `a red car` |
| 9 | `gap` | `It is ___ old car.` — артикль перед гласным звуком прилагательного | 4 | `an` |
| 10 | `short` | «Ким написал: `two reds apples`. Запиши без ошибки» | 5 | `two red apples` |
| 11 | `choice` | по картинке очень большой коробки выбирает строку из трёх | 6 | `It is very big.` |
| 12 | `short` | «Допиши строку так, чтобы она стала сильнее: `The car is big.`» | 6 | `The car is very big.` |

**Требующих произвести ответ — восемь из двенадцати** (`short` четыре, `gap`
два, `order`, и `match` как сборка пар). Доля выше половины, как требует
проверка.

**Все шесть итогов уроков правил спрошены**, и счёт по таблице такой: итог 1 —
вопросы 1, 2, 3; итог 2 — 4 и 5; итог 3 — 6 и 7; итог 4 — 8 и 9; **итог 5 —
только вопрос 10**; итог 6 — 11 и 12. Одиночный вопрос у пятого итога — след
того, что урок 5 не вводит нового строя, а переносит уже разобранное на
несколько вещей. **Если сводящий сочтёт одного вопроса мало, тринадцатым идёт
`short`: «Дана написала: `The box are big.` Запиши без ошибки» — предмет тот же,
а ошибка другая (форма `be` при одной вещи).** Итоги уроков 7–10 работой не
спрашиваются, и это устройство ступени: у умений своя проверка внутри урока. **Сборщику сверить:** если `check:content` требует покрытия ВСЕХ
итогов модуля, вопросов надо не двенадцать, а шестнадцать, — и тогда решает
сводящий.

**Верные ответы вопросов с выбором развести по местам** — сборщику: их четыре
(вопросы 1, 3, 8, 11), и они должны встать на разные позиции.

### 8.1. Отвлекающие: какие настоящие, какие выдуманные

Каждый прочитан как утверждение и поискан **в собственном модуле**, а потом
проверен вторым вопросом — «а говорят ли так вообще?». Две проверки, не одна: у
модуля 8 два отвлекающих из четырёх оказались правильным английским.

| Вопрос | Отвлекающий | Настоящая ли ошибка |
|---|---|---|
| 1 | `The car small.` | **да**: пропуск `is` — главная ошибка русскоязычного, и урок 1 о ней |
| 1 | `The car is big.` | **да, но по картинке, а не по языку** — законный вид отвлекающего |
| 3 | «слово `big` стоит не на месте» | **да**: настоящая догадка ученика, знающего урок 4 |
| 3 | «нужен артикль» | **да**: то же |
| 8 | `a car red` | **да**: порядок как в русском «машина красная» |
| 8 | `red a car` | **да**: ученик знает, что прилагательное впереди, и ставит его впереди ВСЕГО |
| 11 | `It is big very.` | **да**: `very` после слова, как русское «большая очень» |
| 11 | `It is a very big.` | **да**: артикль без названия вещи — ошибка, которую делают, выучив `a very big box` |

**Отвлекающий, который снят по второй проверке.** В первой редакции вопроса 1
стояло `The car is green and small.` — строка **правильная**, и объявить её
ошибкой значило бы наказать за верный ответ. Заменена на `The car is big.`

**Чего среди отвлекающих нет и быть не должно:** `It is very red` — правильный
английский, редкий, но верный; `The apples are reds` в вопросе 10 — там ошибка
уже одна, и двух в строке не бывает.

**Вопрос 10 проверен собственным текстом модуля:** `two red apples` — это пункт
`ADJECTIVE + PLURAL NOUN`, урок 5, и модуль нигде не пишет `reds`.

**Вопрос 6 проверен на второй верный ответ.** `What colour is it?` при картинке
красной коробки допускает и `Red.`, и `It is red.`, и сокращённую запись — все
верны, и все обязаны стоять в `accept`. Иначе ученик, ответивший полной строкой,
получит «пока не так». Это порода 14, и на прошлой ступени она стоила владельцу
трёх находок за один урок.

---

## 9. Звук

**Что звучит:** все примеры уроков (кнопка стоит у строки, а не отдельным
блоком); девять слов цвета по одному; шесть слов урока 1 по одному; образец
задания `speak` урока 9; запись урока 8.

**Что не звучит:**

- **строки в столбце «так сказать нельзя»** — `The car small.`, `a car red`,
  `It is big very.` Услышанная неверная фраза запоминается наравне с верной;
- **русские подписи и транскрипция**;
- **одиночный артикль** — `a` синтез прочтёт названием буквы. Звучит
  `a red car`, а не `a`;
- **цветной квадрат** — это картинка, у неё звука нет.

**Разговоры звучат на два голоса.** Их в модуле четыре: сцена урока 3, сцена
урока 4, запись урока 8, образец урока 9. **У блока `audio` урока 8 обязательно
поле `voice`, а у примеров-разговоров — `razgovor: true` и реплика на строку.**
Курс на этом обжигался трижды, и проверка «файл есть» такого не видит: имя файла
считается от текста и от числа голосов не зависит.

**Голос человека один:** Ким мужской, Дана женский. Порядок реплик подгоняется
полем `pervyyGolos`. В сцене урока 3 первым говорит Ким — значит
`pervyyGolos: "muzhskoy"`.

**Имя рассказчика не стоит ни в одной строке его же сцены** — проверено по всем
четырём: Ким нигде не говорит `Kim`, Дана нигде не говорит `Dana`. В записке
урока 1 имя `Kim` стоит подписью, а читает записку Дана — там это не реплика, и
беды нет.

---

## 10. Спорные строки: их сводит автор, не я

**Строка 1. `What colour is it?` объявлен двум модулям — 9 и 11.** Программа
даёт модулю 11 «Одежда» итог «сказать, что надето, и спросить о цвете». Разбор
и предложенная граница — раздел 1.4. **Решение обязано попасть в оба замысла.**

**Строка 2. Слова цвета понадобятся ещё двоим** — модулю 11 (одежда) и модулю 12
(комната). Это законно: полнота темы спрашивается у курса. Записываю сейчас,
пока те не написаны: **все карточки цвета вводит модуль 9, и повторно их не
заводит никто.**

**Строка 3. Десять уроков против нормы программы в восемь-девять.** Довод и
обратный путь — раздел 5.1. Решает сводящий или владелец.

**Строка 4. Класс слова `very` не называется.** Разбор — раздел 7.2. Если второй
методист решит иначе, цена — одна строка в уроке 6.

**Строка 5. `dark` остаётся без карточки, и это надо положить в `resheno.ts`.**
Причина — раздел 6.1. Методист в `courses/` не пишет.

**Строка 6. `car` берётся из темы `Travel and Transport`, объявленной модулю
25.** Долю темы это не ломает — она считается по курсу, — но модуль 25 должен
знать, что одно его слово уже дано.

**Строка 7. «Вещь» против «предмета».** Модуль 7 зовёт это вещью и объясняет
почему, модуль 8 взял «предмет». Я держу «вещь». Свести надо на всю часть 2, а
не помодульно.

**Строка 8. Покрытие итогов вопросами работы.** Если проверка требует вопроса на
каждый из десяти итогов, работа вырастает с двенадцати вопросов до шестнадцати.

---

## 11. Находки о ЧУЖИХ модулях — передаю сводящему, править не могу

Правило проекта: находка о чужом модуле не доезжает до него сама.

**11.1. Модулю 10 — пункт `ADJECTIVES / combining` и пять слов о людях.** Полный
текст строки — раздел 4.3.

**11.2. Модулю 11 — граница по вопросу о цвете.** Полный текст — раздел 1.4.

**11.3. Модулю 8 — оговорка про `orange` исполнена, править ничего не надо.**
Текст — раздел 6.3.

**11.4. Модулю 25 — слово `car` уже дано.** Тема `Travel and Transport` держит
38 слов A1; одно из них ученик получит в модуле 9.

**11.5. Модулям 7 и 8 — расхождение «вещь / предмет».** Раздел 0.3. Это не
находка об ошибке, а несведённое решение: два модуля одной части зовут одно и то
же разными словами, и ученик читает два слова об одних и тех же коробках.

**11.6. Модулю 1 — карточки `light` и `black` закрывают тему `Colours` только
на бумаге.** `light` переведено «свет», значения «светлый» курс не даёт нигде;
`black` взято ради сочетания `ck`, и цветом оно начнёт работать только в
модуле 9. Ошибки тут нет — записываю, чтобы следующий не решил, что тема
закрыта делом, а не счётом.

---

## 12. Что проверено и чего проверить не удалось

**Проверено, с прогонами:**

- обе опоры программы — CEFR с. 79 и с. 67 — открыты постранично и **двумя
  разборами**; ступень у обеих A1, а первый разбор приклеивает к ним «Pre-A1»
  (раздел 2.1);
- дескрипторы Cambridge — Young Learners, с. 9 и с. 8, столбец LISTENING AND
  SPEAKING ступени Pre A1 Starters;
- строки `What colour is it?`, `What’s your favourite colour?` и
  `Is your house big or small?` — Young Learners, с. 28, Speaking Parts 3 и 4;
- грамматические строки `He’s a small boy`, `Lemons are yellow`,
  `These books are blue` — Young Learners, с. 29, Grammar and Structures List,
  Pre A1 Starters;
- перечень цветов Cambridge — с. 13 (задание) и с. 79 (тематический список), оба
  разбора;
- десять пунктов English Grammar Profile — открыты по `id` настоящим разбором
  CSV, все A1, все свободны (`npm run programma -- english-beginner-2`);
- ступень восемнадцати слов — `npm run slovo`, каждое отдельно;
- порядок карточек по курсу — `grep` по `term:` плюс
  `courses/english-beginner-2/index.ts`;
- восемь строк соседних замыслов, адресованных модулю 9, — открыты в файлах, а
  не по памяти;
- склад замороженного курса — `kakoy-on.ts`, шапка и словари прочитаны целиком.

**Чего проверить не удалось — говорю прямо:**

1. **Есть ли в Twemoji цветные квадраты `1f7e5`–`1f7eb` и `2b1c`.** Проверяется
   прогоном `npm run znachki`, у меня его нет. Если их нет, карточки цвета
   остаются без значка, и урок 2 теряет половину картинок.
2. **Как выглядят синий и фиолетовый квадраты на маленьком размере.** Это
   глазами, и это к владельцу или к сборщику.
3. **Чтение `purple`.** Условность курса даёт `/ˈpɜːpl/`, но самого слова в
   замороженном курсе нет, и сверить с уже принятым мне не с чем.
4. **Разметка столбцов на с. 79 руководства Young Learners.** Оба разбора её
   рвут: первый сливает три столбца в поток, второй выбрасывает `green` и
   `grey (US gray)` из таблицы в свободный текст. Вывод «все цвета на Pre A1
   Starters» сделан по совпадению двух разборов и по перечню с. 13, но **саму
   страницу глазами я не видел**.
5. **Не сломает ли записка урока 1 вывод вида урока.** Утверждение о коде
   сделано ЧТЕНИЕМ `lib/content/vid-uroka.ts`, а правило проекта требует
   прогона. **Сборщику проверить одной строкой.**
6. **Близнецы.** `npm run bliznetsy` по модулю 9 сейчас бессмыслен — модуля нет.
   Проверить обязательно после сборки, и особо — пары «урок 7 модуля 9 против
   урока 6 модуля 7» и «работа модуля 9 против работы модуля 7».
7. **Требует ли `check:content` вопроса на каждый итог модуля.** Если да,
   двенадцати вопросов мало (раздел 10, строка 8). Скрипта я не читал.
8. **Годится ли название модуля, где стоит слово, ни разу не работающее в
   уроках.** Английского `size` в модуле нет (раздел 6.4), русское «размер»
   пришло из программы. Это вопрос к владельцу, а не ко мне.
9. **Какой длины выйдет урок 1 по времени.** Мерка формата — 8–15 минут, а восемь
   случаев дают двадцать с лишним экранов; три модуля подряд объявляли
   расхождение (`voprosy-vladeltsu.md`, раздел 9). Модуль 9 объявляет его
   четвёртым и ничего не решает: решение это уровня программы.
