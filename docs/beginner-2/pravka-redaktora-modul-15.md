# Правка редактора: модуль 15 «Спросить о делах», урок 1

Заведено 11 сентября 2026, первым действием, до первой строки текста.
Правило проекта: работа с единственной точкой записи в конце теряется при обрыве
целиком. Поэтому записка пишется по ходу, а не собирается в отчёт.

**Граница работы:** только урок 1 `«Спрашиваю: Do you work?»`. Заглушки остальных
девяти уроков не тронуты ни одной.

## Что прочитано до первой строки

- `CLAUDE.md` целиком;
- `docs/beginner-2/stil-obyasneniy.md` — уговор о слоге, разделы 8.1–8.6;
- `docs/beginner-2/plan-modul-15.md` — разделы 1.1, 3.1–3.6, 4.4, 6.1;
- шапка `courses/english-beginner-2/sprosit-o-delah.ts`;
- модуль 14 `courses/english-beginner-2/ya-ne-delayu.ts` — голос соседа.

## Ход работы

ПРОВЕРИТЬ: ниже дописывается по ходу.

### Проверено до первой строки: две ловушки кода

1. `razobrat()` (`components/lesson/Ekran.tsx`, строки 76–82) режет строку
   объяснения по первому « — », если слева есть два латинских знака подряд, а
   справа два кириллических. **Слово `do` само по себе даёт эти два латинских
   знака.** Значит правило вида «Слово do стоит в начале строки, а знак вопроса
   — в конце» встало бы на экране ПАРОЙ: слева английской строкой «Слово do
   стоит в начале строки, а знак вопроса», справа переводом «в конце». Ни одного
   тире внутри строк правила не поставлено, союз везде стоит словом.
2. `sZvukom` берёт ключи ТОЛЬКО своего блока (`zvuchashchee(block)`), проверено
   чтением строк 141, 151, 314. Значит врезка без `zvuchat` кнопок не даёт вовсе,
   и верную строку в ней печатать безопасно. В строках правил ни одна строка из
   `zvuchat` своего блока не повторена — второй кнопки на экране не будет.

### НАХОДКА О СЛОВЕ «РАССКАЗ» — методисту и второму редактору

Заголовок таблицы урока 1 стоит не заглушкой, а написан методистом:
`head: ["Рассказ", "Вопрос", "Перевод"]` (`sprosit-o-delah.ts`, случай 5).
Трогать я его не вправе, поэтому пишу здесь.

**В курсе слово «рассказ» уже занято другим значением — связным текстом из
нескольких строк.** Признак: поиск корня «рассказ» по видимому тексту курса,
граница — все написанные модули, а не соседние. Адреса:

- модуль 9 «Цвет и размер»: «Из разобранных строк складывается короткий рассказ
  о вещи» (`cvet-i-razmer.ts`, имя случая) и подпись «Тот же рассказ о вещи, но
  целиком»;
- модуль 10 «Какой человек»: подпись «Тот же рассказ о друге, но целиком»;
- модуль 13 «Что я делаю каждый день»: итог урока чтения «находить в коротком
  рассказе, что человек делает и когда».

Там «рассказ» — это несколько строк подряд о вещи или о человеке. В таблице
урока 1 модуля 15 тем же словом названа ОДНА строка, противопоставленная
вопросу. Ученик, прочитавший модуль 9 и модуль 13, читает заголовок столбца и
ждёт связного текста.

**Своего слова взамен я не завожу и заголовок не трогаю**, а текст урока держу с
ним заодно: расхождение внутри модуля было бы хуже. Решать методисту.

## Написано: вступление и восемь случаев

- **вступление** — русская фраза первой («ты работаешь» и «ты работаешь?»),
  закрывающая строка одна на курс: «Здесь мы разберём…»;
- **случай 1**, подпись и разбор. Подпись зеркалит соседа («Алим рассказывает
  Дане о своём дне, а она отвечает ему. Первым говорит Алим»), чтобы голос был
  один. Разбор считает: «В этом разговоре два вопроса» — вопросов в сцене ровно
  два, и оба у Даны. Про знак вопроса в разборе НЕ сказано нарочно: это предмет
  случая 7, и, сказав здесь, я оставил бы тот экран пустым;
- **случай 2**. Второе предложение правила («переставлять их не нужно») стоит не
  для полноты: с формой be порядок как раз меняется (`You are a teacher.` даёт
  `Are you a teacher?`), и двенадцать модулей ученик делал именно так;
- **случай 3** зеркалит правило соседа дословным строем: «Меняется только начало
  строки, потому что в ней появляется новое слово». Слова «никогда» в правиле
  нет — урок 4 этого же модуля снимает `-s` при `does`;
- **случай 4** — то самое место, где модуль обязан не промолчать (замысел, 3.5):
  «В отрицании такое слово стоит прямо перед глаголом, а в вопросе оно уходит в
  самое начало строки». Формула соседа «слово перед глаголом» здесь названа как
  ПРЕЖНЕЕ место, а не повторена;
- **случай 5**, подпись таблицы. Голого повтора заголовков столбцов нет: подпись
  говорит то, чего столбцы не говорят, — слова в обеих строках одни и те же;
- **случай 6**, врезка. Заголовок и три предложения;
- **случаи 7 и 8** — имя и правило в два предложения.

## Написано: шесть заданий

Все шесть заглушек закрыты. Разбор `speak` кончается формулой курса дословно
(уговор 8.1), и родовых форм в ней нет: «сверься с образцом» рода не требует.

Условия английскую строку ставят после двоеточия (уговор 8.2): «Прочитай строку:
You drive.» Русская фраза в задании на сборку стоит в «ёлочках» (уговор 8.3):
«Собери из трёх кусков вопрос «ты знаешь Дану?»» — выбора там нет, проверяется
запись, значит второй способ законен.

**У З1 условие НЕ повторяет счёт из заглушки, и вот почему.** Заглушка сборщика
говорила «строк с рассказом тут поровну», замысел (6.1, З1) — «две строки из
четырёх рассказ, а не вопрос». **Оба утверждения ложны по собранному блоку:**
строк четыре, рассказ среди них ОДИН (`You live here.`), а `Are you a teacher?`
— вопрос, только начатый формой be. Сборщик поставил строку с формой be по
требованию самого замысла («ОСТОРОЖНО С З1»), и счёт замысла от этого устарел.

Условие написано по собранному блоку: «Таких строк здесь две» — отмечаемых `Do`
ровно два.

**И вторая половина того же места: условие пришлось сузить до «вопрос с do».**
Скажи оно просто «отметь слово, с которого начинается вопрос», и `Are` стало бы
верным ответом — `Are you a teacher?` вопрос и начинается с `Are`. Ученик
отмечал бы верно и получал «пока не так». Уточнение «вопрос с do» — то самое
имя с уточнением, которое разрешает замысел (3.1), а не второе имя вещи.

## Находки, которые я НЕ правил: не мой предмет

### 1. Ответ З2 напечатан в самом имени урока

Урок зовётся «Спрашиваю: Do you work?», и ровно строка `Do you work?` стоит
верным вариантом задания З2 «отметь строку, в которой вопрос записан без
ошибки». Имя урока видно на карте курса и в шапке экрана, то есть ответ у ученика
перед глазами всё время. Та же строка стоит примером словарной карточки `do`, а в
словарь ученик заходит нарочно.

Ступенька у З2 «узнать», и узнавание там законно, поэтому находка не срочная. Но
имя урока и вариант задания назвал методист, менять их я не вправе. Решать ему.

### 2. Ответ З5 напечатан во врезке за два экрана до задания

Врезка случая 6 обязана дать верную запись («поправить, не сказав чем»,
запрещено) и даёт: `Do you know Dana?` Ровно эту строку З5 просит собрать из трёх
кусков. `check:content` молчит, и молчит правильно: у проверки утечки порог
`DLINA_UTECHKI = 4`, а ответ ровно из четырёх слов да ещё и разрезан на куски.

Чинится это не текстом, а сменой строки в одном из двух мест, то есть работой
методиста.

### 3. «признак» — слово из списка запрещённых ярлыков, и оно стоит в девяти
### заглушках ОСТАЛЬНЫХ уроков модуля

`npm run yazyk` даёт по модулю девять ошибок этого вида, и все девять — в
заглушках уроков 2–10 и работы. В своём уроке я это слово снял: имя случая стало
«Вопрос с do виден по началу и по концу строки», а правило говорит «начало и
конец» вместо «два признака».

**Следующему редактору модуля: слово придётся снимать в девяти местах.** Заглушки
сборщика пересказывают замысел, а замысел зовёт эту вещь «признаком» — значит
подстановка идёт из документа, и переписывать надо каждую заглушку, а не менять
слово.

**И имя случая разошлось с его `id`:** `sluchay-u-voprosa-dva-priznaka` при имени
«Вопрос с do виден по началу и по концу строки». `id` — код, я его не трогаю;
сборщику стоит знать.

### 4. У таблицы урока 1 объяснения после неё нет вовсе

Просьба владельца от 20 августа: объяснение идёт ПОСЛЕ таблицы и коротко, а
«таблица отвечает КАКАЯ строка, объяснение ПОЧЕМУ она такая». В уроке 1 за
таблицей сразу идёт врезка об ошибке. Почему строка такая, сказано ДО таблицы
случаями 2, 3 и 4, и отсылки «кому какая — в таблице выше» в уроке нет ни одной,
то есть объяснение не потерялось. Подпись таблицы я написал так, чтобы она несла
сведение, а не повторяла заголовки столбцов.

Говорю об этом вслух, потому что строение таблиц — дело методиста, а не моё.

## Что прогнано и с каким итогом

| Прогон | Итог |
|---|---|
| `npx tsc --noEmit` | чисто, код выхода 0 |
| `npm run yazyk -- english-beginner-2` | по уроку 1 — **ноль**. По модулю осталось 9, все в заглушках уроков 2–10 (слово «признак») |
| `npm run check:content` | по уроку 1 — **ноль ошибок**. По модулю ошибки только на незаполненных заглушках |
| `npm run --silent tekst -- english-beginner-2 sprosit-o-delah` | урок прочитан целиком как его видит ученик, а не как он лежит в файле |

**Две находки сделал не отчёт, а чтение вывода `tekst`, уже после чистых
прогонов:**

1. правило случая 4 стояло с оборотом «В отрицании ТАКОЕ СЛОВО стоит прямо перед
   глаголом». Указательное слово отсылало к `do`, а в отрицании стоит `don't`, и
   ученик прочёл бы, что `do` уже было перед глаголом. Названо своим именем:
   «В отрицании перед глаголом стоит don't»;
2. в разборе З5 стояло «глагол с именем закрывают строку» — сказуемое во
   множественном числе при обороте с предлогом. Стало «глагол и имя закрывают
   строку».

**Правки, ограниченные уроком 1, проверены `git diff`:** все куски правки лежат
между строкой 249 и строкой 605, а урок 2 начинается строкой 618. Ни одной
заглушки остальных девяти уроков не тронуто.

## ПРОВЕРИТЬ СЛЕДУЮЩЕМУ (второму редактору)

- слово «рассказ» в заголовке таблицы — раздел «Находка о слове „рассказ"» выше;
- ответ З2 стоит в имени урока, ответ З5 стоит во врезке — оба к методисту;
- девять «признаков» в заглушках уроков 2–10;
- звука у урока нет ни одного (`check:content` сведениями): озвучку ставит
  сводящий, и до неё кнопки на экране молчат.

---

# Круг второй: правки по приёмке второго редактора

11 сентября 2026. Разбор — `docs/beginner-2/priyomka-uroka-1-modul-15.md`,
приговор «доработать», пять правок мои. Внесены все пять, одна — не теми
словами, что были предложены, и довод ниже.

## Внесено

1. **[364] Подпись таблицы.** Было «Одни и те же слова в рассказе и в вопросе»,
   стало **«Что меняется, когда рассказ становится вопросом»**. Находка верная и
   стоила бы дорого: подпись спорила с вступлением того же урока, где сказано,
   что в начале вопроса появляется новое слово. Замену взял предложенную.

2. **[326] Слово «только» снято.** Было «Меняется только начало строки», стало
   «Меняется начало строки». Находка верная, и вот чем она поучительна:
   **слово пришло из модуля 14**, где его правило стоит дословно («Меняется
   только начало строки, потому что перед глаголом появляется новое слово»). У
   соседа оно истинно — точка в конце отрицания остаётся точкой. Здесь конец
   строки меняется, и правило опровергалось случаем 7 своего же урока и разбором
   З4. Держать голос соседа надо, а его утверждения — не всегда.

3. **[508] «тоже» в разборе З1.** Стало «А строка Are you a teacher? — вопрос, но
   начинается она формой be, а не словом do». Союз «а» в начале снимает то, из-за
   чего «тоже» спотыкалось: предыдущее предложение говорит, что вопроса в строке
   нет. Тире здесь между подлежащим и сказуемым, а не вместо союза, и `razobrat()`
   до разбора задания не достаёт — проверено чтением: пары режутся только в ветке
   `explain` (`Ekran.tsx`, строка 140).

4 и 5. **[403] Врезка: строй приведён к модулю 14, заголовок переписан.** Обе
   правки об одном блоке, поэтому внесены разом.

   Строй соседа прочитан по всем шести его врезкам через `npm run tekst`, а не по
   памяти: заголовок — правило, потом правило развёрнутое, потом «Поэтому запись
   … сделана с ошибкой», потом «Верной будет строка …». **Запись — ошибочная,
   строка — верная**, и у меня было наоборот. Стало:

   > Про действие спрашивают словом do
   >
   > Вопрос о действии начинают словом do, а форма be начинает вопрос о том, кто
   > человек. Поэтому запись Are you know Dana? сделана с ошибкой, ведь в ней
   > после формы be стоит глагол. Верной будет строка Do you know Dana?

## С чем не согласился: заголовок врезки написан не предложенными словами

Предложено было **«После формы be глагол не ставят»**. Я взял другое, и вот
довод.

Находка второго редактора верна: мой прежний заголовок «Вопрос о действии не
начинают формой be» — правило о ЯЗЫКЕ, и его опровергает `Are you working?`,
который курс даёт модулем 17. **Но предложенная замена опровергается тем же
примером, и прямее прежней:** в `Are you working?` после формы be стоит ровно
глагол. Заменив, я починил бы названный признак и оставил породу.

Поэтому заголовок положительный и ничего не запрещает: **«Про действие
спрашивают словом do»**. Правило проекта тут прямое — графа источника это
разрешение, а не запрет, и учить надо своему случаю, а не отрицанию чужого.
Точность, которой просил разбор («вопрос начат формой be, А ПОСЛЕ НЕЁ СТОИТ
ГЛАГОЛ»), никуда не делась: она стоит в теле врезки доводом при ошибочной записи.

## Что нашлось, пока я вносил правки, и в разборе этого не было

**Довод, снятый во врезке, остался жить в разборе З2 — через два экрана.**
Разбор говорил «В строке Are you work? вопрос начат формой be, а спросить надо о
действии», то есть держал ту самую широкую рамку, ради которой правилась врезка.
Стало: «после формы be стоит глагол, а вопрос о действии начинают словом do» —
тот же довод, что и во врезке, слово в слово по устройству.

Порода известная и записана в правилах: довод, снявший одну строку, обязан
пройти по всем её родственникам. `sosedi` этого не видит — врезка и задание
разные блоки, — и разбор второго редактора этого места не назвал, потому что до
правки оно было согласовано с врезкой.

## Чего НЕ трогал, по указанию сводящего

Находки 4 (таблица — ключ к ответам З4 и З5), 5 («рассказ» — третье имя), 6 (З6
повторяет сцену), 7 (`exact: true` у З3) и всё спорное уходят методисту. Слово
«рассказ» остаётся в подписи таблицы потому, что заголовок столбца его держит, и
расходиться с ним я не вправе.

## Прогоны после правок

| Прогон | Итог |
|---|---|
| `npx tsc --noEmit` | чисто |
| `npm run yazyk -- english-beginner-2` | по уроку 1 — ноль. По курсу 9, все в заглушках уроков 2–10 |
| `npm run check:content` | по уроку 1 — ноль ошибок; подписи блоков внутри модуля не повторяются |
| `npm run --silent tekst` | окружение каждого изменённого места перечитано глазами |

Границы правки: последний кусок `git diff` кончается строкой 605, урок 2
начинается строкой 618. Заглушек в уроке 1 не осталось, в остальных девяти не
тронуто ни одной.

## ПРИНЯТО К СВЕДЕНИЮ НА УРОКИ 3, 4 И 6

Заглушка З2 стоит дословно ещё в двух уроках, заглушка З5 — ещё в трёх. Заглушка
это сжатый пересказ будущего текста, и писать по ней значит написать близнеца.
Условия там пишутся от того, что ученик ДЕЛАЕТ: совпало действие — близнец,
сколько бы разных слов вокруг ни стояло. У `gap` и `short` машина этого не видит
по устройству, значит ловить глазами.
