# Модуль 2, уроки 1 и 3–8. Замыслы

6 сентября 2026, правка по разбору второго методиста
([razbor-planov-modul-2-uroki.md](razbor-planov-modul-2-uroki.md): пятнадцать
ошибок, девять вопросов, семь сведений). Пишет методист; весь видимый русский
текст — редактор.

Образец серии — принятый урок 2, [plan-modul-2-urok-1.md](plan-modul-2-urok-1.md).
Замысел модуля — [plan-modul-2.md](plan-modul-2.md).

## Что изменила правка: карта ответов на тридцать один пункт

| № | Что нашёл разбор | Что сделано |
|---|---|---|
| 1 | `is` объяснено числом людей, а `I` тоже один | **Принято.** Основание на весь модуль одно: форму выбирает слово перед ней. Переписаны случай 3 урока 4, разбор сцены урока 4, условие V-задания и подсказка |
| 2 | «вторая форма», «третья форма» — слова заняты причастием | **Принято.** Счёта форм нет нигде: «ещё одна форма глагола be», «форма are» |
| 3 | V5 собирается ещё и в `my friend is Kim` — реплику своей сцены | **Принято.** Сборка из трёх кусков «имя / is / my friend» даёт зеркало ВСЕГДА, поэтому вид задания снят из урока 4 совсем |
| 4 | X5: `Hello, May.` и `Hi, May.` тоже верны | **Принято.** Условие получает слова «приветствием, которое называет время дня» — как у T4 урока 2 |
| 5 | пустая строка в `body` валит сборку | **Принято.** Два блока `text`, каждый со своими пятнадцатью словами |
| 6 | `see you` нет ни в словнике, ни в Oxford | **Принято, карточка снята.** Вопрос владельцу с временным ответом |
| 7 | урок 5, случай 8: «меняется только форма», а в примерах форма одна | **Принято.** Слова «только форма» сняты |
| 8 | мерка 3 объявлена чистой, а «только», «обязательно», «везде» стоят в пяти правилах | **Принято.** Три правила переписаны, мерка дополнена четырьмя словами |
| 9 | пять уроков одним строем, четыре пары заданий-близнецов | **Принято.** Четыре разных строя, ни одной пары с общим условием; разбор ниже |
| 10 | прилагательное после be взято незаявленным | **Принято.** Стало четвёртым готовым оборотом модуля |
| 11 | Z1 стоит на ходе, которого нет в правиле | **Принято.** Ход «отвечают» дописан, а Z1 собирает те три хода, что правило называет |
| 12 | мерка 2 по Z2 объявлена чистой неверно | **Принято.** Отвлекающий напечатан, и это законно: знаком отвлекающий, а не верный |
| 13 | пять случаев не обещаны итогом, свод не спрошен | **Принято частично.** Разбор в разделе «итоги и случаи» ниже: пункт `egp` итогом обещать не обязан, а вот наказывать за необещанное нельзя — `exact: true` у U4 снят |
| 14 | `vozvrat` выпало | **Принято**, стоит в уроке 3 |
| 15 | два документа спорят о значках карточек | **Принято.** Значки у карточек есть; строка в разделе 7 замысла модуля правится |
| 16 | урок 1 без единого производимого ответа | **Принято.** Заданий четыре, два из них — производство |
| 17 | второстепенное ударение объявлено решённым | **Принято.** Ссылка снята, вопрос владельцу поставлен |
| 18 | итоги обещают речь, спрашивают письмо | **Принято.** Четыре итога переписаны по проверяемой половине |
| 19 | в записях три человека и два голоса; Kim меняет пол | **Принято.** В обеих записях по два человека; Kim мужской во всём курсе |
| 20 | `Hello, Nurlan!` в середине письма к Сью | **Принято**, строка снята |
| 21 | у блока `text` нет поля перевода | **Вопрос сборщику**, временный ответ ниже |
| 22 | X4 не исключает `Goodbye, Sam.` | **Принято.** Со снятием `see you` задание переписано целиком |
| 23 | `1f501` несёт четыре мысли | **Принято.** Значок закреплён за «одно вместо другого», два случая получили другие |
| 24 | обещание «следующий урок» без имени | **Принято**, урок назван |
| 25 | счёт экранов у трёх уроков неверен | **Принято.** Один блок — один экран, все счёты пересчитаны |
| 26 | `am` нет ни в Oxford, ни в словнике | **Принято**, опора названа через `be` |
| 27 | шесть полей `accept` мертвы | **Принято**, сняты |
| 28 | случай 5.7 дублирует свою таблицу и не звучит | **Принято**, примеры и `zvuchat` переписаны |
| 29 | заголовок случая сам станет кнопкой | **Принято**, два случая урока 5 переименованы |
| 30 | семь мелких расхождений счёта и ссылок | **Все семь приняты**, поправлены по месту |
| 31 | строй `my friend is …` не разобран ни одним случаем | **Принято.** Случай 4 урока 4 расширен, таблица получила пятую строку |

## Правки 7 сентября: одиннадцать мест второго редактора

| № | Что нашёл | Что сделано |
|---|---|---|
| 1 | случай урока 1 дублирует урок 2 | **снят.** Правило принадлежит уроку правил; мысль осталась в подсказке одного задания из трёх |
| 2 | одно основание напечатано в уроке 4 трижды | названо **один раз**, в случае 2; случаи 3 и 7 говорят своё новое, и заголовок случая 7 сменён |
| 3 | `my` и `your` объяснены дважды подряд | случай 4 урока 4 оставил только новое — `my friend` перед формой |
| 4 | свод урока 6 — близнец таблицы урока 4 | свод переписан на **короткую запись**, которой модуль нигде не собирал; строк три, а не пять |
| 5 | пять мест на рамке `Good ___, Kim.` | рамка осталась ровно за принятым уроком 2: `gap` снят из уроков 1 и 6 |
| 6 | случай «как спросить имя» не несёт нового | **снят**; в уроке 3 семь случаев, недостача объявлена |
| 7 | одна мысль трижды в уроке чтения | врезка снята, осталось два места с разной работой |
| 8 | перевод текстов спрятан в разбор | перевод стоит **под каждым сообщением** блоком `note` |
| 9 | третья реплика записи не сходится с подписью | в записях по **две реплики** и по два человека |
| 10 | ножницы связывают `Bye` с апострофом | `Bye` берёт линейку `1f4cf`; ножницы остались за короткой записью |
| 11 | тире в переводах не решено на модуль | решено: тире внутри перевода нет; вид выбирает редактор, требование — единообразие |
| + | `And you?` в уроке 8 | **строка снята** — она лишняя по правилу самого урока |
| + | «слов в нём не меняем» | **снято**: запрет об английском без графы, и вдобавок неверный |

**И одно, что разбор закрыл без меня:** пара `Good night` / `Good evening` теперь
имеет опору — выписка из Викисловаря в `docs/istochniki-norma-yazyka.md`, пункт
12. Долг снят, и урок 6 ссылается на неё.

---

## Четыре решения на весь модуль, без которых уроки не читаются

### 1. Форму глагола be выбирает СЛОВО ПЕРЕД НЕЙ, а не число людей

Это единственное основание на весь модуль, и другого нигде быть не должно.
Опора: `PRONOUNS / subject-object`, `FORM: (SUBJECT) STATEMENTS`, id
`1741163713868x463659211645272000`, A1 — «Can use the pronouns 'I', 'you', 'he',
'she', 'it', 'we' and 'they' **in the subject position before a verb**». Графа
говорит о МЕСТЕ слова.

Прежняя редакция объясняла `is` тем, что «человек по-прежнему один», и это
неправда: `I` тоже один, а при нём `am`. Опровергала её собственная сцена урока
4, где Сью говорит `I am Sue`. Порода записана в CLAUDE.md списком переписанных
итогов («согласует форму be с числом»).

**Где это правилось:** случай 3 урока 4, разбор под сценой урока 4, условие
задания V4 («Речь об одном человеке»), подсказка V5 («Речь о Пат, и она одна»).

### 2. Формы be НЕ НУМЕРУЮТСЯ

«Вторая форма» и «третья форма» — русские школьные имена прошедшего времени и
причастия, и курс сам их так употребляет: `english-elementary/kak-dolgo.ts`,
строка 240 («там have и третья форма»), `ekzamen.ts`, строка 707. Ступень ведёт к
Past Simple, и ученик встретит их в прежнем значении.

Говорим: «ещё одна форма глагола be», «форма `are`». Имя главной вещи модуля —
**форма глагола be** — от этого не меняется.

### 3. Четыре оборота берутся ГОТОВЫМИ, а не три

К прежним трём (`How are you?`, `I am fine, thank you.`, `My name is …`)
прибавляется четвёртый, и это правка по разбору:

**Прилагательное после be** — строки `I am fine.`, `He is fine.`, `She is fine.`
Графа: `ADJECTIVES / position`, `FORM: PREDICATIVE, WITH 'BE'`, id
`1741163706530x753542801715210100`, A1 — «Can use a limited range of adjectives
predicatively, after 'be'». Замысел модуля сам роздал её **модулям 9 и 10**
(раздел 0.1, пункт 3), а урок 5 её употреблял, нигде не объявив. Это ровно тот
упрёк, за который замысел разбирает `kto-eto`: незаявленное заимствование хуже
заявленного.

Строка обязана стоять и в `sources` модуля, и в его шапке: слово `fine` после
`be` взято здесь внутри готовой формулы, разбирают прилагательное модули 9 и 10.

### 4. Тире внутри перевода в модуле не ставится

**Решение на весь модуль, принято 7 сентября по разбору.** Расхождение стояло в
пяти местах и рядом: `Tom is my friend.` переведено «Том мой друг», а
`My friend is Tom.` — «Мой друг — Том», и обе строки стоят в одной таблице.

**Техническое основание, и оно моё.** Перевод выводится на страницу ЧЕРЕЗ ТИРЕ —
это записано оговоркой 2 к правилу 9 («строка со своим тире перевода не
получает: перевод выводится через тире и с ним сольётся»). Значит тире внутри
перевода даёт в одной строке два тире, и ученик читает второе как продолжение
первого.

**Поэтому единообразие обязательно, а вид его выбирает редактор.** Норма русского
языка — не моя область: я называю только требование, что схема во всех пяти
местах одна. Сейчас во всех переводах модуля тире нет — «Том мой друг», «Мой друг
Ким», «Мой друг Билл». Если редактор решит, что норма требует тире, оно ставится
во ВСЕХ пяти, и тогда сборщику надо посмотреть глазами, как это выглядит рядом с
выводным тире.

### Итоги и случаи: что итог обязан обещать, а что нет

Разбор насчитал пять случаев, не обещанных ни одним итогом (заглавная `I`, `my` и
`your`, `Your name, please?`, `fine` о другом человеке, свод форм be) и назвал
это ошибкой. **Принимаю наполовину, и вот граница.**

Итог обещает **умение**. Пункт грамматики, на котором умение стоит, итогом
обещать не обязан — иначе всякий модуль получит по пять итогов, а правило
«итог не шире материала» превратится в «итог перечисляет материал». Ни программа
ступени, ни `check:content` этого не требуют: они требуют, чтобы каждый ИТОГ
спрашивался работой.

**Но обратное верно и здесь нарушено было:** задание не вправе наказывать за то,
чего итог не обещал. Такое место было одно — `exact: true` у задания U4 урока 3:
ученик терял ответ за строчную `i`, а итог урока о заглавной букве не говорит.
**`exact` снят.** Заглавную `I` спрашивает работа модуля — там ей и место.

Свод форм be в уроке 6 задания не получает нарочно: он повторяет то, что уже
спрошено заданиями уроков 3 и 4, и итога на себе не несёт — как `speak`.

### Строй заданий: четыре разных, и ни одной пары с общим условием

Разбор нашёл главное: пять уроков шли одним порядком `choice → hottext → gap →
short → short → speak`, и четыре пары заданий отличались одним словом. Это та
самая порода «однородные уроки подряд повторяют один промах столько раз, сколько
их», и круг после первого урока её поймать не мог.

Строй теперь задаёт **дело урока**, а не привычка:

| Урок | Дело | Строй заданий |
|---|---|---|
| 1 | услышать приветствие | `choice` · `hottext` · `gap` · `short` |
| 3 | назвать себя, в том числе со слуха | `match` · `hottext` · `gap` · `short` · `short` со звуком · `speak` |
| 4 | сказать о третьем и увидеть чужую ошибку | `hottext` · `choice` (найти ошибку) · `match` · `short` (исправить) · `short` (с нуля) · `speak` |
| 5 | обменяться репликами | `choice` со звуком · `hottext` · `order` · `short` (исправить) · `short` (записать) · `speak` |
| 6 | различить встречу и прощание | `hottext` · `choice` · `short` (исправить) · `short` (записать) · `gap` · `speak` |

Четыре разных порядка на четыре урока правил. **Четыре пары близнецов сняты
сменой действия, а не слова** (решение проекта от 2 сентября):

- прежние V2 и W2 («В двух строках отметь форму глагола be» дословно) — второго
  нет вовсе: `hottext` урока 4 отмечает слова, ПРИ которых стоит `is`, `hottext`
  урока 5 отмечает реплику, которой возвращают вопрос;
- прежние U1 и V1 («Какая запись верна?» с теми же двумя отвлекающими) — в уроке
  3 задание стало `match`, в уроке 4 `choice` перевёрнут: ученик ищет **ошибку**
  среди трёх верных строк, а не верную среди трёх ошибок;
- прежние U3 и V3 (вставка формы be) — `gap` остался в уроке 3 (форма be) и в
  уроке 6 (слово прощания), в уроке 4 его нет;
- прежние U4 и V4 («Запиши ту же строку коротко») — в уроке 3 ученик записывает
  коротко, в уроке 4 **исправляет чужую запись без апострофа**, и это другое
  действие.

**И ещё одно, чего разбор не требовал, но чего требует дело:** в двух уроках
появились задания СО ЗВУКОМ — диктант о себе в уроке 3 и выбор ответа на слух в
уроке 5. Их предмет разговорный, и проверять его одним письмом было половиной
работы.

---

## УРОК 1. «Слушаем, как здороваются» — слушание, открывает модуль

```
slug: "slushaem-privetstviya-2"      vid: "slushanie"      estimatedMinutes: 10
outcome: "узнавать на слух, каким приветствием поздоровались"
```

**Опора.** CEFR, с. 48, Overall oral comprehension, **Pre-A1**: «Can **recognise
everyday, familiar words**/signs, provided they are delivered clearly and slowly
in a clearly defined, familiar everyday context»; **A1**: «Can recognise concrete
information… provided it is delivered slowly and clearly». Образец задания: A2
Key, с. 29, Listening Part 1 — «five short recordings, each with a question and
three images… choose the visual image»; там же Part 2 — ответ одним словом.

**Заданий четыре, и два из них — производство.** Прежняя редакция давала три
задания на узнавание и объявляла это решением; разбор справедливо возразил, что
норма курса обратная («вопрос на слух отвечается одним словом»), а собственное
правило замысла требует не больше половины узнавания. Оба производимых ответа —
одно слово, и списать его с соседнего экрана нельзя: какое из трёх прозвучало,
решает только слух.

### Экраны: девять

| Экран | Что стоит |
|---|---|
| 1 | вступление |
| 2 | запись 1, `audio`, `skryt: true`, `pace: "slow"`, `voice: "два голоса"` |
| 3 | словарь модуля: четыре карточки |
| 4 | врезка: слушать можно сколько нужно |
| 5 | запись 2, те же поля |
| 6 | задание I1 |
| 7 | задание I2 |
| 8 | задание I3 |
| 9 | «готово» |

**Случай из урока снят 7 сентября по разбору второго редактора.** Стояло правило
«три приветствия различаются вторым словом» — а урок 2 говорит то же самое почти
дословно, и именно первое предложение держит там заголовок случая 6. Проверка
«убери экран — что потеряет ученик» отвечает «ничего»: правило принадлежит уроку
правил, а урок слушания даёт запись. Ту же мысль несли подсказки трёх заданий —
теперь она стоит в подсказке ОДНОГО, первого; у остальных подсказки говорят своё.

### Две записи — по две реплики, по два человека

Разбор нашёл, что при трёх репликах и двух голосах Сью здоровается дважды подряд:
чередование отдаёт ей и первую, и третью, а подпись обещает встречу двоих.
Беда моя — я сам расписал голоса и не сложил их с содержанием реплик.

Проверено по коду: у блока `audio` поле `pervyyGolos` не читается вовсе
(`scripts/ozvuchka.mts`), первым всегда женский, дальше чередование. **Значит в
записи ровно два человека и ровно по одной реплике у каждого.**

```
zapis-den:     "— Good afternoon, Nurlan. — Hi, Sue."
zapis-vecher:  "— Good evening, Nurlan. — Good evening, Sue."
```

Сью (ж) — Нурлан (м). `caption`: «Днём на курсах встретились двое» и «Те же двое
вечером».

**Запись 1 заодно показывает то, чему учит урок 2:** на длинное приветствие
отвечают коротким. Ученик слышит это раньше, чем разбирает, — так и задуман
модуль-разговор.

Границу реплик у `audio` ставит тире с пробелами — другой механизм, чем перевод
строки у примера урока.

### Словарь модуля (экран 3)

| Карточка | Чтение | Перевод | Значок |
|---|---|---|---|
| `hi` | /haɪ/ | привет | 270b |
| `good morning` | /gʊd ˈmɔːnɪŋ/ | доброе утро | 1f305 |
| `good afternoon` | /gʊd ɑːftəˈnuːn/ | добрый день | 2600 |
| `good evening` | /gʊd ˈiːvnɪŋ/ | добрый вечер | 1f306 |

Формулы взяты записями целиком: словник A2 Key держит их так же — `good morning`
(962), `good afternoon` (958), `good evening` (960), `hi` (1049).

**Значки у карточек ЕСТЬ, и это решение против прежнего.** Раздел 7 замысла
модуля писал обратное («карточки приветствий значков не получают»); разбор
справедливо назвал расхождение двух документов. Отменяется прежнее: карточка со
значком узнаётся с одного взгляда, а те же значки стоят у случаев урока 2, и
ученик связывает формулу с ними.

**Второстепенное ударение НЕ показываем — но решения об этом ещё нет.** Прежняя
редакция ссылалась на «решение раздела 3 замысла модуля», а там стоит
предложение, а не решение. Вопрос владельцу поставлен
([voprosy-vladeltsu.md](voprosy-vladeltsu.md), пункт 7); до ответа держим запись
без знака `ˌ`, как `goodbye` в замороженном курсе.

### Три задания

| Экран | № | id | Вид | Что делает ученик |
|---|---|---|---|---|
| 6 | I1 | `i1-chem-otvetili` | `choice`, `about: "zapis-den"` | выбирает, каким приветствием ответили |
| 7 | I2 | `i2-zapisat-vecherneye` | `short`, `about: "zapis-vecher"` | записывает приветствие из записи |
| 8 | I3 | `i3-zapisat-dnevnoye` | `short`, `about: "zapis-den"` | записывает приветствие, с которого запись начинается |

Заданий три, производство два. Прежних четыре не выходит: в записи по две
реплики, и четвёртому заданию не на чем стоять. Мерка «узнавание не больше
половины» выполнена — узнавание одно из трёх.

**I1.** «Каким приветствием ответили в записи?»
Варианты `Hi` (верно) · `Hello` · `Good afternoon`.
Отвлекающие настоящие: `Hello` — тоже короткое приветствие, но в записи его нет;
`Good afternoon` в записи есть, но им поздоровались, а не ответили. Условие
«ответили» задаёт ответ однозначно.
**Подсказка** — единственное место урока, где сказано, что различать приветствия
надо по второму слову; у двух других заданий подсказки говорят своё.

**I2.** «Запиши приветствие, которое звучит в записи.»
`answer: "Good evening"` — без `accept`: сверка снимает регистр и знак в конце.
Списать нельзя: на экране три карточки со словом `good`, и какая звучала, слышно
только в записи.

**I3.** «Запиши приветствие, с которого начинается запись.»
`answer: "Good afternoon"`.
**Что ещё подходит:** в записи два приветствия, `Good afternoon` и `Hi`; слова «с
которого начинается» задают одно.

**Рамки `Good ___` в уроке нет, и это решение.** Она стоит в принятом уроке 2
(задание T3, `before: "Good "`, `after: ", Kim."`), и на ней же держались прежний
`gap` этого урока и `gap` урока 6 — пять мест на одну рамку, включая заглушки
работы. Рамка остаётся ровно за уроком 2; остальные задания сменили вид.

### Врезка (экран 4)

`id: "kak-slushat"`, `tone: "info"`. Первый абзац — заголовок. Сказать: слушать
запись можно сколько нужно, а расшифровка открывается кнопкой. **О том, как
устроена платформа, — ни слова.**

---

## УРОК 3. «Меня зовут: I am» — правила

```
slug: "menya-zovut-2"      vid: "pravila"      estimatedMinutes: 12
outcome: "строить строку о себе двумя способами: I am Alim и My name is Alim"
vozvrat: [{ iz: "alfavit-2", zadanie: "a3-zapisat-imya" }]
```

**Итог переписан по разбору.** Стояло «называть себя», а спрашивают его три
письменных задания; «называть» обещало речь, которой работа не спросит. «Строить
строку» проверяется тем, что ученик делает на деле.

**`vozvrat` вернулся.** Замысел модуля обещал его в этом уроке, а из замысла
урока он выпал. Задание `a3-zapisat-imya` модуля 1 — записать со слуха имя `Sam`
— и стоит перед тем, как ученик назовёт этим именем себя.

**Опора.** CEFR, с. 74, **Pre-A1**: «Can greet people, **state their name** and
take leave in a simple way»; A2 Key, с. 51: «introducing oneself», «asking for
and giving personal details: (full) name…».

### Экраны: восемнадцать

Один блок — один экран (`components/lesson/UrokShagami.tsx`).

| Экран | Что стоит |
|---|---|
| 1 | вступление |
| 2 | сцена, `example`, `razgovor: true` |
| 3 | словарь урока: `I`, `am` |
| 4 | случай 1: по-русски между «я» и именем ничего, по-английски `am` |
| 5 | случай 2: слово `I` пишем с заглавной везде |
| 6 | случай 3: `am` стоит при `I` |
| 7 | случай 4: короткая запись `I'm` |
| 8 | задание U1 |
| 9 | задание U2 |
| 10 | случай 5: второй способ — `My name is` |
| 11 | врезка: форма `is` здесь берётся целиком |
| 12 | случай 6: `my` и `your` перед словом |
| 13 | случай 7: оба способа годятся |
| 14 | задание U3 |
| 15 | задание U4 |
| 16 | задание U5 |
| 17 | задание U6 |
| 18 | «готово» |

### Сцена (экран 2)

`id: "scena-znakomstvo"`, `caption`: два незнакомых человека знакомятся утром.

```
Good morning. I am Dana.
Good morning, Dana. My name is Nurlan.
```

Голоса: Дана (ж) первой, Нурлан (м) вторым — по умолчанию.
`explain`: Дана назвала себя одним способом, Нурлан другим, а сказали они одно и
то же.

### Словарь урока (экран 3)

| Карточка | Чтение | Перевод | Значок |
|---|---|---|---|
| `I` | /aɪ/ | я | 1f9d1 |
| `am` | /æm/ | форма глагола be при `I` | 1f517 |

**Опора ступени названа точнее по разбору.** `I` — Oxford 3000, строка 1323,
«I pron. A1»; словник A2 Key, строка 1086. А записи `am` **нет ни в Oxford, ни в
словнике**: там стоит `be v., auxiliary v. A1` (Oxford, строка 243) и `be (av &
v)` (словник, строка 260). Пишем так: `am` — форма глагола `be`, которому Oxford
3000 ставит A1.

### Восемь случаев

**ЛОВУШКА РАЗБОРА СТРОКИ.** В строке ПРАВИЛА тире с пробелами по обе стороны не
ставится: `razobrat()` (`Ekran.tsx`, строка 75) превратит её в пару
«английское — перевод».

**Случай 1 (экран 4). Между «я» и именем по-английски стоит глагол.** `znak: 1f504`
Правило: по-русски между «я» и именем не стоит ничего — «я Алим». По-английски на
этом месте стоит `am`.
- `I am Alim.` — Я Алим.
- `I am Sue.` — Я Сью.

**Правило переписано по разбору.** Стояло «по-русски два слова, по-английски
обязательно три» — и через три экрана случай 4 давал `I'm Dana.`, два слова.
Счёт слов предметом больше не является, слова «обязательно» нет.

**Случай 2 (экран 5). Слово `I` пишут с заглавной буквы везде.** `znak: 1f524`
- `Hello. I am Kim.` — Здравствуйте. Я Ким.
- `Good morning. I am Dana.` — Доброе утро. Я Дана.

Слово «везде» здесь правда, и графы, которая бы это запрещала, не требуется: это
свойство английского письма, как состав пяти гласных букв в модуле 1. Так и
записать в `sources`.

**Случай 3 (экран 6). При `I` стоит форма `am`.** `znak: 1f9e9`
Правило: форму глагола be выбирает слово, стоящее перед ней; при `I` это `am`.
При `he` и `she` форма другая, и её разбирает урок **«Он и она: he is, she is»**.
- `I am Nurlan.` — Я Нурлан.
- `I am Bill.` — Я Билл.

**Обещание получило имя урока** — прежде стояло «следующий урок».

**Случай 4 (экран 7). В разговоре `I am` сокращают до `I'm`.** `znak: 2702`
Правило: буква `a` выпадает, а на её месте ставят апостроф — знак `'`.
- `I'm Dana.` — Я Дана.
- `I'm Sue.` — Я Сью.

Апостроф вводится здесь: шапка модуля 1 прямо обещала, что он понадобится курсу.

**Случай 5 (экран 10). Второй способ: `My name is …`.** `znak: 1faaa`
Правило: по-русски мы говорим «меня зовут Алим»; по-английски эту же мысль
говорят строкой `My name is Alim`.
- `My name is Alim.` — Меня зовут Алим.
- `My name is Kim.` — Меня зовут Ким.

**Врезка (экран 11).** `id: "forma-is-poka-tselikom"`, `tone: "info"`: в строке
`My name is …` стоит **ещё одна форма глагола be**, `is`; здесь она берётся
целиком, а разбирает её урок «Он и она: he is, she is». Счёта форм во врезке нет.

**Случай 6 (экран 12). `my` — «мой», `your` — «твой, ваш».** `znak: 1f446`
Правило: слова `my` и `your` ставят перед словом, которое называет вещь или
человека, и отвечают они на вопрос «чей».
- `my name` — моё имя
- `your name` — ваше имя

Обе пары ученик видел карточками в модуле 1; здесь они впервые разбираются, и это
объявленный пункт `egp` модуля.

**Случай о том, как спрашивают имя, СНЯТ 7 сентября по разбору.** Стояло:
`Your name, please?` — а строка дана в модуле 1, разбора здесь нет, итог урока
спрашивать имя не обещает, и ссылка «у стойки она звучит в модуле „Алфавит"»
отсылала к тому, чего ученик не вспомнит. Случай не нёс нового ничего.

**Случаев в уроке семь, примеров четырнадцать, и мерка формата не выполнена.**
Восьмой случай пришлось бы сочинить из ничего — а сочинённый случай хуже
недостачи. Прецедент тот же, что в модуле 1 (алфавит) и в уроке 6. Слово `your`
при этом остаётся разобранным: оно стоит в случае 6.

**Случай 7 (экран 13). Оба способа годятся.** `znak: 1f500`
- `I am Pat.` — Я Пат.
- `My name is Pat.` — Меня зовут Пат.

**Звучащее поимённо:** 1 `["I am Alim.", "I am Sue."]`;
2 `["Hello. I am Kim.", "Good morning. I am Dana."]`; 3 `["I am Nurlan.", "I am Bill."]`;
4 `["I'm Dana.", "I'm Sue."]`; 5 `["My name is Alim.", "My name is Kim."]`;
6 `["my name", "your name"]`; 7 `["I am Pat.", "My name is Pat."]`.

**Значки:** 1f504, 1f524, 1f9e9, 2702, 1faaa, 1f446, 1f500 — семь разных.

### Шесть заданий: `match` · `hottext` · `gap` · `short` · `short` со звуком · `speak`

| Экран | № | id | Вид | Что делает ученик |
|---|---|---|---|---|
| 8 | U1 | `u1-russkaya-i-angliyskaya` | `match` | соединяет русскую строку с английской |
| 9 | U2 | `u2-lishnee-slovo` | `hottext` | отмечает слово, которого нет в русской фразе |
| 14 | U3 | `u3-vpisat-formu` | `gap` | вписывает форму be |
| 15 | U4 | `u4-korotkaya-zapis` | `short` | записывает строку коротко |
| 16 | U5 | `u5-diktant-o-sebe` | `short` со `zvuk` | записывает со слуха, как человек назвал себя |
| 17 | U6 | `u6-skazat-o-sebe` | `speak` | называет себя вслух двумя способами |

**U1** — вместо прежнего `choice`, который был близнецом задания урока 4.
«Соедини русскую строку с английской, которая значит то же самое.»
`left`: «Я Ник.» · «Меня зовут Ник.» · «Ваше имя, пожалуйста.»
`right`: «Your name, please?» · «I am Nick.» · «My name is Nick.»
`answer: [1, 2, 0]` — пункты справа переставлены нарочно.
**Однозначность:** каждая русская строка отвечает ровно одной английской.
**Мерка 1:** ни одна из трёх английских строк в уроке не напечатана — случаи
дают `I am Alim/Sue/Nurlan/Bill/Pat`, `My name is Alim/Kim/Nurlan/Pat`, а
`Your name, please?` в уроке теперь не печатается вовсе — случай о ней снят, а
строку ученик знает из модуля 1, где она разобрана. Совпадений нет ни одного.

**U2.** «В строке отметь слово, которого в русской фразе нет.»
`parts`: `I ` (sel) · `am` (sel, верно) · ` Sam` (sel) · `.`
Перевод — «Я Сэм», два слова; английских три. Отмечаемых три, верный один.

**U3.** «Тебя зовут Ник. Назови себя. Впиши недостающее слово.»
`before: "I "`, `after: " Nick."`, `answer: "am"`. `exact` не ставить.
**Что ещё подходит:** слово `I` уже стоит в `before`, а при нём форма одна.

**U4.** «Запиши ту же строку коротко: I am Nick.»
`answer: "I'm Nick."` — **без `accept` и без `exact`.**
Разбор показал, что прежние три строки `accept` мертвы: `normalizeKeepingCase` в
`lib/content/check.ts` сама обрезает знак в конце и заменяет типографский
апостроф на обычный. А `exact: true` снят по разделу об итогах: заглавная `I` —
предмет случая 2, но итог урока её не обещает, и наказывать за неё здесь нельзя.

**U5 — новое задание, и оно со звуком.** «Послушай, как человек называет себя, и
запиши эту строку.»
`zvuk: "My name is Sue."`, `answer: "My name is Sue."`
**Зачем оно тут:** предмет урока — строка о себе, и слышат её чаще, чем читают.
Прежняя редакция проверяла урок одним письмом.
**Мерка 1:** строка в уроке не напечатана.
**Порода «набор текста вместо слуха»:** строка короткая и целиком из разобранного,
а выбор между двумя способами делает только слух.

**U6.** `speak`. «Назови себя вслух двумя способами.»
`phrase: "I am Alim. My name is Alim."`, `translation: "Я Алим. Меня зовут Алим."`
Итога не несёт. В разборе — что ученик сверяет себя сам, слушая образец; о том,
кто и что оценивает, ни слова.

**Лестница:** узнать (U1, U2) → произвести (U3, U4, U5) → применить (U6).
Производство три из шести плюс `speak`.

---

## УРОК 4. «Он и она: he is, she is» — правила

```
slug: "on-i-ona-2"      vid: "pravila"      estimatedMinutes: 12
outcome: "строить строку о другом человеке с he is и she is"
```

**Опора.** CEFR, с. 74, A1: «Can make an **introduction**»; A2 Key, с. 51:
«introducing oneself **and other people**». Пункты: `VERBS / types` LINKING VERB
'BE', `PRONOUNS` (SUBJECT) STATEMENTS, `CLAUSES / declarative` AFFIRMATIVE
DECLARATIVE (пример графы — «My friend is a nurse»), `NOUNS` AS COMPLEMENT OF 'BE'.

**Притяжательное здесь возвращается, а не вводится:** урок 3 разобрал `my name` и
`your name`, урок 4 показывает то же слово перед другим существительным.

### Экраны: восемнадцать

| Экран | Что стоит |
|---|---|
| 1 | вступление |
| 2 | сцена, `example`, `razgovor: true` |
| 3 | словарь урока: `he`, `she`, `friend` |
| 4 | случай 1: `he` — «он», `she` — «она» |
| 5 | случай 2: при `he` и `she` стоит `is` |
| 6 | случай 3: вместо `he` и `she` называют имя |
| 7 | задание V1 |
| 8 | задание V2 |
| 9 | случай 4: `my` перед словом, и `my friend` бывает перед `is` |
| 10 | случай 5: `he` и `she` заменяют имя |
| 11 | случай 6: короткие записи `he's`, `she's` |
| 12 | случай 7: форму выбирает слово перед ней + таблица, `naTomZheEkrane` |
| 13 | задание V3 |
| 14 | случай 8: слово `friend` — и друг, и подруга |
| 15 | задание V4 |
| 16 | задание V5 |
| 17 | задание V6 |
| 18 | «готово» |

### Сцена (экран 2)

`id: "scena-sue-i-kim"`, `caption`: Сью знакомит Нурлана со своим другом.

```
Good afternoon. I am Sue. My friend is Kim.
Good afternoon, Sue. My name is Nurlan.
Hello, Kim.
Hi, Nurlan.
```

**Голоса и пол Ким — правка по разбору.** Чередование: Сью (ж) — Нурлан (м) —
Сью (ж) — **Ким (м)**. Прежняя редакция ставила три реплики, и Ким доставался
женский голос, тогда как в модулях 1 и 2 у него мужской: ученик услышал бы, что
Ким сменил пол. Четвёртая реплика ставит его на мужскую очередь.

**Пол `Kim` закреплён на весь курс — мужской.** Источник разрешает и то и другое
(Young Learners, с. 15), но выбор должен быть один, и в тексте курса о Ким
по-прежнему не пишут ни `he`, ни `she`.

`explain` под сценой: Сью назвала себя и назвала друга. **Формулировки «потому
что он один» здесь нет** — форму выбирает слово, стоящее перед ней, и в строке
`I am Sue` это видно рядом.

### Словарь урока (экран 3)

| Карточка | Чтение | Перевод | Значок |
|---|---|---|---|
| `he` | /hiː/ | он | 1f468 |
| `she` | /ʃiː/ | она | 1f469 |
| `friend` | /frend/ | друг, подруга | 1f9d1-200d-1f91d-200d-1f9d1 |

Всем трём Oxford 3000 ставит A1, все три в словнике A2 Key. `friend` —
единственное слово темы `Family and Friends`, которое модуль закрывает.
Значок `friend` свободен: раздел 4.2 замысла модуля звал его занятым словом
`they` в `kto-eto`, а тот модуль разбирается.

### Восемь случаев

**Случай 1 (экран 4). `he` — «он», `she` — «она».** `znak: 1f465`
- `He is Nurlan.` — Он Нурлан.
- `She is Sue.` — Она Сью.

**Случай 2 (экран 5). При `he` и `she` стоит форма `is`.** `znak: 1f9e9`
Правило: форму глагола be выбирает слово, стоящее перед ней; при `he` и `she` это
`is`, а при `I` — `am`.

**Основание названо ЗДЕСЬ и больше нигде.** Прежняя редакция печатала его трижды
— случаями 2, 3 и заголовком случая 7. Правильное основание, повторённое трижды,
занимает три экрана и не прибавляет ни одного нового сведения; нашёл второй
редактор.
- `He is Tom.` — Он Том.
- `She is May.` — Она Мэй.

**Случай 3 (экран 6). Перед формой может стоять имя, а не местоимение.**
`znak: 1faaa`
Правило: вместо `he` и `she` человека называют по имени, и форма при имени та же,
что при `he` и `she`.
- `Nurlan is my friend.` — Нурлан мой друг.
- `Sue is my friend.` — Сью моя подруга.

**Правило переписано целиком.** Стояло «потому что человек по-прежнему один» —
неправда: `I` тоже один, а при нём `am`, и это видно в сцене того же урока.

**Случай 4 (экран 9). `my friend` целиком стоит перед формой.** `znak: 1f446`
Правило: сочетание `my friend` ведёт себя как одно слово и может стоять перед
формой be там же, где имя.
- `My friend is Kim.` — Мой друг Ким.
- `My friend is Sue.` — Моя подруга Сью.

**Первая половина правила снята по разбору.** Прежде случай начинался словами
«слово `my` ставят перед словом, которое называет вещь или человека» — то же, что
случай 6 урока 3, слово в слово. Новое здесь только второе: `my friend` встаёт
перед формой. Сам оборот `my` ученик разобрал уроком раньше.

**И это единственное место, где разбирается строй `my friend is …`** — он стоит
первой репликой сцены, и прежде его не держал ни один случай.

**Случай 5 (экран 10). `he` и `she` заменяют имя.** `znak: 1f501`
- `Dana is my friend.` — Дана моя подруга.
- `She is Dana.` — Она Дана.

Значок `1f501` закреплён за одной мыслью — «одно вместо другого», — и в модуле он
теперь стоит только здесь.

**Случай 6 (экран 11). `he is` и `she is` сокращают.** `znak: 2702`
Правило: пишут и говорят `he's` и `she's`; буква `i` выпадает, а на её месте
стоит апостроф — тот же знак, что в `I'm`.
- `He's Alim.` — Он Алим.
- `She's Kim.` — Она Ким.

**Случай 7 (экран 12). Одно имя, а формы разные.** `znak: 1f4d0`
Правило: об одном и том же человеке говорят и `I am`, и `He is` — форма меняется
вместе со словом перед ней, а человек тот же.
- `I am Sam.` — Я Сэм.
- `He is Sam.` — Он Сэм.

**Заголовок сменён по разбору:** прежний повторял основание случая 2, а новое у
этого случая своё — тот же `Sam` и две разные формы.

**Таблица тем же экраном**, подпись «Что стоит перед формой»:

```
head: ["Перед формой", "Форма", "Пример", "Перевод"]
rows: ["I",          "am", "I am Nick.",         "Я Ник."]
      ["he",         "is", "He is Bill.",        "Он Билл."]
      ["she",        "is", "She is May.",        "Она Мэй."]
      ["имя",        "is", "Tom is my friend.",  "Том мой друг."]
      ["my friend",  "is", "My friend is Tom.",  "Мой друг Том."]
zvuchat: ["I am Nick.", "He is Bill.", "She is May.", "Tom is my friend.", "My friend is Tom."]
```

Пятая строка дописана по разбору: строй `my friend is …` таблица прежде не
держала. Столбец «Перед формой» переводом не дублируется — он и есть основание
выбора.

**Случай 8 (экран 14). Слово `friend` — и друг, и подруга.** `znak: 1f970`
Правило: по-русски мы выбираем между «друг» и «подруга», а по-английски слово
одно, `friend`; кто это, видно из соседнего слова — `he`, `she` или имени.
- `He is my friend.` — Он мой друг.
- `She is my friend.` — Она моя подруга.

**Слово «только» снято.** Стояло «подсказывает пол только `he` или `she` рядом» —
а случай 3 того же урока переводит `Sue is my friend` как «Сью **моя подруга**»,
и род там выбран по имени.

**Звучащее поимённо:** 1 `["He is Nurlan.", "She is Sue."]`;
2 `["He is Tom.", "She is May."]`; 3 `["Nurlan is my friend.", "Sue is my friend."]`;
4 `["My friend is Kim.", "My friend is Sue."]`; 5 `["Dana is my friend.", "She is Dana."]`;
6 `["He's Alim.", "She's Kim."]`; 7 `["I am Sam.", "He is Sam."]` плюс пять строк
таблицы; 8 `["He is my friend.", "She is my friend."]`.

**Значки:** 1f465, 1f9e9, 1faaa, 1f446, 1f501, 2702, 1f4d0, 1f970 — восемь разных.

### Шесть заданий: `hottext` · `choice` · `match` · `short` · `short` · `speak`

| Экран | № | id | Вид | Что делает ученик |
|---|---|---|---|---|
| 7 | V1 | `v1-pered-formoy` | `hottext` | отмечает слова, при которых стоит `is` |
| 8 | V2 | `v2-gde-oshibka` | `choice` | находит строку с ошибкой среди трёх |
| 13 | V3 | `v3-o-kom-rech` | `match` | соединяет строку с тем, о ком она |
| 15 | V4 | `v4-ispravit-apostrof` | `short` | исправляет чужую запись без апострофа |
| 16 | V5 | `v5-zapisat-s-nulya` | `short` | записывает строку по-английски с нуля |
| 17 | V6 | `v6-o-svoyom-druge` | `speak` | называет своего друга вслух |

**V1.** «Отметь слова, при которых стоит форма `is`.»
Ряд: `he` (верно) · `I` · `she` (верно) · `my friend` (верно).
Отмечаемых четыре, верных три; неверный один — `I`, и он единственный, при
котором стоит `am`. Предмет — основание выбора формы, а не число людей.

**V2 — `choice` перевёрнут по разбору.** Прежде и урок 3, и урок 4 просили
выбрать ВЕРНУЮ запись среди двух ошибок, с одинаковыми отвлекающими. Теперь
наоборот: «В какой строке ошибка?»
Варианты: `She is May.` · `Tom is my friend.` · `He am Nick.` (верно — ошибочная).
Отвлекающие тут — безупречные строки, а ошибка настоящая: `He am` пишет тот, кто
выучил `I am` и перенёс форму на другое слово.
**Мерка 2:** `She is May.` и `Tom is my friend.` напечатаны в уроке (случай 2 и
таблица) — то есть знакомы НЕВЕРНЫЕ по условию варианты, а искомая строка нет.

**V3.** «Соедини строку с тем, о ком она.»
`left`: «He is Bill.» · «I am Sue.» · «She is May.»
`right`: «о себе» · «о женщине» · «о мужчине»
`answer: [2, 0, 1]`
Пункты справа различны, `checkDubli` не сработает. Предмет — связь между словом
перед формой и тем, о ком речь.

**V4.** «Ученик написал: Hes my friend. Запиши строку без ошибки.»
`answer: "He's my friend."` — без `accept`: сверка сама заменяет типографский
апостроф и обрезает знак в конце. `exact` не ставить.
Действие — исправление, а не «запиши коротко»: близнец задания U4 снят сменой
действия, а не слова.

**V5.** «Пат — твоя подруга. Запиши это по-английски.»
`answer: "Pat is my friend."` — без `accept`.
**Мерка 1:** строка в уроке не напечатана — случай 3 даёт `Nurlan`/`Sue`, случай
5 `Dana`, таблица `Tom`.
**Что ещё подходит:** `She is my friend.` не годится — условие называет Пат по
имени.

**Сборки из трёх кусков в уроке нет вовсе, и это решение.** Прежнее задание
собирало `Kim is my friend.` — а те же три куска дают `my friend is Kim`,
безупречную английскую строку, стоящую первой репликой сцены. Я перебрал тогда
две перестановки из шести. **Сборка «имя / is / my friend» даёт зеркало всегда**,
каким бы ни было имя, поэтому вид снят из урока, а не починен именем.

**V6.** `speak`. «Скажи вслух, как зовут твоего друга.»
`phrase: "My friend is Bill."`, `translation: "Мой друг Билл."`

**Лестница:** узнать (V1, V2, V3) → произвести (V4, V5) → применить (V6).
Узнавание три из шести — ровно половина.

---

## УРОК 5. «Как дела» — правила

```
slug: "kak-dela-2"      vid: "pravila"      estimatedMinutes: 11
outcome: "строить ответ на вопрос How are you? и встречный вопрос"
```

**Итог переписан по проверяемой половине:** стояло «отвечать и задавать», а
спрашивают его письменные задания.

**Опора.** CEFR, с. 74, A1: «Can **ask how people are** and react to news».
Оборот `How are you?` берётся целиком; графа, на которой он законен, —
`CLAUSES / interrogatives`, «FORM: AFFIRMATIVE INTERROGATIVE, WITH 'BE'», A1,
примеры самой графы «How are you?» и «Are you ok?».

**И четвёртый готовый оборот объявляется здесь:** `fine` после `be` — это
прилагательное после be, графа `ADJECTIVES / position`, FORM: PREDICATIVE, A1,
которую замысел модуля роздал модулям 9 и 10. Берём формулой, разбора не даём.

### Экраны: девятнадцать

Один блок — один экран.

| Экран | Что стоит |
|---|---|
| 1 | вступление |
| 2 | сцена, `example`, `razgovor: true` |
| 3 | словарь урока: `you`, `fine` |
| 4 | случай 1: как спрашивают о делах |
| 5 | врезка: форма `are` берётся целиком |
| 6 | случай 2: чем отвечают |
| 7 | случай 3: короткий ответ |
| 8 | задание W1 |
| 9 | задание W2 |
| 10 | случай 4: как возвращают вопрос |
| 11 | случай 5: приветствие и вопрос одной репликой |
| 12 | случай 6: ответ и встречный вопрос одной репликой |
| 13 | случай 7: весь обмен + таблица, `naTomZheEkrane` |
| 14 | случай 8: `fine` говорят и о другом человеке |
| 15 | задание W3 |
| 16 | задание W4 |
| 17 | задание W5 |
| 18 | задание W6 |
| 19 | «готово» |

### Сцена (экран 2)

`id: "scena-kak-dela"`: двое знакомых встретились утром.

```
Good morning, Nurlan. How are you?
Good morning, Sue. I am fine, thank you. And you?
I am fine, thank you.
```

Голоса: Сью (ж) — Нурлан (м) — Сью (ж).

### Словарь урока (экран 3)

| Карточка | Чтение | Перевод | Значок |
|---|---|---|---|
| `you` | /juː/ | ты, вы | 1f449 |
| `fine` | /faɪn/ | хорошо (о делах) | 1f642 |

Обоим Oxford 3000 ставит A1, оба в словнике A2 Key. Чтение `fine` — из
замороженного курса, строка 704; **чтения `you` там нет** (прежняя ссылка на
строки 703–704 была неверна: 703 — это `are`), оно берётся из модуля 1, где
/juː/ стоит внутри `thank you`.

Перевод `fine` — «хорошо (о делах)» во всех местах курса; замысел модуля,
раздел 3, правится под это (там стояло «о состоянии»).

Карточка `you` закрывает долг `npm run chain`.

### Врезка (экран 5)

`id: "forma-are-poka-tselikom"`, `tone: "info"`: в строке `How are you?` стоит
**ещё одна форма глагола be**, `are`; здесь строка берётся целиком, а форму
разбирают в модуле «Откуда ты и где живёшь». Счёта форм нет.

### Восемь случаев

**Имена случаев 1 и 4 переписаны по разбору.** `sZvukom` в `Ekran.tsx` ищет ключи
`zvuk` и в первой строке блока — то есть в имени случая. Имя «`How are you?`
значит „как дела"» дало бы кнопку и в заголовке, и у примера: два одинаковых
звонка на одном экране. Теперь имена без английской строки.

**Случай 1 (экран 4). Как спрашивают о делах.** `znak: 2753`
Правило: о делах спрашивают строкой `How are you?`.

**Слова «её говорят целиком и слов в ней не меняют» сняты.** Это запрет об
английском, и графы, которая бы его подпирала, нет: `CLAUSES / interrogatives`
начинается с «Can use» — разрешение. По-английски говорят и `How is your friend?`,
и `How are your parents?`, так что запрет вдобавок неверен. А то, что оборот
берётся целиком и не разбирается, — наша кухня: её место в `sources`, а не на
экране. Второй раз то же самое стояло у случая 4, и оттуда тоже снято.
- `How are you?` — Как дела?
- `Hello, Kim. How are you?` — Здравствуй, Ким. Как дела?

**Случай 2 (экран 6). Чем отвечают.** `znak: 1f44d`
- `I am fine, thank you.` — Спасибо, хорошо.
- `Good morning, Sue. I am fine, thank you.` — Доброе утро, Сью. Спасибо, хорошо.

**Случай 3 (экран 7). Короткий ответ.** `znak: 2702`
Правило: в разговоре `I am` сокращают так же, как при знакомстве.
- `I'm fine.` — У меня всё хорошо.
- `I'm fine, thank you.` — Спасибо, у меня всё хорошо.

**Случай 4 (экран 10). Как возвращают вопрос.** `znak: 21a9`
Правило: ответив, спрашивают собеседника в ответ, и вместо целого вопроса говорят
два слова.
- `And you?` — А у тебя?
- `Thank you. And you?` — Спасибо. А у тебя?

Значок сменён с `1f501`: тот закреплён за «одно вместо другого» и стоит в модуле
только у случая 5 урока 4.

**Случай 5 (экран 11). Приветствие и вопрос одной репликой.** `znak: 1f4ac`
- `Good afternoon, Bill. How are you?` — Добрый день, Билл. Как дела?
- `Hi, May. How are you?` — Привет, Мэй. Как дела?

**Случай 6 (экран 12). Ответ и встречный вопрос одной репликой.** `znak: 1f5e3`
- `I'm fine, thank you. And you?` — Спасибо, у меня всё хорошо. А у тебя?
- `I am fine. And you?` — У меня всё хорошо. А у тебя?

**Случай 7 (экран 13). Весь обмен целиком.** `znak: 1f4d0`
Правило: разговор при встрече идёт в три хода — поздоровались, спросили,
ответили.
- `Good evening, Nick. How are you?` — Добрый вечер, Ник. Как дела?
- `I'm fine. And you?` — У меня всё хорошо. А у тебя?

**Примеры случая и строки таблицы разведены по разбору:** прежде они совпадали
дословно, и ученик видел одну пару дважды на одном экране. И у самого случая
теперь есть `zvuchat` — прежде перечень отсылал к «трём строкам таблицы», а обе
строки блока оставались без кнопки.

**Таблица тем же экраном**, подпись «Три хода разговора»:

```
head: ["Ход", "Английский", "Перевод"]
rows: ["здороваются и спрашивают",     "Good evening, Tom. How are you?", "Добрый вечер, Том. Как дела?"]
      ["отвечают и спрашивают в ответ","I am fine, thank you. And you?",  "Спасибо, хорошо. А у тебя?"]
      ["отвечают",                     "I'm fine, thank you.",            "Спасибо, у меня всё хорошо."]
zvuchat: три строки второго столбца
```

**Случай 8 (экран 14). `fine` говорят и о другом человеке.** `znak: 1f465`
Правило: слово `fine` годится и о себе, и о друге; форму be выбирает слово,
стоящее перед ней.
- `He is fine.` — У него всё хорошо.
- `She is fine.` — У неё всё хорошо.

**Слова «меняется только форма» сняты по разбору:** в двух примерах форма как раз
не меняется — в обоих `is`, — и ученик увидел бы обратное сказанному.

**Значки:** 2753, 1f44d, 2702, 21a9, 1f4ac, 1f5e3, 1f4d0, 1f465.

### Шесть заданий: `choice` со звуком · `hottext` · `order` · `short` · `short` · `speak`

| Экран | № | id | Вид | Что делает ученик |
|---|---|---|---|---|
| 8 | W1 | `w1-chto-otvetit` | `choice` со `zvuk` | слышит реплику и выбирает ответ |
| 9 | W2 | `w2-vernut-vopros` | `hottext` | отмечает реплику, которой возвращают вопрос |
| 15 | W3 | `w3-sobrat-repliku` | `order` | собирает ответную реплику |
| 16 | W4 | `w4-ispravit-otvet` | `short` | исправляет чужую запись |
| 17 | W5 | `w5-zapisat-vopros` | `short` | записывает вопрос о делах |
| 18 | W6 | `w6-razygrat-vstrechu` | `speak` | разыгрывает встречу вслух |

**W1 — задание на слух, и это правка по делу урока.** Предмет урока разговорный,
а прежняя редакция проверяла его одним письмом.
`zvuk: "How are you?"`
«Послушай реплику. Что на неё ответить?»
Варианты: `I am fine, thank you.` (верно) · `How are you?` · `Thank you.`
Отвлекающие настоящие: `How are you?` — эхо, ошибка того, кто не разобрал, кто
кого спрашивает; `Thank you.` — благодарность вместо ответа.
**Мерка 2:** все три строки напечатаны в уроке или в модуле 1, знакомы одинаково.

**W2.** «В ответной реплике отметь то, чем возвращают вопрос собеседнику.»
`parts`: `I am fine` (sel) · `, ` · `thank you` (sel) · `. ` · `And you?` (sel,
верно) — отмечаемых три, верный один.
**Близнец снят:** прежде `hottext` уроков 4 и 5 несли одно условие слово в слово
(«В двух строках отметь форму глагола be»); теперь предмет другой.

**W3.** «Собери ответную реплику: скажи, что у тебя всё хорошо, поблагодари и
спроси собеседника.»
`items: ["And you?", "I am fine,", "thank you."]`, `answer: [1, 2, 0]`
→ `I am fine, thank you. And you?`
**Все шесть перестановок перебраны:** `And you?` впереди даёт реплику, где
спрашивают раньше, чем отвечают, а условие говорит «ответную»; `thank you.` перед
`I am fine,` даёт «Спасибо, у меня всё хорошо» без запятой на месте — по-английски
так строку не пишут. Второго верного порядка нет.
**Мерка 1:** строка в уроке не напечатана — случай 6 даёт `I'm fine, thank you.
And you?` и `I am fine. And you?`, таблица — их же.

**W4.** «Ученик ответил: I fine. Запиши строку без ошибки.»
`answer: "I am fine."`, `accept: ["I'm fine."]` — **и это единственный живой
`accept` во всём модуле**: короткая запись верна, урок ей учит случаем 3, и не
принять её значило бы наказать за верное. Остальные шесть полей `accept` прежней
редакции сняты: сверка сама снимает регистр, знак в конце и типографский апостроф.

**W5.** «Ты поздоровался и хочешь спросить собеседника о делах. Запиши вопрос.»
`answer: "How are you?"`
**Мерка 1:** строка напечатана в случае 1 — и это законно: оборот берётся целиком
и заучивается наизусть, а напечатанное для заучивания утечкой не является. Другой
строкой о делах в модуле не спрашивают вовсе.

**W6.** `speak`. «Разыграй встречу вслух: поздоровайся днём, спроси о делах и
ответь за собеседника.»
`phrase: "Good afternoon, Nick. How are you? I am fine, thank you."`

**Лестница:** узнать (W1, W2) → произвести (W3, W4, W5) → применить (W6).

---

## УРОК 6. «До свидания» — правила, последний в серии

```
slug: "do-svidaniya-2"      vid: "pravila"      estimatedMinutes: 12
outcome: "выбирать прощание по случаю и отличать Good night от Good evening"
```

**Итог переписан по проверяемой половине:** «прощаться» обещало речь.

**Опора появилась там, где её не было.** Пара `Good night` / `Good evening`
опоры в `materials/` не имеет — это подтвердили оба методиста. Теперь она есть:
выписка из Викисловаря в `docs/istochniki-norma-yazyka.md`, пункт 12 —
`good night` «A farewell said in the evening or before going to sleep»,
`good evening` «Used as a greeting in the evening». **Долг снят**, и `sources`
модуля ссылается на выписку, а не на решение преподавателя.

**Карточка `see you` снята.** Прежняя редакция писала, что она стоит в словнике
A2 Key и что Oxford даёт ей A1, — **неверно по обоим**: в словнике записи `see
you` нет (есть `see (v)`, строка 1807), в Oxford 3000 нет (есть `see v. A1`,
строка 2367), `npm run slovo` отвечает «не нашлось» по обоим. Опоры ступени у
оборота нет, и карточка убрана; прощаний остаётся три. Вопрос владельцу —
[voprosy-vladeltsu.md](voprosy-vladeltsu.md), пункт 6.

### Экраны: семнадцать

| Экран | Что стоит |
|---|---|
| 1 | вступление |
| 2 | сцена, `example`, `razgovor: true` |
| 3 | словарь урока: `goodbye`, `bye`, `good night` |
| 4 | случай 1: `Goodbye` |
| 5 | случай 2: `Bye` короче |
| 6 | случай 3: `Good night` — прощание |
| 7 | задание X1 |
| 8 | задание X2 |
| 9 | случай 4: `Good night` против `Good evening` |
| 10 | случай 5: имя после прощания |
| 11 | случай 6: свод встреч и прощаний + таблица, `naTomZheEkrane` |
| 12 | задание X3 |
| 13 | задание X4 |
| 14 | случай 7: короткая запись всех трёх форм |
| 15 | таблица свода |
| 16 | задание X5 |
| 17 | «готово» |

**Случаев семь, а не восемь, и примеров четырнадцать, а не шестнадцать.** Со
снятием карточки `see you` случай о нём отпал. Мерка формата не выполнена по
обоим счётам, и урок объявляет это прямо — как модуль 1 объявлял исключение для
алфавита. Восполнять недостачу выдуманным случаем нельзя: прощаний в модуле три,
и восьмой случай пришлось бы сочинить из ничего.

### Сцена (экран 2)

`id: "scena-konets-dnya"`: вечер, занятия кончились.

```
Good evening, Nurlan.
Good evening, Sue.
Goodbye, Nurlan.
Good night, Sue.
```

Голоса: Сью (ж) — Нурлан (м) — Сью (ж) — Нурлан (м). Сцена ставит встречу и
прощание рядом, и различие видно раньше правила.

### Словарь урока (экран 3)

| Карточка | Чтение | Перевод | Значок |
|---|---|---|---|
| `goodbye` | /gʊdˈbaɪ/ | до свидания | 1f6b6 |
| `bye` | /baɪ/ | пока | значка нет: то же прощание, короче |
| `good night` | /gʊd ˈnaɪt/ | спокойной ночи | 1f634 |

Все три — записи словника A2 Key (`goodbye` 959, `bye` 391, `good night` 963);
`goodbye` и `bye` Oxford 3000 даёт A1 (строки 1191 и 371).

**`Good night` уже напечатано в модуле 1** — карточка `night` (`alfavit.ts`,
строка 2439) идёт с примером `Good night.` Ступени это не меняет, но первое место
формулы в курсе — там, и урок 6 даёт ей карточку, а не первое знакомство.

Значок `1f44b` прощанию не достаётся: он закреплён за `Hello`. `1f319` не
достаётся `good night`: он стоит у карточки `evening` модуля 1, а весь урок о
том, что путать их нельзя.

### Семь случаев

**Случай 1 (экран 4). Прощаются словом `Goodbye`.** `znak: 1f6b6`
- `Goodbye.` — До свидания.
- `Goodbye, Kim.` — До свидания, Ким.

**Случай 2 (экран 5). `Bye` — то же самое, короче.** `znak: 1f4cf`
- `Bye.` — Пока.
- `Bye, Sue.` — Пока, Сью.

**Значок сменён с ножниц по разбору.** Ножницы `2702` стоят в модуле у `I'm`,
`he's` и `I'm fine` — везде о короткой ЗАПИСИ с апострофом. У `Bye` апострофа
нет: это другое слово, а не сокращение первого, и ученик прочёл бы знак как
«здесь тоже апостроф». Линейка `1f4cf` говорит о длине и в курсе свободна.
Ножницы остаются ровно за короткой записью.

**Случай 3 (экран 6). `Good night` говорят на прощание.** `znak: 1f634`
Правило: `Good night` говорят, расставаясь вечером или уходя спать; по-русски ему
отвечает «спокойной ночи», и оно тоже прощание.
- `Good night.` — Спокойной ночи.
- `Good night, Dana.` — Спокойной ночи, Дана.

**Случай 4 (экран 9). `Good night` и `Good evening` начинаются одинаково.**
`znak: 26a0`
Правило: `Good evening` говорят при встрече вечером, `Good night` — расставаясь.
- `Good evening, Pat.` — Добрый вечер, Пат.
- `Good night, Pat.` — Спокойной ночи, Пат.

Опора — выписка в `docs/istochniki-norma-yazyka.md`, пункт 12. Слов «только» и
«никогда» в правиле нет: оно говорит, что есть, а не запрещает обратное.

**Случай 5 (экран 10). Имя после прощания.** `znak: 1f4dd`
- `Goodbye, Nurlan.` — До свидания, Нурлан.
- `Bye, Pat.` — Пока, Пат.

**Случай 6 (экран 11). Чем здороваются и чем прощаются.** `znak: 1f4d0`
- `Hi, May.` — Привет, Мэй.
- `Goodbye, May.` — До свидания, Мэй.

**Таблица тем же экраном**, подпись «При встрече и на прощание»:

```
head: ["Строка", "Когда говорят", "Перевод"]
rows: ["Good morning", "при встрече", "Доброе утро"]
      ["Good evening", "при встрече", "Добрый вечер"]
      ["Hello",        "при встрече", "Здравствуйте"]
      ["Good night",   "на прощание", "Спокойной ночи"]
      ["Goodbye",      "на прощание", "До свидания"]
      ["Bye",          "на прощание", "Пока"]
zvuchat: шесть строк первого столбца
```

**Столбец «Когда говорят» здесь законен**, в отличие от снятого столбца «Когда» в
уроке 2: там он повторял перевод, здесь говорит то, чего перевод не говорит вовсе
— «Спокойной ночи» по-русски не подсказывает, что это прощание.

**Случай 7 (экран 15). Короткая запись всех трёх форм.** `znak: 2702`
Правило: короткую запись модуль давал по одной — `I'm` при знакомстве, `he's` и
`she's` о другом человеке, `I'm fine` в ответе о делах; здесь они собраны вместе.
- `I am Sue.` — Я Сью.
- `I'm Sue.` — Я Сью.

**Свод переписан целиком по разбору, и это была настоящая беда.** Прежний свод
повторял таблицу урока 4: те же пять строк, те же четыре столбца, тот же порядок,
сменены только имена. Замена слова близнеца не снимает.

Новый свод собирает то, чего в модуле не собрано нигде: **короткую запись**. Она
разбросана по трём урокам и четырём случаям, а рядом её ученик не видел ни разу.

**Таблица свода (экран 16)**, отдельным экраном, подпись «Короткая запись»:

```
head: ["Перед формой", "Полная запись", "Короткая запись", "Перевод"]
rows: ["I",   "I am Sue.",    "I'm Sue.",    "Я Сью."]
      ["he",  "He is Alim.",  "He's Alim.",  "Он Алим."]
      ["she", "She is Dana.", "She's Dana.", "Она Дана."]
zvuchat: шесть строк второго и третьего столбцов
```

**Строк три, а не пять, и это решение.** Короткой записи после имени
(`Bill's my friend`) модуль не давал — она есть в английском, но её разбирает
модуль 6 программы. Поставить строку с пустой ячейкой значило бы сказать
«короткой записи нет», то есть выдумать запрет; молчание тут честнее.

**Свод задания не получает нарочно:** короткая запись уже спрошена заданиями U4 и
V4, и итога на себе свод не несёт — как `speak`.

**Значки:** 1f6b6, 1f4cf, 1f634, 26a0, 1f4dd, 1f4d0, 2702 — семь разных.

### Пять заданий: `hottext` · `choice` · `short` · `short` · `speak`

| Экран | № | id | Вид | Что делает ученик |
|---|---|---|---|---|
| 7 | X1 | `x1-otmetit-proshchaniya` | `hottext` | отмечает строки, которыми прощаются |
| 8 | X2 | `x2-uhodish-vecherom` | `choice` | выбирает, что сказать, уходя вечером |
| 12 | X3 | `x3-ispravit-vstrechu` | `short` | исправляет чужую запись |
| 13 | X4 | `x4-proshchanie-s-imenem` | `short` | записывает длинное прощание с именем |
| 16 | X5 | `x5-poproshchatsya-vsluh` | `speak` | прощается вслух |

**Прежний `gap` снят по разбору, и это не потеря.** Он стоял на рамке
`before: "Good "`, `after: ", Kim."` — той же, что задание T3 принятого урока 2,
вплоть до имени собеседника. На эту рамку в модуле приходилось **пять мест**,
включая заглушки работы. Рамка остаётся ровно за уроком 2; урок 6 идёт с пятью
заданиями, узнавание два из пяти.

**X1.** «Отметь строки, которыми прощаются.»
Ряд: `Goodbye` (верно) · `Good evening` · `Good night` (верно) · `Hello`.
**Что ещё подходит:** `Bye` тоже прощание, и в ряду его нет — иначе верных было бы
три.

**X2.** «Вечер, ты уходишь домой. Что сказать?»
Варианты: `Good night.` (верно) · `Good evening.` · `Good morning.`
**Что ещё подходит:** `Goodbye.` и `Bye.` уходя вечером тоже верны, и потому в
вариантах их нет.

**X3.** «Ученик встретил Мэй вечером и сказал ей `Good night`. Запиши, что надо
было сказать: поздоровайся приветствием, которое называет время дня, и назови её
по имени.»
`answer: "Good evening, May."` — без `accept`.
**Слова «приветствием, которое называет время дня» обязательны:** без них
`Hello, May.` и `Hi, May.` — верные ответы, и задание наказывало бы за верное.
Ровно это чинил принятый урок 2 у задания T4.
**Мерка 1:** строка в уроке не напечатана.

**X4.** «Ты прощаешься с Сэмом. Запиши длинное прощание и назови его по имени.»
`answer: "Goodbye, Sam."` — без `accept`.
**Что ещё подходит:** слово «длинное» отделяет `Goodbye` от `Bye`, а `Good night`
не годится — оно о позднем вечере, и условие времени не называет.
**Мерка 1:** строка в уроке не напечатана.

**X5.** `speak`. «Скажи вслух, как ты попрощаешься с другом поздно вечером.»
`phrase: "Good night, Bill."`, `translation: "Спокойной ночи, Билл."`

**Лестница:** узнать (X1, X2) → произвести (X3, X4) → применить (X5).

## УРОК 7. «Читаем переписку» — чтение

```
slug: "chitaem-perepisku-2"      vid: "chtenie"      estimatedMinutes: 10
outcome: "понимать по короткому сообщению, кто здоровается и как у него дела"
```

**Опора.** CEFR, с. 55, Reading correspondence, **A1**: «Can understand short,
simple messages sent via social media or e-mail (e.g. proposing what to do, when
and where to meet)». Наше сообщение проще примера графы.

**Урок разведён с уроком 9 модуля 1:** там ученик ищет имя в списке и сверяет
запись буква за буквой, здесь читает связные реплики.

### Экраны: одиннадцать

| Экран | Что стоит |
|---|---|
| 1 | вступление |
| 2 | сообщение 1, `kind: "text"`, `genre: "message"` |
| 3 | перевод сообщения 1, `note`, `tone: "info"` |
| 4 | сообщение 2, тот же вид |
| 5 | перевод сообщения 2, `note`, `tone: "info"` |
| 6 | задание Y1 |
| 7 | задание Y2 |
| 8 | задание Y3 |
| 9 | случай: сообщение начинают приветствием, кончают прощанием и своим именем |
| 10 | задание Y4 |
| 11 | «готово» |

### Два блока текста вместо одного (правка по разбору)

Прежняя редакция ставила два сообщения в один блок и разделяла их **пустой
строкой** — а `check:content` на этом валится: `if (block.body.some(blank))
fail(where, "пустой абзац в тексте")`. Сборка не прошла бы вовсе.

**Сообщение 1** (`id: "soobshchenie-sue"`, `title` называет вещь):

```
body: [
  "Hello, Kim!",
  "How are you? I am fine, thank you.",
  "My friend is Nurlan. He is fine.",
  "Goodbye!",
  "Sue",
]
```

Девятнадцать слов при пороге пятнадцати.

**Сообщение 2** (`id: "soobshchenie-kim"`):

```
body: [
  "Hi, Sue!",
  "I am fine, thank you. And you?",
  "My friend is May. She is fine.",
  "Goodbye!",
  "Kim",
]
```

Девятнадцать слов. **Строка `Hello, Nurlan!` снята по разбору:** она стояла в
середине письма к Сью, приветствие было обращено к третьему лицу, а правило урока
такому месту приветствия не отводит. Взамен Ким пишет о своём друге — тем же
строем, что Сью, и переписка читается связно с первого захода.

**Ни одного слова вне модуля.** Проверил построчно: всё разобрано уроками 2–6,
имена из графы Names.

**Перевод стоит ПОД КАЖДЫМ сообщением, а не в разборе задания — правка по
разбору.** Прежняя редакция прятала его в разбор последнего задания: ученик,
ответивший сразу, перевод получал, а не дошедший до Y4 не получал вовсе. Место
было худшим из возможных, и это моя ошибка.

**Как это сделано в наличных средствах.** У блока `text` полей перевода нет ни
одного: есть `title`, `genre`, `body`, `glossary`, а `glossary` даёт пару
«слово — перевод», не перевод строки. Поэтому под каждым сообщением стоит блок
`note`, `tone: "info"`, чей заголовок прямо говорит «Перевод», а тело даёт
русские строки в том же порядке. Это видно всем и стоит на месте.

Долг остаётся и записан вопросом сборщику
([voprosy-vladeltsu.md](voprosy-vladeltsu.md), пункт 8): когда у `text` появится
поле перевода, обе врезки уйдут.

### Четыре задания

| Экран | № | id | Вид | Что делает ученик |
|---|---|---|---|---|
| 6 | Y1 | `y1-kto-napisal` | `choice`, `about: "soobshchenie-sue"` | выбирает, кто написал |
| 7 | Y2 | `y2-kak-dela-u-nurlana` | `short`, `about: "soobshchenie-sue"` | отвечает одним словом |
| 8 | Y3 | `y3-otmetit-proshchaniya` | `hottext`, `about: "soobshchenie-kim"` | отмечает прощание |
| 10 | Y4 | `y4-nayti-vopros` | `short`, `about: "soobshchenie-kim"` | выписывает встречный вопрос |

**Y1.** «Кто написал это сообщение?» Варианты `Sue` (верно) · `Kim` · `Nurlan`.
Имя пишущего стоит последней строкой, а имя того, кому пишут, — первой; ученик,
читающий только начало, ошибётся. Это настоящая ловушка жанра.

**Y2.** «Как дела у Нурлана? Ответь одним английским словом.» `answer: "fine"`.
Рамка A2 Key, с. 29. О Нурлане в сообщении одна строка — второго ответа нет.

**Y3.** «Отметь строку, которой прощаются.» `parts` повторяют строки сообщения 2;
верная одна — `Goodbye!` Повтор строк текста в `hottext` законен по устройству
вида: ученик отмечает МЕСТА.

**Y4.** «Найди во втором сообщении встречный вопрос и запиши его целиком.»
`answer: "And you?"` — без `accept`.
Опора действия: CEFR, с. 136, Orthographic control, A1 — «Can **copy** familiar
words and short phrases». Списывание найденного и есть задание.

### Случай (экран 9)

`id: "sluchay-nachalo-i-konets"`, `znak: 1f4e8`. Правило: сообщение начинают
приветствием, а кончают прощанием и своим именем — по-русски записку пишут так же.
- `Hello, Bill!` — Здравствуй, Билл!
- `Goodbye! Sue` — До свидания! Сью

**Врезка «имя пишущего стоит в конце» СНЯТА по разбору.** Ту же мысль в уроке
говорили три места подряд: разбор задания Y1, случай на экране 9 и она сама.
Осталось два, и у каждого своя работа: разбор Y1 объясняет ошибку, случай
называет строение сообщения целиком.

---

## УРОК 8. «Знакомимся вслух» — говорение

```
slug: "znakomimsya-vsluh-2"      vid: "govorenie"      estimatedMinutes: 10
outcome: "собирать знакомство из готовых реплик по порядку"
```

Итог назван по проверяемой половине: устную речь машина не оценивает.

**Опора устройства** — A2 Key, с. 42, Speaking Part 1; **цитата берётся только до
первой точки**: дальше в той же строке стоят `and this is …` и `What's your
name?`, обе конструкции A2. Спрашивать имя ученик умеет строкой модуля 1 —
`Your name, please?` Опора задания `speak` — CEFR, с. 135, Sound articulation, A1.

### Экраны: девять

| Экран | Что стоит |
|---|---|
| 1 | вступление |
| 2 | образец разговора, `example`, `razgovor: true` |
| 3 | случай 1: пять ходов знакомства + таблица, `naTomZheEkrane` |
| 4 | задание Z1 |
| 5 | случай 2: приветствие выбирают по времени дня |
| 6 | задание Z2 |
| 7 | врезка: если не расслышал имя |
| 8 | задание Z3 |
| 9 | «готово» |

### Образец (экран 2)

`id: "obrazets-znakomstva"`: двое знакомятся днём.

```
Good afternoon. My name is Sue.
Good afternoon, Sue. I am Nick. How are you?
I am fine, thank you.
Goodbye, Sue.
```

Голоса: Сью (ж) — Ник (м) — Сью (ж) — Ник (м).

**Строка `And you?` снята из образца, и это лучший из ответов на вопрос о её
переводе — его дал второй редактор.** Я спорил, как перевести встречный вопрос
при знакомстве, а строка была **лишней по собственному правилу урока**: правило и
таблица называют пять ходов, встречного вопроса среди них нет, а образец давал
его четвёртой репликой. Снятие убирает разом три вещи — спорный перевод,
расхождение образца с правилом и лишний ход. Встречный вопрос остаётся уроку 5,
где собеседники знакомы и обмен репликами есть предмет урока.

Прощание сменено с `See you` на `Goodbye` вместе со снятием карточки `see you`.

### Случай 1 (экран 3). Пять ходов знакомства. `znak: 1f4d0`

**Ход «отвечают» дописан по разбору.** Прежде правило называло четыре хода, а
задание Z1 стояло на пятом, которого в правиле не было вовсе, — и наоборот, два
названных хода в задании не участвовали.

Правило: знакомство идёт по порядку — здороваются, называют себя, спрашивают о
делах, отвечают, прощаются.

- `Good morning. I am Tom.` — Доброе утро. Я Том.
- `Goodbye, Tom.` — До свидания, Том.

**Таблица тем же экраном**, подпись «Порядок ходов»:

```
head: ["Ход", "Пример", "Перевод"]
rows: ["здороваются",        "Good afternoon.",      "Добрый день."]
      ["называют себя",      "My name is Sue.",      "Меня зовут Сью."]
      ["спрашивают о делах", "How are you?",         "Как дела?"]
      ["отвечают",           "I am fine, thank you.","Спасибо, хорошо."]
      ["прощаются",          "Goodbye.",             "До свидания."]
zvuchat: пять строк второго столбца
```

### Случай 2 (экран 5). Приветствие выбирают по времени дня. `znak: 23f0`

**Слово «только» снято по разбору.** Стояло «меняется только приветствие; имя,
вопрос о делах и прощание остаются теми же» — а урок 3 учит называть себя двумя
способами, урок 6 даёт три прощания, и образец сам кончается иначе, чем пример
случая 1.

Правило: время дня меняет приветствие, а на остальные ходы знакомства оно не
влияет.
- `Good morning. My name is Tom.` — Доброе утро. Меня зовут Том.
- `Good evening. My name is Tom.` — Добрый вечер. Меня зовут Том.

### Врезка (экран 7)

`id: "esli-ne-rasslyshal"`, `tone: "info"`: если имя не расслышал, переспрашивают
строкой `Sorry, how do you spell that?` — она разобрана в модуле «Алфавит и своё
имя», уроке «Спросить, как пишется».

### Три задания

| Экран | № | id | Вид | Что делает ученик |
|---|---|---|---|---|
| 4 | Z1 | `z1-sobrat-znakomstvo` | `order` | собирает три хода по порядку |
| 6 | Z2 | `z2-chto-dalshe` | `choice` | выбирает, что сказать следующим |
| 8 | Z3 | `z3-znakomstvo-vsluh` | `speak` | разыгрывает знакомство вслух |

**Z1 — проверяемая половина итога.** «Собери знакомство: расставь три реплики в
том порядке, который называет правило.»
`items: ["Goodbye, Bill.", "I am Sue.", "Good morning, Bill."]`, `answer: [2,1,0]`
**Все шесть перестановок перебраны.** Правило называет порядок прямо
(здороваются → называют себя → прощаются), и условие на него ссылается; ни одна
другая перестановка этому порядку не отвечает.
**Мерка 1:** ни одна из трёх строк в уроке не напечатана — образец даёт
`My name is Sue.` и `Goodbye, Sue.`, случай 1 `Good morning. I am Tom.` и
`Goodbye, Tom.`
**Все три хода названы правилом** — прежде задание стояло на ходе «отвечают»,
которого правило не знало.

**Z2.** «Собеседник сказал `Good evening. My name is Kim.` Что сказать следующим?»
Варианты: `Good evening, Kim. I am Sam.` (верно) · `Goodbye, Kim.` ·
`I am fine, thank you.`
Отвлекающие настоящие: прощание в начале разговора и ответ на незаданный вопрос.
**Мерка 2 — поправка к прежнему прогону.** Прежде было сказано «ни одна из трёх
строк в уроке не напечатана»; это неверно: `I am fine, thank you.` стоит в
образце дважды. Само по себе это не беда — знаком ОТВЛЕКАЮЩИЙ, а не верный, и
узнавание ведёт к ошибке. Но отчёт мерки, которому нельзя верить, хуже
отсутствующего, и прогон здесь был неверен.

**Z3.** `speak`. «Разыграй знакомство вслух: поздоровайся, назови себя, спроси о
делах и попрощайся.»
`phrase: "Good afternoon. I am Alim. How are you? Goodbye."`

---

## Общий прогон мерок

**Мерка 3 дополнена по разбору.** К словам «всегда», «только», «нельзя»
прибавлены **«обязательно», «везде», «никогда», «любой»** — прежний список их не
ловил, и три выдуманных запрета прошли мимо.

| Мерка | Итог прогона |
|---|---|
| 1. Ответ не совпадает с примером или репликой сцены | Прогнана заново по всем 29 заданиям. Совпадений четыре, все законные и названные по месту: `Your name, please?` (U1), `am` (U2, U3), `How are you?` (W5) — готовые обороты и закрытые наборы. Прежние совпадения сняты сменой имён |
| 2. Верный вариант не единственный знакомый | Прогнана по семи `choice`. В I1, W1, X1, X2 знакомы все варианты; в V2 знакомы НЕВЕРНЫЕ по условию, а искомая ошибочная строка нет; в Y1 и Z2 знаком отвлекающий. Ни одного задания, где знаком только верный |
| 3. Абсолютные слова | Три правила переписаны (урок 3 случай 1, урок 4 случай 8, урок 8 случай 2). Осталось одно «везде» — в правиле о заглавной `I`, и оно правда: это свойство английского письма |
| 4. Хвост итога | Прогнана по шести итогам. Четыре переписаны по проверяемой половине; общего хвоста нет ни у одного |
| 5. Звучащее поимённо | Названо у каждого случая, у всех таблиц и у обоих блоков `audio`. Случай 7 урока 5 получил `zvuchat`, которого у него не было |
| 6. Значки | `1f501` закреплён за одной мыслью и стоит в модуле один раз; `2702` — только за короткой записью с апострофом, `Bye` взял `1f4cf`; `21a9` и `23f0` взяты взамен; `1f44b`, `1f319`, `1f6aa` не берутся — заняты в модуле 1 |
| 7. Производство не раньше узнавания | Прогнана заново. В уроке 1 узнавание одно из трёх, в уроке 4 три из шести, в уроке 6 два из пяти. Везде первые задания — узнавание |
| 8. Имя главной вещи одно | «Форма глагола be». Счёта форм («вторая», «третья») нет нигде |

**Счёт по модулю.** Семь уроков: слушание, четыре правил, чтение, говорение.
Случаев в уроках правил восемь, восемь, восемь и семь; примеров шестнадцать,
шестнадцать, шестнадцать и четырнадцать (урок 6 объявляет недостачу с причиной). Заданий 4 + 6 + 6 + 6 + 6 + 4 + 3 = 35. Карточек
тринадцать: четыре в уроке 1, две в уроке 3, три в уроке 4, две в уроке 5, три в
уроке 6 (`see you` снята).

## Что проверено и чего проверить не удалось

**Проверено самим при этой правке:** `lib/content/check.ts`
(`normalizeKeepingCase` — отсюда снятие шести полей `accept`);
`scripts/check-content.mts`, ветка `case "text"` (пустая строка в `body` валит
сборку); `components/lesson/UrokShagami.tsx` (один блок — один экран, отсюда
пересчёт экранов); `scripts/ozvuchka.mts` (у `audio` `pervyyGolos` не читается —
отсюда по два человека в записи); Oxford 3000 и словник A2 Key по `see you`, `am`,
`be` — обе прежние опоры оказались неверны; `courses/english-elementary/kak-dolgo.ts`
строка 240 и `ekzamen.ts` строка 707 — «третья форма» в прежнем значении;
`alfavit.ts` строка 2439 — карточка `night` с примером `Good night.`

**Чего проверить не удалось:**

1. **Страницы картинкой не смотрел** — `pdftoppm` в среде нет.
2. **Звук не слушал ни одной записи.** Всё о голосах выведено из кода. Обе записи
   урока 1, все сцены и образец урока 8 надо будет послушать: «файл есть» и
   «звучит верно» — разные утверждения, и на этом проект обжигался трижды.
3. **Значки глазами не смотрел** — ни `21a9`, ни `23f0`, ни `1f4e8`, ни `1f50d`,
   ни прежние. Знаю только, чем они заняты в файлах курса.
4. **Работа модуля написана 7 сентября 2026 — раздел 9 ниже.** Двенадцать
   вопросов, каждый итог спрошен, ни одна рамка заданий уроков не повторена.
   Прежняя запись здесь говорила, что работы нет, и устарела.
5. **Естественность строк** источниками не подтверждается и подтверждена быть не
   может — пометок «где как говорят» в `materials/` нет вовсе.
6. **Мерку 1 по модулю 1 я прогонял скриптом, а здесь глазами** — по замыслу,
   где строки лежат в двух документах. Когда уроки будут собраны, прогон надо
   повторить машиной.

---

## 9. РАБОТА МОДУЛЯ — ДВЕНАДЦАТЬ ВОПРОСОВ

**Раздел приписан 7 сентября 2026 методистом работы.** В коде
(`privetstvie.ts`, строки 2793–2881) стоят ВОСЕМЬ ЗАГЛУШЕК с произвольными
ответами, и шапка файла прямо пишет, что работы у модуля нет. Ниже — настоящий
состав; заглушки снимаются целиком, а не правятся.

### 9.1. Как эта работа писалась — порядок обратный

Правило проекта: работу пишут ДО заданий уроков или отдельно от них, с другого
угла. Задания восьми уроков к этому дню написаны и приняты, и взять их за
образец было бы легче всего — так курс набрал когда-то пятьдесят восемь
близнецов в десяти модулях. Поэтому порядок был такой:

1. выписаны РАМКИ всех заданий восьми уроков (таблица 9.2) — не виды, а то, что
   ученик ДЕЛАЕТ;
2. на каждый итог выбран вид, которого у этого итога в его уроке НЕТ;
3. на каждый вопрос выбрано действие, которого нет НИ В ОДНОЙ строке таблицы
   9.2.

**Почему рамки, а не виды.** У `gap` и `short` рамку задаёт сам вид задания
(«впиши слово», «запиши строку»), и `npm run bliznetsy` там молчит по
устройству — правило приписано 6 сентября 2026 на заглушке этого самого модуля.
Значит близнеца ищут глазами, а глазам нужна опись.

### 9.2. Занятые рамки: что ученик уже делал в уроках

| Что ученик ДЕЛАЕТ | Где стоит |
|---|---|
| выбрать услышанное приветствие среди трёх | урок 1, `i1-chem-otvetili` |
| записать приветствие, звучащее в записи | урок 1, `i2-zapisat-vecherneye` |
| записать приветствие, с которого запись начинается | урок 1, `i3-zapisat-dnevnoye` |
| выбрать приветствие по названному времени дня | урок 2, `g1-vremya-dnya` |
| отметить в строке слово, меняющееся со временем дня | урок 2, `g2-chto-menyaetsya` |
| записать приветствие, годное в любое время | урок 2, `g5-lyuboe-vremya` |
| вписать слово в ответное приветствие | урок 2, `g3-otvetit-tem-zhe` |
| записать приветствие с именем целой строкой | урок 2, `g4-zapisat-privetstvie` |
| произнести три приветствия вслух | урок 2, `g6-skazat-vsluh` |
| соединить русскую строку с английской | урок 3, `u1-russkaya-i-angliyskaya` |
| отметить слово, которого нет в русской фразе | урок 3, `u2-lishnee-slovo` |
| вписать форму be при слове I | урок 3, `u3-vpisat-formu` |
| записать строку короткой записью | урок 3, `u4-korotkaya-zapis` |
| записать со слуха строку о себе | урок 3, `u5-diktant-o-sebe` |
| назвать себя вслух двумя способами | урок 3, `u6-skazat-o-sebe` |
| отметить слова, при которых стоит is | урок 4, `v1-pered-formoy` |
| выбрать строку с ошибкой среди трёх | урок 4, `v2-gde-oshibka` |
| соединить строку и того, о ком она | урок 4, `v3-o-kom-rech` |
| исправить короткую запись без апострофа | урок 4, `v4-ispravit-apostrof` |
| записать с нуля строку о своей подруге | урок 4, `v5-zapisat-s-nulya` |
| сказать вслух, как зовут друга | урок 4, `v6-o-svoyom-druge` |
| выбрать ответ на услышанный вопрос о делах | урок 5, `w1-chto-otvetit` |
| отметить в реплике встречный вопрос | урок 5, `w2-vernut-vopros` |
| собрать ответную реплику из трёх кусков | урок 5, `w3-sobrat-repliku` |
| исправить ответ, где потеряна форма be | урок 5, `w4-ispravit-otvet` |
| записать вопрос о делах | урок 5, `w5-zapisat-vopros` |
| разыграть встречу вслух | урок 5, `w6-razygrat-vstrechu` |
| отметить строки, которыми прощаются | урок 6, `x1-otmetit-proshchaniya` |
| выбрать, что сказать, уходя вечером | урок 6, `x2-uhodish-vecherom` |
| исправить приветствие, которым простились | урок 6, `x3-ispravit-vstrechu` |
| записать длинное прощание с именем | урок 6, `x4-proshchanie-s-imenem` |
| попрощаться вслух | урок 6, `x5-poproshchatsya-vsluh` |
| выбрать, кто написал сообщение | урок 7, `y1-kto-napisal` |
| ответить по сообщению одним английским словом | урок 7, `y2-kak-dela-u-nurlana` |
| отметить в сообщении строку прощания | урок 7, `y3-otmetit-proshchaniya` |
| найти в сообщении встречный вопрос и записать его | урок 7, `y4-nayti-vopros` |
| собрать знакомство из трёх реплик | урок 8, `z1-sobrat-znakomstvo` |
| выбрать, что сказать следующим | урок 8, `z2-chto-dalshe` |
| разыграть знакомство вслух | урок 8, `z3-znakomstvo-vsluh` |

**Ни одна из этих строк в работе не повторяется.** Двенадцать действий работы —
все двенадцать новые, и каждое названо в разборе своего вопроса.

### 9.3. Люди работы — свои

Young Learners, **с. 81, графа Names, колонка Pre A1 Starters** (открыла сама,
второй разбор в `materials/.md/`, строки 3605–3615): Alex, Alice, Ann, Anna,
Ben, Bill, Dan, Eva, Grace, Hugo, Jill, Kim, Lucy, Mark, Matt, May, Nick, Pat,
Sam, Sue, Tom.

В уроках модуля заняты Kim, Dana, Alim, Sue, Nurlan, Sam, Bill, Nick, Pat, Tom,
May. **Работа берёт Ben, Grace, Lucy и Matt** — четыре имени, ни одно в уроках
не звучит.

**Пола ни у кого не спрашиваем.** Там, где ученику нужен he или she, пол назван
прямо в условии русским словом: «твой друг Matt», «твоя подруга Grace». Гадать
по имени ученик не должен — правило нажито на работе модуля 3.

### 9.4. Словарь: каждое английское слово работы дано карточкой РАНЬШЕ работы

Работа стоит после восьмого урока, значит доступны все карточки модулей 1 и 2.
Проверено перебором `term:` по коду 7 сентября 2026, а не по памяти:

- **модуль 1:** good, evening, your, name, please, sorry, spell, thank you, no,
  cat, bed, hotel, taxi, book, card, pen, box, yes, my, hello, letter, word,
  tea, sea, bread, black, ticket, night, light, out, mouth, paper, surname;
- **модуль 2:** hi, good morning, good afternoon, good evening, I, am, he, she,
  friend, you, fine, goodbye, bye, good night.

**Слов сверх этих двух списков в работе нет ни одного.** Отдельно названо, чего
работа НЕ берёт, хотя соблазн был: here (карточка в модуле 3, то есть ПОЗЖЕ),
his и her (модуль 5), this (модуль 7). Правило от 2 сентября: у слова
спрашивают не «есть ли карточка», а «есть ли карточка раньше этого модуля», и
порядок берут из `index.ts`.

### 9.5. Состав: двенадцать вопросов

| № | id | Итог | Вид | Что ученик делает | Ступенька |
|---|---|---|---|---|---|
| 1 | `q-vremya-po-privetstviyu` | 1 | `choice` со звуком | называет время суток по услышанному приветствию | узнать |
| 2 | `q-chem-otvetit-utrom` | 2 | `hottext` | отмечает ВСЕ строки, годные в ответ | узнать |
| 3 | `q-sobrat-my-name-is` | 3 | `order` | собирает строку о себе из отдельных слов | произвести |
| 4 | `q-zaglavnaya-i-i-imya` | 3 | `short`, `exact` | восстанавливает заглавные буквы | произвести |
| 5 | `q-vmesto-imeni-she` | 4 | `gap` | ставит слово вместо имени во второй строке | произвести |
| 6 | `q-o-sebe-i-o-nyom` | 4 | `short` | переписывает строку от другого лица | произвести |
| 7 | `q-dopisat-vstrechnyy` | 5 | `gap` | дописывает встречный вопрос к готовому ответу | произвести |
| 8 | `q-vstrecha-i-proshchanie` | 6 | `short` | пишет пару строк: встреча и прощание | произвести |
| 9 | `q-pochemu-neverno-zapiska` | 6 | `choice` | называет ПРИЧИНУ ошибки | узнать |
| 10 | `q-komu-napisano` | 7 | `short` | находит, кому написано сообщение | произвести |
| 11 | `q-o-kom-she-is-fine` | 7 | `choice` | понимает, кого заменяет she | узнать |
| 12 | `q-otvetnaya-replika-znakomstva` | 8 | `short` | пишет ответную реплику знакомства | произвести |

**Произвести ответ требуют восемь из двенадцати** (3, 4, 5, 6, 7, 8, 10, 12) —
`check:content` просит больше половины.

**Итоги покрыты все восемь:** 1 → q1; 2 → q2; 3 → q3 и q4; 4 → q5 и q6; 5 → q7;
6 → q8 и q9; 7 → q10 и q11; 8 → q12. Строка `outcome` у каждого вопроса
копируется из урока ДОСЛОВНО: сверка идёт точным совпадением, и одна изменённая
буква даёт ошибку «итог не проверяется ни одним вопросом».

**Верный ответ у трёх вопросов с выбором стоит на разных местах:** q1 — третьим,
q9 — первым, q11 — вторым.

**Со звуком один вопрос (q1)**, по написанному тексту — два (q10 и q11, оба по
сообщению, которого в уроках нет).

### 9.6. `ask: 12` — решение с причиной

**Ставится `ask: 12`, то есть показываются все двенадцать.** Так же устроены
работы модулей 3, 6, 7 и 8. `check:content` скажет об этом СВЕДЕНИЯМИ («при
пересдаче ученик увидит те же самые»), и это принято сознательно.

Причина считается, а не берётся на вкус: итогов восемь, и каждый обязан быть
спрошен. При `ask` меньше двенадцати выборка случайна, и ученик может пройти
работу, не получив ни одного вопроса о прощаниях или о переписке. Покрытие
итогов дороже разнообразия при пересдаче: пересдача бывает у части учеников, а
покрытие нужно всем.

**По числу показываемых вопросов ничего не меняется:** поля `ask` у модуля не
было вовсе, и показывались все восемь заглушек. Меняется содержание.

### 9.7. Сами вопросы

Английские строки, ответы, `accept`, звук и разбивка вариантов — мои и меняться
не должны. **Русский текст условия, подсказки и разбора пишет редактор**; ниже
у каждого вопроса названо, что этот текст ОБЯЗАН утверждать, и дана моя
черновая формулировка, чтобы было от чего отталкиваться.

### q1. Время суток по услышанному приветствию

```
id:      q-vremya-po-privetstviyu
kind:    choice
outcome: узнавать на слух, каким приветствием поздоровались
zvuk:    Good evening, Grace. — Hello, Ben.
options: утром | днём | вечером ✓
```

**Условие обязано:** велеть послушать разговор и спросить, в какое время суток
он идёт. Ничего больше: ни «слушай первую реплику», ни перечня приветствий.

**Разбор обязан:** назвать, что время суток называет ТОЛЬКО первая реплика
(Good evening), а ответное Hello годится в любое время дня и о времени не
говорит ничего.

**Черновик условия:** «Послушай разговор. В какое время суток он идёт?»
**Черновик подсказки:** «Одно из двух приветствий называет время суток, второе
годится в любое время.»

**Почему это не близнец.** В уроке 1 ученик выбирает УСЛЫШАННОЕ ПРИВЕТСТВИЕ
среди трёх английских строк (`i1-chem-otvetili`) и дважды ЗАПИСЫВАЕТ
услышанное приветствие (`i2`, `i3`). Здесь он не выбирает и не записывает
приветствие: он называет по-русски то, что из приветствия следует. Действия
такого в модуле нет ни одного.

**Осторожно, я называю это сама.** Вопрос стоит на итоге 1, но опирается и на
знание из урока 2 — какое приветствие какому времени отвечает. Иначе на этом
итоге получался близнец: любой вопрос вида «запиши, что услышал» повторяет i2
или i3, а любой выбор английской строки повторяет i1. Считаю обмен честным:
узнать приветствие на слух ученик обязан в любом случае, иначе к вариантам не
подступиться.

**Разговор двухголосый:** реплики разделены тире с пробелами, `razgovorLi()`
прочтёт строку разговором. Оба имени работы, оба приветствия — из карточек
модулей 1 и 2.

### q2. Чем можно ответить на утреннее приветствие

```
id:      q-chem-otvetit-utrom
kind:    hottext
outcome: выбирать приветствие по времени дня и отвечать на чужое приветствие
parts:   Good morning, Ben. ✓ · Hi, Ben. ✓ · Good evening, Ben. · Good night, Ben.
```

**Условие обязано:** назвать время суток (утро), назвать, кто кому здоровается
(Ben поздоровался с Lucy строкой Good morning, Lucy.), и потребовать отметить
ВСЕ строки, которыми Lucy может ответить. Слово «все» обязательно: верных
ответов два, и без него ученик отметит один и получит «пока не так».

**Разбор обязан:** сказать, что ответить можно и тем же приветствием, и
коротким Hi; что Good evening называет вечер, а разговор идёт утром; что Good
night — прощание, а Lucy здоровается.

**Черновик условия:** «Утром Ben поздоровался с Lucy: Good morning, Lucy.
Отметь все строки, которыми Lucy может ему ответить.»

**Оба неверных варианта — настоящие ошибки.** Good evening утром: ученик,
выучивший строки, но не время; Good night вместо приветствия: ровно та путаница,
ради которой написан урок 6. Выдуманного варианта здесь нет ни одного.

**Почему два верных, а не один.** Урок 2 говорит прямо: «Ответить можно тем же
приветствием, а можно коротким Hello или Hi». Оставить один верный значило бы
объявить ошибкой то, чему учит собственный урок, — порода «задание наказывает за
верный ответ».

**Почему это не близнец.** Единственный `hottext` урока 2 отмечает СЛОВО внутри
одной строки (`g2-chto-menyaetsya`). Здесь отмечают целые строки, и признак
другой — годится ли строка в ответ.

### q3. Собрать строку о себе из слов

```
id:      q-sobrat-my-name-is
kind:    order
outcome: строить строку о себе двумя способами: I am Alim и My name is Alim
items:   name | Grace. | My | is
собирается: My name is Grace.
```

**Условие обязано:** назвать имя (Grace) и потребовать собрать строку, которой
человек называет себя ВТОРЫМ способом, — то есть строкой со словами My name is,
а не со словом am. Без этой оговорки ученик вправе ждать, что из этих кусков
соберётся что-то другое.

**Разбор обязан:** пройти по местам: сначала My name, потом форма is, имя стоит
последним.

**Черновик условия:** «Grace называет себя вторым способом, не через am. Собери
её строку.»

**Порядок ровно один.** Из четырёх кусков собирается единственная верная
английская строка; `check:content` сверяет собранное, а не перестановку номеров.

**Почему это не близнец.** `order` в модуле есть дважды: собрать ответную
реплику из трёх готовых ФРАЗ (урок 5) и собрать знакомство из трёх РЕПЛИК
(урок 8). Собирать предложение из отдельных СЛОВ ученик в этом модуле не будет
ни разу.

**Строки My name is Grace. в модуле нет:** уроки печатают My name is Alim, Kim,
Nurlan, Sue, Sam, Pat.

### q4. Заглавная I и заглавное имя

```
id:      q-zaglavnaya-i-i-imya
kind:    short
outcome: строить строку о себе двумя способами: I am Alim и My name is Alim
answer:  I am Ben.
exact:   true
```

**Условие обязано:** показать чужую запись `i am ben.` и потребовать записать её
без ошибки. Про заглавные буквы условие НЕ говорит: ученик обязан увидеть их сам.

**Разбор обязан:** назвать ОБЕ заглавные и обе причины: слово I пишем с
заглавной буквы везде, а не только в начале строки (урок «Меня зовут: I am»);
имя человека пишем с заглавной по правилу урока «Заглавная буква» модуля 1.
Ссылка на урок даётся тем именем, которое видит ученик.

**Черновик условия:** «Ученик написал о себе: i am ben. Запиши эту строку без
ошибки.»

**`exact: true` обязателен, и это не придирка.** Обычная сверка регистр
снимает, и дословно переписанная из условия ошибка засчиталась бы верной. В
`lib/content/check.ts` об этом стоит комментарий с пометкой «проверено опытом:
засчитывало».

**Право работы наказывать за строчную букву объявлено самим модулем.**
Комментарий над заглушками в `privetstvie.ts` (строки 2828–2832): итог урока о
заглавной букве не говорит, поэтому задание урока за неё не наказывает, а
работа вправе. Тем же разбором снят `exact` у задания `u4-korotkaya-zapis`.

**Почему это не близнец, хотя «исправь чужую запись» в модуле есть дважды.** В
уроке 4 (`v4`) ученик возвращает пропавший апостроф, в уроке 5 (`w4`) —
пропавшую форму be. Там из строки что-то ВЫПАЛО. Здесь не выпало ничего: все
буквы на месте, меняется только их вид, и проверить это можно единственным
способом — `exact`. Языковой факт третий, и в уроках он не спрашивается ни разу.

### q5. Вместо имени во второй строке

```
id:      q-vmesto-imeni-she
kind:    gap
outcome: строить строку о другом человеке с he is и she is
before:  Grace is my friend.
after:   is fine.
answer:  She
```

**Условие обязано:** назвать Grace ПОДРУГОЙ (по-русски), сказать, что имя во
второй строке не повторяют, и потребовать вписать слово, с которого вторая
строка начнётся.

**Слово «подруга» в условии обязательно.** Пол ученик угадывать не должен:
правило работы модуля 3, и оно тут решает судьбу ответа — при друге верным было
бы He.

**Разбор обязан:** сказать, что Grace женщина и потому о ней дальше говорят she;
что о мужчине на этом месте стояло бы he; и что имя в каждой строке не повторяют.

**Черновик условия:** «Grace — твоя подруга. Имя её уже названо, и во второй
строке его не повторяют. Впиши слово, с которого вторая строка начнётся.»

**Регистр не спрашиваем:** `exact` не ставится, ответ `she` принимается наравне
с `She`. Заглавную букву в начале предложения курс отдельным правилом не даёт, и
наказывать за неё здесь нельзя — это ровно та порода, из-за которой с задания
`u4` сняли `exact`.

**Почему это не близнец.** Обе вставки модуля просят слово ВНУТРИ разобранной
строки: слово приветствия (`g3`) и форму be (`u3`). Здесь ученик ставит слово
вместо ИМЕНИ и выбирает его по человеку, а не по соседнему слову.

**Случай, на котором стоит вопрос, в уроке есть, а задания у него нет:**
`sluchay-he-she-zamenyayut-imya` («Сначала называем человека по имени, а дальше
говорим о нём he или she»). Ни одно из шести заданий урока 4 этого не
отрабатывает — работа закрывает настоящую дыру, а не выдуманную.

### q6. О себе и о нём

```
id:      q-o-sebe-i-o-nyom
kind:    short
outcome: строить строку о другом человеке с he is и she is
answer:  He is Matt.
accept:  He's Matt.
```

**Условие обязано:** назвать Matt ДРУГОМ (по-русски, чтобы пол был назван),
показать его строку о себе `I am Matt.` и потребовать записать, как о нём скажет
другой человек.

**Разбор обязан:** сказать, что имя в обеих строках одно, а меняются два слова:
вместо I стоит he, и вместе с ним меняется форма be — при he это is.

**Черновик условия:** «Твой друг Matt сказал о себе: I am Matt. Запиши, как о
нём скажет другой человек.»

**`accept` с короткой записью обязателен.** Урок 4 учит короткой записи he's, и
ученик, написавший `He's Matt.`, прав. Наказывать его нельзя.

**Почему это не близнец.** В уроке 4 ученик пишет с нуля строку о своей подруге
(`v5`) — здесь он ПЕРЕПИСЫВАЕТ готовую чужую строку от другого лица. Случай
`sluchay-odno-imya-formy-raznye` учит ровно этому и задания не имеет; в уроке он
показан парой I am Sam. / He is Sam., поэтому в работе взято другое имя, и
строки работы в модуле нет.

### q7. Дописать встречный вопрос

```
id:      q-dopisat-vstrechnyy
kind:    gap
outcome: строить ответ на вопрос How are you? и встречный вопрос
before:  I am fine, thank you.
after:   (пусто)
answer:  And you?
```

**Условие обязано:** сказать, что Ben ответил Grace о своих делах, но спросить
её в ответ забыл, и потребовать дописать недостающую часть его реплики.

**Разбор обязан:** назвать, что встречный вопрос стоит в конце реплики, после
ответа о себе, и что слова How are в нём не повторяются — вместо них короткая
строка из двух слов.

**Черновик условия:** «Ben ответил Grace о своих делах, а спросить её в ответ
забыл. Допиши недостающую часть его реплики.»

**Почему ответ короткий, а не вся реплика.** Полная реплика I am fine, thank
you. And you? напечатана в уроке 5 дважды — в таблице `tablica-tri-hoda` и в
разборе `w3-sobrat-repliku`. Вопрос, требующий её целиком, был бы дословной
цитатой урочного материала: правило от 1 сентября 2026 говорит, что работа
списывает не только у заданий, но и у самого материала. Здесь ученик производит
только ту часть, которой в готовой реплике нет.

**Почему это не близнец.** Обе вставки уроков просят слово в СЕРЕДИНЕ строки;
здесь дописывают ХВОСТ реплики. Отметить встречный вопрос в готовой реплике
(`w2`) и собрать реплику из трёх кусков (`w3`) — другие действия: там вопрос уже
напечатан, здесь его нет.

### q8. Встретила и простилась

```
id:      q-vstrecha-i-proshchanie
kind:    short
outcome: выбирать прощание по случаю и отличать Good night от Good evening
answer:  Good morning, Matt. Bye, Matt.
```

**Условие обязано:** назвать время суток (утро), сказать, что Lucy сначала
поздоровалась приветствием, которое называет время суток, а потом простилась
САМЫМ КОРОТКИМ прощанием, и потребовать записать обе строки с именем.

**Оба указания в условии решают судьбу ответа:** без слов «которое называет
время суток» годилось бы и Hello, без слова «самым коротким» — и Goodbye, и Good
night. Верных ответов стало бы четыре, а принимался бы один.

**Разбор обязан:** сказать, что при встрече утром здороваются словами Good
morning, что самое короткое прощание это Bye, и что имя в обеих строках стоит
после запятой.

**Черновик условия:** «Утром Lucy встретила Matt и поздоровалась приветствием,
которое называет время суток. Расставаясь, она простилась с ним самым коротким
прощанием. Запиши обе её строки, и в каждой назови Matt по имени.»

**Почему это не близнец.** Урок 2 просит записать ОДНО приветствие с именем
(`g4`), урок 6 — ОДНО прощание с именем (`x4`). Здесь ученик пишет пару и сам
решает, чем эти две строки различаются; предмет вопроса — не строка, а разница
между встречей и прощанием, то есть ровно итог урока 6.

**Мимо `x3` тоже прошли нарочно.** Задание урока 6 исправляет прощание, сказанное
при встрече; здесь ничего не исправляют — обе строки ученик пишет сам.

### q9. Почему записка кончается неверно

```
id:      q-pochemu-neverno-zapiska
kind:    choice
outcome: выбирать прощание по случаю и отличать Good night от Good evening
options:
  1) Такой строкой здороваются, а не прощаются. ✓
  2) Имя после приветствия писать не нужно.
  3) Вечером пишут Good afternoon.
```

**Условие обязано:** сказать, что Ben писал записку вечером и закончил её
строкой `Good evening, Lucy!`, и спросить, почему так неверно. Спрашивается
ПРИЧИНА, а не исправление.

**Разбор обязан:** сказать, что строкой Good evening здороваются при встрече, а
записку заканчивают прощанием; и разобрать оба неверных варианта — имя после
приветствия ставят всегда, а Good afternoon называет день, а не вечер.

**Черновик условия:** «Ben писал записку вечером и закончил её строкой Good
evening, Lucy! Почему так неверно?»

**Оба отвлекающих — настоящие ошибки.** Второй: ученик, помнящий про запятую, но
решивший, что имя лишнее в записке. Третий: путаница времён суток, ради которой
и написана таблица урока 6. Ни один из трёх вариантов не проверяется вычиткой
по-русски: все три говорят об английской строке, и выбрать наугад нельзя.

**Действия «назвать причину ошибки» в модуле нет ни одного.** Уроки просят найти
ошибку (`v2`) или исправить её (`v4`, `w4`, `x3`) — назвать ПРИЧИНУ не просит
никто, а это единственный способ проверить, понял ученик правило или запомнил
верную строку.

### q10. Кому написано сообщение

```
id:      q-komu-napisano
kind:    short
outcome: понимать по короткому сообщению, кто здоровается и как у него дела
answer:  Matt
```

**Сообщение работы — своё, в уроках его нет.** Оно печатается в условии вопроса:

```
Hello, Matt!
How are you? I am fine.
My friend is Grace. She is fine.
Goodbye!
Lucy
```

**Условие обязано:** велеть прочитать сообщение и спросить, КОМУ оно написано,
потребовав записать имя.

**Разбор обязан:** сказать, что имя того, кому пишут, стоит в первой строке
рядом с приветствием, а имя пишущего — подписью в самом конце.

**Черновик условия:** «Прочитай сообщение. Кому оно написано? Запиши имя.»

**Почему это не близнец, хотя урок 7 спрашивает о том же сообщении.** Во-первых,
сообщение другое: у урока свои два, и ни одна строка работы с ними не совпадает.
Во-вторых, вопрос обратный: урок спрашивает, КТО написал (`y1`), работа — КОМУ.
В-третьих, сменился вид: там выбор из трёх имён, здесь имя надо написать.

**Сообщение построено по образцу уроков, и так и надо:** умение проверяется на
новом тексте, а не на новом устройстве.

### q11. О ком сказано She is fine.

```
id:      q-o-kom-she-is-fine
kind:    choice
outcome: понимать по короткому сообщению, кто здоровается и как у него дела
options: Lucy | Grace ✓ | Matt
```

**Условие обязано:** отослать к тому же сообщению и спросить, о ком в нём сказано
`She is fine.`

**Разбор обязан:** сказать, что перед этой строкой стоит My friend is Grace., и
слово she заменяет только что названное имя; что Lucy — та, кто пишет, а Matt —
тот, кому пишут.

**Черновик условия:** «О ком в этом сообщении сказано She is fine.?»

**Все три варианта однородны** — три имени из того же сообщения, и по-русски
между ними не выбрать. Оба отвлекающих настоящие: ученик, не проследивший, кого
заменяет she, возьмёт либо пишущего, либо того, кому пишут.

**Почему это не близнец.** Урок 7 спрашивает по сообщению, КАК ДЕЛА у названного
человека (`y2`), и ответ там английское слово fine. Здесь спрашивается, О КОМ
строка, и ответ — имя. Действие другое: ученик идёт по цепочке «имя → she», а не
ищет слово.

### q12. Ответная реплика знакомства

```
id:      q-otvetnaya-replika-znakomstva
kind:    short
outcome: собирать знакомство из готовых реплик по порядку
answer:  Good afternoon, Ben. I am Lucy.
accept:  Good afternoon, Ben. I'm Lucy. | Good afternoon, Ben. My name is Lucy.
```

**Условие обязано:** показать первую реплику (`Good afternoon. I am Ben.`) и
велеть Lucy ответить ТЕМ ЖЕ приветствием, назвать Ben по имени и назвать себя.
О делах не спрашивать — иначе ученик добавит How are you? и получит «пока не
так».

**Слова «тем же приветствием» обязательны:** без них Lucy вправе ответить Hello
или Hi, и верных ответов станет много.

**Разбор обязан:** назвать три хода реплики по порядку — приветствие, имя
собеседника, своё имя — и сказать, что назвать себя можно обоими способами,
поэтому оба ответа засчитаны.

**Черновик условия:** «Днём Ben поздоровался и назвал себя: Good afternoon. I am
Ben. Lucy отвечает ему тем же приветствием, называет его по имени и называет
себя. Запиши её реплику.»

**`accept` перечисляет ВСЕ верные записи.** Урок 3 учит двум способам назвать
себя и говорит, что годится любой, а урок 3 же даёт короткую запись I'm. Значит
верных ответов три, и все три обязаны стоять в `accept`: иначе работа наказывает
за правоту. Поле `accept` не видит ни один скрипт — его перечитывают глазами
вместе с условием.

**Почему это не близнец.** Урок 8 даёт три готовые реплики и просит расставить
их по порядку (`z1`) либо выбрать следующую из трёх (`z2`). Здесь готовых реплик
нет вовсе: ученик строит свою из трёх ходов. Это тот же итог, спрошенный с
производящей стороны, и строки `Good afternoon, Ben. I am Lucy.` в модуле нет.

### 9.8. Что при написании работы проверено — и что при этом нашлось в уроках

**Проверено по источникам:**

1. **Имена работы** — Young Learners, с. 81, графа Names, колонка Pre A1
   Starters. Открывала оба разбора: `materials/.text/` даёт список в столбик
   (строки 3891–3910), `materials/.md/` — таблицей с колонками (строки
   3605–3615), и колонки в ней видно. Ben, Grace, Lucy, Matt стоят в колонке
   Pre A1.
2. **Ответ на слух** — A2 Key, с. 29, THE TASK: «The candidates should only
   write ONE word, or a number, or a date, or a time for their answer». Строку
   нашла поиском по PDF, страница подтверждена. В работе со звуком стоит один
   вопрос, и он с выбором из трёх — это Part 1 того же раздела: «five short
   recordings, each with a question and three images».
3. **Словарь** — перебором `term:` по коду курса, а не по замыслу. Список
   приведён в 9.4.

**Что нашлось в уроках попутно — это находки о ЧУЖОМ тексте, и я не правлю их
сама.**

1. **Урок 5, случай `sluchay-vernut-vopros`, читается как запрет.** Стоит:
   «По-английски на этом месте стоит короткая строка из двух слов, и слова How
   are в ней не повторяем». Ученик прочтёт это как «повторять нельзя», а строка
   `I am fine, thank you. How are you?` — безупречный английский, и запрещающей
   графы у неё нет ни одной. Порода известная: графа источника это разрешение, а
   не запрет. **Работа обходит это место стороной:** отвлекающего с How are you?
   в ней нет нигде, потому что он оказался бы верным. Решать, править ли случай,
   методисту уроков, а не мне.
2. **Заглушка `q-privet-vremya-dnya` была близнецом `g3-otvetit-tem-zhe`** — это
   уже записано в коде комментарием, и я подтверждаю: рамка «Good … , имя» в
   работе не используется ни разу.

### 9.9. Чего проверить не удалось

1. **Звук вопроса q1 не слушала.** Запись `Good evening, Grace. — Hello, Ben.`
   новая, файла для неё пока нет. «Файл есть» и «звучит верно» — разные
   утверждения; сборщику озвучить и послушать, что реплики читают два голоса.
2. **`npm run bliznetsy` по работе не гоняла** — в коде стоят заглушки, и
   сверять пока нечего. После сборки прогнать обязательно: у `gap` и `short`
   отчёт молчит по устройству, но условия и варианты он сверяет, и близнеца с
   заданиями уроков поймает.
3. **Как `lib/content/proiznoshenie.ts` прочтёт Grace, Lucy, Matt и Ben** —
   файла не открывала. Имена английские и в списке Pre A1 стоят, но чтение имён
   собственных у нас решает отдельный файл; сборщику проверить.
4. **Ступень отвлекающих по частотности не проверяла** — данных о частотности
   в `materials/` нет вовсе, а `ngsl-*.csv` отвечает на другой вопрос. Все
   английские слова работы взяты из карточек модулей 1 и 2, и этого довольно.

### 9.10. Для сборщика: точные поля, где они не очевидны

Пробелы в `before` и `after` показаны знаком ␣, чтобы их не потерять.

```
q1  options: [ {утром}, {днём}, {вечером ✓} ]
    zvuk: "Good evening, Grace. — Hello, Ben."

q2  parts: [ {"Good morning, Ben.", selectable, correct},
             {" · "},
             {"Hi, Ben.", selectable, correct},
             {" · "},
             {"Good evening, Ben.", selectable},
             {" · "},
             {"Good night, Ben.", selectable} ]

q3  items: ["name", "Grace.", "My", "is"]     answer: [2, 0, 3, 1]
    собранное: "My name is Grace."

q4  answer: "I am Ben."      exact: true

q5  before: "Grace is my friend.␣"   after: "␣is fine."   answer: "She"

q6  answer: "He is Matt."    accept: ["He's Matt."]

q7  before: "I am fine, thank you.␣"   after: ""   answer: "And you?"

q8  answer: "Good morning, Matt. Bye, Matt."

q9  options: [ {…здороваются, а не прощаются ✓}, {…имя не нужно}, {…Good afternoon} ]

q10 answer: "Matt"

q11 options: [ {Lucy}, {Grace ✓}, {Matt} ]

q12 answer: "Good afternoon, Ben. I am Lucy."
    accept: ["Good afternoon, Ben. I'm Lucy.",
             "Good afternoon, Ben. My name is Lucy."]
```

**Апостроф сверка НЕ вырезает** (`lib/content/check.ts`): `Hes` и `He's` для неё
разные строки. Поэтому короткие записи перечислены в `accept` поимённо, а не
подразумеваются.

**Точка в конце ответа не важна, точка в середине важна.** Приведение снимает
только последний знак `.!?`, поэтому у ответов из двух строк (q8, q12) точка
между ними обязательна и ученик её ставит. Так же устроены ответы работ модулей
3 и 4.

### 9.11. Сверка с работами СОСЕДНИХ модулей — проверка, которую я сперва не сделала

Правило проекта «проверка по курсу идёт первой» я исполнила по урокам своего
модуля и не исполнила по РАБОТАМ соседей. Спохватилась на модуле 5, где мой же
вопрос со звуком оказался повторением вопроса работы модуля 4. Здесь сверка
сделана: выписаны все вопросы работ модулей 1, 3, 4, 6, 7 и 8 (по коду,
`quiz.questions`) и сличены с двенадцатью моими.

**Совпадений, требующих правки, не нашлось.** Три места сличены отдельно, и все
три оставлены:

1. **q3 (`order`, собрать предложение из слов) и работа модуля 3**, где такой же
   `order` собирает `Ben is in an airport.` Действие одно, а предмет разный:
   там артикль перед словом на гласный звук, здесь строка о себе со словами My
   name is. Ни одного общего слова, кроме `is`. Считаю допустимым: вид задания
   сам по себе не близнец, близнец — повторённое ДЕЙСТВИЕ НА ТОМ ЖЕ ПРЕДМЕТЕ.
2. **q5 (`gap`, вписать слово вместо имени) и работа модуля 6**, где `gap`
   просит дописать `We`. Модуль 6 идёт ПОСЛЕ, предмет там перечисление и «мы», а
   не замена имени; строка и слово другие.
3. **q4 (заглавные буквы) и работа модуля 1**, где ученик записывает `Astana` с
   заглавной буквы со слуха. Там заглавную ставят В СВОЁМ ответе, здесь
   исправляют ЧУЖУЮ строчную. Модуль 1 при этом даёт правило, на которое q4
   опирается, — это опора, а не повтор.

**Имена работы совпадают с именами работы модуля 1** (там в последнем вопросе
список `Dan Green, Grace Baker, Mark Green, Matt Baker, Sam Baker, Sue Green`).
Это не совпадение вопросов: имена берутся из одного списка Pre A1 на весь курс,
их там двадцать, и разводить их между модулями смысла нет.
