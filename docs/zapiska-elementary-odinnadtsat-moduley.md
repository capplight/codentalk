# Ступень Elementary: одиннадцать модулей. Записка от 21 августа 2026

Продолжение [zapiska-elementary-nachalo.md](zapiska-elementary-nachalo.md), где
было два модуля и открытые вопросы к владельцу. Все вопросы той записки закрыты,
написаны ещё девять модулей, и главное — сменился порядок работы.

---

## 1. Что написано

**Одиннадцать модулей ступени Elementary**, все прошли скрипты и обоих
проверяющих:

| № | Модуль | О чём | Карточек / новых | На сайте |
|---|---|---|---|---|
| 1 | Это было | `was`, `were`, отрицание, вопрос, обстоятельства | 59 / 24 | да |
| 2 | Вчера я работал | Past Simple, окончание `-ed` и его чтение | 53 / 19 | да |
| 3 | Неправильные глаголы | первый десяток особых форм | 40 / 20 | да |
| 4 | Я не делал | отрицание с `didn't`, возврат к словарной форме | 30 / 28 | да |
| 5 | Спросить о прошлом | вопрос с `did`, короткий ответ, `did` или `was` | 38 / 38 | да |
| 6 | Что происходило | Past Continuous, окончание `-ing`, наречие в середине | 38 / 38 | да |
| 7 | Пока и когда | два действия разом, `while` и `when`, запятая | 39 / 38 | да |
| 8 | Я собираюсь | `be going to`: утверждение, вопрос, «каким это будет» | 39 / 35 | да |
| 9 | Я сделаю | `will`: обещание, отказ, вопрос, просьба, чужое согласие | 34 / 30 | да |
| 10 | Уже договорились | Present Continuous о будущем; договорённость и намерение | 31 / 26 | да |
| 11 | По расписанию | Present Simple о будущем; `when`; `I hope` | 33 / 17 | **нет** |

Первые десять выложены на боевой сайт и проверены запросом: уроки и работы
отвечают 307 — переадресацией на вход, как и положено содержанию за замком.
Контрольный запрос к несуществующему адресу даёт 404, значит 307 действительно
означает «доехало». Модуль 11 написан и озвучен, но не выложен.

Если понадобится выложить не все модули, а часть, — из списка `modules` в
`courses/english-elementary/index.ts` временно убирают лишние: выкладывается
рабочее дерево, а не коммит. И работы едут в базу отдельно, командой
`npm run db:seed:courses`: уроки идут с кодом, банк вопросов живёт в базе.

Дальше по программе — модуль 12 «Давай я», `shall I` и `shall we`.

---


## 2. Решения владельца, принятые за эти дни

1. **Слов в уроках должно быть больше, и они должны быть новыми.** Норма
   ступени: не меньше 30 карточек на модуль, из них не меньше 15 таких, каких на
   прошлой ступени не было. Считает `npm run kontrol`.
2. **Число модулей потолком не ограничено.** Двадцать пять и тридцать один — это
   то, что вышло, а не мерка.
3. **Круг проверяющих один.** Общий проход по ступени — когда она дописана.
   Поводом были четыре круга подряд по двум модулям: каждый следующий находил всё
   более мелкое, а стоил столько же.
4. **Чтение `ate` — /eɪt/.** Слово `abroad` остаётся по помете Oxford, хотя
   словник экзамена его не содержит.
5. **Норму словаря не форсим — уточнение от 21 августа.** Дословно: «не надо
   форсить, можно по ходу добавлять чуть больше слов». Обязательна именно норма
   (30 карточек, 15 новых), а столбец «Слов» в программе — прикидка, и добивать
   модуль до неё карточками ради счёта не надо. Повод: модуль 8, где программа
   обещала пятьдесят слов, а вышло тридцать девять.

**Решения, принятые мной по разбору источников** (владелец может отменить):

- **Строки программы для модулей 8, 9, 11 и 12 исправлены.** Программа обещала
  предсказания в модулях 8 и 9, а источник ставит их на B1 в обоих случаях. И
  «`will` при готовности помочь» она отдавала модулю 12, тогда как источник
  держит готовность и предложение помощи в одной графе одного уровня: значит
  разводить их по модулям — решение, а не выписка. Основания записаны в самой
  программе.
- **Глагол «отправить почтой» — `post`, а не `mail`** (словник даёт `mail`
  только существительным, плюс британская норма курса).
- **Глагол `work` о заведении заменён на `open`**: по-английски «the pool
  doesn't work» читается как «сломан», а не «закрыт».

---

## 3. Инструменты, которых не было

- **`npm run slovo`** — справка о слове: ступень по Oxford, наличие в словнике
  A2 Key, запись из `ipa-en-uk`, не дано ли уже и в каком модуле. `--zapas 40`
  показывает слова ступени, до которых курс ещё не дошёл (их 736).
- **`npm run sosedi`** — смотрит `git diff` и называет блоки, где тронуто одно
  поле, а связанное осталось прежним. Прямой ответ на вопрос владельца «что
  сделать, чтобы одна ошибка не повторялась».
- **Три проверки в `check:content`**: счёт строк в разборе примера, покрытие
  примера звуком, повтор подписей внутри модуля.
- **Проверка новизны словаря в `kontrol`** — двумя источниками сразу.
- **Заслон против придуманных ярлыков в `yazyk`** — списком.
- **Оживлённая проверка родового прошедшего в `yazyk`** — по окончанию, а не по
  списку глаголов.

---

## 4. Чему научились — три разбора

### Правка ломает соседа

Почти все дорогие промахи одного вида: правлю одно место, связанное рядом молча
устаревает. Дописал строки в девять примеров — разборы под ними стали лгать.
Добавил 47 слов — опоры описывают прежний словарь. Причина устройства, а не
внимательности: урок это граф, а правится он как текст. Ловит `npm run sosedi`,
записано в CLAUDE.md, раздел «Правка ломает соседа».

### Проверка, которая молчит, хуже отсутствующей

Три раза за эти дни проверка показывала ноль там, где ошибки были:

- разбор словника A2 Key давал **ноль слов из 1637**, потому что `tekst()`
  схлопывает переносы строк;
- `resheno.ts` гасит замечание по вхождению строки, и слово `like` в перечне
  погасило целый вопрос;
- проверка родового прошедшего искала **по списку глаголов**, а модуль был как
  раз о прошедшем времени: семь форм прошли мимо.

Отсюда правило, которое теперь исполняется: у каждой новой проверки есть сторож
против собственного молчания, и испытывают её нарочно испорченным содержанием.

### Свой ярлык самопроверкой не ловится

«Признак» вместо прилагательного, «особая форма» и «особые глаголы» вместо
неправильных — трижды одна ошибка. Ярлык кажется понятнее термина именно тому,
кто его придумал. Лечится только списком: `YARLYKI` в `scripts/yazyk.mts`.

**И четвёртый раз, в модуле 5.** «Свойство» вместо того, каким что-то было:
`wet`, `scared`, `long` говорят о состоянии, а не о свойстве. Список ярлыков
этого не поймал — слова в нём не было. Но нашлось и худшее: одна и та же вещь
звалась в одном модуле **тремя** именами. Подпись таблицы говорила «о том,
каким было», объяснение под ней — «о свойстве», урок 8 — «оценка». Ученик
читает три слова и ищет три разные вещи.

Отсюда правило шире прежнего: **у главной вещи модуля имя одно, и оно
называется в шапке файла отдельной строкой.** Тогда следующая правка сверяется
с ней, а не с памятью. В модуле 5 такая строка есть — раздел «ИМЯ ДЛЯ ГЛАВНОЙ
ВЕЩИ».

### Модуль объявил ошибкой верный английский — разбор модуля 7

Самая дорогая находка за день, и она же самая поучительная. Модуль 7 ввёл
правило «после `when` стоит только законченное действие» и трижды потребовал
«исправить» безупречную фразу.

Опровергла его **та самая графа, на которой модуль стоял**: CLAUSES
subordinated, A2, «FORM/USE: TIME», пример источника — «I feel comfortable
**when I am wearing** these clothes». Я прочёл цитату, вписал её в опоры и сделал
из неё вывод, противоположный её же примеру.

Отсюда правило: **выписав цитату, прочитай её пример и проверь, не противоречит
ли он тому, что ты собрался запретить.** Пример в источнике весит больше, чем
описание: описание общее, а пример показывает, что засчитано.

### Чего скрипты не видят вовсе — четвёртый разбор, модуль 5

Скрипты дали ноль ошибок, а проверяющие нашли двадцать пять мест. Три породы,
которых машина не увидит никогда:

1. **Подсказка ведёт к неверному ответу.** Ответ `chef`, а подсказка говорит
   «последнее слово записи после слова was» — последнее слово там `ill`. Читают
   подсказку как раз те, кто не уверен.
2. **Условие в третьем лице, а принимается второе.** «Спроси, ездил ли твой
   сослуживец…» — и `Did he go camping?` отвергается, хотя буквально отвечает
   на условие.
3. **Образец учит тому, чего в модуле не было.** `Yes, I did go to the
   stadium` — усилительное `did`, которое English Grammar Profile ставит на C1,
   а соседняя работа объявляет похожую запись негодной.

Все три видны только тому, кто проходит урок, — и все три того же рода, что
три ошибки, которые владелец нашёл сам 19 августа.

---

## 4а. Главный урок этих дней: графа источника — разрешение, а не запрет

За два дня я **трижды** придумал правило, которого у источника нет, и трижды
курс начинал объявлять ошибкой безупречный английский. Каждый раз находил
методист, и каждый раз опровергала не его правота, а **та же графа или тот же
файл**:

| Модуль | Что я объявил | Чем это опровергнуто |
|---|---|---|
| 7 | «после `when` стоит только законченное действие» | пример той же графы A2: «I feel comfortable **when I am wearing** these clothes» |
| 10 | «есть время в строке — значит договорённость» | примеры графы `be going to`: «I'm going to wear them **on holiday**» (A2), «I think **this Saturday** is going to be a nice day» (A2). И наш же модуль 8: три строки из четырёх с названным днём |
| 11 | «после `hope` не ставят `will`» | три работы учеников из того же файла, все сданы: «I hope you **will** be there» (A1), «I hope you **will** be fine very soon» (A1), «I hope it **will** not be boring!» (A2) |

**Причина у всех трёх одна.** Графа источника начинается словом «Can»: «Can use
the present simple with 'I hope'…». Это разрешение — «так можно», — и ничего
больше. Вывести из него «а иначе нельзя» невозможно, но соблазн велик: правило
с запретом легче написать и легче проверить заданием.

**Признак опасного места.** Как только в тексте урока появляется «только», «не
бывает», «нельзя» — надо остановиться и спросить, какая графа это ЗАПРЕЩАЕТ.
Если такой графы нет, а есть лишь графа, разрешающая другое, запрета быть не
должно. Учить надо своему случаю, а не отрицанию чужого.

Правило записано в CLAUDE.md, раздел «Как править содержание», пункт 1.

---

## 4б. Две породы ошибок, которые повторились в модулях 8–11

**Правило дано не по тому основанию.** Модуль 11 объяснил окончание `-s` как
правило ЧИСЛА («когда речь об одной вещи»), а оно правило ЛИЦА: по такой
формулировке выходит `I arrives`. Курс на прошлой ступени говорит верно, а на
этой `-s` не объясняется больше нигде — модуль 11 был единственным местом и
давал неверное правило. Лечится тем же, чем и всё в этом курсе: парадигма
таблицей, по лицу в строке.

**Модуль учит не тому, что обещает.** В первой редакции модуля 11 ни одна
английская строка не несла признака будущего — ни `tomorrow`, ни `on Monday`.
Без него `The train leaves at six` попадает в графу **A1** «привычки и общие
факты»: ученик тренировал прошлую ступень, а обещана ему нынешняя. Сам пример
графы A2 несёт контекст в скобках — «[Talking about a film] It begins…».
Проверяется это счётом: поискать в модуле слова, которые делают его тем, чем он
назван.

---

## 4в. Что за эти дни ушло в скрипты

Правило храповика сработало трижды.

- **Карточка, не встречающаяся нигде, кроме себя.** Ошибку нашли руками дважды
  (модуль 6 — восемь карточек, модуль 8 — четыре), и она ушла в
  `check:content`. Первый прогон по всему курсу дал **86 мест в четырнадцати
  модулях**; они ждут общего прохода. Испытана нарочно испорченным содержанием.
  В модулях 9, 10 и 11 она сразу поймала пять, четыре и пять мёртвых карточек
  соответственно — то есть в каждом свежем модуле.
- **Выпадение конечного `-e` перед окончанием.** Проверка объявила мёртвой
  карточку `telephone`, работавшую как `telephoning`. Ошибка была бы
  систематической: `come → coming`, `leave → leaving`, `take → taking`.
- **Три проверки из находок по модулю 5** (лживая подсказка о месте ответа,
  условие в третьем лице при ответе на `you`, усилительное `did`) — описаны в
  разделе 3.

---

## 5. Долги, записанные явно

- **Ступень Beginner зовёт неправильные глаголы «особыми»** в двух выложенных
  модулях. Записано в `courses/resheno.ts` с причиной: это работа общего прохода.
- **Шесть повторов подписей внутри модулей Beginner** — нашла новая проверка,
  ждут того же прохода.
- **Токен Vercel лежит в `.env.local`** переменной `VERCEL`. После работы его
  стоит отозвать. Строка с ним начинается с пробела и взята в кавычки — как её
  доставать, записано в CLAUDE.md, раздел о выкладке.
- **Общий проход по ступени** — когда она дописана. К нему копятся долги выше
  и мелкое из разборов модулей 6 и 7, которое проверяющие назвали вопросами:
  восемь карточек модуля 6, не встречающихся нигде, кроме себя, и порядок
  подачи слов внутри уроков.
- **Слово `late` употреблено в задании модуля 1, а карточки ему курс не давал
  нигде.** Нашла проверка `chain` после того, как слово попало в словарик урока
  чтения модуля 5. Это дыра модуля 1, а не модуля 5, и её место — общий проход.
- **86 карточек по всему курсу не встречаются нигде, кроме себя.** Нашла новая
  проверка `check:content` при первом же прогоне. Модули 8–11 от этого чисты,
  остальные ждут.
- **Термин «лицо» на ступени Elementary не объясняется ни разу.** В модуле 1 он
  стоит только подписью таблицы «Формы по лицам». Нашёл редактор при разборе
  модуля 10; в модуле 11 строка добавлена, модуль 1 ждёт прохода.
- **Разводящая строка для `tablet`** (планшет, а не таблетка) при первой встрече
  в модуле 4 так и не написана — см. раздел 6, вопрос 1.

---

## 6. Вопросы к владельцу — что с ними стало

Все три разобраны 21 августа. Два закрыл источник, третий — владелец.

**Правило, которое их закрыло, стоит запомнить отдельно.** Вступление к словнику
A2 Key, с. 2, раздел «Exemplification»: пример при слове дан **только там, где
значение надо сузить**. «Example phrases and sentences … are given only where
words with different meanings need to be constrained. For example, bank is
followed by "I changed my money in a bank" … candidates are not expected to know
other meanings, such as "a river bank".» Значит есть пример — других значений от
ученика не ждут; нет примера — значение не сужали.

1. **`tablet` и `medicine` в соседних уроках модуля 4.** Словник даёт `tablet
   (n)` с примером «You can take notes using your tablet» и ставит слово в
   тематический перечень Communication and Technology; в перечне Health,
   Medicine and Exercise стоят `chemist`, `medicine`, `toothache`, а `tablet` и
   `pill` там не значатся. **Значение «планшет» верно, «таблетка» в требования
   A2 не входит.** Остаётся не вопрос словника, а наша забота: по-русски
   «таблетка» — это лекарство, и соседство двух уроков даёт ученику готовый
   неверный вывод. Разводящая строка при первой встрече не написана — долг.
2. **`work` в двух значениях в одной строке письма модуля 4** — закрыт. `work
   (n & v)` стоит в словнике **без примера**, то есть значение не сужали; Oxford
   ставит `work v., n. A1` целиком. Образец рядом: `lift (n)` дан сразу двумя
   примерами — про подъёмник и про «подвезти», — значит два далёких значения у
   одного слова на этой ступени источник держит спокойно. Править нечего.
3. **Итог урока слушания модуля 4** (урок 8, «Слушаем рассказ о неудачном дне»).
   Я насчитал, что расслышать `didn't` требует одно задание из пяти: в остальных
   ответ лежит во второй половине фразы. **Владелец открыл урок и решил, что всё
   в порядке.** Вопрос закрыт, урок не меняется.

---

## 7. Долг всей ступени, найденный при разборе модуля 11

**Времена не называются английскими именами ни в одном модуле Elementary.**

CLAUDE.md требует прямо: «время называется один раз, при первой встрече,
английским именем и без объяснений, зачем оно нужно» — «Это время называется
Present Simple». Методист проверил поиском по всему учебному тексту
`courses/english-elementary/*.ts`: слов `Present Simple`, `Past Simple`,
`Past Continuous`, `Present Continuous` там нет **ни одного**.

Ученик, пришедший сразу на Elementary — а по решению владельца от 16 августа он
вправе так прийти, — не получает этих имён нигде. Значит он не узнает своё время
ни в учебнике, ни в чужом курсе, а ровно ради этого правило и написано.

Задеты все одиннадцать модулей: 2 и 3 (Past Simple), 6 и 7 (Past Continuous),
10 (Present Continuous), 11 (Present Simple). Правка на модуль — одна строка при
первой встрече, но это правка выложенного содержания, и потому её место —
общий проход по ступени.

**Ещё три слова закрыты модулем 11.** `hour`, `full` и `leave` работали в
заданиях модулей 2, 4, 5, 8 и 10 без карточки; модуль 11 их наконец вводит.
Нашла проверка `chain`.
