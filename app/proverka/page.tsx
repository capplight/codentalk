import ProverkaForm from "@/components/ProverkaForm";

export const metadata = {
  title: "Проверка сертификата",
  description:
    "Проверка подлинности сертификата CodeNTalk по номеру: имя, курс, дата и итоговый балл.",
};

/**
 * Проверка сертификата по номеру.
 *
 * Нужна тому, кому дали бумагу, а не ссылку: со ссылкой человек попадает прямо
 * на страницу сертификата, а с распечатки номер приходится вводить руками.
 *
 * Страница открыта всем и намеренно ничего не спрашивает: заставлять
 * работодателя заводить аккаунт ради проверки чужого документа — верный способ
 * добиться, чтобы он не проверял вовсе.
 */
export default function ProverkaPage() {
  return (
    <main className="wrap">
      <ProverkaForm />
    </main>
  );
}
