import type { Course } from "@/lib/content/types";
import ktoEto from "./kto-eto";

/**
 * ОБРАЗЕЦ НОВОГО BEGINNER — замысел в `docs/beginner-2/zamysel.md`.
 *
 * НАРОЧНО НЕ ПОДКЛЮЧЁН к `courses/index.ts`. Значит:
 * · в каталоге его нет и ученик до него не доберётся;
 * · проверки содержания (`check:content`, `kontrol`, `bliznetsy`) его не считают;
 * · типы всё равно сверяются — `npm run build` проходит по всем файлам проекта.
 *
 * КОГДА ОН СТАНЕТ НАСТОЯЩИМ, ЕГО НАДО ПОДКЛЮЧИТЬ ПЕРВЫМ ЖЕ ДЕЛОМ. Иначе
 * повторится то, что уже было с экзаменом Elementary: он сутки пролежал
 * ненаписанным в `index.ts`, все проверки экзамена молчали, потому что обёрнуты
 * в `if (course.exam)`, — и при включении сразу нашлось, что верный ответ стоял
 * на одном месте в одиннадцати вопросах из двенадцати. Содержание, до которого
 * нет дороги, выглядит в отчётах работающим.
 *
 * ЧТО ЗДЕСЬ ПОКА ЕСТЬ: один модуль из двадцати с лишним. Ступень, порядок
 * модулей и программа берутся у нынешнего Beginner — их переделка не начата.
 */
const course: Course = {
  slug: "english-beginner-2",
  track: "english",
  title: "Английский · Beginner (образец нового формата)",
  level: "A1",
  tagline:
    "Образец переделанного формата: объяснение дробится на названные случаи, " +
    "у каждого правило одной строкой и два примера с переводом.",
  access: "free",
  /*
   * Пошаговый вид: один экран — одна мысль. Прежние курсы этого поля не имеют
   * и показываются страницей, как раньше, — оба вида живут рядом, пока идёт
   * переделка.
   */
  format: "shagi",
  modules: [ktoEto],
};

export default course;
