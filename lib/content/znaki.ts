/**
 * Значки, которые называет не содержание урока, а сам вид урока.
 *
 * ЗАЧЕМ ОТДЕЛЬНЫЙ ФАЙЛ. Значки уроков стоят в содержании (поле `znak`), и
 * `npm run znachki` забирает именно их, обходя курсы. Но у пошагового вида есть
 * свои четыре: значок вступления, значок «урок пройден» и два лица в разговоре.
 * Их не называет ни один урок — значит скрипт их не видел и не забирал.
 *
 * ЧЕМ ЭТО КОНЧИЛОСЬ. Три из четырёх случайно совпали со значками словарных
 * слов и потому лежали на месте. Четвёртый — компас у вступления — не совпал ни
 * с чем, и владелец увидел на первом же экране сломанную картинку. Совпадение
 * трёх из четырёх и спрятало дыру: выглядело так, будто всё работает.
 *
 * Поэтому список один и лежит здесь: его читает и страница, и скрипт забора.
 * Добавил значок в вид урока — он забирается сам.
 */
export const ZNACHKI_VIDA = {
  /** Вступление урока: куда идём. */
  nachalo: "1f9ed",
  /** Последний экран: урок пройден. */
  konec: "2705",
  /** Лица в разговоре — они чередуются по репликам. */
  uchenik: "1f9d1-200d-1f393",
  uchitel: "1f9d1-200d-1f3eb",
  /* Замок у проверочной работы, которая ещё не открылась. Значок, а не буква:
     рядом с ним стоят значки уроков, и системный смайлик выбивался бы стилем. */
  zamok: "1f512",
} as const;
